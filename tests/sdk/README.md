# SDK Tests

`tests/sdk/python/` contains repo-backed Python SDK tests.

Node SDK tests live alongside the package in `sdk/node-sdk/tests/`.

Only keep executable tests here. Speculative placeholders and planned directory layouts do not belong in the suite.
