# Python SDK Tests

Keep repo-backed Python SDK tests in this directory.

Current coverage is intentionally small and concrete. Add new files only when they run in CI or protect a real regression.

```bash
pytest tests/sdk/python
```
