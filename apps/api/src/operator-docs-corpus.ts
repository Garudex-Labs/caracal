// Copyright (C) 2026 Garudex Labs.  All Rights Reserved.
// Caracal, a product of Garudex Labs
//
// Generated documentation corpus the Operator retrieves over; regenerate with scripts/build-docs-corpus.mjs.

import type { DocPage } from './operator-docs.js'

export const DOCS_CORPUS: DocPage[] = [{"id":"/get-started","title":"Overview","url":"https://docs.caracal.run/get-started/","concepts":"","body":"Caracal gives AI agents and automated workflows short-lived, policy-approved authority instead of long-lived provider credentials in code or environment files. An agent asks for scoped authority when it acts, policy decides before the action reaches a resource, the Gateway or a verified service enforces the result, and audit records what happened.\n\nUse this section when you want the fastest path from a clean machine to a real protected call.\n\n## Should You Use Caracal?\n\nCaracal fits when agents, services, or automation need to call APIs, tools, SaaS providers, or data systems without directly holding the upstream credential.\n\n| Question                             | Use Caracal when                                                                |\n| ------------------------------------ | ------------------------------------------------------------------------------- |\n| Do agents need credentials?          | Agents need scoped access to tools, APIs, providers, or data.                   |\n| Do you need policy before execution? | Access must be allowed or denied before the protected action happens.           |\n| Do you need revocation?              | Active authority must end centrally without restarting every workload.          |\n| Do you need audit evidence?          | You need to explain which app, run, policy, resource, and result were involved. |\n\nCaracal is not an LLM framework, prompt router, agent scheduler, static config store, general API gateway, or identity provider. If you only have human users behind a normal login, start with an IdP instead.\n\n:::note[Managed option]\nThis section covers the self-hosted open-source edition. If you want managed multi-tenancy, hosted management, SSO, or a supported enterprise deployment, see [Compare Editions](/enterprise/).\n:::\n\n## First Success Path\n\nThe onboarding path keeps runtime lifecycle in the `caracal` CLI and product management in the web console:\n\n1. Install `caracal`.\n2. Start the local stack with `caracal up`.\n3. Sign in to the web console at [http://localhost:3001](http://localhost:3001) and complete onboarding to create your first zone.\n4. Use guided setup to create an agent app, resource, and active policy.\n5. Create a workload with launch bindings on the console's Launcher page and export the workload identity.\n6. Run a workload with `caracal run --` or an SDK.\n7. Call the protected resource through the Gateway.\n8. Inspect **Audit** in the web console.\n\n## Core Terms\n\n| Term        | Meaning                                                                                                                                                     |\n| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Application | The registered client identity your software runs as when it asks Caracal for authority.                                                                    |\n| Session     | One governed execution under that identity: Caracal binds authority around whatever runs - an AI agent step, a job, a tool call - and records audit for it. |\n| Resource    | The protected API, tool, provider, service, or data target.                                                                                                 |\n| Policy      | The rules that allow or deny requested resource scopes.                                                                                                     |\n| Mandate     | The short-lived signed token Caracal issues after policy allows access.                                                                                     |\n| Gateway     | The default boundary that verifies mandates, routes requests, brokers provider credentials when needed, and records action-result audit.                    |\n| Audit       | The decision and result trail for authorization, execution, revocation, and diagnostics.                                                                    |\n\nYou can finish Get Started with only these terms. Use [Concepts](/concepts/) when you need the deeper authority, delegation, revocation, and audit model.\n\n## Choose Your Next Path\n\n| Goal                            | Start here                                                | You will have                                                                           |\n| ------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------- |\n| Evaluate Caracal locally        | [Install Caracal](./install-caracal/)                     | A verified runtime CLI, packaged local web console, and Docker prerequisite.            |\n| Protect one resource end to end | [First Protected Call](./first-protected-call/)           | A Gateway-routed resource, active policy, runtime configuration, and audit explanation. |\n| Add Caracal to app code         | [Add SDK to Your App](./add-sdk-to-your-app/)             | TypeScript, Python, or Go code that opens a Session and calls the Gateway.              |\n| Fix a blocked first run         | [First-Run Troubleshooting](./first-run-troubleshooting/) | A focused checklist for readiness, profile, STS, Gateway, upstream, and audit issues.   |\n| Learn the full model            | [Caracal Mental Model](/concepts/model-overview/)         | The canonical concept path after first success.                                         |\n| Develop Caracal itself          | [Set Up Locally](/contributing/setup/)                    | A source-tree development stack and contributor workflow.                               |\n\n## Next Step\n\nContinue with [Install Caracal](./install-caracal/)."},{"id":"/get-started/install-caracal","title":"Install Caracal","url":"https://docs.caracal.run/get-started/install-caracal/","concepts":"","body":"Install the released runtime when you want to run Caracal as a product. The `caracal` CLI owns local runtime lifecycle, workload execution, upgrades, and the local web development launcher. The packaged web console is a deployable production service in Compose and Helm; it owns product management: zones, applications, resources, providers, policies, sessions, audit, diagnostics, and Control workflows.\n\n## Install the Runtime CLI\n\nRelease assets include platform-specific `caracal-runtime-*` archives, `manifest.json`, and `SHA256SUMS`. The `install.sh` installer installs the runtime-only CLI from those assets; it does not install source-tree development tooling. You can use the installer or download the archive for your platform from the [Caracal releases](https://github.com/Garudex-Labs/caracal/releases), verify it, and place the `caracal` binary on your `PATH`.\n\nTo install with the release script on Linux or macOS:\n\n```sh\ncurl -fsSL https://raw.githubusercontent.com/Garudex-Labs/caracal/main/install.sh | sh\n```\n\nTo verify a download yourself, or to verify archives and container images fetched directly, follow [Verify a Release](/security/verify-releases/).\n\n### Linux and macOS\n\n```sh\ngh release download vX.Y.Z \\\n  --repo Garudex-Labs/caracal \\\n  --pattern 'caracal-runtime-linux-amd64-*.tar.gz' \\\n  --pattern SHA256SUMS\nsha256sum --check SHA256SUMS   # macOS: shasum -a 256 --check SHA256SUMS\ntar -xzf caracal-runtime-linux-amd64-vX.Y.Z.tar.gz\ninstall -m 0755 caracal ~/.local/bin/caracal\n```\n\nChoose the `linux-arm64`, `darwin-amd64`, or `darwin-arm64` archive when that matches your machine. Ensure the destination directory is on your `PATH`.\n\n### Windows\n\n```powershell\ngh release download vX.Y.Z `\n  --repo Garudex-Labs/caracal `\n  --pattern 'caracal-runtime-windows-amd64-*.zip' `\n  --pattern SHA256SUMS\nGet-FileHash .\\caracal-runtime-windows-amd64-vX.Y.Z.zip -Algorithm SHA256\nExpand-Archive .\\caracal-runtime-windows-amd64-vX.Y.Z.zip -DestinationPath $env:LOCALAPPDATA\\Programs\\caracal\n```\n\nCompare the PowerShell hash with the matching entry in `SHA256SUMS`, then add `%LOCALAPPDATA%\\Programs\\caracal` to the user `Path` and open a new shell.\n\n## Verify Installation\n\n```sh\ncaracal --version\ncaracal status --help\n```\n\nOn Windows PowerShell:\n\n```powershell\ncaracal --version\ncaracal status --help\n```\n\nUse `caracal` for local lifecycle, workload execution, and upgrades. Use the packaged web console at `http://localhost:3001` after `caracal up` for the standard local product-management path.\n\n## Verify Docker\n\nRunning the open-source stack locally requires Docker 25+ with the Compose v2 plugin:\n\n```sh\ndocker compose version\n```\n\nOn Windows PowerShell:\n\n```powershell\ndocker compose version\n```\n\nThe local stack includes PostgreSQL, Redis, and the packaged web console. You do not need to install those databases separately for Get Started.\n\n## Start the Local Stack\n\n```sh\ncaracal up\ncaracal status --ready\n```\n\nOn Windows PowerShell:\n\n```powershell\ncaracal up\ncaracal status --ready\n```\n\nAfter readiness succeeds, open [http://localhost:3001](http://localhost:3001). Compose maps the host address `127.0.0.1:3001` to the web BFF container on port `3002`. Before your first sign-in, run `caracal allowlist add <email>` on the host to permit your email, and configure a console sign-in method as described in [First Protected Call](/get-started/first-protected-call/#sign-in-and-create-your-first-zone).\n\n## Platform Notes\n\n| Platform | Architectures | Notes |\n| --- | --- | --- |\n| Linux | `amd64`, `arm64` | Requires `curl` or `wget`, `tar`, and `sha256sum` or `shasum`. Archive checksums are always verified; installing GitHub CLI `gh` additionally verifies provenance attestations. |\n| macOS | `amd64`, `arm64` | GitHub CLI `gh` enables provenance verification; if Gatekeeper quarantines the binary, remove quarantine from the installed file. |\n| Windows | `amd64` | GitHub CLI `gh` enables provenance verification; open a new shell after updating the user `Path`. |\n\n## Next Step\n\nContinue with [First Protected Call](/get-started/first-protected-call/)."},{"id":"/get-started/first-protected-call","title":"First Protected Call","url":"https://docs.caracal.run/get-started/first-protected-call/","concepts":"","body":"This walkthrough creates the baseline every later tutorial assumes: a running local stack, one zone, one confidential agent app, one protected resource, one active policy, runtime configuration, and one audited Gateway call.\n\nComplete [Install Caracal](/get-started/install-caracal/) first.\n\n## Start Caracal\n\n```sh\ncaracal up\ncaracal status --ready\n```\n\nOn Windows PowerShell:\n\n```powershell\ncaracal up\ncaracal status --ready\n```\n\nCreate product objects only after readiness succeeds. If readiness fails, run:\n\n```sh\ncaracal status --ready --json\n```\n\nThe JSON output shows which service is not ready.\n\n## Start Demo Upstream\n\nStart a local HTTP upstream on the same Docker network as Caracal:\n\n```sh\ndocker run --rm -d --name caracalDemoUpstream --network caracalData nginx:stable-alpine\n```\n\nOn Windows PowerShell:\n\n```powershell\ndocker run --rm -d --name caracalDemoUpstream --network caracalData nginx:stable-alpine\n```\n\nUse `http://caracalDemoUpstream:80` as the resource upstream URL in guided setup. If you already have an HTTP service that the Gateway container can reach, you can use that URL instead.\n\n## Sign In and Create Your First Zone\n\nOpen the packaged web console at [http://localhost:3001](http://localhost:3001). The Compose stack maps that localhost port to the web BFF container and serves the SPA same-origin with the console API.\n\nThe packaged console runs the fail-closed production posture: operator registration is closed by default. Admission is controlled from the runtime host. Allow your email:\n\n```sh\ncaracal allowlist add <your email>\n```\n\nThe allowlist is read live by the console, so the change applies immediately. Entries are exact emails or `@domain` suffixes, and the same command manages access later: `lock`/`unlock` suspend and restore sign-in with the account kept intact, `remove` ends access and erases the account's sign-in records, and `list` shows every entry. See [Control Console Access](/runtime-console/console-access/) for the full lifecycle and the uniform access-denied page.\n\nAn allowlisted email still needs a sign-in method. Add one of the following to `$CARACAL_HOME/caracal.env` (Linux default `~/.local/share/caracal/caracal.env`, macOS `~/Library/Application Support/caracal/caracal.env`, Windows `%LOCALAPPDATA%\\caracal\\caracal.env`), then rerun `caracal up`:\n\n| Sign-in method | Required variables |\n| --- | --- |\n| Google or GitHub | `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` or `GITHUB_CLIENT_ID` + `GITHUB_CLIENT_SECRET`. OAuth callback URL: `http://localhost:3001/api/auth/callback/google` or `.../github`. |\n| Email and password | `CARACAL_PASSWORD_SIGNUP=true`, plus `CARACAL_SMTP_URL` and `CARACAL_SMTP_FROM` so the required verification email can be delivered; the console fails closed without a mail transport. |\n\nFor OAuth, create an OAuth app in the [Google Cloud console](https://console.cloud.google.com/apis/credentials) or [GitHub developer settings](https://github.com/settings/developers), set its callback URL to the value above, and paste the client ID and secret into `caracal.env`. With email/password sign-up, registration sends a verification link over SMTP and the account signs in after the link is confirmed.\n\nSee [Environment Variables](/operations/env-vars/#web-console-bff) for the full sign-in reference. Sign in, then complete the short onboarding flow:\n\n1. **Profile** - your operator name and avatar.\n2. **Zone** - create your first zone. A zone is Caracal's primary trust boundary; it isolates applications, resources, policies, and audit.\n3. **Review** - confirm and finish.\n\nOnboarding lands you in the console for that zone. The browser address follows an `account → org → zone` hierarchy (`/<accountId>/<orgId>/<zoneId>/app/...`); use the zone selector to switch zones, and every zone-scoped page acts on the selected zone.\n\n## Build the Access Chain with Guided Setup\n\nThe console opens **Guided setup**, a short walkthrough that explains each building block and opens its real create form. Work through the steps in order:\n\n| Step | What you create |\n| --- | --- |\n| Application | A confidential agent app for the workload. The client secret is held sealed in the Secret Store - copy it now or reveal it later from the application's detail panel; every reveal is recorded in the zone audit timeline. |\n| Provider | The upstream credential source. Skip this step for an internal HTTP API that needs no upstream credential. |\n| Resource | A stable resource identifier, its scopes, and the upstream URL you chose (for example `http://caracalDemoUpstream:80`), bound to a Gateway application. |\n| Policy | A starter policy. Activate it - the zone denies every request until a policy is active. |\n\nGuided setup ticks each step off from live zone data and opens the same Applications, Providers, Resources, and Policies forms you reach from the console navigation.\n\n## Configure Runtime Access\n\nThe console does not write configuration to your machine, and `caracal run` needs no local config file. The workload carries only its identity; everything else lives in the console:\n\n1. Open the **Launcher** page and create a workload. The workload secret is held sealed in the Secret Store - copy it now or reveal it later from the workload's detail panel; every reveal is recorded in the zone audit timeline.\n2. Add a launch binding to the workload: an environment variable name (for example `EXAMPLE_API_TOKEN`) bound to the resource you created, with the scopes the credential should carry. The page then shows the exact launch commands.\n3. Give the workload its identity:\n\n```sh\nexport CARACAL_WORKLOAD_ID=<workload ID>\nexport CARACAL_WORKLOAD_SECRET=<workload secret>\n```\n\nThese configure the launcher itself; they are not passed through to the child process. Instead of the inline secret, local dev and stable launches can read the owner-only file at `<Caracal config dir>/runtime/<workload_id>/secret`, and cloud deployments can point `CARACAL_WORKLOAD_SECRET_FILE` at a mounted secret; see [Confi"},{"id":"/get-started/add-sdk-to-your-app","title":"Add SDK to Your App","url":"https://docs.caracal.run/get-started/add-sdk-to-your-app/","concepts":"","body":"This page shows the smallest SDK integration in TypeScript, Python, and Go. Each example opens a Session, calls a Gateway-protected resource, and leaves authorization and action-result audit behind.\n\nComplete [First Protected Call](/get-started/first-protected-call/) first so you have runtime configuration, a resource ID, and a protected path.\n\n## Author the Runtime Profile\n\nFirst Protected Call leaves you with a zone ID, an application ID, a client secret file, and a resource. All SDKs read those values from a `caracal.toml` profile or from `CARACAL_*` environment variables. The profile is a file you author with the IDs from guided setup - guided setup does not write it for you:\n\n```toml\nzone_id = \"zone_prod\"\napplication_id = \"app_agent\"\napp_client_secret_file = \"/path/to/caracal-client-secret\"\n\n[[credentials]]\nresource = \"resource://example-api\"\nupstream_prefix = \"http://caracalDemoUpstream:80\"\n```\n\nOn the local stack the SDK defaults cover the STS, Coordinator, and Gateway URLs. The profile must name the owner-only client-secret file explicitly; the SDK never searches local credential directories. The secret comes from application creation and can be revealed from the application's detail panel, with every reveal audited. For cloud or custom deployments add `sts_url`, `coordinator_url`, and `gateway_url`; see [Configuration File](/runtime-console/config-file/) for the full schema.\n\nThe profile identifies the workload, not each agent. `zone_id` and `application_id` name the one confidential application your service runs as; every Session the SDK starts executes under that application identity, so one profile serves all the agents your workload creates.\n\nPoint the examples at the profile and choose which configured resource and path they call:\n\n```sh\nexport CARACAL_CONFIG=/path/to/caracal.toml\nexport CARACAL_RESOURCE_ID=resource://example-api\nexport CARACAL_RESOURCE_PATH=/\n```\n\nOn Windows PowerShell:\n\n```powershell\n$env:CARACAL_CONFIG = \"C:\\path\\to\\caracal.toml\"\n$env:CARACAL_RESOURCE_ID = \"resource://example-api\"\n$env:CARACAL_RESOURCE_PATH = \"/\"\n```\n\n## Install the SDK\n\nInstall the package for your application language.\n\n| Language   | Install command                                           | Runtime requirement |\n| ---------- | --------------------------------------------------------- | ------------------- |\n| TypeScript | `npm install @caracalai/sdk` or `pnpm add @caracalai/sdk` | Node.js 22+         |\n| Python     | `pip install caracalai-sdk`                               | Python 3.12+        |\n| Go         | `go get github.com/garudex-labs/caracal/packages/sdk/go`  | Go 1.26+            |\n\n## TypeScript\n\n```typescript\nimport { Caracal } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\nconst resourceId = process.env.CARACAL_RESOURCE_ID\nconst resourcePath = process.env.CARACAL_RESOURCE_PATH\n\nif (!resourceId) throw new Error('CARACAL_RESOURCE_ID is required')\nif (!resourcePath) throw new Error('CARACAL_RESOURCE_PATH is required')\n\nawait caracal.session(async () => {\n  const ctx = caracal.current()\n  console.log('Session:', ctx?.sessionId)\n\n  const response = await caracal.fetch(resourceId, resourcePath, {\n    method: 'GET',\n  })\n\n  console.log(await response.text())\n})\n```\n\n## Python\n\n```python\nimport asyncio\nimport os\n\nfrom caracalai import Caracal\n\ncaracal = Caracal()\nresource_id = os.environ[\"CARACAL_RESOURCE_ID\"]\nresource_path = os.environ[\"CARACAL_RESOURCE_PATH\"]\n\nasync def main():\n    async with caracal.session() as ctx:\n        print(\"Session:\", ctx.session_id)\n\n        response = await caracal.fetch(\n            resource_id,\n            resource_path,\n            method=\"GET\",\n        )\n        print(response.text)\n\nasyncio.run(main())\n```\n\n## Go\n\n```go\npackage main\n\nimport (\n    \"context\"\n    \"fmt\"\n    \"io\"\n    \"net/http\"\n    \"os\"\n\n    caracal \"github.com/garudex-labs/caracal/packages/sdk/go\"\n)\n\nfunc main() {\n    c, err := caracal.New()\n    if err != nil {\n        panic(err)\n    }\n\n    resourceID := os.Getenv(\"CARACAL_RESOURCE_ID\")\n    resourcePath := os.Getenv(\"CARACAL_RESOURCE_PATH\")\n    if resourceID == \"\" || resourcePath == \"\" {\n        panic(\"CARACAL_RESOURCE_ID and CARACAL_RESOURCE_PATH are required\")\n    }\n\n    err = c.Session(context.Background(), func(ctx context.Context) error {\n        cc, _ := c.Current(ctx)\n        fmt.Println(\"Session:\", cc.SessionID)\n\n        resp, err := c.Fetch(ctx, http.MethodGet, resourceID, resourcePath)\n        if err != nil {\n            return err\n        }\n        defer resp.Body.Close()\n\n        body, _ := io.ReadAll(resp.Body)\n        fmt.Println(string(body))\n        return nil\n    })\n    if err != nil {\n        panic(err)\n    }\n}\n```\n\n## SDK Calls\n\n| Call                                    | Role                                                                                                            |\n| --------------------------------------- | --------------------------------------------------------------------------------------------------------------- |\n| `new Caracal()` / `Caracal()` / `New()` | Loads the runtime profile or environment configuration and prepares token exchange.                             |\n| `session()` / `Session()`               | Creates a Session, binds context for the work, and terminates the session when the block exits.                 |\n| `fetch()` / `Fetch()`                   | Sends one request to a resource through the Gateway with context and authority injected.                        |\n| `gatewayRequest()` / `GatewayRequest()` | Builds the Gateway URL and `X-Caracal-Resource` header for explicit resource routing.                           |\n| `transport()` / `Transport()`           | Injects Caracal trace and baggage context on outbound calls and attaches authorization on Gateway-routed calls. |\n\n`session()` creates a Session under the application from your runtime profile. You reuse that one application for every Session your workload starts; you do not register an application per agent"},{"id":"/get-started/first-run-troubleshooting","title":"First-Run Troubleshooting","url":"https://docs.caracal.run/get-started/first-run-troubleshooting/","concepts":"","body":"Use this page when the Get Started path fails before your first protected call or first SDK integration succeeds. For production incidents and deeper operational diagnosis, use [Troubleshoot by Symptom](/operations/troubleshooting/).\n\n## Readiness Failures\n\nRun:\n\n```sh\ncaracal status --ready --json\n```\n\nOn Windows PowerShell:\n\n```powershell\ncaracal status --ready --json\n```\n\n| Symptom                  | Check                                                                                                              |\n| ------------------------ | ------------------------------------------------------------------------------------------------------------------ |\n| Docker command fails     | Confirm Docker Desktop or Docker Engine is running and `docker compose version` succeeds.                          |\n| A service is not ready   | Wait for the dependency named in the JSON output, then rerun readiness.                                            |\n| Ports are already in use | Stop the conflicting local process or change the runtime port configuration before starting Caracal.               |\n| Stack state looks stale  | Run `caracal down`, then `caracal up`. Use `caracal purge` only when you intentionally want to remove local state. |\n\n## Console Sign-In\n\n| Symptom                                               | Check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |\n| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| Sign-up is rejected or the access-denied page appears | The packaged console closes registration by default and shows one uniform page for every allowlist denial. On the runtime host, run `caracal allowlist list` to inspect entries, then `caracal allowlist add <email>` or `caracal allowlist unlock <email>` - see [Control Console Access](/runtime-console/console-access/). Make sure a sign-in method is configured in `$CARACAL_HOME/caracal.env` - see [Sign In and Create Your First Zone](/get-started/first-protected-call/#sign-in-and-create-your-first-zone). |\n| Google or GitHub buttons are missing                  | Set both the client ID and client secret for the provider, then rerun `caracal up`.                                                                                                                                                                                                                                                                                                                                                                                                                                      |\n| Password sign-in is blocked pending verification      | The packaged console requires a verified email. Confirm `CARACAL_SMTP_URL` and `CARACAL_SMTP_FROM` are set and the verification message was delivered.                                                                                                                                                                                                                                                                                                                                                                   |\n\n## Workload Identity Issues\n\n| Symptom                             | Check                                                                                                                       |\n| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |\n| `workload identity not found`       | Export `CARACAL_WORKLOAD_ID` and provide a workload secret source.                                                          |\n| `no credential bindings configured` | Define launch bindings for the workload on the **Launcher** page in the web console.                                        |\n| Secret file is rejected             | Ensure the secret file exists and is readable only by the current user.                                                     |\n| SDK cannot load configuration       | Set `CARACAL_CONFIG` to your `caracal.toml` path, or pass the profile path using the SDK-supported configuration mechanism. |\n\nLinux and macOS:\n\n```sh\nexport CARACAL_WORKLOAD_ID=<workload ID>\nexport CARACAL_WORKLOAD_SECRET=<workload secret>\n```\n\nWindows PowerShell:\n\n```powershell\n$env:CARACAL_WORKLOAD_ID = \"<workload ID>\"\n$env:CARACAL_WORKLOAD_SECRET = \"<workload secret>\"\n```\n\n## Lost Workload Secret\n\nThe workload secret is held sealed in the Secret Store. If you lose your local copy, reveal it again from the web console **Launcher** page - each reveal is recorded in the zone audit timeline - or rotate the secret and update your secret file or environment with the new value.\n\n## STS 403\n\nAn STS 403 means the workload authenticated but did not receive a mandate.\n\n| Check             | Fix                                                                                         |\n| ----------------- | -----------------------------------------------------------------------------------------"},{"id":"/tutorials","title":"Tutorials","url":"https://docs.caracal.run/tutorials/","concepts":"","body":"Tutorials are the guided path after [Get Started](/get-started/). They turn your first local success into the next developer tasks: protect a real API, call it from your app, trace one request, then choose the production integration guide that matches your stack.\n\n## Tutorial Path\n\n| Outcome                                                                       | Tutorial                                                             | What you use                                                                                    |\n| ----------------------------------------------------------------------------- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |\n| Put an enforced boundary in front of one real HTTP service or provider route. | [Protect Your First Real API](./protect-an-api/)                     | web console **Resources**, **provider**, **policy**, Gateway, and audit.                        |\n| Wire application code through Caracal with a runtime profile.                 | [Connect Your App with the SDK](./connect-an-agent/)                 | TypeScript, Python, or Go SDK; Session; Gateway.                                                |\n| Prove why one protected request succeeded or failed.                          | [Trace One Protected Request](./inspect-a-run/)                      | web console **Audit**, **request trace**, sessions, policy diagnostics, and delegation context. |\n| Choose the durable guide for your production boundary.                        | [Choose Your Production Integration Path](./choose-production-path/) | Gateway, SDK, adapter, runtime, delegation, audit, and step-up guides.                          |\n\n## Before You Begin\n\nYou should have completed [First Protected Call](/get-started/first-protected-call/) or equivalent setup:\n\n- a running Caracal stack;\n- one zone;\n- one confidential agent app;\n- one protected resource;\n- one active policy set;\n- a runtime profile.\n\n## After Tutorials\n\nUse [Guides](/guides/) for task-specific implementation details, [SDKs](/sdks/) for package APIs, and [Concepts](/concepts/) when you need the reference model behind policy, delegation, revocation, or audit."},{"id":"/tutorials/protect-an-api","title":"Protect Your First Real API","url":"https://docs.caracal.run/tutorials/protect-an-api/","concepts":"","body":"This tutorial moves from the demo resource in Get Started to one API-style resource that belongs to your stack. The recommended first boundary is Gateway-routed HTTP because it centralizes mandate verification, replay and revocation checks, provider credential brokering, routing, and action-result audit.\n\nUse adapter-verified services only when you own the service boundary and can preserve the same enforcement contract inside that service.\n\n## 1. Choose What Caracal Should Protect\n\n| Boundary | Use when | What writes action-result audit |\n| --- | --- | --- |\n| Gateway-routed HTTP | The resource is HTTP, REST, MCP-over-HTTP, or another Gateway-routable upstream. | Gateway. |\n| In-process adapter | You own the service process and can verify mandates there. | Your service or adapter after adapter verification. |\n\nStart with Gateway-routed HTTP unless your service must enforce directly in process.\n\n## 2. Create or Select the Real Resource\n\nOpen the web console at [http://localhost:3001](http://localhost:3001).\n\nUse **Guided setup** for a first resource or open **Resources** when you are adding one to an existing zone.\n\nDefine:\n\n| Field | Guidance |\n| --- | --- |\n| Resource identifier | Use a stable ID, for example `resource://billing-api`. |\n| Scopes | Use short action names such as `billing:read` or `billing:submit`. |\n| Upstream URL | Use a URL reachable from the Gateway. |\n| Provider | Select one for the route. Use a `None` provider only when Gateway should enforce access but the upstream needs no brokered credential. |\n\nKeep the resource ID. SDK code, policy, Gateway headers, and audit events all refer to it.\n\n## 3. Allow This App to Use This Resource\n\nCaracal denies by default. Use web console **Guided setup** to create the starter policy, or open **Policies** to register and activate a Rego policy set for the zone.\n\nA complete first policy should allow only:\n\n- the expected agent app;\n- the expected resource;\n- the expected scopes;\n- the expected zone.\n\nSee [Author Policy Data](/guides/author-policy/) and [Activate a Policy Set](/guides/activate-policy-set/) for the policy contract.\n\n## 4. Choose Gateway or In-Process Enforcement\n\n### Gateway-routed HTTP\n\nFor Gateway-routed calls, the resource route needs an upstream URL and, when required, a provider credential source. The Gateway receives:\n\n| Request part | Meaning |\n| --- | --- |\n| `Authorization: Bearer <mandate>` | The Caracal access token issued by STS. |\n| `X-Caracal-Resource: <resource-id>` | The stable resource identifier. |\n\nUse [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/) when you need the durable implementation guide for routes, provider auth modes, and validation.\n\n### In-process adapter\n\nFor services you own, mount the adapter at the request boundary:\n\n| Runtime | Guide |\n| --- | --- |\n| Express | [Protect an Express App](/guides/protect-express/) |\n| FastMCP | [Protect a FastMCP App](/guides/protect-fastmcp/) |\n| Go `net/http` | [Protect a Go net/http Service](/guides/protect-nethttp/) |\n| MCP server | [Protect an MCP Server](/guides/protect-mcp/) |\n\nAdapter verification is not enough by itself for a complete production boundary. The protected service must also handle replay protection where applicable, revocation checks, and result audit for accepted work.\n\n## 5. Verify Allowed, Denied, and Audited Calls\n\nCall the resource from an app configured with your runtime profile or environment variables.\n\n| Outcome | What to expect |\n| --- | --- |\n| Allowed | The call reaches the upstream and audit shows authorization plus action-result evidence. |\n| Denied | STS returns a denial and the decision trace shows which policy or missing state caused it. |\n\nOpen the web console **Audit** and filter by the request ID. If an accepted Gateway-routed call has authorization audit but no action-result audit, check the Gateway route and resource header. If a adapter-verified call lacks result audit, the service-side enforcement path is incomplete.\n\n## Next Step\n\nContinue with [Connect Your App with the SDK](../connect-an-agent/) to wire application code through the resource you just protected."},{"id":"/tutorials/connect-an-agent","title":"Connect Your App with the SDK","url":"https://docs.caracal.run/tutorials/connect-an-agent/","concepts":"","body":"This tutorial connects application code to Caracal. The examples use a runtime profile, open one Session, and call a Gateway-protected resource.\n\n## 1. Confirm the App and Profile\n\nIn the Console, confirm that the selected zone has:\n\n- one confidential agent app;\n- a client secret stored in the secret file named by your runtime profile;\n- one protected resource with an active policy;\n- a Gateway URL and resource binding when using Gateway-routed HTTP.\n\nSet the profile path and the resource target for the examples:\n\n```sh\nexport CARACAL_CONFIG=/path/to/caracal.toml\nexport CARACAL_RESOURCE_ID=resource://billing-api\nexport CARACAL_RESOURCE_PATH=/charge\n```\n\nOn Windows PowerShell:\n\n```powershell\n$env:CARACAL_CONFIG = \"C:\\path\\to\\caracal.toml\"\n$env:CARACAL_RESOURCE_ID = \"resource://billing-api\"\n$env:CARACAL_RESOURCE_PATH = \"/charge\"\n```\n\n## 2. Connect with the SDK\n\nThe client constructor is the standard entry point. It loads the runtime profile when available, then falls back to environment configuration. See [Configuration Order](/reference/config-precedence/) for the exact resolution order.\n\n### TypeScript\n\n```sh\nnpm install @caracalai/sdk\n```\n\n```typescript\nimport { Caracal } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\nconst resourceId = process.env.CARACAL_RESOURCE_ID!\nconst resourcePath = process.env.CARACAL_RESOURCE_PATH!\n\nawait caracal.session(async () => {\n  const request = caracal.gatewayRequest(resourceId, resourcePath)\n  const response = await caracal.transport()(request.url, {\n    method: 'POST',\n    headers: { ...request.headers, 'Content-Type': 'application/json' },\n    body: JSON.stringify({ amount: 1200 }),\n  })\n\n  console.log(await response.text())\n})\n```\n\n### Python\n\n```sh\npip install caracalai-sdk\n```\n\n```python\nimport asyncio\nimport os\n\nfrom caracalai import Caracal\n\ncaracal = Caracal()\nresource_id = os.environ[\"CARACAL_RESOURCE_ID\"]\nresource_path = os.environ[\"CARACAL_RESOURCE_PATH\"]\n\nasync def main():\n    async with caracal.session():\n        request = caracal.gateway_request(resource_id, resource_path)\n        async with caracal.transport() as client:\n            response = await client.post(\n                request.url,\n                headers=request.headers,\n                json={\"amount\": 1200},\n            )\n            print(response.text)\n\nasyncio.run(main())\n```\n\n### Go\n\n```sh\ngo get github.com/garudex-labs/caracal/packages/sdk/go\n```\n\n```go\nc, err := caracal.New()\nif err != nil {\n    panic(err)\n}\n\nresourceID := os.Getenv(\"CARACAL_RESOURCE_ID\")\nresourcePath := os.Getenv(\"CARACAL_RESOURCE_PATH\")\n\nerr = c.Session(context.Background(), func(ctx context.Context) error {\n    target, err := c.GatewayRequest(resourceID, resourcePath)\n    if err != nil {\n        return err\n    }\n\n    req, err := http.NewRequestWithContext(ctx, http.MethodPost, target.URL, strings.NewReader(`{\"amount\":1200}`))\n    if err != nil {\n        return err\n    }\n    req.Header = target.Header.Clone()\n    req.Header.Set(\"Content-Type\", \"application/json\")\n\n    resp, err := c.Transport(nil).Do(req)\n    if err != nil {\n        return err\n    }\n    defer resp.Body.Close()\n    return nil\n})\nif err != nil {\n    panic(err)\n}\n```\n\n## 3. Understand the Runtime Behavior\n\n| SDK call                                | What happens                                                                                   |\n| --------------------------------------- | ---------------------------------------------------------------------------------------------- |\n| `new Caracal()` / `Caracal()` / `New()` | Loads configuration and prepares app-secret token exchange.                                    |\n| `session()` / `Session()`               | Creates a Session, binds context, and tears it down after the block returns.                   |\n| `gatewayRequest()` / `GatewayRequest()` | Builds the Gateway URL and `X-Caracal-Resource` header.                                        |\n| `transport()` / `Transport()`           | Injects trace and baggage context, Gateway routing, and Authorization on Gateway-routed calls. |\n\n`session()` is the session boundary. If the function exits or raises, the SDK terminates the Session and active authority for that session is revoked.\n\nOne application credential backs every Session you start. A workload registers one application, then opens a Session per unit of work - you do not register a new application per agent. See [Should I create one application per agent?](/reference/faq/#faq-006).\n\nIf your app starts a Session for each child agent or delegates work, continue to [Implement Multi-Agent Delegation](/guides/delegation/) after this tutorial path.\n\n## 4. Verify Audit\n\nOpen web console **Audit** and find the request. A complete Gateway-routed call produces:\n\n1. an authorization event from STS;\n2. an action-result event from Gateway.\n\nOpen **explain** with the request ID to confirm the application, resource, scopes, policy, policy set version, and decision.\n\n## Next Step\n\nContinue with [Trace One Protected Request](../inspect-a-run/) to investigate the request you just made."},{"id":"/tutorials/inspect-a-run","title":"Trace One Protected Request","url":"https://docs.caracal.run/tutorials/inspect-a-run/","concepts":"","body":"This tutorial shows how to prove what happened during one protected request. You will find the request, inspect the session context, review audit events, and trace the final decision.\n\nThe flow assumes you already completed [Protect Your First Real API](../protect-an-api/) and [Connect Your App with the SDK](../connect-an-agent/).\n\n## 1. Find the Request\n\nOpen the web console at [http://localhost:3001](http://localhost:3001).\n\nUse these web console sections:\n\n| Entry                | Use it for                                                           |\n| -------------------- | -------------------------------------------------------------------- |\n| **Audit**            | Find recent events, then open the decision trace for one request ID. |\n| **Session**          | Find the tracked Session for the selected zone.                      |\n| **Authority record** | Inspect underlying Authority records and parent relationships.       |\n\nRecord the request ID from the SDK, Gateway, STS error, audit event, or web console trace.\n\n## 2. Read the Audit Timeline\n\nOpen **Audit**. The live audit view opens recent events immediately and supports request details, request tracing, advanced filters, reload, and pause/resume.\n\nA Gateway-routed protected call usually produces:\n\n| Event class         | What it proves                                                                             |\n| ------------------- | ------------------------------------------------------------------------------------------ |\n| Authorization event | STS evaluated policy for the app, resource, scopes, Session state, and delegation context. |\n| Action-result event | Gateway verified and routed the request, then recorded the upstream result.                |\n\nIf you see authorization events without matching action-result events, check whether the call reached the Gateway. For adapter-verified services, confirm the service writes its own result audit after mandate verification.\n\n## 3. Trace the Decision\n\nPick an audit event with a request ID. Press the trace action on the selected event in **Audit** to open its decision trace.\n\nThe request trace should show:\n\n- application and execution session;\n- resource and requested scopes;\n- policy set version and policy rules involved;\n- allow, deny, or partial decision;\n- diagnostics from policy evaluation;\n- Delegation constraints when present.\n\nUse the request trace as the source of truth for why a request got its result. If an app behaves differently than expected, compare the trace against the active policy set, resource configuration, and runtime profile.\n\n## 4. Check Delegation Context\n\nIf the request used delegated authority, open **Delegation** and inspect the edge:\n\n| Field                | Why it matters                                       |\n| -------------------- | ---------------------------------------------------- |\n| Source session       | The session that granted authority.                  |\n| Target session       | The session allowed to use the delegated permission. |\n| Scopes               | The narrowed actions.                                |\n| TTL and hop limits   | The lifetime and graph depth of the permission.      |\n| Resource constraints | The targets the delegated permission can affect.     |\n\nUse [Implement Multi-Agent Delegation](/guides/delegation/) when you need to design or debug delegation flows.\n\n## 5. Check Revocation Behavior\n\nRevoking a session, grant, Delegation, or zone/application emergency state invalidates authority anchored to it. Gateway-routed calls check revocation before accepting a request and while streaming responses. Audit records revocation events and marks interrupted action results.\n\n## Wrap Up\n\nYou can now trace one protected request from application identity to policy decision, Gateway action, delegation context, and revocation state.\n\n## Next Step\n\nContinue with [Choose Your Production Integration Path](../choose-production-path/)."},{"id":"/tutorials/choose-production-path","title":"Choose Your Production Integration Path","url":"https://docs.caracal.run/tutorials/choose-production-path/","concepts":"","body":"Use this bridge after the tutorials. Pick the guide that matches the boundary you will own in production.\n\n## Choose by Boundary\n\n| If your production path is...                               | Continue with                                                                                                    |\n| ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |\n| Route HTTP traffic through Caracal Gateway                  | [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/)                                               |\n| Add Caracal session and Gateway calls to app code           | [TypeScript SDK](/guides/sdk-typescript/), [Python SDK](/guides/sdk-python/), or [Go SDK](/guides/sdk-go/)       |\n| Protect an Express resource server                          | [Protect an Express App](/guides/protect-express/)                                                               |\n| Protect a FastMCP resource server                           | [Protect a FastMCP App](/guides/protect-fastmcp/)                                                                |\n| Protect a Go `net/http` resource server                     | [Protect a Go net/http Service](/guides/protect-nethttp/)                                                        |\n| Protect an MCP server without a dedicated adapter           | [Protect an MCP Server](/guides/protect-mcp/)                                                                    |\n| Run an existing CLI or worker under injected runtime tokens | [Run an Agent with caracal run](/guides/runtime-run/)                                                            |\n| Model zones, apps, resources, and customer boundaries       | [Model Your Application in Caracal](/guides/modeling-recipes/)                                                   |\n| Configure provider credentials or OAuth                     | [Define Resources and Providers](/guides/resources-providers/) and [Provider Recipes](/guides/provider-recipes/) |\n| Debug denies or unexpected allows                           | [Debug Authorization Decisions](/guides/authorize-access/)                                                       |\n| Delegate work between agents                                | [Implement Multi-Agent Delegation](/guides/delegation/)                                                          |\n| Export or query audit evidence                              | [Tail and Query the Audit Stream](/guides/audit-stream/)                                                         |\n| Hold sensitive actions for human approval                   | [Human Approval](/guides/step-up/)                                                                               |\n\n## Choose by Team Role\n\n| Role              | Start with                                                                                                                                                               |\n| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| App engineer      | SDK guide for your language, then Gateway or adapter guide.                                                                                                              |\n| Platform engineer | [Production Integration Patterns](/guides/enterprise-runtime-patterns/), then operations pages.                                                                          |\n| Security reviewer | [Debug Authorization Decisions](/guides/authorize-access/), [Audit and Request Traces](/concepts/audit-ledger/), and [Review the Threat Model](/security/threat-model/). |\n| Policy owner      | [Author Policy Data](/guides/author-policy/) and [Activate a Policy Set](/guides/activate-policy-set/).                                                                  |\n\n## Next Step\n\nOpen [Guides](/guides/) and follow the path that matches your boundary."},{"id":"/guides","title":"Guides","url":"https://docs.caracal.run/guides/","concepts":"","body":"Use Guides after [Get Started](/get-started/) or the [Tutorials](/tutorials/) when you know the task you need to complete. You do not need to read every concept first; each guide links to the canonical concept page when the model matters.\n\n## Choose by Task\n\n| Task                                                 | Start with                                                                                                                                                                                    |\n| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Map your architecture onto Caracal                   | [Model Your Application in Caracal](/guides/modeling-recipes/)                                                                                                                                |\n| Serve many of your own customers from one deployment | [Serve Your Own Customers](/guides/serve-customers/)                                                                                                                                          |\n| Define protected targets and upstream credentials    | [Define Resources and Providers](/guides/resources-providers/) and [Provider Recipes](/guides/provider-recipes/)                                                                              |\n| Write and activate authorization logic               | [Author Policy Data](/guides/author-policy/) and [Activate a Policy Set](/guides/activate-policy-set/)                                                                                        |\n| Debug an authorization result                        | [Debug Authorization Decisions](/guides/authorize-access/)                                                                                                                                    |\n| Add Caracal to app code                              | [TypeScript SDK](/guides/sdk-typescript/), [Python SDK](/guides/sdk-python/), or [Go SDK](/guides/sdk-go/)                                                                                    |\n| Run an existing process with Caracal tokens          | [Run an Agent with caracal run](/guides/runtime-run/)                                                                                                                                         |\n| Protect a Gateway-routed HTTP upstream               | [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/)                                                                                                                            |\n| Protect a resource server in process                 | [Express](/guides/protect-express/), [FastAPI](/guides/protect-fastapi/), [FastMCP](/guides/protect-fastmcp/), [Go net/http](/guides/protect-nethttp/), or [MCP server](/guides/protect-mcp/) |\n| Add delegation, audit export, or step-up             | [Delegation](/guides/delegation/), [Audit Stream](/guides/audit-stream/), or [Human Approval](/guides/step-up/)                                                                               |\n| Plan a production integration                        | [Production Integration Patterns](/guides/enterprise-runtime-patterns/)                                                                                                                       |\n\n## Recommended Order\n\n## Surface Boundaries\n\nUse the right surface for each task:\n\n| Surface                                                                       | Use for                                                                                                                        |\n| ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |\n| `caracal up`, `down`, `status`, `upgrade`, `purge`, `allowlist`, `run`, `web` | Local runtime lifecycle, Console sign-in admission, and subprocess injection.                                                  |\n| Console                                                                       | Human-facing zone, application, provider, resource, policy, session, audit, explanation, delegation, and diagnostic workflows. |\n| Admin API and `@caracalai/admin`                                              | Automation for the same control-plane objects.                                                                                 |\n| SDKs and adapters                                                             | Application integration, context propagation, mandate exchange, and mandate verification.                                      |\n\n## Before You Start\n\nYou need a running Caracal runtime, a zone, an application, at least one resource, and an active policy set. [First Protected Call](/get-started/first-protected-call/) creates that baseline."},{"id":"/guides/modeling-recipes","title":"Model Your Application in Caracal","url":"https://docs.caracal.run/guides/modeling-recipes/","concepts":"","body":"The [concept pages](/concepts/) define each noun, and [Provider Recipes](/guides/provider-recipes/) give concrete upstream auth setups. This page helps you decide how to map your architecture onto Caracal before you create production objects.\n\nEvery recipe states the modeling decision, what maps to each noun, and the trade-off. Use the [Caracal Mental Model](/concepts/model-overview/) as the vocabulary reference while you read.\n\n## Choose the Zone Boundary\n\nThe most common question is “what is a zone - an environment, a customer, a team, or a product area?” A zone is none of those by default. It is the isolation boundary that owns signing keys, policy sets, sessions, audit, and authority data. You decide what trust boundary it represents.\n\nUse a **separate zone** whenever two workloads must not share signing keys, policy activation, or audit trails. Use a **shared zone with separate resources** when workloads belong to the same trust boundary but target different upstreams.\n\n:::note[FAQ]\n[What should a zone represent?](/reference/faq/#faq-003) and [does the open-source edition include managed multi-tenancy?](/reference/faq/#faq-004)\n:::\n\n| Question                                                                    | If yes, lean toward           |\n| --------------------------------------------------------------------------- | ----------------------------- |\n| Must these workloads have independent signing keys and JWKS?                | Separate zones                |\n| Must a policy change for one never affect the other?                        | Separate zones                |\n| Must audit and explain traces never mix?                                    | Separate zones                |\n| Do they share keys, policy owners, and audit, but call different upstreams? | One zone, separate resources  |\n| Do they share an upstream but need different actions?                       | One resource, separate scopes |\n\nKeep [resource identifiers](/concepts/resource-grant/) stable across zones so policy, grants, and audit refer to the same target even when upstream URLs differ.\n\n## Single Application, Single Environment\n\nThe simplest deployment: one team, one runtime, a handful of upstreams.\n\n| Noun        | Mapping                                                                                                                                         |\n| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |\n| Zone        | One zone for the whole deployment.                                                                                                              |\n| Application | One managed application per durable workload; one DCR application per isolated, externally-launched identity (per tenant, job, or integration). |\n| Resource    | One resource per protected upstream, with action-oriented scopes.                                                                               |\n| Grant       | One grant per application and subject that may request a resource's scopes.                                                                     |\n\nTrade-off: lowest operational overhead. Add zones only when you need key, policy, or audit isolation.\n\n## Per-Environment Zones\n\nSeparate production, staging, and development so a policy or key change in one cannot affect another.\n\n| Noun       | Mapping                                                                                                              |\n| ---------- | -------------------------------------------------------------------------------------------------------------------- |\n| Zone       | One zone per environment: `prod`, `staging`, `dev`.                                                                  |\n| Resource   | The same stable identifier in each zone, such as `resource://pipernet`, pointing at that environment's upstream URL. |\n| Policy set | Authored and activated independently per zone.                                                                       |\n| Keys       | Each zone has its own signing key and JWKS.                                                                          |\n\nTrade-off: clean blast-radius isolation and independent key rotation, at the cost of registering resources and activating policy in each zone. Automate this with the Admin API so environments stay consistent.\n\nIn application code, identities stay isolated at the client boundary: one SDK client binds exactly one `(zone, application)` identity during concurrent use. A workload that acts as several Applications in one Zone or talks to several Zones constructs one `Caracal` per `(zone, application)` with that identity's credentials and routes work to the matching client. Do not multiplex active identities through one client by swapping resolver output; token, mandate, Session, hook, and shutdown state belong to one identity boundary.\n\n## Multiple Customers or Workspaces\n\nA platform with many customer workspaces and one shared agent service must choose how customer isolation maps onto zones. The open-source edition gives you the **zone** as the isolation primitive; you provision and automate customer onboarding yourself through the Admin API. Managed tenant, team, and SSO lifecycle features are covered in [Compare Editions](/enterprise/).\n\n| Model                                 | When to use                                                                                                                | Trade-off                                                                                                                                       |\n| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------"},{"id":"/guides/serve-customers","title":"Serve Your Own Customers","url":"https://docs.caracal.run/guides/serve-customers/","concepts":"","body":"[Model Your Application in Caracal](/guides/modeling-recipes/) compares the isolation models at a high level. This page is the end-to-end pattern for the most common case: your application is a single product that serves many of _its own_ customers, and you want clean per-customer authority without standing up infrastructure for each customer.\n\nThe short answer: run **one zone for your deployment** and carry **each customer on the work itself** through a `customer:` label that policy confines and audit can filter. Keep `customer_id` metadata only when Coordinator inspection needs an additional business key. Customer separation lives below the zone - in Sessions, labels, and Delegation - which is exactly where Caracal models per-actor authority. You do not create a zone per customer.\n\n:::note[FAQ]\n[What should a zone represent?](/reference/faq/#faq-003) and [does the open-source edition include managed multi-tenancy?](/reference/faq/#faq-004)\n:::\n\n## Where Each Customer Lives in the Model\n\n| Layer                       | Owns                                                                                                                        | Per-customer?                  |\n| --------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |\n| Zone                        | Signing keys, policy set, resources, providers, audit trail                                                                 | No - one per deployment        |\n| Application                 | Your product's service identity                                                                                             | No - shared by all customers   |\n| Customer label and metadata | The customer the work is for: a `customer:<id>` label policy and audit can filter; optional `customer_id` metadata remains Coordinator inspection data | Yes - stamped on every Session |\n| Session                     | One agent run, labeled and attributed to one customer                                                                       | Yes - one per customer task    |\n| Delegation                  | The scoped authority that agent holds for one resource                                                                      | Yes - least privilege per task |\n\nA zone is the trust boundary that owns keys, policy, and audit. A customer is not a trust boundary of its own here; it is an _attribute_ of the work - carried as a label the platform decision contract confines and a metadata key the audit trail filters. Keep customer identity in labels and metadata - never in [scope names](/concepts/resource-grant/).\n\n## Step 1: One Zone, One Application\n\nProvision a single zone for the deployment and one managed application for your product, as in [Model Your Application in Caracal](/guides/modeling-recipes/). Set the application identity once in the environment; it is shared across all customers.\n\n```bash\nCARACAL_ZONE_ID=\"<your-zone-id>\"\nCARACAL_APPLICATION_ID=\"<your-application-id>\"\nCARACAL_APP_CLIENT_SECRET=\"<your-application-secret>\"\n```\n\nThe zone owns one policy set and one audit trail. Every customer's authority is decided and recorded inside it.\n\n## Step 2: Choose a Stable Customer Identifier\n\nCaracal does not authenticate your customers, does not own your user directory, and never generates a customer identifier. Your application's existing authentication system - Auth0, Keycloak, Better Auth, a custom issuer - authenticates the customer; Caracal receives the identifier your application asserts and treats it as an opaque, stable string. It does not need to know what the identifier represents, only that the same customer always presents the same value.\n\nChoose an id that never changes for the life of the account - an account UUID, not an email or display name - so audit history stays attributable even after profile changes. Do not reuse one identifier across two customers: separation is only as strong as your identifier discipline.\n\nThe identifier enters Caracal on every Session your application starts for that customer as a `customer:<id>` label. Policy and audit consume that label. Add a `customer_id` metadata key only for direct Coordinator inspection; Session metadata is not STS policy or audit input. The `sub` recorded on the underlying STS Authority record is your application's identity from the client-credentials chain; the customer rides on the work itself.\n\n## Step 3: Start a per-Customer Session with a Correlation Key\n\nStart the Session for a customer's request inside the one zone and stamp the customer ID into a label. The label travels into policy and audit, making per-customer attribution a direct lookup instead of a guess. Optional metadata can support direct Coordinator inspection but is not copied into decision audit events.\n\n```python\nasync with caracal.session(\n  labels=[f\"customer:{customer_id}\"],\n    metadata={\"customer_id\": customer_id},\n) as ctx:\n    # Every gateway and provider call in this block carries a scoped,\n    # non-root mandate for this customer's work.\n    await do_work(ctx)\n```\n\nThe Session ID and Authority record are the exact authority anchors; the `customer:` label is the business correlation key exposed to policy and audit. Keep the optional `customer_id` metadata key aligned with that label when Coordinator inspection needs the raw value.\n\n:::caution[Attribution is not enforcement]\nSession metadata is stored by Coordinator but does not enter STS policy or decision audit. The platform decision contract decides from Session labels (`input.principal.labels`) and the Delegation. Use the `customer:` label to restrict and find customer activity; use metadata only for direct Session inspection.\n:::\n\nFor fan-out work, give each child agent the least authority it needs with [delegation](/guides/delegation/), keeping one customer's blast radius contained even though all customers share the zone."},{"id":"/guides/resources-providers","title":"Define Resources and Providers","url":"https://docs.caracal.run/guides/resources-providers/","concepts":"","body":"Resources tell Caracal what protected target is being accessed. Providers describe how Gateway authenticates to that target, including no-credential and Caracal mandate flows.\n\n## When to create each object\n\n| Object              | Create it when                                                                                                                                                                          |\n| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Resource            | You need a stable protected target, Caracal resource scopes, an upstream URL, a Gateway application, and one upstream credential provider binding.                                      |\n| Provider            | A resource needs an explicit upstream auth mode: none, Caracal mandate, OAuth 2.0 authorization code, OAuth 2.0 client credentials, API key, bearer-token, or HTTP Basic upstream auth. |\n| Gateway application | You want Gateway-originated upstream calls to use a specific Caracal application identity.                                                                                              |\n\nResource fields should answer “what is being protected and where does Gateway send traffic?” Provider fields should answer “what credential or identity does Gateway attach upstream?” Keep credential details such as client secrets, token endpoints, API-key placement, bearer tokens, identity forwarding, and runtime injection on the provider. Keep target details such as resource identifier, Caracal resource scopes, upstream URL, Gateway application, and the selected upstream credential provider on the resource.\n\n:::note[FAQ]\n[What is the difference between a resource and a provider?](/reference/faq/#faq-009) and [is an application secret the same as a provider credential?](/reference/faq/#faq-013)\n:::\n\n## Web Console Workflow\n\n1. Start the local runtime with `caracal up`.\n2. Open `caracal web`.\n3. Select **Resources**.\n4. Create a resource with a stable identifier, such as `resource://pipernet`.\n5. Add action-oriented scopes, such as `payments:read` and `payments:refund`.\n6. Set the upstream URL, Gateway application, and upstream credential provider for the protected service.\n7. Select a `None` provider when Gateway should enforce Caracal access but the upstream expects no credential.\n8. Select `Caracal mandate` for resources that verify Caracal tokens directly, or choose a provider-native type when the upstream needs external credentials.\n9. Attach exactly one upstream credential provider to the resource.\n10. For a path-addressed REST upstream, declare the operations the Gateway may invoke and keep operation authority `enforced`; choose `transport_uniform` for single-surface transports such as MCP.\n\n## Provider auth modes\n\n| Provider type                | Required main fields                                                                                                   | Runtime behavior                                                                                                                                              |\n| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| None                         | No credential fields.                                                                                                  | Gateway strips caller auth and forwards no upstream credential; use only when Gateway is the enforcement point and the upstream expects no credential.        |\n| Caracal mandate              | No credential fields.                                                                                                  | Gateway forwards the Caracal resource mandate in `Authorization: Bearer ...`; the resource verifies issuer, audience, scopes, target, expiry, and revocation. |\n| OAuth 2.0 authorization code | Authorization endpoint, token endpoint, redirect URI, client ID, client secret, optional upstream OAuth scopes.        | Creates per-subject provider connections through a browser consent callback, then refreshes the brokered upstream tokens inside STS.                          |\n| OAuth 2.0 client credentials | Token endpoint, client ID, grant type, client secret or private key depending on the grant and authentication choices. | STS obtains and caches service-to-service provider tokens for Gateway upstream calls, via `client_credentials` or RFC 7523 `jwt_bearer` assertion grants.     |\n| API key                      | Header name and API key.                                                                                               | Gateway forwards the sealed provider key in the configured header, with an optional auth scheme prefix.                                                       |\n| Bearer token                 | Bearer token.                                                                                                          | Gateway forwards the sealed provider token as `Authorization: Bearer ...` unless Advanced routing configures another header or scheme.                        |\n| HTTP Basic                   | Username and password.                                                                                                 | Gateway forwards `Authorization: Basic ...` built from the username and the sealed password; the pair is never exposed to callers or runtimes.                |\n\nOAuth 2.0 authorization-code providers are for delegated consent. Register the exact Caracal callback URI as the provider redirect URI, such as `https://api.hooli.example/v1/zones/z1/provider-connections/oauth/callback`, then use the provider's **Connect** action t"},{"id":"/guides/provider-recipes","title":"Provider Recipes","url":"https://docs.caracal.run/guides/provider-recipes/","concepts":"","body":"This page turns the abstract [provider auth modes](/guides/resources-providers/) into concrete recipes. Each recipe states the enforcement boundary, the provider kind, the exact fields, and the resource binding so you can reproduce a working setup without guessing.\n\nProvider recipes live in the documentation, not the web console. The Console stays focused on the fields a given setup needs; this page is the reference you keep open beside it.\n\n## Choose the enforcement boundary first\n\nThe most common mistake is treating these four boundaries as interchangeable. They are not - they differ in where enforcement happens, who writes the action-result audit, and who holds the upstream credential.\n\n| Boundary                 | Enforcement point                          | Credential holder                | Action-result audit                     | Use when                                                                                                                  |\n| ------------------------ | ------------------------------------------ | -------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |\n| Gateway-mediated         | Caracal Gateway                            | Gateway (sealed provider secret) | Gateway                                 | The upstream is HTTP-routable and you want Caracal to broker the credential and enforce every call.                       |\n| Adapter-verified         | Your service process                       | Your service                     | Your service after adapter verification | You own the service and must enforce mandates in process.                                                                 |\n| Application-managed call | Your application code                      | Your application                 | Your application                        | You call the provider directly and only attach Caracal context for attribution. Not Caracal-enforced.                     |\n| Runtime token injection  | Your runtime, eligibility gated by Caracal | Your runtime process             | Your application                        | An existing client or CLI must receive a brokered token at runtime, and the provider sets `allow_runtime_injection=true`. |\n\nPrefer **Gateway-mediated** unless you have a specific reason to enforce elsewhere. The recipes below note which boundary they use.\n\n## OpenAI API key (Gateway-mediated)\n\nBroker an OpenAI key so the agent never holds it.\n\n| Field                 | Value                                        |\n| --------------------- | -------------------------------------------- |\n| Provider kind         | `api_key`                                    |\n| `header_name`         | `Authorization`                              |\n| `auth_scheme`         | `Bearer`                                     |\n| `api_key`             | Your OpenAI secret key (sealed at creation). |\n| Resource identifier   | `resource://openai`                          |\n| Resource upstream URL | `https://api.openai.com`                     |\n| Resource scopes       | `openai:chat`, `openai:embeddings`           |\n\nThe Gateway forwards `Authorization: Bearer <sealed key>` upstream and strips caller auth. The agent calls the Caracal resource, not OpenAI directly.\n\n## Google Workspace (OAuth authorization code)\n\nDelegated user consent for a Google API, refreshed inside STS.\n\n| Field                             | Value                                                                                                           |\n| --------------------------------- | --------------------------------------------------------------------------------------------------------------- |\n| Provider kind                     | `oauth2_authorization_code`                                                                                     |\n| `authorization_endpoint`          | `https://accounts.google.com/o/oauth2/v2/auth`                                                                  |\n| `token_endpoint`                  | `https://oauth2.googleapis.com/token`                                                                           |\n| `redirect_uri`                    | Your exact Caracal callback, e.g. `https://api.example.com/v1/zones/<zone>/provider-connections/oauth/callback` |\n| `client_id` / `client_secret`     | From the Google Cloud OAuth client.                                                                             |\n| `scopes`                          | e.g. `https://www.googleapis.com/auth/drive.readonly`                                                           |\n| `allowed_token_hosts`             | `oauth2.googleapis.com`                                                                                         |\n| `authorization_params` (Advanced) | `access_type=offline`, `prompt=consent` for refresh tokens.                                                     |\n| Resource upstream URL             | `https://www.googleapis.com`                                                                                    |\n\nCaracal owns `client_id`, `redirect_uri`, `state`, and PKCE. Use the provider `connect` action to mint the consent URL for a user and resource.\n\n:::note[FAQ]\n[When should I use per-user OAuth instead of a shared provider credential?](/reference/faq/#faq-014)\n:::\n\n## Google Cloud service account (jwt_bearer)\n\nMachine access to Google APIs - Vertex AI, Cloud Storage, and every other `googleapis.com` surface - signed with a service-account key instead of user consent.\n\n| Field                          | Value                                                                   |\n| ------------------------------ | ----------------------------------------------------------------------- |\n| Provider kind                  | `oauth2_client_credentials`                                             |\n| `grant_type`                   | `"},{"id":"/guides/author-policy","title":"Author Policy Data","url":"https://docs.caracal.run/guides/author-policy/","concepts":"","body":"The platform decision contract owns every authorization decision inside the STS. You do not write `result` - you author **policy data documents** that tell the contract which application owns a resource, which roles hold which scopes, and how to confine or restrict authority. This guide writes those documents and validates them before activation.\n\n## Prerequisites\n\n- A zone, application, and resource.\n- Access to the Console or Admin API.\n- The resource identifier and scopes you want to grant.\n\n## Start from a grant\n\nThe core document is `grants`: it names the application that owns a resource view and the scopes each role may hold. Pair it with `app_ids`, which binds the application key you use in `grants` to the control-plane id the STS sees as `input.principal.id`.\n\n```text\n# caracal:data-document\npackage caracal.authz\n\nimport rego.v1\n\napp_ids := {\n  \"pipernet\": \"app-pipernet\",\n}\n\ngrants := {\n  \"resource://pipernet\": {\n    \"application\": \"pipernet\",\n    \"roles\": {\"reader\": [\"pipernet:read\", \"pipernet:write\"]},\n  },\n}\n```\n\nThis is the canonical \"application A may call resource B with scopes C\" pattern,\nexpressed as data. The platform contract allows a mint only when the acting\napplication owns the view, the agent's role label grants the scope, and the\nDelegation narrows to it. You declare the grant; the contract enforces the\nnarrowing.\n\n## Gate scopes on human approval\n\nTwo further documents add an optional human gate on top of a grant. `risk` names a\ntier for each sensitive scope, and `approval_tiers` declares which tiers hold the\nmint until a person decides it:\n\n```text\n# caracal:data-document\npackage caracal.authz\n\nimport rego.v1\n\nrisk := [\n  {\"scope\": \"pipernet:refund\", \"tier\": \"high\"},\n]\n\napproval_tiers := [\n  {\"tier\": \"high\", \"approver\": \"operator\", \"ttl_seconds\": 1800, \"privacy\": \"identified\"},\n]\n```\n\nLike `restrict` and `confinement`, an approval declaration can only add a gate,\nnever widen authority, and a malformed declaration fails the gated mint closed.\n[Human Approval](/guides/step-up/) covers the tier fields, both decision planes,\nand the agent-side wait-and-retry flow.\n\n## Start from a template\n\nYou do not have to write each document by hand. Caracal ships a built-in catalog of\ndata-document starters, served at `/v1/policy-templates` and through the Admin SDK:\n\n```ts\nimport { AdminClient } from '@caracalai/admin'\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n})\n\nconst templates = await admin.policyTemplates.list()\nconst starter = await admin.policyTemplates.get('resource-grants')\n```\n\n| Template               | Use it for                                                                     |\n| ---------------------- | ------------------------------------------------------------------------------ |\n| `application-bindings` | Map each application key used in `grants` to its control-plane application id. |\n| `resource-grants`      | Declare the owning application and per-role scope sets for a resource view.    |\n| `label-confinement`    | Cap every session carrying a label prefix to a fixed scope set.                |\n| `zone-restriction`     | A deny overlay that freezes the zone while an entry is present.                |\n\nFor assisted authoring, describe the outcome to the [Caracal Operator](/concepts/operator/#authoring-policy). Its policy author models the use case as grant, binding, and confinement data, validates and previews each document against the platform contract, and proposes a governed create you review and approve - so the policy that lands is already contract-valid.\n\n## How your data maps to the request\n\nThe decision contract evaluates a fixed input contract and resolves it against your\ndata. The acting application is the principal - there is no `input.application` or\n`input.grant` object. See the full [Policy Input Contract](/concepts/policy/#policy-input-contract)\nfor every field.\n\n| Input the contract reads         | Resolved against                                           |\n| -------------------------------- | ---------------------------------------------------------- |\n| `input.principal.id`             | `app_ids` - to find the application key used in `grants`.  |\n| `input.principal.labels`         | `grants[...].roles` and `confinement` label prefixes.      |\n| `input.resource.identifier`      | the top-level key in `grants`.                             |\n| `input.context.requested_scopes` | the role's scope set and any matching `confinement` rule.  |\n| `input.delegation_edge.scopes`   | the narrowing floor every requested scope must sit inside. |\n\n## Validate before versioning\n\nUse the Console policy workflow to paste the document and run validation. For automation, validate through the Admin API or `@caracalai/admin`:\n\n```ts\nimport { AdminClient } from '@caracalai/admin'\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n})\n\nconst validation = await admin.policies.validate(policySource)\nif (!validation.valid) {\n  throw new Error('policy failed validation')\n}\n```\n\nValidation enforces the data-document contract: the package must be `caracal.authz`,\nthe first line must carry the `# caracal:data-document` directive, the document must\ndefine at least one data rule, and it must **not** define `result`. Validation also\nchecks the schema version, balanced syntax, and forbidden built-ins. Because the\nplatform contract owns the decision, a data document can never authorize on its own.\n\n## Why data, not decisions\n\nAuthorization logic - delegation narrowing, role and grant checks, label\nconfinement, bootstrap isolation - is identical for every adopter and is the part\nmost dangerous to get wrong. A single typo in a hand-written rule (`if { false }` →\n`if { true }`) silently turns a deny into an allow-all. Caracal removes that footgun\nby owning the logic in a signed, versioned platform decision contract and letting\nyou "},{"id":"/guides/activate-policy-set","title":"Activate a Policy Set","url":"https://docs.caracal.run/guides/activate-policy-set/","concepts":"","body":"The STS evaluates the active policy-set version for a zone. Activation is explicit so policy changes can be reviewed, simulated, and rolled out without mutating old versions.\n\n## The mental model\n\nFour immutable layers separate authoring from what runs:\n\n| Layer | What it is | Mutable? |\n| --- | --- | --- |\n| Policy | A named Rego document. | The name; content is versioned. |\n| Policy version | One immutable snapshot of policy content. | No. |\n| Policy-set version | An immutable bundle of policy versions. | No. |\n| Active policy-set version | The one bundle the STS evaluates for a zone. | The pointer moves on activation. |\n\nYou edit by creating a **new** version and moving the active pointer to it.\nNothing already evaluated is ever rewritten, so every audit decision ties back to\nthe exact policy-set version and manifest hash that produced it.\n\nA zone is governed by exactly one policy set at a time. Activating a version of\none set deactivates any other set in the zone in the same transaction, so which\npolicies govern a zone is never ambiguous. Saving or activating a version also\ncompiles the full policy bundle on the STS runtime engine, so a manifest whose\npolicies conflict (for example, two documents defining the same data key) is\nrejected before it can be pinned or promoted.\n\n## Activation flow\n\n## Web Console Workflow\n\n1. Open `caracal web`.\n2. Select **Policies** and create or update the Rego policy.\n3. Select **Policy Sets** and create a set for the zone.\n4. Add the policy version to a new policy-set version.\n5. Simulate the version with a representative input.\n6. Activate the version when the simulated decision matches the intended behavior.\n7. Use **Audit** to follow the decision trace after the first real request.\n\n## Automation workflow\n\n```ts\nimport { AdminClient } from \"@caracalai/admin\";\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n});\n\nconst policy = await admin.policies.create(process.env.CARACAL_ZONE_ID!, {\n  name: \"payments-read\",\n  content: policySource,\n});\n\nconst set = await admin.policySets.create(process.env.CARACAL_ZONE_ID!, \"payments\");\nconst version = await admin.policySets.addVersion(process.env.CARACAL_ZONE_ID!, set.id, [\n  { policy_version_id: policy.version.id },\n]);\n\nconst simulation = await admin.policySets.simulate(process.env.CARACAL_ZONE_ID!, set.id, version.id, sampleInput);\nif (!simulation.would_activate) {\n  throw new Error(simulation.explanation.reason);\n}\n\nawait admin.policySets.activate(process.env.CARACAL_ZONE_ID!, set.id, version.id);\n```\n\n## Wait for propagation\n\nActivation returns `202 Accepted`: the active pointer moves durably, then the\nSTS runtime reloads the bundle through a durable invalidation stream. Poll the\nactivation status until the runtime reports the version as loaded before\ntreating the rollout as live:\n\n```ts\nlet status = await admin.policySets.activationStatus(zoneId, set.id, version.id);\nwhile (status.propagation_status !== \"loaded\") {\n  if (status.propagation_status === \"failed\") {\n    throw new Error(status.outbox.last_error ?? \"policy rollout failed\");\n  }\n  await new Promise((resolve) => setTimeout(resolve, 2000));\n  status = await admin.policySets.activationStatus(zoneId, set.id, version.id);\n}\n```\n\n`propagation_status` moves through `waiting_for_outbox` → `waiting_for_sts` →\n`loaded`. With several STS replicas, the replica that receives the invalidation\nreloads within about a second; the rest converge through a periodic database\npoll (60 seconds by default), so a brief mixed-version window across replicas\nis expected during rollout.\n\n## Validation checklist\n\n| Check | Expected result |\n| --- | --- |\n| Policy validation | `valid: true` and no blocking warnings. |\n| Simulation | Representative input returns the intended decision. |\n| Activation | Zone has the expected active policy-set version. |\n| Propagation | `activationStatus` reports `propagation_status: \"loaded\"`. |\n| First exchange | Audit shows the new policy as a determining policy. |\n\nIf activation changes expected access, keep the old policy-set version ID in the rollout notes so you can promote it again if needed.\n\n## Iterate from real denials\n\nEvery denied decision links to the policy-set version that produced it, and the\naudit explain endpoint reconstructs the policy input for that denial. Stage the\nfix as a new policy-set version, then simulate the denied input against it:\n\n```ts\nconst trace = await admin.audit.explain(zoneId, requestId);\nconst input = trace.denied[0]?.policy_input;\nconst candidate = await admin.policySets.addVersion(zoneId, set.id, manifest);\nconst check = await admin.policySets.simulate(zoneId, set.id, candidate.id, input);\nif (check.result?.decision === \"allow\") {\n  await admin.policySets.activate(zoneId, set.id, candidate.id);\n}\n```\n\nThe [Iterate Policy Safely example](/examples/policy-iterate/) wraps this loop as a runnable script."},{"id":"/guides/authorize-access","title":"Debug Authorization Decisions","url":"https://docs.caracal.run/guides/authorize-access/","concepts":"","body":"Use this guide when a request is denied, unexpectedly allowed, missing audit evidence, or routed to the wrong protected resource. For production incidents and dependency failures, use [Troubleshoot by Symptom](/operations/troubleshooting/).\n\n## Debug Flow\n\n## Start with the Request ID\n\nFind the request ID from the SDK error, STS response, Gateway response, web console audit event, or application log. Open web console **Audit**, filter by the request ID, and open its decision trace.\n\nThe trace should show:\n\n- application and execution session;\n- requested resource and scopes;\n- policy set version;\n- determining policies;\n- diagnostics;\n- final decision;\n- Gateway result when the request reached Gateway.\n\nIf you do not have a request ID, reproduce the request with your runtime configuration and capture the SDK, STS, or Gateway output.\n\n## Common Authorization Failures\n\n| Symptom                           | Check                                                                                     | Fix                                                                                        |\n| --------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |\n| Exchange is denied                | Active policy does not allow the application, subject, resource, or scopes.               | Update the Rego policy, simulate the denied input, and activate a new policy-set version.  |\n| Scope is missing                  | Resource does not define the scope, or policy allowlist omits it.                         | Add the scope to the resource and policy deliberately.                                     |\n| Wrong resource is evaluated       | App used the wrong resource ID or Gateway header.                                         | Use the resource ID from Console and `X-Caracal-Resource`.                                 |\n| Policy change has no effect       | New policy version is not in the active policy-set version.                               | Create and activate a new policy-set version.                                              |\n| Access continues after revocation | Resource server is not consuming revocation state, or Gateway route is not used.          | Confirm Gateway-mediated routing or shared revocation consumers for adapters.              |\n| Gateway returns 403               | Mandate is expired, missing, revoked, or scoped for another resource.                     | Rerun the workload for a fresh mandate and confirm resource bindings.                      |\n| Upstream succeeds without audit   | Request bypassed Gateway or adapter result audit.                                         | Route through Gateway or emit service-side action-result audit after adapter verification. |\n| No audit event appears            | Request never reached STS/Gateway, wrong zone is selected, or audit ingestion is delayed. | Confirm profile, selected zone, and request ID; refresh audit after a short wait.          |\n\n## Iterate from a Denial\n\nEvery denied decision links to the policy-set version that produced it. The audit explain endpoint reconstructs a redaction-safe policy input for denied decisions:\n\n```ts\nconst trace = await admin.audit.explain(zoneId, requestId)\nconst input = trace.denied[0]?.policy_input\n```\n\nFeed that input into [simulation](/guides/activate-policy-set/) against a candidate policy-set version before activating the fix. Actor and subject claims are never written to audit, so add any claim-dependent fields before simulating.\n\n## Authorization Design Rules\n\n- Allow only the scopes an application and subject need.\n- Keep policies resource-specific instead of allowing broad cross-resource access.\n- Express context-sensitive conditions in policy rather than widening scopes.\n- Revoke active sessions when a subject leaves a workflow or an application no longer needs a resource.\n- Use diagnostics for expected denial paths so request traces explain what to change.\n\n## Related Pages\n\n- [Define Resources and Providers](/guides/resources-providers/)\n- [Author Policy Data](/guides/author-policy/)\n- [Activate a Policy Set](/guides/activate-policy-set/)\n- [Resources and Grants](/concepts/resource-grant/)\n- [Policies and Policy Sets](/concepts/policy/)\n- [Sessions and Revocation](/concepts/sessions-revocation/)"},{"id":"/guides/sdk-typescript","title":"Integrate the TypeScript SDK","url":"https://docs.caracal.run/guides/sdk-typescript/","concepts":"","body":"Use `@caracalai/sdk` in Node applications that need Sessions, delegation, Gateway routing, or Caracal context propagation.\n\n## Install\n\n```bash\nnpm install @caracalai/sdk\n```\n\n## Configure\n\n`new Caracal()` loads exactly the profile named by `CARACAL_CONFIG` when set; otherwise it loads `CARACAL_*` environment variables. It does not search default profile or credential paths, and conflicting credential modes fail at startup. Use `Caracal.fromClientSecret(...)` for complete static credentials supplied in code. Power-user loaders and resolver-backed credentials live in `@caracalai/sdk/advanced`.\n\n## Connect and start a Session\n\n```ts\nimport { Caracal } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\n\nawait caracal.session(async () => {\n  const headers = await caracal.headersAsync()\n\n  await fetch('https://api.example.com/tickets', {\n    headers,\n  })\n})\n```\n\n`session()` creates a Session, binds the Caracal context while the callback runs, and terminates the session when the callback exits.\n\nTo make agents distinguishable in policy and audit, pass `labels`. These become `input.principal.labels`, so several agents under one application stay separable without one application per agent. `labels` are descriptive, for policy and audit, not grants; authority always comes from scopes and Delegation. The Session lifecycle is handled for you: `session()` records a `task` Session, while `startSession()` records a heartbeat-leased `service` Session.\n\n```ts\nawait caracal.session(\n  async () => {\n    const headers = await caracal.headersAsync()\n    await fetch('https://api.example.com/tickets', { headers })\n  },\n  { labels: ['refund-agent'] },\n)\n```\n\nSee [If many agents share one managed application, can policy and audit still tell them apart?](/reference/faq/#faq-008).\n\n## Long-lived Sessions\n\nDaemons and workers that outlive a single request use `startSession()` instead of `session()`. It returns a handle you own: the SDK renews the lease from an independent background timer by default, and you retire the Session with `close()`. Each renewal extends the lease. If heartbeats stop before the lease expires, Coordinator suspends the Session for explicit recovery or termination. The stored protocol lifecycle is `service`; unlike `task`, it is not subject to the wall-clock TTL sweeper.\n\n```ts\nconst svc = await caracal.startSession({ labels: ['billing-worker'] })\ntry {\n  while (running) {\n    await caracal.bind(svc.context, async () => {\n      await doWork(await caracal.headersAsync())\n    }) // lease renews in the background\n  }\n} finally {\n  await svc.close()\n}\n```\n\nThe renewal cadence is controlled by `heartbeatIntervalMs`:\n\n| `heartbeatIntervalMs` | Behavior                                                                                                                                        |\n| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |\n| unset                 | Cadence derives from the server lease: renew at roughly a third of the remaining lease, with jitter. The right default for almost every worker. |\n| positive              | Fixed cadence. Use when you need deterministic renewal timing, for example under test clocks.                                                   |\n| `0` or negative       | No background timer; the lease is renewed only by your own `heartbeat()` calls, and a missed renewal lets the lease lapse.                      |\n\nThe handle exposes `deadlineAt` - the lease deadline the coordinator reported on the last renewal - so a worker can surface its own liveness margin. Because renewal runs on an independent timer, the lease stays current even while your code is blocked on a long `await` (a streaming response, a slow tool).\n\nTransient renewal errors are logged and retried on the next tick rather than crashing the worker. If Coordinator reports the Session permanently gone, the timer stops and `onLeaseLost` fires once so the worker can resign instead of spinning. An expired lease instead suspends the Session and reports `suspended` through `onStateChange`.\n\nThe handle's `status` follows Coordinator heartbeat responses. If it becomes `suspended`, automatic heartbeat stops and `onStateChange` reports the transition. Stop taking work, resolve the cause, resume the Session through the control plane, and call `attachSession()` to restart lease renewal. `onLeaseLost` remains reserved for terminal loss.\n\n```ts\nconst svc = await caracal.startSession({\n  labels: ['voice-worker'],\n  onLeaseLost: () => shutdown(),\n})\n```\n\nA renewal cannot run while the event loop is blocked synchronously; in that case the lease correctly lapses, which is the liveness signal working as intended.\n\nA worker that restarts does not need a fresh session: persist `svc.sessionId` and re-attach with `attachSession()`. It validates the session with an immediate lease renewal - a session the coordinator no longer holds live fails with `CoordinatorError`, telling the worker to start a new one - and returns the same handle `startSession()` does:\n\n```ts\nconst svc = await caracal.attachSession(persistedSessionId, {\n  onLeaseLost: () => shutdown(),\n})\n```\n\nThe rebuilt context carries the Session identity only; Delegations bound by the previous holder are re-presented with `acceptDelegation()`.\n\n## Start a narrowed child\n\n```ts\nimport { Authority } from '@caracalai/sdk'\n\nawait caracal.session(async () => {\n  await caracal.session(\n    async () => {\n      const headers = await caracal.headersAsync()\n      await fetch('https://api.example.com/tickets', { headers })\n    },\n    {\n      authority: Authority.narrow(['tickets:read'], {\n        resourceId: 'https://api.example.com/tickets',\n        constraints: { maxHops: 1 },\n        ttlSeconds: 600,\n      }),\n    },\n  )\n})\n```\n\nA plain `session()` runs the child under its parent's effective authority - the application's authority for a root parent, or the p"},{"id":"/guides/sdk-python","title":"Integrate the Python SDK","url":"https://docs.caracal.run/guides/sdk-python/","concepts":"","body":"Use `caracalai-sdk` in Python services and agents that need async Session lifecycle, Delegation, Gateway routing, or ASGI context propagation.\n\n## Install\n\n```bash\npip install caracalai-sdk\n```\n\n## Connect\n\n```python\nfrom caracalai import Caracal\n\ncaracal = Caracal()\n```\n\n`Caracal()` loads exactly the profile named by `CARACAL_CONFIG` when set; otherwise it loads `CARACAL_*` environment variables. It does not search default profile or credential paths, and conflicting credential modes fail at startup. Use `Caracal.from_client_secret(...)` for complete static credentials supplied in code. Power-user loaders and resolver-backed credentials live in `caracalai.advanced`.\n\n## Start a Session and call a resource\n\n```python\nimport httpx\nfrom caracalai import Caracal\n\ncaracal = Caracal()\n\nasync with caracal.session():\n    headers = caracal.headers()\n    async with httpx.AsyncClient() as client:\n        await client.get(\"https://api.example.com/tickets\", headers=headers)\n```\n\n`session()` is an async context manager. It binds the agent context inside the block and releases the session when the block exits.\n\nTo make AI-agent executions distinguishable in policy and audit, pass `labels` when starting a Session. These become `input.principal.labels`, so several agents under one application stay separable without one application per agent. Labels describe work; scopes and Delegations bound authority. `session()` retires the task Session when the block exits, while `start_session()` starts a heartbeat-leased long-lived Session whose protocol lifecycle is `service`.\n\n```python\nasync with caracal.session(labels=[\"refund-agent\"]):\n    headers = caracal.headers()\n```\n\nSee [If many agents share one managed application, can policy and audit still tell them apart?](/reference/faq/#faq-008).\n\n## Long-lived Sessions\n\nDaemons and workers that outlive a single request use `start_session()` instead of `session()`. It returns a handle you own: the SDK renews the lease from an independent background task by default, and you retire the Session with `aclose()`. If heartbeats stop before the lease expires, Coordinator suspends the Session for explicit recovery or termination. The stored protocol lifecycle is `service`; unlike `task`, it is not subject to the wall-clock TTL sweeper.\n\n```python\nsvc = await caracal.start_session(labels=[\"billing-worker\"])\ntry:\n    while running:\n        async with caracal.bind(svc.context):\n            await do_work(caracal.headers())\nfinally:\n    await svc.aclose()\n```\n\nNarrowing requires an active parent. For a long-lived hierarchy, bind a parent service handle while starting its narrowed service child. The child context carries the edge, and closing each handle retires its own Session.\n\n```python\nparent = await caracal.start_session(labels=[\"billing-orchestrator\"])\nasync with caracal.bind(parent.context):\n    svc = await caracal.start_session(\n        labels=[\"billing-worker\"],\n        authority=Authority.narrow(\n            [\"ledger:read\"],\n            ttl_seconds=600,\n            resource_id=\"resource://ledger\",\n        ),\n    )\n```\n\nThe renewal cadence follows the server lease, renewing at roughly a third of the remaining lease with jitter. Pass a positive `heartbeat_interval` to fix the cadence, or `0` to disable the background task and call `heartbeat()` yourself. Because renewal runs on an independent task, the lease stays current even while your code is blocked on a long `await` (a streaming response, a slow tool).\n\nTransient renewal errors are logged and retried on the next tick rather than crashing the worker. If Coordinator reports the Session permanently gone, the task stops and `on_lease_lost` fires once so the worker can resign instead of spinning. An expired lease instead suspends the Session and reports `suspended` through `on_state_change`.\n\nThe handle's `status` follows Coordinator heartbeat responses. If it becomes `suspended`, automatic heartbeat stops and `on_state_change` reports the transition. Stop taking work, resolve the cause, resume the Session through the control plane, and call `attach_session()` to restart lease renewal. `on_lease_lost` remains reserved for terminal loss.\n\n```python\nsvc = await caracal.start_session(\n    labels=[\"voice-worker\"],\n    on_lease_lost=lambda exc: shutdown(),\n)\n```\n\nA renewal cannot run while the event loop is blocked synchronously (CPU-bound work with no `await`); in that case the lease correctly lapses, which is the liveness signal working as intended.\n\nA worker that restarts does not need a fresh session: persist `svc.session_id` and re-attach with `attach_session()`. It validates the session with an immediate lease renewal - a session the coordinator no longer holds live fails with `CoordinatorError`, telling the worker to start a new one - and returns the same handle `start_session()` does:\n\n```python\nsvc = await caracal.attach_session(\n    persisted_session_id,\n    on_lease_lost=lambda exc: shutdown(),\n)\n```\n\nThe rebuilt context carries the Session identity only; Delegations bound by the previous holder are re-presented with `accept_delegation()`.\n\n## Start a narrowed child\n\n```python\nfrom caracalai import Authority, DelegationConstraints\n\nasync with caracal.session() as parent:\n    async with caracal.session(\n        parent_ctx=parent,\n        authority=Authority.narrow(\n            [\"tickets:read\"],\n            resource_id=\"https://api.example.com/tickets\",\n            constraints=DelegationConstraints(max_hops=1),\n            ttl_seconds=600,\n        ),\n    ):\n        headers = caracal.headers()\n```\n\nA plain `caracal.session()` runs the child under its parent's effective authority - the application's authority for a root parent, or the parent's narrowed slice when the parent was itself narrowed (transitive least-privilege). Pass `authority=Authority.narrow(...)` only when the child should hold a smaller subset, or `authority=Authority.none()` for a child with no inherited authority. Use `await caracal.delegate(...)` whe"},{"id":"/guides/sdk-go","title":"Integrate the Go SDK","url":"https://docs.caracal.run/guides/sdk-go/","concepts":"","body":"Use the Go SDK in services and agents that need Caracal context propagation, Sessions, delegation, and Gateway-aware HTTP clients.\n\n## Install\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/sdk/go\n```\n\n## Connect\n\n```go\npackage main\n\nimport (\n\t\"context\"\n\t\"log\"\n\n\tcaracal \"github.com/garudex-labs/caracal/packages/sdk/go\"\n)\n\nfunc main() {\n\tclient, err := caracal.New()\n\tif err != nil {\n\t\tlog.Fatal(err)\n\t}\n\n\tif err := client.Session(context.Background(), func(ctx context.Context) error {\n\t\theaders, err := client.Headers(ctx)\n\t\tif err != nil {\n\t\t\treturn err\n\t\t}\n\t\t_ = headers\n\t\treturn nil\n\t}); err != nil {\n\t\tlog.Fatal(err)\n\t}\n}\n```\n\n`New()` loads exactly the profile named by `CARACAL_CONFIG` when set; otherwise it loads `CARACAL_*` environment variables. It does not search default profile or credential paths.\n\n## Use an HTTP client\n\n```go\nhttpClient := client.Transport(nil)\n\nerr := client.Session(context.Background(), func(ctx context.Context) error {\n\treq, err := http.NewRequestWithContext(ctx, http.MethodGet, \"https://api.example.com/tickets\", nil)\n\tif err != nil {\n\t\treturn err\n\t}\n\t_, err = httpClient.Do(req)\n\treturn err\n})\n```\n\nThe transport injects Caracal envelope headers from `context.Context`. For explicit Gateway routing, use `GatewayRequest()`.\n\n## Long-lived long-lived Sessions\n\nDaemons and workers that outlive a single request use `StartSession` instead of `Session`. It returns a handle you own: the SDK renews the lease from a background goroutine by default, and you retire the Session with `Close`. A long-lived Session is reaped if it stops heartbeating before its lease expires; its stored protocol lifecycle is `service`.\n\n```go\nsvc, err := client.StartSession(context.Background(), caracal.StartSessionOptions{\n\tLabels: []string{\"billing-worker\"},\n})\nif err != nil {\n\treturn err\n}\ndefer svc.Close(context.Background())\n\nfor running {\n\tdoWork(svc.Context) // lease renews in the background\n}\n```\n\nThe renewal cadence follows the server lease, renewing at roughly a third of the remaining lease with jitter. Set `StartSessionOptions.HeartbeatInterval` to a positive duration to fix the cadence, or a negative one to disable the background goroutine and call `Heartbeat` yourself. Transient renewal errors are logged and retried on the next tick; if the coordinator reports the session permanently gone, the goroutine stops and `StartSessionOptions.OnLeaseLost` fires once so the worker can resign instead of spinning.\n\n`Status()` follows Coordinator heartbeat responses. If it becomes `suspended`, automatic heartbeat stops and `OnStateChange` reports the transition. Stop taking work, resolve the cause, resume the Session through the control plane, and call `AttachSession` to restart lease renewal. `OnLeaseLost` remains reserved for terminal loss.\n\nA worker that restarts does not need a fresh session: persist `svc.SessionID()` and re-attach with `AttachSession`. It validates the session with an immediate lease renewal - a session the coordinator no longer holds live fails with `*CoordinatorError`, telling the worker to start a new one - and returns the same handle `StartSession` does:\n\n```go\nsvc, err := client.AttachSession(ctx, persistedSessionID, caracal.AttachSessionOptions{\n\tOnLeaseLost: func(error) { shutdown() },\n})\n```\n\nThe rebuilt context carries the Session identity only; Delegations bound by the previous holder are re-presented with `AcceptDelegation`.\n\n## Start a narrowed child\n\n```go\nerr := client.Session(context.Background(), func(ctx context.Context) error {\n\treturn client.Session(ctx, func(child context.Context) error {\n\t\t_, _ = client.Current(child)\n\t\treturn nil\n\t}, caracal.SessionOptions{\n\t\tAuthority: caracal.AuthorityNarrow([]string{\"tickets:read\"}, caracal.NarrowOptions{\n\t\t\tConstraints: &caracal.DelegationConstraints{MaxHops: 1},\n\t\t\tTTLSeconds:  600,\n\t\t}),\n\t})\n})\n```\n\nA plain `client.Session` runs the child under its parent's effective authority - the application's authority for a root parent, or the parent's narrowed slice when the parent was itself narrowed (transitive least-privilege). Set `SessionOptions.Authority` to `caracal.AuthorityNarrow([]string{...})` only when the child should hold a least-privilege subset, or `caracal.AuthorityNone()` for a child with no inherited authority. Use `client.Delegate` to grant authority to a Session that already exists, typically in another application: it returns the edge, and the receiving session presents it with `client.AcceptDelegation(ctx, edgeID)`. Use `Current(ctx)` to inspect the bound Caracal context and `Headers(ctx)` to project it to outbound HTTP headers.\n\n## Troubleshooting\n\n| Symptom                         | Check                                                                                        |\n| ------------------------------- | -------------------------------------------------------------------------------------------- |\n| Missing config error            | Confirm the runtime profile or required environment variables.                               |\n| `Headers` without context fails | Call inside `Session`, `Delegate`, or pass `CallOptions{AsApplication: true}` intentionally. |\n| Delegation fails                | Ensure delegation runs from a context with an active Session.                                |\n| Gateway URL error               | Confirm the runtime profile includes `gateway_url`.                                          |\n\nRelated pages: [Protect a Go net/http Service](/guides/protect-nethttp/) and [Session Delegation](/concepts/delegation/)."},{"id":"/guides/runtime-run","title":"Run an Agent with caracal run","url":"https://docs.caracal.run/guides/runtime-run/","concepts":"","body":"`caracal run` starts a local subprocess with scoped, short-lived credentials injected as environment variables. The workload carries only its workload ID and secret; the credential bindings live in the web console. Use it for development, demos, and controlled local runs.\n\n## Prepare the workload\n\n1. Run `caracal up`.\n2. Sign in to the web console and use **Guided setup** to create the zone, provider, resource, and policy.\n3. On the console's **Launcher** page, create a workload. Store its secret in the owner-only file at `<Caracal config dir>/runtime/<workload_id>/secret`, or export `CARACAL_WORKLOAD_SECRET`; the secret stays retrievable from the Launcher page, with every reveal audited.\n4. On the same page, bind an environment variable to each resource the workload needs and select the scopes each credential should carry. The page then shows the exact launch commands. See [Configure Workloads](/runtime-console/config-file/).\n\n## Run a command\n\n```bash\nexport CARACAL_WORKLOAD_ID=<workload_id>\ncaracal run -- npm start\n```\n\nThe launcher fetches the workload's launch bindings from STS, mints a short-lived credential for each binding under only its selected scopes, and injects the results into the configured environment variables. The child environment is otherwise scrubbed: `CARACAL_*` configuration variables stay with the launcher, and only a small allowlist such as `PATH`, `HOME`, locale, and `XDG_*` directories is inherited. Credentials are minted once at launch and never renewed; long-running workloads should use an SDK. If policy requires step-up approval for a binding, the launch pauses and emits an `approval_required` line on stderr until the challenge is decided. See [Run Workloads](/runtime-console/runtime/) for the full contract.\n\n## Validate the run\n\n| Check | Command or surface |\n| --- | --- |\n| Runtime is ready | `caracal status --ready` |\n| Credential is injected | `caracal run -- printenv OPENAI_API_KEY` (use your configured `env` name) |\n| First request succeeds | Send a request to a configured resource path. |\n| Audit captured the request | web console **Audit**. |\n\n## Troubleshooting\n\n| Symptom | Fix |\n| --- | --- |\n| `workload identity not found` | Set `CARACAL_WORKLOAD_ID` and a workload secret source. |\n| `invalid workload credentials` | The workload ID or secret is wrong or was rotated; copy the current values from the **Launcher** page. |\n| `no credential bindings configured` | Define launch bindings for this workload on the console's **Launcher** page. |\n| Secret file rejected | Restrict file permissions and avoid setting both inline and file secrets. |\n| Launch pauses on `approval_required` | Approve the step-up challenge in the web console, or adjust the policy tier. |\n| Request denied | Check policy-set activation, scopes, and audit diagnostics. |\n\nRelated pages: [First Protected Call](/get-started/first-protected-call/), [Add SDK to Your App](/get-started/add-sdk-to-your-app/), and [Audit and Request Traces](/concepts/audit-ledger/)."},{"id":"/guides/protect-gateway-http","title":"Protect a Gateway-Routed HTTP API","url":"https://docs.caracal.run/guides/protect-gateway-http/","concepts":"","body":"Use Gateway-routed protection when the upstream is HTTP-routable and you want Caracal to enforce every request before it reaches the target. The agent receives a short-lived Caracal mandate; Gateway verifies it, resolves the resource route, attaches the configured upstream credential when needed, forwards the request, and writes action-result audit.\n\n## When to Use Gateway\n\n| Use Gateway when                                                               | Use another guide when                                                      |\n| ------------------------------------------------------------------------------ | --------------------------------------------------------------------------- |\n| The upstream is HTTP, REST, MCP-over-HTTP, or another Gateway-routable target. | The service must verify mandates inside its own process.                    |\n| You want provider credentials to stay inside the trusted Gateway/STS boundary. | The application must call the provider directly and only needs attribution. |\n| You want action-result audit from the central Gateway.                         | The resource server owns result audit after adapter verification.           |\n| Responses are request/response or streamed HTTP, such as SSE.                  | The upstream requires WebSocket connections, which Gateway does not proxy.  |\n\nFor in-process enforcement, use [Protect an Express App](/guides/protect-express/), [Protect a FastMCP App](/guides/protect-fastmcp/), [Protect a Go net/http Service](/guides/protect-nethttp/), or [Protect an MCP Server](/guides/protect-mcp/).\n\n## Create or Select the Resource\n\nOpen the web console:\n\n```sh\ncaracal web\n```\n\nCreate or select a resource with:\n\n| Field                        | Guidance                                                                                                            |\n| ---------------------------- | ------------------------------------------------------------------------------------------------------------------- |\n| Resource identifier          | Stable audience URI, such as `resource://billing-api`.                                                              |\n| Scopes                       | Action-oriented scopes, such as `billing:read` and `billing:submit`.                                                |\n| Upstream URL                 | Network URL the Gateway can reach.                                                                                  |\n| Gateway application          | The Caracal application identity Gateway uses for upstream exchanges.                                               |\n| Upstream credential provider | The provider record Gateway uses to attach no credential, a Caracal mandate, OAuth token, API key, or bearer token. |\n\nKeep the resource identifier stable even when upstream hosts or provider bindings change.\n\n## Choose Provider Auth Mode\n\n| Provider type            | Use when                                                                |\n| ------------------------ | ----------------------------------------------------------------------- |\n| None                     | Gateway enforces Caracal access and the upstream expects no credential. |\n| Caracal mandate          | The upstream verifies Caracal mandates directly.                        |\n| OAuth client credentials | Gateway needs a machine OAuth token.                                    |\n| OAuth authorization code | Gateway needs a connected upstream account (delegated OAuth consent).   |\n| API key                  | Gateway attaches a sealed static API key in a configured header.        |\n| Bearer token             | Gateway attaches a sealed pre-issued bearer token.                      |\n\nUse [Define Resources and Providers](/guides/resources-providers/) for field definitions and [Provider Recipes](/guides/provider-recipes/) for concrete OpenAI, Google, GitHub, Slack, and internal API examples.\n\n## Route Calls Through Gateway\n\nGateway-routed requests include:\n\n| Request part         | Value                                                                               |\n| -------------------- | ----------------------------------------------------------------------------------- |\n| URL                  | Gateway URL from your runtime profile (`http://localhost:8081` on the local stack). |\n| `Authorization`      | `Bearer <Caracal mandate>`.                                                         |\n| `X-Caracal-Resource` | Resource identifier.                                                                |\n| Path                 | The protected path you configured for the upstream.                                 |\n\nSDKs can build the Gateway request and inject headers for you:\n\n- TypeScript: `caracal.gatewayRequest()` and `caracal.transport()`\n- Python: `caracal.gateway_request()` and `caracal.transport()`\n- Go: `GatewayRequest()` and `Transport()`\n\n## Verify Authorization and Audit\n\nRun these checks before treating the route as ready:\n\n| Check                 | Expected result                                               |\n| --------------------- | ------------------------------------------------------------- |\n| Allowed request       | Gateway forwards to upstream and records action-result audit. |\n| Missing mandate       | Gateway rejects the request.                                  |\n| Missing scope         | STS or Gateway rejects the request.                           |\n| Wrong resource header | Gateway rejects or routes to a different configured resource. |\n| Revoked session       | Gateway rejects the old mandate.                              |\n\nOpen the web console **Audit** and filter by the request ID. A complete Gateway-routed call has both authorization evidence from STS and action-result evidence from Gateway.\n\n## Troubleshooting\n\n| Symptom                                                       | Check                                                                    |\n| ---------------------------------------------------"},{"id":"/guides/protect-express","title":"Protect an Express App","url":"https://docs.caracal.run/guides/protect-express/","concepts":"","body":"Use `@caracalai/express` when an Express app should verify Caracal mandates before route handlers run.\n\n## Install\n\n```bash\nnpm install express @caracalai/express @caracalai/verify @caracalai/revocation\n```\n\nUse a Redis-backed revocation store in production. The in-memory store is only suitable for local development and tests.\n\n## Add middleware\n\n```ts\nimport express from 'express'\nimport { caracalAuth, type CaracalRequest } from '@caracalai/express'\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst app = express()\n\nconst verifier = createMandateVerifier({\n  issuer: process.env.CARACAL_ISSUER!,\n  audience: process.env.CARACAL_AUDIENCE!,\n  zoneId: process.env.CARACAL_ZONE_ID!,\n  revocations: new InMemoryRevocationStore(),\n})\n\nawait verifier.warmup()\n\napp.use(\n  '/mcp',\n  caracalAuth(\n    { verifier },\n    {\n      requiredScopes: ['mcp:tool:call'],\n      requiredTargets: ['https://mcp.example.com'],\n    },\n  ),\n)\n\napp.post('/mcp/tools/list', (req: CaracalRequest, res) => {\n  res.json({\n    principal: req.caracalClaims?.sub,\n    tools: ['search', 'summarize'],\n  })\n})\n```\n\nThe middleware attaches verified claims to `req.caracal` and `req.caracalClaims`, plus the propagation context at `req.caracalContext`.\n\n## Enforce the right boundary\n\n| Option              | Use it for                        |\n| ------------------- | --------------------------------- |\n| `requiredScopes`    | Tool or route-level scope checks. |\n| `requiredTargets`   | Resource-target checks.           |\n| `requireSession`    | Require a governed Session.       |\n| `requireDelegation` | Require delegated authority.      |\n| `maxHopCount`       | Limit delegation depth.           |\n\n## Validate\n\n1. Exchange for a mandate that targets the resource.\n2. Call the protected route with `Authorization: Bearer <mandate>`.\n3. Remove a required scope and confirm the route returns `403`.\n4. Revoke the session and confirm the route rejects the old mandate.\n\nRelated pages: [Mandates](/concepts/mandate/) and [Sessions and Revocation](/concepts/sessions-revocation/)."},{"id":"/guides/protect-fastmcp","title":"Protect a FastMCP App","url":"https://docs.caracal.run/guides/protect-fastmcp/","concepts":"","body":"Use the FastMCP adapter when a FastMCP app should reject invalid, expired, insufficient, or revoked Caracal mandates. Python uses `caracalai-fastmcp`; TypeScript uses `@caracalai/fastmcp`.\n\n## Install\n\n```bash\npip install \"caracalai-fastmcp[fastmcp]\" caracalai-revocation\n```\n\n```bash\nnpm install @caracalai/fastmcp @caracalai/verify @caracalai/revocation\n```\n\nUse `caracalai-revocation-redis` (Python) or the Redis store in `@caracalai/revocation` (TypeScript) for shared production revocation. The in-memory store is only for local development and tests.\n\n## Create an authenticator (Python)\n\n```python\nfrom caracalai_fastmcp import CaracalAuth\nfrom caracalai_revocation import InMemoryRevocationStore\n\nauth = CaracalAuth(\n    issuer=\"https://sts.example.com\",\n    audience=\"https://mcp.example.com\",\n    expected_zone_id=\"zone_prod\",\n    required_scopes=[\"mcp:tool:call\"],\n    required_targets=[\"https://mcp.example.com\"],\n    revocations=InMemoryRevocationStore(),\n    require_session=True,\n)\n\nasync def startup():\n    await auth.warmup()\n```\n\n## Verify before running a tool (Python)\n\n```python\nfrom caracalai_fastmcp import CaracalAuthError\n\nasync def handle_tool_call(token: str, payload: dict):\n    try:\n        claims = await auth(token)\n    except CaracalAuthError as err:\n        return {\"error\": err.code, \"error_description\": err.description, \"error_hint\": err.hint}\n\n    return {\n        \"subject\": claims.sub,\n        \"result\": await run_tool(payload),\n    }\n```\n\nWire this check into the FastMCP auth or request hook used by your server. The important boundary is that the mandate is verified before the tool handler performs work.\n\n## Verify before running a tool (TypeScript)\n\n```ts\nimport { extractBearer, verifyFastMcpToken, FastMcpAuthError } from '@caracalai/fastmcp'\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.example.com',\n  audience: 'https://mcp.example.com',\n  zoneId: 'zone_prod',\n  revocations: new InMemoryRevocationStore(),\n})\n\nasync function handleToolCall(authorization: string, payload: unknown) {\n  const token = extractBearer(authorization)\n  if (!token) return { error: 'missing_token' }\n\n  try {\n    const context = await verifyFastMcpToken(token, verifier, {\n      requiredScopes: ['mcp:tool:call'],\n      requiredTargets: ['https://mcp.example.com'],\n      requireSession: true,\n    })\n    return { subject: context.sub, result: await runTool(payload) }\n  } catch (err) {\n    if (err instanceof FastMcpAuthError) return { error: err.code }\n    throw err\n  }\n}\n```\n\nThe boundary is the same in both languages: verify the mandate before the tool handler performs work.\n\n## Validate\n\n| Test                                                             | Expected result                   |\n| ---------------------------------------------------------------- | --------------------------------- |\n| Missing bearer token                                             | `missing_token` or framework 401. |\n| Wrong audience                                                   | `invalid_token`.                  |\n| Missing scope                                                    | `insufficient_scope`.             |\n| Revoked session                                                  | `session_revoked`.                |\n| Mandate without a Session with `require_session=True` / `requireSession: true` | `session_required`.               |\n\nRelated pages: [FastMCP Adapter](/sdks/adapters/fastmcp/), [Protect an MCP Server](/guides/protect-mcp/), and [Human Approval](/guides/step-up/)."},{"id":"/guides/protect-nethttp","title":"Protect a Go net/http Service","url":"https://docs.caracal.run/guides/protect-nethttp/","concepts":"","body":"Use the Go net/http adapter when a Go service should verify Caracal mandates at the handler boundary.\n\n## Install\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/adapters/nethttp/go\ngo get github.com/garudex-labs/caracal/packages/revocation/go\n```\n\n## Wrap a handler\n\n```go\npackage main\n\nimport (\n\t\"encoding/json\"\n\t\"net/http\"\n\t\"time\"\n\n\tnethttp \"github.com/garudex-labs/caracal/packages/adapters/nethttp/go\"\n\trevocation \"github.com/garudex-labs/caracal/packages/revocation/go\"\n\tverify \"github.com/garudex-labs/caracal/packages/verify/go\"\n)\n\nfunc main() {\n\trevocations := revocation.NewInMemoryStore(24 * time.Hour)\n\n\tverifier := verify.NewVerifier(verify.Options{\n\t\tIssuer:      \"https://sts.example.com\",\n\t\tAudience:    \"https://api.example.com\",\n\t\tZoneID:      \"zone_prod\",\n\t\tRevocations: revocations,\n\t})\n\n\tprotected := nethttp.VerifierMiddleware(verifier.Require(verify.Options{\n\t\tRequiredScopes:  []string{\"tickets:read\"},\n\t\tRequiredTargets: []string{\"https://api.example.com/tickets\"},\n\t}))(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n\t\tclaims, ok := nethttp.ClaimsFromContext(r.Context())\n\t\tif !ok {\n\t\t\thttp.Error(w, \"missing claims\", http.StatusUnauthorized)\n\t\t\treturn\n\t\t}\n\t\t_ = json.NewEncoder(w).Encode(map[string]string{\"subject\": claims.Sub})\n\t}))\n\n\thttp.Handle(\"/tickets\", protected)\n\t_ = http.ListenAndServe(\":8080\", nil)\n}\n```\n\n## Enforce constraints\n\n| Option                 | Use it for                     |\n| ---------------------- | ------------------------------ |\n| `RequiredScopes`       | Route or operation permission. |\n| `RequiredTargets`      | Resource target matching.      |\n| `RequireSession`       | Session-bound endpoints.       |\n| `RequireDelegation`    | Delegated-only endpoints.      |\n| `RequireChainContains` | Application path requirements. |\n| `MaxHopCount`          | Delegation depth limit.        |\n\n## Production revocation\n\nThe in-memory store does not share revocations across instances. Use the Redis revocation backend and consume `caracal.sessions.revoke` for production resource servers.\n\n## Validate\n\n1. Call without a bearer token and expect `401`.\n2. Call with a valid mandate and expect the handler response.\n3. Remove a required scope and expect `403`.\n4. Mark the session revoked and expect `session_revoked`.\n\nRelated pages: [Mandates](/concepts/mandate/) and [Sessions and Revocation](/concepts/sessions-revocation/)."},{"id":"/guides/protect-mcp","title":"Protect an MCP Server","url":"https://docs.caracal.run/guides/protect-mcp/","concepts":"","body":"Use the verify packages when your MCP framework does not have a dedicated Caracal adapter or when you want to build your own boundary.\n\n## TypeScript verifier\n\n```bash\nnpm install @caracalai/verify @caracalai/revocation\n```\n\n```ts\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.example.com',\n  audience: 'https://mcp.example.com',\n  zoneId: 'zone_prod',\n  revocations: new InMemoryRevocationStore(),\n})\n\nexport async function verifyToolRequest(authorization: string | undefined) {\n  const result = await verifier.authorization(authorization, {\n    requiredScopes: ['mcp:tool:call'],\n    requiredTargets: ['https://mcp.example.com'],\n    requireSession: true,\n  })\n\n  if (!result.ok) {\n    throw new Error(`${result.error.code}: ${result.error.description}`)\n  }\n\n  return result.principal\n}\n```\n\n## Python verifier\n\n```bash\npip install caracalai-verify caracalai-revocation\n```\n\n```python\nfrom caracalai_verify import authenticate, extract_bearer\nfrom caracalai_revocation import InMemoryRevocationStore\n\nrevocations = InMemoryRevocationStore()\n\nasync def verify_tool_request(authorization: str | None):\n    token = extract_bearer(authorization)\n    result = await authenticate(\n        token or \"\",\n        issuer=\"https://sts.example.com\",\n        audience=\"https://mcp.example.com\",\n        required_scopes=[\"mcp:tool:call\"],\n        expected_zone_id=\"zone_prod\",\n        revocations=revocations,\n        require_session=True,\n        required_targets=[\"https://mcp.example.com\"],\n    )\n    if result.error is not None:\n        raise RuntimeError(f\"{result.error.code}: {result.error.description}\")\n    return result.principal\n```\n\n## Verification checklist\n\n| Check                            | Why it matters                                             |\n| -------------------------------- | ---------------------------------------------------------- |\n| Issuer and audience              | Prevents accepting mandates from the wrong zone or target. |\n| Required scopes                  | Enforces tool-level authority.                             |\n| Required targets                 | Prevents cross-resource token reuse.                       |\n| Session or Delegation requirements | Keeps application-root and delegated calls separate.       |\n| Revocation store                 | Rejects revoked sessions and Delegations.                  |\n\n## Production revocation\n\nUse Redis-backed revocation packages for multi-instance MCP servers:\n\n- TypeScript: `@caracalai/revocation-redis`\n- Python: `caracalai-revocation-redis`\n- Go: `github.com/garudex-labs/caracal/packages/backends/redis/go`\n\nRun a consumer for the `caracal.sessions.revoke` stream so every resource server instance learns about revoked anchors.\n\nRelated pages: [Protect an Express App](/guides/protect-express/) and [Protect a FastMCP App](/guides/protect-fastmcp/)."},{"id":"/guides/audit-stream","title":"Tail and Query the Audit Stream","url":"https://docs.caracal.run/guides/audit-stream/","concepts":"","body":"Use audit when a request is denied, step-up is triggered, delegation behaves unexpectedly, or an operator needs evidence for a run.\n\n## Web Console Workflow\n\n1. Open `caracal web`.\n2. Select **Audit**.\n3. Filter by zone, decision, event type, or request ID.\n4. Open the event detail to inspect metadata, determining policies, and diagnostics.\n5. Open the decision trace from the event for a full request-level view.\n\n## Automation workflow\n\n```ts\nimport { AdminClient } from '@caracalai/admin'\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n})\n\nconst events = await admin.audit.list(process.env.CARACAL_ZONE_ID!, {\n  decision: 'deny',\n  limit: 25,\n})\n\nconst trace = await admin.audit.explain(process.env.CARACAL_ZONE_ID!, events[0].request_id!)\nconsole.log(trace.final_decision, trace.denied)\n```\n\n## Useful filters\n\n| Filter      | Use it for                                                       |\n| ----------- | ---------------------------------------------------------------- |\n| Request ID  | Stitch together STS, Gateway, adapter, and explain events.       |\n| Decision    | Find denies, allows, or partial decisions.                       |\n| Event type  | Focus on policy, session, delegation, step-up, or admin changes. |\n| Time window | Investigate a run, incident, or rollout.                         |\n\n## Ship events to a SIEM\n\nPoll `admin.audit.list` with a `since` cursor (or subscribe to the audit stream topics) and forward events as-is: the shape is stable JSON, ready for field mapping in Splunk, Elastic, or any OCSF pipeline.\n\n| Field                         | Type           | Meaning                                                                           |\n| ----------------------------- | -------------- | --------------------------------------------------------------------------------- |\n| `id`                          | string         | Unique event id; use as the dedup key.                                            |\n| `zone_id`                     | string         | Zone the event belongs to.                                                        |\n| `event_type`                  | string         | What happened: exchange, decision, replay_detected, revocation, lifecycle events. |\n| `request_id`                  | string \\| null | Correlates every event from one request across STS, Gateway, and adapters.        |\n| `decision`                    | string \\| null | `allow`, `deny`, or `partial` for decision-bearing events.                        |\n| `evaluation_status`           | string \\| null | How policy evaluation concluded.                                                  |\n| `metadata_json`               | object \\| null | Event-specific attributes: session, resource, scopes, client identity.            |\n| `occurred_at` / `ingested_at` | string         | Event time and audit-service ingest time (RFC 3339).                              |\n\nEvent detail (`admin.audit.explain`) adds the determining policies, policy-set version, manifest hash, and diagnostics for decision events - fetch it lazily from the SIEM for alerts rather than shipping it wholesale. Alert first on `decision: \"deny\"` spikes, `replay_detected`, and delegation events for sessions outside your expected label set.\n\n## What to capture in incidents\n\n- request ID;\n- zone ID;\n- application ID;\n- resource identifier;\n- requested scopes;\n- final decision;\n- determining policies;\n- diagnostics;\n- session or Delegation IDs when present.\n\nRelated page: [Audit and Request Traces](/concepts/audit-ledger/)."},{"id":"/guides/delegation","title":"Implement Multi-Agent Delegation","url":"https://docs.caracal.run/guides/delegation/","concepts":"","body":"Use Delegation when one Session needs to hand a narrower slice of authority to another Session. The SDK creates the Delegation and carries the context; the web console shows the graph and impact.\n\n`session()` always starts the child under the **same application** as the parent. Narrow the child's authority only when it should hold less than the parent. To hand authority across applications, use `delegate()` with a peer Session under the other application; it returns the Delegation, and the receiver presents it with `acceptDelegation()`.\n\n## Implementation flow\n\n## TypeScript example\n\n```ts\nimport { Caracal, Authority } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\n\nawait caracal.session(async () => {\n  await caracal.session(\n    async () => {\n      const headers = await caracal.headersAsync()\n      await fetch('https://api.example.com/tickets', { headers })\n    },\n    {\n      authority: Authority.narrow(['tickets:read'], {\n        resourceId: 'https://api.example.com/tickets',\n        constraints: { maxHops: 1 },\n        ttlSeconds: 600,\n      }),\n    },\n  )\n})\n```\n\n## Review the graph\n\n1. Open `caracal web`.\n2. Select **Delegation**.\n3. Inspect active edges, inbound edges, outbound edges, and traversal.\n4. Use impact before revoking an edge.\n5. Check **Audit** for the delegated exchange and resource decision.\n\n## Hand Over the Delegation ID Safely\n\nThe issuer and receiver share the opaque Delegation ID over their authenticated work channel, such as a queue message, RPC field, or task payload. Creation is only an offer and does not mutate receiver context. Possessing and presenting this target-Session-bound ID through `acceptDelegation()` is receiver consent; STS also checks the receiving application and live target Session. Use `acceptDelegation(delegationId, fn, { validate: true })` to confirm that exact ID with Coordinator before work runs under it. Every presentation emits a `delegation.accept` event.\n\n## Safe constraints\n\n| Constraint | Good default                              |\n| ---------- | ----------------------------------------- |\n| Scopes     | Small subset of parent authority.         |\n| TTL        | Required; use minutes, not days.          |\n| Hop count  | `1` unless a deeper graph is intentional. |\n| Resource   | Single resource whenever possible.        |\n\n## Troubleshooting\n\n| Symptom                               | Check                                                                |\n| ------------------------------------- | -------------------------------------------------------------------- |\n| `Delegate requires an active Session` | Call delegation inside `session()` or a bound Caracal context.       |\n| Resource denies delegated request     | Confirm the edge, ancestry, policy grant, and required scopes remain live. |\n| Chain validation fails                | Confirm authoritative parent links are continuous; configure `requireChainContains` separately at verifiers when needed. |\n| Revocation did not affect child       | Confirm cascade revocation and resource-server revocation consumers. |\n\nRelated pages: [Session Delegation](/concepts/delegation/) and [Delegation Constraints](/concepts/constraint/)."},{"id":"/guides/step-up","title":"Human Approval","url":"https://docs.caracal.run/guides/step-up/","concepts":"","body":"Human approval pauses a token exchange until a person decides it. A policy data document classifies scopes into risk tiers and declares which tiers need a human decision; when an agent requests a gated scope, the STS parks the exchange on a durable hold and returns `interaction_required` instead of a mandate. The gate is optional: a zone that declares no approval tiers never sees a hold.\n\n## Flow\n\n## Declare the gate in policy data\n\nApproval is declared as data, like every other policy input. `risk` names a tier for each sensitive scope; `approval_tiers` declares which tiers hold a mint for a decision:\n\n```text\n# caracal:data-document\npackage caracal.authz\n\nimport rego.v1\n\nrisk := [\n  {\"scope\": \"pipernet:refund\", \"tier\": \"high\"},\n]\n\napproval_tiers := [\n  {\"tier\": \"high\", \"approver\": \"operator\", \"ttl_seconds\": 1800, \"privacy\": \"identified\"},\n]\n```\n\n| Field         | Meaning                                                                                                                                                            |\n| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `tier`        | Your tier name; the platform fixes no taxonomy. Required - a declaration without a tier fails the mint closed.                                                     |\n| `approver`    | Who may decide: `operator` (approve-capable admin credential), `subject` (the application's own federated end user - see below), or `any`. Defaults to `operator`. |\n| `ttl_seconds` | How long the hold stays decidable, clamped between 60 seconds and 7 days. Defaults to 1800.                                                                        |\n| `privacy`     | How much approver identity the decision record retains: `identified`, `pseudonymous`, or `anonymous`. Defaults to `identified`.                                    |\n\nWhen one mint matches several compatible tiers, the shortest window and most protective privacy mode win, and a specific `operator` or `subject` requirement wins over `any`. A request that combines an operator-only tier with a subject-only tier is denied and must be split into separate mints: one decision cannot satisfy two independent approver roles. Invalid approver, privacy, or TTL declarations fail the mint closed.\n\nTune `ttl_seconds` to the tier's real decision window: it bounds how long an approved-but-unconsumed hold stays spendable, so a shorter TTL shrinks the window in which a leaked approval could be consumed, while a TTL shorter than your approvers' actual response time just converts legitimate requests into expiries. High-value, rarely exercised operations warrant minutes; routine operator flows can keep the 30-minute default.\n\n## Handle the hold in the agent\n\nThe `interaction_required` error carries the approval id, tier, expiry, and an opaque `binding`. The binding covers the principal, Authority record, governed Session, Delegation, application, canonical resource and scope sets, active policy-set manifest, and platform decision contract. A retry after policy or execution context changes cannot spend an earlier decision. The SDK runs the whole flow - catch the hold, wait for the decision, retry with the approval id so the retried mint consumes it - in one call:\n\n```ts\nimport { Caracal } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\n\nconst mandate = await caracal.withApproval((approvalId) => caracal.mintMandate('resource://pipernet', ['pipernet:refund'], { approvalId }))\n```\n\nWhen the wait outcome is anything but `approved` - rejected, expired, already consumed, or the wait timed out - `withApproval` rethrows the original `ApprovalRequiredError`. Its `approvalId` is durable: persist it and resume later with `waitForApproval`, which long-polls and returns the typed final state without re-raising the hold:\n\n```ts\nconst state = await caracal.waitForApproval(persistedApprovalId, { timeoutMs: 600_000 })\n// 'approved'  -> retry the mint with { approvalId } to consume the decision\n// 'pending'   -> nobody has decided; waiting again is safe\n// 'rejected' | 'expired' | 'consumed' -> terminal; re-request the operation\n```\n\nAn approval is **single-use**: the first mint that presents the approval id spends it. When two workers race one approval - a retried queue message, a redeployed worker resuming the same persisted id - one wins and the loser receives the typed `approval_consumed` error. This is not a new `ApprovalRequired` hold and is not an error to wait out: check whether the intended effect already happened before requesting another approval.\n\nThe Python and Go clients expose the same surface as `with_approval`/`wait_for_approval` and `WithApproval`/`WaitForApproval`. The lower-level `@caracalai/oauth` client offers `waitForApproval` directly for integrations that drive the exchange themselves. Under `caracal run` none of this is hand-written: the engine prints an `approval_required` notice with the approval id and binding, waits on the hold, and retries the exchange itself.\n\n## Decide as an operator\n\nOpen the zone's **Approvals** page in the Console to review pending holds - each shows the requesting principal, the tier, the binding to cross-check against the agent's notice, and the approval window - and approve or reject with an optional reason. For automation, the Admin API exposes the same decision:\n\n```bash\ncurl -X POST \\\n  \"$CARACAL_API_URL/v1/zones/$CARACAL_ZONE_ID/step-up-challenges/$CHALLENGE_ID/approve\" \\\n  -H \"Authorization: Bearer $CARACAL_ADMIN_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"reason\": \"PiperNet refund reviewed against baseline v3\"}'\n```\n\nUse `/reject` to settle the hold terminally. Deciding requires an admin token minted with the `approve` capability - `write` alone cannot decide a hold, so day-to-day automation credentials never carry approval authority. The approver is recorded from the authenticated actor, nev"},{"id":"/guides/enterprise-runtime-patterns","title":"Production Integration Patterns","url":"https://docs.caracal.run/guides/enterprise-runtime-patterns/","concepts":"","body":"Production deployments usually combine the Gateway, adapters, SDKs, revocation consumers, audit export, and policy automation. Keep the same authority model, then place enforcement where it best fits the system.\n\n## Pick an Enforcement Pattern\n\n| Pattern                 | Use when                                                              | Primary guide                                                                                                                                                    |\n| ----------------------- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Gateway-routed HTTP     | You can route traffic through Caracal before it reaches the upstream. | [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/)                                                                                               |\n| In-process adapter      | The resource server owns verification inside its framework.           | [Protect an Express App](/guides/protect-express/), [Protect a FastMCP App](/guides/protect-fastmcp/), [Protect a Go net/http Service](/guides/protect-nethttp/) |\n| Verify engine           | You need framework-neutral bearer verification.                       | [Protect an MCP Server](/guides/protect-mcp/)                                                                                                                    |\n| SDK transport injection | Provider SDK accepts custom fetch, transport, or HTTP client hooks.   | [SDK guides](/guides/)                                                                                                                                           |\n| Batch or queue worker   | Work runs outside request/response but still needs scoped mandates.   | [Run an Agent with caracal run](/guides/runtime-run/)                                                                                                            |\n\n## Reference Architecture\n\n## Production Checklist\n\n| Area            | Requirement                                                                                                     |\n| --------------- | --------------------------------------------------------------------------------------------------------------- |\n| Zones           | Separate production, staging, and customer trust boundaries.                                                    |\n| Keys            | Rotate zone signing keys and verify JWKS refresh behavior.                                                      |\n| Workload launch | Author launch bindings on the console's Launcher page; keep workload-secret files locked down.                  |\n| Revocation      | Use shared revocation stores and stream consumers, not process-local memory.                                    |\n| Policy          | Validate, simulate, version, activate, and audit policy-set changes.                                            |\n| Audit           | Export decisions and diagnostics to operational monitoring or SIEM.                                             |\n| Step-up         | Route gated mints to the zone's approvers through the Console Approvals queue or the Admin API decision routes. |\n\n## Run Many Applications on One Host\n\nEach service keeps its own application identity, even when several share a VM, node, or compose stack. Give every process its own `caracal.toml` and point `CARACAL_CONFIG` at it; the SDKs load that file before any other credential source.\n\n```toml\n# /etc/caracal/reporting.toml\nzone_id = \"zone_prod\"\napplication_id = \"app_reporting\"\napp_client_secret_file = \"/etc/caracal/reporting.secret\"\nsts_url = \"https://sts.example.com\"\ngateway_url = \"https://gateway.example.com\"\n\n[[credentials]]\nresource = \"resource://openai\"\nupstream_prefix = \"https://api.openai.com/v1\"\n```\n\n```ini\n# /etc/systemd/system/reporting.service\n[Service]\nEnvironment=CARACAL_CONFIG=/etc/caracal/reporting.toml\n```\n\nRepeat per service with a distinct `application_id` and secret file. One SDK client represents one `(zone, application)` identity during concurrent use. A process that legitimately acts as several Applications keeps one client per Application in a language-native map or dependency-injection container and selects the client before starting a Session or transport. Close each client independently. Do not share one application identity across services: policy decisions and audit attribution both key on the application, and a shared identity collapses them into one indistinguishable actor. The [config file reference](/runtime-console/config-file/) documents the full schema.\n\nWhen one Application's identity must be resolved at runtime instead of from a file - a vault-issued secret, reprovisioning, or rotation without restarts - use the advanced credentials resolver. The resolver runs on each token exchange, so a rotated secret takes effect on the next mint. Do not alternate a resolver among concurrently active Applications; create one client per Application instead.\n\n```python\nfrom caracalai.advanced import ClientCredentials, from_credentials\n\ndef resolve() -> ClientCredentials:\n    identity = vault.read(\"caracal/reporting\")\n    return ClientCredentials(\n        zone_id=identity[\"zone_id\"],\n        application_id=identity[\"application_id\"],\n        client_secret=identity[\"client_secret\"],\n    )\n\ncaracal = from_credentials(\n    coordinator_url=\"https://coordinator.example.com\",\n    sts_url=\"https://sts.example.com\",\n    credentials=resolve,\n)\n```\n\n## Coexist with OpenTelemetry\n\nCaracal needs no bridge configuration to run alongside an OpenTelemetry deployment. The SDK transport merges its envelope with headers already on the request: a valid `traceparent` written by your instrumentation is preserved rather than overwritten, `tracestate` passes through unchanged, and Caracal appends its `baggage` entries "},{"id":"/concepts","title":"Understand the Model","url":"https://docs.caracal.run/concepts/","concepts":"","body":"Caracal gives agents short-lived, policy-approved authority instead of long-lived secrets. Read this section after your first integration when you need the vocabulary behind guides, SDKs, operations pages, and API reference material.\n\n## Read This Section in Order\n\n| Start here                                                | Use it to understand                                                          |\n| --------------------------------------------------------- | ----------------------------------------------------------------------------- |\n| [Caracal Mental Model](/concepts/model-overview/)         | The smallest useful picture of Caracal.                                       |\n| [Authority and Enforcement](/concepts/authority-model/)   | Where decisions happen before requests reach a target.                        |\n| [Zones](/concepts/zone/)                                  | The tenant boundary that owns keys, policies, resources, sessions, and audit. |\n| [Identities and Applications](/concepts/principal/)       | The identities that authenticate, run, and delegate.                          |\n| [Resources and Grants](/concepts/resource-grant/)         | What can be accessed and which scopes are granted.                            |\n| [Providers](/concepts/provider/)                          | The credential Caracal attaches to the upstream target.                       |\n| [Policies and Policy Sets](/concepts/policy/)             | Rego rules evaluated by the STS during token exchange.                        |\n| [Mandates](/concepts/mandate/)                            | The short-lived JWT that carries approved authority.                          |\n| [Step-Up Challenges](/concepts/step-up/)                  | How sensitive actions are held for human approval.                            |\n| [Session Delegation](/concepts/delegation/)               | How one agent passes bounded authority to another.                            |\n| [Delegation Constraints](/concepts/constraint/)           | The limits attached to delegated authority.                                   |\n| [Sessions and Revocation](/concepts/sessions-revocation/) | How active authority is ended and propagated.                                 |\n| [Audit and Request Traces](/concepts/audit-ledger/)       | The event trail behind decisions and runs.                                    |\n| [Caracal Operator](/concepts/operator/)                   | The governed natural-language assistant for control-plane changes.            |\n\n## Core Flow\n\nThe same model appears across the product:\n\n- Onboarding uses the web console guided setup to create the first zone, application, resource, and policy, then you create a workload with launch bindings on the console's Launcher page and give it its workload ID and secret.\n- Guides use the SDKs, Console, Admin API, and adapters to build repeatable integrations.\n- Operations pages use the same terms when explaining keys, revocation, audit, and runtime health.\n\n## Term Map\n\n| Term              | Short definition                                                                                         | Canonical page                                            |\n| ----------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |\n| Zone              | Tenant boundary for authority data and signing keys.                                                     | [Zones](/concepts/zone/)                                  |\n| Application       | Registered client or agent workload.                                                                     | [Identities and Applications](/concepts/principal/)       |\n| Principal         | User, service, or agent identity bound to a session.                                                     | [Identities and Applications](/concepts/principal/)       |\n| Resource          | Protected API, MCP server, tool group, or upstream target.                                               | [Resources and Grants](/concepts/resource-grant/)         |\n| Grant             | Binding from an application and user to resource scopes.                                                 | [Resources and Grants](/concepts/resource-grant/)         |\n| Provider          | Credential mode Caracal uses toward the upstream target.                                                 | [Providers](/concepts/provider/)                          |\n| Policy            | Versioned Rego logic evaluated at token exchange.                                                        | [Policies and Policy Sets](/concepts/policy/)             |\n| Policy set        | Versioned bundle of policies activated for a zone.                                                       | [Policies and Policy Sets](/concepts/policy/)             |\n| Mandate           | Short-lived JWT issued by the STS after policy approval.                                                 | [Mandates](/concepts/mandate/)                            |\n| Delegation        | Bounded authority transfer between Sessions.                                                             | [Session Delegation](/concepts/delegation/)               |\n| Revocation anchor | Authority record ID, Root authority record ID, Session ID, or Delegation ID checked by resource servers. | [Sessions and Revocation](/concepts/sessions-revocation/) |\n| Caracal Operator  | Governed natural-language assistant that turns intent into reviewed control-plane changes.               | [Caracal Operator](/concepts/operator/)                   |\n| System zone       | Reserved `caracal.sys/` zone for the infrastructure that runs Caracal.                                   | [Zones](/concepts/zone/#system-zone)                      |\n\n## What to Read Next\n\nAfter the concepts, use [Guides](/guides/) for task-focused procedures or [SDKs](/sdks/) for language-specific refe"},{"id":"/concepts/model-overview","title":"Caracal Mental Model","url":"https://docs.caracal.run/concepts/model-overview/","concepts":"","body":"Caracal answers one question: **should this principal or agent receive scoped authority for this resource right now?**\n\nIt answers that question during token exchange, records the decision, and returns a mandate only when the active policy set allows the request.\n\n## Six Core Nouns\n\n| Noun        | What it means                                                                     |\n| ----------- | --------------------------------------------------------------------------------- |\n| Zone        | The tenant boundary for configuration, signing keys, policy, sessions, and audit. |\n| Application | A registered client, service, or agent workload.                                  |\n| Principal   | The acting identity, such as a user, service, or agent.                           |\n| Resource    | The protected target: API, MCP server, tool group, or upstream service.           |\n| Policy      | Rego logic that evaluates the requested exchange.                                 |\n| Mandate     | The short-lived JWT that proves authority to a Gateway or adapter.                |\n\nMost of these are configured directly. A _grant_ is a permission binding for resource scopes that policy can read during evaluation; it is not the mandate a resource server verifies.\n\n## One Application, Many Sessions\n\nAn application and a Session are different layers, and they scale differently:\n\n- An **application** is the credentialed security boundary - operator-provisioned or dynamically registered, holding the secret Caracal authenticates. It is created deliberately.\n- A **Session** is the governed execution unit - started the moment a workload acts, with no secret and no registration step.\n\nOne application backs many Sessions. A durable workload registers **one managed application**, then starts, delegates, and fans out as many Sessions as it needs under that single credential. You do not create an application per AI agent; per-execution attribution comes from the Session, not a new application. See [Should I create one application per agent?](/reference/faq/#faq-006).\n\n## Three Runtime Verbs\n\n| Verb     | Meaning                                                                                                    |\n| -------- | ---------------------------------------------------------------------------------------------------------- |\n| Exchange | Ask the STS to convert existing identity into a resource mandate.                                          |\n| Start    | Open a governed Session, optionally attaching a Subject authority record ID for attribution and lifecycle. |\n| Delegate | Pass constrained authority from one Session to another.                                                    |\n\n## One Decision Point\n\nThe STS is the decision point. It evaluates the active policy set with the principal, Authority record, Session, resource, scopes, grant, delegation, and approval context. If policy allows the request, the STS signs a mandate. If policy denies the request, no mandate is issued.\n\nResource servers still verify mandates locally through the Gateway or adapters. That keeps every request protected even after token exchange succeeds.\n\n## Why This Model Matters\n\n- Long-lived provider secrets stay out of agents.\n- Authority expires quickly and can be revoked.\n- Delegation carries typed constraints instead of informal trust.\n- Every allow, deny, step-up, and revocation path is auditable.\n\nNext, read [Authority and Enforcement](/concepts/authority-model/) to see where each enforcement layer fits."},{"id":"/concepts/authority-model","title":"Authority and Enforcement","url":"https://docs.caracal.run/concepts/authority-model/","concepts":"","body":"Caracal separates **granting authority** from **using authority**.\n\n- The STS grants authority by issuing a short-lived mandate after policy evaluation.\n- The Gateway or adapter uses authority by verifying the mandate before forwarding a request or running a tool.\n\n## Enforcement Flow\n\n## Control Layers\n\n| Layer              | Responsibility                                                   | Source of truth                    |\n| ------------------ | ---------------------------------------------------------------- | ---------------------------------- |\n| Zone               | Owns keys, policies, resources, sessions, and audit data.        | Console or Admin API               |\n| Grant              | Declares which application and user may request resource scopes. | Console or Admin API               |\n| Policy set         | Makes the final allow, deny, or step-up decision.                | Rego policy versions               |\n| Mandate            | Carries approved authority as a short-lived signed JWT.          | STS                                |\n| Gateway or adapter | Verifies mandate claims and revocation before use.               | Runtime and resource server config |\n| Audit ledger       | Records decisions, diagnostics, and request correlation.         | API, Console, and storage          |\n\n## Where Resource Authority Comes From\n\nA session never holds resource authority by itself: starting one grants only lifecycle authority - the right to exist, heartbeat, and be attributed. Resource authority always arrives over a delegation, and the STS mints resource mandates only along one of these paths:\n\nThis is why a mint from a plain session fails with `access_denied` and a lifecycle-only-authority hint: the session sits above the delegation layer, and nothing has issued it an edge yet. Narrow the session at start, accept a delegation from a peer, or call as the application - all three end at the same place, a bounded edge the STS can evaluate.\n\n## Default Posture\n\nCaracal should be treated as deny-by-default:\n\n1. A resource must be registered.\n2. A principal must have an applicable grant or delegation path.\n3. The active policy set must allow the requested resource and scopes.\n4. The resource server must verify the mandate and revocation anchors.\n\nMissing configuration, invalid signatures, expired mandates, insufficient scopes, revoked sessions, and failed delegation checks all stop the request.\n\n## Gateway and Adapter Roles\n\nUse the Gateway when you want Caracal to front an HTTP upstream and mediate requests centrally. Use an adapter when the resource server should verify mandates inside its own framework, such as Express, FastMCP, or net/http, or use the verify engine directly for custom boundaries.\n\nBoth patterns share the same authority model. The difference is only where mandate verification runs.\n\n## Enforce, Propagate, or Attribute\n\nThe SDK exposes several call paths. They are not interchangeable: each does a different job, and only some **enforce** authority. Pick by what you need at that boundary.\n\n| Call path                                                                                    | Role          | Verifies the mandate?                                              | Use when                                                                         |\n| -------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------- |\n| `caracal.fetch` / `gateway_request` through the Gateway                                      | **Enforce**   | Yes - the Gateway verifies claims and revocation before forwarding | Caracal fronts an HTTP upstream and mediates centrally                           |\n| Direct Gateway call (`curl` to the Gateway URL)                                              | **Enforce**   | Yes - same Gateway verification                                    | Non-SDK clients or debugging the enforced path                                   |\n| Adapter verify (`context_middleware(verifier=…)`, Express, FastMCP, net/http, verify engine) | **Enforce**   | Yes - the adapter verifies inside the resource server              | The resource server should enforce in its own framework                          |\n| `caracal.transport` / `context_middleware()` (no verifier)                                   | **Propagate** | No - carries and binds the envelope only                           | A Gateway or adapter already enforced upstream and you only need context to flow |\n| App-managed provider call (your code calls the provider SDK directly)                        | **Attribute** | No - Caracal records who acted, but does not gate the call         | You want audit attribution without routing through the Gateway                   |\n\nRule of thumb: **use the Gateway or an adapter verifier to enforce; use transport/propagation to carry identity after enforcement; treat app-managed provider calls as attribution only.** If a path says \"no\" under _verifies the mandate_, something upstream must have already enforced it.\n\n## Next Step\n\nRead [Zones](/concepts/zone/) to understand the tenant boundary that owns authority data.\n\n## Related Pages\n\n- [Policies and Policy Sets](/concepts/policy/) explains the decision contract.\n- [Mandates](/concepts/mandate/) explains the issued token.\n- [Sessions and Revocation](/concepts/sessions-revocation/) explains how active authority ends."},{"id":"/concepts/zone","title":"Zones","url":"https://docs.caracal.run/concepts/zone/","concepts":"","body":"A zone is the main isolation boundary in Caracal. It groups the configuration and runtime state needed to decide, issue, verify, revoke, and audit authority.\n\n## What a Zone Owns\n\n| Area          | Zone-owned data                                                              |\n| ------------- | ---------------------------------------------------------------------------- |\n| Identity      | Applications, credentials, Authority records, and Sessions.                  |\n| Authorization | Resources, providers, grants, policies, policy sets, and step-up challenges. |\n| Cryptography  | Zone signing keys and JWKS used to verify mandates.                          |\n| Delegation    | Delegations, constraints, depth limits, and cascade revocation state.        |\n| Audit         | Decision events, diagnostics, request IDs, and explain traces.               |\n\n## Why Zones Exist\n\nZones let teams run separate environments, tenants, or trust domains without mixing authority data.\n\nCommon zone boundaries include:\n\n- production, staging, and development environments;\n- separate customers in a hosted deployment;\n- isolated product areas with different policy owners;\n- high-sensitivity resources that need distinct keys and audit trails.\n\nWhen several boundaries could apply at once, use [Model Your Application in Caracal](/guides/modeling-recipes/) to choose between a separate zone, a shared zone with separate resources, and a customer attribute in policy input. To serve many of your own customers from one zone, follow [Serve Your Own Customers](/guides/serve-customers/).\n\n:::note[FAQ]\n[What should a zone represent?](/reference/faq/#faq-003)\n:::\n\n## Zone Lifecycle\n\nZone setup is normally managed through the web console. The Admin API exposes the same objects for automation.\n\n## Key and Policy Isolation\n\nEach zone has its own signing-key and JWKS context. Resource servers verify mandates against the issuer, audience, and expected zone. Policy activation is also zone-scoped: activating a policy set in one zone does not affect another zone.\n\n## Dynamic Client Registration\n\nA zone gates whether workloads may self-register short-lived [DCR applications](/concepts/principal/#managed-and-dcr-applications). DCR is **off by default**; an operator turns it on per zone (`dcr_enabled`). While enabled, a control-plane workload holding an admin token can mint auto-expiring application identities through the zone DCR endpoint - Console never creates them.\n\nDisabling DCR on a zone that still has live DCR applications requires an explicit decision, because turning the gate off does not by itself revoke identities already issued:\n\n| Choice      | Effect                                                                                                                                 |\n| ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |\n| Keep live   | Blocks new registrations; existing DCR applications stay valid until their own expiry.                                                 |\n| Revoke live | Blocks new registrations and immediately archives live DCR applications, revoking their sessions and terminating related agent access. |\n\nThe Console prompts for this choice when you disable DCR with live applications, and the Admin API takes the same decision through the `dcr_shutdown` field on a zone patch (`zones.patch(id, { dcr_enabled: false, dcr_shutdown: 'keep_live' | 'revoke_live' })`). Use `zones.dcrStatus(id)` to read the live DCR application count before deciding.\n\n## System Zone\n\nCaracal reserves one **system zone** for the infrastructure that runs the platform itself, distinct from the tenant zones you create. It carries the reserved `caracal.sys/` namespace and is provisioned by the bootstrap admin identity, so a tenant can never create, author, or impersonate the objects inside it.\n\nThe [Caracal Operator](/concepts/operator/) self-governs through this zone: its reserved control identity, least-privilege control grants, and the providers and resources that route its model calls all live there. The Operator will not open a session in, or execute against, the system zone, so its delegated authority stays away from the infrastructure that runs Caracal. An administrator can also mark additional zones as system zones to place them out of the Operator's reach.\n\n## Operational Guidance\n\n- Keep production and non-production authority in separate zones.\n- Name zones after the trust boundary, not a single service.\n- Keep resource identifiers stable because policies, grants, and audit traces refer to them.\n- Rotate zone signing keys using the operations workflow, then confirm resource servers load the current JWKS.\n\n## Next Step\n\nRead [Identities and Applications](/concepts/principal/) to understand who acts inside a zone.\n\n## Related Pages\n\n- [Resources and Grants](/concepts/resource-grant/)\n- [Model Your Application in Caracal](/guides/modeling-recipes/)\n- [Audit and Request Traces](/concepts/audit-ledger/)"},{"id":"/concepts/principal","title":"Identities and Applications","url":"https://docs.caracal.run/concepts/principal/","concepts":"","body":"A principal is the acting identity. An application is the registered client or workload that authenticates and requests authority. A Subject is the JWT `sub` identity work is attributed to.\n\n## Principal Types\n\n| Principal | Typical source                                                 | How Caracal sees it                                                                                                                       |\n| --------- | -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |\n| User      | Your application's own authenticated end user.                 | Federated Subject (`sub`) recorded for attribution, provider connections, revocation, and audit - never an authorization input by itself. |\n| Service   | Workload credential or client secret.                          | Application identity recorded on an Authority record and Session.                                                                         |\n| AI agent  | Agent framework or autonomous software acting through the SDK. | One or more governed Sessions with labels, parentage, and delegation context.                                                             |\n\nPrincipals are not enough on their own. A request also needs an application, Authority record, resource, scopes, and policy approval. Governed execution additionally has a Session.\n\n## Application Roles\n\nApplications represent software that can participate in Caracal flows:\n\n- an agent runtime that starts Sessions for child agents;\n- a backend service that requests mandates;\n- a Gateway application that fronts protected upstreams;\n- a adapter-protected resource server;\n- a managed or dynamically registered client.\n\nApplications have registration metadata, a server-owned token credential, and a registration method. Managed applications are operator-provisioned for known durable software, including runtimes that start Sessions for child agents. Dynamically registered applications are short-lived, isolated identities created through the zone DCR endpoint when a separate auto-expiring credential boundary is needed, such as a per-tenant or per-integration identity, rather than for ordinary Session fan-out.\n\n## Applications Are the Credential Boundary; Sessions Are the Runtime Unit\n\nAn application is registered, holds a server-owned secret, and is the identity Caracal authenticates. A Session is started at runtime by the workload that already holds that secret; it carries parent, Subject authority record ID, labels, and delegation context. One application backs many Sessions, so a durable workload uses **one managed application** and starts, delegates, and fans out as many Sessions as it needs. You do not register an application per AI agent - see [Should I create one application per agent?](/reference/faq/#faq-006).\n\n## Managed and DCR Applications\n\n| Method  | Identity boundary                                                                                                | Operational rule                                                                                                                                                                                                                                     |\n| ------- | ---------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Managed | One durable service, orchestrator, Gateway, or agent runtime - including a runtime that fans out AI-agent work.  | Created in the web console or Admin API and reused across many Sessions for the same workload.                                                                                                                                                       |\n| DCR     | One short-lived, isolated application identity per tenant, integration, or externally issued ephemeral identity. | Created **programmatically only** through the Admin API or Admin SDK `applications.dcr()`, never in the web console; always expires, binds to exactly one Session, and cannot parent further Sessions. The Console lists DCR applications read-only. |\n\nCoordinator records each Session's `lifecycle`: `task` for a block-scoped Session started by `session()`, or `service` for a long-lived, heartbeat-leased Session started by [`start_session()`](/sdks/python/). The value names lifecycle, not actor type. The credential boundary between managed and DCR identities is `registration_method`, not lifecycle; a DCR application's single Session is always `task`.\n\n`lifecycle` carries one behavioral distinction: a `service` session holds a heartbeat lease and is swept when that lease lapses, while a `task` session is task-scoped - it is retired when its work completes (optionally bounded by a TTL). The two are governed by **different reapers**: a `task` is subject to the wall-clock TTL sweeper, and a `service` is **not** - its lifetime is governed solely by the heartbeat lease, so a service that keeps renewing its lease is never terminated on a timer. `lifecycle` does not change what an agent may do or which authority it carries.\n\nThe Session topology enforces two structural rules so a child can never outlast or out-scope what its position allows:\n\n- A `task` parent cannot start a `service` child. The protocol reports `task_session_cannot_start_service`. A `service` parent may start either lifecycle.\n- A DCR Session is a **leaf**: it cannot parent or be a child Session. The protocol reports `dcr_application_cannot_start_child` and `dcr_application_cannot_be_child`.\n\nA **short-lived worker is not a separate lifecycle** - it is an ordinary `task` session created "},{"id":"/concepts/resource-grant","title":"Resources and Grants","url":"https://docs.caracal.run/concepts/resource-grant/","concepts":"","body":"A resource is something Caracal protects. A grant is a configured permission binding that says an application and user may request specific scopes for that resource.\n\n## Resources\n\nResources describe protected targets such as:\n\n- HTTP APIs behind the Gateway;\n- MCP servers and tool groups;\n- internal services protected by Express, FastMCP, or net/http adapters;\n- provider-backed targets that need credential mediation.\n\n| Resource field | Purpose |\n| --- | --- |\n| Identifier | Stable policy and token audience target. Always use the `resource://<slug>` convention, such as `resource://pipernet`; keep it stable even when the upstream URL changes. |\n| Upstream URL | Gateway forwarding target. |\n| Scopes | Named Caracal resource actions that policies and mandates can constrain. |\n| Gateway application | Managed application identity used by Gateway-mediated resources. |\n| Upstream credential provider | Resource binding to the provider record used when Gateway attaches no credential, a Caracal mandate, OAuth tokens, API keys, or bearer tokens. |\n\n:::note[FAQ]\n[What is the difference between a resource and a provider?](/reference/faq/#faq-009) and [why must the resource identifier stay stable?](/reference/faq/#faq-010)\n:::\n\n## Grants\n\nA grant binds:\n\n1. a zone;\n2. an application;\n3. a user or subject;\n4. a resource;\n5. one or more scopes.\n\nGrants are not the final decision. They are one input to policy. The active policy set can still deny, require step-up, or constrain the exchange.\n\nCaracal expresses grants in two related forms. The platform decision contract reads grants as a policy **data document** in `package caracal.authz`, keyed by resource and mapping each role to its allowed scopes; see [Policy](/concepts/policy/) for the exact shape. The control plane also stores managed **delegated grants** (the `delegated_grants` table, authored through the Admin or Control API): administrative records of subject-level assignments whose revocation immediately ends the subject's active sessions. The decision contract reads the policy data document, not this table - delegated grants govern lifecycle and revocation, not per-exchange scope decisions. Per-subject upstream accounts live separately as **provider connections** (the `provider_connections` table, authored under Providers).\n\n## Exchange Relationship\n\n## Scope Design\n\nPrefer small, action-oriented scopes:\n\n| Good | Avoid |\n| --- | --- |\n| `payments:read` | `admin` |\n| `tickets:comment` | `write_all` |\n| `mcp:tool:call` | `tools` |\n\nUse resource identifiers for targets and scopes for actions. Do not encode environment, tenant, or user identity into scope names when those belong in the zone, principal, or policy input.\n\n:::note[FAQ]\n[How should I design scopes?](/reference/faq/#faq-011) and [do I manage grants directly?](/reference/faq/#faq-012)\n:::\n\n## Next Step\n\nRead [Providers](/concepts/provider/) to understand the credential Caracal attaches when it calls the upstream target.\n\n## Related Pages\n\n- [Define Resources and Providers](/guides/resources-providers/)\n- [Model Your Application in Caracal](/guides/modeling-recipes/)\n- [Debug Authorization Decisions](/guides/authorize-access/)"},{"id":"/concepts/policy","title":"Policies and Policy Sets","url":"https://docs.caracal.run/concepts/policy/","concepts":"","body":"The platform decision contract decides whether the STS may issue a mandate. The contract is a signed, versioned Rego module that Caracal owns and embeds in the STS; it is the only thing that emits an authorization decision. You author **policy data documents** - grants, bindings, confinement, and restrictions - that the contract reads. Policies are versioned immutably, bundled into policy sets, and activated per zone.\n\n## Policy Objects\n\n| Object                    | Purpose                                             |\n| ------------------------- | --------------------------------------------------- |\n| Policy                    | Named policy data document with immutable versions. |\n| Policy version            | A specific content hash and schema version.         |\n| Policy set                | A named bundle of policy versions.                  |\n| Policy set version        | A specific manifest of policy versions.             |\n| Active policy set version | The version the STS evaluates for a zone.           |\n\n## Decision Contract\n\nThe platform decision contract owns the `result` object in `package caracal.authz`. It is deny by default and only allows an exchange that matches one of its vetted rules: a bootstrap mandate, a delegated mint narrowed to the Delegation, or a mandate presented at the Gateway. You never write `result`; you supply the data the contract reads.\n\n```text\n# caracal:data-document\npackage caracal.authz\n\nimport rego.v1\n\ngrants := {\n  \"resource://mercury-bank\": {\n    \"application\": \"payments\",\n    \"roles\": {\"payment-execution\": [\"payments:read\", \"payments:write\"]},\n  },\n}\n```\n\nThe contract reads four adopter documents from `package caracal.authz`:\n\n| Document      | Shape                                                | Effect                                                                                                                           |\n| ------------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |\n| `app_ids`     | `{binding_key: application_id}`                      | Binds the application key used in `grants` to the control-plane id the STS sees as `input.principal.id`.                         |\n| `grants`      | `{resource: {application, roles: {role: [scopes]}}}` | Declares which application owns a resource view and which scopes each role may hold.                                             |\n| `confinement` | `[{label_prefix, scopes}]`                           | Caps every Session whose principal carries a matching label prefix to a fixed scope set. Optional; omitting it confines nothing. |\n| `restrict`    | set of reasons                                       | Deny overlay. Any entry denies every exchange in the zone. Optional; keep it empty to authorize normally.                        |\n\nA zone that supplies no data authorizes nothing: every allow rule collapses to the default deny.\n\nExample input values the contract evaluates:\n\n| Input                                 | Example                                                     |\n| ------------------------------------- | ----------------------------------------------------------- |\n| `input.principal.id`                  | `app_lynx_control` (the acting application, user, or agent) |\n| `input.principal.registration_method` | `managed` or `dcr`                                          |\n| `input.resource.identifier`           | `resource://mercury-bank`                                   |\n| `input.context.requested_scopes`      | `[\"payments:read\", \"payments:write\"]`                       |\n| `input.delegation_edge`               | Present when authority was delegated from another Session.  |\n\n## Policy Input Contract\n\nEvery evaluation receives this exact input shape (`schema_version` `2026-05-20`). There is no `input.application` or `input.grant` object - the acting application is the principal, and grant bounds are enforced by the STS before the contract runs. The platform decision contract evaluates the fields below; your data documents map applications, roles, and scopes onto them.\n\n| Path                                  | Type     | Meaning                                                                                                                                                                            |\n| ------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `input.principal.type`                | string   | `Application`, user, service, or agent principal class.                                                                                                                            |\n| `input.principal.id`                  | string   | Stable identifier of the acting application/user/agent.                                                                                                                            |\n| `input.principal.zone_id`             | string   | Zone the principal belongs to.                                                                                                                                                     |\n| `input.principal.registration_method` | string   | `managed` or `dcr`; match `dcr` to scope dynamically registered workloads.                                                                                                         |\n| `input.principal.lifecycle`           | string   | `task` or `service` for Sessions.                                                                                                                                                  |\n| `input.principal.labels`              | string[] | Role descriptors set at `session(labels=[...])`; drive least-privilege role checks.                           "},{"id":"/concepts/step-up","title":"Approvals","url":"https://docs.caracal.run/concepts/step-up/","concepts":"","body":"An approval is Caracal's way to pause a token exchange until a human decides it. Policy data classifies scopes into risk tiers; when a mint requests a gated scope, the STS creates a durable hold and returns `interaction_required` with a challenge id instead of a mandate. The mandate is released only after an authorized approver decides the hold.\n\nApproval is an optional security primitive. A zone that declares no `approval_tiers` data never creates a hold, and nothing in the platform requires one.\n\n## Approval Flow\n\n## Two Decision Planes\n\n| Plane    | Approver                                                  | Surface                                                                                                 |\n| -------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |\n| Operator | A control-plane admin holding an `approve`-capable token. | Console Approvals page or `POST /v1/zones/{zone}/step-up-challenges/{id}/approve` / `/reject`.          |\n| Subject  | The requesting application's own authenticated end user.  | `POST /step-up/{id}/decision` on the STS, requiring a user-type session mandate and the hold's binding. |\n\nThe tier's `approver` declaration picks the plane: `operator`, `subject`, or `any`. On the operator plane, approval authority is a distinct admin capability - a `write` token cannot decide a hold. An `any` hold admits either plane, so operators can always decide it.\n\nA subject-only hold is the application's promise that only its own end user decides: the operator plane refuses it with `subject_approval_required`. Deciding it requires a user session minted through [subject federation](/api/sts/#subject-identity) - the application registers its identity system as a zone subject issuer, exchanges the end user's identity token for a user session mandate, and posts that user's decision to the STS. Declare `subject` tiers only for applications that federate their end users; without a registered subject issuer, a subject-reserved hold can only expire.\n\n## Components\n\n| Component            | Responsibility                                                                                                                         |\n| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |\n| Policy data          | Declares `risk` tiers per scope and `approval_tiers` gates; the platform fixes no tier taxonomy.                                       |\n| STS                  | Creates the hold, serves its state to long-polling agents, records decisions, verifies the binding, and consumes the approval at mint. |\n| Console or Admin API | Lists, inspects, and decides operator-plane holds.                                                                                     |\n| Application          | Relays subject-plane holds to its own federated end user and posts the decision with the user's session mandate.                       |\n| SDK or OAuth client  | Surfaces `interaction_required` and waits on the hold (`waitForApproval`); `caracal run` parks and retries automatically.              |\n\n## Challenge Lifecycle\n\n| State      | Meaning                                                                                  |\n| ---------- | ---------------------------------------------------------------------------------------- |\n| `pending`  | The hold is live and awaiting a decision.                                                |\n| `approved` | An approver granted the hold; the next matching exchange mints.                          |\n| `rejected` | An approver refused the hold; terminal.                                                  |\n| `expired`  | The approval window closed without a decision, or an approval lapsed before consumption. |\n| `consumed` | The approval released its one mandate; terminal.                                         |\n\nAn approval releases at most one token. Its opaque binding covers the requesting principal, Authority record, governed Session, Delegation, application, canonical resource and scope sets, active policy-set manifest, and platform decision contract. A policy rollout or execution-context change therefore requires a fresh decision. Consumed and rejected are terminal and outrank expiry, so both planes report the same state for the same hold.\n\n## Privacy Modes\n\nThe tier's `privacy` declaration controls what the decision record retains of a Subject-plane approver: `identified` stores the Subject verbatim, `pseudonymous` a stable zone-scoped pseudonym, and `anonymous` a redaction marker. The approver's Authority record ID is always kept as the forensic and revocation anchor. Operator-plane decisions always record the deciding admin identity. Caracal stores authorization facts, never business context.\n\n## Design Guidance\n\n- Gate high-risk scopes, not everything: approval latency is a person, so reserve it for authority worth a pause.\n- Keep the decision outside policy; policy declares that a decision is needed, never performs it.\n- Use `subject` tiers when the risk belongs to the application's end user and the application federates its users through a zone subject issuer; use `operator` tiers when the risk belongs to the zone.\n- Give automation credentials `write` without `approve`, so no pipeline can silently settle a hold.\n- Cross-check the binding: the agent prints it beside the challenge id, and the Console shows it on the hold.\n\n## Next Step\n\nRead [Mandates](/concepts/mandate/) to understand what the STS issues once an exchange is allowed or approved.\n\n## Related Pages\n\n- [Human Approval](/guides/step-up/)\n- [Policies and Policy Sets](/concepts/policy/)\n- [Audit and Request Traces](/concepts/audit-ledger/)"},{"id":"/concepts/mandate","title":"Mandates","url":"https://docs.caracal.run/concepts/mandate/","concepts":"","body":"A mandate is the token Caracal issues after the STS approves an exchange. It is a short-lived JWT signed with the zone signing key and verified by the Gateway or resource adapters.\n\n## What a Mandate Proves\n\nA valid mandate proves:\n\n- which zone issued it;\n- which application and principal are acting;\n- which session anchors are active;\n- which resource targets and scopes were approved;\n- which Authority record, Session, and Delegation supplied the authority;\n- when the authority expires.\n\n## Issuance Path\n\n## Mandate Use\n\n| Use                | Verification focus                                                             |\n| ------------------ | ------------------------------------------------------------------------------ |\n| Gateway request    | Issuer, audience, zone, resource, scopes, expiry, revocation.                  |\n| MCP tool call      | Bearer token, required scopes, required targets, Session and Delegation constraints. |\n| SDK outbound call  | Context propagation and mandate header injection.                              |\n| Delegated exchange | Session, Delegation, scopes, hop count, and constraints.                       |\n\n## Mandates Are Not API Keys\n\nMandates are intentionally short lived and context bound. They should not be stored as durable credentials, copied into configuration files, or reused across unrelated resources.\n\nResource servers should always verify a mandate at request time. Verification includes signature and claim checks plus revocation checks for the Authority record ID, Root authority record ID, Session ID, and Delegation ID when those claims are present. See the [parsed claim mapping](/sdks/identity/#parsed-claim-names) for language-level and raw JWT names.\n\n## Failure Modes\n\n| Failure                   | Meaning                                                                   |\n| ------------------------- | ------------------------------------------------------------------------- |\n| `invalid_token`           | Signature, issuer, audience, required claim, or expiry validation failed. |\n| `scope_insufficient`      | The mandate does not contain a required scope.                            |\n| `session_revoked`         | One of the mandate revocation anchors has been revoked.                   |\n| `session_required`        | The resource requires authority from a governed Session.                  |\n| `delegation_required`     | The resource requires delegated authority.                                |\n| `chain_mismatch`          | The delegation chain does not include the required application.           |\n| `hop_count_exceeded`      | The delegation path exceeds the configured hop limit.                     |\n\n## Next Step\n\nRead [Session Delegation](/concepts/delegation/) to understand how Sessions pass bounded authority.\n\n## Related Pages\n\n- [Sessions and Revocation](/concepts/sessions-revocation/)\n- [Protect an MCP Server](/guides/protect-mcp/)\n- [Run an Agent with caracal run](/guides/runtime-run/)"},{"id":"/concepts/delegation","title":"Session Delegation","url":"https://docs.caracal.run/concepts/delegation/","concepts":"","body":"Delegation lets one Session pass a narrower, typed slice of authority to another Session.\n\nAuthority follows the **application**. Sessions started under the same application already act under that application's authority. Create a Delegation in exactly two cases:\n\n- To **narrow** authority, so a child holds only a subset of what its parent can do (least privilege).\n- To carry authority **across applications**, when a receiving Session belongs to a different application and explicitly presents the offered Delegation ID.\n\nIt is represented as a graph of directed edges. Each edge connects a source session to a target session, carries scopes and constraints, and can be revoked independently.\n\n## Graph Model\n\n## Delegation Fields\n\n| Field                | Purpose                                                          |\n| -------------------- | ---------------------------------------------------------------- |\n| Source session       | The session that delegates authority.                            |\n| Target session       | The child or receiving Session.                                  |\n| Issuer application   | Application creating the delegation.                             |\n| Receiver application | Application receiving authority.                                 |\n| Resource             | Optional resource boundary for the edge.                         |\n| Scopes               | Subset of authority being delegated.                             |\n| Constraints          | Typed resource, scope, TTL, and hop limits plus audit metadata. |\n| Status               | Active, expired, or revoked lifecycle state.                     |\n\n## Rules\n\n- Delegation should narrow authority, not expand it.\n- Every Delegation must have a positive TTL; unbounded edges are rejected before creation.\n- Delegation paths must not cycle.\n- Hop count should be bounded.\n- Revoking an upstream Delegation should invalidate downstream authority.\n- Resource servers should verify delegation claims when they require delegated access.\n\nExpiry removes the edge's authority without terminating either endpoint Session. STS rejects the expired edge and caps every mandate minted through it so the mandate cannot outlive the edge. The target Session remains active but cannot use that Delegation again; it must receive another live Delegation before it can mint delegated authority. Revocation is different: it is an explicit monotonic action that cascades through downstream Delegations and affected Session subtrees.\n\n## What a Delegation Bounds - and What It Does Not\n\nA Delegation bounds **the token issued for that Delegation** and **any further Delegation chained from it**. It does **not** raise the application's authority ceiling. Internally and on the wire, Delegations are represented as graph edges.\n\n- When a child is created with a **narrowing grant**, a `source → child` edge is recorded. A token exchange that presents this edge is bounded to the edge's scopes. Coordinator prevents widening when an edge is created, and STS independently rechecks every adjacent ancestor and child for scope, resource, expiry, TTL, and hop attenuation before every delegated issuance.\n- When a child is started with **inherit** under a narrowed parent, Coordinator mirrors the parent's Delegation onto the child in the same creation transaction. Least privilege is transitive by default.\n- When a child is created with **inherit** under a **root parent** (one that holds the application's full authority with no inbound edge), no edge is recorded and the child runs under the application's policy-bounded authority. There is nothing narrower to carry forward - and because the decision contract mints resource mandates only over a Delegation, an edge-less session cannot present _delegated_ authority at all.\n\nWorked example: A starts B with `Authority.narrow([tickets:read])`, then B starts C.\n\n- If B starts C with **inherit**, Coordinator records a `B → C` edge mirroring B's `tickets:read` slice, so C remains bounded by B's narrowing.\n- If B starts C with narrower authority, Coordinator records a `B → C` edge and rejects it unless `C ⊆ B`.\n\nThe consequence: the **application plus policy** is the hard, server-enforced boundary every session is contained by. Within one application, transitive inheritance keeps a narrowed subtree narrowed automatically. To place a subtree behind a real cross-application trust boundary, move it under a different application via `delegate()`. Creation records an offer and does not mutate the receiver's context. The receiver consents by possessing and presenting the opaque, target-Session-bound Delegation ID with `acceptDelegation()`; STS also verifies receiver application ownership and the live target Session before minting. All sessions of one application share a credential and a trust domain by design; transitive inheritance contains an _accidentally_ un-narrowed descendant, while a separate application contains a _mutually distrusting_ one.\n\n## SDK Relationship\n\nThe SDKs expose one primitive for creating children, one for granting a peer, and one for presenting a received grant:\n\n| Language   | Start a child                            | Delegate to an existing peer | Present a received Delegation |\n| ---------- | ---------------------------------------- | ---------------------------- | ----------------------------- |\n| TypeScript | `session()` / `session({ authority })`   | `delegate()` / `revokeDelegation()` | `acceptDelegation()` |\n| Python     | `session()` / `session(authority=…)`     | `delegate()` / `revoke_delegation()` | `accept_delegation()` |\n| Go         | `Session()` / `Session` with `Authority` | `Delegate()` / `RevokeDelegation()` | `AcceptDelegation()` |\n\n`session()` returns a child running under the **same application's** authority; it never moves the child into another application. Pass `Authority.narrow(...)` to bind a least-privilege Delegation or `Authority.none()` for no inherited authority. To hand authori"},{"id":"/concepts/constraint","title":"Delegation Constraints","url":"https://docs.caracal.run/concepts/constraint/","concepts":"","body":"Typed constraints make delegated authority explicit. Coordinator validates them at edge creation and STS revalidates the current edge and its authoritative ancestry at token exchange.\n\n## Constraint Types\n\n| Constraint       | Use it to limit                                             |\n| ---------------- | ----------------------------------------------------------- |\n| Resource         | Which protected target can be reached.                      |\n| Scopes           | Which actions can be requested.                             |\n| TTL              | How long the delegated edge remains useful.                 |\n| Hop count        | How deep the delegation chain may become.                   |\n| Approval metadata | Audit/display annotation; it does not authorize the edge.    |\n| Broad reason      | Audit/display note for an elevated resource-unbounded edge.  |\n\n## Example Shape\n\n```json\n{\n  \"resource\": \"https://api.example.com/tickets\",\n  \"scopes\": [\"tickets:read\", \"tickets:comment\"],\n  \"max_hops\": 2,\n  \"expires_at\": \"2026-06-01T12:00:00Z\",\n  \"policy_approved\": true\n}\n```\n\nDelegation constraints bound authority, lifetime, and propagation. They are not usage accounting. Use STS rate limits for exchange volume and a durable domain-specific quota when calls, work, or cost must be consumed atomically. `policy_approved` and `broad_reason` are metadata only.\n\nApplication-chain requirements are verifier options rather than edge constraints. Use `requireChainContains` at a resource boundary when a specific application must appear in the signed delegation chain.\n\n## Where Constraints Are Enforced\n\n## Design Guidance\n\n- Put durable business rules in policy data documents.\n- Put per-edge runtime limits in constraints.\n- Prefer positive allowlists over open-ended deny lists.\n- Keep constraints small enough to review in audit traces.\n- Use consistent field names across agents so policies stay readable.\n\n## Next Step\n\nRead [Sessions and Revocation](/concepts/sessions-revocation/) to understand how active authority ends.\n\n## Related Pages\n\n- [Session Delegation](/concepts/delegation/)\n- [Policies and Policy Sets](/concepts/policy/)\n- [Implement Multi-Agent Delegation](/guides/delegation/)"},{"id":"/concepts/sessions-revocation","title":"Sessions and Revocation","url":"https://docs.caracal.run/concepts/sessions-revocation/","concepts":"","body":"Authority records, Sessions, and Delegations make Caracal authority temporary and revocable. A mandate includes their revocation anchors, and resource servers check those anchors before accepting it.\n\n## Identity and Execution Records\n\n| Record                | Role                                                                                                                                                            |\n| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Subject               | Opaque JWT `sub` identity. The Console's **Subjects** page groups Authority records by this value.                                                              |\n| Authority record      | One STS exchange record, stored in `authority_records`. Its Authority record ID anchors revocation and audit.                                                    |\n| Root authority record | Root of an STS exchange ancestry chain. Its ID anchors ancestry-wide revocation.                                                                                 |\n| Session               | Governed Coordinator execution, stored in `sessions`. A delegated Session is a Session with an inbound Delegation, not a separate object type.                  |\n\n## Subject Identity Is Federated\n\nCaracal does not generate Subjects, and **Subjects** is not a login surface. The `sub` on an Authority record is taken verbatim from the token the application exchanges. To federate end users, a zone registers the identity system as a Subject issuer and the application exchanges each identity token for an Authority record with no resource authority.\n\nA Session may attach that record through `subjectAuthorityRecordId`, `subject_authority_record_id`, or `SubjectAuthorityRecordID`. Session creation must also present the federated Subject mandate through `subjectAuthorityRecordToken`, `subject_authority_record_token`, or `SubjectAuthorityRecordToken`; a bare record ID is not proof of control. The verified record remains the Session's immutable attribution and revocation anchor while application credentials rotate for lease maintenance. It does **not** by itself cause later resource mandates minted for the Session to carry the federated user's `sub`; callers that need the user's own mandate must present the federated mandate on the supported exchange or approval path.\n\n## Revocation Anchors\n\nResource servers check every relevant anchor: Authority record ID, Root authority record ID, Session ID, and Delegation ID. The [parsed claim mapping](/sdks/identity/#parsed-claim-names) lists the canonical language-level names and raw JWT fields.\n\nIf any anchor is revoked, the mandate should be rejected as `session_revoked`.\n\n## Revocation Flow\n\nThe Redis stream name is `caracal.sessions.revoke`. The Redis backend packages can consume the stream and populate local revocation stores.\n\nSuspension is reversible Session state, not permanent revocation. Gateway-routed requests perform a fresh STS exchange and reject a suspended Session immediately through authoritative Session validation. Already issued mandates checked directly by a resource verifier can remain usable until their mandate TTL expires; keep mandate TTLs within the documented 15-minute cap when suspension latency matters. Termination and Delegation revocation remain monotonic revocation events.\n\n## Cascade Behavior\n\nRevocation should follow authority:\n\n- revoking an Authority record invalidates authority descended from it;\n- revoking a Session invalidates its child Delegations;\n- revoking a Delegation invalidates downstream delegated authority;\n- revoking a grant prevents future exchange and can invalidate active Authority records and Sessions depending on workflow.\n\n## Resource-Server Responsibility\n\nThe Gateway and adapters must be configured with a revocation store. For development, an in-memory store can be useful. For production, use a shared store and stream consumer so revocations propagate across resource-server instances.\n\n## Next Step\n\nRead [Audit and Request Traces](/concepts/audit-ledger/) to understand how decisions and requests are explained.\n\n## Related Pages\n\n- [Mandates](/concepts/mandate/)\n- [Protect an MCP Server](/guides/protect-mcp/)\n- [Tail and Query the Audit Stream](/guides/audit-stream/)"},{"id":"/concepts/audit-ledger","title":"Audit and Request Traces","url":"https://docs.caracal.run/concepts/audit-ledger/","concepts":"","body":"The audit ledger records what Caracal decided, why it decided it, and which request or run produced the event.\n\n## What Gets Audited\n\n| Event area              | Examples                                                              |\n| ----------------------- | --------------------------------------------------------------------- |\n| Token exchange          | Allow, deny, step-up required, policy diagnostics.                    |\n| Gateway and adapter use | Resource decision, mandate verification failure, request correlation. |\n| Policy lifecycle        | Policy creation, validation, policy-set activation, simulation.       |\n| Delegation              | Creation, traversal, impact, and revocation cascade.                  |\n| Sessions                | Start, terminate, revoke, expire.                                     |\n| Administration          | Zone, application, resource, provider, grant, and challenge changes.  |\n\n## Audit Flow\n\n## How to Use Audit\n\n| Question                                      | Where to look                                                        |\n| --------------------------------------------- | -------------------------------------------------------------------- |\n| Why was a request denied?                     | Console `request trace` or Admin API explain endpoint by request ID. |\n| Which policy caused the decision?             | Determining policies and diagnostics.                                |\n| Did revocation propagate?                     | Session, delegation, and resource decision events.                   |\n| Which run made a request?                     | Request ID, Authority record ID, Session ID, and trace context.      |\n| Was an approval required, and who decided it? | Step-up issuance, decision, and consumption events.                  |\n\n## Request IDs\n\nRequest IDs tie multiple events together. Keep the request ID from an SDK, Gateway, STS error, or Console trace whenever debugging. The explain view uses it to collect related decision events and diagnostics.\n\n## Integrity and Retention\n\nAudit events should be treated as operational evidence. Configure retention, export, and SIEM forwarding according to your deployment requirements. Do not rely on local process logs as the only authority trail.\n\n## Next Step\n\nUse [Guides](/guides/) when you are ready to apply the model.\n\n## Related Pages\n\n- [Tail and Query the Audit Stream](/guides/audit-stream/)\n- [Trace One Protected Request](/tutorials/inspect-a-run/)\n- [Operations](/operations/)"},{"id":"/operations","title":"Operate Caracal","url":"https://docs.caracal.run/operations/","concepts":"","body":"Use Operations when Caracal is running as infrastructure: Docker Compose for self-hosted installs, Helm for Kubernetes, managed Postgres and Redis, production secrets, rollout gates, observability, incident handling, upgrades, and platform handoff.\n\n## Operating Model\n\n## Start by Role\n\n| Role | Start with |\n| --- | --- |\n| Local or self-hosted operator | [Deploy with Docker Compose](/operations/docker-compose/) |\n| Kubernetes platform team | [Deploy with Helm](/operations/kubernetes-helm/) and [Deploy on Managed Kubernetes](/operations/cloud-reference-deployments/) |\n| Security reviewer | [Harden Production](/operations/tls-hardening/) and [Rotate Keys and Secrets](/operations/key-management/) |\n| SRE or on-call engineer | [Monitor Health and Metrics](/operations/observability/), [Configure Alerts](/operations/alerts/), [Recover from Failures](/operations/failure-modes/), and [Run Failure Drills](/operations/failure-drills/) |\n| Release owner | [Plan a Platform Rollout](/operations/platform-rollout-kit/), [Deploy Policy Changes](/operations/policy-deployment/), and [Upgrade Caracal](/operations/upgrade/) |\n\n## Deployment Choices\n\n| Environment | Recommended path | Notes |\n| --- | --- | --- |\n| Local development | `caracal up` / `infra/docker/docker-compose.yml` | Builds local images, binds service ports to `127.0.0.1`, writes local secrets, and serves the packaged web console at `http://localhost:3001`. |\n| Self-hosted runtime | `infra/docker/runtime-compose.yml` | Uses versioned GHCR images, mounted secrets, and the `caracal-web` browser entrypoint. |\n| Kubernetes | `infra/helm/caracal` | Uses Deployments/StatefulSets, pre-install/pre-upgrade migration Job, ClusterIP Services, web Ingress by default, optional API/Gateway Ingress, NetworkPolicy, PDBs, HPAs, ServiceMonitor, and PrometheusRule. |\n\n## Core Operational Invariants\n\n- Postgres is the durable control-plane store.\n- Redis Streams move audit, policy invalidation, session revocation, key invalidation, agent, invocation, and delegation events.\n- STS and Gateway keep audit replay directories so audit emission can drain after Redis/Audit recovery.\n- Published modes are `rc` and `stable`; they require production-grade HMAC keys and reject unsafe fallbacks.\n- Product-management operations happen through the web console, Admin SDK, or Control API; the top-level runtime CLI only manages local lifecycle, the development web launcher, and `caracal run`.\n\n## Section Map\n\n| Need | Page |\n| --- | --- |\n| Compose deployment | [Deploy with Docker Compose](/operations/docker-compose/) |\n| Helm deployment | [Deploy with Helm](/operations/kubernetes-helm/) |\n| Workload configuration | [Configure Service Environment](/operations/env-vars/) and [Choose a Cloud Profile](/operations/cloud-native-profiles/) |\n| Cloud deployment | [Deploy on Managed Kubernetes](/operations/cloud-reference-deployments/) |\n| Storage | [Operate PostgreSQL](/operations/postgres/) and [Operate Redis Streams](/operations/redis/) |\n| Hardening | [Harden Production](/operations/tls-hardening/) |\n| Rotation | [Rotate Keys and Secrets](/operations/key-management/) |\n| Scaling | [Scale Capacity](/operations/scale-capacity/) |\n| Observability | [Monitor Health and Metrics](/operations/observability/) and [Configure Alerts](/operations/alerts/) |\n| Recovery | [Recover from Failures](/operations/failure-modes/), [Run Failure Drills](/operations/failure-drills/), [Back Up and Retain Data](/operations/backup-retention/), and [Respond to Incidents](/operations/incident-response/) |"},{"id":"/operations/docker-compose","title":"Deploy with Docker Compose","url":"https://docs.caracal.run/operations/docker-compose/","concepts":"","body":"Caracal ships two Compose shapes:\n\n| File | Purpose |\n| --- | --- |\n| `infra/docker/docker-compose.yml` | Local development stack that builds repository images. |\n| `infra/docker/runtime-compose.yml` | Self-hosted runtime stack using versioned GHCR images. |\n\nBoth stacks run Postgres, Redis, migrations, STS, API, Gateway, Audit, Coordinator, the packaged web console BFF, and optional Control.\n\n## Topology\n\nThe packaged `web` service is the browser entrypoint. It maps `127.0.0.1:3001` on the host to port `3002` in the container, serves the built SPA same-origin, and proxies `/api/console/*` to the internal API and Coordinator. It starts after Postgres, API, and Coordinator are healthy. API, Coordinator, Gateway, and STS remain bound to localhost; the web service is the only browser-facing service in the Compose topology.\n\n## Prerequisites\n\n- Docker with Compose support.\n- Generated runtime secrets under the expected secrets directory.\n- For the self-hosted runtime file, `CARACAL_VERSION` and optional `CARACAL_REGISTRY`.\n\n## Local Development Flow\n\n```bash\npnpm secrets:init\ncaracal up\ncaracal status --ready\nbash infra/scripts/smokeTest.sh\n```\n\n`caracal up` builds local images in development mode, including `caracal-web` from `infra/docker/Dockerfile.web`; release builds publish the same image from the service-image matrix because `release.config.json` registers it as a runtime-tier container. It writes operator secrets outside the repository under the managed dev secret directory, exports `CARACAL_SECRETS_DIR` to Compose, and keeps the host directory private so only the operator account can mount those files into services. The secret set includes `authDatabaseUrl` for the dedicated `caracal_auth` session database and `caracalAuthSecret` for Better Auth signing. `caracal status --ready` checks dependency readiness, and `smokeTest.sh` probes `/ready` for API, Gateway, STS, Audit, and Coordinator, plus the API `/health` liveness endpoint.\n\nAfter readiness succeeds, open the packaged console at [http://localhost:3001](http://localhost:3001). Use `caracal web` only when you want the local development launcher backed by the Vite dev server instead of the packaged Compose web service.\n\n## Self-Hosted Runtime Flow\n\n```bash\nexport CARACAL_VERSION=0.2.0\ndocker compose -f infra/docker/runtime-compose.yml up -d\ndocker compose -f infra/docker/runtime-compose.yml ps\nbash infra/scripts/smokeTest.sh\n```\n\nThe runtime compose file expects secrets under `./secrets/` relative to the compose file unless `CARACAL_SECRETS_DIR` points at a platform-managed secret mount. It pulls `caracal-web:v<version>` from GHCR, maps `127.0.0.1:3001` to container port `3002`, and uses `CARACAL_WEB_URL` when the browser origin is not `http://localhost:3001`. Keep the secret directory private to the operator account and never place inline secrets in the compose file.\n\nTo move an existing runtime onto a newer release, use `caracal upgrade`. It stages the pinned images, applies expand-phase migrations against the running database, then rolls the services and waits for readiness - no maintenance window. See [Upgrade Caracal](/operations/upgrade/).\n\n## Operator Secret Boundary\n\nAdmin, Coordinator, auth signing, database, Redis, HMAC, and KEK material are operator secrets. Do not place those files in a source workspace, agent workspace, MCP tool workspace, or any directory mounted into untrusted agent containers.\n\n| Deployment | Secret location |\n| --- | --- |\n| Local development through `caracal up` | Managed dev secret directory outside the repository; passed to Compose as `CARACAL_SECRETS_DIR`. |\n| Released self-hosted runtime | `$CARACAL_HOME/secrets` or an explicit `CARACAL_SECRETS_DIR`. |\n| Cloud or orchestrated deployment | Cloud/Kubernetes secret mount or secret-manager projection passed as `CARACAL_SECRETS_DIR` or service-specific `*_FILE` paths. |\n\nRun agents with workload credentials only. If an agent must execute untrusted code, run it as a separate OS user, container, VM, or sandbox that cannot read `CARACAL_SECRETS_DIR`, `$CARACAL_HOME/secrets`, Docker socket credentials, or repository control-plane files.\n\n## Control Plugin\n\nControl runs as an optional in-process plugin inside the API service rather than a\nseparate container. Set `CARACAL_CONTROL_ENABLED=true` on API to mount the plugin,\nthen toggle availability at runtime with the gate file (`CONTROL_GATE_FILE`). Enable\nit only when the Console or platform automation is ready to manage Control exposure\nand credentials.\n\n## Rollback\n\n1. Stop the stack with `caracal down` or `docker compose ... down`.\n2. Restore the previous `CARACAL_VERSION`.\n3. Start the stack and wait for readiness.\n4. Confirm audit replay and outbox queues drain before reopening high-risk traffic.\n\n## Troubleshooting\n\n| Symptom | Check |\n| --- | --- |\n| Postgres or Redis never becomes healthy | Check secret files, mounted volumes, and host port conflicts on `5432` or `6379`. |\n| API ready fails | Confirm migrations completed and `DATABASE_URL_FILE`, `REDIS_URL_FILE`, and `CARACAL_ADMIN_TOKEN_FILE` resolve. |\n| Web console fails readiness | Confirm `authDatabaseUrl`, `caracalAuthSecret`, `caracalAdminToken`, and `caracalCoordinatorToken` are mounted, and that API and Coordinator are healthy. |\n| STS or Gateway fails in `rc`/`stable` | Confirm `AUDIT_HMAC_KEY`, `STREAMS_HMAC_KEY`, and `GATEWAY_STS_HMAC_KEY` are hex-encoded and at least 32 bytes. |\n| Gateway rejects upstreams | Operator-provisioned private upstreams are allowed by default; if set, confirm the host is in `UPSTREAM_HOST_ALLOWLIST`. Cloud metadata, loopback, CGNAT, and multicast are always blocked. |\n| Audit gaps appear after Redis outage | Keep STS and Gateway replay volumes intact and wait for replay metrics to drain. |\n\n## Next Step\n\nUse [Configure Service Environment](/operations/env-vars/) to review required service variables and secret-file settings."},{"id":"/operations/kubernetes-helm","title":"Deploy with Helm","url":"https://docs.caracal.run/operations/kubernetes-helm/","concepts":"","body":"The Helm chart in `infra/helm/caracal` renders production-shaped Kubernetes resources for API, STS, Gateway, Audit, Coordinator, the browser-facing web console BFF, and optional Control.\n\n## Rendered Resources\n\n| Resource                      | Purpose                                                                                     |\n| ----------------------------- | ------------------------------------------------------------------------------------------- |\n| Deployment                    | API, Audit, Coordinator, Web, and Control when enabled.                                     |\n| StatefulSet                   | STS and Gateway when replay persistence is enabled.                                         |\n| Job                           | Pre-install/pre-upgrade Postgres migrations for the control-plane and web session schemas.  |\n| Service                       | ClusterIP service per enabled component.                                                    |\n| Ingress                       | Optional external Web, API, Gateway, and STS endpoints.                                     |\n| NetworkPolicy                 | Default-deny with explicit internal, storage, DNS, OTLP, HTTPS, and custom egress controls. |\n| PDB/HPA                       | Availability and scaling controls per service.                                              |\n| ServiceMonitor/PrometheusRule | Metrics scraping and built-in alert recipes.                                                |\n\n## Profile Selection\n\nThe chart ships three overlays on top of `values.yaml`. Pick one explicitly - `values.production.yaml` is the canonical enterprise install path:\n\n| Overlay                  | `global.mode` | Posture                                                                               | Use for                          |\n| ------------------------ | ------------- | ------------------------------------------------------------------------------------- | -------------------------------- |\n| `values.production.yaml` | `stable`      | Fail-closed; replicas, PDBs, anti-affinity baked in                                   | **Production (canonical)**       |\n| `values.rc.yaml`         | `rc`          | Same fail-closed trust boundary; release-candidate build may be functionally unstable | Staging / pre-release validation |\n| `values.dev.yaml`        | `dev`         | Relaxed, single-host conveniences                                                     | Local evaluation only            |\n\nAlways pass the chosen overlay with `--values`; never install on bare `values.yaml`.\n\n## Minimal Install Shape\n\nReleased chart versions are published to the GitHub Container Registry as OCI artifacts; pin an explicit version for production:\n\n```bash\nhelm upgrade --install caracal oci://ghcr.io/garudex-labs/charts/caracal \\\n  --version 0.2.0 \\\n  --namespace caracal \\\n  --create-namespace \\\n  --values values.production.yaml\n```\n\nFrom a repository checkout, install the working-tree chart instead:\n\n```bash\nhelm upgrade --install caracal infra/helm/caracal \\\n  --namespace caracal \\\n  --create-namespace \\\n  --values infra/helm/caracal/values.production.yaml\n```\n\nTo manage the install declaratively, [Provision with OpenTofu](/operations/opentofu/) wraps this chart with pinned versions, atomic upgrades, and environment roots.\n\nBefore installing, provide the runtime Secret named by `secrets.runtimeSecretName` or set `secrets.create=true` with plaintext values only for a controlled evaluation environment. The runtime Secret must include service-specific database URLs (`apiDatabaseUrl`, `stsDatabaseUrl`, `gatewayDatabaseUrl`, `auditDatabaseUrl`, and `coordinatorDatabaseUrl`), `idempotencyHmacKey`, `caracalAuthSecret`, `authDatabaseUrl`, Redis, admin, Coordinator, KEK, HMAC, and metrics keys.\n\nFor a managed-cloud install with the runtime Secret provisioned by the External Secrets Operator, a production overlay, and cert-manager TLS ingress, follow [Deploy on Managed Kubernetes](/operations/cloud-reference-deployments/).\n\n## Service Ports\n\n| Component       | Port                                    |\n| --------------- | --------------------------------------- |\n| API             | `3000`                                  |\n| STS             | `8080`                                  |\n| Gateway         | `8081`                                  |\n| Audit           | `9090`                                  |\n| Coordinator     | `4000`                                  |\n| Web Console BFF | `3002`                                  |\n| Control         | in-process in API (`3000`) when enabled |\n\n## Production Values to Review\n\n| Area          | Values                                                                                  |\n| ------------- | --------------------------------------------------------------------------------------- |\n| Images        | `global.registry`, `global.tag`, `global.imagePullPolicy`, `global.mode`                |\n| Secrets       | `secrets.runtimeSecretName`, `secrets.database.*`, `secrets.redis.*`, token secret refs |\n| Network       | `services.<name>.internalUrl`, `services.sts.issuerUrl`, `networkPolicy.*`, `ingress.*` |\n| Reliability   | `services.<name>.replicas`, `pdb`, `hpa`, resource requests/limits                      |\n| Audit export  | `services.audit.exportScratchSizeLimit`                                                 |\n| Replay        | `replayPersistence.enabled`, `storageClassName`, `size`                                 |\n| Observability | `serviceMonitor.enabled`, `alertRules.enabled`, `global.otelExporterOtlpEndpoint`       |\n\n## Web Console Deployment\n\n`services.web` is enabled in the default chart values with two replicas, image `caracal-web`, container port `3002`, PDB/HPA settings, resource requests and limits, `databaseSsl: require`, and `autoMigrate: \"false\"`. Set `services.web.publicUrl` to the external HTTPS origin browsers load; the template requires this value and passes it to `CARACAL_AUTH_U"},{"id":"/operations/cloud-native-profiles","title":"Choose a Cloud Profile","url":"https://docs.caracal.run/operations/cloud-native-profiles/","concepts":"","body":"Cloud-native Caracal deployments should use the Helm chart with externalized state: managed Postgres, managed Redis, platform secrets, ingress, NetworkPolicy, and cluster observability.\n\nFor a concrete, copy-paste version of this profile - including External Secrets manifests, a production overlay, cert-manager TLS web ingress, and a per-cloud (EKS/GKE/AKS) substitution table - see [Deploy on Managed Kubernetes](/operations/cloud-reference-deployments/).\n\n## Profile Map\n\n| Profile            | Values to adjust                                                                                                                                         |\n| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Evaluation         | `global.mode=rc`, short retention, limited replicas, temporary secrets, no public ingress unless required.                                               |\n| Production         | `global.mode=stable`, managed Postgres/Redis, HTTPS web ingress, NetworkPolicy, PDB/HPA, ServiceMonitor, PrometheusRule.                                 |\n| Private cluster    | Keep API/Gateway private; expose the web console through an internal load balancer, private ingress, or service mesh when operators need browser access. |\n| Regulated workload | Enforce external secret manager, immutable backups, audit export, restricted egress, and incident evidence retention.                                    |\n\n## Managed Dependency Contract\n\n| Dependency    | Requirements                                                                                                                                                               |\n| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Postgres      | TLS-capable connection, migration role, durable backups, point-in-time recovery, sufficient connection capacity, and a dedicated `caracal_auth` database for web sessions. |\n| Redis         | Streams support, authentication, memory policy sized for audit/revocation/outbox traffic, persistence according to recovery goals.                                         |\n| Secrets       | Runtime Secret with database, auth database, Redis, admin, Coordinator, Better Auth signing, zone KEK, audit HMAC, stream HMAC, and Gateway-STS HMAC keys.                 |\n| Observability | Metrics scraping for all services and alerts equivalent to the chart PrometheusRule.                                                                                       |\n\n## Network Stance\n\nThe chart NetworkPolicy allows Caracal pod-to-pod traffic and storage egress. Add explicit DNS, HTTPS, identity provider, object store, and provider API egress only as needed.\n\n```yaml\nnetworkPolicy:\n  enabled: true\n  allowOpenDns: false\n  allowOpenHttps: false\n  dnsEgress: []\n  extraEgress: []\n  extraIngress: []\n```\n\n## Ingress Stance\n\nExpose only the endpoints required by your environment:\n\n| Endpoint                  | When to expose                                                                                                                                                      |\n| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Web Console               | Default public entrypoint for browser operators. Set `services.web.publicUrl`, enable HTTPS, and allow the ingress controller through `networkPolicy.extraIngress`. |\n| Gateway                   | Expose only when protected resource traffic reaches Caracal Gateway from outside the private network.                                                               |\n| API                       | Prefer private access; expose only for Admin SDK or automation clients that cannot reach it privately.                                                              |\n| STS                       | Expose only when token exchange clients cannot reach it privately.                                                                                                  |\n| Audit/Coordinator/Control | Keep private.                                                                                                                                                       |\n\nThe chart renders optional Ingress resources for Web, API, Gateway, and STS. Set `services.sts.issuerUrl` to the public HTTPS STS origin. Leave `services.sts.internalUrl` empty for release-local ClusterIP routing, or set it to an internal cross-cluster endpoint. The issuer and routing URL are separate so tokens retain one stable identity while services use a private route. Production values leave every Ingress disabled until a deployment opts in.\n\n## Validation\n\n1. Render manifests with production values.\n2. Confirm no plaintext secrets are committed.\n3. Confirm NetworkPolicy egress covers required provider APIs and object stores.\n4. Confirm ServiceMonitor or equivalent metrics scraping is active.\n5. Confirm backup and restore evidence exists for Postgres and runtime secrets.\n\n## Next Step\n\nUse [Deploy on Managed Kubernetes](/operations/cloud-reference-deployments/) for a concrete External Secrets, cert-manager, managed Postgres, and managed Redis deployment."},{"id":"/operations/cloud-reference-deployments","title":"Deploy on Managed Kubernetes","url":"https://docs.caracal.run/operations/cloud-reference-deployments/","concepts":"","body":"This page turns the [Choose a Cloud Profile](/operations/cloud-native-profiles/) map into a concrete, copy-paste deployment. The chart is cloud-agnostic by design; the only per-cloud surface is the secret-store authentication, the storage class, and the ingress annotations.\n\nThe reference artifacts live in `infra/helm/caracal/examples`:\n\n- `values.cloud-managed.yaml` - production overlay with externalized state, cert-manager TLS Web/STS/API/Gateway ingress, ServiceMonitor, and tightened egress.\n- `external-secrets/` - External Secrets Operator stores for AWS, GCP, and Azure plus one cloud-agnostic `ExternalSecret`.\n\n## Prerequisites\n\n| Component                                                 | Why                                                                                          |\n| --------------------------------------------------------- | -------------------------------------------------------------------------------------------- |\n| Managed Postgres                                          | Authoritative product, policy, audit, and delegation state.                                  |\n| Managed Redis                                             | Streams for audit, revocation, policy invalidation, and coordination.                        |\n| [External Secrets Operator](https://external-secrets.io/) | Provisions `caracal-runtime` from your secret manager so no plaintext secret is rendered.    |\n| [cert-manager](https://cert-manager.io/)                  | Issues TLS certificates for Web, STS, and any optional API or Gateway ingress.                |\n| Prometheus Operator                                       | Consumes the chart `ServiceMonitor` and `PrometheusRule`.                                    |\n\n## 1. Store Secret Material\n\nLoad the Caracal secret material into your secret manager under the keys the `ExternalSecret` reads:\n\n| Secret manager key                                                                   | Contents                                                                                                                           |\n| ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------- |\n| `caracal/postgres`                                                                   | `username`, `password`, `host`, `port`, `database`, and `authDatabase` naming the dedicated web session database (`caracal_auth`). |\n| `caracal/redis`                                                                      | `password`, `host`, `port`, `database`.                                                                                            |\n| `caracal/zone-kek`                                                                   | Zone key-encryption key.                                                                                                           |\n| `caracal/audit-hmac-key`, `caracal/streams-hmac-key`, `caracal/idempotency-hmac-key`, `caracal/gateway-sts-hmac-key` | Integrity and idempotency keys.                                                                                                    |\n| `caracal/admin-token`, `caracal/coordinator-token`                                   | Control-plane tokens held by services and the web BFF.                                                                             |\n| `caracal/auth-secret`                                                                | Better Auth signing secret; the `ExternalSecret` composes the web session database URL from `caracal/postgres`.                    |\n| `caracal/sts-admin-token`, `caracal/audit-admin-token`, `caracal/metrics-bearer`     | Optional service tokens.                                                                                                           |\n\n## 2. Apply the Secret Store and ExternalSecret\n\nPick the store for your cloud and apply it with the agnostic `ExternalSecret`:\n\n```bash\ncd infra/helm/caracal/examples\nkubectl apply -f external-secrets/secretstore-aws.yaml\nkubectl apply -f external-secrets/externalsecret-runtime.yaml\n```\n\nThe `ExternalSecret` composes `databaseUrl`, `redisUrl`, and the per-service connection strings - each service connects as its own least-privilege database role, with passwords derived from the administrative password and provisioned by the migration job - and materializes every chart key, including `idempotencyHmacKey`, so `secrets.create` stays `false`.\n\n## 3. Install the Chart with the Overlay\n\n```bash\nhelm upgrade --install caracal infra/helm/caracal \\\n  --namespace caracal --create-namespace \\\n  -f infra/helm/caracal/examples/values.cloud-managed.yaml \\\n  --set secrets.database.host=<managed-postgres-host> \\\n  --set secrets.redis.host=<managed-redis-host>\n```\n\nStable mode enforces externalized Postgres and Redis hosts and forbids in-chart plaintext secrets, so the render fails fast if either is left at a default.\n\n## Per-Cloud Substitutions\n\n| Setting                              | AWS (EKS)                     | GCP (GKE)                                  | Azure (AKS)                                  |\n| ------------------------------------ | ----------------------------- | ------------------------------------------ | -------------------------------------------- |\n| Secret store                         | `secretstore-aws.yaml` (IRSA) | `secretstore-gcp.yaml` (Workload Identity) | `secretstore-azure.yaml` (Workload Identity) |\n| Managed Postgres                     | RDS / Aurora PostgreSQL       | Cloud SQL for PostgreSQL                   | Azure Database for PostgreSQL                |\n| Managed Redis                        | ElastiCache / MemoryDB        | Memorystore for Redis                      | Azure Cache for Redis                        |\n| `replayPersistence.storageClassName` | `gp3`                         | `premium-rwo`                              | `ma"},{"id":"/operations/enterprise-install-kit","title":"Package an Install Kit","url":"https://docs.caracal.run/operations/enterprise-install-kit/","concepts":"","body":"Use the enterprise install kit pattern when a platform team needs a controlled bundle of Caracal deployment assets, values, secrets instructions, verification steps, and rollback guidance. Keep the open-source and enterprise products isolated: do not import, copy, or depend on files across product roots.\n\n## Kit Contents\n\n| Artifact | Purpose |\n| --- | --- |\n| Release version | Exact Caracal image tag and chart version. |\n| Helm values | Environment-specific overrides for registry, tag, secrets, network policy, ingress, resources, replay persistence, and observability. |\n| Secret manifest | Required keys and where the platform secret manager stores them. |\n| Runbook | Install, verify, rollback, backup, restore, and incident steps. |\n| Evidence checklist | Rendered manifest, migration result, readiness output, smoke test, alert wiring, backup proof, and owner sign-off. |\n\n## Required Boundaries\n\n- Keep OSS local ports and enterprise local ports non-overlapping in examples.\n- Do not reference private source paths from open-source docs or configs.\n- Share behavior only through documented deployment contracts: images, chart values, APIs, event topics, and secrets.\n- Keep customer-specific values outside the repository.\n\n## Installation Sequence\n\n## Verification Evidence\n\n| Check | Evidence |\n| --- | --- |\n| Version pin | Image tags and chart values match the release approval. |\n| Secrets | Runtime Secret contains database, Redis, admin, Coordinator, zone KEK, audit HMAC, stream HMAC, and Gateway-STS HMAC material. |\n| Network | NetworkPolicy admits only required ingress and egress. |\n| Storage | Postgres migrations complete; Redis streams and groups exist. |\n| Runtime | `/ready` passes for API, STS, Gateway, Audit, and Coordinator. |\n| Observability | ServiceMonitor and PrometheusRule are installed or equivalent alerts exist. |\n| Recovery | Backup and restore runbook has been tested for Postgres and runtime secrets. |\n\n## Handoff Package\n\nInclude links to [Deploy with Helm](/operations/kubernetes-helm/), [Configure Service Environment](/operations/env-vars/), [Back Up and Retain Data](/operations/backup-retention/), [Configure Alerts](/operations/alerts/), and [Respond to Incidents](/operations/incident-response/).\n\n## Next Step\n\nUse [Hand Off to Platform Teams](/operations/platform-team-handoff/) when the install bundle is ready for production ownership."},{"id":"/operations/env-vars","title":"Configure Service Environment","url":"https://docs.caracal.run/operations/env-vars/","concepts":"","body":"Caracal services load configuration from environment variables and `*_FILE` secret variants. Use file-backed secrets in `rc` and `stable` modes.\n\nFor the installed runtime, set overrides in `$CARACAL_HOME/caracal.env` (Linux default `~/.local/share/caracal/caracal.env`); `caracal up` passes that file to Compose. In the development repository, overrides live in `infra/docker/local.env`.\n\n## Mode Requirements\n\n`CARACAL_MODE` selects one of two security postures, not three:\n\n- **`dev`** - the only relaxed posture. Permits local conveniences (open metrics, `INSECURE_*` overrides, generated/relaxed settings) for development on a single host. Never use it for anything reachable by others.\n- **Published (`rc` and `stable`)** - the production posture. Both are **security-identical and fail-closed**: valid HMAC keys are required, Gateway fail-open settings are denied, and incomplete service configuration is rejected before readiness succeeds.\n\nThe difference between `rc` and `stable` is **maturity, not security**. `rc` is a release-candidate build that enforces the full production trust boundary but may be functionally unstable; run `stable` for production workloads and reserve `rc` for staging or pre-release validation.\n\n| Mode     | Posture                                               | Use for                          |\n| -------- | ----------------------------------------------------- | -------------------------------- |\n| `dev`    | Relaxed (single-host only)                            | Local development                |\n| `rc`     | Published - fail-closed, may be functionally unstable | Staging / pre-release validation |\n| `stable` | Published - fail-closed                               | Production                       |\n\nPublished modes require valid HMAC keys, deny unsafe Gateway fail-open settings, and reject incomplete service configuration before readiness succeeds.\n\n## Shared Service Variables\n\n| Variable                                             | Mode      | Used by                               | Meaning                                                                                                                                                                               |\n| ---------------------------------------------------- | --------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `CARACAL_MODE`                                       | all       | All services                          | `dev`, `rc`, or `stable`.                                                                                                                                                             |\n| `PORT`                                               | all       | HTTP services                         | Fixed service port: API `3000`, STS `8080`, Gateway `8081`, Audit `9090`, Coordinator `4000`, Web Console BFF `3002`. Control runs in-process in API.                                 |\n| `HOST`                                               | all       | TypeScript services                   | Bind host; defaults to `0.0.0.0` in published modes.                                                                                                                                  |\n| `LOG_LEVEL`                                          | all       | Services                              | Log verbosity.                                                                                                                                                                        |\n| `DATABASE_URL` / `DATABASE_URL_FILE`                 | all       | API, STS, Gateway, Audit, Coordinator | Postgres connection for product, policy, audit, and delegation state.                                                                                                                 |\n| `REDIS_URL` / `REDIS_URL_FILE`                       | all       | API, STS, Gateway, Audit, Coordinator | Redis connection.                                                                                                                                                                     |\n| `STREAMS_HMAC_KEY` / `STREAMS_HMAC_KEY_FILE`         | rc+stable | API, STS, Gateway, Coordinator        | Signs Redis stream messages.                                                                                                                                                          |\n| `AUDIT_HMAC_KEY` / `AUDIT_HMAC_KEY_FILE`             | rc+stable | API, STS, Gateway, Audit, Control     | Signs or verifies audit events.                                                                                                                                                       |\n| `IDEMPOTENCY_HMAC_KEY` / `IDEMPOTENCY_HMAC_KEY_FILE` | rc+stable | Coordinator                           | Digests external idempotency keys before PostgreSQL storage. Hex-encoded, at least 32 bytes.                                                                                          |\n| `METRICS_BEARER` / `METRICS_BEARER_FILE`             | rc+stable | STS, Gateway, API, Audit, Coordinator | Metrics endpoint bearer token. Generated as the managed `metricsBearer` secret by `caracal up`; in published mode (`rc`/`stable`) the metrics endpoints fail closed (401) without it. |\n\n## Service-Specific Variables\n\n| Service           | Variables                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     "},{"id":"/operations/tls-hardening","title":"Harden Production","url":"https://docs.caracal.run/operations/tls-hardening/","concepts":"","body":"Production Caracal should run in published mode (`CARACAL_MODE=stable`, or `rc` for staging), use TLS at external boundaries, and keep stateful dependencies private. `rc` and `stable` enforce the same fail-closed trust boundary; `rc` is a release-candidate build that may be functionally unstable, so use `stable` for production.\n\n## Hardening Checklist\n\n| Area | Required stance |\n| --- | --- |\n| External TLS | Terminate TLS at ingress, load balancer, service mesh, or Gateway TLS files. The web console public edge must be HTTPS. |\n| Internal services | Prefer private ClusterIP or bridge-network traffic. |\n| Secrets | Use mounted secret files, not inline env vars. |\n| Containers | Drop Linux capabilities, set no-new-privileges, use read-only filesystems where supported. |\n| NetworkPolicy | Admit only required ingress and egress, including explicit ingress-controller traffic to the web tier. |\n| Gateway upstreams | Keep private upstreams disabled unless explicitly allowed and allowlisted. |\n| Runtime CLI | Do not expose product management as top-level runtime CLI commands. |\n\n## Web Console Edge\n\nThe production web tier is a same-origin deployment: the `caracal-web` image serves the built SPA and the backend-for-frontend from the same HTTPS origin. The BFF proxies `/api/console/*` to the internal API and Coordinator, so browser traffic does not reach those services directly and no cross-origin CORS or CSRF surface is needed for the standard deployment.\n\nUse HTTPS at the public edge and set `CARACAL_AUTH_URL` to that origin. The BFF pins `Secure`, `HttpOnly`, and `SameSite` session cookies, sets hardened response headers (`Content-Security-Policy` with `frame-ancestors 'none'`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer`, and HSTS when HTTPS is in use), enforces the request `Origin` on state-changing proxied calls, rate-limits credential endpoints, and validates proxied paths.\n\nFor a cross-site deployment where the browser site differs from the BFF site, set `CARACAL_WEB_ORIGIN` to the exact trusted browser origins and `CARACAL_AUTH_COOKIE_SAME_SITE=none`. This mode refuses to start unless cookies are Secure. A cross-origin deployment on sibling subdomains can keep the safer `lax` default when both origins are same-site. Packaged Helm and Compose serve the SPA same-origin.\n\nWith Helm NetworkPolicy enabled, add a `networkPolicy.extraIngress` rule for the ingress controller to reach the web service on port `3002`. API, Coordinator, and Gateway should remain private unless a deployment requires their endpoints externally.\n\n## Gateway Upstream Safety\n\nUpstream hosts are operator-provisioned through the Control API, never client-supplied, so the\nGateway routes to private and on-prem upstreams (internal tools, local MCPs) by default. Dangerous\ninfrastructure ranges that are never a legitimate upstream - cloud metadata (`169.254.0.0/16`),\nloopback, carrier-grade NAT, and multicast - are always blocked, including under DNS rebinding.\n\nTo pin egress to an explicit set of hosts as a hardening control, set:\n\n```bash\nUPSTREAM_HOST_ALLOWLIST=internal-api.example.svc,provider.example.com\n```\n\nProvider token endpoints and subject-issuer JWKS remain public-address-only by default. When an IdP is reachable over a private interconnect, set `CARACAL_PRIVATE_EGRESS_HOSTS` on API and STS to its exact DNS hostname. This exception permits private and unique-local addresses only for those names; loopback, metadata, link-local, multicast, and unspecified addresses remain blocked at resolution and connection time.\n\n## HMAC and Signing Keys\n\nPublished modes require production-grade keys:\n\n| Key | Used for |\n| --- | --- |\n| `AUDIT_HMAC_KEY` | Audit event integrity. |\n| `STREAMS_HMAC_KEY` | Redis stream message signatures. |\n| `GATEWAY_STS_HMAC_KEY` | Gateway-to-STS service exchange. |\n| `SECRET_STORE_KEK` | Secret Store envelope encryption. |\n\nHMAC keys must be hex-encoded and at least 32 bytes where validated.\n\n## Kubernetes Controls\n\nThe Helm chart sets non-root pods, RuntimeDefault seccomp, dropped capabilities, read-only root filesystem for service containers, optional NetworkPolicy, PDBs, HPAs, and optional Ingress TLS.\n\n## Troubleshooting\n\n| Symptom | Check |\n| --- | --- |\n| Published-mode startup fails | Missing or short HMAC key, unsafe URL, forbidden fail-open setting, or wrong service port. |\n| Gateway cannot reach upstream | NetworkPolicy egress, DNS egress, HTTPS egress, and upstream allowlist. |\n| Metrics endpoint denied | Confirm `METRICS_BEARER` and scraper configuration. |\n| TLS works externally but clients fail token validation | Confirm STS `ISSUER_URL` matches the URL clients use for JWKS and token issuer checks. |\n\n## Next Step\n\nUse [Rotate Keys and Secrets](/operations/key-management/) to plan secret storage, key overlap, and rotation evidence."},{"id":"/operations/key-management","title":"Rotate Keys and Secrets","url":"https://docs.caracal.run/operations/key-management/","concepts":"","body":"Caracal uses secret material for zone encryption, mandate signing, audit integrity, stream signatures, service exchange, admin access, Coordinator access, database access, and Redis access.\n\n## Key Inventory\n\n| Material                     | Purpose                                                                                                            |\n| ---------------------------- | ------------------------------------------------------------------------------------------------------------------ |\n| `SECRET_STORE_KEK`           | Root key for Secret Store envelopes: provider credentials, zone signing material, connection tokens, sink secrets. |\n| Zone signing keys            | Sign mandates exposed through STS JWKS.                                                                            |\n| `AUDIT_HMAC_KEY`             | Signs/verifies audit evidence.                                                                                     |\n| `STREAMS_HMAC_KEY`           | Signs Redis stream messages.                                                                                       |\n| `IDEMPOTENCY_HMAC_KEY`       | Digests Coordinator idempotency identifiers before storage.                                                        |\n| `GATEWAY_STS_HMAC_KEY`       | Authenticates Gateway exchanges to STS.                                                                            |\n| `CARACAL_ADMIN_TOKEN`        | Bootstrap/operator API access.                                                                                     |\n| `CARACAL_COORDINATOR_TOKEN`  | Coordinator Session and Delegation management.                                                                     |\n| Postgres and Redis passwords | Storage access.                                                                                                    |\n\n## Storage Boundary\n\nGenerated operator secrets are file-backed inside a private operator directory. Local development stores them outside the repository; released self-hosted runtimes store them under `$CARACAL_HOME/secrets` unless `CARACAL_SECRETS_DIR` points to another operator-managed directory. Cloud deployments should use the platform secret manager and mount secrets into services through `*_FILE` paths or `CARACAL_SECRETS_DIR`.\n\nNever mount operator secrets into agent workspaces or containers. Agents should receive only application client secrets at provisioning time or short-lived resource mandates from `caracal run`.\n\n## Rotation Principles\n\n- Rotate one class of material at a time.\n- Keep old verification material available until existing tokens, stream messages, or audit replay windows drain.\n- Verify `/ready`, audit ingestion, revocation propagation, and Gateway exchange after each rotation.\n- Record who rotated, when, why, new secret location, and rollback window.\n\n## Suggested Rotation Order\n\n## Rotation Checklist\n\n| Key                         | Services to roll                                                                                                                                                                                                                      | Verify                                                                                                                                 |\n| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |\n| `SECRET_STORE_KEK`          | API and STS with `SECRET_STORE_KEK_PREVIOUS` carrying the retiring key, then the re-seal sweep, then both services again without the previous key. [Configure Secret Backends](/operations/secret-backends/) documents the procedure. | Provider credentials decrypt, STS reads active signing material, and the sweep reports zero failures.                                  |\n| Zone signing key            | STS, then verifiers after JWKS cache windows.                                                                                                                                                                                         | JWKS exposes expected key IDs and old mandates expire or verify during overlap.                                                        |\n| `AUDIT_HMAC_KEY`            | Producers and Audit verifier: API, STS, Gateway, Audit, Control.                                                                                                                                                                      | Audit ingestion accepts new events and rejects mismatched signatures.                                                                  |\n| `STREAMS_HMAC_KEY`          | Producers and consumers: API, STS, Gateway, Coordinator, Audit.                                                                                                                                                                       | Consumers accept new stream messages and pending entries drain.                                                                        |\n| `IDEMPOTENCY_HMAC_KEY`      | Coordinator. Set the replacement as `IDEMPOTENCY_HMAC_KEY` and the retiring key as `IDEMPOTENCY_HMAC_KEY_PREVIOUS`; keep both for at least `IDEMPOTENCY_RETENTION_SECONDS`, then remove the previous key.                             | Existing receipts replay through the overlap; new receipt digests use only the replacement; conflict and replay metrics remain stable. |\n| `GATEWAY_STS_HMAC_KEY`      | API, STS, and Gateway.                                                                                                                                                                                                                | Gateway exchange succeed"},{"id":"/operations/postgres","title":"Operate PostgreSQL","url":"https://docs.caracal.run/operations/postgres/","concepts":"","body":"Postgres stores Caracal control-plane state: zones, providers, applications, resources, grants, Authority records, policies, audit events, Sessions, Delegations, outboxes, admin tokens, and step-up challenges.\n\n## Storage Model\n\n## Migrations\n\nThe migration runner:\n\n- applies `/migrations/*.up.sql` files in sorted order;\n- records versions in `public.schema_migrations` (schema-qualified so it survives migrations that reset `search_path`);\n- serializes concurrent runners with a Postgres advisory lock;\n- rejects unsafe migration filenames;\n- is used by Compose `dbMigrate` and Helm pre-install/pre-upgrade Job.\n\nThe schema starts from a single consolidated baseline (`0001_baseline.up.sql`). Later releases append expand-only migrations on top of it. Each migration must be backward compatible with the previous application version so it can be applied while that version still serves traffic; contract-phase changes are deferred to a later release and tagged `-- caracal:phase contract`. `validateMigrations.sh` enforces this, which is what lets [`caracal upgrade`](/operations/upgrade/) migrate without a maintenance window.\n\n```bash\nPGHOST=localhost PGPORT=5432 PGUSER=caracal PGDATABASE=caracal \\\nPGPASSWORD_FILE=\"${CARACAL_SECRETS_DIR:-${CARACAL_HOME:-$HOME/.local/share/caracal}/secrets}/postgresPassword\" \\\nbash infra/postgres/scripts/migrate.sh\n```\n\nProduction tooling is forward-only and does not reference down migrations.\n\n## Verification\n\n```bash\nbash infra/postgres/scripts/validateMigrations.sh\n```\n\nThe validation script checks migration idempotency, concurrent migrators, expand-only schema discipline, expected tables, append-only audit permissions, immutable policy-version trigger, fail-closed row-level security, and the rolling audit partition window.\n\n## Capacity Controls\n\n| Area             | Settings                                                                                                                                    |\n| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |\n| Compose Postgres | `POSTGRES_MAX_CONNECTIONS`, `POSTGRES_SHARED_BUFFERS`, `POSTGRES_EFFECTIVE_CACHE_SIZE`, `POSTGRES_WORK_MEM`, `POSTGRES_LOG_MIN_DURATION_MS` |\n| Service pools    | `DB_POOL_MAX`, statement timeout, idle-in-transaction timeout, connection timeout, idle timeout                                             |\n| Helm             | Use managed Postgres sizing plus service resource requests/limits.                                                                          |\n\n## Backup and Restore\n\nBack up the whole database. Audit events are append-only and partitioned; restore must preserve audit tables, admin audit tables, outboxes, Session/Delegation state, and key material references.\n\n## Troubleshooting\n\n| Phase          | Symptom             | First check                                                                                   |\n| -------------- | ------------------- | --------------------------------------------------------------------------------------------- |\n| Bootstrap      | Migration Job fails | Database secret, host, port, user, database, SSL mode, and advisory-lock connectivity.        |\n| Operations     | API outbox grows    | Redis reachability, outbox dispatcher logs, DB pool saturation, and `event_outbox` dead rows. |\n| Performance    | Audit queries slow  | Partition health, indexes, retention, and query time windows.                                 |\n| Access control | RLS errors          | Confirm service role and zone context handling before widening permissions.                   |\n\n## Next Step\n\nUse [Operate Redis Streams](/operations/redis/) to verify stream groups, pending entries, invalidation, revocation, and audit delivery."},{"id":"/operations/redis","title":"Operate Redis Streams","url":"https://docs.caracal.run/operations/redis/","concepts":"","body":"Redis Streams move operational events between Caracal services. Redis is not the durable source of product state; Postgres remains authoritative.\n\n## Streams and Groups\n\n| Stream                           | Consumer groups                   |\n| -------------------------------- | --------------------------------- |\n| `caracal.audit.events`           | `audit-ingestor`, `siem-export`   |\n| `caracal.audit.events.dlq`       | `audit-dlq-observer`              |\n| `caracal.policy.invalidate`      | `opa-engine`                      |\n| `caracal.sessions.revoke`        | `sts-revocation`                  |\n| `caracal.keys.invalidate`        | `sts-keys`                        |\n| `caracal.agents.lifecycle`       | `coordinator-relay`               |\n| `caracal.invocations.lifecycle`  | `invocations-observer`            |\n| `caracal.delegations.invalidate` | `delegations-observer`            |\n| `caracal.providers.ratelimit`    | initialized as a one-entry stream |\n\n## Provision\n\n```bash\nREDIS_HOST=localhost REDIS_PORT=6379 \\\nREDIS_PASSWORD_FILE=\"${CARACAL_SECRETS_DIR:-${CARACAL_HOME:-$HOME/.local/share/caracal}/secrets}/redisPassword\" \\\nbash infra/redis/provision-streams.sh\n```\n\nThe provisioner is idempotent, creates consumer groups with `MKSTREAM`, and warns when an existing stream length exceeds the intended bound.\n\n## Verify\n\n```bash\nREDIS_HOST=localhost REDIS_PORT=6379 \\\nREDIS_PASSWORD_FILE=\"${CARACAL_SECRETS_DIR:-${CARACAL_HOME:-$HOME/.local/share/caracal}/secrets}/redisPassword\" \\\nbash infra/redis/scripts/verify.sh\n```\n\nVerification checks stream existence, consumer group existence, and provisioner idempotency.\n\n## Event Path\n\nStream messages are signed with `STREAMS_HMAC_KEY` in published modes.\n\n## Durability and Eviction\n\nRedis is a correctness-critical store, not a disposable cache: revocation entries and unacknowledged stream messages must survive memory pressure and restarts. Configure every Caracal Redis with:\n\n| Setting            | Required value | Why                                                                                                                                     |\n| ------------------ | -------------- | --------------------------------------------------------------------------------------------------------------------------------------- |\n| `maxmemory-policy` | `noeviction`   | Any `allkeys-*`/`volatile-*` policy can silently drop revocation entries and pending stream messages, weakening revocation correctness. |\n| `appendonly`       | `yes`          | Persists stream and revocation state across restarts.                                                                                   |\n| `appendfsync`      | `everysec`     | Bounds data loss on crash to roughly one second.                                                                                        |\n\nThe bundled Redis image (`infra/redis/redis.conf`) already sets these. **External or managed Redis is your responsibility** - many managed tiers default to `allkeys-lru`. On startup each consumer service (STS, Gateway, Audit) reads the live `maxmemory-policy` and logs a warning when it is not `noeviction`, so a misconfigured store is visible in service logs immediately. The check never blocks startup and is skipped when the provider restricts `CONFIG GET`.\n\n## Troubleshooting\n\n| Symptom                           | Check                                                                                          |\n| --------------------------------- | ---------------------------------------------------------------------------------------------- |\n| Audit DLQ grows                   | HMAC failures, malformed producers, Redis memory pressure, and Audit database writes.          |\n| Policy changes do not take effect | `caracal.policy.invalidate` group health and STS policy age metrics.                           |\n| Revocation is delayed             | `caracal.sessions.revoke` pending entries and Gateway/STS consumer health.                     |\n| Provisioner warns about length    | Plan an explicit stream retention/reset; provisioning does not rewrite stream policy silently. |\n\n## Next Step\n\nUse [Scale Capacity](/operations/scale-capacity/) when storage, streams, or service pools approach operational limits."},{"id":"/operations/scale-capacity","title":"Scale Capacity","url":"https://docs.caracal.run/operations/scale-capacity/","concepts":"","body":"Scale Caracal around three bottlenecks: token exchange and policy evaluation in STS, upstream proxying in Gateway, and durable writes in Postgres/Audit.\n\n## Scaling Levers\n\n| Component   | Primary levers                                                                                                    |\n| ----------- | ----------------------------------------------------------------------------------------------------------------- |\n| API         | Replicas, `DB_POOL_MAX`, outbox batch/interval settings, request rate limits.                                     |\n| STS         | Replicas, OPA policy age, Redis invalidation health, `MAX_GRANT_TTL_SECONDS`, replay persistence.                 |\n| Gateway     | Replicas, `MAX_REQUEST_BYTES`, `STS_TIMEOUT`, `UPSTREAM_TIMEOUT`, upstream allowlist, revocation snapshot health. |\n| Audit       | Replicas, Postgres write capacity, DLQ thresholds, retention, S3 export settings.                                 |\n| Coordinator | Replicas, DB pool, sweeper intervals, outbox batch, and long-lived Session leases.                               |\n| Postgres    | Connection limits, indexes, partitions, storage IOPS, backup windows.                                             |\n| Redis       | Stream memory, pending entries, consumer lag, persistence, network latency.                                       |\n\n## Helm Defaults\n\nThe chart defaults to two replicas for API, STS, Gateway, Audit, and Coordinator. Gateway has a higher maximum HPA ceiling because protected traffic fans through it.\n\n| Service     | Default port | Default max HPA replicas |\n| ----------- | ------------ | ------------------------ |\n| API         | `3000`       | `8`                      |\n| STS         | `8080`       | `8`                      |\n| Gateway     | `8081`       | `16`                     |\n| Audit       | `9090`       | `8`                      |\n| Coordinator | `4000`       | `8`                      |\n\n## Capacity Signals\n\n| Signal                         | Meaning                                                                 |\n| ------------------------------ | ----------------------------------------------------------------------- |\n| Postgres pool ratio near `0.9` | Service pool saturation; inspect queries and pool size.                 |\n| Audit consumer lag             | Audit ingestion cannot keep up with Redis stream input.                 |\n| Audit replay backlog age       | STS/Gateway cannot emit audit events to Redis/Audit promptly.           |\n| Gateway STS circuit open       | Gateway is fast-failing because STS exchange is unhealthy.              |\n| Revocation propagation lag     | Access-safety state is not reaching Gateway within the expected window. |\n| Readiness flapping             | Pods or dependencies are unstable under current load.                   |\n\n## Scale Procedure\n\n1. Identify the bottleneck from metrics and logs.\n2. Scale stateless service replicas first when storage is healthy.\n3. Increase Postgres and Redis capacity before raising service pools.\n4. Verify readiness, lag, replay backlog, and DLQ after each change.\n5. Document the new limit and alert threshold.\n\n## Worked Escalation Patterns\n\n| Signal                               | First action                                                                                           | Expected outcome                                                                |\n| ------------------------------------ | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- |\n| Postgres pool ratio stays near `0.9` | Inspect slow queries, then raise storage capacity or service pool size one step.                       | `/ready` stabilizes and pool saturation falls before replicas increase further. |\n| Gateway STS circuit opens            | Check STS readiness and exchange latency, then scale STS or reduce Gateway fan-in.                     | Gateway stops fast-failing protected requests and audit evidence resumes.       |\n| Audit consumer lag grows             | Check Postgres write IOPS, partition health, and Audit replicas before extending retention or exports. | Redis pending entries and DLQ growth stop increasing.                           |\n| Revocation propagation lags          | Check Redis latency, stream consumers, and Gateway snapshot freshness.                                 | Gateway denial decisions reflect current revocation state.                      |\n\n## Troubleshooting\n\n| Symptom                             | First check                                                                       |\n| ----------------------------------- | --------------------------------------------------------------------------------- |\n| Higher replicas make failures worse | Postgres connection pressure or Redis latency.                                    |\n| Gateway latency spikes              | STS exchange latency, upstream timeout, JWKS cache, and private upstream egress.  |\n| Audit cannot catch up               | Postgres write IOPS, partition health, Audit replicas, and Redis pending entries. |\n\n## Next Step\n\nUse [Monitor Health and Metrics](/operations/observability/) to turn capacity signals into readiness gates and operator dashboards."},{"id":"/operations/observability","title":"Monitor Health and Metrics","url":"https://docs.caracal.run/operations/observability/","concepts":"","body":"Every service exposes health/readiness endpoints; most services also expose metrics. Use readiness for automation gates and health for liveness.\n\n## Endpoint Map\n\n| Service | Health | Readiness | Metrics |\n| --- | --- | --- | --- |\n| API | `/health` | `/ready` | `/metrics` |\n| STS | `/health` | `/ready` | `/metrics`, `/metrics.json` |\n| Gateway | `/health` | `/ready` | `/metrics`, `/metrics.json` |\n| Audit | `/health` | `/ready` | `/metrics`, `/metrics.json` |\n| Coordinator | `/health` | `/ready` | `/metrics` |\n| Control | `/health` | `/ready` | Not primary operator surface |\n\n## Metrics Authentication\n\nIn published builds (`CARACAL_MODE=rc` or `stable`), every metrics endpoint fails closed: it returns `401` unless `METRICS_BEARER` is set and the scraper presents `Authorization: Bearer <token>`. This applies to API, STS, Gateway, Audit, and Coordinator, all of which are reachable on the internal service network. `caracal up` generates the managed `metricsBearer` secret and mounts it into every service via `METRICS_BEARER_FILE`; the web console **Diagnostics** checks discover the same secret and authenticate their metrics probes automatically. Point external scrapers at the same secret file. In `dev` mode metrics stay open for local inspection.\n\n## Readiness Ladder\n\n| Rung | What it proves | User-facing impact when it fails |\n| --- | --- | --- |\n| Process health | The service process can answer liveness. | Restart loops or dead containers. |\n| Postgres and Redis | Durable state and stream/cache dependencies are reachable. | Management writes, token exchange, audit, or revocation can fail. |\n| Streams and outbox | Event delivery paths are draining. | Decisions may succeed while evidence or invalidation lags. |\n| Service readiness | Service-specific invariants are met. | That service should not receive production traffic. |\n| End-to-end smoke test | API, STS, Gateway, Audit, and Coordinator work together. | User workflows may fail even when individual services look healthy. |\n\n## Local Checks\n\n```bash\ncaracal status\ncaracal status --ready\nbash infra/scripts/smokeTest.sh\n```\n\n`smokeTest.sh` probes API `/ready` and `/health`, Gateway `/ready`, STS `/ready`, Audit `/ready`, and Coordinator `/ready`.\n\n## Kubernetes Checks\n\n```bash\nkubectl -n caracal get pods\nkubectl -n caracal get servicemonitor,prometheusrule\nkubectl -n caracal logs deploy/caracal-api\n```\n\nEnable `serviceMonitor.enabled` when using Prometheus Operator. Keep chart alert rules enabled or provide equivalent alerts.\n\n## Operator Diagnostics\n\nUse Console `diagnostics` for API health, readiness, zone diagnostics, and local preflight checks. Use Console `audit` and `request trace` views for decision investigation.\n\n## Troubleshooting\n\n| Symptom | Check |\n| --- | --- |\n| Health passes but readiness fails | Dependency, stream, outbox, policy, revocation, or audit readiness. |\n| Metrics scrape returns unauthorized | Published builds require `METRICS_BEARER`; set it and match the scraper's bearer token. |\n| Readiness flaps | CPU throttling, OOM, Postgres/Redis latency, probe timeouts, or dependency restarts. |\n| Smoke test fails only for API `/health` | Confirm the API liveness endpoint responds in the deployment shape being tested. |\n\n## Next Step\n\nUse [Configure Alerts](/operations/alerts/) to wire the readiness, audit, revocation, policy, and capacity signals into on-call response."},{"id":"/operations/alerts","title":"Configure Alerts","url":"https://docs.caracal.run/operations/alerts/","concepts":"","body":"The Helm chart can render PrometheusRule alerts for policy freshness, audit health, outbox health, Gateway exchange, revocation safety, database saturation, and readiness flapping.\n\n## Built-In Alerts\n\n| Alert | Severity | First response |\n| --- | --- | --- |\n| `CaracalSTSOPACompileErrors` | warning | Inspect policy activation history and STS logs; affected policy loads fail closed. |\n| `CaracalSTSPolicyBundleStale` | warning | Check Redis policy invalidation and STS policy age. |\n| `CaracalSTSProviderRefreshErrors` | warning | Check Redis coordination and upstream provider availability. |\n| `CaracalSTSProviderCircuitOpen` | warning | Restore provider or credential health; provider-backed grants fail closed. |\n| `CaracalAuditDLQNonEmpty` | warning | Inspect Audit DLQ records and producer HMAC/signature failures. |\n| `CaracalAuditDLQGrowth` | critical | Stop risky rollouts and recover Audit/Redis/Postgres. |\n| `CaracalAuditConsumerLagHigh` | warning | Scale Audit or restore Postgres/Redis performance. |\n| `CaracalGatewayAuditReplayBacklogOld` | warning | Recover Redis/Audit and confirm Gateway replay drains. |\n| `CaracalSTSAuditReplayBacklogOld` | warning | Recover Redis/Audit and confirm STS replay drains. |\n| `CaracalAuditTamperDetected` | critical | Treat as a security incident. |\n| `CaracalAPIOutboxDeadMessages` | critical | Reconcile outbox rows and Redis consumers before continuing rollouts. |\n| `CaracalAPIOutboxPendingOldest` | warning | Check Redis health, API outbox workers, DB pool, and stream memory. |\n| `CaracalGatewaySTSExchangeErrors` | warning | Check Gateway readiness, STS readiness, bindings, and service exchange key. |\n| `CaracalGatewaySTSCircuitOpen` | critical | Restore STS or service exchange before routing protected traffic. |\n| `CaracalGatewayRevocationSnapshotStale` | critical | Treat as access-safety incident until snapshot freshness returns. |\n| `CaracalGatewayRevocationPropagationLag` | critical | Restore revocation stream consumers and pending-entry reclamation. |\n| `CaracalGatewayRevocationReloadErrors` | critical | Fix Postgres/Redis snapshot load before relying on protected routes. |\n| `CaracalPostgresPoolSaturation` | warning | Inspect long queries, pool sizing, and connection leaks. |\n| `CaracalReadinessFlapping` | warning | Check dependencies, throttling, OOM, and probe timeouts. |\n\n## CaracalSTSOPACompileErrors\n\nSTS is reporting OPA policy compilation failures. Inspect policy activation history and STS logs for the failing bundle; affected policy loads fail closed until the bundle compiles successfully.\n\n## CaracalSTSPolicyBundleStale\n\nSTS policy age is above the configured freshness threshold. Check Redis policy invalidation, PostgreSQL polling, and STS readiness before relying on newly activated policies.\n\n## CaracalSTSProviderRefreshErrors\n\nSTS provider refresh coordination is failing. Restore Redis coordination and upstream provider availability; provider-backed grants may fail closed while refresh results cannot be coordinated.\n\n## CaracalSTSProviderCircuitOpen\n\nSTS has opened a provider refresh circuit after repeated provider failures. Restore the provider or credential health before retrying provider-backed grants.\n\n## CaracalAuditDLQNonEmpty\n\nThe Audit dead-letter queue contains records. Inspect Audit logs, DLQ records, producer HMAC/signature failures, and Redis stream health before treating evidence capture as healthy.\n\n## CaracalAuditDLQGrowth\n\nThe Audit dead-letter queue is growing. Stop risky rollouts and recover Audit, Redis, or Postgres before continuing changes that depend on complete audit evidence.\n\n## CaracalAuditConsumerLagHigh\n\nAudit ingestion is behind the Redis stream. Scale Audit or restore Postgres and Redis performance until consumer lag returns below the configured threshold.\n\n## CaracalGatewayAuditReplayBacklogOld\n\nGateway has audit replay files waiting on disk beyond the configured threshold. Recover Redis or Audit and confirm Gateway replay drains before continuing risky rollouts.\n\n## CaracalSTSAuditReplayBacklogOld\n\nSTS has audit replay files waiting on disk beyond the configured threshold. Recover Redis or Audit and confirm STS replay drains before continuing risky rollouts.\n\n## CaracalAuditTamperDetected\n\nAudit chain verification detected a mismatch, ordering break, or stored HMAC failure. Treat this as a security incident and preserve evidence before attempting recovery.\n\n## CaracalAPIOutboxDeadMessages\n\nThe API transactional outbox has abandoned messages. Recover Redis or stream consumers, inspect API logs, and reconcile affected outbox rows before continuing rollouts.\n\n## CaracalAPIOutboxPendingOldest\n\nThe API transactional outbox is not draining promptly. Check Redis health, API outbox workers, database pool saturation, and stream memory pressure.\n\n## CaracalGatewaySTSExchangeErrors\n\nGateway cannot reliably exchange mandates with STS. Check Gateway readiness, STS readiness, route bindings, and the service exchange key before routing protected traffic.\n\n## CaracalGatewaySTSCircuitOpen\n\nGateway is fast-failing STS exchanges after repeated STS-unavailable failures. Restore STS or service exchange health before routing protected traffic that requires exchanged authority.\n\n## CaracalGatewayRevocationSnapshotStale\n\nGateway cannot prove it has a fresh revocation baseline. Treat this as an access-safety incident until Postgres and Redis revocation snapshot loading are healthy.\n\n## CaracalGatewayRevocationPropagationLag\n\nRevocation stream messages are reaching Gateway outside the configured safety window. Restore Redis consumer health and confirm pending entries are reclaimed.\n\n## CaracalGatewayRevocationReloadErrors\n\nGateway cannot refresh revocation state from Postgres. Treat this as an access-safety incident until reloads succeed and snapshot freshness returns.\n\n## CaracalPostgresPoolSaturation\n\nA service is holding most of its Postgres connection pool. Inspect long-running queries, statement timeouts, pool sizing, and conn"},{"id":"/operations/troubleshooting","title":"Troubleshoot by Symptom","url":"https://docs.caracal.run/operations/troubleshooting/","concepts":"","body":"Most Caracal failures are easy to fix once you know **where** they happened. This page starts from the symptom you can see - usually an SDK error or an HTTP status - and routes you to the failing surface, the object to inspect, and the tool that confirms it. It complements [Debug Infrastructure Issues](/operations/debugging/), which works from infrastructure inward; start here when you have an application-level failure and an error or request ID.\n\n## Locate the Surface First\n\nThe web console **Audit** decision trace is the fastest disambiguator: filter by one request ID to tie it to the resource, subject, scopes, determining policies, diagnostics, and result, so you can see whether the denial came from STS policy or from a resource verifier.\n\n:::note[FAQ]\n[What is the difference between a 403 from STS and a 403 from Gateway?](/reference/faq/#faq-016)\n:::\n\n## Route by Symptom\n\n| Symptom                                                       | Surface                        | Inspect                                                                                        | Tool                                                           |\n| ------------------------------------------------------------- | ------------------------------ | ---------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |\n| SDK throws `config_missing` or cannot start                   | SDK / runtime config           | Runtime profile, secret file, and required environment variables.                              | `caracal status --json`, profile file                          |\n| `401` from API or Console                                     | Authentication                 | Admin or Control token, or workload credential source.                                         | [Use the Console](/runtime-console/console/) credential inputs |\n| `403` from a token exchange (`access_denied`)                 | STS policy                     | Active policy-set activation, application ID, subject, requested scopes, and grant.            | `request trace`, Console `audit`                               |\n| `403` at the resource (`invalid_token`, `scope_insufficient`) | Gateway or in-process verifier | Mandate validity, issuer/JWKS trust, `X-Caracal-Resource`, binding, and verifier scope checks. | [Proxy Through Gateway](/api/gateway/), verifier logs          |\n| `scope_insufficient` even after a policy allow                | Resource definition            | Whether the resource defines the scope and the policy authorizes it.                           | Console `resource`, `request trace`                            |\n| Call works but no audit event appears                         | Audit pipeline                 | Selected zone, time window, Redis stream health, and Audit readiness.                          | Console `diagnostics`, `audit`                                 |\n| Access continues after revocation                             | Resource verifier              | Whether revocation consumers run on the resource server.                                       | [Sessions and Revocation](/concepts/sessions-revocation/)      |\n\n## Denied SDK Call Steps\n\nWhen an SDK call returns access denied and you are not sure why, walk these in order. Each step either fixes the problem or hands you to the next surface.\n\n1. **Confirm the runtime is healthy.** Run `caracal status --ready`. If a dependency is down, fix that first.\n2. **Confirm configuration.** Check the runtime profile, secret file, selected zone, and resource identifier the SDK is using. A wrong resource ID or zone produces denials that look like policy failures.\n3. **Trace the request.** Open web console **Audit**, filter by the request ID, and open its decision trace. It shows the resource, subject, requested scopes, determining policies, and result.\n4. **Check the grant and policy.** Confirm a grant binds the application and subject to the resource scopes, and that the active policy set authorizes them. See [Debug Authorization Decisions](/guides/authorize-access/).\n5. **Check policy-set activation.** A policy that exists but is not the active set will not take effect. See [Activate a Policy Set](/guides/activate-policy-set/).\n6. **Check the requested scopes.** Confirm the SDK requests scopes the resource defines and the policy allows; a typo'd scope denies silently.\n7. **Check the Gateway route or verifier.** If STS allowed the exchange but the resource rejected it, inspect the resource's upstream URL, or the in-process verifier's scope and identity checks. See [Proxy Through Gateway](/api/gateway/).\n8. **Confirm the audit trace.** Open Console `audit` for the request ID to read the final decision and diagnostics.\n\n## Diagnostic Bundle\n\nCaracal exposes the triage bundle through existing surfaces rather than a separate command. Gather these together when you open an incident or escalate:\n\n| Item                                          | Where                                                     |\n| --------------------------------------------- | --------------------------------------------------------- |\n| Config summary and selected zone              | `caracal status --json`, runtime profile                  |\n| Service readiness and dependencies            | Console `diagnostics` (Doctor: `health`, `readiness`)     |\n| Visible zones, resources, and policy sets     | Console `diagnostics` (Doctor: `zones`)                   |\n| Deployment and operator environment checks    | Console `diagnostics` (Doctor: `preflight`)               |\n| Resource-to-provider binding and policy allow | [Check Provider Readiness](/examples/provider-preflight/) |\n| Last denial and request trace                 | Console `audit` and `request trace`                       |\n\nThe Console `diagnostics` Doctor view runs `health`, `readiness`, `zones`, and `preflight` in one place, including strict readiness"},{"id":"/operations/debugging","title":"Debug Infrastructure Issues","url":"https://docs.caracal.run/operations/debugging/","concepts":"","body":"Debug from the boundary inward: runtime lifecycle, readiness, storage, streams, service config, then request-specific audit evidence.\n\nFor an application-level failure where you have an error or request ID but not the failing surface, start from the symptom-first [Troubleshoot by Symptom](/operations/troubleshooting/) instead.\n\n## Triage Flow\n\n## Commands\n\n| Environment | Commands                                                                                                         |\n| ----------- | ---------------------------------------------------------------------------------------------------------------- |\n| Local       | `caracal status --ready`, `docker compose ps`, `docker compose logs <service>`                                   |\n| Helm        | `kubectl -n caracal get pods,svc,jobs`, `kubectl -n caracal describe pod <pod>`, `kubectl -n caracal logs <pod>` |\n| Storage     | `infra/postgres/scripts/validateMigrations.sh`, `infra/redis/scripts/verify.sh`                                  |\n| App-level   | Console `diagnostics`, `audit`, and `request trace`                                                              |\n\n## Common Cases\n\n| Symptom                       | Likely area                                                                                              |\n| ----------------------------- | -------------------------------------------------------------------------------------------------------- |\n| `401` or `403` from API       | Admin token, scope, Control token, or workload credential source.                                        |\n| STS exchange fails            | Zone ID, application ID, grant, policy, client secret, step-up, or STS readiness.                        |\n| Gateway fails before upstream | Mandate verification, STS exchange, binding, revocation snapshot, or upstream allowlist.                 |\n| Session views fail            | Coordinator URL/token, selected zone, or Coordinator readiness.                                          |\n| Audit event missing           | Redis stream health, Audit readiness, DLQ, replay backlog, or request never reaching protected boundary. |\n\n## Request Investigation\n\n1. Capture request ID, zone ID, subject, resource, and timestamp.\n2. Open Console `audit` or `request trace`.\n3. Confirm policy decision, scopes, target resource, Authority record, Session, and Delegation.\n4. Compare token claims with resource-server verifier settings.\n5. Check revocation and step-up state when authority appears valid but access fails.\n\n## Escalation Bundle\n\nInclude readiness output, relevant logs, service versions, Helm values diff, Redis stream status, Postgres migration status, request ID, audit explanation, and any recent secret or policy changes.\n\n## Next Step\n\nUse [Recover from Failures](/operations/failure-modes/) when infrastructure debugging identifies a degraded dependency, service, stream, or safety guarantee."},{"id":"/operations/failure-modes","title":"Recover from Failures","url":"https://docs.caracal.run/operations/failure-modes/","concepts":"","body":"Caracal is designed to fail closed for access-safety boundaries. Recovery should restore evidence, revocation, and policy freshness before reopening risky traffic.\n\n## Failure Matrix\n\n| Failure               | User impact                                                                      | Recovery                                                                                                       |\n| --------------------- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |\n| Postgres unavailable  | API/STS/Gateway/Audit/Coordinator readiness fails or degrades.                   | Restore Postgres, confirm migrations, check pools, replay outboxes.                                            |\n| Redis unavailable     | Streams, revocation, policy invalidation, audit ingestion, and coordination lag. | Restore Redis, verify streams/groups, watch pending entries and replay backlog.                                |\n| STS unavailable       | Token exchange and Gateway exchanges fail.                                       | Restore STS readiness, policy bundle freshness, JWKS, and HMAC config.                                         |\n| Gateway unhealthy     | Protected upstream traffic fails before provider dispatch.                       | Check bindings, STS exchange, revocation snapshot, upstream allowlist, and audit replay.                       |\n| Audit unhealthy       | Evidence ingestion delayed; DLQ or replay grows.                                 | Recover Audit/Postgres/Redis, replay DLQ, verify tamper chain.                                                 |\n| Coordinator unhealthy | Session and Delegation views and lifecycle management fail.                       | Restore Coordinator readiness, token, DB, Redis, and sweeper health.                                           |\n| Control unhealthy     | Automation dispatch unavailable.                                                 | Use Console/Admin SDK directly if appropriate; restore Control gate, token, JWKS, Redis, and API reachability. |\n\n## Recovery Order\n\n## Data-Flow Reconciliation\n\n1. Check Postgres migrations and expected tables.\n2. Check Redis stream groups and pending entries.\n3. Check API and Coordinator outbox tables.\n4. Check Audit DLQ and audit replay directories.\n5. Check Gateway revocation snapshot freshness.\n\n## Troubleshooting\n\n| Symptom                           | Action                                                                     |\n| --------------------------------- | -------------------------------------------------------------------------- |\n| Requests fail closed after outage | Confirm STS policy freshness, revocation snapshot, and audit replay drain. |\n| Events appear duplicated          | Verify idempotent consumers and dedupe keys before manual cleanup.         |\n| Rollback does not restore service | Schema may have moved forward; roll forward with a compatible fix.         |\n\n## Next Step\n\nUse [Run Failure Drills](/operations/failure-drills/) to rehearse the recovery path before a production incident."},{"id":"/operations/failure-drills","title":"Run Failure Drills","url":"https://docs.caracal.run/operations/failure-drills/","concepts":"","body":"A failure drill injects one fault, confirms the **expected alert** fires, observes readiness behavior, then validates recovery. Run these against a non-production cluster before you depend on Caracal in production. The alerts referenced here are the [PrometheusRule recipes](/operations/alerts/) shipped by the chart; the recovery steps extend [Recover from Failures](/operations/failure-modes/).\n\nCaracal fails closed for access-safety boundaries, so a healthy drill ends with evidence, revocation, and policy freshness restored **before** traffic resumes.\n\n## How to Run a Drill\n\n1. Confirm a green baseline: all `/ready` endpoints pass and no alerts are firing.\n2. Inject the fault from the drill.\n3. Confirm the expected alert transitions to firing within its `for` window.\n4. Observe the documented readiness and traffic behavior.\n5. Remove the fault and complete the recovery validation.\n6. Record time-to-detect and time-to-recover.\n\n## Drill Catalog\n\n### Redis outage\n\n| Field             | Value                                                                                                                                                                                                             |\n| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Inject            | Scale managed Redis to unavailable, or block the Redis egress port.                                                                                                                                               |\n| Expected alerts   | `CaracalAuditConsumerLagHigh`, `CaracalGatewayRevocationSnapshotStale`, `CaracalGatewayRevocationReloadErrors`, and replay backlog via `CaracalGatewayAuditReplayBacklogOld` / `CaracalSTSAuditReplayBacklogOld`. |\n| Expected behavior | Streams, revocation refresh, and audit ingestion lag; STS and Gateway write audit to replay volumes; readiness degrades for affected services.                                                                    |\n| Recover           | Restore Redis, verify streams and consumer groups, drain replay backlog and DLQ, confirm revocation snapshot is fresh before resuming risky traffic.                                                              |\n\n### Postgres outage\n\n| Field             | Value                                                                                                                |\n| ----------------- | -------------------------------------------------------------------------------------------------------------------- |\n| Inject            | Fail over or block the managed Postgres endpoint.                                                                    |\n| Expected alerts   | `CaracalPostgresPoolSaturation`, `CaracalAPIOutboxPendingOldest`, and `CaracalReadinessFlapping`.                    |\n| Expected behavior | API, STS, Gateway, Audit, and Coordinator readiness fail or degrade; outbox delivery stalls.                         |\n| Recover           | Restore Postgres, confirm migrations applied, check connection pools, replay outboxes, confirm readiness stabilizes. |\n\n### STS unreachable from Gateway\n\n| Field             | Value                                                                                                                             |\n| ----------------- | --------------------------------------------------------------------------------------------------------------------------------- |\n| Inject            | Scale STS to zero or block Gateway-to-STS traffic.                                                                                |\n| Expected alerts   | `CaracalGatewaySTSExchangeErrors` then `CaracalGatewaySTSCircuitOpen`.                                                            |\n| Expected behavior | Gateway token exchanges fail closed; protected upstream traffic is rejected before provider dispatch.                             |\n| Recover           | Restore STS readiness, JWKS, HMAC config, and policy bundle freshness; confirm the circuit closes and a canary exchange succeeds. |\n\n### Stale policy bundle\n\n| Field             | Value                                                                                     |\n| ----------------- | ----------------------------------------------------------------------------------------- |\n| Inject            | Pause policy distribution or hold the STS policy bundle past its freshness budget.        |\n| Expected alerts   | `CaracalSTSPolicyBundleStale`.                                                            |\n| Expected behavior | STS keeps evaluating the last good bundle; new policy activations do not take effect.     |\n| Recover           | Restore distribution, confirm the active policy set version, and verify the alert clears. |\n\n### Bad policy activation\n\n| Field             | Value                                                                                                      |\n| ----------------- | ---------------------------------------------------------------------------------------------------------- |\n| Inject            | Activate a Rego policy set with a compile error in a test zone.                                            |\n| Expected alerts   | `CaracalSTSOPACompileErrors`.                                                                              |\n| Expected behavior | STS rejects the broken bundle and continues on the last good policy; the activation does not widen access. |\n| Recover           | Roll the policy set forward to a valid version and confirm the alert clears.                               |\n\n### Provider credential refresh failure\n\n| Field             | Value                                                                                         |\n| ----------------- | --------------------------------------------------------------------------------------"},{"id":"/operations/backup-retention","title":"Back Up and Retain Data","url":"https://docs.caracal.run/operations/backup-retention/","concepts":"","body":"Backups must preserve both access state and evidence state. A restore that loses audit, revocation, keys, or delegation data can produce unsafe or unauditable behavior.\n\n## What to Protect\n\n| Asset                            | Why it matters                                                                                          |\n| -------------------------------- | ------------------------------------------------------------------------------------------------------- |\n| Postgres database                | Product state, policies, grants, Authority records, Sessions, audit events, Delegations, outboxes.      |\n| Runtime secrets                  | Database/Redis credentials, admin token, Coordinator token, zone KEK, HMAC keys, service exchange keys. |\n| STS/Gateway replay volumes       | Audit replay files during Redis/Audit outages.                                                          |\n| Redis snapshot or managed backup | Optional operational recovery for stream pending entries; Postgres remains authoritative.               |\n| Audit exports                    | Long-term evidence and SIEM/compliance integration.                                                     |\n\n## Backup Flow\n\n## Compose Stack Tooling\n\nSelf-hosted Compose deployments ship first-party backup and restore scripts. Both resolve containers through Compose labels, so they work against any project name without knowing the compose file path.\n\n`infra/scripts/backup.sh` runs online against the live stack: transaction-consistent `pg_dumpall`/`pg_dump` snapshots of every database, a Redis copy taken after a completed AOF rewrite, and the STS/Gateway replay-protection state, bundled into one timestamped archive.\n\n```bash\nCARACAL_BACKUP_DIR=/var/backups/caracal bash infra/scripts/backup.sh\n```\n\n| Variable                  | Default     | Purpose                              |\n| ------------------------- | ----------- | ------------------------------------ |\n| `CARACAL_COMPOSE_PROJECT` | `caracal`   | Compose project to back up.          |\n| `CARACAL_BACKUP_DIR`      | `./backups` | Bundle output directory.             |\n| `CARACAL_BACKUP_RETAIN`   | `7`         | Bundles kept; older ones are pruned. |\n\nBundles never contain secrets. Back up the secrets directory through your secret-management workflow and encrypt bundles at rest.\n\n`infra/scripts/restore.sh` performs disaster recovery from a bundle: it stops application services, recreates every database from its dump, replaces the Redis dataset and replay state, then restarts the stack. It refuses to run without explicit confirmation:\n\n```bash\nCARACAL_RESTORE_CONFIRM=yes bash infra/scripts/restore.sh /var/backups/caracal/caracalBackup-20260625T120000Z.tar.gz\n```\n\nAfter restore, verify with `infra/scripts/smokeTest.sh` and the [restore validation](#restore-validation) checklist below. Kubernetes deployments in stable mode require externally managed HA Postgres and Redis, so backups there belong to your data-platform tooling, not these scripts.\n\n## Retention Controls\n\n| Area             | Controls                                                                                                                                                                                                                                         |\n| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| Audit database   | `AUDIT_RETENTION_DAYS`, partitions, audit export watermarks.                                                                                                                                                                                     |\n| Coordinator data | `DELEGATION_RETENTION_DAYS`, `OUTBOX_RETENTION_DAYS`, `IDEMPOTENCY_RETENTION_SECONDS` (seven days by default), sweeper intervals. Restores also restore live replay receipts; this prevents pre-backup operations from being silently recreated. |\n| Redis streams    | Provisioner intended max lengths and managed Redis retention.                                                                                                                                                                                    |\n| Backups          | Platform backup policy and legal/compliance requirements.                                                                                                                                                                                        |\n\n## Restore Validation\n\n1. Restore Postgres into an isolated environment.\n2. Restore required secrets into the environment secret store.\n3. Run migration verification.\n4. Start services and verify `/ready`.\n5. Confirm audit query, policy-set activation state, resource routing, Authority records, Sessions, and Delegations.\n6. Run a canary token exchange and protected Gateway request.\n\n## Troubleshooting\n\n| Symptom                          | Check                                                                                      |\n| -------------------------------- | ------------------------------------------------------------------------------------------ |\n| Restored STS cannot decrypt keys | `SECRET_STORE_KEK` does not match the envelopes in the database.                           |\n| Audit chain verification fails   | Missing audit rows, wrong `AUDIT_HMAC_KEY`, or partial restore.                            |\n| Gateway cannot route             | Missing resource rows or resources restored without their upstream URL and provider.       |\n| Revocation state is incomplete   | Restore Postgres revocation/session state and replay Redis revocation events where needed. |\n\n## Next Step\n\nUse [Respond to Incidents](/operations/incident-response/) to define containment, evidence preservation, and recovery validation."},{"id":"/operations/incident-response","title":"Respond to Incidents","url":"https://docs.caracal.run/operations/incident-response/","concepts":"","body":"Treat incidents involving audit tamper evidence, stale revocation state, STS/Gateway fail-open risk, secret exposure, or policy corruption as security incidents.\n\n## Severity Triggers\n\n| Trigger                                              | Severity                                               |\n| ---------------------------------------------------- | ------------------------------------------------------ |\n| `CaracalAuditTamperDetected`                         | Critical security incident.                            |\n| Gateway revocation snapshot stale or propagation lag | Critical access-safety incident.                       |\n| Gateway STS circuit open during protected traffic    | Critical availability/access incident.                 |\n| Audit DLQ growth or replay backlog aging             | Critical evidence pipeline incident when sustained.    |\n| Secret exposure                                      | Critical until rotated and blast radius is understood. |\n| Policy activation causes unexpected broad allow      | Critical authorization incident.                       |\n\n## Response Flow\n\n## First-Hour Runbook\n\n| Symptom            | Severity | Contain                                                   | Preserve                                                    | Validate recovery                                                |\n| ------------------ | -------- | --------------------------------------------------------- | ----------------------------------------------------------- | ---------------------------------------------------------------- |\n| Audit tamper alert | Critical | Freeze changes and stop destructive cleanup.              | Audit tables, exports, DLQ, service logs.                   | Tamper sweep result, audit ingestion, and timeline are recorded. |\n| Revocation stale   | Critical | Fail closed at Gateway or resource servers.               | Redis streams, revocation snapshots, session IDs.           | Revoked sessions are denied and readiness is stable.             |\n| Bad policy allow   | Critical | Activate last known-good policy set.                      | Policy set version, request IDs, audit/explain output.      | Canary allow/deny decisions match expected policy.               |\n| Secret exposure    | Critical | Rotate exposed material and invalidate affected sessions. | Secret location, key IDs, affected services, audit records. | Old material no longer authenticates.                            |\n\n## Evidence to Preserve\n\n- Alert name, firing time, and labels.\n- Request IDs, zone IDs, resource IDs, policy-set versions, Session IDs, Delegation IDs.\n- Console audit/explain output.\n- Service logs for Web, API, STS, Gateway, Audit, Coordinator, and Control.\n- Redis stream status, pending entries, and DLQ contents.\n- Postgres backup or snapshot before manual remediation.\n- Helm values or Compose config diff.\n\n## Containment Actions\n\n| Incident         | Containment                                                                                              |\n| ---------------- | -------------------------------------------------------------------------------------------------------- |\n| Audit tamper     | Freeze changes, preserve database and audit exports, rotate only after evidence capture.                 |\n| Revocation stale | Fail closed at resource servers or Gateway, restore Redis/Postgres consumers, verify snapshot freshness. |\n| Bad policy allow | Activate last known-good policy set, verify canary denies/allows, inspect audit impact.                  |\n| Secret exposure  | Rotate exposed material, roll dependent services, invalidate sessions/grants as needed.                  |\n\n## Recovery Complete When\n\n- Relevant `/ready` endpoints pass.\n- Audit DLQ/replay/outbox backlogs are understood or drained.\n- Revocation and policy freshness metrics are healthy.\n- Canary token exchange and Gateway requests match expected decisions.\n- Evidence and timeline are preserved for review.\n\n## Next Step\n\nUse [Plan a Platform Rollout](/operations/platform-rollout-kit/) when the environment is stable and ready for controlled change."},{"id":"/operations/platform-rollout-kit","title":"Plan a Platform Rollout","url":"https://docs.caracal.run/operations/platform-rollout-kit/","concepts":"","body":"Use this runbook for version upgrades, configuration changes, chart changes, secret rotations, ingress changes, and policy rollout waves.\n\n## Rollout Gates\n\n| Gate | Pass condition |\n| --- | --- |\n| Source verification | Values, env vars, ports, and secrets match current chart and service config. |\n| Render validation | `helm lint` and `helm template` succeed with environment values. |\n| Migration safety | Migrations are forward-only and complete before service rollout. |\n| Readiness | Web, API, STS, Gateway, Audit, and Coordinator readiness endpoints pass. |\n| Event health | Redis streams, outbox dispatch, audit ingestion, revocation propagation, and replay backlog are healthy. |\n| Rollback plan | Previous image tag, chart revision, values, and compatible schema plan are documented. |\n\n## Rollout Sequence\n\n## Execution\n\n```bash\nhelm -n caracal diff upgrade caracal infra/helm/caracal -f values.production.yaml\nhelm -n caracal upgrade caracal infra/helm/caracal -f values.production.yaml\nkubectl -n caracal rollout status deploy/caracal-web\nkubectl -n caracal rollout status deploy/caracal-api\nkubectl -n caracal rollout status deploy/caracal-audit\nkubectl -n caracal rollout status deploy/caracal-coordinator\n```\n\nWhen replay persistence is enabled, STS and Gateway render as StatefulSets:\n\n```bash\nkubectl -n caracal rollout status statefulset/caracal-sts\nkubectl -n caracal rollout status statefulset/caracal-gateway\n```\n\n## Stop Conditions\n\nStop the rollout when any of these occur:\n\n- Migration Job fails.\n- Web, STS, or Gateway cannot prove readiness.\n- Audit DLQ grows or replay backlog ages.\n- Gateway revocation snapshot becomes stale.\n- API outbox dead messages appear.\n- Postgres pool saturation or Redis memory pressure persists.\n\n## Rollback\n\nUse `helm rollback` only after confirming the previous app version is compatible with the current database schema. If schema compatibility is uncertain, roll forward with a corrected app image instead of applying down migrations.\n\n## Next Step\n\nUse [Deploy Policy Changes](/operations/policy-deployment/) for policy-specific rollout gates, simulation, activation, and rollback."},{"id":"/operations/policy-deployment","title":"Deploy Policy Changes","url":"https://docs.caracal.run/operations/policy-deployment/","concepts":"","body":"Policy changes affect STS decisions and Gateway access. Treat them as production changes with simulation, activation, audit review, and rollback readiness.\n\n## Deployment Sequence\n\n## Pre-Deployment\n\n1. Confirm the policy uses current resource IDs, scopes, and canonical terminology.\n2. Validate syntax and invariants through Console or Admin API. Saving a policy-set version also compiles the merged bundle on the STS runtime engine, so cross-policy conflicts are rejected before the version exists.\n3. Simulate expected allow and deny cases.\n4. Confirm STS readiness.\n5. Note the last known-good policy-set version ID for rollback.\n\n## Activation\n\nUse Console `policy` and `policy set` views or Admin SDK/API automation. Do not use top-level `caracal` runtime commands for policy management.\n\n## Verification\n\nActivation returns `202 Accepted` with a `status_url`. Poll `GET /v1/zones/:zoneId/policy-sets/:id/activation-status` (Admin SDK: `policySets.activationStatus`) until `propagation_status` is `loaded`; it reconciles the database binding, the outbox dispatch state, and the live STS bundle in one response.\n\n| Check                   | Expected                                                                                 |\n| ----------------------- | ---------------------------------------------------------------------------------------- |\n| API activation response | `202` with `outbox_id` and `status_url`.                                                 |\n| Activation status       | `propagation_status: loaded`; `sts.policy_set_version_id` matches the activated version. |\n| STS bundle age          | `sts.age_seconds` in the activation status resets after reload.                          |\n| Audit decisions         | Expected allow/deny records appear for canary requests.                                  |\n| Gateway behavior        | Protected upstream access follows the new decision.                                      |\n\nWith several STS replicas, the replica that consumes the invalidation reloads within about a second and the rest converge through the periodic database poll (60 seconds by default). The activation status samples one replica, so allow one poll interval before treating the whole fleet as converged.\n\n## Rollback\n\nActivate the last known-good policy-set version. Then verify STS policy freshness, canary decisions, audit records, and Gateway behavior.\n\n## Troubleshooting\n\n| Symptom                                         | Check                                                                                                                                         |\n| ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |\n| Activation succeeds but decisions do not change | Poll the activation status: `outbox.state` shows whether the invalidation dispatched, and `sts` shows the bundle the runtime actually serves. |\n| Simulation differs from live decisions          | Input shape, active grants, resource IDs, subject/session claims, and step-up state.                                                          |\n| Policy fails closed unexpectedly                | Rego compile errors, missing scope/resource, or invalid grant.                                                                                |\n\n## Next Step\n\nUse [Upgrade Caracal](/operations/upgrade/) for image, chart, migration, and runtime configuration upgrades."},{"id":"/operations/upgrade","title":"Upgrade Caracal","url":"https://docs.caracal.run/operations/upgrade/","concepts":"","body":"Caracal upgrades are forward-only at the database layer. Every release ships **expand-only** schema changes: new migrations are backward compatible with the previous application version, so the database can be migrated while the old version is still serving. This removes the maintenance window - the schema moves first, then services roll onto it.\n\nContract-phase changes (dropping, renaming, retyping, or tightening a column to `NOT NULL`) are split into a later release and must be tagged `-- caracal:phase contract`. CI (`validateMigrations.sh`) rejects untagged contract changes so the no-window guarantee cannot silently regress.\n\n## Supported Upgrade Path\n\n`caracal upgrade` moves the stack to the release pinned inside the installed binary - it does not update the binary itself. Upgrading a Compose-based runtime is always two steps:\n\n1. Install the new release binary (re-run the install script from the release page).\n2. Run `caracal upgrade`.\n\nUpgrade one stable release at a time. Each release's migrations are validated against the immediately previous release; jumping across releases applies every pending migration in one pass, which is outside the validated path. A lock file (`$CARACAL_HOME/.lock`) serializes `caracal up`, `caracal down`, and `caracal upgrade` on a host; a concurrent invocation exits with an error instead of interleaving. A lock left by a crashed process is taken over automatically.\n\n## One-Command Upgrade (Runtime)\n\n```bash\ncaracal upgrade\n```\n\nThe command performs the expand-then-roll sequence in order:\n\n1. **Refresh runtime assets.** `$CARACAL_HOME/compose.yml` is rewritten to the release topology. `caracal.env` and existing secrets are left untouched; any secret the release newly requires is generated.\n2. **Stage images.** Pull the pinned release images (`build` in dev). Nothing running is touched; a pull failure leaves the previous version serving. Pass `--no-pull` to roll already-staged images without fetching.\n3. **Apply migrations.** Run the one-shot `dbMigrate` against the running database. Migrations apply one file per transaction under an advisory lock and are idempotent. On failure the upgrade aborts before touching services.\n4. **Roll services.** `up -d` recreates only containers whose image or configuration changed. Each service restarts briefly as it rolls; removed services are cleaned up.\n5. **Gate readiness.** Poll the core services' `/ready` endpoints (API, STS, Gateway, Audit, Coordinator) for up to two minutes, exiting non-zero if any fails.\n\nEvery phase is idempotent. If the process is interrupted at any point - including power loss mid-roll - run `caracal upgrade` again; staged images, applied migrations, and rolled services all converge. Each attempt appends its outcome to `$CARACAL_HOME/upgrade.log` (JSON lines with timestamp, versions, and the phase reached), which records the upgrade history for the host.\n\n## What Is Preserved\n\n| State | During upgrade |\n| --- | --- |\n| Postgres data (`postgresData` volume) | Preserved. Expand-only migrations add schema; nothing is dropped or rewritten. |\n| Console operator accounts (`caracal_auth` database) | Preserved. Its schema migrates automatically when the web service starts. |\n| Redis streams and queues (`redisData` volume) | Preserved. |\n| Replay state (`stsReplay`, `gatewayReplay` volumes) | Preserved. |\n| Secrets (`$CARACAL_HOME/secrets`) | Preserved. Existing values are never regenerated; newly required secrets are generated on first use. |\n| Operator configuration (`caracal.env`) | Preserved. Never overwritten; add configuration keys a release introduces manually, per its release notes. |\n| `compose.yml` under `$CARACAL_HOME` | **Replaced** with the release topology. Local edits are discarded - customize through `caracal.env` and environment variables instead. |\n\nNew volumes a release introduces are created empty on the first roll.\n\n## Pre-Upgrade\n\n1. Read release notes and changed chart values.\n2. Back up Postgres and Redis (`infra/scripts/backup.sh`) **and the secrets directory**. Data backups deliberately exclude secrets, and sealed state - zone signing keys, grants, provider credentials - is unrecoverable without the zone KEK.\n3. Confirm secrets and required env vars still match service config.\n4. Confirm Redis streams/groups and audit health.\n5. Prepare canary token exchange and Gateway request.\n\n## Helm Upgrade\n\n```bash\nhelm -n caracal diff upgrade caracal infra/helm/caracal -f values.production.yaml\nhelm -n caracal upgrade caracal infra/helm/caracal -f values.production.yaml\nkubectl -n caracal get jobs\nkubectl -n caracal rollout status deploy/caracal-api\n```\n\nThe migration Job runs as a pre-install/pre-upgrade hook when migrations are enabled.\n\n## Compose Upgrade (Manual)\n\n`caracal upgrade` automates this. To run it by hand:\n\n```bash\nexport CARACAL_VERSION=<new-version>\ndocker compose -f infra/docker/runtime-compose.yml pull\ndocker compose -f infra/docker/runtime-compose.yml run --rm dbMigrate\ndocker compose -f infra/docker/runtime-compose.yml up -d\nbash infra/scripts/smokeTest.sh\n```\n\nOn Windows PowerShell, set the version with `$env:CARACAL_VERSION = \"<new-version>\"` before the `docker compose` commands, and run the smoke test from Git Bash or WSL.\n\n## Post-Upgrade Validation\n\n| Check | Expected |\n| --- | --- |\n| Readiness endpoints | Web, API, STS, Gateway, Audit, Coordinator pass. |\n| Migrations | `schema_migrations` includes the expected versions. |\n| Redis | Streams and consumer groups exist; pending entries are bounded. |\n| Audit | DLQ empty or understood; replay backlog drains. |\n| Token exchange | STS exchange succeeds for canary application/resource. |\n| Gateway | Canary protected request reaches upstream and produces audit evidence. |\n\n## Rollback\n\nPrefer roll-forward fixes after migrations. Applied migrations are never rolled back automatically. An older binary refuses to touch a home directory whose stack was installed by a newer release (`$CARACAL_HOME/runtime.json` records the "},{"id":"/operations/compliance-audit-integration","title":"Export Audit Evidence","url":"https://docs.caracal.run/operations/compliance-audit-integration/","concepts":"","body":"Caracal audit records provide decision evidence for STS, Gateway, Coordinator, policy evaluation, Control, and administrative activity. Treat audit as a protected evidence pipeline.\n\n## Evidence Path\n\nAudit events are HMAC-protected in published modes. Audit storage includes tamper checks and append-only permission validation.\n\n## Integration Points\n\n| Need                 | Surface                                                  |\n| -------------------- | -------------------------------------------------------- |\n| Search recent events | Console `audit` or Audit API search.                     |\n| Trace one request    | Console `request trace` or API request ID lookup.        |\n| SIEM export          | `caracal.audit.events` consumer or Audit export path.    |\n| Evidence archive     | Postgres backup plus object-store export.                |\n| Tamper response      | `CaracalAuditTamperDetected` alert and incident runbook. |\n\n## Operational Controls\n\n- Monitor Audit DLQ, consumer lag, tamper metrics, and export watermarks.\n- Preserve audit HMAC keys according to retention requirements.\n- Keep audit partitions and backup retention aligned with legal hold requirements.\n- Size writable `AUDIT_EXPORT_TMP_DIR` scratch space for the largest complete hourly Parquet partition; Helm uses `services.audit.exportScratchSizeLimit`.\n- Do not manually mutate `audit_events`; verification expects append-only behavior.\n\n## Troubleshooting\n\n| Symptom                     | Check                                                                                       |\n| --------------------------- | ------------------------------------------------------------------------------------------- |\n| SIEM is missing events      | Redis group lag, export watermark, Audit readiness, and network egress.                     |\n| DLQ has HMAC failures       | Producer key mismatch or corrupted stream payload.                                          |\n| Request explanation missing | Confirm request ID, zone, retention window, and that the protected request reached Caracal. |\n\n## Next Step\n\nUse [Hand Off to Platform Teams](/operations/platform-team-handoff/) when deployment, recovery, audit, alerting, and ownership evidence are ready."},{"id":"/operations/platform-team-handoff","title":"Hand Off to Platform Teams","url":"https://docs.caracal.run/operations/platform-team-handoff/","concepts":"","body":"Use this checklist before a platform team accepts production ownership of a Caracal environment.\n\n## Handoff Checklist\n\n| Area          | Evidence                                                                             |\n| ------------- | ------------------------------------------------------------------------------------ |\n| Ownership     | Service owner, on-call rotation, escalation path, security owner.                    |\n| Versioning    | Image tags, chart values, release notes, rollback revision.                          |\n| Secrets       | Secret inventory, storage location, rotation owner, last rotation date.              |\n| Storage       | Postgres backup/restore proof, Redis stream verification, retention policy.          |\n| Network       | Ingress, NetworkPolicy, DNS, TLS, upstream egress, provider endpoints.               |\n| Observability | Dashboards, ServiceMonitor, PrometheusRule or equivalent alerts, log access.         |\n| Runbooks      | Deployment, upgrade, incident, backup, restore, policy deployment, failure recovery. |\n| Access        | Console/Admin access, Control API credentials, audit access, break-glass policy.     |\n| Compliance    | Audit export path, evidence retention, tamper-response plan.                         |\n\n## Day-2 Operations\n\n| Cadence     | Action                                                                                |\n| ----------- | ------------------------------------------------------------------------------------- |\n| Daily       | Check readiness, critical alerts, audit DLQ, outbox health, and revocation freshness. |\n| Weekly      | Review capacity, backups, policy changes, and pending stream entries.                 |\n| Monthly     | Rotate or review secrets, restore test, audit export review, dependency updates.      |\n| Per release | Render diff, migration review, rollout gates, rollback compatibility check.           |\n\n## Escalation Package\n\nWhen escalating, include environment, version, recent changes, readiness output, alert names, request IDs, audit explanations, Redis stream status, migration status, and whether any access-safety guarantee is degraded."},{"id":"/architecture","title":"Understand Architecture","url":"https://docs.caracal.run/architecture/","concepts":"","body":"Caracal is a pre-execution authority system for AI agents. It separates control-plane management, token exchange, protected-resource routing, audit evidence, and agent/delegation coordination into independent services backed by Postgres and Redis Streams.\n\n## System Overview\n\n## Core Design Choices\n\n| Choice                          | Effect                                                                                                                                             |\n| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Postgres as source of truth     | Product state, policy versions, grants, Authority records, Sessions, audit rows, Delegations, and outboxes are durable.                            |\n| Redis Streams for propagation   | Audit, invalidation, revocation, key, agent, invocation, and delegation events move asynchronously.                                                |\n| STS for mandate issuance        | Every protected access path receives a scoped, short-lived mandate.                                                                                |\n| Gateway for protected upstreams | Gateway verifies inbound authority, exchanges with STS, blocks unsafe routing, and emits audit evidence.                                           |\n| Coordinator for Session authority | Sessions, service leases, Delegations, and invocation lifecycle stay explicit.                                                                   |\n| Console/API boundary            | Human management uses Console; broad automation uses the Admin API; zone-scoped dispatch uses the Control API; runtime CLI remains lifecycle-only. |\n\n## System Flows\n\n| Need                                         | Page                                                  |\n| -------------------------------------------- | ----------------------------------------------------- |\n| Identify services, clients, and dependencies | [Map the System](/architecture/system-topology/)      |\n| Understand STS mandate issuance              | [Exchange Tokens](/architecture/token-exchange-flow/) |\n| Understand Sessions and delegation           | [Coordinate Sessions](/architecture/delegation-flow/) |\n\n## State and Safety\n\n| Need                                          | Page                                                  |\n| --------------------------------------------- | ----------------------------------------------------- |\n| Understand Redis Streams and outboxes         | [Propagate Events](/architecture/event-streams/)      |\n| Understand durable data ownership             | [Store State](/architecture/storage-model/)           |\n| Understand signing, HMACs, JWKS, and rotation | [Manage Keys](/architecture/crypto-keys/)             |\n| Understand security and command boundaries    | [Enforce Boundaries](/architecture/trust-boundaries/) |\n\n## Next Step\n\nStart with [Map the System](/architecture/system-topology/) before tracing request and state flows.\n\nFor service-by-service detail, continue to [Understand Services](/services/). For deployment detail, use [Operations](/operations/)."},{"id":"/architecture/system-topology","title":"Map the System","url":"https://docs.caracal.run/architecture/system-topology/","concepts":"","body":"Caracal has six primary HTTP services plus Postgres and Redis.\n\n## Topology\n\n## Service Responsibilities\n\n| Service     | Responsibility                                                                                                                      |\n| ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |\n| API         | Zones, applications, providers, resources, policies, policy sets, grants, step-up challenges, admin audit, and API outbox.          |\n| STS         | OAuth token exchange, mandate issuance, policy evaluation, JWKS, step-up status, policy simulation, signing-key rotation internals. |\n| Gateway     | Protected reverse proxy, inbound mandate verification, per-request STS exchange, SSRF guard, revocation checks, audit replay.       |\n| Audit       | Redis audit ingestion, DLQ, tamper checks, retention, search, metrics.                                                              |\n| Coordinator | Sessions, long-lived Session services, Delegations, invocations, sweeper jobs, Coordinator outbox.                                  |\n| Control     | Optional remote management invoke endpoint gated by token auth, replay protection, rate limiting, and runtime gate file.            |\n\n## Deployment Shapes\n\n| Shape               | Source                                                  |\n| ------------------- | ------------------------------------------------------- |\n| Local development   | `infra/docker/docker-compose.yml` through `caracal up`. |\n| Self-hosted Compose | `infra/docker/runtime-compose.yml`.                     |\n| Kubernetes          | `infra/helm/caracal`.                                   |\n\n## Next Step\n\nUse [Exchange Tokens](/architecture/token-exchange-flow/) to follow how workloads receive scoped mandates.\n\n## Related Pages\n\n- [Understand Services](/services/)\n- [Deploy with Docker Compose](/operations/docker-compose/)\n- [Deploy with Helm](/operations/kubernetes-helm/)"},{"id":"/architecture/token-exchange-flow","title":"Exchange Tokens","url":"https://docs.caracal.run/architecture/token-exchange-flow/","concepts":"","body":"STS implements the token exchange boundary. Workloads, SDKs, `caracal run`, and Gateway submit existing authority plus requested resources and receive scoped mandates.\n\n## Exchange Sequence\n\n## Inputs\n\n| Input                                                  | Purpose                                                                   |\n| ------------------------------------------------------ | ------------------------------------------------------------------------- |\n| `grant_type`                                           | OAuth token-exchange request type.                                        |\n| `subject_token`                                        | An existing Caracal session mandate whose authority the exchange narrows. |\n| `resource`                                             | Target resource identifiers.                                              |\n| `scope`                                                | Requested resource scopes.                                                |\n| `zone_id`                                              | Tenant boundary.                                                          |\n| `application_id` and client credential                 | Authenticates the calling application.                                    |\n| `session_id`, `agent_session_id`, `delegation_edge_id` | Protocol names for Authority record ID, Session ID, and Delegation ID.    |\n| `challenge_id`                                         | Consumes an approved step-up hold during retry.                           |\n\nRFC 8693 `actor_token` is not supported; the STS rejects requests that carry one. The calling application's authenticated identity is published to policy input as `input.context.actor_claims.caracal_client_id`.\n\n## TTL Contracts\n\n| Mandate type                | Contract                                                                   |\n| --------------------------- | -------------------------------------------------------------------------- |\n| Resource mandate            | Capped at 15 minutes.                                                      |\n| Session mandate             | Capped at 60 minutes.                                                      |\n| Runtime-injected credential | `caracal run` injects credentials capped at 15 minutes after STS exchange. |\n\nGateway performs a per-request exchange and rejects inbound tokens that are too close to expiry before proxying.\n\n## Gateway-Authenticated Exchange\n\nGateway exchanges with STS using a request signature, timestamp, and nonce over the token exchange request. STS verifies the Gateway HMAC key and consumes the nonce before trusting the Gateway-authenticated path.\n\n## Next Step\n\nUse [Coordinate Sessions](/architecture/delegation-flow/) to see how Sessions and Delegations feed token exchange.\n\n## Related Pages\n\n- [Issue Mandates](/services/sts/)\n- [Mandates](/concepts/mandate/)\n- [Step-Up Challenges](/concepts/step-up/)\n- [Use STS Endpoint](/api/sts/)"},{"id":"/architecture/delegation-flow","title":"Coordinate Sessions","url":"https://docs.caracal.run/architecture/delegation-flow/","concepts":"","body":"Coordinator owns governed execution state. It records Sessions, long-lived Session leases, invocation lifecycle, and Delegations that STS later uses to validate delegated exchanges.\n\n## Flow\n\n## Coordinator State\n\n| State                     | Purpose                                                                                                           |\n| ------------------------- | ----------------------------------------------------------------------------------------------------------------- |\n| `sessions`                | Session tree, parent-child relationships, status, leases, and Subject authority record ID.                        |\n| `agent_services`          | Retained internal table for long-lived Session heartbeat health.                                                  |\n| `delegation_edges`        | Retained internal table for Delegation endpoints, resource and scope bounds, expiry, constraints, and status.     |\n| `agent_invocations`       | Retained internal table for invocation lifecycle and deadline tracking.                                           |\n| `delegation_graph_epochs` | Graph invalidation and traversal consistency.                                                                     |\n| `caracal_outbox`          | Durable event publication for Coordinator-produced topics.                                                        |\n\n## Event Topics\n\n| Topic                            | Meaning                                                |\n| -------------------------------- | ------------------------------------------------------ |\n| `caracal.agents.lifecycle`       | Protocol topic for Session lifecycle.                  |\n| `caracal.invocations.lifecycle`  | Invocation lifecycle.                                  |\n| `caracal.delegations.invalidate` | Delegation graph invalidation.                         |\n| `caracal.sessions.revoke`        | Authority record, Session, and Delegation revocation propagation. |\n\n## Operator Boundaries\n\nHuman operators use the Console **Sessions** and **Delegation** views. Automation uses the Coordinator API or Admin SDK. Top-level `caracal` runtime commands do not manage Sessions or Delegations.\n\n## Next Step\n\nUse [Propagate Events](/architecture/event-streams/) to understand how lifecycle, invalidation, revocation, and audit messages move.\n\n## Related Pages\n\n- [Coordinate Session State](/services/coordinator/)\n- [Session Delegation](/concepts/delegation/)\n- [Manage Sessions and Delegation](/runtime-console/agents/)"},{"id":"/architecture/event-streams","title":"Propagate Events","url":"https://docs.caracal.run/architecture/event-streams/","concepts":"","body":"Caracal uses Postgres outboxes for durable enqueue and Redis Streams for asynchronous delivery. In rc and stable builds, producers sign stream messages with `STREAMS_HMAC_KEY` and consumers verify the signature before processing.\n\n## Event Pipeline\n\n## Streams\n\n[Review Event Topics](/api/event-topics/) is the canonical list of stream topics, producers, and consumer groups. This page covers how events move: outbox enqueue, signed delivery, consumer groups, and replay.\n\n## Outbox Behavior\n\n| Outbox | Owner | Behavior |\n| --- | --- | --- |\n| `event_outbox` | API | Durable enqueue inside API transactions, cooperative dispatcher, signed Redis `XADD`, retry/backoff, dead-row metrics. |\n| `caracal_outbox` | Coordinator | Dedupe by producer/topic/dedupe key and publishes Coordinator topics. |\n\n## Audit Replay\n\nSTS and Gateway use replay directories under `/var/lib/caracal/audit-replay`. When Redis or Audit is unavailable, replay files preserve pending audit events so they can drain after recovery.\n\n## Next Step\n\nUse [Store State](/architecture/storage-model/) to understand which data is durable, transient, or recoverable.\n\n## Related Pages\n\n- [Operate Redis Streams](/operations/redis/)\n- [Ingest Audit Evidence](/services/audit/)\n- [Use Event Topics](/api/event-topics/)"},{"id":"/architecture/storage-model","title":"Store State","url":"https://docs.caracal.run/architecture/storage-model/","concepts":"","body":"Caracal separates durable state from propagation state.\n\n| Store                | Role                                                                    |\n| -------------------- | ----------------------------------------------------------------------- |\n| Postgres             | Durable product, authority, audit, coordination, and outbox state.      |\n| Redis Streams        | Event propagation, invalidation, revocation, and consumer coordination. |\n| Replay volumes       | Temporary audit emission buffer for STS and Gateway.                    |\n| Secret manager/files | Runtime credentials and HMAC/encryption material.                       |\n\n## Postgres Ownership\n\n| Domain           | Tables                                                                                                                                                                                                         |\n| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Product model    | `zones`, `providers`, `applications`, `resources`, `workloads`                                                                                                                                                 |\n| Authority        | `authority_records`, `delegated_grants`, `provider_connections`, `step_up_challenges`, `secrets`, `secret_store`                                                                                               |\n| Policy           | `policies`, `policy_versions`, `policy_sets`, `policy_set_versions`, `policy_set_bindings`                                                                                                                     |\n| Audit            | `audit_events`, partitions, `audit_chain_rehash`, `audit_retention`, `audit_export_watermark`, `audit_ingest_alerts`, `admin_audit_events`                                                                     |\n| Sessions         | `sessions`, `agent_topology`, `agent_services`, `agent_invocations`, `delegation_edges`, `delegation_graph_epochs`                                                                                              |\n| Caracal Operator | `operator_conversations`, `operator_turns`, `operator_message_runs`, `operator_message_run_events`, `operator_plan_secrets`, `operator_zone_memory`, `operator_conversation_counters`, `operator_ai_providers` |\n| Operations       | `event_outbox`, `caracal_outbox`, `admin_tokens`                                                                                                                                                               |\n\n`agent_topology`, `agent_services`, `agent_invocations`, `delegation_edges`, and `delegation_graph_epochs` are retained internal schema names. Public APIs expose Sessions, Delegations, Session IDs, and Delegation IDs.\n\n## Integrity Controls\n\n- Migrations are forward-only in production tooling.\n- `schema_migrations` records applied versions.\n- Audit role cannot update or delete `audit_events`.\n- Policy versions are immutable.\n- Zone-scoped tables use fail-closed row-level security.\n- Audit partitions are maintained for the rolling window.\n\n## Redis Ownership\n\nRedis keeps delivery state, pending entries, consumer groups, rate-limit coordination, and revocation/invalidation streams. Redis does not replace Postgres for durable product state.\n\n## Restore Priority\n\n1. Postgres and runtime secrets.\n2. STS/Gateway replay volumes.\n3. Redis streams/pending entries when available.\n4. Audit exports and SIEM archive.\n\n## Next Step\n\nUse [Manage Keys](/architecture/crypto-keys/) to understand how signing, encryption, and HMAC keys protect stored and propagated state.\n\n## Related Pages\n\n- [Operate PostgreSQL](/operations/postgres/)\n- [Operate Redis Streams](/operations/redis/)\n- [Back Up and Retain Data](/operations/backup-retention/)"},{"id":"/architecture/crypto-keys","title":"Manage Keys","url":"https://docs.caracal.run/architecture/crypto-keys/","concepts":"","body":"Caracal uses separate keys for encryption, mandate signing, audit integrity, stream integrity, and service-to-service exchange.\n\n## Key Map\n\n| Material                     | Used by                                         | Purpose                                                                                                                       |\n| ---------------------------- | ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |\n| Zone signing keys            | STS                                             | Sign mandate JWTs and publish public keys through JWKS.                                                                       |\n| `SECRET_STORE_KEK`           | API and STS                                     | Root key that seals Secret Store envelopes: provider credentials, zone signing material, connection tokens, and sink secrets. |\n| `AUDIT_HMAC_KEY`             | API, STS, Gateway, Audit, Control               | Protect audit event integrity.                                                                                                |\n| `STREAMS_HMAC_KEY`           | API, STS, Gateway, Coordinator, Audit/consumers | Sign Redis stream messages in published modes.                                                                                |\n| `GATEWAY_STS_HMAC_KEY`       | Gateway and STS                                 | Authenticate Gateway token-exchange requests to STS.                                                                          |\n| Admin and Coordinator tokens | API, Coordinator, Console, Control              | Authenticate management and Session/Delegation operations.                                                                    |\n\n## Secret Envelopes\n\nEvery stored secret is sealed into a self-describing envelope: a fresh data-encryption key encrypts the value with ChaCha20-Poly1305, and `SECRET_STORE_KEK` wraps that data key. Envelopes bind a key fingerprint and a purpose-specific associated-data string, so a value decrypts only under the correct key and in the context it was written for. The embedded fingerprint also routes each read to the right key while `SECRET_STORE_KEK_PREVIOUS` carries a retiring key through a rotation window. The KEK lives in the service environment, never beside the ciphertext, and startup rejects weak or short keys. [Configure Secret Backends](/operations/secret-backends/) covers where envelopes and user-entered credentials are stored and how the master key rotates.\n\n## Mandate Verification\n\nSTS JWKS responses are cacheable; rotation plans must preserve overlap until verifier caches expire.\n\n## Gateway Exchange Signature\n\nGateway signs STS exchange requests with timestamp, request ID, method, path, and form body. STS checks the signature, skew, request ID, and nonce before accepting the Gateway-authenticated path.\n\n## Published-Mode Requirements\n\nIn `rc` and `stable`, HMAC keys used by services must be present and at least 32 bytes where validated. Missing keys cause startup or readiness failure rather than silent downgrade.\n\n## Next Step\n\nUse [Enforce Boundaries](/architecture/trust-boundaries/) to connect keys, services, commands, and data stores to trust boundaries.\n\n## Related Pages\n\n- [Rotate Keys and Secrets](/operations/key-management/)\n- [Configure Secret Backends](/operations/secret-backends/)\n- [Identity Package](/sdks/identity/)\n- [Enforce Boundaries](/architecture/trust-boundaries/)"},{"id":"/architecture/trust-boundaries","title":"Enforce Boundaries","url":"https://docs.caracal.run/architecture/trust-boundaries/","concepts":"","body":"Caracal’s trust model depends on clear boundaries. Do not blur runtime lifecycle, product management, token issuance, protected-resource routing, and audit evidence.\n\n## Boundary Map\n\n| Boundary              | Trusted side                                                         | Untrusted or constrained side                                              |\n| --------------------- | -------------------------------------------------------------------- | -------------------------------------------------------------------------- |\n| Runtime CLI           | Local lifecycle and `caracal run` config                             | Product-management state and admin actions.                                |\n| Console and Admin API | Authenticated operators and Admin SDK clients                        | Anonymous clients and expired or insufficient tokens.                      |\n| STS                   | Valid application credentials, policy/grant/session state            | Malformed subject tokens, invalid client secrets, undecided step-up holds. |\n| Gateway               | Verified inbound mandate, configured binding, revocation-fresh state | Arbitrary upstream URLs, path traversal, replayed/expiring tokens.         |\n| Audit                 | HMAC-verified stream events and append-only database role            | Tampered stream payloads or mutable evidence.                              |\n| Redis                 | Signed operational messages                                          | Unsigned or mismatched stream messages in published modes.                 |\n| Postgres              | Service roles and fail-closed RLS                                    | Cross-zone reads without zone context.                                     |\n| Control               | Enabled gate, verified JWT, replay-safe JTI, rate limit              | Disabled gate, replayed tokens, unsupported commands.                      |\n\n## Gateway Fail-Closed Checks\n\nGateway denies before upstream dispatch when:\n\n- bearer token is missing, malformed, too large, expiring, revoked, replayed, or signature-invalid;\n- `X-Caracal-Resource` is missing;\n- no binding exists for the token zone and resource;\n- path traversal is detected;\n- STS exchange fails or the STS circuit is open;\n- upstream host safety rules reject the destination.\n\n## Control Boundary\n\nControl is optional. It requires a gate file, JWKS-backed JWT verification, replay protection, rate limits, and an API token for dispatch. It must not be exposed as a top-level runtime CLI command.\n\n## Related Pages\n\n- [Harden Production](/operations/tls-hardening/)\n- [Automate Management](/services/control/)\n- [Protect Upstreams](/services/gateway/)"},{"id":"/runtime-console","title":"Operate Runtime and the Web Console","url":"https://docs.caracal.run/runtime-console/","concepts":"","body":"Use this section after installation when you need to operate Caracal locally or understand which operator surface owns a task.\n\nCaracal has three operator surfaces:\n\n| Surface                 | Use it for                                                                                                                                                                                                                                                                                                         | Do not use it for                                                                                                              |\n| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ |\n| `caracal`               | Local stack lifecycle, readiness checks, local reset, token-injected process execution, and the local web development launcher.                                                                                                                                                                                    | Creating or changing zones, applications, providers, resources, policies, audit queries, agents, delegation, or Control setup. |\n| Web console             | Human product management for zones, applications, providers, resources, policies, policy sets, subjects, Control, audit, request tracing, sessions, delegation, and diagnostics. In Compose it is served by the packaged `web` service at `http://localhost:3001`; `caracal web` remains the development launcher. | Starting or stopping the stack, purging local runtime state, running workloads, or CI automation.                              |\n| Control API / Admin SDK | Scripted product management from trusted operator automation.                                                                                                                                                                                                                                                      | Local lifecycle, workload execution, or browser-based guided setup.                                                            |\n\nThe runtime CLI stays small so lifecycle and workload execution do not depend on admin tokens, zone selection, or Control credentials. Human product management belongs to the web console; automation belongs to the Control API or Admin SDK.\n\n## Recommended Path\n\n1. [Choose the Right Surface](/runtime-console/cli-and-console/) to confirm whether a task belongs in `caracal`, the web console, Control API, or Admin SDK.\n2. [Start and Check the Stack](/runtime-console/stack/) with `caracal up` and `caracal status --ready`.\n3. [Use the Web Console](/runtime-console/console/) to run guided setup and bind the first application's launch credentials.\n4. [Configure Workloads](/runtime-console/config-file/) when a workload needs explicit profile, credential, or service endpoint settings.\n5. [Run Workloads](/runtime-console/runtime/) with `caracal run -- <command>`.\n6. [Manage Product Objects](/runtime-console/admin/) for ongoing zone, application, provider, resource, policy, policy-set, and Control work.\n7. [Inspect Diagnostics and Audit](/runtime-console/observability/) when debugging health, readiness, audit, or request decisions.\n8. [Manage Sessions and Delegation](/runtime-console/agents/) when workloads create sessions or delegations.\n\n## Command Map\n\n| Outcome                                                                  | Surface                                    |\n| ------------------------------------------------------------------------ | ------------------------------------------ |\n| Start, stop, check, upgrade, or purge the local runtime stack            | `caracal`                                  |\n| Open the packaged local web console                                      | `http://localhost:3001` after `caracal up` |\n| Launch the local web development server                                  | `caracal web`                              |\n| Run an agent process with scoped resource tokens                         | `caracal run -- <command>`                 |\n| Create a zone, app, provider, resource, policy, or policy set            | Web console                                |\n| Enable Control automation                                                | Web console **Control** section            |\n| Script management workflows                                              | Control API or Admin SDK                   |\n| Inspect audit events, request traces, diagnostics, agents, or delegation | Web console                                |\n\n## Next Step\n\nStart with [Choose the Right Surface](/runtime-console/cli-and-console/) if you are deciding where an operation belongs. If you already know you need a local stack, go directly to [Start and Check the Stack](/runtime-console/stack/)."},{"id":"/runtime-console/cli-and-console","title":"Choose the Right Surface","url":"https://docs.caracal.run/runtime-console/cli-and-console/","concepts":"","body":"Use this page before reaching for a command. Caracal separates local runtime lifecycle from product management so workload execution does not depend on admin tokens or selected zones.\n\n## Ownership Rule\n\n```text\nlocal lifecycle and process execution -> caracal\nhuman product management              -> web console\nscripted product management           -> Control API or Admin SDK\n```\n\nThe top-level runtime CLI intentionally contains only `up`, `down`, `status`, `upgrade`, `purge`, `allowlist`, `run`, and `web`.\n\n:::note[FAQ]\n[Why are zone and policy commands in the web console instead of the `caracal` CLI?](/reference/faq/#faq-015)\n:::\n\n## Runtime CLI Tasks\n\n| Outcome                                          | Command                                             |\n| ------------------------------------------------ | --------------------------------------------------- |\n| Start the local stack                            | `caracal up`                                        |\n| Stop the local stack                             | `caracal down`                                      |\n| Stop and remove volumes                          | `caracal down -v`                                   |\n| Probe service health                             | `caracal status`                                    |\n| Probe dependency readiness                       | `caracal status --ready`                            |\n| Emit structured status                           | `caracal status --json`                             |\n| Upgrade the local stack to the installed release | `caracal upgrade`                                   |\n| Remove local stack/runtime state                 | `caracal purge`                                     |\n| Manage which emails may sign in on the console   | `caracal allowlist add\\|remove\\|lock\\|unlock\\|list` |\n| Run a process with resource tokens               | `caracal run -- <command>`                          |\n| Open the packaged local web console              | `http://localhost:3001` after `caracal up`          |\n| Launch the local web development server          | `caracal web`                                       |\n\nRuntime CLI commands must not require admin tokens, selected zones, product credentials, or Control credentials.\n\n## Web Console Tasks\n\n| Outcome                                           | Web console section |\n| ------------------------------------------------- | ------------------- |\n| Create or select zones                            | **Zones**           |\n| Register confidential agent applications          | **Applications**    |\n| Configure provider credential sources             | **Providers**       |\n| Register protected resources and scopes           | **Resources**       |\n| Author access policies                            | **Policies**        |\n| Simulate and activate policy sets                 | **Policy Sets**     |\n| Inspect Subjects and their Authority records      | **Subjects**        |\n| Enable and manage Control API access              | **Control**         |\n| Search audit records and trace selected requests  | **Audit**           |\n| Trace one known request ID                        | **Audit**           |\n| Inspect or manage Sessions                        | **Sessions**        |\n| Inspect or revoke Delegations                     | **Delegation**      |\n| Run health, readiness, zone, and preflight checks | **Diagnostics**     |\n\nProduct-management workflows are not mirrored as top-level runtime CLI commands. Keeping them in the web console prevents command drift and keeps human operations tied to the same engine/admin helpers.\n\n## Automation Tasks\n\nUse the Admin SDK from trusted operator environments when scripts need to create zones. Use the Control API for zone-bound automation over applications, resources, policies, Authority records, Sessions, Delegations, audit queries, and Control credentials. Enable Control from the web console's **Control** section, then call the authenticated HTTP surface from CI or automation.\n\nAutomation must run with operator credentials only in trusted control-plane environments. Agent workloads should use `caracal run` or application credentials and must not receive `CARACAL_ADMIN_TOKEN`, `CARACAL_COORDINATOR_TOKEN`, `CARACAL_SECRETS_DIR`, or mounted operator secret files.\n\n## Troubleshooting\n\n| Symptom                                                           | Check                                                                                                                                                      |\n| ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| The web console cannot reach product data                         | Run `caracal up` and confirm `caracal status --ready`; for the development launcher, use `caracal web --allow-offline` only for sign-in or UI-only checks. |\n| A command for zones, policies, or audit is missing from `caracal` | Open the web console. Those workflows are intentionally web-console-owned.                                                                                 |\n| Product management is needed in CI                                | Use the Control API or Admin SDK with scoped operator credentials.                                                                                         |\n| Runtime command asks for product-management context               | Treat it as a bug; runtime lifecycle must not depend on admin or zone state.                                                                               |\n\n## Next Step\n\nUse [Start and Check the Stack](/runtime-console/stack/) when you need local services running."},{"id":"/runtime-console/stack","title":"Start and Check the Stack","url":"https://docs.caracal.run/runtime-console/stack/","concepts":"","body":"Runtime lifecycle commands manage local infrastructure only. They delegate to Docker Compose and keep product state changes in the web console.\n\n## Service Layout\n\n| Service     | Default local port |\n| ----------- | ------------------ |\n| API         | `3000`             |\n| STS         | `8080`             |\n| Gateway     | `8081`             |\n| Audit       | `9090`             |\n| Coordinator | `4000`             |\n\nControl runtime exposure is managed through the web console **Control** section, not by targeting a stack service directly from the runtime CLI.\n\n## Start\n\n```bash\ncaracal up\n```\n\nDevelopment mode rebuilds local images. Release modes use installed compose assets and the selected Caracal version.\n\n## Stop\n\n```bash\ncaracal down\ncaracal down -v\n```\n\nUse `-v` only when you intend to remove Docker volumes.\n\n## Check Health and Readiness\n\n```bash\ncaracal status\ncaracal status --ready\ncaracal status --ready --json\n```\n\n`status` probes service health endpoints. `status --ready` probes readiness endpoints and dependencies. The command exits `0` only when the selected probes pass.\n\n## Purge Local State\n\n```bash\ncaracal purge\n```\n\n`purge` can remove stack artifacts, volumes, logs, config, runtime state, secrets, images, cached assets, or all local runtime state depending on the selected purge target. Use it when you intentionally want a clean local environment.\n\nAfter a full purge, recreate product state through the web console: zones, applications, providers, resources, policies, and Control keys.\n\n## Troubleshooting\n\n| Symptom                                       | Likely cause                                                                                       |\n| --------------------------------------------- | -------------------------------------------------------------------------------------------------- |\n| `status` passes but `status --ready` fails    | A service is alive but one of its dependencies or readiness checks is not ready.                   |\n| Web console cannot connect after `up`         | Check `caracal status --ready`; custom deployments should confirm the Admin API URL override.      |\n| Workloads cannot exchange tokens              | Readiness for STS, Gateway, and audit must pass before debugging workload config.                  |\n| Local tokens are stale after `purge` and `up` | Prefer local generated secret files over exported shell tokens for localhost web-console sessions. |\n\n## Next Step\n\nContinue to [Use the Web Console](/runtime-console/console/) to create the first zone, application, provider, resource, policy, and runtime profile."},{"id":"/runtime-console/console","title":"Use the Web Console","url":"https://docs.caracal.run/runtime-console/console/","concepts":"","body":"The web console is the supported browser UI for setting up and operating protected agent access. The packaged `web` service serves the built SPA and a session-guarded backend-for-frontend from the same origin, then proxies console API calls to the internal API and Coordinator. For local Compose, open [http://localhost:3001](http://localhost:3001) after `caracal up`; use `caracal web` when you need the development launcher.\n\n## Local Access\n\nThe web console expects the local stack to be running:\n\n```bash\ncaracal up\ncaracal status --ready\n```\n\nOpen [http://localhost:3001](http://localhost:3001). Compose maps `127.0.0.1:3001` to the BFF container on port `3002`.\n\nUse the development launcher only when working on the console or auth BFF locally:\n\n```bash\ncaracal web\n```\n\nThe launcher serves on the same port as the packaged console ([http://localhost:3001](http://localhost:3001)), with the backend-for-frontend on `3002`. While it runs it stops the packaged web container so the two never contend for the port. Stopping the launcher (Ctrl+C) tears down both the UI and the backend; the packaged web container stays stopped, so run `caracal up` to bring the packaged console back.\n\nUse `--allow-offline` with the development launcher when you only need sign-in or UI-only checks and the stack is not ready:\n\n```bash\ncaracal web --allow-offline\n```\n\n## Development Launcher Options\n\n| Option               | Purpose                                                            |\n| -------------------- | ------------------------------------------------------------------ |\n| `--web-port <port>`  | Bind the browser UI to a specific port (default `3001`).           |\n| `--auth-port <port>` | Bind the session/auth backend to a specific port (default `3002`). |\n| `--build`            | Build the web assets before launching.                             |\n| `--allow-offline`    | Start the UI even when local Caracal services are not ready.       |\n\n## Credentials and Endpoints\n\n| Input                                                           | Purpose                                                               |\n| --------------------------------------------------------------- | --------------------------------------------------------------------- |\n| `CARACAL_API_URL`                                               | Cloud/custom admin API URL override.                                  |\n| `CARACAL_ADMIN_TOKEN` or `CARACAL_ADMIN_TOKEN_FILE`             | Admin bearer token for management views.                              |\n| `CARACAL_COORDINATOR_URL`                                       | Cloud/custom Coordinator URL override for agent and delegation views. |\n| `CARACAL_COORDINATOR_TOKEN` or `CARACAL_COORDINATOR_TOKEN_FILE` | Coordinator token for agent and delegation views.                     |\n\nFor BFF deployment variables such as `CARACAL_AUTH_URL`, `CARACAL_AUTH_DATABASE_URL`, `CARACAL_AUTH_SECRET`, `CARACAL_WEB_ROOT`, and `CARACAL_AUTH_AUTO_MIGRATE`, use [Configure Service Environment](/operations/env-vars/#web-console-bff).\n\nFor default local services, generated secret files from `caracal up` are preferred over exported token values so local reset cycles do not keep stale shell credentials. Those files live outside the source workspace by default and must not be mounted into agent workspaces.\n\n## Browser Model\n\nOpen the packaged or development URL in a browser, sign in to the session-guarded UI, and navigate by sections instead of terminal keys.\n\n| Section              | What it manages                                                                                                                                    |\n| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |\n| **Guided setup**     | First zone, application, provider, resource, and access policy.                                                                                    |\n| **Caracal Operator** | Operate the control plane from natural-language intent: describe a change, review the resolved plan, and apply it within your operator scope.      |\n| **Zones**            | Zone records and active zone selection.                                                                                                            |\n| **Applications**     | Confidential applications and their client secrets.                                                                                                |\n| **Providers**        | Gateway upstream auth modes for Caracal mandates, OAuth 2.0 authorization code, OAuth 2.0 client credentials, API-key, and bearer-token upstreams. |\n| **Resources**        | Protected resource identifiers, scopes, upstream URLs, Gateway application bindings, and upstream credential provider bindings.                    |\n| **Policies**         | Policy content and versions.                                                                                                                       |\n| **Policy Sets**      | Policy-set composition, simulation, and activation.                                                                                                |\n| **Subjects**         | The identities applications act for - federated users and application identities - with the authority each holds.                                  |\n| **Sessions**         | Governed execution sessions, lifecycle, lease health, and session trees.                                                                           |\n| **Delegation**       | Authority delegated between sessions.                                                                                                              |\n| **Audit**            | Recent decisions, filters, event details, and built-in request tracing.                                                                            |\n| **Services**         | Catalog of platform surfaces beyond the conso"},{"id":"/runtime-console/config-file","title":"Configure Workloads","url":"https://docs.caracal.run/runtime-console/config-file/","concepts":"","body":"Workload configuration has two consumers. `caracal run` needs only a workload identity; its credential bindings live in the web console. SDK credential loaders read a local profile. Neither is Console state and neither creates, selects, or manages zones.\n\n## Workload Identity for caracal run\n\n`caracal run` authenticates as one workload, created on the console's **Launcher** page, and fetches that workload's launch bindings from STS. The environment carries only identity:\n\n| Variable                       | Meaning                                               |\n| ------------------------------ | ----------------------------------------------------- |\n| `CARACAL_WORKLOAD_ID`          | Workload identifier from the Launcher page. Required. |\n| `CARACAL_WORKLOAD_SECRET`      | Inline local-development workload secret.             |\n| `CARACAL_WORKLOAD_SECRET_FILE` | Cloud/custom path to a mounted workload-secret file.  |\n| `CARACAL_STS_URL`              | Cloud/custom STS URL override for credential minting. |\n\nSet only one of `CARACAL_WORKLOAD_SECRET` and `CARACAL_WORKLOAD_SECRET_FILE`. Local dev and stable service URLs resolve automatically; set `CARACAL_STS_URL` only for cloud deployments, containers, or custom service locations.\n\nWhen neither secret variable is set, local dev and stable launches read the owner-only secret file at:\n\n```text\n<Caracal config dir>/runtime/<workload_id>/secret\n```\n\nThe OS Caracal config directory is `$XDG_CONFIG_HOME/caracal` (or `~/.config/caracal`) on Linux, `~/Library/Application Support/Caracal` on macOS, and `%APPDATA%\\Caracal` on Windows. Set `CARACAL_CONFIG_HOME` to override it on any platform.\n\nYou create the secret file yourself. The web console holds the workload secret sealed in the Secret Store - reveal it from the Launcher page whenever you provision - but never writes to your machine:\n\n```bash\nmkdir -p ~/.config/caracal/runtime/<workload_id>\nprintf '%s' '<workload secret>' > ~/.config/caracal/runtime/<workload_id>/secret\nchmod 600 ~/.config/caracal/runtime/<workload_id>/secret\n```\n\nThe default path is read only in local dev and stable modes; production launches (`CARACAL_ENV=production`) require explicit secret configuration.\n\n## Launch Bindings\n\nWhich environment variables `caracal run` injects, which resources fill them, the scopes each credential carries, and optional-credential behavior are all part of the workload's launch bindings, authored on the **Launcher** page in the web console. STS serves the bindings to the workload after it proves possession of the workload secret, so the zone, resource identifiers, and variable names never live on the machine that launches the workload.\n\nEvery workload is scoped to its zone, and each save is attributed: the Launcher page shows which console operator configured the bindings and when, and the change appears in the zone's admin audit feed under that operator's profile.\n\nEach binding selects the scopes its credential should carry; leaving all scopes unselected requests the resource's full scope set. The Launcher page also renders the exact launch commands for the workload. See [Run Workloads](/runtime-console/runtime/) for the launch flow.\n\n## SDK Profile\n\nSDK credential loaders resolve a local profile instead of calling STS for a manifest:\n\n| Order | Source                                                                                                     |\n| ----- | ---------------------------------------------------------------------------------------------------------- |\n| 1     | `CARACAL_CONFIG`, when set and the file exists.                                                            |\n| 2     | Environment runtime config (`CARACAL_ZONE_ID`, `CARACAL_APPLICATION_ID`, secret and credential variables). |\n\nSDKs do not search the current working directory, home directory, or OS config directory. Secret and credential files are read only when an explicit profile field or `*_FILE` environment variable names them.\n\n### TOML Schema\n\n```toml\nzone_id = \"zone_prod\"\napplication_id = \"app_agent\"\ndefault_ttl_seconds = 900\n\n[[credentials]]\nresource = \"resource://openai\"\nupstream_prefix = \"https://api.openai.com/v1\"\n\n[[optional_credentials]]\nresource = \"resource://mandate-aware-api\"\n```\n\n### Field Reference\n\n| Field                    | Meaning                                                                       |\n| ------------------------ | ----------------------------------------------------------------------------- |\n| `sts_url`                | Cloud/custom STS endpoint for token exchange.                                 |\n| `gateway_url`            | Cloud/custom Gateway endpoint override for SDK transports.                    |\n| `coordinator_url`        | Cloud/custom Coordinator endpoint override for SDK agent/delegation features. |\n| `zone_id`                | Zone created in the web console.                                              |\n| `application_id`         | Confidential application used by the workload.                                |\n| `app_client_secret_file` | Cloud/custom secret-file path override.                                       |\n| `app_client_secret`      | Inline local-development secret.                                              |\n| `default_ttl_seconds`    | Default Session TTL used when a call does not provide one.                    |\n| `credentials[]`          | Resource audiences and optional Gateway routing bindings.                     |\n| `optional_credentials[]` | Additional resource audiences and optional Gateway routing bindings.          |\n\nSDK credential entries use `resource` and optional `upstream_prefix`. Launcher binding fields such as `env`, `credential_type`, `on_failure`, and `continue_on_failure` belong to `caracal run` launch bindings and are not SDK profile behavior.\n\n## Security Rules\n\n- Store the workload secret at the runtime path with owner-only permissions for dev and stable launches.\n- Use explicit secret-file paths for cloud deployments, co"},{"id":"/runtime-console/runtime","title":"Run Workloads","url":"https://docs.caracal.run/runtime-console/runtime/","concepts":"","body":"`caracal run` is the data-plane launcher. It authenticates its workload identity, mints scoped, short-lived credentials, injects them into environment variables, spawns the workload command, and returns the workload's exit code. That is its entire job.\n\n```bash\ncaracal run -- python3 agent.py\n```\n\nUse `--` when the child command accepts flags.\n\n## Scope and Boundaries\n\n`caracal run` complements the web console and the control plane; it does not duplicate them.\n\n| Concern                                                                              | Owner                                               |\n| ------------------------------------------------------------------------------------ | --------------------------------------------------- |\n| Launching a workload with injected credentials                                       | `caracal run`                                       |\n| Creating and managing zones, workloads, applications, resources, policies, providers | Web console (`caracal web`), Control API, Admin SDK |\n| Credential renewal during execution                                                  | Caracal SDKs, which re-exchange on demand           |\n| Request-level governance, routing, and audit                                         | Gateway and control-plane services                  |\n| Process supervision, restarts, scheduling                                            | Your process manager or orchestrator                |\n\nThe launcher performs one credential mint per configured binding at startup and never renews. Injected credentials are short-lived and expire on the schedule of the minted mandate or provider token. Workloads that run longer than their credentials must use a Caracal SDK instead of relying on injected environment variables.\n\n`caracal run` never requires operator or admin credentials. It authenticates only as the configured workload, and the credentials it can obtain are bounded by its bindings and the policies active in the zone.\n\n## Execution Flow\n\n```text\nworkload identity -> binding fetch -> credential mint -> env injection -> child process -> exit code\n```\n\n1. Load the workload identity: `CARACAL_WORKLOAD_ID` plus a workload secret from the environment or the default secret file.\n2. Fetch the workload's launch bindings from STS; each binding carries the environment name, resource, scopes, and failure behavior authored in the web console.\n3. Validate secret-file permissions and credential environment names.\n4. Mint a credential for each required binding under only its selected scopes. Binding scopes narrow the policy decision that authorizes the mint; for static provider credentials such as API keys and bearer tokens, the injected value itself carries the upstream's full authority regardless of the scopes selected.\n5. Mint each optional binding and apply its unavailable behavior.\n6. Spawn the child process without `shell: true`, attached to your terminal's stdin, stdout, and stderr.\n7. Wait for the child and exit with its exit code.\n\n## Required Workload Identity\n\n`caracal run` needs:\n\n| Input           | Source                                                                                                                                    |\n| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |\n| STS URL         | Automatic for local dev/stable; `CARACAL_STS_URL` only for cloud/custom deployments.                                                      |\n| Workload ID     | `CARACAL_WORKLOAD_ID`.                                                                                                                    |\n| Workload secret | `CARACAL_WORKLOAD_SECRET`, `CARACAL_WORKLOAD_SECRET_FILE`, or the owner-only file at `<Caracal config dir>/runtime/<workload_id>/secret`. |\n| Launch bindings | Authored on the console's **Launcher** page and served by STS.                                                                            |\n\nWeb console **Guided setup** is the recommended way to create the first zone, provider, resource, and policy; the **Launcher** page then creates the workload and its bindings. The workload's secret is held sealed in the Secret Store and stays retrievable from the Launcher page with every reveal audited; you store it in a local file yourself, because the console never writes files to your machine. See [Configure Workloads](/runtime-console/config-file/) for the exact paths and permissions. Local dev and stable service URLs resolve automatically; cloud/custom deployments configure explicit overrides.\n\n## Credential Lifecycle\n\n| Stage        | Behavior                                                                                                                                                                                                                                                          |\n| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Provisioning | Create the workload and its launch bindings on the console's **Launcher** page; copy its secret into an owner-only file at the runtime path.                                                                                                                      |\n| Consumption  | At launch, the workload secret authenticates the binding fetch and one credential mint per binding; results are injected as child environment variables. The binding fetch and every mint are recorded in the zone's audit ledger, correlated by a per-launch id. |\n| TTL          | Injected credentials carry the expiry of the minted mandate or provider token; when the provider reports one, it is also injected as `<ENV>_EXPIRES_AT` in epoch seconds.                              "},{"id":"/runtime-console/admin","title":"Manage Product Objects","url":"https://docs.caracal.run/runtime-console/admin/","concepts":"","body":"The web console is the supported human interface for product management. Launch it with:\n\n```bash\ncaracal web\n```\n\n## Management Flow\n\n```text\nzone -> application -> provider -> resource -> policy -> policy set -> Subject/Session/audit\n```\n\nWeb console **Guided setup** walks the first version of that flow, opening the real create form at each step. Bind launch credentials on the **Run** page; the workload then needs only its application ID and client secret.\n\n## Web Console Workflows\n\n| Section          | What it manages                                                                                                                                    |\n| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |\n| **Zones**        | Zone records and active zone selection.                                                                                                            |\n| **Applications** | Confidential agent applications and their client secrets.                                                                                          |\n| **Providers**    | Gateway upstream auth modes for Caracal mandates, OAuth 2.0 authorization code, OAuth 2.0 client credentials, API-key, and bearer-token upstreams. |\n| **Resources**    | Protected resource identifiers, scopes, upstream URLs, Gateway application bindings, and upstream credential provider bindings.                    |\n| **Policies**     | Policy content and versions.                                                                                                                       |\n| **Policy Sets**  | Policy-set composition, simulation, and activation.                                                                                                |\n| **Subjects**     | Subjects and their Authority record histories.                                                                                                     |\n| **Sessions**     | Governed Coordinator executions and lifecycle state.                                                                                               |\n| **Control**      | Control API exposure and Control credentials.                                                                                                      |\n\n## Secrets and Credentials\n\nApplication client secrets and workload secrets are held sealed in the Secret Store: copy one at creation, or reveal it later from the object's detail panel - every reveal is recorded in the zone audit timeline. Provider secrets are accepted only in provider create or rotation-style edit flows and are stored sealed by the Control API. The web console masks secret-shaped fields and error output.\n\n## Automation Handoff\n\nUse the web console's **Control** section when management needs to move from interactive operations to automation. Control API calls are authenticated and audited. They use the same product-management boundary as the web console; they are not exposed as top-level runtime CLI commands.\n\n## Troubleshooting\n\n| Symptom                                      | Check                                                                                                                                    |\n| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |\n| A page says a zone is required               | Select a zone before opening zone-scoped pages.                                                                                          |\n| A created client secret is no longer visible | Reveal it from the application's detail panel (the reveal is audited); applications created before credential custody must rotate first. |\n| Policy activation fails                      | Run simulation, inspect validation errors, and confirm the policy set references the intended policy versions.                           |\n| Control API calls fail                       | Confirm Control is enabled from the web console's **Control** section and the automation token targets the Control resource.             |\n\n## Next Step\n\nUse [Inspect Diagnostics and Audit](/runtime-console/observability/) when you need health checks, audit records, or request traces."},{"id":"/runtime-console/observability","title":"Inspect Diagnostics and Audit","url":"https://docs.caracal.run/runtime-console/observability/","concepts":"","body":"Observability workflows live in the web console because they need operator context, selected zones, and structured API responses.\n\n## Diagnostics\n\nOpen **Diagnostics**. The Doctor view runs the shared engine diagnostics across four sections:\n\n| Section       | Checks                                                                                                          |\n| ------------- | --------------------------------------------------------------------------------------------------------------- |\n| **Health**    | API reachability, admin authentication, and operator clock skew against the platform.                           |\n| **Readiness** | Per-service `/ready` probes plus threshold evaluation of STS, Gateway, audit, and Coordinator operator metrics. |\n| **Zones**     | Visible zones, resources, policy sets, and audit queryability.                                                  |\n| **Preflight** | Local secrets, keys, TLS material, and Postgres/Redis reachability.                                             |\n\nDoctor does more than report liveness: it evaluates the operator metrics it collects and raises a warning or failure when a signal needs action. Notable deeper checks include:\n\n| Signal                                                       | Result | Why it matters                                                                             |\n| ------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------ |\n| Clock skew vs the API exceeds 30s                            | `fail` | Token `iat`/`nbf`/`exp` validation and audit ordering break when hosts are not NTP-synced. |\n| STS OPA `compile_errors` > 0                                 | `fail` | A policy bundle will not compile, so token exchanges deny or run on a stale bundle.        |\n| Audit `tamper_mismatch_total` or `chain_breaks` > 0          | `fail` | Audit chain integrity is broken - treat as a security incident.                            |\n| Audit `dlq_size` or `hmac_failures` > 0                      | `warn` | Events are failing processing or HMAC verification before they age out.                    |\n| Coordinator outbox `dead` rows, or a large `pending` backlog | `warn` | Session-start and revocation events are not propagating downstream.                        |\n\nUseful Doctor controls include reload, all-zones or selected-zone checks, preflight-only checks, and strict readiness where warnings fail automation gates. The `Next actions` block collects the advice for every non-passing check.\n\nThis view is deeper than `caracal status`: `status` answers \"is the stack up\", while Doctor inspects internal health - policy compilation, audit integrity, event propagation, clock alignment, and local key material - that keeps the platform working correctly over time.\n\n## Audit\n\nOpen **Audit**. Audit records tie STS, Gateway, Coordinator, policy, and Control decisions to request IDs and authority anchors. Use the audit view to filter recent events, inspect denials, and gather incident context.\n\nThe audit view opens recent events immediately and streams new ones as they arrive. Advanced filters include decision, event type, time window, request ID, Session, agent label, and result limit. Time-window fields accept a relative window such as `15m`, `2h`, or `7d`, an ISO timestamp, or a date. Use row actions to open the full event group or the decision trace for the selected request.\n\nAudit is an event ledger: it records what authorization decided. It is distinct from the session views, which show the live state of subjects, sessions, and delegations. Filter audit by `session` or `agent label` to connect a decision back to the session that made it.\n\n## Request Trace\n\nWhen you already have a request ID - from a log line, error response, or Gateway header - filter **Audit** by that request ID and open the decision trace from the row, without scanning the full list. Trace output connects the request, selected resource, subject, scopes, determining policies, diagnostics, and result.\n\n## Troubleshooting\n\n| Symptom                        | Check                                                                                       |\n| ------------------------------ | ------------------------------------------------------------------------------------------- |\n| Diagnostics cannot reach API   | Run `caracal status --ready`; custom deployments should confirm the admin API URL override. |\n| Zone diagnostics show no zones | Confirm the admin token has visibility into the expected zone.                              |\n| Audit records are missing      | Confirm the selected zone, time window, and audit service readiness.                        |\n\nFor a symptom-first path from a failing application call to the right surface and tool, see [Troubleshoot by Symptom](/operations/troubleshooting/).\n\n:::note[FAQ]\n[Why is an audit event missing?](/reference/faq/#faq-018)\n:::\n\n## Next Step\n\nUse [Manage Sessions and Delegation](/runtime-console/agents/) when a request involves Sessions, child Sessions, or delegated authority."},{"id":"/runtime-console/agents","title":"Manage Sessions and Delegation","url":"https://docs.caracal.run/runtime-console/agents/","concepts":"","body":"Session and delegation operations are web console workflows backed by the Coordinator. They are not top-level runtime CLI commands.\n\n## Requirements\n\n| Input                                                           | Purpose                                                                     |\n| --------------------------------------------------------------- | --------------------------------------------------------------------------- |\n| Coordinator endpoint                                            | Automatic for local dev/stable; explicit only for cloud/custom deployments. |\n| `CARACAL_COORDINATOR_TOKEN` or `CARACAL_COORDINATOR_TOKEN_FILE` | Coordinator authority for session and delegation views.                     |\n| Selected zone                                                   | Scope for session and delegation queries.                                   |\n\nFor the default local stack, the web console prefers the Coordinator token generated by `caracal up` so a purge/up cycle does not leave an old exported token in use.\n\n## Sessions\n\nOpen **Sessions** to list governed executions, inspect a Session tree, suspend an active subtree, resume a suspended subtree, or terminate a Session. Tree views show parent-child relationships so operators can understand inherited authority.\n\n## Delegation\n\nOpen **Delegation** to inspect active, inbound, and outbound delegations. Detail views show source and target sessions, resources, scope bounds, expiry, and traversal state.\n\nRevoking a delegation ends the delegated authority immediately. Review outbound delegations before terminating a session so dependent work can be stopped cleanly.\n\n## Audit Correlation\n\nSession IDs and Delegation IDs appear in audit events. Use **Audit** request tracing when you need the policy decision and diagnostic payload behind an allow or deny.\n\n## Troubleshooting\n\n| Symptom                               | Check                                                                                      |\n| ------------------------------------- | ------------------------------------------------------------------------------------------ |\n| Session pages are empty               | Confirm the selected zone and that workloads are creating sessions through an SDK.         |\n| Coordinator token missing             | Run `caracal up` locally or set `CARACAL_COORDINATOR_TOKEN_FILE` for a remote Coordinator. |\n| Delegation revocation appears delayed | Confirm resource servers consume revocation state and reject revoked delegation anchors.   |"},{"id":"/sdks","title":"Choose an SDK or Package","url":"https://docs.caracal.run/sdks/","concepts":"","body":"Use SDKs when you need package-level details after a guide. The [Guides](/guides/) section shows task workflows; this section explains package names, install commands, public APIs, runtime requirements, and where each package fits.\n\n## Choose by What You Are Building\n\n| I am building...                     | Start here                                           | Then read                                                                                             |\n| ------------------------------------ | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |\n| TypeScript app or agent workflow     | [TypeScript SDK](./typescript/)                      | [OAuth Package](./oauth/), [Verify Package](./verify/)                                                |\n| Python app, agent, or ASGI service   | [Python SDK](./python/)                              | [ASGI Adapter](./adapters/asgi/), [FastMCP Adapter](./adapters/fastmcp/), [Verify Package](./verify/) |\n| Go service or agent workflow         | [Go SDK](./go/)                                      | [Go net/http Adapter](./adapters/nethttp/)                                                            |\n| Express resource server              | [Express Adapter](./adapters/express/)               | [Verify Package](./verify/), [Redis Revocation Store](./backends/redis/)                              |\n| FastAPI or Starlette resource server | [ASGI Adapter](./adapters/asgi/)                     | [Verify Package](./verify/), [Redis Revocation Store](./backends/redis/)                              |\n| FastMCP server                       | [FastMCP Adapter](./adapters/fastmcp/)               | [Verify Package](./verify/)                                                                           |\n| Custom verification boundary         | [Verification Layer Overview](./verification-layer/) | [Identity Package](./identity/), [Revocation Package](./revocation/)                                  |\n| Admin or provisioning automation     | [Admin Package](./admin/)                            | [Use the Admin API](/api/control-plane/)                                                              |\n| Shared revocation storage            | [Redis Revocation Store](./backends/redis/)          | [Revocation Package](./revocation/)                                                                   |\n\n## Package Map\n\n| Area                   | TypeScript / Node             | Python                       | Go                                                             |\n| ---------------------- | ----------------------------- | ---------------------------- | -------------------------------------------------------------- |\n| App SDK                | `@caracalai/sdk`              | `caracalai-sdk`              | `github.com/garudex-labs/caracal/packages/sdk/go`              |\n| Identity verification  | `@caracalai/identity`         | `caracalai-identity`         | `github.com/garudex-labs/caracal/packages/identity/go`         |\n| OAuth token exchange   | `@caracalai/oauth`            | `caracalai-oauth`            | `github.com/garudex-labs/caracal/packages/oauth/go`            |\n| Revocation store       | `@caracalai/revocation`       | `caracalai-revocation`       | `github.com/garudex-labs/caracal/packages/revocation/go`       |\n| Verification engine    | `@caracalai/verify`           | `caracalai-verify`           | `github.com/garudex-labs/caracal/packages/verify/go`           |\n| Express adapter        | `@caracalai/express`          | Not applicable               | Not applicable                                                 |\n| FastMCP adapter        | `@caracalai/fastmcp`          | `caracalai-fastmcp`          | Not applicable                                                 |\n| ASGI adapter           | Not applicable                | `caracalai-asgi`             | Not applicable                                                 |\n| net/http adapter       | Not applicable                | Not applicable               | `github.com/garudex-labs/caracal/packages/adapters/nethttp/go` |\n| Redis revocation store | `@caracalai/revocation-redis` | `caracalai-revocation-redis` | `github.com/garudex-labs/caracal/packages/backends/redis/go`   |\n\n## Runtime Requirements\n\n| Ecosystem | Current package target                                                                                                |\n| --------- | --------------------------------------------------------------------------------------------------------------------- |\n| Node.js   | Node `>=22` for published TypeScript packages that declare an engine.                                                 |\n| Python    | Python `>=3.12` for published Python packages.                                                                        |\n| Go        | Module paths under `github.com/garudex-labs/caracal/packages/...`; current modules declare Go `1.26` where specified. |\n\n## Related Guides\n\n- [Add SDK to Your App](/get-started/add-sdk-to-your-app/)\n- [Connect Your App with the SDK](/tutorials/connect-an-agent/)\n- [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/)\n- [Implement Multi-Agent Delegation](/guides/delegation/)"},{"id":"/sdks/typescript","title":"TypeScript SDK","url":"https://docs.caracal.run/sdks/typescript/","concepts":"","body":"`@caracalai/sdk` is the main TypeScript package for Session lifecycle, Delegation, Gateway routing, and Caracal context propagation.\n\n## Install\n\n```bash\nnpm install @caracalai/sdk\n```\n\nThe package is ESM-only and targets Node `>=22`. To consume it from a CommonJS project, use a dynamic `await import('@caracalai/sdk')` or set `\"type\": \"module\"` in your `package.json`.\n\n## Connect and Configure\n\n| API                                 | Use it when                                                                                              |\n| ----------------------------------- | -------------------------------------------------------------------------------------------------------- |\n| `new Caracal()`                     | Use normal deployment configuration: exactly `CARACAL_CONFIG` when set, otherwise `CARACAL_*` variables. |\n| `Caracal.fromClientSecret(options)` | Supply one complete static client-secret configuration directly.                                         |\n\n```ts\nimport { Caracal } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\n```\n\nThe constructor never searches home directories or default profile paths. Multiple credential modes fail at startup instead of using precedence. Explicit environment mappings, profile loading, dynamic credential resolvers, custom transports, and raw configuration live in `@caracalai/sdk/advanced`.\n\n`Caracal.fromClientSecret` requires the static `zoneId`/`applicationId`/`clientSecret` triple. Resources are optional for per-resource application transports; Session and lifecycle operations fail clearly when no lifecycle resource audience is configured. It refreshes application subject tokens automatically.\n\n## Core methods\n\n| Method                                         | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |\n| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `session(fn, options?)`                        | Run `fn` inside a governed Session; `fn` receives the bound context, including its Session ID. Pass `authority: Authority.narrow(...)` to bound authority and `task` to describe the work. Retry protection is automatic; ordinary code should omit `idempotencyKey`. See [Safe Retries and Idempotency](/guides/idempotency/) for durable-source redelivery.                                                                                                                                  |\n| `startSession(options?)`                       | Start a governed Session that outlives a block; auto-renews its lease and returns a handle with `heartbeat()`, `deadlineAt`, and `close()`. Service lifetime is lease-only. Pass `authority: Authority.narrow(...)` with a positive Delegation TTL to bound the handle's authority. Retry protection is automatic.                                                                                                                                                                              |\n| `attachSession(sessionId, options?)`           | Re-attach to a persisted long-lived Session after a restart: validates it with an immediate lease renewal and returns the same handle `startSession` does.                                                                                                                                                                                                                                                                                                                                     |\n| `delegate(options)`                            | Delegate a slice of the current Session's authority to an existing peer Session; returns the Delegation for the receiver to accept. A transient Coordinator failure is retried once under an idempotency key, so no duplicate Delegation is issued.                                                                                                                                                                                                                                            |\n| `revokeDelegation(delegationId)`               | Revoke a Delegation issued by this application.                                                                                                                                                                                                                                                                                                                                                                                    |\n| `acceptDelegation(delegationId, fn, options?)` | Present a received Delegation: bind a derived context carrying it while `fn` runs. Pass `{ validate: true }` to confirm with the Coordinator that the Delegation is live for the bound Session first.                                                                                                                                                                                                                                                                                          |\n| `headers(options?)`                            | Project the current bound context into HTTP headers s"},{"id":"/sdks/python","title":"Python SDK","url":"https://docs.caracal.run/sdks/python/","concepts":"","body":"`caracalai-sdk` is the main Python package for async Session lifecycle, Delegation, Gateway routing, and ASGI context propagation.\n\n## Install\n\n```bash\npip install caracalai-sdk\n```\n\nThe package requires Python `>=3.12`.\n\n## Connect and Configure\n\n| API                               | Use it when                                                                                              |\n| --------------------------------- | -------------------------------------------------------------------------------------------------------- |\n| `Caracal()`                       | Use normal deployment configuration: exactly `CARACAL_CONFIG` when set, otherwise `CARACAL_*` variables. |\n| `Caracal.from_client_secret(...)` | Supply one complete static client-secret configuration directly.                                         |\n\n```python\nfrom caracalai import Caracal\n\ncaracal = Caracal()\n```\n\nThe constructor never searches home directories or default profile paths. Multiple credential modes fail at startup. Explicit environment mappings, profile loading, dynamic credential resolvers, and raw configuration live in `caracalai.advanced`.\n\n## Core methods\n\n| Method                                                                                                      | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |\n| ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `async with caracal.session(...)`                                                                           | Run the block inside a governed Session - a bounded identity Caracal establishes around whatever the block executes; pass `authority=Authority.narrow(...)` to bound its authority and `task=` to record what the Session is for. Retry protection is automatic; ordinary code should omit `idempotency_key`. See [Safe Retries and Idempotency](/guides/idempotency/) for durable-source redelivery.                                                                                                                                                                          |\n| `await caracal.start_session(...)`                                                                          | Start a governed Session that outlives a block; auto-renews its lease and returns a handle with `heartbeat()` and `aclose()`. Service lifetime is lease-only. Pass `authority=Authority.narrow(...)` with a positive Delegation TTL to bound the handle's authority. Retry protection is automatic.                                                                                                                                                                                                               |\n| `await caracal.attach_session(session_id, ...)`                                                             | Re-attach to a persisted long-lived Session after a restart: validates it with an immediate lease renewal and returns the same handle `start_session` does.                                                                                                                                                                                                                                                                                                                                                                                                                    |\n| `await caracal.delegate(...)`                                                                               | Delegate a slice of the bound Session's authority to an existing peer Session; returns the Delegation for the receiver to accept. A transient Coordinator failure is retried once under an idempotency key, so no duplicate Delegation is issued.                                                                                                                                                                                                                                                                                                                              |\n| `await caracal.revoke_delegation(delegation_id)`                                                            | Revoke a Delegation issued by this application.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |\n| `async with caracal.accept_delegation(delegation_id, validate=False)`                                       | Present a received Delegation: bind a derived context carrying it. Pass `validate=True` to confirm with the Coordinator that the Delegation "},{"id":"/sdks/go","title":"Go SDK","url":"https://docs.caracal.run/sdks/go/","concepts":"","body":"The Go SDK provides Session lifecycle, Delegation, Gateway routing, and context propagation for Go services.\n\n## Install\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/sdk/go\n```\n\n## Connect and Configure\n\n```go\nimport caracal \"github.com/garudex-labs/caracal/packages/sdk/go\"\n\nclient, err := caracal.New()\nif err != nil {\n\treturn err\n}\n```\n\n`New()` loads exactly `CARACAL_CONFIG` when set; otherwise it loads `CARACAL_*` environment variables. It never searches home directories or default profile paths. Multiple credential modes fail at startup.\n\n## Core methods\n\n| API                                                    | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |\n| ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `New()`                                                | Use normal deployment configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |\n| `FromClientSecret(options)`                            | Supply one complete static client-secret configuration directly.                                                                                                                                                                                                                                                                                                                                                                                                                                         |\n| `FromEnv()`, `FromConfig(path)`, `FromCredentials(...)` | Advanced explicit loading, profile, and dynamic credential paths.                                                                                                                                                                                                                                                                                                                                                                                                                                         |\n| `client.Session(ctx, fn, opts...)`                     | Run `fn` inside a governed Session with a bound `context.Context`; set `SessionOptions.Authority` to bound its authority and `Task` to record what the Session is for. Retry protection is automatic; ordinary code should leave `IdempotencyKey` empty. See [Safe Retries and Idempotency](/guides/idempotency/) for durable-source redelivery.                                                                                                                                                         |\n| `client.StartSession(ctx, opts...)`                    | Start a governed Session that outlives a block; auto-renews its lease and returns a `*SessionHandle` with `Heartbeat`, `DeadlineAt`, and `Close`. Retry protection is automatic.                                                                                                                                                                                                                                                                                                                         |\n| `client.AttachSession(ctx, sessionID, opts...)`        | Re-attach to a persisted long-lived Session after a restart: validates it with an immediate lease renewal and returns the same handle `StartSession` does.                                                                                                                                                                                                                                                                                                                                               |\n| `client.Delegate(ctx, opts)`                           | Delegate a slice of the current Session's authority to a peer Session; returns the Delegation for the receiver to accept. A transient Coordinator failure is retried once under an idempotency key, so no duplicate Delegation is issued.                                                                                                                                                                                                                                                                 |\n| `client.RevokeDelegation(ctx, delegationID)`           | Revoke a Delegation issued by this application.                                                                                                                                                                                                                                                                                                                                                                            "},{"id":"/sdks/verification-layer","title":"Verification Layer Overview","url":"https://docs.caracal.run/sdks/verification-layer/","concepts":"","body":"Use this page when you are protecting an inbound resource-server boundary and need to choose the right package layer. Start with the highest-level adapter that fits your framework; use lower-level packages only when you are building a custom boundary.\n\n## Which Layer Should I Use?\n\n| Need                                                      | Use                                                                                  |\n| --------------------------------------------------------- | ------------------------------------------------------------------------------------ |\n| Express route middleware                                  | [Express Adapter](./adapters/express/)                                               |\n| FastAPI/Starlette (ASGI) middleware                       | [ASGI Adapter](./adapters/asgi/)                                                     |\n| FastMCP server or tool authentication                     | [FastMCP Adapter](./adapters/fastmcp/)                                               |\n| Go `net/http` middleware                                  | [Go net/http Adapter](./adapters/nethttp/)                                           |\n| Framework-neutral bearer parsing and mandate verification | [Verify Package](./verify/)                                                          |\n| Custom JWT claim verification                             | [Identity Package](./identity/)                                                      |\n| Shared revocation checks                                  | [Revocation Package](./revocation/) plus [Redis Revocation Store](./backends/redis/) |\n\n## Framework Adapters\n\nAdapters bind the shared verify and identity packages to common server frameworks. They should reject failed requests before your handler or tool runs, attach verified claims to framework context, and preserve the same 401/403 behavior across languages.\n\nUse adapters first when your framework is supported. They reduce boilerplate and keep error mapping consistent with the rest of Caracal.\n\n## Verify Package\n\n[Verify Package](./verify/) is the reusable verification engine under the adapters. Use it when your framework is unsupported or when you need direct control over bearer parsing, verifier defaults, route-level scopes, targets, Session requirements, Delegation requirements, hop limits, and safe error hints.\n\n## Identity Package\n\n[Identity Package](./identity/) verifies mandate JWT claims directly. Use it when you are composing a custom verifier or adapter. It does not provide the full verify-engine error mapping or revocation-store integration by itself.\n\n## Revocation and Shared State\n\nResource servers must reject mandates anchored to a revoked Authority record ID, Root authority record ID, Session ID, or Delegation ID. Use in-memory revocation stores for local development only. Use [Redis Revocation Store](./backends/redis/) for multi-instance resource servers that consume the protocol stream `caracal.sessions.revoke`.\n\n## Failure Behavior\n\nAll HTTP verification layers should preserve the shared status mapping:\n\n| Status | Meaning                                                                                            |\n| ------ | -------------------------------------------------------------------------------------------------- |\n| `401`  | The credential was missing, invalid, expired, revoked, or from the wrong zone.                     |\n| `403`  | The mandate verified but lacks required scope, target, Session, Delegation, chain, or hop authority. |\n\n## Related Pages\n\n- [Protect an MCP Server](/guides/protect-mcp/)\n- [Protect an Express App](/guides/protect-express/)\n- [Protect a FastAPI App](/guides/protect-fastapi/)\n- [Protect a FastMCP App](/guides/protect-fastmcp/)\n- [Protect a Go net/http Service](/guides/protect-nethttp/)"},{"id":"/sdks/adapters","title":"Framework Adapters","url":"https://docs.caracal.run/sdks/adapters/","concepts":"","body":"Framework adapters bind the lower-level identity and verify packages to common server frameworks. Use them before reaching for lower-level verification APIs.\n\n## Adapter Map\n\n| Adapter | Package | Use it for |\n| --- | --- | --- |\n| [Express](/sdks/adapters/express/) | `@caracalai/express` | Protecting Express 5 routes with Caracal mandate verification. |\n| [ASGI](/sdks/adapters/asgi/) | `caracalai-asgi` | Protecting FastAPI, Starlette, and other Python ASGI apps with Caracal mandate verification. |\n| [FastMCP](/sdks/adapters/fastmcp/) | `@caracalai/fastmcp`, `caracalai-fastmcp` | Verifying FastMCP bearer tokens before tool execution. |\n| [Go net/http](/sdks/adapters/nethttp/) | `github.com/garudex-labs/caracal/packages/adapters/nethttp/go` | Protecting Go HTTP handlers. |\n\n## Choose a Verification Layer\n\n| Need | Use |\n| --- | --- |\n| Framework-neutral verification | [Verify Package](/sdks/verify/) |\n| Express route middleware | [Express Adapter](/sdks/adapters/express/) |\n| FastAPI/Starlette (ASGI) middleware | [ASGI Adapter](/sdks/adapters/asgi/) |\n| FastMCP tool/server authentication | [FastMCP Adapter](/sdks/adapters/fastmcp/) |\n| Go HTTP middleware | [Go net/http Adapter](/sdks/adapters/nethttp/) |\n| Shared revocation store | [Redis Revocation Store](/sdks/backends/redis/) |\n\n## Boundary Semantics\n\nEvery HTTP adapter maps verification failures through one canonical status function in `@caracalai/verify` (`httpStatusForAuthError` in TypeScript, `verify.HTTPStatus` in Go), so the boundary behaves identically across frameworks and languages:\n\n- **401** - the credential itself was not accepted: `missing_token`, `invalid_token`, `invalid_zone`, `session_revoked`, `delegation_stale`.\n- **403** - the mandate verified but its authority is insufficient for the route: `insufficient_scope`, `session_required`, `delegation_required`, `chain_mismatch`, `hop_count_exceeded`.\n\nAdapters never re-derive these status codes; they consume the shared mapping.\n\n## Related State Backends\n\n- [Redis Revocation Store](/sdks/backends/redis/)\n\n## Related Guides\n\n- [Verification Layer Overview](/sdks/verification-layer/)\n- [Protect an MCP Server](/guides/protect-mcp/)\n- [Protect an Express App](/guides/protect-express/)\n- [Protect a FastAPI App](/guides/protect-fastapi/)\n- [Protect a FastMCP App](/guides/protect-fastmcp/)\n- [Protect a Go net/http Service](/guides/protect-nethttp/)"},{"id":"/sdks/adapters/express","title":"Express Adapter","url":"https://docs.caracal.run/sdks/adapters/express/","concepts":"","body":"`@caracalai/express` protects Express routes by parsing the bearer token, verifying the mandate through `@caracalai/verify`, and attaching Caracal claims to the request.\n\n## Install\n\n```bash\nnpm install @caracalai/express @caracalai/verify @caracalai/revocation-redis\n```\n\nThe adapter has an Express `^5.0.0` peer dependency and targets Node `>=22`.\n\n## Middleware\n\n```ts\nimport express from 'express'\nimport { caracalAuth } from '@caracalai/express'\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { RedisRevocationStore } from '@caracalai/revocation-redis'\n\nconst app = express()\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.example.com',\n  audience: 'https://mcp.example.com',\n  zoneId: 'zone_prod',\n  revocations: new RedisRevocationStore(redis),\n})\n\napp.use(\n  '/mcp',\n  caracalAuth(\n    { verifier },\n    {\n      requiredScopes: ['mcp:tool:call'],\n      requiredTargets: ['https://mcp.example.com'],\n      requireSession: true,\n    },\n  ),\n)\n```\n\n## Request shape\n\nThe middleware attaches Caracal claims to `req.caracal` and `req.caracalClaims` when verification succeeds. Use the exported `CaracalRequest` type when a handler needs typed access.\n\n```ts\nimport type { CaracalRequest } from '@caracalai/express'\n\napp.post('/mcp/tools/search', (req: CaracalRequest, res) => {\n  res.json({ subject: req.caracal?.sub })\n})\n```\n\n## Failure behavior\n\nFailed verification returns a verify-engine error code such as `missing_token`, `invalid_token`, `insufficient_scope`, or `session_revoked`, plus a safe `error_hint` field. Pair the adapter with a shared revocation store when multiple resource-server instances serve the same resource.\n\n## Related pages\n\n- [Protect an Express App](/guides/protect-express/)\n- [Verify Package](/sdks/verify/)\n- [Redis Revocation Store](/sdks/backends/redis/)"},{"id":"/sdks/adapters/asgi","title":"ASGI Adapter","url":"https://docs.caracal.run/sdks/adapters/asgi/","concepts":"","body":"The ASGI adapter protects any Python ASGI application - FastAPI, Starlette, Quart, Django ASGI - with fail-closed Caracal mandate verification. It is pure ASGI: it imports no web framework and delegates every check to [Verify Package](/sdks/verify/).\n\n## Install\n\n```bash\npip install caracalai-asgi\n```\n\n## Add middleware\n\n```python\nfrom caracalai_asgi import CaracalASGIAuth\nfrom caracalai_revocation import InMemoryRevocationStore\nfrom fastapi import FastAPI\n\napp = FastAPI()\napp.add_middleware(\n    CaracalASGIAuth,\n    audience=\"resource://billing-api\",\n    revocations=InMemoryRevocationStore(),\n    required_scopes=[\"billing:read\"],\n    routes={\n        \"/payouts\": {\"required_scopes\": [\"billing:payout\"], \"require_delegation\": True},\n    },\n    exclude=[\"/healthz\"],\n)\n\n@app.get(\"/balances\")\nasync def balances(request):\n    principal = request.state.caracal\n    return {\"sub\": principal.sub, \"scopes\": principal.scope}\n```\n\n`issuer` defaults to `CARACAL_STS_URL` and `expected_zone_id` to `CARACAL_ZONE_ID`, so a provider deployed with the standard Caracal workload variables only states its own audience and revocation store. Construction fails if no issuer or zone can be resolved.\n\n## Options\n\n| Option                                                   | Use it for                                                                                                      |\n| -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |\n| `audience`                                               | The provider's own resource identifier; mandates minted for other resources are rejected.                       |\n| `revocations`                                            | Revocation store consulted on every request. Use [Redis Revocation Store](/sdks/backends/redis/) in production. |\n| `required_scopes` / `required_targets`                   | Default scope and target requirements for every route.                                                          |\n| `require_session` / `require_delegation` / `max_hop_count` | Session identity, delegated authority, and Delegation-depth requirements.                                      |\n| `routes`                                                 | Per-route overrides by path prefix; the longest matching prefix wins. Any option above can be overridden.       |\n| `exclude`                                                | Path prefixes served without verification (health and readiness probes).                                        |\n\n## Behavior\n\n- Verified claims are stored as `scope[\"state\"][\"caracal\"]`, surfaced as `request.state.caracal` in Starlette and FastAPI.\n- Failed verification answers with the shared status mapping (`http_status_for_auth_error`): 401 for credential failures, 403 for insufficient authority, with the standard `error`/`error_description` JSON body.\n- WebSocket connections that fail verification are closed with policy code `1008`; lifespan events pass through.\n- Call `await middleware.warmup()` at startup to prefetch the zone JWKS before the first request.\n\n## Boundary\n\nThe adapter verifies inbound mandates; it does not create Sessions or Delegations. Use the [Python SDK](/sdks/python/) to create Caracal context before making outbound calls.\n\n## Related pages\n\n- [Protect a FastAPI App](/guides/protect-fastapi/)\n- [Verify Package](/sdks/verify/)\n- [Sessions and Revocation](/concepts/sessions-revocation/)"},{"id":"/sdks/adapters/fastmcp","title":"FastMCP Adapter","url":"https://docs.caracal.run/sdks/adapters/fastmcp/","concepts":"","body":"FastMCP adapters expose small verifier APIs that delegate to the verify packages. Use them when your FastMCP integration needs to authenticate a bearer token before running a tool.\n\n## Install\n\n| Ecosystem  | Package                                                                  |\n| ---------- | ------------------------------------------------------------------------ |\n| TypeScript | `npm install @caracalai/fastmcp @caracalai/verify @caracalai/revocation` |\n| Python     | `pip install caracalai-fastmcp`                                          |\n\n## TypeScript verifier\n\n```ts\nimport { extractBearer, verifyFastMcpToken } from '@caracalai/fastmcp'\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.example.com',\n  audience: 'https://mcp.example.com',\n  zoneId: 'zone_prod',\n  revocations: new InMemoryRevocationStore(),\n})\n\nconst token = extractBearer(request.headers.get('authorization') ?? '')\nif (!token) throw new Error('missing bearer token')\n\nconst context = await verifyFastMcpToken(token, verifier, {\n  requiredScopes: ['mcp:tool:call'],\n  requiredTargets: ['https://mcp.example.com'],\n  requireSession: true,\n})\n\nconsole.log(context.sub, context.zoneId, context.scope)\n```\n\n`verifyFastMcpToken()` returns `{ sub, zoneId, scope }` or throws `FastMcpAuthError`.\n\n## Python verifier\n\n```python\nfrom caracalai_fastmcp import CaracalAuth, CaracalAuthError\n\nauth = CaracalAuth(\n    issuer=\"https://sts.example.com\",\n    audience=\"https://mcp.example.com\",\n    zone_id=\"zone_prod\",\n    required_scopes=[\"mcp:tool:call\"],\n    required_targets=[\"https://mcp.example.com\"],\n    require_session=True,\n    revocations=revocations,\n)\n\ntry:\n    context = await auth.verify_token(token)\nexcept CaracalAuthError as exc:\n    raise RuntimeError(exc.code) from exc\n```\n\n## Boundary\n\nThe adapter verifies tokens; it does not create Sessions or Delegations. Use the [Python SDK](/sdks/python/) or [TypeScript SDK](/sdks/typescript/) to create Caracal context before making outbound calls.\n\n## Related pages\n\n- [Protect a FastMCP App](/guides/protect-fastmcp/)\n- [Verify Package](/sdks/verify/)\n- [Sessions and Revocation](/concepts/sessions-revocation/)"},{"id":"/sdks/adapters/nethttp","title":"Go net/http Adapter","url":"https://docs.caracal.run/sdks/adapters/nethttp/","concepts":"","body":"The Go net/http adapter wraps handlers with verify-engine verification and stores verified claims in the request context.\n\n## Install\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/adapters/nethttp/go\n```\n\n## Middleware\n\n```go\nimport (\n\t\"net/http\"\n\t\"time\"\n\n\tnethttp \"github.com/garudex-labs/caracal/packages/adapters/nethttp/go\"\n\trevocation \"github.com/garudex-labs/caracal/packages/revocation/go\"\n\tverify \"github.com/garudex-labs/caracal/packages/verify/go\"\n)\n\nrevocations := revocation.NewInMemoryStore(24 * time.Hour)\n\nverifier := verify.NewVerifier(verify.Options{\n\tIssuer:          \"https://sts.example.com\",\n\tAudience:        \"https://api.example.com\",\n\tZoneID:          \"zone_prod\",\n\tRevocations:     revocations,\n})\n\nhandler := nethttp.VerifierMiddleware(verifier.Require(verify.Options{\n\tRequiredScopes:  []string{\"tickets:read\"},\n\tRequiredTargets: []string{\"https://api.example.com/tickets\"},\n\tRequireSession:  true,\n}))(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n\tclaims, ok := nethttp.ClaimsFromContext(r.Context())\n\tif !ok {\n\t\thttp.Error(w, \"missing claims\", http.StatusUnauthorized)\n\t\treturn\n\t}\n\t_, _ = w.Write([]byte(claims.Sub))\n}))\n```\n\n## APIs\n\n| API | Purpose |\n| --- | --- |\n| `Middleware(opts)` | Return middleware that verifies the bearer token and rejects failed requests. |\n| `VerifierMiddleware(verifier)` | Return middleware backed by a reusable verifier with shared defaults. |\n| `ClaimsFromContext(ctx)` | Retrieve verified Caracal claims inside a handler. |\n\n## Failure behavior\n\nThe middleware maps verification errors to HTTP failures before the handler runs and includes a safe `error_hint` in JSON failures. Use a shared revocation store through `verify.Options` in production so revoked sessions are rejected consistently across service instances.\n\n## Related pages\n\n- [Protect a Go net/http Service](/guides/protect-nethttp/)\n- [Go SDK](/sdks/go/)\n- [Verify Package](/sdks/verify/)"},{"id":"/sdks/verify","title":"Verify Package","url":"https://docs.caracal.run/sdks/verify/","concepts":"","body":"The verify packages authenticate Caracal mandates without tying the check to Express, FastMCP, or Go net/http. Framework adapters build on this engine.\n\n## Install\n\n| Ecosystem  | Package                                                     |\n| ---------- | ----------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/verify`                             |\n| Python     | `pip install caracalai-verify`                              |\n| Go         | `go get github.com/garudex-labs/caracal/packages/verify/go` |\n\n## Core APIs\n\n| API                                                                        | Purpose                                                                                                      |\n| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |\n| `extractBearer` / `extract_bearer` / `ExtractBearer`                       | Parse a bearer token from an `Authorization` header.                                                         |\n| `createMandateVerifier` / `NewVerifier`                                    | Build a reusable verifier with secure defaults, JWKS caching, revocation checks, and per-route overrides.    |\n| `create_mandate_verifier`                                                  | Python reusable verifier with the same defaults, warmup, and per-route override model.                       |\n| `authenticate` / `Authenticate`                                            | Verify one token against identity claims and revocation anchors.                                             |\n| `authenticateRequest` / `unauthorizedResponse`                             | Authenticate a fetch-standard `Request` and render an `AuthError` as a JSON `Response` in WinterTC runtimes. |\n| `checkActiveAuthority` / `check_active_authority` / `CheckActiveAuthority` | Check expiry and revoked anchors for verified claims.                                                        |\n\n## TypeScript example\n\n```ts\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.example.com',\n  audience: 'https://mcp.example.com',\n  zoneId: 'zone_prod',\n  revocations: new InMemoryRevocationStore(),\n})\n\nconst result = await verifier.authorization(req.headers.authorization, {\n  requiredScopes: ['mcp:tool:call'],\n  requiredTargets: ['https://mcp.example.com'],\n  requireSession: true,\n})\n\nif (!result.ok) {\n  throw new Error(`${result.error.code}: ${result.error.hint}`)\n}\n```\n\nUse one verifier per resource server and add route-level scopes or targets through `verifier.authorization(..., overrides)` or `verifier.require(overrides)`. The verifier defaults to resource mandates, checks the STS issuer and audience, enforces zone, scope, target, Session, Delegation, and hop constraints, checks expiry, and queries the Authority record ID, Root authority record ID, Session ID, and Delegation ID parsed claims as revocation anchors. See the [parsed claim mapping](/sdks/identity/#parsed-claim-names) for language-level and raw JWT names.\n\n### Fetch-standard runtimes\n\nRuntimes built on WinterTC `Request`/`Response` globals — Node 18+, Deno, Bun, and edge workers — can authenticate without a framework adapter:\n\n```ts\nimport { authenticateRequest, unauthorizedResponse } from '@caracalai/verify'\n\nexport default {\n  async fetch(request: Request): Promise<Response> {\n    const result = await authenticateRequest(request, deps)\n    if (!result.ok) {\n      return unauthorizedResponse(result.error)\n    }\n    return handle(request, result.principal)\n  },\n}\n```\n\n`unauthorizedResponse` maps error codes to 401 or 403 and renders the same JSON error body as the framework adapters. The same pair drops into any fetch-based framework. In Hono:\n\n```ts\napp.use('/api/*', async (c, next) => {\n  const result = await authenticateRequest(c.req.raw, deps)\n  if (!result.ok) return unauthorizedResponse(result.error)\n  c.set('principal', result.principal)\n  await next()\n})\n```\n\nAnd in a Next.js route handler:\n\n```ts\nexport async function GET(request: Request): Promise<Response> {\n  const result = await authenticateRequest(request, deps)\n  if (!result.ok) return unauthorizedResponse(result.error)\n  return Response.json(await loadTickets(result.principal))\n}\n```\n\nPython and Go services cover this role with the [ASGI adapter](/sdks/adapters/asgi/) and the [net/http adapter](/sdks/adapters/nethttp/), which wrap their ecosystems' native request types.\n\n## Python example\n\n```python\nfrom caracalai_revocation import InMemoryRevocationStore\nfrom caracalai_verify import AuthOptions, create_mandate_verifier\n\nverifier = create_mandate_verifier(\n    AuthOptions(\n        issuer=\"https://sts.example.com\",\n        audience=\"https://api.example.com\",\n        expected_zone_id=\"zone_prod\",\n        revocations=InMemoryRevocationStore(),\n    )\n)\n\nawait verifier.warmup()\n\nresult = await verifier.authorization(\n    request.headers.get(\"authorization\"),\n    required_scopes=[\"tickets:read\"],\n    required_targets=[\"https://api.example.com/tickets\"],\n)\n\nif not result.ok:\n    raise PermissionError(f\"{result.error.code}: {result.error.hint}\")\n```\n\n## Error codes\n\n`authenticate` and reusable verifiers normalize failures into typed error codes: `missing_token`, `invalid_token`, `invalid_zone`, `insufficient_scope`, `session_revoked`, `delegation_stale`, `session_required`, `delegation_required`, `chain_mismatch`, and `hop_count_exceeded`. TypeScript, Python, and Go reusable verifiers include a safe debugging hint for operator logs and API error bodies.\n\n## Related pages\n\n- [Verification Layer Overview](/sdks/verification-layer/)\n- [Protect an MCP Server](/guides/protect-mcp/)\n- [Express Adapter](/sdks/adapters/express/)\n- [FastMCP Adapter](/sdks/adapters/fastmcp/)\n- [Go net/http A"},{"id":"/sdks/identity","title":"Identity Package","url":"https://docs.caracal.run/sdks/identity/","concepts":"","body":"The identity packages verify Caracal mandate JWTs. Use them when you are building an adapter or custom resource-server boundary that accepts mandates directly.\n\n## Install\n\n| Ecosystem  | Package                                                       |\n| ---------- | ------------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/identity`                             |\n| Python     | `pip install caracalai-identity`                              |\n| Go         | `go get github.com/garudex-labs/caracal/packages/identity/go` |\n\nNode packages target Node `>=22`; Python packages require Python `>=3.12`.\n\n## Verification inputs\n\n| Option                                                                     | Meaning                                                                                                                                                                                                                             |\n| -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `issuer` / `Issuer`                                                        | Expected STS issuer.                                                                                                                                                                                                                |\n| `audience` / `Audience`                                                    | Expected audience for the mandate.                                                                                                                                                                                                  |\n| `zoneId` / `expected_zone_id` / `ZoneID`                                   | Required zone trust anchor. It fixes which zone's signing keyset verifies the token and must equal the `zone_id` claim, so key selection is never influenced by the unverified token. Verification fails closed when it is missing. |\n| `requiredScopes` / `required_scopes` / `RequiredScopes`                    | Scopes every accepted mandate must contain.                                                                                                                                                                                         |\n| `requiredTargets` / `required_targets` / `RequiredTargets`                 | Target resources every accepted mandate must include.                                                                                                                                                                               |\n| `requiredUse` / `required_use` / `RequiredUse`                             | Token use, usually `resource`.                                                                                                                                                                                                      |\n| `requireSession` / `require_session` / `RequireSession`                    | Require a governed Session identity.                                                                                                                                                                                                   |\n| `requireDelegation` / `require_delegation` / `RequireDelegation`           | Require a Delegation claim (`delegation_edge_id` on the wire).                                                                                                                                                                      |\n| `requireChainContains` / `require_chain_contains` / `RequireChainContains` | Require an application in the delegation chain.                                                                                                                                                                                     |\n| `maxHopCount` / `max_hop_count` / `MaxHopCount`                            | Cap delegation chain depth.                                                                                                                                                                                                         |\n\n## TypeScript example\n\n```ts\nimport { verify } from '@caracalai/identity'\n\nconst claims = await verify(token, {\n  issuer: 'https://sts.example.com',\n  audience: 'https://api.example.com',\n  zoneId: 'zone_prod',\n  requiredScopes: ['tickets:read'],\n  requiredTargets: ['https://api.example.com/tickets'],\n})\n```\n\n## Parsed claim names\n\nThe identity packages map raw JWT claims to canonical language-level fields:\n\n| Meaning                  | TypeScript              | Python                     | Go                      | Raw JWT claim        |\n| ------------------------ | ----------------------- | -------------------------- | ----------------------- | -------------------- |\n| Authority record ID      | `authorityRecordId`     | `authority_record_id`      | `AuthorityRecordID`     | `sid`                |\n| Root authority record ID | `rootAuthorityRecordId` | `root_authority_record_id` | `RootAuthorityRecordID` | `root_sid`           |\n| Session ID               | `sessionId`             | `session_id`               | `SessionID`             | `agent_session_id`   |\n| Delegation ID            | `delegationId`          | `delegation_id`            | `DelegationID`          | `delegation_edge_id` |\n\n## JWKS caching\n\nSTS signing keysets are zone-scoped: every fetch hits `{issuer}/.well-known/jwks.json?zone_id={zone}` and is cached per issuer and zone. TypeScript verifiers use an in-memory JWKS cache by default. Build a cache explicitly when a resource server wants shorter local-development TTLs, a custom fetch implementation, or warmup during service boot.\n\n```ts\nimport { createJwksCache, verify } fro"},{"id":"/sdks/revocation","title":"Revocation Package","url":"https://docs.caracal.run/sdks/revocation/","concepts":"","body":"The revocation packages define the store contract resource servers use to reject mandates after an Authority record, Root authority record, Session, or Delegation has been revoked.\n\n## Install\n\n| Ecosystem  | Package                                                         |\n| ---------- | --------------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/revocation`                             |\n| Python     | `pip install caracalai-revocation`                              |\n| Go         | `go get github.com/garudex-labs/caracal/packages/revocation/go` |\n\n## Contract\n\n| Operation                                                                    | Meaning                                                                                                                                      |\n| ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |\n| `isRevoked(anchorId)` / `is_revoked(anchor_id)` / `IsRevoked(anchorID)`                      | Return whether an Authority record, Root authority record, Session, or Delegation anchor is revoked. |\n| `markRevoked(anchorId, ttl)` / `mark_revoked(anchor_id, ttl)` / `MarkRevoked(anchorID, ttl)` | Record a revocation anchor for a TTL.                                                                |\n\n## In-memory stores\n\nUse in-memory stores for local development, tests, and single-process examples:\n\n| Ecosystem  | In-memory API                                               |\n| ---------- | ----------------------------------------------------------- |\n| TypeScript | `new InMemoryRevocationStore({ defaultTtlMs, maxEntries })` |\n| Python     | `InMemoryRevocationStore(default_ttl_ms=...)`               |\n| Go         | `revocation.NewInMemoryStore(defaultTTL)`                   |\n\nIn-memory stores do not share revocation state across processes. Production resource servers should use a shared backend and consume `caracal.sessions.revoke`.\n\n## Production path\n\nUse [Redis Revocation Store](/sdks/backends/redis/) for multi-instance resource servers. The Redis backend reads signed revocation stream messages, marks every revocation anchor, and lets verifiers fail closed when Redis is unavailable.\n\n## Related pages\n\n- [Sessions and Revocation](/concepts/sessions-revocation/)\n- [Verify Package](/sdks/verify/)\n- [Protect an MCP Server](/guides/protect-mcp/)"},{"id":"/sdks/oauth","title":"OAuth Package","url":"https://docs.caracal.run/sdks/oauth/","concepts":"","body":"The OAuth packages exchange existing subject authority for resource mandates through the STS `/oauth/2/token` endpoint.\n\n## Install\n\n| Ecosystem  | Package                                                    |\n| ---------- | ---------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/oauth`                             |\n| Python     | `pip install caracalai-oauth`                              |\n| Go         | `go get github.com/garudex-labs/caracal/packages/oauth/go` |\n\n## Exchange inputs\n\n| Option                     | Meaning                                                                                                                       |\n| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |\n| Subject token              | Existing user, service, agent, or application authority.                                                                      |\n| Resource                   | Resource identifier or audience requested from STS.                                                                           |\n| Client secret or assertion | Application authentication for client-secret flows.                                                                           |\n| Actor token                | Optional actor authority for delegated exchanges.                                                                             |\n| Authority record ID        | Sent in the STS form field `session_id`. It is distinct from a governed Session ID.                                           |\n| Session ID                 | Sent in the STS form field `agent_session_id`.                                                                                |\n| Delegation ID              | Sent in the STS form field `delegation_edge_id`.                                                                              |\n| Scopes                     | Requested resource scopes.                                                                                                    |\n| TTL seconds                | Requested mandate lifetime.                                                                                                   |\n\n## TypeScript example\n\n```ts\nimport { OAuthClient, ApprovalRequiredError } from '@caracalai/oauth'\n\nconst oauth = new OAuthClient(stsUrl, zoneId, applicationId)\n\ntry {\n  const token = await oauth.exchange(subjectToken, 'https://api.example.com/tickets', {\n    scopes: ['tickets:read'],\n    clientSecret: process.env.CARACAL_APP_CLIENT_SECRET,\n  })\n  console.log(token.accessToken, token.expiresIn)\n} catch (error) {\n  if (error instanceof ApprovalRequiredError) {\n    console.log('step-up required', error.approvalId)\n  } else {\n    throw error\n  }\n}\n```\n\n## Behavior\n\n- Successful responses are validated for `access_token`, `token_type`, and `expires_in`.\n- Token responses are cached by identity, resource, scopes, TTL, and credential context.\n- Concurrent identical exchanges share in-flight work.\n- Transient HTTP statuses are retried with bounded backoff.\n- STS `interaction_required` responses surface as `ApprovalRequiredError`.\n\n## Subject Federation\n\nWhen the zone registers the application's identity system as a Subject issuer, the client exchanges an end user's identity token for a Caracal Authority record and can post that user's decision on a Subject-reserved approval hold:\n\n- `federateSubject(idToken, opts)` / `federate_subject(id_token, ...)` / `FederateSubject(ctx, idToken, opts)` - creates the Subject's Authority record and returns the Subject mandate response. Never cached: each federation is an explicit identity event, and the record carries no resource authority. The high-level SDK wrappers also return the Authority record ID as `subjectAuthorityRecordId`, `subject_authority_record_id`, or `SubjectAuthorityRecordID`.\n- `decideApproval({...})` / `decide_approval(...)` / `DecideApproval(ctx, input)` - posts the user's decision with the session mandate, echoing the hold's exact binding.\n\nSee [Human Approval](/guides/step-up/#decide-as-the-applications-end-user) for the end-to-end flow.\n\n## Related pages\n\n- [Human Approval](/guides/step-up/)\n- [Mandates](/concepts/mandate/)\n- [Use STS Endpoint](/api/sts/)"},{"id":"/sdks/admin","title":"Admin Package","url":"https://docs.caracal.run/sdks/admin/","concepts":"","body":"The admin package is the automation client for control-plane objects. It mirrors Console management workflows for scripts and services, and ships in three languages: `@caracalai/admin` (TypeScript), `caracalai-admin` (Python), and `github.com/garudex-labs/caracal/packages/admin/go` (Go).\n\n## Install\n\n```bash\nnpm install @caracalai/admin\n```\n\n```bash\npip install caracalai-admin\n```\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/admin/go\n```\n\n## Create a client\n\n```ts\nimport { AdminClient } from '@caracalai/admin'\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  coordinatorUrl: process.env.CARACAL_COORDINATOR_URL,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n  coordinatorToken: process.env.CARACAL_COORDINATOR_TOKEN,\n})\n```\n\n`apiUrl` and `adminToken` are required for API-backed resources. `coordinatorUrl` and `coordinatorToken` are required for Session lifecycle and Delegation methods.\n\nPython:\n\n```python\nfrom caracalai_admin import AdminClient\n\nadmin = AdminClient(\n    api_url=os.environ[\"CARACAL_API_URL\"],\n    admin_token=os.environ[\"CARACAL_ADMIN_TOKEN\"],\n    coordinator_url=os.environ.get(\"CARACAL_COORDINATOR_URL\"),\n    coordinator_token=os.environ.get(\"CARACAL_COORDINATOR_TOKEN\"),\n)\n```\n\nGo:\n\n```go\nimport admin \"github.com/garudex-labs/caracal/packages/admin/go\"\n\nclient := admin.NewAdminClient(admin.AdminClientOptions{\n    APIURL:           os.Getenv(\"CARACAL_API_URL\"),\n    AdminToken:       os.Getenv(\"CARACAL_ADMIN_TOKEN\"),\n    CoordinatorURL:   os.Getenv(\"CARACAL_COORDINATOR_URL\"),\n    CoordinatorToken: os.Getenv(\"CARACAL_COORDINATOR_TOKEN\"),\n})\n```\n\n## API groups\n\n| Group              | Methods                                                                                                                                                                                                                                                       |\n| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `zones`            | `list`, `get`, `dcrStatus`, `create`, `patch`, `delete`                                                                                                                                                                                                       |\n| `applications`     | `list`, `get`, `create`, `patch`, `rotateSecret`, `getClientSecret`, `delete`, `dcr`                                                                                                                                                                        |\n| `resources`        | `list`, `get`, `create`, `patch`, `delete`                                                                                                                                                                                                                    |\n| `providers`        | `list`, `get`, `create`, `patch`, `delete`                                                                                                                                                                                                                    |\n| `policies`         | `list`, `get`, `create`, `validate`, `addVersion`, `delete`                                                                                                                                                                                                   |\n| `policyTemplates`  | `list`, `get`                                                                                                                                                                                                                                                 |\n| `policySets`       | `list`, `get`, `create`, `addVersion`, `listVersions`, `simulate`, `activate`, `activationStatus`, `delete`                                                                                                                                                   |\n| `grants`           | `list`, `get`, `create`, `revoke`                                                                                                                                                                                                                             |\n| `subjectIssuers`   | `list`, `get`, `create`, `patch`, `delete`                                                                                                                                                                                                                    |\n| `providerConnections` | `create`, `authorizeOAuth`, `revoke`                                                                                                                                                                                                                       |\n| `workloads`        | `list`, `get`, `create`, `update`, `rotateSecret`, `getSecret`, `delete`                                                                                                                                                                                     |\n| `authorityRecords` | `list`                                                                                                                                                                                                                                                        |\n| `subjects`         | `revoke` - the kill switch: one call revokes every live Authority record for the Subject, terminates linked Sessions, revokes their Delegations and provider connections, and feeds the revocation stream so in-flight mandates die before `exp`. Idempotent. |\n| `sessions`         | `list`, `get`, `children`, `suspend`, `resume`, `terminate`, `effectiveAuthority`                                                                               "},{"id":"/sdks/backends/redis","title":"Redis Revocation Store","url":"https://docs.caracal.run/sdks/backends/redis/","concepts":"","body":"The Redis backends provide shared revocation state for resource servers. They let multiple service instances reject mandates after sessions, roots, agents, or Delegations are revoked.\n\nThe resource server does not need to share a network with Caracal services. For separate VPC, cluster, or cloud deployments, give the resource workload private TLS connectivity to the managed Redis endpoint that carries Caracal streams, authenticate it with a dedicated least-privilege Redis identity, and run the consumer beside the verifier. Do not expose Redis publicly or distribute Caracal administrative tokens to resource applications.\n\n## Install\n\n| Ecosystem  | Package                                                             |\n| ---------- | ------------------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/revocation-redis`                           |\n| Python     | `pip install caracalai-revocation-redis`                            |\n| Go         | `go get github.com/garudex-labs/caracal/packages/backends/redis/go` |\n\n## Defaults\n\n| Setting           | Default                                         |\n| ----------------- | ----------------------------------------------- |\n| Revocation stream | `caracal.sessions.revoke`                       |\n| Consumer group    | `resource-revocation`                           |\n| Revocation TTL    | 24 hours                                        |\n| Signature support | Optional HMAC verification for stream messages. |\n\n## TypeScript store\n\n```ts\nimport { RedisRevocationStore } from '@caracalai/revocation-redis'\n\nconst revocations = new RedisRevocationStore(redis, {\n  defaultTtlMs: 24 * 60 * 60 * 1000,\n})\n\nawait revocations.markRevoked('sess_123')\nconst blocked = await revocations.isRevoked('sess_123')\n```\n\n## Stream consumer\n\nUse the stream consumer when your resource server should learn revocations from the audit/control plane instead of marking them locally.\n\n```ts\nimport { RedisRevocationConsumer } from '@caracalai/revocation-redis'\n\nconst consumer = new RedisRevocationConsumer(redis, revocations, {\n  consumerName: 'api-1',\n  hmacSecret: process.env.CARACAL_REVOCATION_HMAC_SECRET,\n})\n\nawait consumer.start()\n```\n\nStream events can revoke multiple anchors. Resource servers should treat Redis or signature failures according to their risk posture; high-risk resources should fail closed.\n\nDelegated verification also needs the `caracal.delegations.invalidate` stream. Start `RedisDelegationInvalidationConsumer` beside `RedisRevocationConsumer` so the shared store receives the zone's current graph epoch; without it, signature and revocation-anchor checks still run, but `delegation_stale` cannot detect a mandate issued against an older graph epoch. The Python and Go Redis packages expose the equivalent delegation-invalidation consumer.\n\nEach independently deployed resource instance uses a unique consumer name under deployment-specific groups for both streams. Supply `STREAMS_HMAC_KEY` through that workload's secret manager and require stream signatures in published deployments. Network policy should permit only DNS and the exact private Redis endpoint; the application needs no Postgres, API, Coordinator, or internal service access for revocation.\n\n## Related pages\n\n- [Revocation Package](/sdks/revocation/)\n- [Sessions and Revocation](/concepts/sessions-revocation/)\n- [Verify Package](/sdks/verify/)"},{"id":"/security","title":"Secure Caracal","url":"https://docs.caracal.run/security/","concepts":"","body":"Security in Caracal centers on pre-execution authority: STS issues scoped mandates, Gateway enforces protected-resource access, Coordinator tracks agent/delegation state, Audit preserves evidence, and Operations keeps storage, streams, secrets, and releases recoverable.\n\n## Security Model\n\n## Security Review Path\n\n| Need | Page |\n| --- | --- |\n| Understand assets, boundaries, threats, and mitigations | [Review the Threat Model](/security/threat-model/) |\n| Harden a production deployment | [Harden Security Posture](/security/hardening/) |\n| Verify a release before installing | [Verify a Release](/security/verify-releases/) |\n| Demonstrate assurance to a reviewer or auditor | [Generate an Evidence Pack](/security/evidence-pack/) |\n| Report a vulnerability privately | [Report a Vulnerability](/security/disclosure/) |\n| Operate a security incident | [Respond to Incidents](/operations/incident-response/) |\n| Evaluate the project for enterprise adoption | [Enterprise Security Readiness](/security/enterprise-readiness/) |\n\n## Core Invariants\n\n- STS must fail closed on policy, key, session, revocation, replay, step-up, and signing errors.\n- Gateway must never trust caller-supplied destinations and must deny before upstream dispatch when authority or routing is unsafe.\n- Redis stream messages require HMAC signing in published modes where configured.\n- Audit evidence must remain append-only, tamper-evident, and recoverable through replay/DLQ paths.\n- Secrets must come from secret files or platform secret managers in production.\n- The open-source product must not depend on enterprise-only code or controls.\n\n## Next Step\n\nRead [Review the Threat Model](/security/threat-model/) before hardening a deployment.\n\n## Related Sections\n\n- [Enforce Boundaries](/architecture/trust-boundaries/)\n- [Harden Production](/operations/tls-hardening/)\n- [Rotate Keys and Secrets](/operations/key-management/)\n- [Configure Alerts](/operations/alerts/)\n- [Compare Editions](/enterprise/)"},{"id":"/security/threat-model","title":"Review the Threat Model","url":"https://docs.caracal.run/security/threat-model/","concepts":"","body":"The repository threat model covers the current open-source Caracal runtime, services, packages, deployment assets, and release artifacts.\n\n## Assurance Case\n\nCaracal's security requirements are met under an explicit assurance case maintained in [`governance/THREAT_MODEL.md`](https://github.com/Garudex-Labs/caracal/blob/main/governance/THREAT_MODEL.md). It argues the claim from four pillars:\n\n- **Threat model:** threats T1–T14 each carry an owner, a required mitigation, and a verification step.\n- **Trust boundaries:** every boundary names what is untrusted and where mediation happens (see [Enforce Boundaries](/architecture/trust-boundaries/)).\n- **Secure design principles:** deny-by-default, complete mediation, least privilege, defense in depth, and separation of privilege are enforced in STS, gateway, control, and the data layer.\n- **Implementation weaknesses countered:** input validation, token-confusion prevention, SSRF blocking, secret redaction, audit/stream integrity, and a signed, provenance-attested release path, each backed by tests and scanners.\n\n## In-Scope Areas\n\n| Area                     | Scope                                                                                               |\n| ------------------------ | --------------------------------------------------------------------------------------------------- |\n| API                      | Control-plane routes, admin auth, product state, step-up APIs, admin audit, AI Operator routes.     |\n| Coordinator              | Session lifecycle, Delegation, invocation, TTL, retention, outbox.                                  |\n| STS                      | OAuth token exchange, signing, JWKS, policy decisions, replay, revocation, step-up, audit emission. |\n| Gateway                  | Proxy enforcement, upstream safety, STS exchange, bindings, replay, revocation checks.              |\n| Audit                    | Redis stream consumption, append-only ledger, HMAC chain, tamper sweeps, retention, export.         |\n| Control                  | Optional invoke endpoint, engine catalog enforcement, JTI replay, rate limits, audit.               |\n| Web console and auth BFF | Sign-in, session cookies, origin enforcement, proxied admin calls, derived tokens.                  |\n| Relay and streams        | Coordinator lifecycle relay, Redis Streams signatures, dedupe, pending-entry handling.              |\n| Packages and infra       | SDKs, adapters, backends, Docker, Helm, migrations, installers, release artifacts.                  |\n\nEnterprise-only code and customer deployments outside the provided deployment model are out of scope for this open-source threat model.\n\n## Assets\n\n| Asset                                                                        | Why it matters                                                     |\n| ---------------------------------------------------------------------------- | ------------------------------------------------------------------ |\n| Agent and application authority                                              | Controls what autonomous agents can access and do.                 |\n| Policies, grants, zones, resource bindings                                   | Define authorization and proxy destinations.                       |\n| Signing keys, KEKs, admin tokens, client secrets, database/Redis credentials | Compromise enables impersonation or service takeover.              |\n| Tokens, sessions, JTIs, revocations, step-up state                           | Enforce identity, replay prevention, expiry, and emergency denial. |\n| Audit events and chain state                                                 | Provide evidence for incidents and tamper detection.               |\n| Redis Streams and outboxes                                                   | Carry lifecycle, invalidation, audit, and revocation events.       |\n| Images, installers, releases, lockfiles                                      | Define what users execute.                                         |\n\n## Threats and Mitigations\n\n| Threat                                      | Required mitigation                                                                                                                                                                                |\n| ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Control-plane mutation bypass               | Mandatory auth hooks, schemas, zone guards, and admin audit.                                                                                                                                       |\n| STS over-issues authority                   | Deny-by-default policy, grant/session validation, step-up, replay, revocation, and signing checks.                                                                                                 |\n| Gateway forwards unsafely                   | Per-request STS exchange, upstream bindings, SSRF guard, header stripping, timeouts, replay and revocation checks.                                                                                 |\n| Agent/delegation state races                | Transactions, locks, outbox events, dedupe, bounded relay retry.                                                                                                                                   |\n| Secrets leak                                | File-secret resolution, redaction, no plaintext key material in responses/logs/audit/examples.                                                                                                     |\n| Agent reads operator secrets                | Operator secrets outside source workspaces, private operator directories, no secret mounts in agent containers, workload-only token injection, and separate users/containers for "},{"id":"/security/hardening","title":"Harden Security Posture","url":"https://docs.caracal.run/security/hardening/","concepts":"","body":"Use this checklist before exposing Caracal to production workloads or protected upstreams.\n\n## Deployment Mode\n\n| Check      | Expected                                                                                      |\n| ---------- | --------------------------------------------------------------------------------------------- |\n| Mode       | `CARACAL_MODE=stable` or `rc`; never rely on `dev` defaults for production.                   |\n| Ports      | Public ingress exposes only required API/Gateway endpoints; storage remains private.          |\n| Network    | NetworkPolicy or equivalent restricts ingress and egress.                                     |\n| Containers | Non-root, dropped capabilities, no-new-privileges, read-only root filesystem where supported. |\n\n## Secrets and Keys\n\n| Check                    | Expected                                                                                                                                 |\n| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |\n| Secret delivery          | Mounted secret files or platform secret manager, not inline production secrets.                                                          |\n| HMAC keys                | `AUDIT_HMAC_KEY`, `STREAMS_HMAC_KEY`, `IDEMPOTENCY_HMAC_KEY`, and `GATEWAY_STS_HMAC_KEY` are strong and rotated under control.           |\n| Idempotency receipts     | Plaintext keys never enter PostgreSQL or metric labels; changed-payload key reuse fails with `409`; retention is explicit and monitored. |\n| Secret Store KEK         | `SECRET_STORE_KEK` is strong, held outside the database, and backed up alongside it.                                                     |\n| Admin/Coordinator tokens | Stored privately, rotated, and scoped to operator need.                                                                                  |\n| Runtime secrets          | Client-secret files and SDK `caracal.toml` profiles are owner-only when written locally.                                                 |\n\n## Access Enforcement\n\n| Check            | Expected                                                                                                                                                                                                       |\n| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| STS              | Fails closed on invalid client credentials, policy denial, revoked sessions, replay, invalid delegation, and undecided step-up holds.                                                                          |\n| Gateway          | Requires bearer token and `X-Caracal-Resource`, validates binding, rejects path traversal and unsafe upstreams. Pin egress hosts with `UPSTREAM_HOST_ALLOWLIST=api.internal.example,billing.internal.example`. |\n| Control          | Disabled unless explicitly needed; invoke endpoint requires gate, JWT, replay protection, rate limit, and audit.                                                                                               |\n| Resource servers | Verify mandate signature, issuer, audience, scopes, token use, Session and Delegation requirements, hop limits, and revocation.                                                                                |\n\n## Audit and Recovery\n\n| Check         | Expected                                                         |\n| ------------- | ---------------------------------------------------------------- |\n| Audit stream  | `caracal.audit.events` and DLQ are monitored.                    |\n| Tamper checks | Audit tamper alerts page the security/on-call path.              |\n| Backups       | Postgres, runtime secrets, and audit exports are restorable.     |\n| Replay        | STS/Gateway audit replay volumes are preserved through rollouts. |\n\n## Next Step\n\nUse [Report a Vulnerability](/security/disclosure/) if hardening review uncovers a suspected vulnerability.\n\n## Related Pages\n\n- [Harden Production](/operations/tls-hardening/)\n- [Rotate Keys and Secrets](/operations/key-management/)\n- [Back Up and Retain Data](/operations/backup-retention/)"},{"id":"/security/disclosure","title":"Report a Vulnerability","url":"https://docs.caracal.run/security/disclosure/","concepts":"","body":"Report suspected vulnerabilities privately. Do not open public issues for credential exposure, policy bypass, unsafe execution, exploitable operational failures, or other security defects.\n\n## Reporting Channels\n\n| Channel | Use for |\n| --- | --- |\n| GitHub private advisory | Open-source Caracal vulnerabilities: `https://github.com/Garudex-Labs/caracal/security/advisories/new` |\n| Email | Sensitive reports, attachments, patches, exploit demonstrations, or enterprise-related reports: `support@caracal.run` |\n\nEnterprise-related vulnerabilities must be reported by email, not GitHub advisories.\n\n## Email Format\n\n```text\nSubject: [SECURITY][caracal] Short description\n\n1. Summary\n2. Steps to reproduce\n3. Impact\n4. Affected area\n5. Suggested fix\n6. Attachments\n```\n\nKeep reports clear, reproducible, and private. Do not include secrets in public channels or public pull requests.\n\n## Response and Disclosure\n\nThe maintainers aim to review and respond within up to 7 days. Resolution timing depends on complexity and validation needs. Public disclosure should wait until a fix or mitigation is available or maintainers decide not to address the issue.\n\n## Related Pages\n\n- [Security Policy in the repository](https://github.com/Garudex-Labs/caracal/blob/main/.github/SECURITY.md)\n- [Respond to Incidents](/operations/incident-response/)\n- [Review the Threat Model](/security/threat-model/)"},{"id":"/examples","title":"Use Examples","url":"https://docs.caracal.run/examples/","concepts":"","body":"Examples show Caracal integrated into concrete applications and automation scripts. Use them after the first tutorials when you want runnable code that matches a specific integration job. All examples live in the dedicated [Caracal examples repository](https://github.com/Garudex-Labs/examples); clone it once and every guide below runs from that checkout:\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\n```\n\n## Choose an example\n\n| Goal                                                                                        | Start here                                                | Code path                                                                                 |\n| ------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------- |\n| Need a local protected target for the first Gateway call.                                   | [Run Echo Upstream](/examples/echo-upstream/)             | [echoUpstream](https://github.com/Garudex-Labs/examples/tree/main/echoUpstream)           |\n| Want to automate zone setup from a script or pipeline through the Control API.              | [Bootstrap Control State](/examples/control-bootstrap/)   | [controlBootstrap](https://github.com/Garudex-Labs/examples/tree/main/controlBootstrap)   |\n| Need to prove a provider-backed resource is ready.                                          | [Check Provider Readiness](/examples/provider-preflight/) | [providerPreflight](https://github.com/Garudex-Labs/examples/tree/main/providerPreflight) |\n| Need to turn a denial into a safe policy fix.                                               | [Iterate Policy Safely](/examples/policy-iterate/)        | [policyIterate](https://github.com/Garudex-Labs/examples/tree/main/policyIterate)         |\n| Want to launch a plain CLI agent with injected provider credentials.                        | [Launch Research Agent](/examples/research-agent/)        | [ResearchAgent](https://github.com/Garudex-Labs/examples/tree/main/ResearchAgent)         |\n| Want a full app reference lab with agents, providers, Gateway, STS, and Console inspection. | [Run Lynx Capital](/examples/lynx-capital/)               | [lynxCapital](https://github.com/Garudex-Labs/examples/tree/main/lynxCapital)             |\n\n## Recommended order\n\n1. Start with [Run Echo Upstream](/examples/echo-upstream/) if you have not completed a Gateway-mediated request yet.\n2. Use [Bootstrap Control State](/examples/control-bootstrap/) when automation should own zone setup instead of manual Console clicks.\n3. Run [Check Provider Readiness](/examples/provider-preflight/) before the first real provider-backed Gateway call.\n4. Use [Iterate Policy Safely](/examples/policy-iterate/) when an audit denial needs to become a tested policy change.\n5. Try [Launch Research Agent](/examples/research-agent/) to see `caracal run` inject provider-native credentials into an existing-style CLI process.\n6. Study [Run Lynx Capital](/examples/lynx-capital/) when you need a full app topology and live Console inspection path.\n\n## Use examples safely\n\n- Start Caracal through the released runtime and Console path described by the example.\n- Use Console for zones, applications, providers, resources, policies, control keys, and launch bindings.\n- Keep example fixtures inside their own example directory in the examples repository.\n- Run each example's offline tests before adapting it.\n- Do not commit provider secrets, admin tokens, or real third-party credentials.\n\n## Related sections\n\n- [Get Started](/get-started/)\n- [Tutorials](/tutorials/)\n- [Guides](/guides/)\n- [SDKs](/sdks/)\n- [Runtime and Console](/runtime-console/)"},{"id":"/examples/echo-upstream","title":"Run Echo Upstream","url":"https://docs.caracal.run/examples/echo-upstream/","concepts":"","body":"Echo Upstream is a zero-dependency HTTP service in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `echoUpstream/`. It stands in for the API you would put behind Caracal, so you can verify a Gateway-mediated request end to end without hosting your own upstream. Every response states whether the call was brokered by the Gateway or hit the service directly, and shows the evidence behind that verdict.\n\n## What it demonstrates\n\n| Area | Behavior |\n| --- | --- |\n| Brokered-call proof | Reports `viaGateway` plus the request ID, trace context, and forwarding metadata the Gateway stamped on the request. |\n| Credential handling | Confirms the Gateway injected the brokered credential and redacts credential values from the echoed headers. |\n| Gateway reachability | Joins the `caracalData` Docker network so Gateway can reach `http://echoUpstream:8088`. |\n| Local debugging | Exposes `http://127.0.0.1:8088` on the host and logs one line per request, marked `[gateway]` or `[direct]`. |\n\n## Run with Docker\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/echoUpstream\ndocker compose -f compose.yml up --build\n```\n\nCheck the local health endpoint:\n\n```bash\ncurl http://127.0.0.1:8088/healthz\n```\n\n## Run with Node\n\n```bash\ncd caracal-examples/echoUpstream\nnpm start\n```\n\nThe server listens on port `8088`. Set `ECHO_PORT` when you need a different local port.\n\n## Use as the protected upstream\n\nIn web console guided setup or through the Control API, create a resource with this upstream URL:\n\n```text\nhttp://echoUpstream:8088\n```\n\nThen send a request to the Gateway with the Caracal access token and resource ID from guided setup. The command has this shape:\n\n```bash\ncurl http://localhost:8081/v1/hello \\\n  -H \"Authorization: Bearer $CARACAL_RESOURCE_PIPERNET_TOKEN\" \\\n  -H \"X-Caracal-Resource: resource://pipernet\"\n```\n\n## Read the response\n\nA brokered call returns `\"viaGateway\": true` with a `gateway` section:\n\n| Field | What it proves |\n| --- | --- |\n| `viaGateway` | The request carried the Gateway's request ID and forwarding metadata. |\n| `gateway.requestId` | Audit handle - filter web console **Audit** by it to trace the policy decision. |\n| `gateway.credentialInjected` | The Gateway brokered a credential the client never held. |\n| `request.headers` | The headers the upstream actually received, with credentials redacted. |\n\nCalling `http://127.0.0.1:8088/` directly returns `\"viaGateway\": false`, which makes the difference between a protected and an unprotected path visible.\n\n## Test\n\n```bash\ncd caracal-examples/echoUpstream\nnpm test\n```\n\n## Next step\n\nContinue to [Bootstrap Control State](/examples/control-bootstrap/) when you want repeatable setup for demo resources and policies."},{"id":"/examples/control-bootstrap","title":"Bootstrap Control State","url":"https://docs.caracal.run/examples/control-bootstrap/","concepts":"","body":"Control Bootstrap is the canonical Control API automation example in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `controlBootstrap/`. A small CI/CD-style pipeline keeps one agent's environment - application, provider, resource, and policy - matching a declared plan, without adding product-management verbs to the runtime CLI.\n\n## What it demonstrates\n\n| Area               | Behavior                                                                                                    |\n| ------------------ | ----------------------------------------------------------------------------------------------------------- |\n| Automation surface | Calls `/v1/control/invoke` instead of using a root admin token.                                             |\n| Identity model     | Uses scoped, short-lived control keys created in the web console, one scope tier per pipeline stage.        |\n| Reconciliation     | `apply` creates missing objects, patches drifted ones, and publishes a new policy version on content drift. |\n| CI gating          | `verify` is a read-only drift check that exits non-zero when the zone does not match the plan.              |\n| Safety model       | Uses replay protection, rate limits, scoped control permissions, and audit.                                 |\n\nThe plan describes the PiperNet reporter agent's environment: the agent application, the `provider://pipernet-mandate` provider, the `resource://pipernet` resource wired to it, and the baseline policy that allows `read`.\n\n## Web Console Setup\n\n1. Start the runtime and open the web console:\n\n   ```bash\n   caracal up\n   caracal status --ready\n   caracal web\n   ```\n\n2. Create or select the target zone.\n3. Create a control key with only the scopes the stage needs: read/write on app, provider, resource, and policy for `apply`; read for `verify`; read/delete for `teardown`.\n4. Save the `client_id` and `client_secret`. STS resolves the zone from the bound control key; the secret stays retrievable from the Applications page through an audited reveal.\n\n## Run the pipeline\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/controlBootstrap\ncp env.example .env\n$EDITOR .env\n. .env\nnpm run apply\nnpm run verify\n```\n\n`apply` is idempotent: rerunning it against an in-sync zone changes nothing, and rerunning it against a drifted zone converges the drift. Run teardown when you want to remove the environment:\n\n```bash\nnpm run teardown\n```\n\n## Files to study\n\n| File                | Purpose                                                                             |\n| ------------------- | ----------------------------------------------------------------------------------- |\n| `controlClient.mjs` | Exchanges client credentials at STS and calls the Control API.                      |\n| `plan.mjs`          | Declares the desired environment, drift checks, scope tiers, and env-driven config. |\n| `apply.mjs`         | Reconciles the live zone with the plan.                                             |\n| `verify.mjs`        | Read-only drift gate for CI.                                                        |\n| `teardown.mjs`      | Removes the environment in reverse dependency order.                                |\n\n## Test\n\n```bash\ncd caracal-examples/controlBootstrap\nnpm test\n```\n\nThe tests use a fake zone and mock transport and do not call a live Caracal stack.\n\n## Next step\n\nContinue to [Check Provider Readiness](/examples/provider-preflight/) before sending traffic through a provider-backed Gateway resource."},{"id":"/examples/provider-preflight","title":"Check Provider Readiness","url":"https://docs.caracal.run/examples/provider-preflight/","concepts":"","body":"Provider Preflight is an automatable readiness check in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `providerPreflight/`. Use it after creating a provider-backed resource, before cutting traffic over, and from CI before each deploy. It walks the same chain a real Gateway request depends on - control plane, Gateway dependencies, application identity, provider binding and configuration, network paths, and the active policy decision - and reports exactly which link is broken and how to fix it.\n\n## What it checks\n\nChecks run in five phases:\n\n| Phase         | Check                  | What it confirms                                                                                                       |\n| ------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------- |\n| readiness     | Admin API readiness    | The control plane responds ready on `GET /ready`.                                                                      |\n| readiness     | Gateway readiness      | The Gateway responds ready, which verifies its bindings, Postgres, Redis, revocation freshness, audit replay, and STS. |\n| dependencies  | Resource binding       | The resource resolves to exactly one credential provider and a Gateway application.                                    |\n| dependencies  | Application            | The application exists, is not expired, and matches the resource's routing binding.                                    |\n| configuration | Provider configuration | Kind-required fields are present and `allowed_token_hosts` covers the token endpoint.                                  |\n| configuration | Scope coverage         | Every requested scope is declared on the resource.                                                                     |\n| configuration | Runtime injection      | Runtime-injection eligibility is enabled when required.                                                                |\n| connectivity  | Token endpoint host    | OAuth token endpoints are HTTPS and publicly routable, or explicitly granted as private egress.                        |\n| connectivity  | Callback reachability  | Authorization-code callback origins are HTTPS and reachable.                                                           |\n| connectivity  | Upstream reachability  | The protected upstream is reachable, with a warning when it resolves to a private address.                             |\n| authorization | Policy authorization   | Simulating the active policy set with the input shape STS uses for real token exchanges returns `allow`.               |\n\nThe preflight fails closed. Any failed check exits non-zero, and every failure or warning prints a `fix:` line with the concrete remediation.\n\n## Run the preflight\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/providerPreflight\nCARACAL_API_URL=http://127.0.0.1:3000 \\\nCARACAL_ADMIN_TOKEN=<admin-token> \\\nPREFLIGHT_ZONE_ID=<zone-id> \\\nPREFLIGHT_RESOURCE_ID=<resource-id> \\\nPREFLIGHT_APPLICATION_ID=<app-id> \\\nPREFLIGHT_SCOPES=pipernet:read,pipernet:write \\\nnpm run preflight\n```\n\nOptional settings:\n\n| Variable                                   | Purpose                                                                                                       |\n| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |\n| `PREFLIGHT_GATEWAY_URL`                    | Probes the Gateway's `/ready` endpoint; without it the Gateway phase reports a warning instead of validating. |\n| `PREFLIGHT_REQUIRE_RUNTIME_INJECTION=true` | Requires providers to allow runtime injection.                                                                |\n| `PREFLIGHT_OUTPUT=json`                    | Emits the full report as JSON for CI pipelines and dashboards.                                                |\n\nExit codes: `0` when all checks pass, `1` when any check fails, `2` when the preflight itself cannot run.\n\n## Network position\n\n`Upstream reachability` and `Callback reachability` run from the host executing the script. Run it from a network position comparable to Gateway, or inside the cluster, when you need the reachability result to match production routing.\n\n## Test\n\n```bash\ncd caracal-examples/providerPreflight\nnpm test\n```\n\nThe tests inject network, DNS, and policy responses and do not call live systems.\n\n## Next step\n\nContinue to [Iterate Policy Safely](/examples/policy-iterate/) when a denied request needs to become a tested policy change."},{"id":"/examples/policy-iterate","title":"Iterate Policy Safely","url":"https://docs.caracal.run/examples/policy-iterate/","concepts":"","body":"Policy Iterate is an audit-driven policy rollout loop in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `policyIterate/`. Use it when a real request is denied and you want to test a candidate policy change against the exact redaction-safe audit input - and prove it does not change other decisions - before activation.\n\n## Loop\n\n1. **Diagnose** - a request is denied and you capture its audit `request_id`. The audit explain endpoint reconstructs the denied `policy_input` along with the diagnostics and determining policies.\n2. **Simulate** - you edit the policy, stage a candidate policy-set version, and the example replays the denied input against it through the same OPA engine that serves live traffic.\n3. **Regress** - the example replays your expected-decision cases against the same candidate, so loosening a policy for one caller cannot silently change decisions for everyone else.\n4. **Decide** - activation is gated on evidence: the candidate allows the denied input, the rollout contract validates, simulation produced no warnings, and every regression case keeps its expected decision.\n5. **Activate** - with `ACTIVATE=true` and a clean verdict, the example activates the version and polls activation status until the STS runtime reports it loaded.\n\n## Run the iteration\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/policyIterate\nCARACAL_API_URL=http://127.0.0.1:3000 \\\nCARACAL_ADMIN_TOKEN=<admin-token> \\\nCARACAL_ZONE_ID=<zone-id> \\\nDENIED_REQUEST_ID=<denied-request-id> \\\nPOLICY_SET_ID=<policy-set-id> \\\nCANDIDATE_VERSION_ID=<staged-version-id> \\\nREGRESSION_FILE=./regressions.json \\\nnpm run iterate\n```\n\nThe run is a dry run by default: it narrates each phase on stderr, prints a JSON report on stdout, and exits `0` only when the verdict is clean. Re-run with `ACTIVATE=true` to roll out the version and wait for propagation.\n\n`REGRESSION_FILE` is optional and points to a JSON array of `{ name, expect, input }` cases - see `regressions.example.json` in the example directory for the format.\n\n## Claims note\n\nActor and subject claims are not written to audit, so reconstructed input does not contain them. For a claim-dependent denial, add the relevant `context.actor_claims` to a regression case built from the printed `policyInput` and iterate with that case instead.\n\n## Test\n\n```bash\ncd caracal-examples/policyIterate\nnpm test\n```\n\nThe tests inject the Admin API transport and do not call a live Caracal stack.\n\n## Next step\n\nContinue to [Launch Research Agent](/examples/research-agent/) to see runtime credential injection for a plain CLI agent."},{"id":"/examples/research-agent","title":"Launch Research Agent","url":"https://docs.caracal.run/examples/research-agent/","concepts":"","body":"Research Agent is a `caracal run` example in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `ResearchAgent/`. It launches a normal Node.js CLI agent and injects provider-native credentials only into the child process after Caracal authorizes the run.\n\n## What it demonstrates\n\n| Resource | Injected env | Used for |\n| --- | --- | --- |\n| `resource://google-drive` | `GOOGLE_DRIVE_ACCESS_TOKEN` | Searching and exporting Google Drive documents. |\n| `resource://google-calendar` | `GOOGLE_CALENDAR_ACCESS_TOKEN` | Reading relevant Calendar events. |\n| `resource://openai` | `OPENAI_API_KEY` | Answering the terminal question with model context. |\n\nThe agent has no Caracal SDK dependency. It reads provider-native environment variables and behaves like an existing third-party terminal tool.\n\n## Console setup\n\nUse Console to create or select:\n\n| Object | Purpose |\n| --- | --- |\n| Zone | Owns the workload, providers, resources, and policies. |\n| Workload | Identity used by `caracal run` to call STS; created on the **Launcher** page. |\n| Google Drive provider | Returns a Drive read token. |\n| Google Calendar provider | Returns a Calendar read token. |\n| OpenAI provider | Returns an OpenAI-compatible credential. |\n| Resources | Map `resource://google-drive`, `resource://google-calendar`, and `resource://openai` to providers. |\n| Policy | Allows the workload to request all three resources. |\n\nEnable runtime injection on each provider.\n\nThen open the console's **Launcher** page, create a workload named `research agent`, and add three launch bindings:\n\n| Environment variable | Resource |\n| --- | --- |\n| `GOOGLE_DRIVE_ACCESS_TOKEN` | `resource://google-drive` |\n| `GOOGLE_CALENDAR_ACCESS_TOKEN` | `resource://google-calendar` |\n| `OPENAI_API_KEY` | `resource://openai` |\n\nSelect the read-only scopes on the Drive and Calendar bindings so each injected credential carries only what the agent needs. The Launcher page then shows the exact launch commands.\n\n## Store the workload secret\n\nThe launcher needs one local file: the workload's secret at the runtime secret path. Copy it from the Launcher page - it stays retrievable there, with every reveal audited.\n\n```bash\nexport CARACAL_WORKLOAD_ID=\"<workload ID from the Launcher page>\"\nmkdir -p ~/.config/caracal/runtime/$CARACAL_WORKLOAD_ID\nprintf '%s' '<workload secret>' > ~/.config/caracal/runtime/$CARACAL_WORKLOAD_ID/secret\nchmod 600 ~/.config/caracal/runtime/$CARACAL_WORKLOAD_ID/secret\n```\n\nDo not export `GOOGLE_DRIVE_ACCESS_TOKEN`, `GOOGLE_CALENDAR_ACCESS_TOKEN`, or `OPENAI_API_KEY` yourself. Caracal injects them into the child process after STS authorization.\n\n## Launch the agent\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/ResearchAgent\nexport CARACAL_WORKLOAD_ID=\"<workload ID from the Launcher page>\"\ncaracal run -- node agent.mjs\n```\n\nThe agent confirms the injected credentials (values masked) and opens an interactive prompt:\n\n```text\n[agent] credential preflight (values masked, injected by launcher):\n[agent]   GOOGLE_DRIVE_ACCESS_TOKEN  present  -> Google Drive (read-only scope)\n[agent]   GOOGLE_CALENDAR_ACCESS_TOKEN  present  -> Google Calendar (read-only scope)\n[agent]   OPENAI_API_KEY  present  -> OpenAI\nCaracal run research agent ready. Ask about Drive docs or Calendar events. Type \"exit\" to quit.\n>\n```\n\nStarted directly with `node agent.mjs`, the preflight fails with exit code 2 before any network call. Credentials disappear with the child-process environment when the process exits.\n\n## Test\n\n```bash\ncd caracal-examples/ResearchAgent\npnpm test\n```\n\nThe tests do not contact Google, OpenAI, or Caracal.\n\n## Next step\n\nContinue to [Run Lynx Capital](/examples/lynx-capital/) when you want a full app reference lab with live Console inspection."},{"id":"/examples/lynx-capital","title":"Run Lynx Capital","url":"https://docs.caracal.run/examples/lynx-capital/","concepts":"","body":"Lynx Capital is a runnable reference in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `lynxCapital/`. It models a\nfinance-operations platform: an LLM swarm of orchestrators, regional workflows, and\nthousands of ephemeral domain workers executes payout cycles across twenty partner\nproviders, with every agent and every provider call governed by Caracal. It is the primary\nreference for modelling permission boundaries, Sessions for agents, providers, resources, and\npolicies on Caracal.\n\n## Architecture\n\n| Building block | Role                                                                                                                                                                                                                             |\n| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Applications   | `lynx-operations`, `lynx-intake`, `lynx-ledger`, `lynx-compliance`, `lynx-treasury`, `lynx-payments`, `lynx-audit` - one **managed application** per permission boundary, each holding only its own partner authority.           |\n| Agents         | Every agent execution - orchestrator or worker - is its own **Session** under its role's application, labeled `[role, lynx-swarm]` with run and agent metadata, narrowed by a Delegation to its role's scopes and views.         |\n| Providers      | Twenty partner **credential providers** (`provider://<slug>`), each registered in the exact config shape its kind supports: API key, bearer token, OAuth client credentials, OAuth authorization code, Caracal mandate, or none. |\n| Resources      | Per-application **resource views** (`resource://<app>-<provider>`). The Gateway binds each view to exactly one application, so shared partners expose one view per boundary, each carrying only that boundary's scopes.          |\n| Policy set     | `lynx-finance-ops`: default-deny base, generated bindings and grants data, and one shared decisions policy allowing exactly each application's role-granted mandate mints and gateway calls.                                     |\n\nThe model is declared once in `config/tenancy.yaml`; the SDK seam, agent runner,\nprovisioning, and policy all read from it.\n\n### Why one application per permission boundary\n\nA Caracal application is a credential and trust boundary, and the Gateway binds each\nresource to exactly one application. Splitting the swarm by permission boundary means a\npayments worker and an audit worker can both reach the same partner - through different\nviews, with different scopes - while a compromised intake agent can never present payment\nauthority. Agent executions are Sessions, not applications: each started Session gets its own identity, labels,\nDelegation, and audit trail without minting new application credentials.\n\n### Sessions for agents\n\nThe swarm's runner gives every agent its own session via the SDK's `session()`:\norchestrators inherit under the operations boundary; each domain worker starts under its\napplication's per-run dispatcher root with `Authority.narrow(role scopes, views, max_hops=1,\nrun TTL)`. Ad-hoc partner-integration workers resolve their boundary, scope, and view\ndynamically from the requested provider operation. Logs and policy decisions identify\nexactly which agent did what.\n\n### Customer attribution and confinement\n\nWorkers acting on one customer's records - invoicing, dunning, payment application - start Sessions\nwith a `customer:<id>` label and a `customer_id` metadata key. The metadata key makes\nper-customer audit a direct filter over the shared zone trail; the label is policy input,\nand the base policy confines customer-labeled agents to the customer-record scopes, so a\nworker dunning one customer can never mint treasury or payment-rail authority. This is the\n[Serve Your Own Customers](/guides/serve-customers/) pattern applied to app-only work: one\nzone, customer separation carried by sessions, labels, metadata, and policy.\n\n## Setup flow\n\n## Commands\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/lynxCapital\npython -m venv .venv\nsource .venv/bin/activate\npip install -e \".[dev]\"\ncp -n .env.example .env\n```\n\nThe workload `.env` carries the zone and one `LYNX_CARACAL_<APP>_APPLICATION_ID` /\n`_CLIENT_SECRET` pair per boundary. Provisioning uses a separate operator file and a\nscoped Control key created once in Console.\n\n```bash\ncp -n .env.provision.example .env.provision   # set CONTROL_CLIENT_ID / _SECRET\n. .env.provision\npython scripts/provision.py     # applications, providers, views, policy set (idempotent)\npython scripts/reference.py     # SDK walkthrough: labeled Sessions, narrowed authority, mandates\npython scripts/teardown.py      # remove the provisioned objects\n```\n\n`provision.py` prints the per-application credential exports as it creates each\napplication; each client secret is returned exactly once. It also renders the\napplication-id bindings into the policy library before authoring it, so policy decisions\nkey on the real control-plane UUIDs.\n\n## Policies\n\n`policies/` is an importable, OPA-tested library. The base policy default-denies and owns\nthe decision contract; generated data documents carry the application bindings and the\nresource-view grants; one shared decisions policy allows mandate mints (scope ∩\nDelegation, role label granted, view owned by the caller) and gateway uses (mandate\ntarget includes the view), naming the deciding application boundary in every decision.\nExpected access behavior is documented in\n`policies/README.md`.\n\n```bash\nopa test policies/ -v\n```\n\n## SDK integration\n\nApplication code uses two seams: `app/caracal.py` (per-application runtimes, worker\nauthority, mandate minting, gateway calls) and `app/agents/runner.py` (per-Session\nlifecycle):\n\n```python\nhandle = await r"},{"id":"/api","title":"Use API Reference","url":"https://docs.caracal.run/api/","concepts":"","body":"Use this section when you need wire-level behavior. For task workflows, start in [Guides](/guides/). For service ownership and operations, use [Understand Services](/services/) and [Operations](/operations/).\n\n## API Surfaces\n\n| Surface                                  | Base                | Purpose                                                                                                            |\n| ---------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------ |\n| [Use the Admin API](/api/control-plane/) | API service `/v1`   | Zones, applications, providers, resources, policies, policy sets, grants, Authority records, Sessions, audit, approvals, and templates. |\n| [Use Coordinator API](/api/coordinator/) | Coordinator service | Sessions, long-lived Session services, invocations, Delegations, and SDK lifecycle endpoints.                                   |\n| [Use STS Endpoint](/api/sts/)            | STS service         | OAuth token exchange, JWKS, step-up status, and internal policy operations.                                        |\n| [Proxy Through Gateway](/api/gateway/)   | Gateway service     | Protected reverse-proxy behavior rather than CRUD endpoints.                                                       |\n| [Use Event Topics](/api/event-topics/)   | Redis Streams       | Audit, invalidation, revocation, agent, invocation, and delegation topics.                                         |\n\n## Service Ports\n\n| Service     | Local port              |\n| ----------- | ----------------------- |\n| API         | `3000`                  |\n| Coordinator | `4000`                  |\n| STS         | `8080`                  |\n| Gateway     | `8081`                  |\n| Audit       | `9090`                  |\n| Control     | API `3000` when enabled |\n\n## Error Shape\n\nCaracal service errors use the shared OAuth-compatible shape:\n\n```json\n{\n  \"error\": \"access_denied\",\n  \"error_description\": \"policy denied request\",\n  \"requestId\": \"018f...\"\n}\n```\n\nSee [Error Codes](/reference/errors/) for canonical codes.\n\n## Reading Path\n\n| Need                                      | Page                                     |\n| ----------------------------------------- | ---------------------------------------- |\n| Manage product objects over HTTP          | [Use the Admin API](/api/control-plane/) |\n| Manage Session and Delegation runtime state | [Use Coordinator API](/api/coordinator/) |\n| Exchange authority for mandates           | [Use STS Endpoint](/api/sts/)            |\n| Understand Gateway proxy requirements     | [Proxy Through Gateway](/api/gateway/)   |\n| Consume or verify stream contracts        | [Use Event Topics](/api/event-topics/)   |\n\n## Next Step\n\nStart with [Use the Admin API](/api/control-plane/) when automating product setup."},{"id":"/api/control-plane","title":"Use the Admin API","url":"https://docs.caracal.run/api/control-plane/","concepts":"","body":"The Admin API is served by the API service on port `3000`. Management routes are registered under `/v1` and are protected by admin authentication.\n\n## Authentication\n\nSend an admin token as a bearer credential on every `/v1` request:\n\n```\nAuthorization: Bearer <admin token>\n```\n\n| Status | Error                                         | Meaning                                           |\n| ------ | --------------------------------------------- | ------------------------------------------------- |\n| `401`  | `unauthorized`, `invalid_admin_token`         | Token is missing, malformed, expired, or revoked. |\n| `403`  | `admin_token_read_only`                       | Write attempted with a read-only token.           |\n| `403`  | `admin_token_zone_mismatch`, `zone_forbidden` | Token is scoped to a different zone.              |\n| `403`  | `system_zone_read_only`                       | Write attempted against the reserved system zone. |\n\n## Error Shape\n\nEvery non-2xx `/v1` response carries a single JSON envelope:\n\n```json\n{\n  \"error\": \"invalid_body\",\n  \"error_description\": \"Request body failed validation\",\n  \"details\": { \"issues\": [{ \"path\": [\"name\"], \"message\": \"Required\" }] }\n}\n```\n\n`error` is the stable machine code to branch on. `error_description`, when present, is a human-readable summary of the failure. `details`, when present, carries structured context: for example, the validation issues on `invalid_body`, the failed connectivity check on `provider_check_failed`, or the live dynamic-client count on `dcr_shutdown_required`. See [Error Codes](/reference/errors/) for the shared code catalog.\n\n## Health and Diagnostics\n\n| Method | Path       | Purpose                           |\n| ------ | ---------- | --------------------------------- |\n| `GET`  | `/health`  | Liveness check.                   |\n| `GET`  | `/ready`   | Dependency and service readiness. |\n| `GET`  | `/metrics` | Service metrics.                  |\n| `GET`  | `/docs`    | Optional API docs when enabled.   |\n\n## Core Resources\n\n| Resource     | Collection                                   | Item                                                        | Extra                                                                                                                                                      |\n| ------------ | -------------------------------------------- | ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Zones        | `GET`, `POST /v1/zones`                      | `GET`, `PATCH`, `DELETE /v1/zones/:id`                      | `GET /v1/zones/:id/dcr-status`, `GET /v1/zones/:id/overview`                                                                                               |\n| Applications | `GET`, `POST /v1/zones/:zoneId/applications` | `GET`, `PATCH`, `DELETE /v1/zones/:zoneId/applications/:id` | `POST /v1/zones/:zoneId/applications/dcr`, `POST /v1/zones/:zoneId/applications/:id/rotate-secret`, `GET /v1/zones/:zoneId/applications/:id/client-secret` |\n| Providers    | `GET`, `POST /v1/zones/:zoneId/providers`    | `GET`, `PATCH`, `DELETE /v1/zones/:zoneId/providers/:id`    | -                                                                                                                                                          |\n| Resources    | `GET`, `POST /v1/zones/:zoneId/resources`    | `GET`, `PATCH`, `DELETE /v1/zones/:zoneId/resources/:id`    | -                                                                                                                                                          |\n| Workloads    | `GET`, `POST /v1/zones/:zoneId/workloads`    | `GET`, `PUT`, `DELETE /v1/zones/:zoneId/workloads/:id`      | `POST /v1/zones/:zoneId/workloads/:id/rotate-secret`, `GET /v1/zones/:zoneId/workloads/:id/secret`                                                         |\n\nWorkloads are the launcher identities `caracal run` uses. Create a workload, deliver its secret to the launch environment, and rotate the secret here when you automate runtime setup; the secret stays retrievable from Secret Store custody through an audited reveal endpoint.\n\n## Policy and Access\n\n| Area                 | Read                                                                                                                                                                                                 | Write                                                                                                                                                                                                                                     |\n| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Policy validation    | -                                                                                                                                                                                                    | `POST /v1/policies/validate`                                                                                                                                                                                                              |\n| Policies             | `GET /v1/zones/:zoneId/policies`, `GET /v1/zones/:zoneId/policies/:id`                                                                                                                               | `POST /v1/zones/:zoneId/policies`, `POST /v1/zones/:zoneId/policies/:id/versions`, `DELET"},{"id":"/api/coordinator","title":"Use Coordinator API","url":"https://docs.caracal.run/api/coordinator/","concepts":"","body":"Coordinator is served on port `4000`. It owns governed Session and Delegation graph state. This page documents the raw protocol, whose stable paths and fields retain `agents`, `agent_session_id`, and `delegation_edge_id` names.\n\n## Health and Metrics\n\n| Method | Path       | Purpose                       |\n| ------ | ---------- | ----------------------------- |\n| `GET`  | `/health`  | Liveness check.               |\n| `GET`  | `/ready`   | Dependency and job readiness. |\n| `GET`  | `/metrics` | Service metrics.              |\n\n## Session Management\n\n| Method   | Path                                 | Purpose                                          |\n| -------- | ------------------------------------ | ------------------------------------------------ |\n| `POST`   | `/zones/:zoneId/agents`              | Start a Session. `/agents` is the protocol path. |\n| `GET`    | `/zones/:zoneId/agents`              | List Sessions.                                   |\n| `GET`    | `/zones/:zoneId/agents/:id`          | Inspect one Session.                             |\n| `GET`    | `/zones/:zoneId/agents/:id/children` | List child Sessions.                             |\n| `PATCH`  | `/zones/:zoneId/agents/:id/suspend`  | Suspend a Session subtree.                       |\n| `PATCH`  | `/zones/:zoneId/agents/:id/resume`   | Resume a suspended Session subtree.              |\n| `DELETE` | `/zones/:zoneId/agents/:id`          | Terminate a Session.                             |\n\n## Long-lived Sessions and Invocations\n\n| Method  | Path                                      | Purpose                                                                               |\n| ------- | ----------------------------------------- | ------------------------------------------------------------------------------------- |\n| `POST`  | `/zones/:zoneId/agent-services`           | Register or update a long-lived Session lease. `agent-services` is the protocol path. |\n| `GET`   | `/zones/:zoneId/agent-services`           | List long-lived Session leases.                                                       |\n| `POST`  | `/zones/:zoneId/agents/:id/heartbeat`     | Refresh a long-lived Session lease.                                                   |\n| `POST`  | `/zones/:zoneId/invocations`              | Create an invocation.                                                                 |\n| `GET`   | `/zones/:zoneId/invocations/:id`          | Inspect an invocation.                                                                |\n| `PATCH` | `/zones/:zoneId/invocations/:id/start`    | Mark an invocation running.                                                           |\n| `PATCH` | `/zones/:zoneId/invocations/:id/cancel`   | Cancel an invocation.                                                                 |\n| `PATCH` | `/zones/:zoneId/invocations/:id/complete` | Complete an invocation.                                                               |\n\n## Delegation\n\nRuntime credentials see only Delegations where their application is the issuer or receiver. Recursive traversal and impact analysis are operator-only because descendants can cross application boundaries and expose another application's topology or Authority record IDs. The managed operator token retains zone-wide inspection for Console and Admin SDK workflows.\n\n| Method  | Path                                                   | Purpose                                                                                              |\n| ------- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- |\n| `POST`  | `/zones/:zoneId/delegations`                           | Create a Delegation. The response field `delegation_edge_id` is the protocol name for Delegation ID. |\n| `GET`   | `/zones/:zoneId/delegations/active`                    | List active Delegations.                                                                             |\n| `GET`   | `/zones/:zoneId/delegations/inbound/:sessionId`        | List inbound edges for a session.                                                                    |\n| `GET`   | `/zones/:zoneId/delegations/inbound/:sessionId/:id`    | Validate one opaque inbound Delegation ID for its receiver Session.                                  |\n| `GET`   | `/zones/:zoneId/delegations/outbound/:sessionId`       | List outbound edges for a session.                                                                   |\n| `GET`   | `/zones/:zoneId/delegations/:id/traverse`              | Traverse a Delegation; `coordinator.admin` required.                                                  |\n| `GET`   | `/zones/:zoneId/delegations/:id/impact`                | Compute revocation impact; `coordinator.admin` required.                                              |\n| `GET`   | `/zones/:zoneId/agents/:sessionId/effective-authority` | Compute effective authority for a Session.                                                           |\n| `PATCH` | `/zones/:zoneId/delegations/:id/revoke`                | Revoke delegated authority.                                                                          |\n\n## SDK Lifecycle Protocol Endpoints\n\n| Method | Path                             |\n| ------ | -------------------------------- |\n| `POST` | `/v1/begin`                      |\n| `POST` | `/v1/end`                        |\n| `POST` | `/v1/spawn-child`                |\n| `POST` | `/v1/exchange`                   |\n| `POST` | `/v1/delegate-to-existing-agent` |\n| `POST` | `/v1/revoke-delegation`          |\n| `POST` | `/v1/verify`                     |\n\nSDKs usually call these protocol endpoints through a language-specific Caracal client rather than direct HTTP. `/v1/spawn-child` and `/v1/delegate-to-existing-agent` are stable wire names; product code starts child Sessions and delegates to peer Sessions.\n\nDelegation creation authenticates the issuer and rec"},{"id":"/api/sts","title":"Use STS Endpoint","url":"https://docs.caracal.run/api/sts/","concepts":"","body":"STS is served on port `8080` and issues scoped Caracal mandate JWTs.\n\n## Public Endpoints\n\n| Method | Path                                    | Purpose                                                                               |\n| ------ | --------------------------------------- | ------------------------------------------------------------------------------------- |\n| `POST` | `/oauth/2/token`                        | OAuth token exchange for resource, session, Gateway, or delegated mandates.           |\n| `GET`  | `/.well-known/jwks.json?zone_id={zone}` | Public signing keys for mandate verification, scoped per zone.                        |\n| `GET`  | `/step-up/{id}`                         | Step-up hold state; `?wait={seconds}` long-polls until the state changes.             |\n| `POST` | `/step-up/{id}/decision`                | Subject-plane approval decision, authenticated with a federated user session mandate. |\n| `GET`  | `/health`                               | Liveness check.                                                                       |\n| `GET`  | `/ready`                                | Readiness check.                                                                      |\n| `GET`  | `/metrics`                              | Prometheus metrics.                                                                   |\n| `GET`  | `/metrics.json`                         | JSON metrics.                                                                         |\n\n## Token Exchange Request\n\n`POST /oauth/2/token` accepts form-encoded parameters.\n\n| Parameter                                                    | Purpose                                                                                               |\n| ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- |\n| `grant_type`                                                 | OAuth grant type.                                                                                     |\n| `subject_token`, `subject_token_type`                        | Existing authority to exchange: a session mandate this STS issued.                                    |\n| `resource`                                                   | One or more target resource identifiers.                                                              |\n| `scope`                                                      | Requested scopes.                                                                                     |\n| `zone_id`                                                    | Zone boundary.                                                                                        |\n| `application_id`                                             | Calling application.                                                                                  |\n| `client_secret`, `client_assertion`, `client_assertion_type` | Application authentication.                                                                           |\n| `session_id`, `agent_session_id`, `delegation_edge_id`       | Protocol names for Authority record ID, Session ID, and Delegation ID. They are distinct identifiers. |\n| `ttl_seconds`                                                | Requested TTL.                                                                                        |\n| `challenge_id`                                               | Consumes an approved step-up hold during retry.                                                       |\n\nRFC 8693 `actor_token` is not supported: a request that carries one is rejected with `invalid_token`. The acting application authenticates with its own client credentials, and its identity reaches policy input as `input.context.actor_claims.caracal_client_id`.\n\n## Subject Identity\n\nSTS never generates a subject identity. Every exchange records its subject verbatim: the `sub` claim of the presented `subject_token`, or the authenticated application's id when no subject token is presented. Two subject token classes are accepted:\n\n- `urn:ietf:params:oauth:token-type:access_token` - a session mandate this STS issued for the same zone; resource mandates are rejected as subject tokens (RFC 8693 subject-confusion mitigation).\n- `urn:ietf:params:oauth:token-type:id_token` - **subject federation**: an end user's identity token from a registered Subject issuer. The application authenticates with its client credentials and relays the token; STS verifies it against the issuer's JWKS and creates an Authority record whose `sub` is copied verbatim. A federation exchange names no resources and mints no scopes: the record is an identity and revocation anchor, not resource authority.\n\nThe Subject identifier is opaque to Caracal. It may be a user ID, UUID, employee ID, or customer ID. STS requires only that the same identity always presents the same value. The Authority record stores that `sub`; provider connections, approval bindings, revocation, and audit can attribute activity to it. Scope authority is decided by application bindings, resource grants, Delegation narrowing, and label confinement - not by the Subject identifier.\n\nWhen an SDK starts a Session with `subjectAuthorityRecordId`, `subject_authority_record_id`, or `SubjectAuthorityRecordID`, that field supplies a **Subject authority record ID**. It links the Session to the Authority record for attribution and lifecycle. It does not, by itself, make later resource mandates carry that record's federated `sub`.\n\n### Subject Identifier Guarantees\n\nAny stable identifier format a trusted issuer signs is accepted - UUIDs, emails, numeric ids, prefixed ids such as `auth0|507f...`, URIs, ARNs, or unicode names. Caracal never parses, normalizes, case-folds, or trims the value: it is compared byte-for-byte everywhere, so two byte-distinct values are two subjects, and the recorded value is always byte-identical to "},{"id":"/api/gateway","title":"Proxy Through Gateway","url":"https://docs.caracal.run/api/gateway/","concepts":"","body":"Gateway is served on port `8081`. It is not a CRUD API; it is a protected reverse proxy for configured resources.\n\n## Operator Endpoints\n\n| Method | Path                           | Purpose                     |\n| ------ | ------------------------------ | --------------------------- |\n| `GET`  | `/health`                      | Liveness check.             |\n| `GET`  | `/ready`                       | Readiness check.            |\n| `GET`  | `/metrics`                     | Prometheus metrics.         |\n| `GET`  | `/metrics.json`                | JSON metrics.               |\n| `POST` | `/internal/revocations/reload` | Reload revocation snapshot. |\n\n## Request Requirements\n\n| Input                       | Required | Purpose                                                      |\n| --------------------------- | -------- | ------------------------------------------------------------ |\n| `Authorization: Bearer ...` | yes      | Inbound Caracal mandate.                                     |\n| `X-Caracal-Resource`        | yes      | Resource identifier STS resolves to the routed upstream.     |\n| Request path                | yes      | Forwarded to the configured upstream after traversal checks. |\n\nGateway rejects any request that sets `X-Caracal-Client-ID`; the caller's application identity derives from the verified mandate's `client_id` claim.\n\n## Denial Checks\n\nGateway rejects before upstream dispatch. Treat these checks as the safety gate in front of every protected upstream.\n\n| Stage               | Checks                                                                                                                           |\n| ------------------- | -------------------------------------------------------------------------------------------------------------------------------- |\n| Request preflight   | Bearer token exists, token size is bounded, `X-Caracal-Resource` exists, and request path is not traversal.                      |\n| Token validation    | Token is well formed, not expiring inside the preflight window, signature-valid, not replayed, not revoked, and includes a zone. |\n| Resource resolution | STS resolves `(zone_id, resource)` to a gateway-routed resource, and the returned upstream passes host safety checks.            |\n| STS exchange        | Gateway’s STS circuit is closed and the signed exchange succeeds; STS enforces the resource’s declared scopes on the mandate.    |\n\n## Forwarding Behavior\n\nAfter checks pass, Gateway performs a signed STS exchange, receives a resource mandate, forwards the request to the resource's upstream, and emits audit evidence. Request and upstream timeouts are controlled by Gateway service config.\n\nGateway proxies HTTP request/response traffic, including streamed responses such as SSE. Streamed bodies are flushed chunk by chunk, and revocation anchors are re-checked on every chunk boundary; a mid-stream revocation truncates the stream and sets the `X-Caracal-Revoked` trailer. Gateway does not proxy WebSocket connections: hop-by-hop headers, including `Upgrade`, are stripped before forwarding, so upgrade handshakes never reach the upstream. Protect WebSocket workloads with in-process verification at the service edge instead, using [Protect an MCP Server](/guides/protect-mcp/) or a framework adapter.\n\n## Replay Protection\n\nEvery resource mandate is single-use at the Gateway. On first presentation the Gateway records the token's `jti` in Redis with a lifetime equal to the token's remaining validity; presenting the same mandate again is rejected as a confirmed replay and emits a `replay_detected` audit event carrying the request id, resource, and client identity. Session mandates are exempt because their only valid audience is the STS exchange itself. There is no client-side nonce to manage: the mandate's `jti` is the nonce. Scoped SDK transports therefore bypass mandate caching and single-flight for the final exchange, and application transports cache only their source/target Session and delegation state before minting a fresh final mandate per request. They never retry the data-plane request or replay its body. When Redis is unreachable, the production default rejects the request rather than silently widening the replay window; a deployment can opt into fail-open, which forwards the request and logs the tracker error.\n\n## Next Step\n\nContinue to [Use Event Topics](/api/event-topics/) to understand the Redis Stream topics that carry audit, invalidation, revocation, and Session lifecycle events.\n\n## Related Pages\n\n- [Protect Upstreams](/services/gateway/)\n- [Enforce Boundaries](/architecture/trust-boundaries/)\n- [Protect an MCP Server](/guides/protect-mcp/)"},{"id":"/api/event-topics","title":"Use Event Topics","url":"https://docs.caracal.run/api/event-topics/","concepts":"","body":"Caracal uses Redis Streams for propagation. Published modes sign stream messages with `STREAMS_HMAC_KEY`.\n\n## Topics\n\n| Topic                            | Producers                               | Consumers                                          |\n| -------------------------------- | --------------------------------------- | -------------------------------------------------- |\n| `caracal.audit.events`           | API, STS, Gateway, Coordinator, Control | Audit `audit-ingestor`, SIEM exporters             |\n| `caracal.audit.events.dlq`       | Audit                                   | DLQ observers                                      |\n| `caracal.policy.invalidate`      | API                                     | STS policy loader                                  |\n| `caracal.sessions.revoke`        | API, Coordinator                        | STS, Gateway, resource-server revocation consumers |\n| `caracal.keys.invalidate`        | API, STS                                | STS key caches                                     |\n| `caracal.agents.lifecycle`       | Coordinator                             | Coordinator lifecycle relay job                    |\n| `caracal.invocations.lifecycle`  | Coordinator                             | Invocation observers                               |\n| `caracal.delegations.invalidate` | Coordinator                             | Delegation observers                               |\n| `caracal.providers.ratelimit`    | Provisioner/provider coordination       | Provider rate-limit coordination                   |\n\n## Consumer Groups\n\n| Topic                            | Groups                          |\n| -------------------------------- | ------------------------------- |\n| `caracal.audit.events`           | `audit-ingestor`, `siem-export` |\n| `caracal.audit.events.dlq`       | `audit-dlq-observer`            |\n| `caracal.policy.invalidate`      | `opa-engine`                    |\n| `caracal.sessions.revoke`        | `sts-revocation`, stable per-host `gateway-revocation:<host>`, deployment-specific resource groups |\n| `caracal.keys.invalidate`        | `sts-keys`                      |\n| `caracal.agents.lifecycle`       | `coordinator-relay`             |\n| `caracal.invocations.lifecycle`  | `invocations-observer`          |\n| `caracal.delegations.invalidate` | `delegations-observer`          |\n\n## Message Integrity\n\nSigned stream messages include the `_sig` field. Consumers in published modes must reject unsigned or mismatched messages for streams that require origin verification.\n\n## Related Pages\n\n- [Propagate Events](/architecture/event-streams/)\n- [Operate Redis Streams](/operations/redis/)\n- [Wire Contracts](/reference/interoperability-contracts/)"},{"id":"/services","title":"Understand Services","url":"https://docs.caracal.run/services/","concepts":"","body":"Caracal services are small, explicit runtime components. Each service owns a bounded part of the authority lifecycle and exposes health/readiness endpoints for operations.\n\n## Service Map\n\n| Service                                            | Port       | Owns                                                                               |\n| -------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------- |\n| [Manage Product State](/services/api/)             | `3000`     | Product state, management routes, policy/grant resources, admin audit, API outbox. |\n| [Coordinate Session State](/services/coordinator/) | `4000`     | Sessions, service leases, Delegations, invocations, Coordinator outbox.            |\n| [Issue Mandates](/services/sts/)                   | `8080`     | Token exchange, mandate issuance, JWKS, policy evaluation, step-up status.         |\n| [Protect Upstreams](/services/gateway/)            | `8081`     | Protected reverse proxy, per-request exchange, revocation checks, upstream safety. |\n| [Ingest Audit Evidence](/services/audit/)          | `9090`     | Audit ingestion, DLQ, tamper checks, retention, search.                            |\n| [Automate Management](/services/control/)          | API `3000` | Optional in-process remote management invocation through shared engine dispatch.   |\n\n## Dependency Map\n\n## Service Reading Path\n\n| Path                    | Pages                                                                                       |\n| ----------------------- | ------------------------------------------------------------------------------------------- |\n| Management plane        | [Manage Product State](/services/api/) → [Coordinate Session State](/services/coordinator/) |\n| Authority path          | [Issue Mandates](/services/sts/) → [Protect Upstreams](/services/gateway/)                  |\n| Evidence and automation | [Ingest Audit Evidence](/services/audit/) → [Automate Management](/services/control/)       |\n\n## Next Step\n\nStart with [Manage Product State](/services/api/) to understand where Caracal product objects are owned.\n\n## Related Sections\n\n- [Understand Architecture](/architecture/)\n- [Operations](/operations/)\n- [Use API Reference](/api/)"},{"id":"/services/api","title":"Manage Product State","url":"https://docs.caracal.run/services/api/","concepts":"","body":"The API service owns product-management state and route handlers for zones, applications, providers, resources, policies, policy sets, grants, step-up challenges, policy templates, and zone events.\n\n## Runtime\n\n| Property            | Value                             |\n| ------------------- | --------------------------------- |\n| Port                | `3000`                            |\n| Health              | `GET /health`                     |\n| Readiness           | `GET /ready`                      |\n| Metrics             | `GET /metrics`                    |\n| Main route prefix   | `/v1`                             |\n| Optional docs route | `/docs` when API docs are enabled |\n\n## Dependencies\n\n| Dependency      | Purpose                                                                                     |\n| --------------- | ------------------------------------------------------------------------------------------- |\n| Postgres        | Product state, admin tokens, policy versions, grants, sessions, outbox, audit admin events. |\n| Redis           | Event streams and outbox dispatch.                                                          |\n| STS             | Service URL used by management flows that need STS coordination.                            |\n| Runtime secrets | Admin token, zone KEK, stream HMAC, audit HMAC, Gateway-STS HMAC, database and Redis URLs.  |\n\n## Event Output\n\nAPI writes `event_outbox` rows in the same transaction as product-state changes. The dispatcher signs payloads with `STREAMS_HMAC_KEY` and publishes Redis stream events such as policy invalidation and audit records.\n\n## Operational Signals\n\n| Signal                      | Meaning                                                                                             |\n| --------------------------- | --------------------------------------------------------------------------------------------------- |\n| API readiness               | Database, Redis, outbox dead rows, and service config are healthy enough for management operations. |\n| `API_READY_OUTBOX_DEAD_MAX` | Readiness threshold for dead outbox rows.                                                           |\n| Admin auth failures         | Token, scope, or rate-limit issue for Console/Admin clients.                                        |\n| Outbox age/dead metrics     | Redis or dispatcher is not draining management events.                                              |\n\n## Next Step\n\nUse [Coordinate Session State](/services/coordinator/) to understand Sessions, invocations, and delegation runtime state.\n\n## Related Pages\n\n- [Use the Admin API](/api/control-plane/)\n- [Manage Product Objects](/runtime-console/admin/)\n- [Operate PostgreSQL](/operations/postgres/)"},{"id":"/services/coordinator","title":"Coordinate Session State","url":"https://docs.caracal.run/services/coordinator/","concepts":"","body":"Coordinator owns Session and Delegation runtime state. SDKs and Console use it to start, inspect, suspend, resume, terminate, traverse, and revoke that state.\n\n## Runtime\n\n| Property   | Value                                                                        |\n| ---------- | ---------------------------------------------------------------------------- |\n| Port       | `4000`                                                                       |\n| Health     | `GET /health`                                                                |\n| Readiness  | `GET /ready`                                                                 |\n| Metrics    | `GET /metrics`                                                               |\n| Auth scope | `AGENT_COORDINATOR_SCOPE`, default `agent:lifecycle` in deployment templates |\n\n## Dependencies\n\n| Dependency        | Purpose                                                                               |\n| ----------------- | ------------------------------------------------------------------------------------- |\n| Postgres          | Sessions, service leases, invocations, Delegations, graph epochs, Coordinator outbox. |\n| Redis             | Lifecycle, invocation, delegation, and revocation streams.                            |\n| STS/JWKS          | Token validation and authority alignment.                                             |\n| Coordinator token | Console and operator access to Session and Delegation views.                          |\n\n## Jobs\n\n| Job                   | Purpose                                                                                |\n| --------------------- | -------------------------------------------------------------------------------------- |\n| TTL sweeper           | Expires stale sessions and delegation state.                                           |\n| Service lease sweeper | Marks stale long-lived Sessions unhealthy.                                             |\n| Deadline enforcer     | Handles invocation deadlines.                                                          |\n| Retention cleaner     | Removes expired historical coordination records.                                       |\n| Outbox publisher      | Publishes Coordinator outbox messages to Redis.                                        |\n| Lifecycle relay       | Observes `caracal.agents.lifecycle` delivery with HMAC origin verification and dedupe. |\n\n## Event Topics\n\nCoordinator publishes `caracal.agents.lifecycle`, `caracal.invocations.lifecycle`, `caracal.delegations.invalidate`, and session revocation events.\n\n## Next Step\n\nUse [Issue Mandates](/services/sts/) to understand how STS validates sessions and Delegations during token exchange.\n\n## Related Pages\n\n- [Use Coordinator API](/api/coordinator/)\n- [Coordinate Sessions](/architecture/delegation-flow/)\n- [Manage Sessions and Delegation](/runtime-console/agents/)"},{"id":"/services/sts","title":"Issue Mandates","url":"https://docs.caracal.run/services/sts/","concepts":"","body":"STS is the authority issuance boundary. It authenticates applications, validates sessions and delegated authority, evaluates policies, handles step-up challenges, and returns scoped mandate JWTs.\n\nSTS federates subject identity rather than managing it: it never authenticates end users and never generates a `sub`. The subject recorded on a minted session is the `sub` of the exchanged subject token - an opaque, stable identifier owned by the application's own identity system - or the application id itself when the exchange carries no subject token. A zone registers **subject issuers** (external issuer, JWKS URL, audience) to accept end-user identity tokens directly; see [Use STS Endpoint](/api/sts/#subject-identity) for the exchange contract.\n\n## Endpoints\n\nSTS listens on port `8080`.\n\n| Method and path                             | Purpose                                                                   |\n| ------------------------------------------- | ------------------------------------------------------------------------- |\n| `POST /oauth/2/token`                       | Token exchange that issues mandates.                                      |\n| `POST /v1/run/manifest`                     | Binding manifest for `caracal run` workloads.                             |\n| `POST /v1/run/credential`                   | Credential minting for `caracal run` workloads.                           |\n| `GET /.well-known/jwks.json?zone_id={zone}` | Public signing keys.                                                      |\n| `GET /step-up/{id}`                         | Step-up hold status.                                                      |\n| `POST /step-up/{id}/decision`               | Subject-plane step-up decision (federated user session mandate required). |\n| `GET /health`, `GET /ready`                 | Liveness and readiness.                                                   |\n| `GET /metrics`, `GET /metrics.json`         | Service metrics.                                                          |\n\n### Internal Endpoints\n\nOnly other Caracal services call these routes. Do not expose them to workloads or the public network.\n\n| Method and path                                    | Purpose                                        |\n| -------------------------------------------------- | ---------------------------------------------- |\n| `POST /internal/policy/simulate`                   | Policy-set simulation for the API service.     |\n| `GET /internal/policy/status/{zoneID}`             | Policy activation status for the API service.  |\n| `POST /internal/zones/{zoneID}/signing-key/rotate` | Zone signing-key rotation for the API service. |\n\n## Dependencies\n\n| Dependency       | Purpose                                                                                |\n| ---------------- | -------------------------------------------------------------------------------------- |\n| Postgres         | Applications, grants, resources, policies, sessions, step-up challenges, signing keys. |\n| Redis            | Policy/key invalidation, revocation, provider coordination, audit emission.            |\n| Zone KEK         | Decrypt signing and secret material.                                                   |\n| Gateway HMAC key | Verify Gateway-authenticated exchanges.                                                |\n| Audit replay dir | Persist audit events while Redis/Audit is unavailable.                                 |\n\n## Issuance Rules\n\n| Rule                    | Behavior                                                               |\n| ----------------------- | ---------------------------------------------------------------------- |\n| Resource mandate TTL    | Capped at 15 minutes.                                                  |\n| Session mandate TTL     | Capped at 60 minutes.                                                  |\n| `MAX_GRANT_TTL_SECONDS` | Defaults to `3600`.                                                    |\n| `OPA_POLL_SECONDS`      | Defaults to `60`, capped at `300`.                                     |\n| Published modes         | Require `GATEWAY_STS_HMAC_KEY` and audit/stream keys where configured. |\n\n## Failure Posture\n\nInvalid client credentials, missing resources, invalid subject tokens, revoked sessions, undecided step-up holds, policy denial, invalid delegation, and failed Gateway signatures deny exchange. Gateway-authenticated mandate use is additionally held to the resource's native operation floor: an operation that is not declared on an enforced resource, or whose required scope is absent from the mandate, is denied with `operation_not_permitted` independently of policy.\n\n## Next Step\n\nUse [Protect Upstreams](/services/gateway/) to understand how Gateway validates inbound authority and performs per-request STS exchange.\n\n## Related Pages\n\n- [Exchange Tokens](/architecture/token-exchange-flow/)\n- [Use STS Endpoint](/api/sts/)\n- [Step-Up Challenges](/concepts/step-up/)"},{"id":"/services/gateway","title":"Protect Upstreams","url":"https://docs.caracal.run/services/gateway/","concepts":"","body":"Gateway is the protected-resource ingress. It validates inbound Caracal authority, checks replay and revocation, exchanges with STS, and forwards to configured upstreams only after safety checks pass.\n\n## Runtime\n\n| Property | Value |\n| --- | --- |\n| Port | `8081` |\n| Health/readiness | `/health`, `/ready` |\n| Metrics | `/metrics`, `/metrics.json` |\n| Revocation reload | `POST /internal/revocations/reload` |\n\n## Request Path\n\n## Deny-Before-Upstream Checks\n\n- Missing, malformed, oversized, expiring, replayed, revoked, or signature-invalid bearer token.\n- Missing `X-Caracal-Resource`.\n- No binding for `(zone_id, resource)`.\n- Client-supplied `X-Caracal-Client-ID`.\n- Path traversal.\n- Operation (method and path) not declared on an enforced resource, or missing the operation's required scope.\n- STS exchange failure or open STS circuit.\n- Unsafe upstream destination.\n\n## Protocol Boundary\n\nGateway proxies HTTP request/response traffic, including streamed responses such as SSE, with revocation re-checked on every streamed chunk. It does not proxy WebSocket connections: hop-by-hop headers, including `Upgrade`, are stripped before forwarding, so upgrade handshakes never reach the upstream. Enforce mandates for WebSocket workloads in-process at the service edge with the verification engine or a framework adapter.\n\nGateway replicas consume revocations through a stable per-host consumer group, with one process-specific consumer in that group. A startup Postgres snapshot establishes current state before the group begins at the stream tail; pending recovery claims messages left by a crashed process on the same host identity. This preserves replica broadcast without creating a fresh group or replaying full history on every restart.\n\n## Configuration Highlights\n\n| Variable | Purpose |\n| --- | --- |\n| `STS_URL` | STS endpoint for exchange and JWKS. |\n| `GATEWAY_STS_HMAC_KEY` | Signs Gateway exchange requests. |\n| `MAX_REQUEST_BYTES` | Request body limit, default 10 MiB. |\n| `UPSTREAM_HOST_ALLOWLIST` | Optional egress allowlist pinning upstreams to named hosts. |\n| `JTI_FAIL_OPEN` | Forbidden in published modes. |\n| `AUDIT_REPLAY_DIR` | Replay directory for audit events. |\n\n## Next Step\n\nUse [Ingest Audit Evidence](/services/audit/) to understand how Gateway, STS, API, Coordinator, and Control evidence becomes searchable audit state.\n\n## Related Pages\n\n- [Proxy Through Gateway](/api/gateway/)\n- [Enforce Boundaries](/architecture/trust-boundaries/)\n- [Harden Production](/operations/tls-hardening/)"},{"id":"/services/audit","title":"Ingest Audit Evidence","url":"https://docs.caracal.run/services/audit/","concepts":"","body":"Audit consumes signed audit events from Redis, verifies integrity, writes append-only audit rows to Postgres, manages DLQ, and exposes operator search and metrics.\n\n## Runtime\n\n| Property | Value |\n| --- | --- |\n| Port | `9090` |\n| Health/readiness | `GET /health`, `GET /ready` |\n| Metrics | `GET /metrics`, `GET /metrics.json` |\n| Search | `GET /api/audit/search` |\n| DLQ | `GET /api/audit/dlq`, `GET /api/audit/dlq/{id}`, `POST /api/audit/dlq/replay` |\n\nSearch and DLQ routes require the `AUDIT_ADMIN_TOKEN` bearer. When the token is not configured, these routes return `404`. For zone-scoped audit reads with admin tokens, use `GET /v1/zones/:zoneId/audit` on the API service (port `3000`) instead; see [Use the Admin API](/api/control-plane/).\n\n## Consumer Behavior\n\nAudit consumes `caracal.audit.events` with the `audit-ingestor` consumer group. On startup it drains pending entries for its consumer, periodically claims orphaned entries, retries until `AUDIT_MAX_DELIVERIES`, and moves permanent failures to `caracal.audit.events.dlq`.\n\n## Integrity Controls\n\n| Control | Meaning |\n| --- | --- |\n| `AUDIT_HMAC_KEY` | Verifies producer-signed events in published modes. |\n| Tamper checks | Detect content hash mismatch, chain breaks, and HMAC failures. |\n| Append-only database role | Audit role cannot update or delete `audit_events`. |\n| Retention | `AUDIT_RETENTION_DAYS`, partitions, and optional export watermarks. |\n\n## Readiness Signals\n\n| Signal | Meaning |\n| --- | --- |\n| DLQ threshold | `AUDIT_READY_DLQ_MAX` controls readiness tolerance. |\n| Consumer lag | `AUDIT_READY_LAG_MAX` controls accepted stream lag. |\n| PEL age | `AUDIT_READY_PEL_OLDEST_SECS_MAX` controls pending-entry staleness. |\n\n## Next Step\n\nUse [Automate Management](/services/control/) when remote automation needs the same product-management operations available in Console.\n\n## Related Pages\n\n- [Audit and Request Traces](/concepts/audit-ledger/)\n- [Export Audit Evidence](/operations/compliance-audit-integration/)\n- [Configure Alerts](/operations/alerts/)"},{"id":"/services/control","title":"Automate Management","url":"https://docs.caracal.run/services/control/","concepts":"","body":"Control is an optional automation surface for remote management operations. It runs as an in-process plugin inside the API service and dispatches through the shared engine after gate, authentication, replay, rate-limit, and scope checks.\n\n## Runtime\n\nControl has no separate deployable or port. The plugin is mounted inside API when\n`CARACAL_CONTROL_ENABLED=true` and serves on the API port. A runtime gate file\n(`CONTROL_GATE_FILE`) is the instant on/off switch: when the file is absent the\nendpoint returns `503` without a restart.\n\n| Property     | Value                              |\n| ------------ | ---------------------------------- |\n| Host         | API service (port `3000`)          |\n| Health       | `GET /health`                      |\n| Readiness    | `GET /ready`                       |\n| Invoke       | `POST /v1/control/invoke`          |\n| Enablement   | `CARACAL_CONTROL_ENABLED=true`     |\n| Runtime gate | `CONTROL_GATE_FILE` present        |\n| Helm default | disabled (`control.enabled=false`) |\n\n## Invoke Flow\n\n## Required Config\n\n| Variable                  | Purpose                                                       |\n| ------------------------- | ------------------------------------------------------------- |\n| `CARACAL_CONTROL_ENABLED` | Mounts the control plugin inside API.                         |\n| `STS_JWKS_URL`            | JWT verification keys.                                        |\n| `STS_ISSUER_URL`          | Expected issuer.                                              |\n| `CONTROL_AUDIENCE`        | Expected token audience.                                      |\n| `CONTROL_API_TOKEN`       | API token used for downstream dispatch.                       |\n| `CONTROL_GATE_FILE`       | Runtime gate file that must exist before invoke is available. |\n\n## Non-Interactive Automation\n\nControl is the automation counterpart to Console. A CI job, provisioning script,\nor onboarding tool drives the same management operations a human performs in\nConsole, because both run through the shared engine dispatch. Automation never\nreceives the root admin token; it uses a scoped control key.\n\n1. Create a control key once in Console from the Control menu. Console returns a\n   `client_id` and `client_secret` and records the `control:<command>:<verb>`\n   scopes the key may request, along with an optional maximum token TTL. The key\n   is bound to the zone where it was created; its secret is held sealed in the\n   Secret Store and stays retrievable from the Applications page through an\n   audited reveal.\n2. Exchange the key for a short-lived token at the STS token endpoint.\n\n   ```bash\n   curl -s -X POST \"$STS_URL/oauth/2/token\" \\\n     -d grant_type=client_credentials \\\n     -d application_id=\"$CONTROL_CLIENT_ID\" \\\n     -d client_secret=\"$CONTROL_CLIENT_SECRET\" \\\n     -d resource=caracal-control \\\n     -d scope=\"control:resource:write control:policy:write\"\n   ```\n\n3. Invoke a management command with the returned token.\n\n   ```bash\n   curl -s -X POST \"$CARACAL_API_URL/v1/control/invoke\" \\\n     -H \"authorization: Bearer $TOKEN\" \\\n     -H \"content-type: application/json\" \\\n     -d '{\"command\":\"resource\",\"subcommand\":\"create\",\"flags\":{\"name\":\"PiperNet\",\"identifier\":\"resource://pipernet\",\"scopes\":[\"pipernet.read\"]}}'\n   ```\n\nSTS resolves the bound zone from the authenticated control key; standard\nbootstrap workflows must not ask users to provide a separate zone id. Supplying\n`zone_id` on a Control key token exchange remains accepted for compatibility but\nis deprecated. Each token is short-lived, replay-protected, rate-limited, and audited, so\nleast-privilege automation stays the easy path. The `controlBootstrap` example\nships a reusable client plus an apply/verify/teardown pipeline that keeps an\nagent's environment in sync with a declared plan through this flow.\n\n## Declarative Management\n\n`resource create` and the other per-object verbs are imperative: the caller must\nknow whether an object already exists and choose create or patch. For real\nautomation, declare the desired state once and let the server converge to it. The\nreconciliation logic lives in the engine, so every adopter - any language, any\ndevice - gets identical behavior without reimplementing a client-side diff.\n\nThree operations cover the full lifecycle, all keyed on each object's stable\nidentity (application `name`, provider and resource `identifier`, policy and\npolicy-set `name`):\n\n| Command  | Subcommand                                                             | Effect                                                                                                                                                                                                        |\n| -------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `ensure` | `application`, `identity-provider`, `resource`, `policy`, `policy-set` | Create-or-patch a single object from its `--spec`. Safe to repeat.                                                                                                                                            |\n| `state`  | `apply`                                                                | Reconcile a whole zone toward a desired-state `--document`: create missing objects, patch drifted ones, publish a new policy version on content change, and optionally `--prune` objects not in the document. |\n| `state`  | `plan` / `verify`                                                      | Read-only. Return the same reconciliation report without writing. `verify` is the CI gate: the report's `drift` field is `true` when the live zone does not match the document.                               |\n\nA desired-state document is a flat object set. Pass it as the JSON-encoded\n`document` flag:\n\n```jso"},{"id":"/reference","title":"Use Reference","url":"https://docs.caracal.run/reference/","concepts":"","body":"Reference pages are for exact names, defaults, limits, compatibility claims, and schema contracts. Use them after you understand the workflow or when validating automation.\n\n## Find Answers\n\n| Need | Page |\n| --- | --- |\n| Direct answers to common questions | [FAQ](/reference/faq/) |\n| Canonical terms | [Glossary](/reference/glossary/) |\n| Error response codes | [Error Codes](/reference/errors/) |\n\n## Configure and Run\n\n| Need | Page |\n| --- | --- |\n| Runtime and service configuration keys | [Configuration Keys](/reference/configuration/) |\n| Configuration source order | [Configuration Order](/reference/config-precedence/) |\n| Ports, TTLs, limits | [Defaults and Limits](/reference/defaults-and-limits/) |\n| Runtime CLI exit behavior | [CLI Exit Codes](/reference/runtime-exit-codes/) |\n\n## Versions and Contracts\n\n| Need | Page |\n| --- | --- |\n| Supported runtimes and deployment targets | [Compatibility](/reference/compatibility/) |\n| Package, image, and release naming | [Release Map](/reference/release-package-runtime-map/) |\n| JSON schemas and fixtures | [Wire Contracts](/reference/interoperability-contracts/) |\n| Community and Enterprise feature boundary | [Compare Editions](/enterprise/) |\n\n## Source-of-Truth Rule\n\nWhen Reference conflicts with API, Operations, or SDK pages, treat code and generated schemas as source of truth and update the docs section coherently.\n\n## Next Step\n\nStart with [FAQ](/reference/faq/) for recurring modeling, runtime, troubleshooting, and edition questions."},{"id":"/reference/faq","title":"FAQ","url":"https://docs.caracal.run/reference/faq/","concepts":"","body":"import FaqRegistryScript from '../../../components/FaqRegistryScript.astro'\n\n<section className=\"faqSearchPanel\" aria-label=\"Search FAQs\">\n  <div className=\"faqSearchMeta\">\n    <span id=\"faq-search-status\" className=\"faqSearchStatus\" aria-live=\"polite\">\n      26 results\n    </span>\n  </div>\n  <div className=\"faqSearchRow\">\n    <label className=\"faqSearchLabel\" htmlFor=\"faq-search\">\n      Search FAQs\n    </label>\n    <input\n      id=\"faq-search\"\n      className=\"faqSearchInput\"\n      type=\"search\"\n      placeholder=\"Search by FAQ ID, title, keyword...\"\n      aria-controls=\"faq-registry\"\n      autocomplete=\"off\"\n    />\n    <button type=\"button\" className=\"faqClearFilters\" data-faq-clear hidden>\n      Clear\n    </button>\n    <button type=\"button\" className=\"faqFilterToggle\" data-faq-filter-toggle aria-expanded=\"false\" aria-controls=\"faq-filter-popover\">\n      <span data-faq-filter-label>Filter</span>\n      <svg className=\"faqFilterIcon\" viewBox=\"0 0 16 16\" aria-hidden=\"true\" focusable=\"false\">\n        <path d=\"M2.5 4.5h11M5.5 8h5M7 11.5h2\" fill=\"none\" stroke=\"currentColor\" strokeLinecap=\"round\" strokeWidth=\"1.5\" />\n      </svg>\n    </button>\n  </div>\n  <div id=\"faq-filter-popover\" className=\"faqFilterPopover\" data-faq-filter-popover aria-label=\"Filter FAQs by category\" hidden>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"platform\" data-faq-filter-name=\"Platform\" /> <span>Platform</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"model\" data-faq-filter-name=\"Model\" /> <span>Model</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"resources\" data-faq-filter-name=\"Resources\" /> <span>Resources</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"providers\" data-faq-filter-name=\"Providers\" /> <span>Providers</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"runtime\" data-faq-filter-name=\"Runtime\" /> <span>Runtime</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"editions\" data-faq-filter-name=\"Editions\" /> <span>Editions</span>\n    </label>\n  </div>\n</section>\n\n<nav className=\"faqStrip\" data-faq-strip aria-label=\"Suggested FAQs\">\n  <a className=\"faqStripItem\" href=\"#faq-001\">\n    <span>FAQ-001</span>\n    <strong>What problem does Caracal solve?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-003\">\n    <span>FAQ-003</span>\n    <strong>What should a zone represent?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-006\">\n    <span>FAQ-006</span>\n    <strong>Should I create one application per agent?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-008\">\n    <span>FAQ-008</span>\n    <strong>Can policy and audit tell agents apart under one application?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-021\">\n    <span>FAQ-021</span>\n    <strong>Policy vs grant - which wins?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-016\">\n    <span>FAQ-016</span>\n    <strong>403 from STS vs Gateway?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-017\">\n    <span>FAQ-017</span>\n    <strong>Where is the diagnostic bundle or doctor command?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-019\">\n    <span>FAQ-019</span>\n    <strong>Which features are Enterprise Edition only?</strong>\n  </a>\n</nav>\n\n<section className=\"faqListPanel\" aria-label=\"FAQ results\">\n<p id=\"faq-empty\" className=\"faqEmpty\" hidden>No FAQs match that search.</p>\n\n<div id=\"faq-registry\" className=\"faqRegistry\">\n    <details id=\"faq-001\" className=\"faqEntry\" data-faq-entry data-faq-category=\"platform\" data-faq-id=\"FAQ-001\" data-faq-title=\"What problem does Caracal solve?\" data-keywords=\"authority broker short-lived credentials policy STS Gateway Audit automation workflows\">\n      <summary className=\"faqEntryHeader\">\n        <span className=\"faqNumber\">FAQ-001</span>\n        <span className=\"faqTag faqTagPlatform\">Platform</span>\n        <h3>What problem does Caracal solve?</h3>\n        <button type=\"button\" className=\"faqShareButton\" data-faq-share aria-label=\"Share FAQ-001\">Share</button>\n      </summary>\n      <span id=\"faq-what-problem-does-caracal-solve\" className=\"faqAnchor\"></span>\n      <p>Caracal gives agents and automated workflows short-lived, policy-approved authority instead of long-lived credentials. The agent asks for scoped authority at the moment it acts, STS evaluates policy, Gateway or an adapter enforces the mandate, and Audit records the decision and result.</p>\n      <p>See <a href=\"/concepts/authority-model/\">Authority and Enforcement</a> and the <a href=\"/concepts/model-overview/\">Caracal Mental Model</a>.</p>\n      <p className=\"faqRelated\">Related: <a href=\"#faq-002\">FAQ-002</a>, <a href=\"#faq-003\">FAQ-003</a>, <a href=\"#faq-016\">FAQ-016</a></p>\n    </details>\n\n    <details id=\"faq-002\" className=\"faqEntry\" data-faq-entry data-faq-category=\"platform\" data-faq-id=\"FAQ-002\" data-faq-title=\"Is Caracal an identity provider, secrets manager, or API gateway?\" data-keywords=\"IdP identity provider secrets manager API gateway authority broker provider credentials\">\n      <summary className=\"faqEntryHeader\">\n        <span className=\"faqNumber\">FAQ-002</span>\n        <span className=\"faqTag faqTagPlatform\">Platform</span>\n        <h3>Is Caracal an identity provider, secrets manager, or API gateway?</h3>\n        <button type=\"button\" className=\"faqShareButton\" data-faq-share aria-label=\"Share FAQ-002\">Share</button>\n      </summary>\n      <span id=\"faq-is-caracal-an-idp-secrets-manager-or-api-gateway\" className=\"faqAnchor\"></span>\n      <p>No. Caracal is an authority broker for agent and workload actions. It can sit in front of HTTP resources like a protected Gateway, and it can broker provider credentials, but it does not rep"},{"id":"/reference/glossary","title":"Glossary","url":"https://docs.caracal.run/reference/glossary/","concepts":"","body":"Use these terms consistently across docs, API names, web console labels, and examples.\n\n| Term                        | Meaning                                                                                                                                                                                                                                                                                                                                                                   |\n| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Application                 | Registered client identity in a zone that software runs as; confidential applications hold a secret and can exchange credentials.                                                                                                                                                                                                                                         |\n| Authority record            | One STS exchange record. Its Authority record ID anchors revocation, ancestry, and audit for authority minted by that exchange. Stored in the `authority_records` table.                                                                                                                                                                                                        |\n| Audit ledger                | Append-only evidence stream and database records for decisions and operations.                                                                                                                                                                                                                                                                                            |\n| Approval                    | A policy-raised hold that pauses a request until a human with authority approves or rejects it; surfaced on the Console Approvals page and returned on the wire as `interaction_required`.                                                                                                                                                                                |\n| Caracal Operator            | Governed natural-language console assistant that turns intent into reviewed, audited control-plane changes within your operator scope.                                                                                                                                                                                                                                    |\n| Console                     | Browser-based management UI served by the packaged web tier in Compose and Helm; `caracal web` opens it during local development.                                                                                                                                                                                                                                         |\n| Control API                 | Optional authenticated automation surface for remote management dispatch, authorized by scoped control keys.                                                                                                                                                                                                                                                              |\n| Delegation                  | Bounded authority one session grants to another: scopes, optional resource, constraints, and expiry, revocable independently. Stored internally as an edge in the delegation graph.                                                                                                                                                                                       |\n| Delegation ID               | Unique product identifier for a Delegation.                                                                                                                                                                                                                                                                                                                               |\n| Gateway                     | Reverse proxy that verifies inbound authority, exchanges with STS, and forwards to upstreams.                                                                                                                                                                                                                                                                             |\n| Grant                       | Access assignment for a resource. Policy `grants` data maps roles to allowed scopes per resource; managed delegated grants record subject-level assignments and cascade revocation to the subject's sessions.                                                                                                                                                             |\n| Mandate                     | Short-lived scoped access token (a JWT) carrying Caracal authority.                                                                                                                                                                                                                                                                                                       |\n| Policy                      | Rego content that participates in allow/deny decisions.                                                                                                                                                                                                                                                                                   "},{"id":"/reference/errors","title":"Error Codes","url":"https://docs.caracal.run/reference/errors/","concepts":"","body":"Caracal packages and services use a shared machine-readable error shape.\n\n```json\n{\n  \"error\": \"invalid_token\",\n  \"error_description\": \"bearer signature invalid\",\n  \"requestId\": \"018f...\",\n  \"details\": {}\n}\n```\n\n`details` appears only when a service or package provides structured context. The same code can be emitted by different surfaces - an `access_denied` from STS is a policy decision, while an `access_denied` surfaced by an in-process verifier is a missing scope on an otherwise valid mandate. Use the `requestId` with the web console **Audit** decision trace to confirm which surface produced the error before acting.\n\n## Well-Known Codes\n\nThe **Likely source** column names the surface that most often emits the code, and **First check** is the fastest thing to confirm. When a call fails, start from the surface, not the code.\n\n| Code                               | Meaning                                                                                                          | Likely source                                                     | First check                                                                                                                                                     |\n| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `access_denied`                    | Request is authenticated but not authorized.                                                                     | STS policy decision, or an in-process verifier scope check.       | Run `request trace`: confirm the active policy set allows the application, subject, resource, and scopes.                                                       |\n| `invalid_token`                    | Token is missing, malformed, expired, invalid, replayed, or fails verification.                                  | Gateway or a resource verifier.                                   | Confirm the mandate is unexpired, the verifier trusts the issuing zone JWKS, and the token was not already consumed.                                            |\n| `resource_not_found`               | Requested resource does not exist or is unavailable in the current scope.                                        | API, or STS resource resolution.                                  | Confirm the resource identifier and that the resource carries an upstream URL for `(zone, resource)`.                                                           |\n| `internal_error`                   | Service failed unexpectedly.                                                                                     | Any service.                                                      | Check service readiness and logs for the `requestId`.                                                                                                           |\n| `policy_eval_failed`               | Policy evaluation failed.                                                                                        | STS policy engine.                                                | Confirm the active policy set compiles and that required `input` fields are present.                                                                            |\n| `provider_rate_limited`            | Provider or provider coordination rate limit denied work.                                                        | Gateway upstream or provider coordination.                        | Inspect provider limits and retry/backoff; confirm the provider grant is active.                                                                                |\n| `interaction_required`             | Policy holds the mint for human approval.                                                                        | STS approval gate.                                                | Wait for the hold to be decided, then retry with the approval id (`challenge_id` on the wire); see [Human Approval](/guides/step-up/).                          |\n| `sts_unavailable`                  | STS could not satisfy an exchange.                                                                               | STS, or Gateway's STS circuit.                                    | Run `caracal status --ready`; confirm STS readiness, JWKS, and policy bundle freshness.                                                                         |\n| `credential_expired_not_renewable` | Credential is too close to expiry or cannot be renewed.                                                          | STS provider-token refresh.                                       | Reconnect the provider grant; confirm OAuth refresh configuration.                                                                                              |\n| `payload_too_large`                | Request body or token exceeded the configured limit.                                                             | Gateway or API preflight.                                         | Reduce payload size or adjust the configured limit.                                                                                                             |\n| `zone_invalid`                     | Zone claim or route zone is invalid.                                                                             | Gateway or STS.                                                   | Confirm the request targets the correct zone and the mandate carries a matching zone claim.                                                                     |\n| `scope_insufficient`               | Required scope is missing.                                                                                       | A resource ver"},{"id":"/reference/configuration","title":"Configuration Keys","url":"https://docs.caracal.run/reference/configuration/","concepts":"","body":"Caracal has three configuration domains:\n\n| Domain                     | Used by                                             | Choose it when                                                |\n| -------------------------- | --------------------------------------------------- | ------------------------------------------------------------- |\n| Workload identity          | `caracal run`.                                      | A workload launches with console-managed credential bindings. |\n| SDK profile                | SDK clients.                                        | An SDK workload needs Caracal-issued resource credentials.    |\n| Service environment config | API, STS, Gateway, Audit, Coordinator, and Control. | A service needs URLs, secrets, limits, or readiness settings. |\n| Deployment values          | Helm, Compose, Postgres, and Redis.                 | Operators size, schedule, expose, or secure infrastructure.   |\n\n## Workload Identity Keys\n\n| Key                            | Meaning                                                                  |\n| ------------------------------ | ------------------------------------------------------------------------ |\n| `CARACAL_WORKLOAD_ID`          | Workload ID from the console's Launcher page; required by `caracal run`. |\n| `CARACAL_WORKLOAD_SECRET`      | Inline local-development workload secret.                                |\n| `CARACAL_WORKLOAD_SECRET_FILE` | Cloud/custom mounted workload-secret file path.                          |\n| `CARACAL_STS_URL`              | Cloud/custom STS URL override.                                           |\n\nCredential bindings, zone, scopes, and failure behavior come from the workload's launch bindings, authored in the web console. Local dev and stable launches can omit both secret variables and read the owner-only file at `<Caracal config dir>/runtime/<workload_id>/secret`.\n\n## SDK Profile Fields\n\n| Field                    | Meaning                                               |\n| ------------------------ | ----------------------------------------------------- |\n| `sts_url`                | Cloud/custom STS URL for token exchange.              |\n| `coordinator_url`        | Cloud/custom SDK/Console Coordinator URL override.    |\n| `gateway_url`            | Cloud/custom Gateway URL override for SDK transports. |\n| `zone_id`                | Zone identifier.                                      |\n| `application_id`         | Application identifier.                               |\n| `app_client_secret_file` | Cloud/custom secret-file path override.               |\n| `app_client_secret`      | Inline local-development secret.                      |\n| `default_ttl_seconds`    | Default TTL for block-style Session calls.            |\n| `credentials[]`          | Resource audiences with optional upstream prefixes.   |\n| `optional_credentials[]` | Additional resource audiences and upstream prefixes.  |\n\nSDK credential entries use `resource` and optional `upstream_prefix`. SDKs do not read launcher fields or search the OS config directory. Set `CARACAL_CONFIG` to an explicit profile path.\n\n## Core Service Environment Keys\n\n| Key                                                                    | Services                                                    |\n| ---------------------------------------------------------------------- | ----------------------------------------------------------- |\n| `CARACAL_MODE`                                                         | All services.                                               |\n| `DATABASE_URL` / `DATABASE_URL_FILE`                                   | API, STS, Gateway, Audit, Coordinator.                      |\n| `REDIS_URL` / `REDIS_URL_FILE`                                         | API, STS, Gateway, Audit, Coordinator.                      |\n| `STREAMS_HMAC_KEY` / `STREAMS_HMAC_KEY_FILE`                           | Stream producers and consumers.                             |\n| `AUDIT_HMAC_KEY` / `AUDIT_HMAC_KEY_FILE`                               | Audit producers and Audit service.                          |\n| `IDEMPOTENCY_HMAC_KEY` / `IDEMPOTENCY_HMAC_KEY_FILE`                   | Coordinator idempotency receipt key digest.                 |\n| `IDEMPOTENCY_HMAC_KEY_PREVIOUS` / `IDEMPOTENCY_HMAC_KEY_PREVIOUS_FILE` | Coordinator during one receipt-retention rotation window.   |\n| `GATEWAY_STS_HMAC_KEY` / `GATEWAY_STS_HMAC_KEY_FILE`                   | API, STS, Gateway.                                          |\n| `SECRET_STORE_KEK` / `SECRET_STORE_KEK_FILE`                           | API and STS.                                                |\n| `SECRET_STORE_KEK_PREVIOUS` / `SECRET_STORE_KEK_PREVIOUS_FILE`         | API and STS during a master-key rotation window.            |\n| `CARACAL_SECRET_BACKEND`                                               | API and STS; selects the secret backend, default `builtin`. |\n| `CARACAL_ADMIN_TOKEN` / `CARACAL_ADMIN_TOKEN_FILE`                     | API and management clients.                                 |\n| `CARACAL_COORDINATOR_TOKEN` / `CARACAL_COORDINATOR_TOKEN_FILE`         | Coordinator and Console Session/Delegation views.           |\n\n## Deployment Values\n\nHelm values live under `infra/helm/caracal/values.yaml`. Compose environment and secrets are defined by `infra/docker/docker-compose.yml` and `infra/docker/runtime-compose.yml`.\n\n## Next Step\n\nUse [Configuration Order](/reference/config-precedence/) to understand which file, environment variable, or deployment value wins.\n\n## Related Pages\n\n- [Configure Workloads](/runtime-console/config-file/)\n- [Configure Service Environment](/operations/env-vars/)\n- [Choose a Cloud Profile](/operations/cloud-native-profiles/)"},{"id":"/reference/config-precedence","title":"Configuration Order","url":"https://docs.caracal.run/reference/config-precedence/","concepts":"","body":"## caracal run Workload Identity\n\n`caracal run` reads no profile file. It resolves only the workload identity:\n\n1. `CARACAL_WORKLOAD_ID` names the workload created on the console's Launcher page.\n2. The workload secret comes from `CARACAL_WORKLOAD_SECRET`, or `CARACAL_WORKLOAD_SECRET_FILE`, or (local dev and stable only) the owner-only file at `<Caracal config dir>/runtime/<workload_id>/secret`. Set at most one of the two variables.\n3. `CARACAL_STS_URL` overrides the STS endpoint; local dev and stable resolve it automatically.\n\nThe credential bindings, zone, scopes, and failure behavior come from the workload's launch bindings, authored in the web console and served by STS at launch.\n\n## SDK Profile Resolution\n\nSDK loaders use this order:\n\n1. `CARACAL_CONFIG`, when set. A missing file at that path is an error, not a fallthrough.\n2. Environment runtime config.\n\nSDKs do not read `./caracal.toml` from the current working directory.\n\nEnvironment config uses `CARACAL_ZONE_ID` and `CARACAL_APPLICATION_ID` with one\nexplicit credential source. SDKs read secret and manifest files only when named\nby `CARACAL_APP_CLIENT_SECRET_FILE` or an explicit profile field. Setting `CARACAL_ENV=production` requires explicit\nservice URLs. Cloud and custom deployments can provide\n`CARACAL_RESOURCES_FILE` values when mounted\nsecret or config paths differ from the local convention. A deployment without a\nclient secret can instead supply a pre-minted session mandate through\n`CARACAL_BOOTSTRAP_TOKEN`; the SDK rejects a bootstrap token that is already\nexpired at startup.\n\nResource bindings resolve from profile credentials, then\n`CARACAL_RESOURCES_FILE`, then `CARACAL_RESOURCES`. Later bindings with the\nsame resource ID override earlier bindings, so a short environment override can\nreplace one entry from a mounted JSON file without duplicating the file.\n\n## File-Secret Resolution\n\nServices support `*_FILE` variants for configured secret keys. File values are resolved before validation so deployment templates can mount secrets instead of placing sensitive material directly in environment variables.\n\n## Deployment Values\n\n| Deployment path | Precedence model                                                                                                |\n| --------------- | --------------------------------------------------------------------------------------------------------------- |\n| Docker Compose  | Shell environment overrides defaults in compose files; secrets are mounted from files.                          |\n| Helm            | CLI `--set` and later values files override earlier chart values; runtime Secret keys feed mounted service env. |\n\n## Troubleshooting\n\n| Symptom                                | Check                                                                                     |\n| -------------------------------------- | ----------------------------------------------------------------------------------------- |\n| Expected SDK profile is ignored        | Set `CARACAL_CONFIG` to the exact profile path; SDKs do not search default directories.   |\n| `caracal run` cannot find bindings     | Bindings live on the **Run** page in the web console, not in a local file.                |\n| Service fails with missing secret      | Confirm the `*_FILE` variable name is supported by that service and the file is readable. |\n| Helm values render unexpected defaults | Check values file order and CLI overrides.                                                |\n\n## Next Step\n\nUse [Defaults and Limits](/reference/defaults-and-limits/) to confirm ports, TTLs, request limits, and stream defaults."},{"id":"/reference/defaults-and-limits","title":"Defaults and Limits","url":"https://docs.caracal.run/reference/defaults-and-limits/","concepts":"","body":"## Ports\n\n| Component   | Port   |\n| ----------- | ------ |\n| API         | `3000` |\n| STS         | `8080` |\n| Gateway     | `8081` |\n| Audit       | `9090` |\n| Coordinator | `4000` |\n| Postgres    | `5432` |\n| Redis       | `6379` |\n\n## Token and Authority Lifetimes\n\n| Limit                                        | Default                                                     |\n| -------------------------------------------- | ----------------------------------------------------------- |\n| STS resource mandate cap                     | 15 minutes                                                  |\n| STS session mandate cap                      | 60 minutes                                                  |\n| STS `MAX_GRANT_TTL_SECONDS`                  | `3600`                                                      |\n| DCR application lifetime default and maximum | 3600 seconds                                                |\n| `caracal run` injected credential TTL        | 900 seconds                                                 |\n| Runtime approval wait                        | until the hold expires; 5-minute fallback when expiry is unavailable |\n| Step-up hold TTL                             | 1800 seconds default, clamped between 60 seconds and 7 days |\n| Step-up decision reason                      | 500 characters maximum                                      |\n| Gateway expiring-token preflight window      | 35 seconds                                                  |\n\n## Service Limits\n\n| Limit                             | Default             |\n| --------------------------------- | ------------------- |\n| API body limit                    | `1_048_576` bytes   |\n| API request timeout               | `30_000` ms         |\n| Gateway max request bytes         | 10 MiB              |\n| Gateway STS timeout               | 5 seconds           |\n| Gateway upstream timeout          | 30 seconds          |\n| Gateway non-stream write timeout  | 60 seconds          |\n| Gateway stream idle timeout       | 60 seconds per chunk |\n| Gateway STS circuit failure limit | 3 failures          |\n| Gateway STS circuit open window   | 10 seconds          |\n| STS `OPA_POLL_SECONDS`            | 60 seconds, max 300 |\n| Control body limit                | 64 KiB              |\n| Control rate capacity             | 60 per window       |\n| Control rate window               | 60 seconds          |\n| Control replay TTL                | 3600 seconds        |\n\n## SDK Timeouts and Retries\n\nThe SDKs share one timeout philosophy: **control-plane calls are always bounded, data-plane calls are bounded by you.** Control-plane operations (coordinator, STS) carry defaults because a hung control plane must never hang your worker; provider traffic through `transport()` carries no default because only the caller knows whether a request is a 200 ms lookup or a 10-minute LLM stream - pass `timeoutMs`, an `AbortSignal`/`timeout=`, or your HTTP client's own budget.\n\n| Operation                 | Default                              | Notes                                                                                                                                                                                                                                           |\n| ------------------------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Coordinator call          | 10 seconds                           | Per request, all SDKs.                                                                                                                                                                                                                          |\n| Session start             | up to 2 retries                      | 5xx/network only. The SDK generates one operation id before the first attempt; Coordinator durably replays the same creation response and rejects changed inputs. This protects Session creation, not callback execution or downstream effects. |\n| Delegation create         | 1 retry                              | Same durable creation-replay contract.                                                                                                                                                                                                          |\n| Retry backoff             | 250 ms escalating, jittered          | A server `Retry-After` wins, capped at 10 seconds.                                                                                                                                                                                              |\n| STS token exchange        | 30-second budget, up to 3 attempts   | Transient statuses only; policy denials never retry.                                                                                                                                                                                            |\n| TypeScript Control invoke | 30-second total budget               | Covers token mint, its one safe retry, and invoke; mutating invoke timeouts are outcome-ambiguous and never auto-retried.                                                                                                                       |\n| Approval wait             | 300-second default                   | Long-polls in chunks; `pending` on timeout means waiting again is safe.                                                                                                                                                                         |\n| Heartbeat renewal         | 10-second bound per tick             | Failures retry on the next tick; a session reported gone stops the timer and fires `onLeaseLost`.                                                    "},{"id":"/reference/runtime-exit-codes","title":"CLI Exit Codes","url":"https://docs.caracal.run/reference/runtime-exit-codes/","concepts":"","body":"Top-level `caracal` commands are limited to local runtime lifecycle, status, upgrade, purge, Console sign-in admission (`allowlist`), `run`, and web console launch.\n\n## Exit Behavior\n\n| Command                                                       | Success                                                                                              | Failure                                                                                                               |\n| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |\n| `caracal up`                                                  | `0` after stack start succeeds.                                                                      | Non-zero when Compose/build/start fails.                                                                              |\n| `caracal down`                                                | `0` after stack stop succeeds.                                                                       | Non-zero when Compose stop fails.                                                                                     |\n| `caracal status`                                              | `0` when health probes pass.                                                                         | `1` when service health fails.                                                                                        |\n| `caracal status --ready`                                      | `0` when readiness probes pass.                                                                      | `1` when dependency readiness fails.                                                                                  |\n| `caracal upgrade`                                             | `0` after images stage, migrations apply, services roll, and readiness passes.                       | Non-zero when image staging, migration, the roll, or the readiness gate fails.                                        |\n| `caracal purge`                                               | `0` after selected state is removed.                                                                 | Non-zero when cleanup fails.                                                                                          |\n| `caracal allowlist <add\\|remove\\|lock\\|unlock\\|list> [email]` | `0` after the entry change or listing succeeds, including no-op repeats.                             | `1` on an unknown subcommand, malformed entry, missing entry, locked entry on `add`, or an unreadable allowlist file. |\n| `caracal run -- <command>`                                    | Child process exit code after credentials are acquired; `128 + N` when the child dies from signal N. | `1` before spawn when config, credential exchange, or step-up fails; `127` when the command cannot be spawned.        |\n| `caracal web`                                                 | `0` after the web console exits cleanly.                                                             | Non-zero when launcher, port binding, build, session backend, or required stack checks fail.                          |\n\n## Structured Output\n\n`caracal status --json` emits machine-readable status. Web-console-owned diagnostics are available through the web console **Diagnostics** section and management command catalog, not as top-level runtime CLI workflows.\n\n## Next Step\n\nUse [Compatibility](/reference/compatibility/) before changing supported runtime, package manager, deployment, or docs build targets.\n\n## Related Pages\n\n- [Choose the Right Surface](/runtime-console/cli-and-console/)\n- [Start and Check the Stack](/runtime-console/stack/)\n- [Run Workloads](/runtime-console/runtime/)"},{"id":"/reference/compatibility","title":"Compatibility","url":"https://docs.caracal.run/reference/compatibility/","concepts":"","body":"## Language Runtimes\n\n| Area | Current target |\n| --- | --- |\n| TypeScript/Node packages | Node `>=22` where package engines are declared. |\n| Python packages | Python `>=3.12`. |\n| Go modules | Go `1.26` where modules declare a version. |\n| Root package manager | `pnpm@11.1.1`. |\n\n## Deployment Targets\n\n| Target | Source |\n| --- | --- |\n| Local development | `caracal up` and `infra/docker/docker-compose.yml`. |\n| Self-hosted Compose | `infra/docker/runtime-compose.yml` with versioned GHCR images. |\n| Kubernetes | `infra/helm/caracal`. |\n| Docs site | Astro `^6.3.1` and Starlight `0.39.2`. |\n\n## Platform Notes\n\n- Runtime and Console binaries are documented for Linux, macOS, and Windows launchers where release packaging provides them.\n- Local OSS ports must remain distinct from any enterprise product ports.\n- Published modes are `rc` and `stable`; both enforce stricter service config.\n\n## Version Pinning\n\nCaracal packages release in lockstep: every `@caracalai/*` npm package, `caracalai*` Python distribution, and Go module tag carries the same version, and cross-package wire contracts are only validated within one release. Pin every Caracal package in a service to one exact version and upgrade them together - a mixed set (for example an older `@caracalai/oauth` under a newer `@caracalai/sdk`) is unsupported and can fail in ways that look like platform errors. Lockfiles make this the default; when bumping, change every Caracal entry in the same commit.\n\n## Next Step\n\nUse [Release Map](/reference/release-package-runtime-map/) to map product versions to packages, images, and release surfaces.\n\n## Related Pages\n\n- [Release Map](/reference/release-package-runtime-map/)\n- [Deploy with Helm](/operations/kubernetes-helm/)\n- [Set Up Locally](/contributing/setup/)"},{"id":"/reference/release-package-runtime-map","title":"Release Map","url":"https://docs.caracal.run/reference/release-package-runtime-map/","concepts":"","body":"## Repository Release Surfaces\n\n| Surface                | Current source                                                      |\n| ---------------------- | ------------------------------------------------------------------- |\n| Root package manager   | `package.json` declares `pnpm@11.1.1`.                              |\n| Release scripts        | `scripts/release.sh`, `scripts/release.mjs`, `release.config.json`. |\n| Docker runtime compose | `infra/docker/runtime-compose.yml`.                                 |\n| Helm chart             | `infra/helm/caracal/Chart.yaml` and values files.                   |\n| Docs build             | `docs/package.json`.                                                |\n\n## Service Images\n\nCaracal ships two application images. Each runs multiple roles selected by the\ncontainer `command`; this keeps the trust boundary (separate processes/pods per\nrole) while cutting build, signing, and supply-chain surface.\n\n| Image name         | Roles               | Runtime |\n| ------------------ | ------------------- | ------- |\n| `caracal-node`     | API, Coordinator    | Node    |\n| `caracal-go`       | STS, Gateway, Audit | Go      |\n| `caracal-postgres` | Postgres            | -       |\n| `caracal-redis`    | Redis               | -       |\n\nRole selection:\n\n- `caracal-node` → `command: [\"/app/api/dist/main.js\"]` or `[\"/app/coordinator/dist/main.js\"]`.\n- `caracal-go` → `command: [\"/usr/local/bin/sts\"]`, `[\"/usr/local/bin/gateway\"]`, or `[\"/usr/local/bin/audit\"]`.\n\nRuntime Compose references `${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}<image>:v${CARACAL_VERSION}`.\n\n## Published Package Names\n\n| Area          | TypeScript              | Python                 | Go                                                       |\n| ------------- | ----------------------- | ---------------------- | -------------------------------------------------------- |\n| SDK           | `@caracalai/sdk`        | `caracalai-sdk`        | `github.com/garudex-labs/caracal/packages/sdk/go`        |\n| Identity      | `@caracalai/identity`   | `caracalai-identity`   | `github.com/garudex-labs/caracal/packages/identity/go`   |\n| OAuth         | `@caracalai/oauth`      | `caracalai-oauth`      | `github.com/garudex-labs/caracal/packages/oauth/go`      |\n| Revocation    | `@caracalai/revocation` | `caracalai-revocation` | `github.com/garudex-labs/caracal/packages/revocation/go` |\n| Verify engine | `@caracalai/verify`     | `caracalai-verify`     | `github.com/garudex-labs/caracal/packages/verify/go`     |\n\n## Versioning Notes\n\n- Pin exact image tags in production values.\n- Run migration and readiness validation after image or chart changes.\n- Do not use product-management runtime CLI aliases as release surfaces; broad automation belongs to the Admin API and zone-scoped dispatch belongs to the Control API.\n\n## Next Step\n\nUse [Wire Contracts](/reference/interoperability-contracts/) when validating SDK, adapter, exporter, or provider-plugin compatibility."},{"id":"/reference/interoperability-contracts","title":"Wire Contracts","url":"https://docs.caracal.run/reference/interoperability-contracts/","concepts":"","body":"Caracal publishes schema files under `docs/public/schemas/` and test fixtures under `tests/shared/fixtures/interoperability/`.\n\n## Schema Files\n\n| Contract                            | Schema                                                               |\n| ----------------------------------- | -------------------------------------------------------------------- |\n| Agent connector manifest            | `caracal-agent-connector-manifest-2026-05-21.schema.json`            |\n| Audit event                         | `caracal-audit-event-2026-05-21.schema.json`                         |\n| Audit exporter manifest             | `caracal-audit-exporter-manifest-2026-05-21.schema.json`             |\n| Gateway upstream manifest           | `caracal-gateway-upstream-manifest-2026-05-21.schema.json`           |\n| JWT claims                          | `caracal-jwt-claims-2026-05-21.schema.json`                          |\n| Policy input                        | `caracal-policy-input-2026-05-20.schema.json`                        |\n| Policy pack manifest                | `caracal-policy-pack-manifest-2026-05-21.schema.json`                |\n| Policy result                       | `caracal-policy-result-2026-05-20.schema.json`                       |\n| Provider credential plugin manifest | `caracal-provider-credential-plugin-manifest-2026-05-21.schema.json` |\n| Resource verifier manifest          | `caracal-resource-verifier-manifest-2026-05-21.schema.json`          |\n| Revocation event                    | `caracal-revocation-event-2026-05-21.schema.json`                    |\n| Token response                      | `caracal-token-response-2026-05-21.schema.json`                      |\n| W3C baggage                         | `caracal-w3c-baggage-2026-05-21.schema.json`                         |\n\n## Fixture Files\n\nFixtures include valid examples for audit events, audit exporter manifests, Gateway upstream manifests, JWT claims, policy input/results, policy pack manifests, provider credential plugins, resource verifier manifests, revocation events, token responses, W3C baggage, trace context headers, and stream signature canonicalization vectors.\n\n## Usage\n\n- Use schemas when building adapters, exporters, or provider plugins.\n- Use fixtures when adding SDK or interoperability tests.\n- Preserve schema filenames when documenting versioned contracts.\n\n## Next Step\n\nUse [Compare Editions](/enterprise/) when deciding between self-hosted Community Edition and managed Enterprise Edition.\n\n## Related Pages\n\n- [Use Event Topics](/api/event-topics/)\n- [Verify Package](/sdks/verify/)\n- [Validate Changes](/contributing/testing/)"},{"id":"/enterprise","title":"Compare Editions","url":"https://docs.caracal.run/enterprise/","concepts":"","body":"The open-source **Community Edition** gives you the complete authority model - mandates, delegation, policy, and audit - free to self-host under Apache 2.0. The commercial **Enterprise Edition** from Garudex Labs runs it for you: managed multi-tenant operation, a hosted control plane, single sign-on, and a fully managed data plane on top of the same authority model. Everything in this documentation describes the Community Edition unless marked otherwise.\n\nThe two editions share one model, one mandate format, and one set of SDKs. Enterprise does not fork the concepts - it removes the operational work of running and scaling them yourself.\n\n:::note[FAQ]\n[Which features are Enterprise Edition only?](/reference/faq/#faq-019)\n:::\n\n:::tip[Talk to us]\nFor pricing, a demo, or a deployment fit assessment, [book an enterprise call](https://cal.com/rawx18/caracal-enterprise-sales).\n:::\n\n## Edition Comparison\n\n| Capability                                           | Community Edition (Apache 2.0)             | Enterprise Edition (commercial)                                 |\n| ---------------------------------------------------- | ------------------------------------------ | --------------------------------------------------------------- |\n| Authority model, mandates, delegation, policy, audit | Included                                   | Included                                                        |\n| SDKs and adapters (TypeScript, Python, Go)           | Included                                   | Included                                                        |\n| Zones as a manual isolation primitive                | Included                                   | Included                                                        |\n| Managed multi-tenancy                                | Self-modeled with zones and the Admin API  | Native tenant, organization, and workspace lifecycle            |\n| Single sign-on (SSO)                                 | Not included                               | SAML and OIDC SSO with SCIM provisioning                        |\n| Teams and role-based access control                  | Not included                               | Org, team, and role management across tenants                   |\n| Control surface                                      | Local Console and self-hosted Admin API    | Hosted management UI for all tenants                            |\n| Gateway and services                                 | You deploy and operate every service       | Fully managed Gateway, STS, Coordinator, and audit plane        |\n| Integration effort                                   | Run the full stack, then integrate the SDK | Integrate the SDK against managed endpoints; no services to run |\n| Support                                              | Community and issues                       | Commercial SLA, priority support, and onboarding                |\n\n## Enterprise Additions\n\n### Managed multi-tenancy\n\nThe Community Edition treats a [zone](/concepts/zone/) as a manual isolation boundary, and you map tenants onto zones yourself as described in [Model Your Application in Caracal](/guides/modeling-recipes/). The Enterprise Edition provides a managed tenancy layer with first-class tenants, organizations, and workspaces, including provisioning, lifecycle, and isolation guarantees handled for you instead of automated by hand through the Admin API.\n\n### Hosted management UI\n\nThe Community Edition is operated through the local Console and the self-hosted Admin API. The Enterprise Edition replaces the local Console with a hosted management UI that administers every tenant, application, resource, provider, policy set, session, and audit stream from one place, with role-aware access for your teams.\n\n### Managed data plane\n\nIn the Community Edition you deploy and operate the Gateway, STS, Coordinator, audit, and supporting stores yourself. In the Enterprise Edition these run as a managed data plane: the Gateway and every service are operated, scaled, and upgraded for you. Your application work reduces to integrating the SDK against managed endpoints - there are no Caracal services for your team to host, patch, or scale.\n\n### Single sign-on and team access\n\nThe Enterprise Edition adds SAML and OIDC single sign-on with SCIM user provisioning, plus organizations, teams, and role-based access control so administrators and reviewers get scoped access across tenants. None of this is part of the open-source edition.\n\n### Commercial support and assurances\n\nEnterprise customers receive a support SLA, priority response, guided onboarding, and the commercial agreement terms covering warranty, indemnification, and enterprise integrations.\n\n## Licensing\n\nThe Community Edition is governed by the Apache 2.0 license and is free to use, modify, and self-host. Enterprise Components are proprietary and require a written commercial subscription from Garudex Labs. Using the managed multi-tenancy, hosted UI, managed data plane, or SSO components without that agreement is not permitted under Apache 2.0.\n\n## Get Enterprise\n\n- [Book an enterprise call](https://cal.com/rawx18/caracal-enterprise-sales) for pricing, a demo, or a deployment fit assessment.\n- Email `support@caracal.run` for licensing and procurement questions.\n- Visit [garudexlabs.com](https://www.caracal.run) for company and product details.\n\n## Related Pages\n\n- [Compliance and Operations Readiness](/enterprise/compliance-readiness/)\n- [Zones](/concepts/zone/)\n- [Model Your Application in Caracal](/guides/modeling-recipes/)\n- [Authority and Enforcement](/concepts/authority-model/)"},{"id":"/contributing","title":"Contribute to Caracal","url":"https://docs.caracal.run/contributing/","concepts":"","body":"Caracal uses a multi-language workspace: TypeScript apps/packages, Go services/packages, Python packages/examples, Docker/Helm infra, and Astro docs.\n\n## Contributor Path\n\n| Need | Page |\n| --- | --- |\n| Prepare your machine | [Set Up Locally](/contributing/setup/) |\n| Learn project boundaries and naming conventions | [Follow Project Standards](/contributing/style/) |\n| Work on an issue or pull request | [Make a Change](/contributing/workflow/) |\n| Run the right checks | [Validate Changes](/contributing/testing/) |\n\n## Maintainer Path\n\n| Need | Page |\n| --- | --- |\n| Understand review, ownership, and security process | [Understand Governance](/contributing/governance/) |\n| Prepare, publish, or recover a release | [Release Caracal](/contributing/release/) |\n\n## Current Toolchain\n\n| Tool | Version |\n| --- | --- |\n| Node.js | 24+ |\n| pnpm | 11.1.1 |\n| Docker + Compose v2 | 24+ |\n| Go | 1.26+ |\n| Python | 3.14+ |\n| Bun | latest stable used by package scripts |\n\n## Before You Start\n\nRead [Follow Project Standards](/contributing/style/) before editing code, docs, commands, or product boundaries.\n\n## Next Step\n\nStart with [Set Up Locally](/contributing/setup/) before making source changes."},{"id":"/contributing/setup","title":"Set Up Locally","url":"https://docs.caracal.run/contributing/setup/","concepts":"","body":"## Prerequisites\n\n| Tool | Version |\n| --- | --- |\n| Node.js | 24+ |\n| pnpm | 11.1.1 |\n| Docker + Compose v2 | 24+ |\n| Go | 1.26+ |\n| Python | 3.14+ |\n| Bun | latest stable used by package scripts |\n\n## Install Dependencies\n\n```bash\ngit clone https://github.com/Garudex-Labs/caracal.git\ncd caracal\npnpm install\n```\n\n`pnpm install` is the standard dependency install command for the Node workspace. Run `pnpm run setup` when you need the full cross-platform developer environment: it runs `pnpm install`, downloads Go module dependencies, creates `.venv`, installs Python test and style tools, and installs local Python packages in editable mode.\n\nRead [CONTRIBUTING.md](https://github.com/Garudex-Labs/caracal/blob/main/CONTRIBUTING.md) in the repository root for the contributor policy, review expectations, and testing requirements.\n\n## Start the Local Stack\n\n```bash\npnpm secrets:init\npnpm caracal up\npnpm caracal status --ready\n```\n\nThe local stack uses `infra/docker/docker-compose.yml`, builds local service images, and binds service ports to loopback.\n\n## Open the Web Console\n\n```bash\npnpm caracal web\n```\n\nUse the web console for zones, applications, providers, resources, policies, Authority records, Sessions, audit, Delegation, diagnostics, and Control. Do not expect top-level runtime CLI commands for product management.\n\n## Stop or Reset\n\n```bash\npnpm caracal down\npnpm caracal purge\n```\n\nUse `purge` only when you intentionally want to remove local stack/runtime state.\n\n## Next Step\n\nRead [Follow Project Standards](/contributing/style/) before editing source files.\n\n## Related Pages\n\n- [Runtime and Web Console](/runtime-console/)\n- [Deploy with Docker Compose](/operations/docker-compose/)\n- [Validate Changes](/contributing/testing/)"},{"id":"/contributing/style","title":"Follow Project Standards","url":"https://docs.caracal.run/contributing/style/","concepts":"","body":"Caracal style favors small, explicit boundaries and source-aligned documentation.\n\n## Language Style Guides\n\n| Language                  | Required style                                                                        | Enforcement                                                                     |\n| ------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |\n| TypeScript and JavaScript | TypeScript Handbook style plus the repository Prettier profile in `.prettierrc.json`. | `pnpm run style` checks changed TS/JS source files with Prettier.               |\n| Go                        | Effective Go with canonical `gofmt` formatting.                                       | `pnpm run style` checks changed Go source files with `gofmt -l`.                |\n| Python                    | PEP 8 layout as formatted by Ruff.                                                    | `pnpm run style` checks changed Python source files with `ruff format --check`. |\n\nThe pre-commit hook activated by `pnpm install` formats staged files automatically on every commit, and `pnpm run style:fix` formats changed files on demand. The gate always runs the Ruff version pinned in `scripts/pythonStyleRequirements.in`, so local formatting matches CI exactly. Pull requests run the same changed-file style gate automatically for primary-language source files.\n\n## Code Conventions\n\n| Convention                  | Apply it                                                                                                                                                      |\n| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Keep changes focused        | Avoid unrelated refactors in feature or docs PRs.                                                                                                             |\n| Prefer explicit validation  | Fail closed on auth, policy, config, stream, and key errors.                                                                                                  |\n| Preserve product boundaries | Do not couple open-source code to enterprise-only code.                                                                                                       |\n| Respect command ownership   | Runtime CLI is lifecycle and setup; Console is the human surface, the Admin API is the broad automation surface, and the Control API is zone-scoped dispatch. |\n| Use existing shared layers  | Reuse core config, errors, crypto, logging, engine dispatch, and SDK helpers.                                                                                 |\n\n## Documentation Conventions\n\n| Page type    | Pattern                                                               |\n| ------------ | --------------------------------------------------------------------- |\n| Landing      | Purpose, audience, map, recommended reading path.                     |\n| Workflow     | Prerequisites, steps, validation, troubleshooting, related links.     |\n| Reference    | Exact names, defaults, tables, examples, source-of-truth links.       |\n| Architecture | Diagram, component responsibilities, flow, boundaries, related pages. |\n\nDo not use docs to preserve stale command names, screenshots, package names, or workflows. Update the whole affected page coherently.\n\n## Project Boundaries\n\n- Top-level `caracal` commands are limited to runtime lifecycle, setup, and `caracal run`.\n- Product-management workflows for zones, policies, grants, audit, Sessions, Delegation, and Control belong in Console, Admin SDK, or Control API docs.\n- Open-source code must not import, reference, or depend on enterprise-only code.\n- Security-sensitive findings must follow [Report a Vulnerability](/security/disclosure/), not public issues.\n\n## Naming\n\nUse canonical terms from [Glossary](/reference/glossary/): zone, principal, resource, grant, policy, policy set, mandate, Session, Delegation, audit ledger, Console, Control API, STS, Gateway.\n\n## Next Step\n\nUse [Make a Change](/contributing/workflow/) to plan and submit a focused pull request."},{"id":"/contributing/workflow","title":"Make a Change","url":"https://docs.caracal.run/contributing/workflow/","concepts":"","body":"## Standard Flow\n\n1. Start from `main`.\n2. Keep the change focused on one component or workflow.\n3. Read the `instructions.md` file in the directory you are editing. Every service, app, and package directory has one; it lists the required and forbidden patterns for that area.\n4. Update docs when behavior, APIs, commands, config, examples, or operations change.\n5. Run targeted tests first, then broader checks when shared boundaries changed.\n6. Open a pull request with the problem, solution, validation, and risk notes.\n\n## Choose the Area\n\n| Area              | Common sources                                                          |\n| ----------------- | ----------------------------------------------------------------------- |\n| Runtime CLI       | `apps/runtime`, `packages/engine`, runtime tests.                       |\n| Web console       | `apps/web`, Admin SDK, web console tests.                               |\n| API               | `apps/api`, migrations, Admin package, API tests.                       |\n| Coordinator       | `apps/coordinator`, Coordinator tests, SDK tests.                       |\n| STS/Gateway/Audit | `services/*`, Go tests, operations docs.                                |\n| SDKs/adapters     | `packages/*`, language-specific tests, interoperability fixtures.       |\n| Infra             | `infra/docker`, `infra/helm`, Postgres/Redis scripts.                   |\n| Docs              | `docs/src/content/docs`, `docs/astro.config.mjs`, generated docs pages. |\n\n## Command Boundary\n\nDo not add top-level runtime CLI commands for zones, policies, grants, audit, Sessions, Delegation, or Control. Human workflows belong to the web console; broad automation uses the Admin API, and zone-scoped dispatch uses the Control API.\n\n## Security Reports\n\nDo not discuss suspected vulnerabilities in public issues. Use GitHub private advisories or the email path in [Report a Vulnerability](/security/disclosure/).\n\n## Next Step\n\nUse [Validate Changes](/contributing/testing/) to choose the narrowest useful test command before opening a pull request."},{"id":"/contributing/testing","title":"Validate Changes","url":"https://docs.caracal.run/contributing/testing/","concepts":"","body":"Run the smallest relevant suite first, then broaden when a change touches shared code, security boundaries, generated artifacts, or deployment behavior.\n\n## Testing Policy\n\nThis policy is mandatory and is enforced during review:\n\n- Major new functionality MUST add automated tests covering that functionality, in the same change that introduces it.\n- Every bug fix MUST add a regression test that fails without the fix and passes with it.\n- Reviewers MUST confirm the required tests exist and run in CI before approving; pull requests that omit them are not merged.\n\n## Root Commands\n\n| Command | Purpose |\n| --- | --- |\n| `pnpm run build:typescript` | Build TypeScript apps and packages. |\n| `pnpm run lint` | Run package linters where present. |\n| `pnpm run typecheck` | Run TypeScript type checks. |\n| `pnpm run test` | Full TypeScript, Go, and Python test suite. |\n| `pnpm run test:typescript` | TypeScript app/package tests. |\n| `pnpm run test:go` | Go service/package and interoperability tests. |\n| `pnpm run test:python` | Python package tests. |\n| `pnpm run ci` | Build, lint, typecheck, and test sequence. |\n\n## Targeted Examples\n\n| Area | Command |\n| --- | --- |\n| Runtime CLI | `pnpm --dir apps/runtime test` |\n| Web console | `pnpm --dir apps/web test` |\n| Web backend-for-frontend | `pnpm --dir apps/auth test` |\n| API | `pnpm --dir apps/api test` |\n| Coordinator | `pnpm --dir apps/coordinator test` |\n| STS | `go test ./services/sts/...` |\n| Gateway | `go test ./services/gateway/...` |\n| Audit | `go test ./services/audit/...` |\n| Docs | `pnpm --dir docs build` |\n\nRunnable examples live in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) and carry their own test suites and CI.\n\n## CI Mirror\n\n```bash\nscripts/testCi.sh\nscripts/testCi.sh --smoke\nscripts/testCi.sh --go\nscripts/testCi.sh --py\nscripts/testCi.sh --ts\n```\n\nUse broader checks when a change affects auth, crypto, config, release, infra, shared packages, SDK contracts, or interoperability schemas.\n\n## Next Step\n\nAfter validation, review [Understand Governance](/contributing/governance/) for contribution scale, review ownership, and private security process."},{"id":"/contributing/governance","title":"Understand Governance","url":"https://docs.caracal.run/contributing/governance/","concepts":"","body":"Caracal is maintained by Garudex Labs. Maintainers listed in `.github/MAINTAINERS` are the primary decision-makers for project areas, reviews, triage, standards, and releases.\n\n## Contribution Scale\n\n| Change size | Expected process |\n| --- | --- |\n| Small focused fix | Pull request with clear validation. |\n| Medium bug or feature | GitHub issue with context and expected outcome before implementation. |\n| Large or cross-cutting change | Proposal with problem statement, alternatives, trade-offs, open questions, and smaller sub-issues. |\n| Security-sensitive change | Private security process and maintainer coordination. |\n\n## Maintainer Responsibilities\n\n- Review changes in owned areas.\n- Enforce repository standards and product boundaries.\n- Keep security reports private.\n- Approve releases and release workflow changes.\n- Preserve open-source and enterprise product isolation.\n\n## Code Review Requirements\n\nEvery change is proposed as a pull request and reviewed before merge or release.\n\n| Requirement | Expectation |\n| --- | --- |\n| Independent review | At least one maintainer other than the author approves each pull request; authors do not approve or merge their own changes. |\n| Area ownership | `.github/CODEOWNERS` owners are requested automatically for their paths. |\n| What reviewers check | Correctness and edge cases, focused scope, Testing Policy compliance with passing CI, the `pnpm run style` gate, input validation and trust boundaries, secret hygiene, OSS/enterprise isolation, and updated docs. |\n| Acceptance bar | One approving non-author review, all required CI checks green, resolved comments, and a judgment that the change is worthwhile and free of known disqualifying defects. |\n| Release approval | Stable releases require `release-approval` from a maintainer other than the release preparer. |\n\nThe full contributor-facing policy lives in [`./CONTRIBUTING.md`](https://github.com/Garudex-Labs/caracal/blob/main/CONTRIBUTING.md#code-review).\n\n## Community Standards\n\nThe project follows the repository [Code of Conduct](https://github.com/Garudex-Labs/caracal/blob/main/.github/CODE_OF_CONDUCT.md). Harassment, private-information disclosure, and disruptive behavior are not acceptable.\n\n## Security Governance\n\nSecurity concerns must be reported through [Report a Vulnerability](/security/disclosure/). Public issues are not appropriate for vulnerabilities, credential exposure, unsafe execution, or exploitable operational failures.\n\n## Next Step\n\nMaintainers preparing a cut should use [Release Caracal](/contributing/release/).\n\n## Related Pages\n\n- [Make a Change](/contributing/workflow/)\n- [Review the Threat Model](/security/threat-model/)\n- [Respond to Incidents](/operations/incident-response/)"},{"id":"/contributing/release","title":"Release Caracal","url":"https://docs.caracal.run/contributing/release/","concepts":"","body":"Every Caracal release artifact shares one Semantic Version: `product.version` in `release.config.json`. Release candidates append `-rc.N`; `scripts/release.sh stamp` propagates the version into every package manifest, image reference, chart, and install pin.\n\n## Release Surfaces\n\n| Surface | Source |\n| --- | --- |\n| Product version | `release.config.json` `product.version`, currently `0.2.0`. |\n| Runtime binary | `apps/runtime/dist/caracal-*`. |\n| Containers | API, STS, Gateway, Audit, Coordinator, Control, Postgres, Redis, Runtime. |\n| Helm | `infra/helm/caracal`. |\n| npm packages | Public `@caracalai/*` packages from `release.config.json`. |\n| PyPI packages | Public `caracalai-*` packages from `release.config.json`. |\n\n## Build Targets\n\nRuntime release builds compile Linux x64/arm64, macOS x64/arm64, and Windows x64 binaries through Bun compile scripts.\n\nGo-based service container builds strip debug symbols by default (`GO_LDFLAGS` defaults to `-s -w`) and honor native build arguments passed to Docker: `CGO_ENABLED`, `CC`, `CFLAGS`, `CXX`, `CXXFLAGS`, `LDFLAGS`, `GOFLAGS`, `GO_BUILDFLAGS`, and `GO_LDFLAGS`. The Dockerfiles add `-mod=readonly` and `-trimpath`; override `GO_LDFLAGS` for diagnostic builds that need symbol tables.\n\n## Release Flow\n\n| Stage | Command pattern |\n| --- | --- |\n| RC prepare | Set `product.version` to `X.Y.Z-rc.N`, then `scripts/release.sh rc prepare` |\n| RC dry run | `scripts/release.sh rc dry-run --local` |\n| Stable stamp | Set `product.version` to `X.Y.Z`, then `scripts/release.sh stamp` and commit |\n| Stable dry run | `scripts/release.sh stable --dry-run` |\n| Stable publish | `scripts/release.sh stable` |\n| Promote an rc | `scripts/release.sh promote --from vX.Y.Z-rc.N` |\n\nProtected GitHub workflows publish npm and PyPI packages. Every release publishes all publishable packages at the shared version; `package=<name>` exists only to resume a partially failed publish. Local stable publishing requires explicit approval and environment override.\n\n## Rollback Rule\n\nDo not delete published tags. Roll forward with a new SemVer tag. Pinned `vX.Y.Z` tags are immutable; the floating `vX.Y` series tag moves with the new cut.\n\n## Registry Cleanup After the SemVer Cutover\n\nThe migration from CalVer to lockstep SemVer leaves registry state that only a maintainer with registry access can retire. Complete these steps manually after the first `v0.2.0` stable release is live:\n\n1. Deprecate dead npm names with a pointer to their successors — never unpublish:\n   ```bash\n   npm deprecate @caracalai/transport-mcp@'*' 'renamed: use the current @caracalai packages'\n   npm deprecate @caracalai/transport-a2a@'*' 'renamed: use the current @caracalai packages'\n   npm deprecate @caracalai/mcp-express@'*' 'renamed: use @caracalai/express'\n   npm deprecate @caracalai/mcp-fastmcp@'*' 'renamed: use @caracalai/fastmcp'\n   npm deprecate @caracalai/tokenstate-postgres@'*' 'renamed: use the current @caracalai packages'\n   ```\n2. Deprecate the pre-0.2.0 CalVer and `0.1.x` versions on live npm names the same way.\n3. Yank (PEP 592) the pre-0.2.0 versions on live PyPI projects and all versions of dead-name PyPI projects. Yanking keeps files installable by exact pin while removing them from resolution.\n4. Delete the CalVer `ghcr.io` image tags (`v2026.*`, `2026.*`) and CalVer OCI Helm chart versions. CalVer sorts above SemVer, so any surviving `2026.*` tag would outrank `0.2.0` in version resolution.\n5. Mark pre-0.2.0 GitHub Releases as pre-release with a note pointing to the current release line. Do this only after `v0.2.0` is published so `releases/latest` never resolves to nothing.\n6. Keep all git tags: deleting them breaks reproducibility and Go module caching.\n7. Refresh the `lynxCapital` dependency pins in the [examples repository](https://github.com/Garudex-Labs/examples) to the published `0.2.0` packages.\n\n## Related Pages\n\n- [Release Map](/reference/release-package-runtime-map/)\n- [Upgrade Caracal](/operations/upgrade/)"},{"id":"/404","title":"Page not found","url":"https://docs.caracal.run/404/","concepts":"","body":"import LandingFooter from '../../components/LandingFooter.astro'\n\n<div class=\"notfound\">\n  <div class=\"notfound__inner\">\n    <p class=\"notfound__eyebrow\">Error · 404</p>\n    <h1 class=\"notfound__title\">This docs path is not available.</h1>\n    <p class=\"notfound__lede\">\n      The page may have moved during the documentation refresh, or the link may\n      point to an older route. Use search, start from the docs home, or open the\n      onboarding path below.\n    </p>\n    <div class=\"notfound__actions\">\n      <a class=\"landing-button landing-button--primary\" href=\"/\">Go to docs home</a>\n      <a class=\"landing-button landing-button--secondary\" href=\"/get-started/\">Start onboarding</a>\n    </div>\n  </div>\n</div>\n\n<LandingFooter />"},{"id":"/blog/caracal-joins-lfx-mentorship","title":"Caracal Joins LFX Mentorship","url":"https://docs.caracal.run/blog/caracal-joins-lfx-mentorship/","concepts":"","body":"import BlogPost from '../../../components/BlogPost.astro'\nimport LandingFooter from '../../../components/LandingFooter.astro'\n\n<BlogPost title={frontmatter.title} post={frontmatter.post}>\n\nOpen source does not grow by code alone. It grows when people contribute, learn, review, document, and help others build with confidence. That is the real value of mentorship in open source.\n\nCaracal is a security-first AI authority, delegation, and identity system for autonomous agents. As the project evolves, one of the most important priorities is making it easier for developers to integrate, understand, and operate Caracal in real-world environments. That is why participating in LFX Mentorship is such a strong fit.\n\n## Real work, not a classroom\n\nLFX Mentorship gives contributors the chance to work on real open source projects with guidance from maintainers and mentors. It is not a classroom exercise. It is hands-on work inside an active project, with real problems, real collaboration, and real outcomes.\n\nFor mentees, the value is practical experience. They learn how open source projects are built, maintained, and sustained. They gain exposure to code review, documentation, workflows, communication, and the day-to-day reality of contributing to a project that serves a broader community. They also learn how to think beyond features and focus on maintainability, usability, and long-term project health.\n\n## What the project gets\n\nFor projects like Caracal, the value is equally important. Mentorship brings fresh perspective, additional contributors, and focused effort on areas that directly improve adoption. In this mentorship, the emphasis is on developer experience and ease of integration and operation. That means making Caracal clearer, smoother, and easier to work with, while keeping its security-first foundation intact.\n\nThis year, Caracal's LFX Mentorship project includes Ashutosh Singh and Mohammad Haider Ali. Their work is part of a larger effort to strengthen the project, improve the contributor experience, and show how open source software is sustained over time through collaboration and shared ownership.\n\n## Investing in the ecosystem\n\nOpen source projects survive when people invest in them. Mentorship is one of the best ways to do that. It helps new contributors grow, helps projects improve, and helps communities stay strong.\n\nCaracal is proud to take part in LFX Mentorship and continue building with the open source ecosystem.\n\n</BlogPost>\n\n<LandingFooter />"},{"id":"/blogs","title":"Blogs","url":"https://docs.caracal.run/blogs/","concepts":"","body":"import BlogsDirectory from '../../components/BlogsDirectory.astro'\nimport LandingFooter from '../../components/LandingFooter.astro'\n\n<BlogsDirectory />\n\n<LandingFooter />"},{"id":"/concepts/operator","title":"Caracal Operator","url":"https://docs.caracal.run/concepts/operator/","concepts":"","body":"Caracal Operator lets you operate the control plane by describing what you want instead of navigating each console page by hand. It turns intent into a reviewed plan, then applies it through the same guarded APIs you use manually - within your operator scope and recorded in the audit log. Every change it makes is one you could have made yourself; it introduces no new authority.\n\n## Why It Exists\n\nMost control-plane work is a sequence of small, related changes: register an application, connect a provider, define a resource and its scopes, then activate the policy that ties them together. The Operator collapses that into a described outcome while keeping every safety property - validation, least privilege, approval, and audit - in the platform rather than the model.\n\nThe Operator works against a **capability catalog**: the set of control-plane actions it can take, grouped by the objects you operate - zones, applications, providers, resources, access, and policy. Each capability is classified as read-only or state-changing, so a request that only inspects state is always distinguishable from one that changes it.\n\n## The Governed Lifecycle\n\nA change never applies directly from natural language. Within a session the Operator follows a fixed lifecycle, and the language model only ever produces a draft that enters it:\n\n1. **Propose.** Intent becomes a plan whose every step is validated against the capability catalog. A step that names an unknown action or invalid arguments is rejected before anything runs.\n2. **Preview.** The plan is resolved against your live state as a read-only dry run, so each step is marked as a create, an update, a no-op, or blocked when a referenced object is missing. Nothing is written.\n3. **Decide.** You approve or reject the plan. A plan is decided once, and only an approved plan is eligible to apply.\n4. **Apply.** An approved plan is re-validated and re-previewed, then executed step by step. A plan applies only once, and any secret it produces is surfaced in the apply response, never written to the conversation or the audit log; issued credentials stay retrievable from Secret Store custody through the owning object's audited reveal.\n\n## Authority and Isolation\n\nThe Operator runs under its own reserved identity, distinct from the human operator who approves a plan. When a plan applies, your approval is recorded in the admin audit log, while the execution record names the Operator's reserved principal as the authority that made the change - a delegated, attenuated form of your own authority.\n\nThat delegated authority is least-privilege: the Operator may execute only the capabilities explicitly granted to it. A plan that asks for a capability outside its grant is refused as forbidden, before execution. The Operator is also bounded by zone isolation - it will not open a session in, or execute against, a [system zone](/concepts/zone/#system-zone), keeping its authority away from the infrastructure that runs Caracal itself.\n\n## Ask and Agent Modes\n\nEvery conversation runs in one of two modes, enforced by Caracal and never chosen by the model:\n\n| Mode | What the Operator can do |\n| --- | --- |\n| Agent | Answer questions, read state, and propose plans that apply changes after your approval. |\n| Ask | Strictly read-only: explain, investigate, and diagnose. It never produces a plan and cannot apply anything. |\n\nAsk mode is enforced in two independent places - the planning skill is never selected, and the change endpoints refuse outright - so a read-only conversation is provably write-incapable.\n\n## Autopilot\n\nIn agent mode you can engage **autopilot**, which lets Caracal auto-satisfy the approval step for every plan in the conversation. Engaging it is an explicit opt-in to acting without a human in the loop; it is off by default and per conversation, and a platform-level master switch must also be on. Auto-approval never widens authority - the governed execute path still enforces the capability allowlist, the least-privilege executor token, and zone isolation on every apply. Autopilot defers while a plan still needs credentials from the console's secure prompt and stops when a preview shows the plan cannot apply. A deployment can also bound an engaged conversation with a **write budget**: once the cumulative auto-approved write operations would exceed it, autopilot pauses, records the pause in the conversation ledger, and the plan waits for explicit human approval. The master switch is a single kill switch that stops all auto-approval on the next turn.\n\n## Authoring Policy\n\nPolicy is the densest control-plane object to write by hand: a decision rests on grant, binding, and confinement data that must parse as valid Rego and satisfy the platform decision contract. The Operator includes a dedicated policy author for exactly this. Describe the access you want - which application owns a resource, which roles hold which scopes, how to confine a label - and it drafts the matching [data documents](/concepts/policy/), explains each one, and reports the least-privilege posture, the risks it detected, ready-to-run simulations, and activation readiness.\n\nEvery draft is validated and previewed against the same contract the platform enforces, so a document the Operator emits is already contract-valid; if it cannot produce a valid document it fails closed rather than returning broken Rego. A draft is not a change. Turning one into a policy runs the ordinary [governed lifecycle](#the-governed-lifecycle) - you review the proposed create, approve it, and the create is re-validated on apply and attributed to you. Policies authored this way carry provenance marking them AI-assisted, and their later version, simulation, and activation steps stay under the same review and audit as any other policy work.\n\n## Natural-Language Provider\n\nTurning your words into a plan needs a language model, and that model is something you supply. The Operator is built on the [Vercel AI SDK](https://ai-sdk.dev) and tal"},{"id":"/concepts/provider","title":"Providers","url":"https://docs.caracal.run/concepts/provider/","concepts":"","body":"A provider answers one question: after Caracal approves a call, what credential does the upstream target receive? Every Gateway-routed resource binds exactly one provider. One provider can serve many resources.\n\n## Why Providers Exist\n\nCallers never hold upstream credentials. The agent presents a Caracal mandate; Gateway verifies it, then attaches the upstream credential the provider describes. Provider secrets are sealed at creation and are not returned by list or detail APIs.\n\n## Auth Modes\n\n| Mode | Upstream receives | Use when |\n| --- | --- | --- |\n| None | No credential. | Gateway is the enforcement point and the upstream expects nothing. |\n| Caracal mandate | The Caracal mandate as a bearer token. | The upstream verifies Caracal tokens itself with a verifier or adapter. |\n| OAuth 2.0 authorization code | A consented upstream account's token. | The upstream needs delegated account consent. |\n| OAuth 2.0 client credentials | A service-to-service token. | The upstream uses machine-to-machine OAuth, via the standard grant or an RFC 7523 signed-assertion grant such as a Google service account. |\n| API key | A static key in a configured header. | The upstream uses vendor API keys. |\n| Bearer token | A static pre-issued token. | The upstream expects a fixed bearer credential Caracal does not mint. |\n| HTTP Basic | `Authorization: Basic` from a username and sealed password. | The upstream authenticates with a username/password or username/API-token pair. |\n\n## How a Provider Is Used\n\nGateway strips the caller's authorization and forwards the provider credential instead. For OAuth modes, STS obtains and refreshes the upstream tokens; delegated consent is stored as a provider connection - one authenticated upstream account per subject and provider, distinct from Caracal grants, which express authorization.\n\n## Boundaries\n\n- A resource says what is protected; its provider says how the upstream is authenticated. Keep credential detail on the provider and target detail on the resource.\n- Name providers with stable `provider://` identifiers, such as `provider://hooli-oidc`.\n- OAuth providers support a real connectivity check before creation. The other modes are validated at creation and exercised when a resource first uses them.\n\n:::note[FAQ]\n[What is the difference between a resource and a provider?](/reference/faq/#faq-009) and [is an application secret the same as a provider credential?](/reference/faq/#faq-013)\n:::\n\n## Next Step\n\nRead [Policies and Policy Sets](/concepts/policy/) to understand how requests against a resource are allowed, denied, or held for step-up.\n\n## Related Pages\n\n- [Define Resources and Providers](/guides/resources-providers/)\n- [Provider Recipes](/guides/provider-recipes/)\n- [Resources and Grants](/concepts/resource-grant/)"},{"id":"/enterprise/compliance-readiness","title":"Compliance and Operations Readiness","url":"https://docs.caracal.run/enterprise/compliance-readiness/","concepts":"","body":"This page is a compliance and operations readiness review of the Caracal **Community Edition** (open-source, Apache 2.0, self-hosted), written for enterprise security, compliance, audit, procurement, and operations teams evaluating adoption.\n\nIt is written conservatively and is intended to be verifiable. Read it with the following ground rules.\n\n## Reading Rules and Non-Claims\n\n- **Caracal is not certified.** This project does not hold, claim, or imply certification or attestation under any framework named on this page. No framework is represented as fully satisfied or guaranteed by Caracal.\n- **Best-effort alignment.** Maintainers implement controls in a best-practice-oriented way and try to keep them aligned with common compliance needs. That alignment is best effort, not a guarantee, and not every requirement of any framework is implemented or verified.\n- **Caracal is a control, not a compliance program.** Caracal is infrastructure that can support specific technical controls inside _your_ compliance program. Adopting Caracal does not make an organization compliant with any framework. The control environment, the audit, and the attestation remain the adopter's responsibility.\n- **Evidence over assertion.** Every \"covered\" or \"partially covered\" statement below cites a path in this repository or docs so you can confirm it against the exact commit you deploy. Where a control depends on settings outside the source tree (organization policy, cloud configuration, operator action), it is marked accordingly and not asserted as implemented.\n- **Status vocabulary.**\n  - <span className=\"status status-covered\">Covered</span> - Caracal implements a relevant control in code, schema, CI, or the release\n    pipeline, verifiable at the cited path.\n  - <span className=\"status status-partial\">Partially covered</span> - Caracal implements part of the control or bounds the risk, but full\n    coverage depends on operator configuration or application behavior.\n  - <span className=\"status status-none\">Not covered</span> - Caracal does not address this item, and it is the adopter's or another\n    system's responsibility within a domain Caracal could otherwise contribute to.\n  - <span className=\"status status-na\">Not applicable</span> - The item does not map to Caracal's function - for example model behavior,\n    model output, an organizational process, or data-subject content. Where Caracal supplies inputs to such an item, that is noted in the\n    row.\n- **Framework labels.** OWASP Agentic AI categories, NIST AI RMF functions, EU AI Act articles, SOC 2 Trust Services Criteria, and the agentic authority controls (AARM R1–R9) are referenced as evaluation lenses. Control titles evolve across framework versions; reconcile the exact wording of each item against your authoritative copy of the framework. The AARM R1–R9 mappings use a working interpretation of agentic-authority control intents and must be reconciled with your authoritative control definitions before use in an audit.\n\n---\n\n## A. Executive Summary for Compliance and Operations Teams\n\nCaracal is a pre-execution authority layer for agents and automated workloads. Its Security Token Service (STS) is the sole issuer of scoped, signed mandates; the Gateway enforces protected-resource access before execution; the Coordinator tracks agent and delegation state; and the Audit service preserves tamper-evident decision evidence. Postgres is the system of record with per-zone row-level security; Redis Streams move revocation and operational events.\n\nFor a compliance and operations reader, the honest summary is:\n\n- **What Caracal is good at, and can evidence today:** least-privilege machine authority (short-lived signed mandates instead of long-lived credentials), centralized revocation, zone-based isolation and blast-radius containment, and tamper-evident, HMAC-chained audit. These map cleanly to logical-access, traceability, and accountability controls.\n- **What Caracal supports but does not own:** human/administrator identity and SSO (intentionally external to the runtime trust boundary - see [the SSO position](#e-oss-vs-enterprise-edition-boundary)), risk management process, data-subject rights, and the overall control environment. Caracal supplies technical evidence into these; it does not satisfy them.\n- **What Caracal does not address:** model-level agent behavior (prompt injection, hallucination, memory poisoning, goal manipulation). Caracal constrains what an agent is _authorized_ to do and records what it _did_; it does not judge whether the agent _should_ have wanted to.\n- **Certification posture:** none claimed. No FIPS validation is claimed ([details](/security/enterprise-readiness/#regulated-enterprise-and-fips-readiness)). SOC 2 is an attestation of an operating organization, which the open-source project is not; Caracal provides controls that can support an adopter's SOC 2, not a SOC 2 report.\n\nAdoption recommendation in one line: **suitable as a least-privilege authority, revocation, and audit-evidence control for agentic systems, on the explicit understanding that the adopter owns the surrounding compliance program and the items marked partially covered below.**\n\n---\n\n## B. Control-by-Control Matrix\n\n### B.1 OWASP Agentic AI Top 10 (ASI01–ASI10) and Agent Traceability\n\nMapped to the recognized agentic threat categories. Caracal is an authority and audit control, so it addresses the _authority, identity, traceability, and revocation_ classes well and the _model-behavior_ classes not at all. This is by design.\n\n| #     | Agentic risk category                    | Status                                                           | Caracal control                                                                                                                                                                                                             |\n| ----- | ---------------------------------------- | -------------------------------------------------"},{"id":"/guides/approval-notifications","title":"Approval Notifications","url":"https://docs.caracal.run/guides/approval-notifications/","concepts":"","body":"Approval holds are decided fastest when the right person hears about them immediately. A notification sink is a zone-scoped webhook endpoint that Caracal pushes approval lifecycle events to: your relay turns the delivery into a page, a chat message, or a ticket in whatever system your team watches. Sinks are optional - a zone without one loses nothing except the push, since the Console badges pending holds and the [Approvals page](/guides/step-up/#decide-as-an-operator) remains the decision surface.\n\n## Create a sink\n\nOpen **Settings → Notifications** in the Console and add a sink with a name, an HTTPS endpoint, and the event types it should receive. The response reveals the signing secret exactly once - store it in your receiver's secret manager before closing the dialog, because Caracal keeps only a sealed copy it cannot show again. For automation, the Admin API exposes the same lifecycle:\n\n```bash\ncurl -X POST \"$CARACAL_API_URL/v1/zones/$CARACAL_ZONE_ID/notification-sinks\" \\\n  -H \"Authorization: Bearer $CARACAL_ADMIN_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"name\": \"Pied Piper on-call relay\",\n    \"url\": \"https://hooks.hooli.example/caracal-approvals\",\n    \"event_types\": [\"step_up_issued\", \"step_up_decided\"]\n  }'\n```\n\nEndpoints must be HTTPS; plain HTTP is accepted only for loopback addresses during local development, and URLs may never embed credentials. Delivery resolves and pins DNS for each connection and rejects loopback, private, metadata, link-local, CGNAT, multicast, mapped, and NAT64-embedded restricted addresses. Authentication is the signature, not the URL. A zone holds at most 20 sinks.\n\n| Event type         | Fires when                                                                                              |\n| ------------------ | ------------------------------------------------------------------------------------------------------- |\n| `step_up_issued`   | A hold is created and waits for a decision. This is the event to page on.                               |\n| `step_up_decided`  | An approver settles the hold - the payload's `decision` field says whether it was approved or rejected. |\n| `step_up_consumed` | The approved hold releases its mandate to the retrying agent.                                           |\n\n## Verify every delivery\n\nEach delivery is an HTTP `POST` with a JSON body and these headers:\n\n| Header                | Content                                                                             |\n| --------------------- | ----------------------------------------------------------------------------------- |\n| `X-Caracal-Event`     | The event type, for routing before parsing.                                         |\n| `X-Caracal-Delivery`  | Unique delivery id. Deliveries are at-least-once; use this id to deduplicate.       |\n| `X-Caracal-Sink`      | The sink id, so one receiver can serve several sinks.                               |\n| `X-Caracal-Timestamp` | Unix seconds when the delivery was signed. Reject stale timestamps to stop replays. |\n| `X-Caracal-Signature` | `v1=` followed by hex HMAC-SHA256 of `timestamp.body` under the sink secret.        |\n\nVerify the signature over the raw request body before trusting anything in it:\n\n```ts\nimport { createHmac, timingSafeEqual } from 'node:crypto'\n\nfunction verifySink(secret: string, headers: Record<string, string>, rawBody: string): boolean {\n  const timestamp = headers['x-caracal-timestamp']\n  if (Math.abs(Date.now() / 1000 - Number(timestamp)) > 300) return false\n  const expected = `v1=${createHmac('sha256', secret).update(`${timestamp}.${rawBody}`).digest('hex')}`\n  const received = headers['x-caracal-signature'] ?? ''\n  return received.length === expected.length && timingSafeEqual(Buffer.from(received), Buffer.from(expected))\n}\n```\n\nThe body carries the audit event verbatim - `data` is the same metadata the zone audit stream records for the hold, so a relay can render a complete approval prompt without calling back:\n\n```json\n{\n  \"id\": \"0195f2aa-7c31-7d4e-a01b-3f6c2d9e8b10\",\n  \"type\": \"step_up_issued\",\n  \"zone_id\": \"0195f2a9-1b22-7c3d-9e4f-5a6b7c8d9e0f\",\n  \"decision\": \"pending\",\n  \"occurred_at\": \"2026-01-01T12:00:00.000Z\",\n  \"data\": {\n    \"challenge_id\": \"0195f2aa-6d20-7b3c-8e1a-2f4d6c8b0a9e\",\n    \"tier\": \"high\",\n    \"approver_class\": \"operator\",\n    \"privacy_mode\": \"identified\",\n    \"binding\": \"9f3c…\",\n    \"expires_at\": \"2026-01-01T12:30:00Z\",\n    \"application_id\": \"0195f2a9-4e55-7a1b-bc2d-3e4f5a6b7c8d\",\n    \"session_id\": \"0195f2aa-2c11-7e9f-8a0b-1c2d3e4f5a6b\"\n  }\n}\n```\n\nRespond with any `2xx` status within ten seconds. The response body is ignored - do your downstream work asynchronously and acknowledge fast.\n\n## Delivery semantics\n\nDelivery is at-least-once with per-sink ordering by the zone audit sequence. A non-`2xx` response or a timeout schedules a retry with exponential backoff from 30 seconds to a 15-minute ceiling; after 8 attempts the delivery is abandoned and the failure is visible on the sink's delivery record in the Console. Pausing a sink stops deliveries without losing anything: the sink's cursor into the audit stream holds its place, and resuming catches up from where it stopped. A run of consecutive failures flags the sink as failing in the Console but never disables it - fix the receiver and the backlog drains on its own.\n\nRotating the secret (Console or `POST …/notification-sinks/{id}/rotate-secret`) invalidates the old value immediately and reveals the replacement once; update the receiver in the same change window. Sink creation, edits, rotations, and deletions are themselves recorded in the zone audit stream, with secret material redacted.\n\nRelated pages: [Human Approval](/guides/step-up/) and [Tail and Query the Audit Stream](/guides/audit-stream/)."},{"id":"/guides/frameworks","title":"Govern Agent Frameworks","url":"https://docs.caracal.run/guides/frameworks/","concepts":"","body":"Caracal does not ship framework connectors for LangChain, LangGraph, or CrewAI, because it does not need them. Frameworks orchestrate model and tool calls through the provider clients you construct; Caracal governs those clients through the [transport wiring](/guides/provider-recipes/#wire-the-transport-into-provider-clients). Configure the client once, and every chain, graph node, agent, and tool call the framework issues is authenticated, policy-checked, and audited - with no framework-version coupling to maintain.\n\nThe pattern is always the same:\n\n1. Bind the provider upstream to a Caracal resource.\n2. Build the provider client with `caracal.transport()` (or the language equivalent).\n3. Hand that client to the framework.\n4. Run framework work inside `session()` so each agent execution gets its own Session ID.\n\n## LangChain\n\nLangChain model classes accept the same custom HTTP client hooks as the underlying provider SDKs.\n\n```python\nfrom caracalai import Caracal\nfrom langchain_openai import ChatOpenAI\n\ncaracal = Caracal()\n\nllm = ChatOpenAI(\n    model=\"gpt-4o\",\n    api_key=\"caracal-gateway\",\n    http_client=caracal.sync_transport(),\n    http_async_client=caracal.transport(),\n)\n```\n\n```ts\nimport { Caracal } from '@caracalai/sdk'\nimport { ChatOpenAI } from '@langchain/openai'\n\nconst caracal = new Caracal()\n\nconst llm = new ChatOpenAI({\n  model: 'gpt-4o',\n  apiKey: 'caracal-gateway',\n  configuration: { fetch: caracal.transport() },\n})\n```\n\nThe `configuration` object forwards to the OpenAI SDK, so the governed `fetch` carries every LangChain invocation. Anthropic and Gemini model classes take the corresponding hooks from their provider recipes.\n\n## LangGraph\n\nLangGraph nodes call whatever model client the graph was built with, so a governed client governs the whole graph. What LangGraph adds is concurrency: parallel branches and subgraphs fan out, and each logical agent should be distinguishable in policy and audit. Start a labeled Session around each run, or around each branch that represents a distinct agent:\n\n```python\nfrom langgraph.prebuilt import create_react_agent\n\nagent = create_react_agent(llm, tools)\n\nasync with caracal.session(labels=[\"research-agent\"]):\n    result = await agent.ainvoke({\"messages\": [(\"user\", question)]})\n```\n\nEvery model call and governed tool call inside the run carries that Session's identity. To narrow a subagent below its parent, start its Session with reduced authority; see [Implement Multi-Agent Delegation](/guides/delegation/).\n\n## CrewAI\n\nCrewAI routes model traffic through LiteLLM, so the LiteLLM session hooks govern an entire crew:\n\n```python\nimport litellm\nfrom caracalai import Caracal\n\ncaracal = Caracal()\nlitellm.client_session = caracal.sync_transport()\nlitellm.aclient_session = caracal.transport()\n```\n\nSet the sessions before constructing agents. Each provider LiteLLM reaches needs its own resource binding, exactly as in the [LiteLLM recipe](/guides/provider-recipes/#litellm).\n\n## Framework tools\n\nTools are where agent traffic actually touches your systems, and they follow the same rule as models: build the tool's HTTP client from the transport.\n\n```python\ntickets = caracal.transport(scopes=[\"tickets:read\"])\n\nasync def fetch_ticket(ticket_id: str) -> dict:\n    response = await tickets.get(f\"https://api.example.com/tickets/{ticket_id}\")\n    return response.json()\n```\n\nBuild the transport once and share it: identity is resolved per request from the bound context, so concurrent agents can safely use the same client. Passing `scopes` makes each call present a narrowed resource mandate instead of the raw subject token, so a tool holds only the authority it needs. For a complete multi-agent LangChain and LangGraph deployment governed this way, see the [Lynx Capital example](/examples/lynx-capital/).\n\n## Related\n\n- [Provider Recipes](/guides/provider-recipes/)\n- [Integrate the Python SDK](/guides/sdk-python/)\n- [Integrate the TypeScript SDK](/guides/sdk-typescript/)\n- [Implement Multi-Agent Delegation](/guides/delegation/)"},{"id":"/guides/idempotency","title":"Safe Retries and Idempotency","url":"https://docs.caracal.run/guides/idempotency/","concepts":"","body":"Most developers can ignore idempotency. Caracal automatically protects its own session and delegation creation calls against the network failures and transient server errors that ordinary applications encounter.\n\nThis guide explains the exceptional case: work delivered by a queue, webhook provider, workflow engine, or scheduler that can arrive again after the original process has stopped.\n\n## What Idempotency Means\n\nAn operation is **idempotent** when repeating the same request has the same effect as performing it once. For example, asking Caracal twice to create the same governed Session should not create two Session records.\n\nIdempotency does not mean that arbitrary application code runs exactly once. A process can call an external API successfully and crash before recording success. After restart, no generic SDK can determine whether the external API committed the change.\n\n## The Default: Do Nothing\n\nFor a normal request handler, command, or in-process task, omit `idempotencyKey`:\n\n```ts\nawait caracal.session(\n  async () => {\n    await caracal.fetch('resource://pipernet', '/reports/market-risk')\n  },\n  {\n    labels: ['research-agent'],\n    task: 'Prepare the market risk report',\n  },\n)\n```\n\nThe SDK generates a cryptographically random operation identifier before contacting the Coordinator. If the connection drops, the Coordinator returns a transient error, or the SDK refreshes a rejected bearer, every retry reuses that identifier. The Coordinator stores a durable receipt in PostgreSQL, so a committed session or delegation creation is replayed instead of created again.\n\nThe receipt contains:\n\n- a keyed digest of the operation identifier, never the plaintext value;\n- the authenticated tenant and application scope;\n- a canonical fingerprint of every security-relevant request field;\n- the original response and created resource id;\n- an explicit expiry time.\n\nReceipts store only bounded operational response fields. They do not duplicate session metadata, invocation parameters, credentials, or bearer tokens.\n\nThe default receipt window is seven days for explicit stable identifiers (`IDEMPOTENCY_RETENTION_SECONDS`) and one day for automatically generated retry identifiers (`GENERATED_IDEMPOTENCY_RETENTION_SECONDS`). The shorter generated window contains storage growth without weakening process-local retry safety.\n\nEach application and operation can retain at most 10,000 live receipts by default (`IDEMPOTENCY_MAX_RECEIPTS_PER_SCOPE`), preventing a compromised workload from growing the receipt index without bound. The Coordinator also caps request bodies at 256 KiB (`COORDINATOR_BODY_LIMIT_BYTES`).\n\n## When an Explicit Key Helps\n\nSupply a key only when the system delivering work already has a stable identifier that survives process restart:\n\n| Source          | Use                                                       |\n| --------------- | --------------------------------------------------------- |\n| Queue           | Queue or subscription namespace plus immutable message id |\n| Webhook         | Verified provider namespace plus delivery or event id     |\n| CloudEvents     | `source` plus `id`                                        |\n| Workflow engine | Workflow namespace, run id, and step or activity id       |\n| Scheduler       | Schedule namespace plus intended fire time                |\n\n```ts\nasync function handleWorkItem(message: QueueMessage<Ticket>) {\n  const ticket = message.body\n  await caracal.session(\n    async () => {\n      await caracal.fetch('resource://pipernet', '/reports/market-risk')\n    },\n    {\n      labels: ['research-agent'],\n      task: `${ticket.title} (${ticket.key})`,\n      idempotencyKey: `ticket-queue:v1:${message.id}`,\n    },\n  )\n}\n```\n\nPython uses `idempotency_key=`. Go uses `SessionOptions{IdempotencyKey: ...}`.\n\nThe namespace and version prevent unrelated integrations from colliding. Change the version only when intentionally defining a different operation.\n\n## Exact Guarantee\n\nWith the same key and the same request fields, Caracal replays the original **Coordinator creation response** during the configured retention window. A replay carries the `Idempotency-Replayed: true` response header and appears as `replayed: true` on SDK coordinator events.\n\nWith the same key and different fields, Caracal returns `409 idempotency_key_conflict`. This includes changes to the Subject authority record ID, parent Session, application, lifecycle, labels, task or metadata, authority mode, TTL, Delegation endpoints, resource, scopes, constraints, or invocation parameters.\n\nIf the receipt's governed Session or Delegation has already terminated, expired, or been revoked, Caracal returns `409 idempotency_result_inactive` instead of replaying an unusable identity. This prevents a delayed sequential redelivery from entering the callback. Use a newly versioned operation id only for an intentional rerun.\n\nWhile the original session remains active, a valid replay still enters your callback. The key does not claim a queue message, serialize consumers, cache the callback result, or make downstream effects exactly once. Two concurrent consumers can both execute application code. Use the queue's own visibility/lease mechanism to prevent concurrent delivery.\n\n## Protect External Side Effects\n\nPass a stable operation id to every destination that supports idempotency:\n\n```ts\nconst operationId = `market-risk:v1:${message.id}`\n\nawait caracal.session(\n  async () => {\n    await caracal.fetch('resource://pipernet', '/reports/market-risk', {\n      method: 'POST',\n      headers: { 'Idempotency-Key': operationId },\n    })\n  },\n  {\n    task: `Prepare ${ticket.key}`,\n    labels: ['research-agent'],\n    idempotencyKey: `session:${operationId}`,\n  },\n)\n```\n\nFor a database mutation, use a transactional inbox or unique operation-id constraint in the same database transaction as the mutation. For asynchronous publication, use a transactional outbox. For APIs that do not support idempotency, reconcile the des"},{"id":"/guides/protect-fastapi","title":"Protect a FastAPI App","url":"https://docs.caracal.run/guides/protect-fastapi/","concepts":"","body":"Use `caracalai-asgi` when a FastAPI, Starlette, or other ASGI app should verify Caracal mandates before request handlers run. This is the provider-side boundary: a partner serving Caracal-governed customers verifies each inbound mandate against the zone's keys before doing any work.\n\n## Install\n\n```bash\npip install caracalai-asgi caracalai-revocation\n```\n\nUse a Redis-backed revocation store in production. The in-memory store is only suitable for local development and tests.\n\n## Add middleware\n\n```python\nfrom caracalai_asgi import CaracalASGIAuth\nfrom caracalai_revocation import InMemoryRevocationStore\nfrom fastapi import FastAPI, Request\n\napp = FastAPI()\napp.add_middleware(\n    CaracalASGIAuth,\n    audience=\"resource://billing-api\",\n    revocations=InMemoryRevocationStore(),\n    required_scopes=[\"billing:read\"],\n    routes={\n        \"/payouts\": {\"required_scopes\": [\"billing:payout\"], \"require_delegation\": True},\n    },\n    exclude=[\"/healthz\"],\n)\n\n@app.post(\"/payouts/create\")\nasync def create_payout(request: Request):\n    principal = request.state.caracal\n    return {\"subject\": principal.sub}\n```\n\nWith `CARACAL_STS_URL` and `CARACAL_ZONE_ID` set - the standard Caracal workload variables - the middleware resolves the issuer and zone itself; you state only your own audience and revocation store. Requests reach your handlers only after the mandate's signature, issuer, audience, zone, token use, scopes, and revocation anchors all verify.\n\n## Enforce the right boundary\n\n| Option               | Use it for                                                                |\n| -------------------- | ------------------------------------------------------------------------- |\n| `required_scopes`    | Route-level scope checks.                                                 |\n| `required_targets`   | Resource-target checks.                                                   |\n| `require_session`    | Require a governed Session.                                               |\n| `require_delegation` | Require delegated authority.                                              |\n| `max_hop_count`      | Limit delegation depth.                                                   |\n| `routes`             | Apply any of the above per path prefix; the longest matching prefix wins. |\n\n## Validate\n\n1. Exchange for a mandate that targets the resource.\n2. Call the protected route with `Authorization: Bearer <mandate>`.\n3. Remove a required scope and confirm the route returns `403`.\n4. Revoke the session and confirm the route rejects the old mandate.\n\nRelated pages: [Mandates](/concepts/mandate/) and [Sessions and Revocation](/concepts/sessions-revocation/)."},{"id":"/guides/testing","title":"Test Caracal Integrations","url":"https://docs.caracal.run/guides/testing/","concepts":"","body":"Caracal integrations test cleanly because every control-plane dependency enters through an injectable HTTP client: `fetchImpl` in TypeScript, `http_client` and `httpx.MockTransport` in Python, `HTTPClient` and `httptest` in Go. Fake the wire, not the SDK - your test exercises the same request construction, retry, and caching code that runs in production, and nothing reaches a network.\n\n## What to stub\n\nA governed Session touches a handful of endpoints. Stub the ones your code path uses and let anything unexpected fail loudly:\n\n| Call                             | Respond with                                                                                                            |\n| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |\n| `POST /zones/{zone}/agents`      | Protocol Session-start response `{\"agent_session_id\": \"agent-1\"}`; add `heartbeat_deadline_at` for long-lived Sessions. |\n| `POST /zones/{zone}/delegations` | Protocol Delegation response `{\"delegation_edge_id\": \"edge-1\"}`.                                                        |\n| `POST .../agents/{id}/heartbeat` | `{\"agent\": {\"status\": \"active\", \"heartbeat_deadline_at\": \"...\"}}`                                                       |\n| `DELETE .../agents/{id}`         | `204`                                                                                                                   |\n| `POST /oauth/2/token`            | `{\"access_token\": \"tok\", \"token_type\": \"Bearer\", \"expires_in\": 900}`                                                    |\n\nReturn an error status to exercise failure paths: a `503` drives the idempotent Session-start retry, `429` with `Retry-After` drives hinted backoff, and a `401` from an `interaction_required` body raises the approval hold.\n\nMake scoped-transport Gateway fakes replay-sensitive: remember each presented bearer and reject a duplicate with `token_replayed`. Repeated and concurrent requests must carry distinct mandates, while an application transport must still create only one source/target Session pair and one delegation per authority-cache key.\n\n## TypeScript\n\n```ts\nimport { describe, expect, it, vi } from 'vitest'\nimport { Caracal } from '@caracalai/sdk'\n\nit('runs work inside a governed Session', async () => {\n  const calls: { url: string; method: string }[] = []\n  const fetchImpl = (async (input: RequestInfo | URL, init: RequestInit = {}) => {\n    calls.push({ url: String(input), method: init.method ?? 'GET' })\n    if (init.method === 'POST' && String(input).endsWith('/agents')) {\n      return new Response(JSON.stringify({ agent_session_id: 'agent-1' }), { status: 200 })\n    }\n    return new Response(null, { status: 204 })\n  }) as typeof fetch\n\n  const caracal = new Caracal({\n    coordinator: { baseUrl: 'http://coord.test', fetchImpl },\n    zoneId: 'z',\n    applicationId: 'app',\n    subjectToken: 'test-token',\n  })\n\n  const result = await caracal.session(async (ctx) => ctx.sessionId)\n\n  expect(result).toBe('agent-1')\n  expect(calls.at(-1)?.method).toBe('DELETE') // the session was retired\n})\n```\n\n## Python\n\n```python\nimport httpx\nfrom caracalai import Caracal, CaracalConfig\nfrom caracalai.coordinator import CoordinatorClient\n\nasync def test_runs_work_inside_a_governed_session() -> None:\n    async def handler(request: httpx.Request) -> httpx.Response:\n        if request.method == \"POST\" and str(request.url).endswith(\"/agents\"):\n            return httpx.Response(200, json={\"agent_session_id\": \"agent-1\"})\n        return httpx.Response(204)\n\n    coordinator = CoordinatorClient(\n        base_url=\"http://coord.test\",\n        http_client=httpx.AsyncClient(transport=httpx.MockTransport(handler)),\n    )\n    caracal = Caracal(\n        CaracalConfig(\n            coordinator=coordinator,\n            zone_id=\"z\",\n            application_id=\"app\",\n            subject_token=\"test-token\",\n        )\n    )\n\n    async with caracal.session() as ctx:\n        assert ctx.session_id == \"agent-1\"\n```\n\n## Go\n\n```go\nfunc TestRunsWorkInsideAGovernedSession(t *testing.T) {\n\tsrv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n\t\tif r.Method == http.MethodPost && strings.HasSuffix(r.URL.Path, \"/agents\") {\n\t\t\tw.Header().Set(\"Content-Type\", \"application/json\")\n\t\t\tfmt.Fprint(w, `{\"agent_session_id\":\"agent-1\"}`)\n\t\t\treturn\n\t\t}\n\t\tw.WriteHeader(http.StatusNoContent)\n\t}))\n\tdefer srv.Close()\n\n\tclient := &caracal.Caracal{\n\t\tCoordinator:   &caracal.CoordinatorClient{BaseURL: srv.URL},\n\t\tZoneID:        \"z\",\n\t\tApplicationID: \"app\",\n\t\tSubjectToken:  \"test-token\",\n\t}\n\n\terr := client.Session(context.Background(), func(ctx context.Context) error {\n\t\tcur, _ := caracal.Current(ctx)\n\t\tif cur.SessionID != \"agent-1\" {\n\t\t\tt.Fatalf(\"unexpected session: %s\", cur.SessionID)\n\t\t}\n\t\treturn nil\n\t})\n\tif err != nil {\n\t\tt.Fatal(err)\n\t}\n}\n```\n\n## Assert on behavior, not internals\n\nTwo seams make assertions precise without reaching into SDK state. The fake transport records every request, so you can assert the wire contract: an `idempotency-key` header on Session starts, the Delegation body, and termination on Session exit. `onEvent` reports control-plane operations as data, so tests can assert the expected `coordinator.call`, `token.exchange`, or `delegation.accept` events.\n\nClient-secret flows need the STS stub too: answer `POST /oauth/2/token` with a bearer and `expires_in`, and mint distinct tokens per call when the test asserts caching (a second exchange means a cache miss). For approval flows, return the `interaction_required` error body once and a token on the retry - `withApproval` completes end to end against the fake.\n\n## Integration tier\n\nFakes prove your code; a local runtime proves the contract. `caracal up` starts the full platform on localhost, and the same test binary points at it by swapping the injected client for real URLs from `caracal status`. Keep this tier thin - a handful of end-"},{"id":"/operations/deployment-profiles","title":"Choose a Deployment Profile","url":"https://docs.caracal.run/operations/deployment-profiles/","concepts":"","body":"Caracal ships as one versioned set of six container images with a single release train. Every deployment profile runs the same images and the same policy, audit, and credential behavior; profiles differ only in where the containers run, who operates the data stores, and how much surrounding platform you attach. Kubernetes is not required: a single Docker host is a supported production posture, and the orchestrated tiers exist for availability and scale rather than for capability.\n\n## Profile Map\n\n| Profile             | Scale                       | Path                                                                                                                                         | Data stores                                  |\n| ------------------- | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |\n| Evaluation          | Laptop or shared dev box    | [Install the CLI](/get-started/install-caracal/) and run `caracal up`                                                                        | Bundled Postgres and Redis containers        |\n| Single VM           | One team, production        | Installed CLI on any Docker host, optionally provisioned with the [`caracalHost` OpenTofu module](/operations/opentofu/#single-vm-bootstrap) | Bundled containers plus scheduled backups    |\n| Lightweight cluster | Small production Kubernetes | [Helm chart](/operations/kubernetes-helm/) in `rc` or `stable` mode                                                                          | Entry-tier managed Postgres and Redis        |\n| Enterprise HA       | Multi-team production       | Helm `stable` mode via the [`caracalStack` OpenTofu root](/operations/opentofu/) with an external secret manager                             | Managed HA Postgres and Redis with PITR      |\n| Hyperscale          | High-volume, multi-cluster  | Same chart with raised HPA ceilings, replay persistence, and topology spread; one release per cluster                                        | Data-platform-owned stores sized for streams |\n\n## Evaluation\n\nRun `caracal up` on any machine with Docker. The stack self-generates its secrets, binds every port to loopback, and reports readiness when all services pass their probes. Nothing is exposed beyond the host and `caracal purge` removes everything. Use this tier to evaluate policy behavior and SDK integration, not to hold real credentials long-term.\n\n## Single VM\n\nThe installed CLI carries the production compose topology, pinned images, health probes, and `caracal upgrade` for no-maintenance-window upgrades. Runtime secrets are generated on the host at first start and never leave it. All service ports stay loopback-bound: publishing the web console or Gateway is a deliberate act through the reverse proxy or tunnel you already operate, which keeps TLS and network exposure under your control.\n\nThis tier is production-grade for a single team when you schedule [backups](/operations/backup-retention/) and treat the VM as replaceable: a fresh host plus the latest backup bundle restores the stack. To provision hosts declaratively, the [`caracalHost` module](/operations/opentofu/#single-vm-bootstrap) renders cloud-init user data that works with any VM resource on any cloud.\n\nMove up when you need zero-downtime host maintenance, more than one replica per service, or organizational separation of data-store operations.\n\n## Lightweight Cluster\n\nThe Helm chart deploys the same topology onto any conformant Kubernetes 1.30+. In `rc` mode the chart may still run in-cluster data stores for staging; in `stable` mode the chart's guardrails reject in-cluster Postgres and Redis, so pair it with entry-tier managed database and cache services. This is the smallest footprint that gives rolling upgrades with zero unavailable replicas and per-service scaling.\n\n## Enterprise HA\n\nDrive the chart through the [`caracalStack` OpenTofu root](/operations/opentofu/), pin released chart versions from the OCI registry, and deliver the runtime Secret with your secret manager - the External Secrets flow in [Deploy on Managed Kubernetes](/operations/cloud-reference-deployments/) is the reference shape. The [cloud profile contract](/operations/cloud-native-profiles/) defines what the managed Postgres, Redis, secrets, and observability services must provide. Applies are atomic, migration hooks roll back with the release, and NetworkPolicy defaults to closed egress.\n\n## Hyperscale\n\nThe enterprise HA shape scales without new machinery: raise HPA replica ceilings, enable replay persistence, add topology spread across zones, and size Redis Streams and Postgres connection capacity with your data platform team. Run one release per cluster and shard by zone or region at the load balancer rather than stretching a single stack; see [Scale Capacity](/operations/scale-capacity/) for per-service scaling behavior.\n\n## Ownership Boundaries\n\nEvery external dependency attaches through an open standard, so managed services from any provider plug in without Caracal-specific adapters.\n\n| Component                   | Ownership                                         | Boundary                                                                                                                            |\n| --------------------------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |\n| Caracal services            | Caracal                                           | Versioned images on one release train; `caracal upgrade` or Helm upgrades.                                                          |\n| PostgreSQL                  | Bundled on VM tiers; yours on `stable` Kubernetes | Standard connection URLs meeting the [managed dependency "},{"id":"/operations/opentofu","title":"Provision with OpenTofu","url":"https://docs.caracal.run/operations/opentofu/","concepts":"","body":"The OpenTofu configuration in `infra/tofu` provisions Caracal declaratively across two substrates: Kubernetes through the Helm chart, and single VMs through cloud-init. The Kubernetes path wraps the same chart described in [Deploy with Helm](/operations/kubernetes-helm/), so chart values profiles stay the single source of deployment defaults while OpenTofu owns environment identity, chart provenance, and drift control.\n\n## Layout\n\n| Path                              | Purpose                                                                                          |\n| --------------------------------- | ------------------------------------------------------------------------------------------------ |\n| `infra/tofu/modules/caracalStack` | Kubernetes module: namespace, optional runtime Secret, Helm release.                             |\n| `infra/tofu/modules/caracalHost`  | VM module: provider-agnostic cloud-init user data for any VM resource.                           |\n| `infra/tofu/envs/dev`             | Installs the working-tree chart with `values.dev.yaml` against any kubeconfig-reachable cluster. |\n| `infra/tofu/envs/production`      | Installs a pinned OCI-published chart version with `values.production.yaml`.                     |\n| `infra/tofu/scripts/validate.sh`  | Formatting and offline validation harness used by CI.                                            |\n\n## Requirements\n\n- OpenTofu `>= 1.8`\n- Kubernetes `>= 1.30` (the chart's floor)\n- A kubeconfig context for the target cluster; the provider blocks in each environment root can be swapped for exec-based cloud IAM auth without touching the module.\n\n## Dev Environment\n\nThe dev root deploys the chart directly from the working tree, so infrastructure changes are exercised before any artifact is published:\n\n```bash\ncd infra/tofu/envs/dev\ncp terraform.tfvars.example terraform.tfvars   # fill in throwaway credentials\ntofu init\ntofu apply\n```\n\nDev runs in `mode: dev` with `evaluation: true`. The `runtimeSecrets` variable provisions the `caracal-runtime` Secret through OpenTofu instead of chart plaintext values, keeping one secret-delivery path across environments.\n\n## Production Environment\n\nThe production root always pins a released chart version from the OCI registry and enforces the chart's stable-mode guardrails, which reject in-cluster data-store defaults:\n\n```bash\ncd infra/tofu/envs/production\ncp terraform.tfvars.example terraform.tfvars   # pin chartVersion, set HA hosts\ntofu init\ntofu plan\ntofu apply\n```\n\nProduction never carries credentials in variables. Deliver the `caracal-runtime` Secret with your secret manager - for example the External Secrets Operator flow in [Deploy on Managed Kubernetes](/operations/cloud-reference-deployments/) - before the first apply.\n\nSet `extraValues` for environment overlays such as ingress hosts, the web console `publicUrl`, and OTLP endpoints. Every apply is atomic: a failed install or upgrade rolls back automatically, including migration hook Jobs.\n\n## Single VM Bootstrap\n\nThe `caracalHost` module renders cloud-init user data that installs Docker and the Caracal runtime CLI on first boot, then starts the pinned stack. It declares no providers, so the output attaches to any VM resource - AWS, Google Cloud, Azure, or your own hypervisor - and the host generates its runtime secrets locally at first start, keeping credential material out of OpenTofu state.\n\n```hcl\nmodule \"caracal\" {\n  source         = \"github.com/Garudex-Labs/caracal//infra/tofu/modules/caracalHost\"\n  caracalVersion = \"v0.2.0\"\n\n  envOverrides = {\n    CARACAL_WEB_URL = \"https://caracal.pipernet.example\"\n  }\n}\n\nresource \"aws_instance\" \"caracal\" {\n  ami           = data.aws_ami.host.id\n  instance_type = \"t3.large\"\n  user_data     = module.caracal.userData\n}\n```\n\nUse `userDataBase64` for providers that require encoded input, such as `azurerm_linux_virtual_machine.custom_data`. Overrides in `envOverrides` land in the host's `caracal.env` before first start, and `extraRuncmd` inserts commands - a reverse proxy or monitoring agent install, for example - between the CLI install and stack start. Service ports stay loopback-bound on the VM; publish the web console or Gateway through the reverse proxy you attach. See [Choose a Deployment Profile](/operations/deployment-profiles/) for when the single-VM tier fits and when to move to the cluster tiers.\n\n## State Management\n\nRoots default to local state for evaluation. For shared environments, configure a remote backend (S3-compatible object storage, Kubernetes Secret backend, or your platform's state service) with `tofu init -backend-config`; the roots contain no provider-specific backend assumptions.\n\n## Validation\n\n```bash\nbash infra/tofu/scripts/validate.sh\n```\n\nThe harness runs `tofu fmt -check` across the tree and an offline `tofu validate` of every environment root and module. CI runs the same harness on any `infra/` change."},{"id":"/operations/secret-backends","title":"Configure Secret Backends","url":"https://docs.caracal.run/operations/secret-backends/","concepts":"","body":"Caracal ships a built-in Secret Store and a pluggable backend layer for user-entered credentials such as provider client secrets. The rest of the platform reads and writes secrets through one interface, so switching backends never changes application behavior.\n\n## Storage Tiers\n\n| Secret class | Where it lives |\n| --- | --- |\n| User-entered credentials (provider client secrets and API keys) | The configured backend, addressed by a stable reference such as `zones/{zone}/providers/{provider}/secretConfig`. |\n| Issued credential custody copies (application client secrets at `zones/{zone}/applications/{application}/clientSecret`, workload secrets at `zones/{zone}/workloads/{workload}/secret`) | The configured backend. Authentication verifies only the argon2 hash on the owning row; the custody copy exists so operators and automation can retrieve the credential through an audited reveal instead of couriering it at creation time. |\n| Machine-generated material (zone signing keys, connection tokens, sink secrets, operator plan credentials) | Always sealed envelopes in their owning Postgres tables, regardless of backend. |\n\nEvery value, in every backend, is sealed under `SECRET_STORE_KEK` before it leaves the service: a fresh data key encrypts the value with ChaCha20-Poly1305 and the KEK wraps that data key. External backends only ever hold ciphertext.\n\n## Choose a Backend\n\nSet `CARACAL_SECRET_BACKEND` on the API and STS services. The default is `builtin` and requires no configuration beyond `SECRET_STORE_KEK`.\n\n| Value | Backend |\n| --- | --- |\n| `builtin` | Encrypted `secret_store` table in Caracal's Postgres. Default. |\n| `vault` | HashiCorp Vault KV v2. |\n| `infisical` | Infisical. |\n| `azurekeyvault` | Azure Key Vault. |\n| `awssecretsmanager` | AWS Secrets Manager. |\n| `gcpsecretmanager` | Google Secret Manager. |\n| `custom` | Any HTTPS service implementing the custom REST contract below. |\n\nThe API owns writes and deletes. STS holds a read-only view of the data plane and caches external reads for 60 seconds; the builtin backend reads Postgres directly and is not cached.\n\n## Backend Variables\n\n| Backend | Variables |\n| --- | --- |\n| `builtin` | None. |\n| `vault` | `CARACAL_VAULT_ADDR`, `CARACAL_VAULT_TOKEN`; optional `CARACAL_VAULT_MOUNT` (default `secret`), `CARACAL_VAULT_NAMESPACE`. |\n| `infisical` | `CARACAL_INFISICAL_TOKEN`, `CARACAL_INFISICAL_PROJECT_ID`; optional `CARACAL_INFISICAL_URL` (default `https://app.infisical.com`), `CARACAL_INFISICAL_ENV` (default `prod`), `CARACAL_INFISICAL_PATH` (default `/`). |\n| `azurekeyvault` | `CARACAL_AZURE_VAULT_URL`; either `CARACAL_AZURE_TENANT_ID`, `CARACAL_AZURE_CLIENT_ID`, and `CARACAL_AZURE_CLIENT_SECRET`, or none of them to use the VM or pod managed identity. |\n| `awssecretsmanager` | `CARACAL_AWS_REGION` (or `AWS_REGION`); either `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` (optional `AWS_SESSION_TOKEN`), or neither to use the container credentials endpoint. |\n| `gcpsecretmanager` | `CARACAL_GCP_PROJECT`; either `CARACAL_GCP_CREDENTIALS_FILE` (or `GOOGLE_APPLICATION_CREDENTIALS`), or neither to use the metadata server identity. |\n| `custom` | `CARACAL_CUSTOM_SECRETS_URL`, `CARACAL_CUSTOM_SECRETS_TOKEN`. |\n\nToken-valued variables support `*_FILE` variants on both services.\n\nOperator-supplied backend endpoints (`CARACAL_VAULT_ADDR`, `CARACAL_INFISICAL_URL`, `CARACAL_AZURE_VAULT_URL`, `CARACAL_CUSTOM_SECRETS_URL`) must be `https` URLs; plain `http` is accepted only for loopback hosts, which keeps local development against a same-host backend working.\n\nString-valued stores receive base64-encoded envelope bytes; the byte-oriented custom contract and the builtin store carry raw envelope bytes.\n\n## Ambient Cloud Credentials\n\nOn managed platforms the cloud backends authenticate without long-lived static keys. When the static credential variables are absent, each backend resolves its identity from the platform:\n\n| Backend | Ambient source |\n| --- | --- |\n| `awssecretsmanager` | The container credentials endpoint (`AWS_CONTAINER_CREDENTIALS_FULL_URI` or `AWS_CONTAINER_CREDENTIALS_RELATIVE_URI`), as injected by EKS Pod Identity and ECS task roles; `AWS_CONTAINER_AUTHORIZATION_TOKEN` and `AWS_CONTAINER_AUTHORIZATION_TOKEN_FILE` are honored. |\n| `azurekeyvault` | The IMDS managed-identity endpoint of the VM or pod. |\n| `gcpsecretmanager` | The metadata server service account, as configured by GKE Workload Identity. |\n\nEC2 instance-profile credentials without a container endpoint and Vault auth methods beyond static tokens are not resolved ambiently; supply static credentials or a token with a sufficient TTL and renew it through your secret delivery pipeline.\n\n## Custom REST Contract\n\nA custom backend is any HTTPS service exposing three routes under a base URL, authenticated with a bearer token:\n\n| Operation | Route | Behavior |\n| --- | --- | --- |\n| Write | `PUT {base}/secrets/{ref}` | Stores the `application/octet-stream` request body under `ref`. |\n| Read | `GET {base}/secrets/{ref}` | Returns the stored bytes, or `404` when absent. |\n| Delete | `DELETE {base}/secrets/{ref}` | Removes the value; `404` is treated as success. |\n\nRequests carry `Authorization: Bearer <CARACAL_CUSTOM_SECRETS_TOKEN>` and time out after 10 seconds. Stored payloads are sealed envelopes, so a compromised custom store exposes ciphertext only.\n\n## Failure Behavior\n\nWhen an external backend is unreachable during provider creation or update, the API rolls the provider row back inside the same transaction and returns `secret_backend_unavailable`, so the stored configuration and the credential document never diverge. The STS caches external reads for 60 seconds and serves a stale entry for up to 10 minutes while the backend errors, so a short backend outage does not stop token exchange; Caracal never falls back to another backend. Backend traffic is visible as `caracal_secret_backend_operations_total` and `caracal_secret_backend_errors_total` on the API and `caracal_sts_secret_bac"},{"id":"/releases","title":"Releases","url":"https://docs.caracal.run/releases/","concepts":"","body":"import ReleasesDirectory from '../../components/ReleasesDirectory.astro'\nimport LandingFooter from '../../components/LandingFooter.astro'\n\n<ReleasesDirectory />\n\n<LandingFooter />"},{"id":"/runtime-console/console-access","title":"Control Console Access","url":"https://docs.caracal.run/runtime-console/console-access/","concepts":"","body":"Console access on a self-hosted deployment is decided on the runtime host with `caracal allowlist`. The console's auth backend reads the allowlist file live, so every change applies on the next request with no restart. This page explains each command, what it does to existing accounts and sessions, and when a person sees the access-denied page.\n\n## Manage the Allowlist\n\n```sh\ncaracal allowlist add richard.hendricks@piedpiper.example\ncaracal allowlist add @piedpiper.example\ncaracal allowlist lock monica.hall@piedpiper.example\ncaracal allowlist unlock monica.hall@piedpiper.example\ncaracal allowlist remove gavin.belson@hooli.example\ncaracal allowlist list\n```\n\nEntries are exact emails or `@domain` suffixes; an exact entry always wins over a domain entry. While the list has active or locked entries, it is the sole authority over who may register and sign in. While it is empty, registration follows the deployment default: open in development, closed in production.\n\n| Command  | Effect on access                                                                        | Effect on the account                                                                                                                           |\n| -------- | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |\n| `add`    | The email may register and sign in. Re-adding a removed email starts a fresh admission. | None; a new person registers through a configured sign-in method.                                                                               |\n| `lock`   | Sign-in and console use are blocked until unlocked.                                     | Sessions are revoked immediately; the account and its data stay intact.                                                                         |\n| `unlock` | Restores sign-in for a locked entry.                                                    | None.                                                                                                                                           |\n| `remove` | Access ends permanently unless re-added.                                                | Sessions are revoked and the account's sign-in records are erased on its next contact. Zone data and audit history remain in the control plane. |\n\nRemoval is recorded as an explicit `removed` marker in the allowlist file rather than by deleting the line. Erasure only ever follows that deliberate marker: a missing, empty, or unreadable allowlist file can deny access but can never delete an account.\n\n## When the Access-Denied Page Appears\n\nThe console shows one uniform access-denied page for every denial. It appears when:\n\n- An email that has no active allowlist entry attempts to sign in or register, with a password or through Google/GitHub.\n- A signed-in person's entry is locked. The next request from their browser revokes their sessions and lands them on the page, typically within seconds.\n- A signed-in person's entry is removed. The next request revokes their sessions, erases their sign-in records, and lands them on the page.\n\nThe page is deliberately identical in all three cases, and the browser receives the same `access_denied` response for each. Whether an email was never admitted, suspended, or removed is visible only to you: the auth service log records the concrete reason (`sign-in denied by allowlist`, `console access denied by allowlist`) with the decision that triggered it.\n\n## Resolve a Denial\n\n1. Run `caracal allowlist list` on the runtime host to see every entry and its status.\n2. For a locked entry, run `caracal allowlist unlock <email>`.\n3. For a missing or removed email you want to admit, run `caracal allowlist add <email>`. A previously removed person registers again from scratch.\n4. If the list is empty and you expected the deployment default to admit sign-ins, check `CARACAL_OPEN_REGISTRATION` and the deployment's `NODE_ENV` posture in [Environment Variables](/operations/env-vars/#web-console-bff).\n\n## Next Step\n\nConfigure a sign-in method for admitted emails in [Sign In and Create Your First Zone](/get-started/first-protected-call/#sign-in-and-create-your-first-zone).\n\n## Related Pages\n\n- [Choose the Right Surface](/runtime-console/cli-and-console/)\n- [First-Run Troubleshooting](/get-started/first-run-troubleshooting/)\n- [CLI Exit Codes](/reference/runtime-exit-codes/)"},{"id":"/security/enterprise-readiness","title":"Enterprise Security Readiness","url":"https://docs.caracal.run/security/enterprise-readiness/","concepts":"","body":"This page is the single source of truth for evaluating Caracal's security posture and supply-chain trust. It is written for enterprise security, procurement, and platform teams who need to verify that the project follows security and operational best practices before adopting it.\n\nEach control carries a status:\n\n- **Implemented** - enforced in code, CI, or the release pipeline today.\n- **Documented** - a stated policy or expectation the project operates under.\n- **Planned** - intended, not yet in place.\n- **Not claimed** - deliberately not asserted, because it is not independently verified.\n\nNothing here asserts a compliance status that cannot be proven. Where a control depends on organization settings that are not visible in the repository, it is marked **Documented**, not **Implemented**.\n\n## What Caracal Is\n\nCaracal is a pre-execution authority layer for agents and workloads. Its Security Token Service (STS) issues scoped, signed mandates; the Gateway enforces protected-resource access; the Coordinator tracks agent and delegation state; and the Audit service preserves tamper-evident evidence. The Community Edition is licensed under Apache 2.0 and is designed to be self-hosted.\n\nThe data Caracal handles, where encryption occurs, how keys and tokens are isolated, and how trust boundaries work are covered in [Review the Threat Model](/security/threat-model/). Start there before a deeper review.\n\n## Security Control Summary\n\n| Area | Control | Status |\n| --- | --- | --- |\n| Static analysis | CodeQL (security-extended) for Go, Python, TypeScript | Implemented |\n| Dependency vulnerabilities | OSV-Scanner sweep across npm, Go, Python | Implemented |\n| Dependency vulnerabilities | Per-pull-request dependency review before merge | Implemented |\n| License compliance | Copyleft license deny-list on introduced dependencies | Implemented |\n| Dependency updates | Dependabot for actions, npm, Go, pip, Docker | Implemented |\n| Container CVEs | Trivy scan of published images (Critical/High) | Implemented |\n| Container hardening | Distroless, non-root, pinned base digests | Implemented |\n| Release signing | Sigstore keyless build provenance attestations | Implemented |\n| SBOM | SBOM and provenance attached to every image | Implemented |\n| Reproducible builds | Archives rebuilt and byte-compared in CI | Implemented |\n| Release verification | Post-release attestation and provenance checks | Implemented |\n| Supply-chain posture | OpenSSF Scorecard, results in code scanning | Implemented |\n| Vulnerability disclosure | Private reporting policy and process | Implemented |\n| Threat model | Assets, boundaries, encryption, key isolation | Documented |\n| Incident response | Defined response process | Documented |\n| Branch protection | Protected `main`, required review | Documented |\n| Maintainer MFA | MFA required for maintainers and committers | Documented |\n| FIPS validation | FIPS-validated cryptographic modules | Not claimed |\n\n## How Releases Are Signed\n\nReleases are signed with [GitHub Artifact Attestations](https://docs.github.com/en/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds), built on the [Sigstore](https://www.sigstore.dev/) keyless model. Signatures are produced in the release workflow using short-lived keys issued to the workflow's OpenID Connect identity, so no long-lived signing key is stored on any distribution server. The trust root is Sigstore's transparency log: verification confirms that an artifact was built by the `Garudex-Labs/caracal` release workflow.\n\n- CLI and Console archives are checksummed in `SHA256SUMS` and carry build-provenance attestations.\n- npm packages publish with provenance.\n- Container images carry both provenance and SBOM attestations.\n\nVerification steps for archives, images, and packages are in [Verify a Release](/security/verify-releases/). The bundled installers run `gh attestation verify` automatically.\n\n## How SBOMs Are Generated\n\nEvery container image is built with SBOM and provenance generation enabled in the release pipeline. The SBOM is attached to the image as an attestation in the registry and can be inspected with the registry tooling:\n\n```sh\ndocker buildx imagetools inspect ghcr.io/garudex-labs/caracal-<service>:<tag> \\\n  --format '{{ json .SBOM }}'\n```\n\nFor a source-level dependency inventory across all ecosystems, the weekly OSV-Scanner workflow enumerates and checks the npm, Go, and Python dependency graphs from their lockfiles.\n\n## How Vulnerabilities Are Handled\n\n- **Reporting.** Vulnerabilities are reported privately through GitHub Security Advisories or email, never as public issues. The full policy, required report format, and response timeline are in [Report a Vulnerability](/security/disclosure/).\n- **Detection.** CodeQL (security-extended) runs on pull requests and on a schedule. OSV-Scanner runs weekly across every dependency ecosystem and reports into GitHub code scanning. Trivy scans published container images weekly for Critical and High CVEs.\n- **Triage.** Findings surface in the repository's code scanning view as a single consolidated feed, so maintainers and evaluators review one source instead of disparate scan logs.\n- **Response.** Confirmed incidents follow the documented incident response process, and fixes ship through the standard signed release pipeline.\n\n## How Dependencies Are Reviewed\n\n- **Automated gate.** A scheduled dependency review runs every two days over the changes merged to `main` in that window and fails on newly introduced High-or-higher vulnerabilities and on a copyleft license deny-list. It is skipped automatically when no commits landed in the window.\n- **Updates.** Dependabot opens grouped weekly update pull requests for GitHub Actions, npm, Go modules, pip, and Docker base images. Updates pass the same CI, dependency review, and code review as any other change.\n- **Pinning.** GitHub Actions are pinned to commit SHAs, container base images are pinned to digests, and langua"},{"id":"/security/evidence-pack","title":"Generate an Evidence Pack","url":"https://docs.caracal.run/security/evidence-pack/","concepts":"","body":"A distributed topology is not the same as a large attack surface. During evaluation that distinction is easy to lose: reviewers see several services, Postgres, and Redis, and assume proportional risk. The evidence pack closes that gap by turning the architecture into demonstrated assurance - a single, reproducible bundle a security or compliance reviewer can run, read, and attach to an approval record.\n\nThe pack does not add new trust claims. It runs the verifiers Caracal already ships and captures their output in one place, mapped to the written assurance case in [`governance/THREAT_MODEL.md`](https://github.com/Garudex-Labs/caracal/blob/main/governance/THREAT_MODEL.md).\n\n## What It Proves\n\n| Pillar | Check | Demonstrates |\n| --- | --- | --- |\n| Supply chain | Release provenance | Every image you run was built by the `Garudex-Labs/caracal` release workflow and carries provenance and SBOM attestations. |\n| Enforcement (data) | Schema enforcement | Row-level security is fail-closed, audit is append-only, and policy versions are immutable - enforced by the database independent of application code. |\n| Enforcement (runtime) | Runtime readiness | Every mediation point (API, STS, Gateway, Audit, Coordinator) is live and answering readiness. |\n| Assurance case | Threat model | The written threat model the checks above exercise, captured with the pack. |\n\n## Generate the Pack\n\nRun the generator against a deployment. Each check runs only when its inputs are present, so the pack is useful at any stage of evaluation and records which checks were skipped and why.\n\n```bash\n# Supply-chain provenance (release tag + gh CLI authenticated)\nexport CARACAL_VERSION=<release-tag>\n\n# Schema enforcement (point at the running database)\nexport PGHOST=<host> PGPORT=5432 PGUSER=caracal PGDATABASE=caracal\nexport PGPASSWORD=<password>\n\n# Runtime readiness (defaults to 127.0.0.1)\nexport CARACAL_SMOKE_HOST=<host>\n\nbash infra/scripts/evidencePack.sh\n```\n\nThe pack is written to `evidence/caracal-evidence-<UTC>/`:\n\n- `REPORT.md` - summary table and per-check narrative with status and links.\n- `raw/` - the unmodified output of each verifier, including a copy of the threat model.\n\nThe generator exits non-zero if any executed check fails, so it can also gate CI or a release-readiness review. Skipped checks do not fail the run.\n\n## Inputs\n\n| Variable | Used by | Effect when unset |\n| --- | --- | --- |\n| `CARACAL_VERSION` | Release provenance | Provenance check is skipped. |\n| `CARACAL_REGISTRY` | Release provenance | Defaults to `ghcr.io/garudex-labs`. |\n| `PGPASSWORD` (+ `PG*`) | Schema enforcement | Schema check is skipped. |\n| `CARACAL_SMOKE_HOST` | Runtime readiness | Defaults to `127.0.0.1`. |\n| `OUT_DIR` | All | Defaults to `./evidence`. |\n\nGenerated packs are ignored by Git. Treat a completed pack as evidence: store it with the change record or compliance artifact it supports.\n\n## Related Pages\n\n- [Verify a Release](/security/verify-releases/)\n- [Review the Threat Model](/security/threat-model/)\n- [Enterprise Security Readiness](/security/enterprise-readiness/)\n- [Export Audit Evidence](/operations/compliance-audit-integration/)"},{"id":"/security/verify-releases","title":"Verify a Release","url":"https://docs.caracal.run/security/verify-releases/","concepts":"","body":"Every published Caracal release is cryptographically signed and verifiable. Verify a download before you install or deploy it, especially when you fetch archives or images directly.\n\n## How Releases Are Signed\n\nCaracal signs releases with [GitHub Artifact Attestations](https://docs.github.com/en/actions/security-guides/using-artifact-attestations-to-establish-provenance-for-builds), built on the [Sigstore](https://www.sigstore.dev/) keyless model:\n\n- Signatures are produced in the release workflow with short-lived keys issued to the workflow's OpenID Connect identity. No long-lived private signing key exists, so no signing key is stored on the servers that distribute the artifacts.\n- The public trust root is Sigstore's transparency log. The verifier confirms the artifact was built by the `Garudex-Labs/caracal` release workflow rather than checking a static public key you download separately.\n- Release archives are additionally checksummed in `SHA256SUMS`, npm packages publish with provenance, and container images carry provenance and SBOM attestations.\n\n## Prerequisites\n\nInstall the GitHub CLI and authenticate it:\n\n```sh\ngh auth login\n```\n\n`gh attestation verify` needs network access to the Sigstore transparency log on first use.\n\n## Verify Runtime Archives\n\nDownload the runtime archives, `manifest.json`, and `SHA256SUMS` from the release page, then verify checksums and provenance:\n\n```sh\n# 1. Confirm archive integrity against the published checksums.\nsha256sum --check SHA256SUMS   # macOS: shasum -a 256 --check SHA256SUMS\n\n# 2. Confirm each archive was built by the Caracal release workflow.\ngh attestation verify caracal-runtime-*.tar.gz --repo Garudex-Labs/caracal\ngh attestation verify caracal-runtime-*.zip --repo Garudex-Labs/caracal\n```\n\nOn Windows PowerShell, compute the hash, compare it with the matching `SHA256SUMS` entry, then verify provenance:\n\n```powershell\nGet-FileHash .\\caracal-runtime-windows-amd64-vX.Y.Z.zip -Algorithm SHA256\ngh attestation verify .\\caracal-runtime-windows-amd64-vX.Y.Z.zip --repo Garudex-Labs/caracal\n```\n\n`gh attestation verify` exits non-zero and prints the failing policy if the signature, identity, or transparency-log entry does not match.\n\n## Verify Container Images\n\nPull the image by digest, then verify its build provenance:\n\n```sh\ngh attestation verify oci://ghcr.io/garudex-labs/caracal-go:vX.Y.Z \\\n  --repo Garudex-Labs/caracal\n```\n\nInspect the attached SBOM and provenance attestations with the registry tooling:\n\n```sh\ndocker buildx imagetools inspect ghcr.io/garudex-labs/caracal-go:vX.Y.Z \\\n  --format '{{ json .Provenance }}'\n```\n\n## Verify Published Packages\n\nnpm packages publish with provenance; verify before installing:\n\n```sh\nnpm audit signatures\n```\n\n## If Verification Fails\n\nDo not install or run the artifact. Re-download from the official release page, confirm you used the correct release tag, and if verification still fails, [report it privately](/security/disclosure/).\n\n## Related Pages\n\n- [Install Caracal](/get-started/install-caracal/)\n- [Harden Security Posture](/security/hardening/)\n- [Review the Threat Model](/security/threat-model/)"},{"id":"/vlogs","title":"Vlogs","url":"https://docs.caracal.run/vlogs/","concepts":"","body":"import VlogsDirectory from '../../components/VlogsDirectory.astro'\nimport LandingFooter from '../../components/LandingFooter.astro'\n\n<VlogsDirectory />\n\n<LandingFooter />"}]
