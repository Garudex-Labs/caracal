// Copyright (C) 2026 Garudex Labs.  All Rights Reserved.
// Caracal, a product of Garudex Labs
//
// Generated documentation corpus the Operator retrieves over; regenerate with scripts/build-docs-corpus.mjs.

import type { DocPage } from './operator-docs.js'

export const DOCS_CORPUS: DocPage[] = [{"id":"/v0.2/get-started","title":"Overview","url":"https://docs.caracal.run/v0.2/get-started/","concepts":"","body":"Caracal gives Applications short-lived, policy-approved authority instead of exposing upstream credentials to their processes. An Application asks for scoped access to a Resource, the active policy decides before the action runs, the Gateway or a verified service enforces the Mandate, and audit records the result.\n\nUse this section when you want the fastest path from a clean machine to a real protected call.\n\n## Should You Use Caracal?\n\nCaracal fits when agents, services, or automation need to call APIs, tools, SaaS providers, or data systems without directly holding the upstream credential.\n\n| Question                             | Use Caracal when                                                                |\n| ------------------------------------ | ------------------------------------------------------------------------------- |\n| Do agents need credentials?          | Agents need scoped access to tools, APIs, providers, or data.                   |\n| Do you need policy before execution? | Access must be allowed or denied before the protected action happens.           |\n| Do you need revocation?              | Active authority must end centrally without restarting every workload.          |\n| Do you need audit evidence?          | You need to explain which app, run, policy, resource, and result were involved. |\n\nCaracal is not an LLM framework, prompt router, agent scheduler, static config store, general API gateway, or identity provider. If you only have human users behind a normal login, start with an IdP instead.\n\n:::note[Documentation scope]\nThis site documents only the self-hosted open-source product implemented in this repository. It makes no claims about separate products, managed services, support terms, or commercial capabilities.\n:::\n\n## Prerequisites\n\nYou need a supported Linux, macOS, or Windows machine, Docker 25 or later with Compose v2, and permission to run containers. No Caracal source checkout is required.\n\n## First Success Path\n\nThe onboarding path keeps runtime lifecycle in the `caracal` CLI and product management in the web console:\n\n1. Install `caracal`.\n2. Start the local stack with `caracal up`.\n3. Sign in to the web console at [http://localhost:3001](http://localhost:3001) and complete onboarding to create your first zone.\n4. Use guided setup to create an Application, Resource, and active policy.\n5. Create a workload with launch bindings on the console's Launcher page and export the workload identity.\n6. Run a workload with `caracal run --` or an SDK.\n7. Call the protected resource through the Gateway.\n8. Inspect **Audit** in the web console.\n\n## What Success Looks Like\n\nYou are done evaluating when `caracal status --ready` succeeds, a request reaches the demo upstream through the Gateway, and the web console shows the matching allow decision and action result. A successful local run proves the enforcement path; it does not make the local stack production-ready.\n\n## Core Terms\n\n| Term        | Meaning                                                                                                                                                     |\n| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Application | The registered client identity your software runs as when it asks Caracal for authority.                                                                    |\n| Subject     | An optional identity from your own identity provider, federated for attribution and supported approval flows; it does not receive per-Subject permissions by itself. |\n| Session     | One governed execution under an Application identity.                                                                                                                  |\n| Resource    | The protected API, tool, provider, service, or data target.                                                                                                 |\n| Policy      | The rules that allow or deny requested resource scopes.                                                                                                     |\n| Mandate     | The short-lived signed token Caracal issues after policy allows access.                                                                                     |\n| Gateway     | The default boundary that verifies mandates, routes requests, brokers provider credentials when needed, and records action-result audit.                    |\n| Audit       | The decision and result trail for authorization, execution, revocation, and diagnostics.                                                                    |\n\nYou can finish Get Started with only these terms. Use [Concepts](/concepts/) when you need the deeper authority, delegation, revocation, and audit model.\n\n## Choose Your Next Path\n\n| Goal                            | Start here                                                | You will have                                                                           |\n| ------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------- |\n| Evaluate Caracal locally        | [Install Caracal](./install-caracal/)                     | A verified runtime CLI, packaged local web console, and Docker prerequisite.            |\n| Protect one resource end to end | [First Protected Call](./first-protected-call/)           | A Gateway-routed resource, active policy, runtime configuration, and audit explanation. |\n| Add Caracal to app code         | [Add SDK to Your App](./add-sdk-to-your-app/)             | TypeScript, Python, or Go code that opens a Session and calls the Gateway.              |\n| Fix a blocked first run         | [First-Run Troubleshooting](./first-run-troubleshooting/) | A focused checklist for readiness, profile, STS, Gateway, upstream, and"},{"id":"/v0.2/get-started/install-caracal","title":"Install Caracal","url":"https://docs.caracal.run/v0.2/get-started/install-caracal/","concepts":"","body":"Install the released runtime when you want to evaluate Caracal without a source checkout. The `caracal` CLI manages the local stack and launches workloads. The web console manages Zones, Applications, Resources, credential Providers, policies, Sessions, audit, and approvals.\n\n## Prerequisites\n\n- a supported Linux, macOS, or Windows machine;\n- Docker 25 or later with Compose v2;\n- `curl` or a browser for downloading a release;\n- permission to install one executable on your `PATH`.\n\n## Install the Runtime CLI\n\nRelease assets include platform-specific `caracal-runtime-*` archives, `manifest.json`, and `SHA256SUMS`. The `install.sh` installer installs the runtime-only CLI from those assets; it does not install source-tree development tooling. You can use the installer or download the archive for your platform from the [Caracal releases](https://github.com/Garudex-Labs/caracal/releases), verify it, and place the `caracal` binary on your `PATH`.\n\nTo install with the release script on Linux or macOS:\n\n```sh\ncurl -fsSL https://raw.githubusercontent.com/Garudex-Labs/caracal/main/install.sh | sh\n```\n\nTo verify a download yourself, or to verify archives and container images fetched directly, follow [Verify a Release](/security/verify-releases/).\n\n### Linux and macOS\n\n```sh\ngh release download vX.Y.Z \\\n  --repo Garudex-Labs/caracal \\\n  --pattern 'caracal-runtime-linux-amd64-*.tar.gz' \\\n  --pattern SHA256SUMS\nsha256sum --check SHA256SUMS   # macOS: shasum -a 256 --check SHA256SUMS\ntar -xzf caracal-runtime-linux-amd64-vX.Y.Z.tar.gz\ninstall -m 0755 caracal ~/.local/bin/caracal\n```\n\nChoose the `linux-arm64`, `darwin-amd64`, or `darwin-arm64` archive when that matches your machine. Ensure the destination directory is on your `PATH`.\n\n### Windows\n\n```powershell\ngh release download vX.Y.Z `\n  --repo Garudex-Labs/caracal `\n  --pattern 'caracal-runtime-windows-amd64-*.zip' `\n  --pattern SHA256SUMS\nGet-FileHash .\\caracal-runtime-windows-amd64-vX.Y.Z.zip -Algorithm SHA256\nExpand-Archive .\\caracal-runtime-windows-amd64-vX.Y.Z.zip -DestinationPath $env:LOCALAPPDATA\\Programs\\caracal\n```\n\nCompare the PowerShell hash with the matching entry in `SHA256SUMS`, then add `%LOCALAPPDATA%\\Programs\\caracal` to the user `Path` and open a new shell.\n\n## Verify Installation\n\n```sh\ncaracal --version\ncaracal status --help\n```\n\nOn Windows PowerShell:\n\n```powershell\ncaracal --version\ncaracal status --help\n```\n\nUse `caracal` for local lifecycle, workload execution, and upgrades. Use the packaged web console at `http://localhost:3001` after `caracal up` for the standard local product-management path.\n\n## Verify Docker\n\nRunning the open-source stack locally requires Docker 25+ with the Compose v2 plugin:\n\n```sh\ndocker compose version\n```\n\nOn Windows PowerShell:\n\n```powershell\ndocker compose version\n```\n\nThe local stack includes PostgreSQL, Redis, and the packaged web console. You do not need to install those databases separately for Get Started.\n\n## Start the Local Stack\n\n```sh\ncaracal up\ncaracal status --ready\n```\n\nOn Windows PowerShell:\n\n```powershell\ncaracal up\ncaracal status --ready\n```\n\nAfter readiness succeeds, open [http://localhost:3001](http://localhost:3001). Compose maps the host address `127.0.0.1:3001` to the web BFF container on port `3002`. Before your first sign-in, run `caracal allowlist add <email>` on the host to permit your email, and configure a console sign-in method as described in [First Protected Call](/get-started/first-protected-call/#sign-in-and-create-your-first-zone).\n\n## Common Mistakes\n\n- Do not run the source-tree `pnpm caracal` command for this evaluator path. Install the released `caracal` executable.\n- Do not use `caracal purge` to recover from a normal startup error; it intentionally removes local state.\n- If the shell cannot find `caracal` after installation, reopen it or add the install directory to `PATH`.\n\n## Expected Outcome\n\n`caracal status --ready` exits successfully and [http://localhost:3001](http://localhost:3001) opens the packaged web console. Readiness proves the local dependencies are available; it does not validate a policy or protected Resource yet.\n\n## Platform Notes\n\n| Platform | Architectures | Notes |\n| --- | --- | --- |\n| Linux | `amd64`, `arm64` | Requires `curl` or `wget`, `tar`, and `sha256sum` or `shasum`. Archive checksums are always verified; installing GitHub CLI `gh` additionally verifies provenance attestations. |\n| macOS | `amd64`, `arm64` | GitHub CLI `gh` enables provenance verification; if Gatekeeper quarantines the binary, remove quarantine from the installed file. |\n| Windows | `amd64` | GitHub CLI `gh` enables provenance verification; open a new shell after updating the user `Path`. |\n\n## Next Step\n\nContinue with [First Protected Call](/get-started/first-protected-call/)."},{"id":"/v0.2/get-started/first-protected-call","title":"First Protected Call","url":"https://docs.caracal.run/v0.2/get-started/first-protected-call/","concepts":"","body":"This evaluator walkthrough creates the baseline every tutorial assumes: a running stack, one Zone, one confidential Application, one Resource, one active policy, and one audited Gateway call.\n\n## Prerequisites\n\n- Complete [Install Caracal](/get-started/install-caracal/).\n- Choose an email address you can use with the configured console sign-in method.\n- Keep a terminal open for lifecycle commands.\n\n## Start Caracal\n\n```sh\ncaracal up\ncaracal status --ready\n```\n\nOn Windows PowerShell:\n\n```powershell\ncaracal up\ncaracal status --ready\n```\n\nCreate product objects only after readiness succeeds. If readiness fails, run:\n\n```sh\ncaracal status --ready --json\n```\n\nThe JSON output shows which service is not ready.\n\n## Start Demo Upstream\n\nStart a local HTTP upstream on the released stack's Docker network:\n\n```sh\ndocker run --rm -d --name nucleusUpstream --network caracalData nginx:stable-alpine\n```\n\nOn Windows PowerShell:\n\n```powershell\ndocker run --rm -d --name nucleusUpstream --network caracalData nginx:stable-alpine\n```\n\nUse `http://nucleusUpstream:80` as the Resource upstream URL. A source-tree development stack uses its isolated development network instead; this walkthrough targets the released runtime installed on the previous page.\n\n## Sign In and Create Your First Zone\n\nOpen the packaged web console at [http://localhost:3001](http://localhost:3001). The Compose stack maps that localhost port to the web BFF container and serves the SPA same-origin with the console API.\n\nThe packaged console runs the fail-closed production posture: operator registration is closed by default. Admission is controlled from the runtime host. Allow your email:\n\n```sh\ncaracal allowlist add <your email>\n```\n\nThe allowlist is read live by the console, so the change applies immediately. Entries are exact emails or `@domain` suffixes, and the same command manages access later: `lock`/`unlock` suspend and restore sign-in with the account kept intact, `remove` ends access and erases the account's sign-in records, and `list` shows every entry. See [Control Console Access](/runtime-console/console-access/) for the full lifecycle and the uniform access-denied page.\n\nAn allowlisted email still needs a sign-in method. Add one of the following to `$CARACAL_HOME/caracal.env` (Linux default `~/.local/share/caracal/caracal.env`, macOS `~/Library/Application Support/caracal/caracal.env`, Windows `%LOCALAPPDATA%\\caracal\\caracal.env`), then rerun `caracal up`:\n\n| Sign-in method | Required variables |\n| --- | --- |\n| Google or GitHub | `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` or `GITHUB_CLIENT_ID` + `GITHUB_CLIENT_SECRET`. OAuth callback URL: `http://localhost:3001/api/auth/callback/google` or `.../github`. |\n| Email and password | `CARACAL_PASSWORD_SIGNUP=true`, plus `CARACAL_SMTP_URL` and `CARACAL_SMTP_FROM` so the required verification email can be delivered; the console fails closed without a mail transport. |\n\nFor OAuth, create an OAuth app in the [Google Cloud console](https://console.cloud.google.com/apis/credentials) or [GitHub developer settings](https://github.com/settings/developers), set its callback URL to the value above, and paste the client ID and secret into `caracal.env`. With email/password sign-up, registration sends a verification link over SMTP and the account signs in after the link is confirmed.\n\nSee [Environment Variables](/operations/env-vars/#web-console-bff) for the full sign-in reference. Sign in, then complete the short onboarding flow:\n\n1. **Profile** - your operator name and avatar.\n2. **Zone** - create your first zone. A zone is Caracal's primary trust boundary; it isolates applications, resources, policies, and audit.\n3. **Review** - confirm and finish.\n\nOnboarding lands you in the console for that zone. The browser address follows an `account → org → zone` hierarchy (`/<accountId>/<orgId>/<zoneId>/app/...`); use the zone selector to switch zones, and every zone-scoped page acts on the selected zone.\n\n## Build the Access Chain with Guided Setup\n\nThe console opens **Guided setup**, a short walkthrough that explains each building block and opens its real create form. Work through the steps in order:\n\n| Step | What you create |\n| --- | --- |\n| Application | Create **Son of Anton** as a confidential Application. Copy its client secret; every later reveal is audited. |\n| Provider | The upstream credential source. Skip this step for an internal HTTP API that needs no upstream credential. |\n| Resource | Create **Nucleus** with identifier `resource://nucleus`, scope `nucleus:read`, and upstream URL `http://nucleusUpstream:80`. |\n| Policy | Create and activate a starter policy that allows Son of Anton to request `nucleus:read`. The Zone denies requests without an active policy. |\n\nGuided setup ticks each step off from live zone data and opens the same Applications, Providers, Resources, and Policies forms you reach from the console navigation.\n\n## Configure Runtime Access\n\nThe console does not write configuration to your machine, and `caracal run` needs no local config file. The workload carries only its identity; everything else lives in the console:\n\n1. Open the **Launcher** page and create a workload. The workload secret is held sealed in the Secret Store - copy it now or reveal it later from the workload's detail panel; every reveal is recorded in the zone audit timeline.\n2. Add a launch binding named `CARACAL_RESOURCE_NUCLEUS_TOKEN`, bound to `resource://nucleus` with `nucleus:read`. The page then shows the exact launch commands.\n3. Give the workload its identity:\n\n```sh\nexport CARACAL_WORKLOAD_ID=<workload ID>\nexport CARACAL_WORKLOAD_SECRET=<workload secret>\n```\n\nThese configure the launcher itself; they are not passed through to the child process. Instead of the inline secret, local dev and stable launches can read the owner-only file at `<Caracal config dir>/runtime/<workload_id>/secret`, and cloud deployments can point `CARACAL_WORKLOAD_SECRET_FILE` at a mounted secret; see [Configure Workloads](/"},{"id":"/v0.2/get-started/add-sdk-to-your-app","title":"Add SDK to Your App","url":"https://docs.caracal.run/v0.2/get-started/add-sdk-to-your-app/","concepts":"","body":"This page proves that your own code can call the Resource created in the evaluator flow. Choose one language. Each example uses the configured Application and creates the bounded Sessions and Delegation needed by `applicationTransport`.\n\n## Prerequisites\n\n- Complete [First Protected Call](/get-started/first-protected-call/).\n- Keep Caracal and `nucleusUpstream` running.\n- Install one supported runtime from the table below.\n\n## Author the Runtime Profile\n\nFirst Protected Call leaves you with a zone ID, an application ID, a client secret file, and a resource. All SDKs read those values from a `caracal.toml` profile or from `CARACAL_*` environment variables. The profile is a file you author with the IDs from guided setup - guided setup does not write it for you:\n\n```toml\nzone_id = \"<Pied Piper Production zone ID>\"\napplication_id = \"<Son of Anton application ID>\"\napp_client_secret_file = \"/path/to/son-of-anton-client-secret\"\n\n[[credentials]]\nresource = \"resource://nucleus\"\nupstream_prefix = \"http://nucleusUpstream:80\"\n```\n\nOn the local stack the SDK defaults cover the STS, Coordinator, and Gateway URLs. The profile must name the owner-only client-secret file explicitly; the SDK never searches local credential directories. The secret comes from application creation and can be revealed from the application's detail panel, with every reveal audited. For cloud or custom deployments add `sts_url`, `coordinator_url`, and `gateway_url`; see [Configuration File](/runtime-console/config-file/) for the full schema.\n\nThe profile identifies the workload, not each agent. `zone_id` and `application_id` name the one confidential application your service runs as; every Session the SDK starts executes under that application identity, so one profile serves all the agents your workload creates.\n\nPoint the examples at the profile and choose which configured resource and path they call:\n\n```sh\nexport CARACAL_CONFIG=/path/to/caracal.toml\nexport CARACAL_RESOURCE_ID=resource://nucleus\nexport CARACAL_RESOURCE_PATH=/\nexport CARACAL_RESOURCE_SCOPE=nucleus:read\n```\n\nOn Windows PowerShell:\n\n```powershell\n$env:CARACAL_CONFIG = \"C:\\path\\to\\caracal.toml\"\n$env:CARACAL_RESOURCE_ID = \"resource://nucleus\"\n$env:CARACAL_RESOURCE_PATH = \"/\"\n$env:CARACAL_RESOURCE_SCOPE = \"nucleus:read\"\n```\n\n## Install the SDK\n\nInstall the package for your application language.\n\n| Language   | Install command                                           | Runtime requirement |\n| ---------- | --------------------------------------------------------- | ------------------- |\n| TypeScript | `npm install @caracalai/sdk` or `pnpm add @caracalai/sdk` | Node.js 22+         |\n| Python     | `pip install caracalai-sdk`                               | Python 3.12+        |\n| Go         | `go get github.com/garudex-labs/caracal/packages/sdk/go`  | Go 1.26+            |\n\n## TypeScript\n\n```typescript\nimport { Caracal } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\nconst resourceId = process.env.CARACAL_RESOURCE_ID\nconst resourcePath = process.env.CARACAL_RESOURCE_PATH\nconst resourceScope = process.env.CARACAL_RESOURCE_SCOPE\n\nif (!resourceId) throw new Error('CARACAL_RESOURCE_ID is required')\nif (!resourcePath) throw new Error('CARACAL_RESOURCE_PATH is required')\nif (!resourceScope) throw new Error('CARACAL_RESOURCE_SCOPE is required')\n\nconst governedFetch = caracal.applicationTransport(resourceId, {\n    scopes: [resourceScope],\n})\nconst target = caracal.gatewayRequest(resourceId, resourcePath)\ntry {\n    const response = await governedFetch(target.url, {\n        method: 'GET',\n    })\n    if (!response.ok) throw new Error(`protected call failed: ${response.status}`)\n    console.log(await response.text())\n} finally {\n    await caracal.close()\n}\n```\n\n## Python\n\n```python\nimport asyncio\nimport os\n\nfrom caracalai import Caracal\n\ncaracal = Caracal()\nresource_id = os.environ[\"CARACAL_RESOURCE_ID\"]\nresource_path = os.environ[\"CARACAL_RESOURCE_PATH\"]\nresource_scope = os.environ[\"CARACAL_RESOURCE_SCOPE\"]\n\nasync def main():\n    target = caracal.gateway_request(resource_id, resource_path)\n    try:\n        async with caracal.application_transport(\n            resource_id,\n            scopes=[resource_scope],\n        ) as governed:\n            response = await governed.get(target.url)\n            response.raise_for_status()\n            print(response.text)\n    finally:\n        await caracal.aclose()\n\nasyncio.run(main())\n```\n\n## Go\n\n```go\npackage main\n\nimport (\n    \"fmt\"\n    \"io\"\n    \"os\"\n\n    caracal \"github.com/garudex-labs/caracal/packages/sdk/go\"\n)\n\nfunc main() {\n    c, err := caracal.New()\n    if err != nil {\n        panic(err)\n    }\n\n    resourceID := os.Getenv(\"CARACAL_RESOURCE_ID\")\n    resourcePath := os.Getenv(\"CARACAL_RESOURCE_PATH\")\n    resourceScope := os.Getenv(\"CARACAL_RESOURCE_SCOPE\")\n    if resourceID == \"\" || resourcePath == \"\" || resourceScope == \"\" {\n        panic(\"CARACAL_RESOURCE_ID, CARACAL_RESOURCE_PATH, and CARACAL_RESOURCE_SCOPE are required\")\n    }\n    defer c.Close()\n\n    governed, err := c.ApplicationTransport(nil, resourceID, caracal.ApplicationTransportOptions{\n        Scopes: []string{resourceScope},\n    })\n    if err != nil {\n        panic(err)\n    }\n    target, err := c.GatewayRequest(resourceID, resourcePath)\n    if err != nil {\n        panic(err)\n    }\n    resp, err := governed.Get(target.URL)\n    if err != nil {\n        panic(err)\n    }\n    defer resp.Body.Close()\n    if resp.StatusCode < 200 || resp.StatusCode >= 300 {\n        panic(fmt.Sprintf(\"protected call failed: %s\", resp.Status))\n    }\n    body, _ := io.ReadAll(resp.Body)\n    fmt.Println(string(body))\n}\n```\n\n## Expected Outcome\n\nRun the example in your selected language. It should print the Nginx welcome page and create matching authorization and Gateway action-result events in **Audit**. A constructor or profile error means no request reached Caracal; a policy denial appears in Audit with a request ID.\n\n## SDK Calls\n\n- `new Caracal()` / `Caracal()` / `New()` loads runtime configu"},{"id":"/v0.2/get-started/first-run-troubleshooting","title":"First-Run Troubleshooting","url":"https://docs.caracal.run/v0.2/get-started/first-run-troubleshooting/","concepts":"","body":"Use this page when the Get Started path fails before your first protected call or first SDK integration succeeds. For production incidents and deeper operational diagnosis, use [Troubleshoot by Symptom](/operations/troubleshooting/).\n\n## Prerequisite\n\nRun the failing step once and keep its request ID or exact error. Diagnose from the first failing boundary below rather than changing policy, credentials, and routing at the same time.\n\n## Readiness Failures\n\nRun:\n\n```sh\ncaracal status --ready --json\n```\n\nOn Windows PowerShell:\n\n```powershell\ncaracal status --ready --json\n```\n\n| Symptom                  | Check                                                                                                              |\n| ------------------------ | ------------------------------------------------------------------------------------------------------------------ |\n| Docker command fails     | Confirm Docker Desktop or Docker Engine is running and `docker compose version` succeeds.                          |\n| A service is not ready   | Wait for the dependency named in the JSON output, then rerun readiness.                                            |\n| Ports are already in use | Stop the conflicting local process or change the runtime port configuration before starting Caracal.               |\n| Stack state looks stale  | Run `caracal down`, then `caracal up`. Use `caracal purge` only when you intentionally want to remove local state. |\n\n## Console Sign-In\n\n| Symptom                                               | Check                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |\n| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| Sign-up is rejected or the access-denied page appears | The packaged console closes registration by default and shows one uniform page for every allowlist denial. On the runtime host, run `caracal allowlist list` to inspect entries, then `caracal allowlist add <email>` or `caracal allowlist unlock <email>` - see [Control Console Access](/runtime-console/console-access/). Make sure a sign-in method is configured in `$CARACAL_HOME/caracal.env` - see [Sign In and Create Your First Zone](/get-started/first-protected-call/#sign-in-and-create-your-first-zone). |\n| Google or GitHub buttons are missing                  | Set both the client ID and client secret for the provider, then rerun `caracal up`.                                                                                                                                                                                                                                                                                                                                                                                                                                      |\n| Password sign-in is blocked pending verification      | The packaged console requires a verified email. Confirm `CARACAL_SMTP_URL` and `CARACAL_SMTP_FROM` are set and the verification message was delivered.                                                                                                                                                                                                                                                                                                                                                                   |\n\n## Workload Identity Issues\n\n| Symptom                             | Check                                                                                                                       |\n| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |\n| `workload identity not found`       | Export `CARACAL_WORKLOAD_ID` and provide a workload secret source.                                                          |\n| `no credential bindings configured` | Define launch bindings for the workload on the **Launcher** page in the web console.                                        |\n| Secret file is rejected             | Ensure the secret file exists and is readable only by the current user.                                                     |\n| SDK cannot load configuration       | Set `CARACAL_CONFIG` to your `caracal.toml` path, or pass the profile path using the SDK-supported configuration mechanism. |\n\nLinux and macOS:\n\n```sh\nexport CARACAL_WORKLOAD_ID=<workload ID>\nexport CARACAL_WORKLOAD_SECRET=<workload secret>\n```\n\nWindows PowerShell:\n\n```powershell\n$env:CARACAL_WORKLOAD_ID = \"<workload ID>\"\n$env:CARACAL_WORKLOAD_SECRET = \"<workload secret>\"\n```\n\n## Lost Workload Secret\n\nThe workload secret is held sealed in the Secret Store. If you lose your local copy, reveal it again from the web console **Launcher** page - each reveal is recorded in the zone audit timeline - or rotate the secret and update your secret file or environment with the new value.\n\n## STS 403\n\nAn STS 403 means the workload authenticated but did not receive a mandate.\n\n| Check             | Fi"},{"id":"/v0.2/tutorials","title":"Tutorials","url":"https://docs.caracal.run/v0.2/tutorials/","concepts":"","body":"Tutorials begin after [Get Started](/get-started/). They do not repeat installation or first-call setup. They help you replace the demo with one real HTTP boundary, add a governed Session to app code, inspect one trace, and choose a production guide.\n\n## Tutorial Path\n\n| Outcome                                                                       | Tutorial                                                             | What you use                                                                                    |\n| ----------------------------------------------------------------------------- | -------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |\n| Put an enforced boundary in front of one real HTTP service or provider route. | [Protect Your First Real API](./protect-an-api/)                     | web console **Resources**, **provider**, **policy**, Gateway, and audit.                        |\n| Wire application code through Caracal with a runtime profile.                 | [Connect Your App with the SDK](./connect-an-agent/)                 | TypeScript, Python, or Go SDK; Session; Gateway.                                                |\n| Prove why one protected request succeeded or failed.                          | [Trace One Protected Request](./inspect-a-run/)                      | web console **Audit**, **request trace**, sessions, policy diagnostics, and delegation context. |\n| Choose the durable guide for your production boundary.                        | [Choose Your Production Integration Path](./choose-production-path/) | Gateway, SDK, adapter, runtime, Delegation, audit, and Approval guides.                         |\n\n## Before You Begin\n\nYou should have completed [First Protected Call](/get-started/first-protected-call/) or equivalent setup:\n\n- a running Caracal stack;\n- one zone;\n- one confidential Application;\n- one protected resource;\n- one active policy set;\n- a working SDK example from [Add SDK to Your App](/get-started/add-sdk-to-your-app/).\n\nIf any prerequisite is missing, return to Get Started. Tutorials assume the evaluator stack already works.\n\n## After Tutorials\n\nUse [Guides](/guides/) for task-specific implementation details, [SDKs](/sdks/) for package APIs, and [Concepts](/concepts/) when you need the reference model behind policy, delegation, revocation, or audit."},{"id":"/v0.2/tutorials/protect-an-api","title":"Protect Your First Real API","url":"https://docs.caracal.run/v0.2/tutorials/protect-an-api/","concepts":"","body":"This tutorial replaces Nucleus from Get Started with one HTTP API that belongs to your stack. It does not repeat Zone, Application, workload, or SDK setup.\n\nUse adapter-verified services only when you own the service boundary and can preserve the same enforcement contract inside that service.\n\n## Prerequisites\n\n- Complete [Add SDK to Your App](/get-started/add-sdk-to-your-app/).\n- Choose one HTTP endpoint the Gateway container can reach.\n- Know whether that endpoint needs an upstream credential.\n\n## 1. Choose What Caracal Should Protect\n\n| Boundary | Use when | What writes action-result audit |\n| --- | --- | --- |\n| Gateway-routed HTTP | The resource is HTTP, REST, MCP-over-HTTP, or another Gateway-routable upstream. | Gateway. |\n| In-process adapter | You own the service process and can verify mandates there. | Your service or adapter after adapter verification. |\n\nStart with Gateway-routed HTTP unless your service must enforce directly in process.\n\n## 2. Create or Select the Real Resource\n\nOpen the web console at [http://localhost:3001](http://localhost:3001).\n\nUse **Guided setup** for a first resource or open **Resources** when you are adding one to an existing zone.\n\nDefine:\n\n| Field | Guidance |\n| --- | --- |\n| Resource identifier | Use `resource://pipernet` for this tutorial. |\n| Scopes | Start with one action, `pipernet:read`. |\n| Upstream URL | Use a URL reachable from the Gateway. |\n| Provider | Select one for the route. Use a `None` provider only when Gateway should enforce access but the upstream needs no brokered credential. |\n\nKeep the resource ID. SDK code, policy, Gateway headers, and audit events all refer to it.\n\n## 3. Allow This App to Use This Resource\n\nCaracal denies by default. Use web console **Guided setup** to create the starter policy, or open **Policies** to register and activate a Rego policy set for the zone.\n\nA complete policy change should allow only:\n\n- the expected Application;\n- the expected resource;\n- the expected scopes;\n- the expected zone.\n\nSee [Author Policy Data](/guides/author-policy/) and [Activate a Policy Set](/guides/activate-policy-set/) for the policy contract.\n\n## 4. Keep Gateway Enforcement\n\n### Gateway-routed HTTP\n\nFor Gateway-routed calls, the resource route needs an upstream URL and, when required, a provider credential source. The Gateway receives:\n\n| Request part | Meaning |\n| --- | --- |\n| `Authorization: Bearer <mandate>` | The Caracal access token issued by STS. |\n| `X-Caracal-Resource: <resource-id>` | The stable resource identifier. |\n\nUse [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/) when you need the durable implementation guide for routes, provider auth modes, and validation.\n\nDo not switch to an adapter during this tutorial. The [production-path bridge](../choose-production-path/) links to adapters after you have verified this central boundary.\n\n## 5. Verify Allowed, Denied, and Audited Calls\n\nCall the resource from an app configured with your runtime profile or environment variables.\n\n| Outcome | What to expect |\n| --- | --- |\n| Allowed | The call reaches PiperNet and Audit shows authorization plus action-result evidence. |\n| Denied | STS returns a denial and the decision trace shows which policy or missing state caused it. |\n\nOpen **Audit** and filter by request ID. If authorization exists without an action result, the call did not complete through the Gateway; check the Gateway URL, Resource header, and upstream reachability.\n\n## Expected Outcome\n\nThe same SDK flow that reached Nucleus now reaches PiperNet through the Gateway, and Audit shows the PiperNet Resource, `pipernet:read`, the active policy version, and the upstream result under one request trace.\n\n## Common Mistakes\n\n- Do not reuse `resource://nucleus` for a different production target.\n- Do not put a provider secret in Application code; bind a credential Provider to the Resource.\n- Do not treat a successful direct upstream call as enforcement proof.\n\n## Next Step\n\nContinue with [Connect Your App with the SDK](../connect-an-agent/) to wire application code through the resource you just protected."},{"id":"/v0.2/tutorials/connect-an-agent","title":"Connect Your App with the SDK","url":"https://docs.caracal.run/v0.2/tutorials/connect-an-agent/","concepts":"","body":"This tutorial turns the working SDK call from Get Started into an identifiable agent run. The application-owned transport already creates a bounded Session pair and Delegation; you will label those Sessions so Audit can distinguish this run from other work under the same Application.\n\n## Prerequisites\n\n- Complete [Protect Your First Real API](../protect-an-api/).\n- Keep the working profile and SDK example from Get Started.\n- Set the Resource ID, path, and scope for PiperNet.\n\n## 1. Confirm the App and Profile\n\nIn the Console, confirm that the selected zone has:\n\n- one confidential Application;\n- a client secret stored in the secret file named by your runtime profile;\n- one protected resource with an active policy;\n- a Gateway URL and resource binding when using Gateway-routed HTTP.\n\nSet the profile path and the resource target for the examples:\n\n```sh\nexport CARACAL_CONFIG=/path/to/caracal.toml\nexport CARACAL_RESOURCE_ID=resource://pipernet\nexport CARACAL_RESOURCE_PATH=/\nexport CARACAL_RESOURCE_SCOPE=pipernet:read\n```\n\nOn Windows PowerShell:\n\n```powershell\n$env:CARACAL_CONFIG = \"C:\\path\\to\\caracal.toml\"\n$env:CARACAL_RESOURCE_ID = \"resource://pipernet\"\n$env:CARACAL_RESOURCE_PATH = \"/\"\n$env:CARACAL_RESOURCE_SCOPE = \"pipernet:read\"\n```\n\n## 2. Label the Application-Owned Call\n\nKeep the complete example from [Add SDK to Your App](/get-started/add-sdk-to-your-app/). Change only the `applicationTransport` options to add the label `pipernet-reader`. The SDK creates the source Session, target Session, and narrowed Delegation required for this Application-owned call.\n\n### TypeScript\n\n```sh\nnpm install @caracalai/sdk\n```\n\n```typescript\nconst governedFetch = caracal.applicationTransport(resourceId, {\n    scopes: [resourceScope],\n    labels: ['pipernet-reader'],\n})\n```\n\n### Python\n\n```sh\npip install caracalai-sdk\n```\n\n```python\nasync with caracal.application_transport(\n    resource_id,\n    scopes=[resource_scope],\n    labels=[\"pipernet-reader\"],\n) as governed:\n    response = await governed.get(target.url)\n```\n\n### Go\n\n```sh\ngo get github.com/garudex-labs/caracal/packages/sdk/go\n```\n\n```go\ngoverned, err := c.ApplicationTransport(nil, resourceID, caracal.ApplicationTransportOptions{\n    Scopes: []string{resourceScope},\n    Labels: []string{\"pipernet-reader\"},\n})\nif err != nil {\n    panic(err)\n}\n```\n\n## 3. Understand the Runtime Behavior\n\n| SDK call                                | What happens                                                                            |\n| --------------------------------------- | --------------------------------------------------------------------------------------- |\n| `new Caracal()` / `Caracal()` / `New()` | Loads configuration and prepares app-secret token exchange.                             |\n| `gatewayRequest()` / `GatewayRequest()` | Builds the Gateway URL and `X-Caracal-Resource` header.                                 |\n| `applicationTransport()`                | Creates and caches a bounded Session pair and Delegation, then mints a fresh Mandate for each request. |\n\nOne Application credential backs many Sessions. Labels describe a role or run; they are not identity and do not grant scopes. Use the Session ID in Audit when exact attribution matters.\n\nIf your app starts a Session for each child agent or delegates work, continue to [Implement Multi-Agent Delegation](/guides/delegation/) after this tutorial path.\n\n## 4. Verify Audit\n\nOpen web console **Audit** and find the request. A complete Gateway-routed call produces:\n\n1. an authorization event from STS;\n2. an action-result event from Gateway.\n\nOpen **explain** with the request ID to confirm the application, resource, scopes, policy, policy set version, and decision.\n\n## Expected Outcome\n\nAudit shows the Son of Anton Application, the `pipernet-reader` label, distinct Session IDs, a narrowed Delegation, `resource://pipernet`, and the Gateway result. The label helps find the run but does not replace the Session ID.\n\n## Common Mistakes\n\n- Do not wrap `applicationTransport` in a plain Session expecting that Session to grant Resource authority; `applicationTransport` provisions its own bounded path.\n- Labels do not authorize access. Keep `pipernet:read` in the active policy.\n- Federating a Subject adds verified attribution and supported Subject approval context. It does not create per-Subject scope authorization.\n\n## Next Step\n\nContinue with [Trace One Protected Request](../inspect-a-run/) to investigate the request you just made."},{"id":"/v0.2/tutorials/inspect-a-run","title":"Trace One Protected Request","url":"https://docs.caracal.run/v0.2/tutorials/inspect-a-run/","concepts":"","body":"This tutorial shows how to prove what happened during one protected request. You will find the request, inspect the session context, review audit events, and trace the final decision.\n\n## Prerequisites\n\n- Complete [Connect Your App with the SDK](../connect-an-agent/).\n- Run one request and keep its request ID.\n- Keep the same Zone selected in the web console.\n\n## 1. Find the Request\n\nOpen the web console at [http://localhost:3001](http://localhost:3001).\n\nUse these web console sections:\n\n| Entry                | Use it for                                                           |\n| -------------------- | -------------------------------------------------------------------- |\n| **Audit**            | Find recent events, then open the decision trace for one request ID. |\n| **Sessions**         | Find the tracked Session for the selected Zone.                      |\n| **Authority record** | Inspect underlying Authority records and parent relationships.       |\n\nRecord the request ID from the SDK, Gateway, STS error, audit event, or web console trace.\n\n## 2. Read the Audit Timeline\n\nOpen **Audit**. The live audit view opens recent events immediately and supports request details, request tracing, advanced filters, reload, and pause/resume.\n\nA Gateway-routed protected call usually produces:\n\n| Event class         | What it proves                                                                             |\n| ------------------- | ------------------------------------------------------------------------------------------ |\n| Authorization event | STS evaluated policy for the app, resource, scopes, Session state, and delegation context. |\n| Action-result event | Gateway verified and routed the request, then recorded the upstream result.                |\n\nIf you see authorization events without matching action-result events, check whether the call reached the Gateway. For adapter-verified services, confirm the service writes its own result audit after mandate verification.\n\n## 3. Trace the Decision\n\nPick an audit event with a request ID. Press the trace action on the selected event in **Audit** to open its decision trace.\n\nThe request trace should show:\n\n- application and execution session;\n- resource and requested scopes;\n- policy set version and policy rules involved;\n- allow, deny, or partial decision;\n- diagnostics from policy evaluation;\n- Delegation constraints when present.\n\nUse the request trace as the source of truth for why a request got its result. If an app behaves differently than expected, compare the trace against the active policy set, resource configuration, and runtime profile.\n\n## 4. Check Delegation Context\n\nIf the request used delegated authority, open **Delegation** and inspect the edge:\n\n| Field                | Why it matters                                       |\n| -------------------- | ---------------------------------------------------- |\n| Source session       | The session that granted authority.                  |\n| Target session       | The session allowed to use the delegated permission. |\n| Scopes               | The narrowed actions.                                |\n| TTL and hop limits   | The lifetime and graph depth of the permission.      |\n| Resource constraints | The targets the delegated permission can affect.     |\n\nUse [Implement Multi-Agent Delegation](/guides/delegation/) when you need to design or debug delegation flows.\n\n## 5. Check Revocation Behavior\n\nRevoking a session, grant, Delegation, or zone/application emergency state invalidates authority anchored to it. Gateway-routed calls check revocation before accepting a request and while streaming responses. Audit records revocation events and marks interrupted action results.\n\n## Expected Outcome\n\nYou can identify the Application, Session, Resource, requested scopes, active policy version, final decision, and Gateway result for one request ID. If a Subject appears, treat it as federated attribution unless the trace shows a supported Subject approval flow; it is not evidence of per-Subject authorization.\n\n## Next Step\n\nContinue with [Choose Your Production Integration Path](../choose-production-path/)."},{"id":"/v0.2/tutorials/choose-production-path","title":"Choose Your Production Integration Path","url":"https://docs.caracal.run/v0.2/tutorials/choose-production-path/","concepts":"","body":"Use this bridge after the tutorials. You should have one working Gateway call and one trace. Pick one production boundary; do not combine patterns until that boundary is verified.\n\n## Prerequisite\n\nComplete [Trace One Protected Request](../inspect-a-run/) or be able to explain an equivalent request from Application to Resource.\n\n## Choose by Boundary\n\n| If your production path is...                               | Continue with                                                                                                    |\n| ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |\n| Route HTTP traffic through Caracal Gateway                  | [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/)                                               |\n| Add Caracal session and Gateway calls to app code           | [TypeScript SDK](/guides/sdk-typescript/), [Python SDK](/guides/sdk-python/), or [Go SDK](/guides/sdk-go/)       |\n| Protect an Express resource server                          | [Protect an Express App](/guides/protect-express/)                                                               |\n| Protect a FastMCP resource server                           | [Protect a FastMCP App](/guides/protect-fastmcp/)                                                                |\n| Protect a Go `net/http` resource server                     | [Protect a Go net/http Service](/guides/protect-nethttp/)                                                        |\n| Protect an MCP server without a dedicated adapter           | [Protect an MCP Server](/guides/protect-mcp/)                                                                    |\n| Run an existing CLI or worker under injected runtime tokens | [Run an Agent with caracal run](/guides/runtime-run/)                                                            |\n| Model zones, apps, resources, and customer boundaries       | [Model Your Application in Caracal](/guides/modeling-recipes/)                                                   |\n| Configure provider credentials or OAuth                     | [Define Resources and Providers](/guides/resources-providers/) and [Provider Recipes](/guides/provider-recipes/) |\n| Debug denies or unexpected allows                           | [Debug Authorization Decisions](/guides/authorize-access/)                                                       |\n| Delegate work between agents                                | [Implement Multi-Agent Delegation](/guides/delegation/)                                                          |\n| Export or query audit evidence                              | [Tail and Query the Audit Stream](/guides/audit-stream/)                                                         |\n| Hold sensitive actions for human approval                   | [Human Approval](/guides/step-up/)                                                                               |\n\n## Choose by Team Role\n\n| Role              | Start with                                                                                                                                                               |\n| ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| App engineer      | SDK guide for your language, then Gateway or adapter guide.                                                                                                              |\n| Platform engineer | [Production Integration Patterns](/guides/production-patterns/), then operations pages.                                                                                  |\n| Security reviewer | [Debug Authorization Decisions](/guides/authorize-access/), [Audit and Request Traces](/concepts/audit-ledger/), and [Review the Threat Model](/security/threat-model/). |\n| Policy owner      | [Author Policy Data](/guides/author-policy/) and [Activate a Policy Set](/guides/activate-policy-set/).                                                                  |\n\n## Expected Outcome\n\nYou leave this page with one primary enforcement boundary and one implementation guide. Record that choice before adding Delegation, Approvals, or alternative adapters.\n\n## Next Step\n\nOpen [Guides](/guides/) and follow the path that matches your boundary."},{"id":"/v0.2/guides","title":"Guides","url":"https://docs.caracal.run/v0.2/guides/","concepts":"","body":"Use Guides after [Get Started](/get-started/) when you have a concrete integration job. These pages teach complete application-integrator and resource-server workflows; package and API pages remain the source for signatures and wire fields.\n\n## When to use this section\n\n- **Application integrators** start with an SDK guide, then route outbound calls through Gateway or `caracal run`.\n- **Resource-server integrators** start with Gateway routing or the adapter matching their server framework.\n- **Platform integrators** use the modeling, resource/provider, policy, testing, and audit workflows before production traffic.\n\n## Choose by Task\n\n| Task                                                 | Start with                                                                                                                                                                                    |\n| ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Map your architecture onto Caracal                   | [Model Your Application in Caracal](/guides/modeling-recipes/)                                                                                                                                |\n| Serve many of your own customers from one deployment | [Serve Your Own Customers](/guides/serve-customers/)                                                                                                                                          |\n| Define protected targets and upstream credentials    | [Define Resources and Providers](/guides/resources-providers/) and [Provider Recipes](/guides/provider-recipes/)                                                                              |\n| Write and activate authorization logic               | [Author Policy Data](/guides/author-policy/) and [Activate a Policy Set](/guides/activate-policy-set/)                                                                                        |\n| Debug an authorization result                        | [Debug Authorization Decisions](/guides/authorize-access/)                                                                                                                                    |\n| Add Caracal to app code                              | [TypeScript SDK](/guides/sdk-typescript/), [Python SDK](/guides/sdk-python/), or [Go SDK](/guides/sdk-go/)                                                                                    |\n| Run an existing process with Caracal tokens          | [Run an Agent with caracal run](/guides/runtime-run/)                                                                                                                                         |\n| Protect a Gateway-routed HTTP upstream               | [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/)                                                                                                                            |\n| Protect a resource server in process                 | [Express](/guides/protect-express/), [FastAPI](/guides/protect-fastapi/), [FastMCP](/guides/protect-fastmcp/), [Go net/http](/guides/protect-nethttp/), or [MCP server](/guides/protect-mcp/) |\n| Add Delegation, audit export, or Approval            | [Delegation](/guides/delegation/), [Audit Stream](/guides/audit-stream/), or [Human Approval](/guides/step-up/)                                                                               |\n| Plan a production integration                        | [Production Integration Patterns](/guides/production-patterns/)                                                                                                                              |\n\n## Recommended Order\n\n## Surface Boundaries\n\nUse the right surface for each task:\n\n| Surface                                                                       | Use for                                                                                                                        |\n| ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |\n| `caracal up`, `down`, `status`, `upgrade`, `purge`, `allowlist`, and `run`    | Local runtime lifecycle, Console sign-in admission, and subprocess injection.                                                  |\n| Console                                                                       | Human-facing zone, application, provider, resource, policy, session, audit, explanation, delegation, and diagnostic workflows. |\n| Admin API and `@caracalai/admin`                                              | Automation for the same control-plane objects.                                                                                 |\n| SDKs and adapters                                                             | Application integration, context propagation, mandate exchange, and mandate verification.                                      |\n\n## Before You Start\n\nYou need a running Caracal runtime, a zone, an application, at least one resource, and an active policy set. [First Protected Call](/get-started/first-protected-call/) creates that baseline.\n\n## Expected result\n\nAfter following one path through the table, an allowed call reaches exactly one protected resource, a denied call fails before protected work runs, and both outcomes can be found by request ID in **Audit**.\n\n:::caution[Common mistake]\nUse the linked [SDK and package reference](/sdks/) for exact release signatures. Guides own sequencing, boundary choices, validation, and recovery—not duplicate API catalogs.\n:::\n\n## Next step\n\nChoose the first unfinished job in **Choose by Task**. For a new integration, start with [Model Your Applica"},{"id":"/v0.2/guides/modeling-recipes","title":"Model Your Application in Caracal","url":"https://docs.caracal.run/v0.2/guides/modeling-recipes/","concepts":"","body":"Use this guide before creating production objects or when an existing zone has become hard to reason about. It turns deployment boundaries into a reviewed model; the [concept pages](/concepts/) remain the canonical definitions.\n\n## Prerequisites\n\n- A list of workloads, protected upstreams, deployment environments, and policy owners.\n- A decision about which systems require separate signing keys and audit trails.\n- The stable actions each upstream exposes; do not start from hostnames or current credentials.\n\nThe deliverable is a short model table naming each zone, application, resource, provider, and scope. Review it with application and resource-server owners before provisioning.\n\nEvery recipe states the modeling decision, what maps to each noun, and the trade-off. Use the [Caracal Mental Model](/concepts/model-overview/) as the vocabulary reference while you read.\n\n## Choose the Zone Boundary\n\nThe most common question is “what is a zone - an environment, a customer, a team, or a product area?” A zone is none of those by default. It is the isolation boundary that owns signing keys, policy sets, sessions, audit, and authority data. You decide what trust boundary it represents.\n\nUse a **separate zone** whenever two workloads must not share signing keys, policy activation, or audit trails. Use a **shared zone with separate resources** when workloads belong to the same trust boundary but target different upstreams.\n\n:::note[FAQ]\n[What should a zone represent?](/reference/faq/#faq-003) and [does this repository implement managed multi-tenancy?](/reference/faq/#faq-004)\n:::\n\n| Question                                                                    | If yes, lean toward           |\n| --------------------------------------------------------------------------- | ----------------------------- |\n| Must these workloads have independent signing keys and JWKS?                | Separate zones                |\n| Must a policy change for one never affect the other?                        | Separate zones                |\n| Must audit and explain traces never mix?                                    | Separate zones                |\n| Do they share keys, policy owners, and audit, but call different upstreams? | One zone, separate resources  |\n| Do they share an upstream but need different actions?                       | One resource, separate scopes |\n\nKeep [resource identifiers](/concepts/resource-grant/) stable across zones so policy, grants, and audit refer to the same target even when upstream URLs differ.\n\n## Single Application, Single Environment\n\nThe simplest deployment: one team, one runtime, a handful of upstreams.\n\n| Noun        | Mapping                                                                                                                                         |\n| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |\n| Zone        | One zone for the whole deployment.                                                                                                              |\n| Application | One managed application per durable workload; one DCR application per isolated, externally-launched identity (per tenant, job, or integration). |\n| Resource    | One resource per protected upstream, with action-oriented scopes.                                                                               |\n| Grant       | One grant per application and subject that may request a resource's scopes.                                                                     |\n\nTrade-off: lowest operational overhead. Add zones only when you need key, policy, or audit isolation.\n\n## Per-Environment Zones\n\nSeparate production, staging, and development so a policy or key change in one cannot affect another.\n\n| Noun       | Mapping                                                                                                              |\n| ---------- | -------------------------------------------------------------------------------------------------------------------- |\n| Zone       | One zone per environment: `prod`, `staging`, `dev`.                                                                  |\n| Resource   | The same stable identifier in each zone, such as `resource://pipernet`, pointing at that environment's upstream URL. |\n| Policy set | Authored and activated independently per zone.                                                                       |\n| Keys       | Each zone has its own signing key and JWKS.                                                                          |\n\nTrade-off: clean blast-radius isolation and independent key rotation, at the cost of registering resources and activating policy in each zone. Automate this with the Admin API so environments stay consistent.\n\nIn application code, identities stay isolated at the client boundary: one SDK client binds exactly one `(zone, application)` identity during concurrent use. A workload that acts as several Applications in one Zone or talks to several Zones constructs one `Caracal` per `(zone, application)` with that identity's credentials and routes work to the matching client. Do not multiplex active identities through one client by swapping resolver output; token, mandate, Session, hook, and shutdown state belong to one identity boundary.\n\n## Multiple Customers or Workspaces\n\nA platform with many customer workspaces and one shared agent service must choose how customer isolation maps onto Zones. This open-source product gives you the **Zone** as the isolation primitive; you provision and automate customer onboarding yourself through the Admin API. Managed tenant, team, and SSO lifecycle are not implemented in this repository.\n\n| Model                                 | When to use                                                                                                                | Trade-off                   "},{"id":"/v0.2/guides/serve-customers","title":"Serve Your Own Customers","url":"https://docs.caracal.run/v0.2/guides/serve-customers/","concepts":"","body":"[Model Your Application in Caracal](/guides/modeling-recipes/) compares the isolation models at a high level. This page is the end-to-end pattern for the most common case: your application is a single product that serves many of _its own_ customers, and you want clean per-customer authority without standing up infrastructure for each customer.\n\n## When to use this pattern\n\nUse one shared zone only when customers share signing keys, policy ownership, audit storage, and operational rate limits. If any customer requires cryptographic or audit isolation, use a zone per customer and automate provisioning yourself.\n\n## Prerequisites\n\n- An application-owned, authenticated customer ID that is stable and non-personal.\n- A reviewed label vocabulary and grant/confinement data.\n- Capacity and audit filters tested against expected customer concurrency.\n\nThe short answer: run **one zone for your deployment** and carry **each customer on the work itself** through a `customer:` label that policy confines and audit can filter. Keep `customer_id` metadata only when Coordinator inspection needs an additional business key. Customer separation lives below the zone - in Sessions, labels, and Delegation - which is exactly where Caracal models per-actor authority. You do not create a zone per customer.\n\n:::note[FAQ]\n[What should a zone represent?](/reference/faq/#faq-003) and [does this repository implement managed multi-tenancy?](/reference/faq/#faq-004)\n:::\n\n## Where Each Customer Lives in the Model\n\n| Layer                       | Owns                                                                                                                        | Per-customer?                  |\n| --------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------ |\n| Zone                        | Signing keys, policy set, resources, providers, audit trail                                                                 | No - one per deployment        |\n| Application                 | Your product's service identity                                                                                             | No - shared by all customers   |\n| Customer label and metadata | The customer the work is for: a `customer:<id>` label policy and audit can filter; optional `customer_id` metadata remains Coordinator inspection data | Yes - stamped on every Session |\n| Session                     | One agent run, labeled and attributed to one customer                                                                       | Yes - one per customer task    |\n| Delegation                  | The scoped authority that agent holds for one resource                                                                      | Yes - least privilege per task |\n\nA zone is the trust boundary that owns keys, policy, and audit. A customer is not a trust boundary of its own here; it is an _attribute_ of the work - carried as a label the platform decision contract confines and a metadata key the audit trail filters. Keep customer identity in labels and metadata - never in [scope names](/concepts/resource-grant/).\n\n## Step 1: One Zone, One Application\n\nProvision a single zone for the deployment and one managed application for your product, as in [Model Your Application in Caracal](/guides/modeling-recipes/). Set the application identity once in the environment; it is shared across all customers.\n\n```bash\nCARACAL_ZONE_ID=\"<your-zone-id>\"\nCARACAL_APPLICATION_ID=\"<your-application-id>\"\nCARACAL_APP_CLIENT_SECRET=\"<your-application-secret>\"\n```\n\nThe zone owns one policy set and one audit trail. Every customer's authority is decided and recorded inside it.\n\n## Step 2: Choose a Stable Customer Identifier\n\nCaracal does not authenticate your customers, does not own your user directory, and never generates a customer identifier. Your application's existing authentication system - Auth0, Keycloak, Better Auth, a custom issuer - authenticates the customer; Caracal receives the identifier your application asserts and treats it as an opaque, stable string. It does not need to know what the identifier represents, only that the same customer always presents the same value.\n\nChoose an id that never changes for the life of the account - an account UUID, not an email or display name - so audit history stays attributable even after profile changes. Do not reuse one identifier across two customers: separation is only as strong as your identifier discipline.\n\nThe identifier enters Caracal on every Session your application starts for that customer as a `customer:<id>` label. Policy and audit consume that label. Add a `customer_id` metadata key only for direct Coordinator inspection; Session metadata is not STS policy or audit input. The `sub` recorded on the underlying STS Authority record is your application's identity from the client-credentials chain; the customer rides on the work itself.\n\n## Step 3: Start a per-Customer Session with a Correlation Key\n\nStart the Session for a customer's request inside the one zone and stamp the customer ID into a label. The label travels into policy and audit, making per-customer attribution a direct lookup instead of a guess. Optional metadata can support direct Coordinator inspection but is not copied into decision audit events.\n\n```python\nasync with caracal.session(\n  labels=[f\"customer:{customer_id}\"],\n    metadata={\"customer_id\": customer_id},\n) as ctx:\n    # Every gateway and provider call in this block carries a scoped,\n    # non-root mandate for this customer's work.\n    await do_work(ctx)\n```\n\nThe Session ID and Authority record are the exact authority anchors; the `customer:` label is the business correlation key exposed to policy and audit. Keep the optional `customer_id` metadata key aligned with that label when Coordinator inspection needs the raw value.\n\n:::caution[Attribution is not enforcement]\nSession metad"},{"id":"/v0.2/guides/resources-providers","title":"Define Resources and Providers","url":"https://docs.caracal.run/v0.2/guides/resources-providers/","concepts":"","body":"Use this guide after modeling an upstream and before writing policy or application code. The workflow creates the routing and credential boundary that Gateway enforces. The [Admin package](/sdks/admin/) and [Admin API](/api/control-plane/) hold exact automation signatures.\n\n## Prerequisites\n\n- A zone and durable managed application for Gateway routing.\n- A stable resource identifier, action-oriented scopes, and an upstream URL reachable from Gateway.\n- The upstream's documented authentication method and a production secret source.\n\n## When to create each object\n\n| Object              | Create it when                                                                                                                                                                          |\n| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Resource            | You need a stable protected target, Caracal resource scopes, an upstream URL, a Gateway application, and one upstream credential provider binding.                                      |\n| Provider            | A resource needs an explicit upstream auth mode: none, Caracal mandate, OAuth 2.0 authorization code, OAuth 2.0 client credentials, API key, bearer-token, or HTTP Basic upstream auth. |\n| Gateway application | You want Gateway-originated upstream calls to use a specific Caracal application identity.                                                                                              |\n\nResource fields should answer “what is being protected and where does Gateway send traffic?” Provider fields should answer “what credential or identity does Gateway attach upstream?” Keep credential details such as client secrets, token endpoints, API-key placement, bearer tokens, identity forwarding, and runtime injection on the provider. Keep target details such as resource identifier, Caracal resource scopes, upstream URL, Gateway application, and the selected upstream credential provider on the resource.\n\n:::note[FAQ]\n[What is the difference between a resource and a provider?](/reference/faq/#faq-009) and [is an application secret the same as a provider credential?](/reference/faq/#faq-013)\n:::\n\n## Web Console Workflow\n\n1. Start the local runtime with `caracal up`.\n2. Open the packaged web console at `http://localhost:3001`.\n3. Select **Resources**.\n4. Create a resource with a stable identifier, such as `resource://pipernet`.\n5. Add action-oriented scopes, such as `pipernet:read` and `pipernet:refund`.\n6. Set the upstream URL, Gateway application, and upstream credential provider for the protected service.\n7. Select a `None` provider when Gateway should enforce Caracal access but the upstream expects no credential.\n8. Select `Caracal mandate` for resources that verify Caracal tokens directly, or choose a provider-native type when the upstream needs external credentials.\n9. Attach exactly one upstream credential provider to the resource.\n10. For a path-addressed REST upstream, declare the operations the Gateway may invoke and keep operation authority `enforced`; choose `transport_uniform` for single-surface transports such as MCP.\n\n## Provider auth modes\n\n| Provider type                | Required main fields                                                                                                   | Runtime behavior                                                                                                                                              |\n| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| None                         | No credential fields.                                                                                                  | Gateway strips caller auth and forwards no upstream credential; use only when Gateway is the enforcement point and the upstream expects no credential.        |\n| Caracal mandate              | No credential fields.                                                                                                  | Gateway forwards the Caracal resource mandate in `Authorization: Bearer ...`; the resource verifies issuer, audience, scopes, target, expiry, and revocation. |\n| OAuth 2.0 authorization code | Authorization endpoint, token endpoint, redirect URI, client ID, client secret, optional upstream OAuth scopes.        | Creates per-subject provider connections through a browser consent callback, then refreshes the brokered upstream tokens inside STS.                          |\n| OAuth 2.0 client credentials | Token endpoint, client ID, grant type, client secret or private key depending on the grant and authentication choices. | STS obtains and caches service-to-service provider tokens for Gateway upstream calls, via `client_credentials` or RFC 7523 `jwt_bearer` assertion grants.     |\n| API key                      | Header name and API key.                                                                                               | Gateway forwards the sealed provider key in the configured header, with an optional auth scheme prefix.                                                       |\n| Bearer token                 | Bearer token.                                                                                                          | Gateway forwards the sealed provider token as `Authorization: Bearer ...` unless Advanced routing configures another header or scheme.                        |\n| HTTP Basic                   | Username and password.                                                                                                 | Gateway forwards `Authoriza"},{"id":"/v0.2/guides/provider-recipes","title":"Provider Recipes","url":"https://docs.caracal.run/v0.2/guides/provider-recipes/","concepts":"","body":"Use this page while creating a provider for a known upstream. First select the enforcement boundary, then apply one recipe, bind the resource, wire one language-native transport, and validate the whole path. Field schemas stay canonical in [Define Resources and Providers](/guides/resources-providers/) and the [Admin package reference](/sdks/admin/).\n\n## Prerequisites\n\n- The resource, scopes, Gateway application, and active policy already exist.\n- The upstream's official token or authentication documentation is available.\n- Production secrets are ready to enter through Console or a trusted Admin API process, never application source.\n\nProvider recipes live in the documentation, not the web console. The Console stays focused on the fields a given setup needs; this page is the reference you keep open beside it.\n\n## Choose the enforcement boundary first\n\nThe most common mistake is treating these four boundaries as interchangeable. They are not - they differ in where enforcement happens, who writes the action-result audit, and who holds the upstream credential.\n\n| Boundary                 | Enforcement point                          | Credential holder                | Action-result audit                     | Use when                                                                                                                  |\n| ------------------------ | ------------------------------------------ | -------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |\n| Gateway-mediated         | Caracal Gateway                            | Gateway (sealed provider secret) | Gateway                                 | The upstream is HTTP-routable and you want Caracal to broker the credential and enforce every call.                       |\n| Adapter-verified         | Your service process                       | Your service                     | Your service after adapter verification | You own the service and must enforce mandates in process.                                                                 |\n| Application-managed call | Your application code                      | Your application                 | Your application                        | You call the provider directly and only attach Caracal context for attribution. Not Caracal-enforced.                     |\n| Runtime token injection  | Your runtime, eligibility gated by Caracal | Your runtime process             | Your application                        | An existing client or CLI must receive a brokered token at runtime, and the provider sets `allow_runtime_injection=true`. |\n\nPrefer **Gateway-mediated** unless you have a specific reason to enforce elsewhere. The recipes below note which boundary they use.\n\n## OpenAI API key (Gateway-mediated)\n\nBroker an OpenAI key so the agent never holds it.\n\n| Field                 | Value                                        |\n| --------------------- | -------------------------------------------- |\n| Provider kind         | `api_key`                                    |\n| `header_name`         | `Authorization`                              |\n| `auth_scheme`         | `Bearer`                                     |\n| `api_key`             | Your OpenAI secret key (sealed at creation). |\n| Resource identifier   | `resource://openai`                          |\n| Resource upstream URL | `https://api.openai.com`                     |\n| Resource scopes       | `openai:chat`, `openai:embeddings`           |\n\nThe Gateway forwards `Authorization: Bearer <sealed key>` upstream and strips caller auth. The agent calls the Caracal resource, not OpenAI directly.\n\n## Google Workspace (OAuth authorization code)\n\nDelegated user consent for a Google API, refreshed inside STS.\n\n| Field                             | Value                                                                                                           |\n| --------------------------------- | --------------------------------------------------------------------------------------------------------------- |\n| Provider kind                     | `oauth2_authorization_code`                                                                                     |\n| `authorization_endpoint`          | `https://accounts.google.com/o/oauth2/v2/auth`                                                                  |\n| `token_endpoint`                  | `https://oauth2.googleapis.com/token`                                                                           |\n| `redirect_uri`                    | Your exact Caracal callback, e.g. `https://api.pipernet.example/v1/zones/<zone>/provider-connections/oauth/callback` |\n| `client_id` / `client_secret`     | From the Google Cloud OAuth client.                                                                             |\n| `scopes`                          | e.g. `https://www.googleapis.com/auth/drive.readonly`                                                           |\n| `allowed_token_hosts`             | `oauth2.googleapis.com`                                                                                         |\n| `authorization_params` (Advanced) | `access_type=offline`, `prompt=consent` for refresh tokens.                                                     |\n| Resource upstream URL             | `https://www.googleapis.com`                                                                                    |\n\nCaracal owns `client_id`, `redirect_uri`, `state`, and PKCE. Use the provider `connect` action to mint the consent URL for a user and resource.\n\n:::note[FAQ]\n[When should I use per-user OAuth instead of a shared provider credential?](/reference/faq/#faq-014)\n:::\n\n## Google Cloud service account (jwt_bearer)\n\nMachine access to Google APIs - Vertex AI, Cloud Storage, and every other `googleapis.com` surface - signed with a service-account key instead o"},{"id":"/v0.2/guides/author-policy","title":"Author Policy Data","url":"https://docs.caracal.run/v0.2/guides/author-policy/","concepts":"","body":"Use this guide after resources and applications exist and before activating production access. You author only data consumed by Caracal's signed decision contract; you do not replace decision logic or define `result`.\n\n## Prerequisites\n\n- A zone, application, and resource.\n- Access to the Console or Admin API.\n- The resource identifier and scopes you want to grant.\n- The real application ID and the Session labels the application emits.\n- Representative allow and deny inputs for simulation.\n\n## Start from a grant\n\nThe core document is `grants`: it names the application that owns a resource view and the scopes each role may hold. Pair it with `app_ids`, which binds the application key you use in `grants` to the control-plane id the STS sees as `input.principal.id`.\n\n```text\n# caracal:data-document\npackage caracal.authz\n\nimport rego.v1\n\napp_ids := {\n  \"pipernet\": \"app-pipernet\",\n}\n\ngrants := {\n  \"resource://pipernet\": {\n    \"application\": \"pipernet\",\n    \"roles\": {\"reader\": [\"pipernet:read\", \"pipernet:write\"]},\n  },\n}\n```\n\nThis is the canonical \"application A may call resource B with scopes C\" pattern,\nexpressed as data. The platform contract allows a mint only when the acting\napplication owns the view, the agent's role label grants the scope, and the\nDelegation narrows to it. You declare the grant; the contract enforces the\nnarrowing.\n\n## Gate scopes on human approval\n\nTwo further documents add an optional human gate on top of a grant. `risk` names a\ntier for each sensitive scope, and `approval_tiers` declares which tiers hold the\nmint until a person decides it:\n\n```text\n# caracal:data-document\npackage caracal.authz\n\nimport rego.v1\n\nrisk := [\n  {\"scope\": \"pipernet:refund\", \"tier\": \"high\"},\n]\n\napproval_tiers := [\n  {\"tier\": \"high\", \"approver\": \"operator\", \"ttl_seconds\": 1800, \"privacy\": \"identified\"},\n]\n```\n\nLike `restrict` and `confinement`, an approval declaration can only add a gate,\nnever widen authority, and a malformed declaration fails the gated mint closed.\n[Human Approval](/guides/step-up/) covers the tier fields, both decision planes,\nand the agent-side wait-and-retry flow.\n\n## Start from a template\n\nYou do not have to write each document by hand. Caracal ships a built-in catalog of\ndata-document starters, served at `/v1/policy-templates` and through the Admin SDK:\n\n```ts\nimport { AdminClient } from '@caracalai/admin'\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n})\n\nconst templates = await admin.policyTemplates.list()\nconst starter = await admin.policyTemplates.get('resource-grants')\n```\n\n| Template               | Use it for                                                                     |\n| ---------------------- | ------------------------------------------------------------------------------ |\n| `application-bindings` | Map each application key used in `grants` to its control-plane application id. |\n| `resource-grants`      | Declare the owning application and per-role scope sets for a resource view.    |\n| `label-confinement`    | Cap every session carrying a label prefix to a fixed scope set.                |\n| `zone-restriction`     | A deny overlay that freezes the zone while an entry is present.                |\n\nFor assisted authoring, describe the outcome to the [Caracal Operator](/concepts/operator/#authoring-policy). Its policy author models the use case as grant, binding, and confinement data, validates and previews each document against the platform contract, and proposes a governed create you review and approve - so the policy that lands is already contract-valid.\n\n## How your data maps to the request\n\nThe decision contract evaluates a fixed input contract and resolves it against your\ndata. The acting application is the principal - there is no `input.application` or\n`input.grant` object. See the full [Policy Input Contract](/concepts/policy/#policy-input-contract)\nfor every field.\n\n| Input the contract reads         | Resolved against                                           |\n| -------------------------------- | ---------------------------------------------------------- |\n| `input.principal.id`             | `app_ids` - to find the application key used in `grants`.  |\n| `input.principal.labels`         | `grants[...].roles` and `confinement` label prefixes.      |\n| `input.resource.identifier`      | the top-level key in `grants`.                             |\n| `input.context.requested_scopes` | the role's scope set and any matching `confinement` rule.  |\n| `input.delegation_edge.scopes`   | the narrowing floor every requested scope must sit inside. |\n\n## Validate before versioning\n\nUse the Console policy workflow to paste the document and run validation. For automation, validate through the Admin API or `@caracalai/admin`:\n\n```ts\nimport { AdminClient } from '@caracalai/admin'\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n})\n\nconst validation = await admin.policies.validate(policySource)\nif (!validation.valid) {\n  throw new Error('policy failed validation')\n}\n```\n\nValidation enforces the data-document contract: the package must be `caracal.authz`,\nthe first line must carry the `# caracal:data-document` directive, the document must\ndefine at least one data rule, and it must **not** define `result`. Validation also\nchecks the schema version, balanced syntax, and forbidden built-ins. Because the\nplatform contract owns the decision, a data document can never authorize on its own.\n\n## Why data, not decisions\n\nAuthorization logic - delegation narrowing, role and grant checks, label\nconfinement, bootstrap isolation - is identical for every adopter and is the part\nmost dangerous to get wrong. A single typo in a hand-written rule (`if { false }` →\n`if { true }`) silently turns a deny into an allow-all. Caracal removes that footgun\nby owning the logic in a signed, versioned platform decision contract and letting\nyou supp"},{"id":"/v0.2/guides/activate-policy-set","title":"Activate a Policy Set","url":"https://docs.caracal.run/v0.2/guides/activate-policy-set/","concepts":"","body":"Use this guide when validated policy data must become live. The job is complete only after simulation, activation, propagation, and a real audited exchange; creating a policy version alone changes no authorization result.\n\n## Prerequisites\n\n- A validated policy version and representative allow and deny inputs.\n- A policy set in the target zone and the current active version recorded for rollback.\n- Console access or an Admin client in a trusted automation environment.\n\n## The mental model\n\nFour immutable layers separate authoring from what runs:\n\n| Layer | What it is | Mutable? |\n| --- | --- | --- |\n| Policy | A named Rego document. | The name; content is versioned. |\n| Policy version | One immutable snapshot of policy content. | No. |\n| Policy-set version | An immutable bundle of policy versions. | No. |\n| Active policy-set version | The one bundle the STS evaluates for a zone. | The pointer moves on activation. |\n\nYou edit by creating a **new** version and moving the active pointer to it.\nNothing already evaluated is ever rewritten, so every audit decision ties back to\nthe exact policy-set version and manifest hash that produced it.\n\nA zone is governed by exactly one policy set at a time. Activating a version of\none set deactivates any other set in the zone in the same transaction, so which\npolicies govern a zone is never ambiguous. Saving or activating a version also\ncompiles the full policy bundle on the STS runtime engine, so a manifest whose\npolicies conflict (for example, two documents defining the same data key) is\nrejected before it can be pinned or promoted.\n\n## Activation flow\n\n## Web Console Workflow\n\n1. Open the packaged web console at `http://localhost:3001` after `caracal up`.\n2. Select **Policies** and create or update the Rego policy.\n3. Select **Policy Sets** and create a set for the zone.\n4. Add the policy version to a new policy-set version.\n5. Simulate the version with a representative input.\n6. Activate the version when the simulated decision matches the intended behavior.\n7. Use **Audit** to follow the decision trace after the first real request.\n\n## Automation workflow\n\n```ts\nimport { AdminClient } from \"@caracalai/admin\";\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n});\n\nconst policy = await admin.policies.create(process.env.CARACAL_ZONE_ID!, {\n  name: \"pipernet-read\",\n  content: policySource,\n});\n\nconst set = await admin.policySets.create(process.env.CARACAL_ZONE_ID!, \"pipernet\");\nconst version = await admin.policySets.addVersion(process.env.CARACAL_ZONE_ID!, set.id, [\n  { policy_version_id: policy.version.id },\n]);\n\nconst simulation = await admin.policySets.simulate(process.env.CARACAL_ZONE_ID!, set.id, version.id, sampleInput);\nif (!simulation.would_activate) {\n  throw new Error(simulation.explanation.reason);\n}\n\nawait admin.policySets.activate(process.env.CARACAL_ZONE_ID!, set.id, version.id);\n```\n\n## Wait for propagation\n\nActivation returns `202 Accepted`: the active pointer moves durably, then the\nSTS runtime reloads the bundle through a durable invalidation stream. Poll the\nactivation status until the runtime reports the version as loaded before\ntreating the rollout as live:\n\n```ts\nlet status = await admin.policySets.activationStatus(zoneId, set.id, version.id);\nwhile (status.propagation_status !== \"loaded\") {\n  if (status.propagation_status === \"failed\") {\n    throw new Error(status.outbox.last_error ?? \"policy rollout failed\");\n  }\n  await new Promise((resolve) => setTimeout(resolve, 2000));\n  status = await admin.policySets.activationStatus(zoneId, set.id, version.id);\n}\n```\n\n`propagation_status` moves through `waiting_for_outbox` → `waiting_for_sts` →\n`loaded`. With several STS replicas, the replica that receives the invalidation\nreloads within about a second; the rest converge through a periodic database\npoll (60 seconds by default), so a brief mixed-version window across replicas\nis expected during rollout.\n\n## Validation checklist\n\n| Check | Expected result |\n| --- | --- |\n| Policy validation | `valid: true` and no blocking warnings. |\n| Simulation | Representative input returns the intended decision. |\n| Activation | Zone has the expected active policy-set version. |\n| Propagation | `activationStatus` reports `propagation_status: \"loaded\"`. |\n| First exchange | Audit shows the new policy as a determining policy. |\n\nIf activation changes expected access, keep the old policy-set version ID in the rollout notes so you can promote it again if needed.\n\n## Iterate from real denials\n\nEvery denied decision links to the policy-set version that produced it, and the\naudit explain endpoint reconstructs the policy input for that denial. Stage the\nfix as a new policy-set version, then simulate the denied input against it:\n\n```ts\nconst trace = await admin.audit.explain(zoneId, requestId);\nconst input = trace.denied[0]?.policy_input;\nconst candidate = await admin.policySets.addVersion(zoneId, set.id, manifest);\nconst check = await admin.policySets.simulate(zoneId, set.id, candidate.id, input);\nif (check.result?.decision === \"allow\") {\n  await admin.policySets.activate(zoneId, set.id, candidate.id);\n}\n```\n\nThe [Iterate Policy Safely example](/examples/policy-iterate/) wraps this loop as a runnable script.\n\n## Likely failure points\n\n| Failure | Response |\n| --- | --- |\n| Simulation warns or denies the intended input | Correct the data document and create another immutable version. |\n| Activation is accepted but status never reaches `loaded` | Inspect the outbox identifier and STS readiness; do not assume propagation. |\n| Real decisions still name the previous manifest | Treat the replica as unconverged and keep rollout monitoring active. |\n| Rollback is needed | Activate the recorded prior version; never mutate the failed version. |\n\n## Next step\n\nRun [Iterate Policy Safely](/examples/policy-iterate/) for the first real denial, or [Debug Authorization Decisions](/guides/authorize-access/) wh"},{"id":"/v0.2/guides/authorize-access","title":"Debug Authorization Decisions","url":"https://docs.caracal.run/v0.2/guides/authorize-access/","concepts":"","body":"Use this guide when a request is denied, unexpectedly allowed, missing audit evidence, or routed to the wrong protected resource. For production incidents and dependency failures, use [Troubleshoot by Symptom](/operations/troubleshooting/).\n\n## Prerequisites\n\n- The request ID, zone, application, resource, scopes, and approximate timestamp.\n- Access to Audit and active policy-set status.\n- A safe way to reproduce without changing production data.\n\n## Debug Flow\n\n## Start with the Request ID\n\nFind the request ID from the SDK error, STS response, Gateway response, web console audit event, or application log. Open web console **Audit**, filter by the request ID, and open its decision trace.\n\nThe trace should show:\n\n- application and execution session;\n- requested resource and scopes;\n- policy set version;\n- determining policies;\n- diagnostics;\n- final decision;\n- Gateway result when the request reached Gateway.\n\nIf you do not have a request ID, reproduce the request with your runtime configuration and capture the SDK, STS, or Gateway output.\n\n## Common Authorization Failures\n\n| Symptom                           | Check                                                                                     | Fix                                                                                        |\n| --------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |\n| Exchange is denied                | Active policy does not allow the application, subject, resource, or scopes.               | Update the Rego policy, simulate the denied input, and activate a new policy-set version.  |\n| Scope is missing                  | Resource does not define the scope, or policy allowlist omits it.                         | Add the scope to the resource and policy deliberately.                                     |\n| Wrong resource is evaluated       | App used the wrong resource ID or Gateway header.                                         | Use the resource ID from Console and `X-Caracal-Resource`.                                 |\n| Policy change has no effect       | New policy version is not in the active policy-set version.                               | Create and activate a new policy-set version.                                              |\n| Access continues after revocation | Resource server is not consuming revocation state, or Gateway route is not used.          | Confirm Gateway-mediated routing or shared revocation consumers for adapters.              |\n| Gateway returns 403               | Mandate is expired, missing, revoked, or scoped for another resource.                     | Rerun the workload for a fresh mandate and confirm resource bindings.                      |\n| Upstream succeeds without audit   | Request bypassed Gateway or adapter result audit.                                         | Route through Gateway or emit service-side action-result audit after adapter verification. |\n| No audit event appears            | Request never reached STS/Gateway, wrong zone is selected, or audit ingestion is delayed. | Confirm profile, selected zone, and request ID; refresh audit after a short wait.          |\n\n## Iterate from a Denial\n\nEvery denied decision links to the policy-set version that produced it. The audit explain endpoint reconstructs a redaction-safe policy input for denied decisions:\n\n```ts\nconst trace = await admin.audit.explain(zoneId, requestId)\nconst input = trace.denied[0]?.policy_input\n```\n\nFeed that input into [simulation](/guides/activate-policy-set/) against a candidate policy-set version before activating the fix. Actor and subject claims are never written to audit, so add any claim-dependent fields before simulating.\n\n## Authorization Design Rules\n\n- Allow only the scopes an application and subject need.\n- Keep policies resource-specific instead of allowing broad cross-resource access.\n- Express context-sensitive conditions in policy rather than widening scopes.\n- Revoke active sessions when a subject leaves a workflow or an application no longer needs a resource.\n- Use diagnostics for expected denial paths so request traces explain what to change.\n\n## Related Pages\n\n- [Define Resources and Providers](/guides/resources-providers/)\n- [Author Policy Data](/guides/author-policy/)\n- [Activate a Policy Set](/guides/activate-policy-set/)\n- [Resources and Grants](/concepts/resource-grant/)\n- [Policies and Policy Sets](/concepts/policy/)\n- [Sessions and Revocation](/concepts/sessions-revocation/)\n\n## Validation and expected result\n\nReproduce once after correction. The trace must name the intended resource, Session/Delegation, active manifest, determining data, final decision, and Gateway result. A deny fix is complete only when nearby negative cases still deny.\n\n:::caution[Failure point: changing several layers]\nDo not edit resource scopes, policy data, application labels, and Gateway routing simultaneously. Trace from request ID inward and change the first incorrect boundary; otherwise the new allow cannot be explained.\n:::\n\n## Next step\n\nUse [Iterate Policy Safely](/examples/policy-iterate/) for policy-data correction or [Check Provider Readiness](/examples/provider-preflight/) for route/provider failures."},{"id":"/v0.2/guides/sdk-typescript","title":"Integrate the TypeScript SDK","url":"https://docs.caracal.run/v0.2/guides/sdk-typescript/","concepts":"","body":"Use `@caracalai/sdk` in a Node application that must create governed Sessions, narrow authority, call Gateway-routed resources, or propagate verified Caracal context. Resource servers that only verify inbound mandates should use [the Express adapter](/guides/protect-express/) or [verify package](/sdks/verify/) instead.\n\n## Prerequisites\n\n- A managed application credential, active policy, resource, provider binding, and Gateway route.\n- `CARACAL_CONFIG` or a complete `CARACAL_*` environment configuration.\n- A destination timeout and idempotency plan for mutating calls.\n\n## Install\n\n```bash\nnpm install @caracalai/sdk\n```\n\n## Configure\n\n`new Caracal()` loads exactly the profile named by `CARACAL_CONFIG` when set; otherwise it loads `CARACAL_*` environment variables. It does not search default profile or credential paths, and conflicting credential modes fail at startup. Use `Caracal.fromClientSecret(...)` for complete static credentials supplied in code. Power-user loaders and resolver-backed credentials live in `@caracalai/sdk/advanced`.\n\n## Connect and call a protected resource\n\n```ts\nimport { Authority, Caracal } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\nconst resourceId = 'resource://pipernet'\n\nawait caracal.session(async () => {\n  await caracal.session(\n    async () => {\n      await caracal.fetch(resourceId, '/reports', { scopes: ['pipernet:read'] })\n    },\n    {\n      authority: Authority.narrow(['pipernet:read'], {\n        resourceId,\n        ttlSeconds: 600,\n      }),\n    },\n  )\n})\n```\n\nThe outer `session()` creates the lifecycle parent. The narrowed child creates the positive-TTL Delegation required for resource authority, and `fetch()` mints a one-shot Gateway mandate for the protected call. Both Sessions terminate when their callbacks exit.\n\nTo make agents distinguishable in policy and audit, pass `labels`. These become `input.principal.labels`, so several agents under one application stay separable without one application per agent. `labels` are descriptive, for policy and audit, not grants; authority always comes from scopes and Delegation. The Session lifecycle is handled for you: `session()` records a `task` Session, while `startSession()` records a heartbeat-leased `service` Session.\n\n```ts\nawait caracal.session(\n  async (ctx) => {\n    console.log('refund Session', ctx.sessionId)\n  },\n  { labels: ['refund-agent'] },\n)\n```\n\nSee [If many agents share one managed application, can policy and audit still tell them apart?](/reference/faq/#faq-008).\n\n## Long-lived Sessions\n\nDaemons and workers that outlive a single request use `startSession()` instead of `session()`. It returns a handle you own: the SDK renews the lease from an independent background timer by default, and you retire the Session with `close()`. Each renewal extends the lease. If heartbeats stop before the lease expires, Coordinator suspends the Session for explicit recovery or termination. The stored protocol lifecycle is `service`; unlike `task`, it is not subject to the wall-clock TTL sweeper.\n\n```ts\nconst svc = await caracal.startSession({ labels: ['fiona-worker'] })\ntry {\n  while (running) {\n    await caracal.bind(svc.context, async () => {\n      await doWork(await caracal.headersAsync())\n    }) // lease renews in the background\n  }\n} finally {\n  await svc.close()\n}\n```\n\nThe renewal cadence is controlled by `heartbeatIntervalMs`:\n\n| `heartbeatIntervalMs` | Behavior                                                                                                                                        |\n| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |\n| unset                 | Cadence derives from the server lease: renew at roughly a third of the remaining lease, with jitter. The right default for almost every worker. |\n| positive              | Fixed cadence. Use when you need deterministic renewal timing, for example under test clocks.                                                   |\n| `0` or negative       | No background timer; the lease is renewed only by your own `heartbeat()` calls, and a missed renewal lets the lease lapse.                      |\n\nThe handle exposes `deadlineAt` and `leaseGeneration`. The generation is a monotonic ownership token: every heartbeat and `close()` sends it, so a process holding an earlier generation cannot renew or terminate the Session after another process takes ownership. Because renewal runs on an independent timer, the lease stays current even while your code is blocked on a long `await` (a streaming response, a slow tool).\n\nTransient renewal errors are logged and retried on the next tick rather than crashing the worker. If Coordinator reports the Session permanently gone or the generation has been fenced by a new holder, the timer stops and `onLeaseLost` fires once so the worker can resign instead of spinning. An expired lease instead suspends the Session and reports `suspended` through `onStateChange`.\n\nThe handle's `status` follows Coordinator heartbeat responses. If it becomes `suspended`, automatic heartbeat stops and `onStateChange` reports the transition. Stop taking work, resolve the cause, resume the Session through the control plane, and call `attachSession()` to restart lease renewal. `onLeaseLost` remains reserved for terminal loss.\n\n```ts\nconst svc = await caracal.startSession({\n  labels: ['voice-worker'],\n  onLeaseLost: () => shutdown(),\n})\n```\n\nA renewal cannot run while the event loop is blocked synchronously; in that case the lease correctly lapses, which is the liveness signal working as intended.\n\nA worker that restarts does not need a fresh session: persist `svc.sessionId` and re-attach with `attachSession()`. Attach atomically acquires a new lease generation and renews the deadline, fencing every older handle. A Session the Coordinator no longer holds live fails with `CoordinatorError`; a suspended Session must be resu"},{"id":"/v0.2/guides/sdk-python","title":"Integrate the Python SDK","url":"https://docs.caracal.run/v0.2/guides/sdk-python/","concepts":"","body":"Use `caracalai-sdk` in an async Python application that must create governed Sessions, narrow authority, route HTTP through Gateway, or bind verified inbound context. Resource servers that only verify inbound mandates should use [the ASGI adapter](/guides/protect-fastapi/) or [verify package](/sdks/verify/).\n\n## Prerequisites\n\n- A managed application credential, active policy, resource/provider binding, and Gateway route.\n- An async runtime; move CPU-bound work off the event loop so service heartbeats can run.\n- A timeout and destination idempotency plan for mutating requests.\n\n## Install\n\n```bash\npip install caracalai-sdk\n```\n\n## Connect\n\n```python\nfrom caracalai import Caracal\n\ncaracal = Caracal()\n```\n\n`Caracal()` loads exactly the profile named by `CARACAL_CONFIG` when set; otherwise it loads `CARACAL_*` environment variables. It does not search default profile or credential paths, and conflicting credential modes fail at startup. Use `Caracal.from_client_secret(...)` for complete static credentials supplied in code. Power-user loaders and resolver-backed credentials live in `caracalai.advanced`.\n\n## Start a Session and call a protected resource\n\n```python\nfrom caracalai import Authority, Caracal\n\ncaracal = Caracal()\nresource_id = \"resource://pipernet\"\n\nasync with caracal.session() as parent:\n    async with caracal.session(\n        parent_ctx=parent,\n        authority=Authority.narrow(\n            [\"pipernet:read\"],\n            resource_id=resource_id,\n            ttl_seconds=600,\n        ),\n    ):\n        await caracal.fetch(resource_id, \"/reports\", scopes=[\"pipernet:read\"])\n```\n\nThe outer Session is the lifecycle parent. The narrowed child creates the positive-TTL Delegation required for resource authority, and `fetch()` mints a one-shot Gateway mandate for the protected call. Both async context managers retire their Sessions on exit.\n\nTo make AI-agent executions distinguishable in policy and audit, pass `labels` when starting a Session. These become `input.principal.labels`, so several agents under one application stay separable without one application per agent. Labels describe work; scopes and Delegations bound authority. `session()` retires the task Session when the block exits, while `start_session()` starts a heartbeat-leased long-lived Session whose protocol lifecycle is `service`.\n\n```python\nasync with caracal.session(labels=[\"refund-agent\"]) as ctx:\n    print(\"refund Session\", ctx.session_id)\n```\n\nSee [If many agents share one managed application, can policy and audit still tell them apart?](/reference/faq/#faq-008).\n\n## Long-lived Sessions\n\nDaemons and workers that outlive a single request use `start_session()` instead of `session()`. It returns a handle you own: the SDK renews the lease from an independent background task by default, and you retire the Session with `aclose()`. If heartbeats stop before the lease expires, Coordinator suspends the Session for explicit recovery or termination. The stored protocol lifecycle is `service`; unlike `task`, it is not subject to the wall-clock TTL sweeper.\n\n```python\nsvc = await caracal.start_session(labels=[\"fiona-worker\"])\ntry:\n    while running:\n        async with caracal.bind(svc.context):\n            await do_work(caracal.headers())\nfinally:\n    await svc.aclose()\n```\n\nNarrowing requires an active parent. For a long-lived hierarchy, bind a parent service handle while starting its narrowed service child. The child context carries the edge, and closing each handle retires its own Session.\n\n```python\nparent = await caracal.start_session(labels=[\"pipernet-orchestrator\"])\nasync with caracal.bind(parent.context):\n    svc = await caracal.start_session(\n        labels=[\"fiona-worker\"],\n        authority=Authority.narrow(\n            [\"pipernet:read\"],\n            ttl_seconds=600,\n            resource_id=\"resource://pipernet\",\n        ),\n    )\n```\n\nThe renewal cadence follows the server lease, renewing at roughly a third of the remaining lease with jitter. Pass a positive `heartbeat_interval` to fix the cadence, or `0` to disable the background task and call `heartbeat()` yourself. Because renewal runs on an independent task, the lease stays current even while your code is blocked on a long `await` (a streaming response, a slow tool).\n\nThe handle exposes `heartbeat_deadline_at` and `lease_generation`. Every heartbeat and `aclose()` sends that monotonic ownership token, so a process holding an earlier generation cannot renew or terminate the Session after another process takes ownership.\n\nTransient renewal errors are logged and retried on the next tick rather than crashing the worker. If Coordinator reports the Session permanently gone or the generation has been fenced by a new holder, the task stops and `on_lease_lost` fires once so the worker can resign instead of spinning. An expired lease instead suspends the Session and reports `suspended` through `on_state_change`.\n\nThe handle's `status` follows Coordinator heartbeat responses. If it becomes `suspended`, automatic heartbeat stops and `on_state_change` reports the transition. Stop taking work, resolve the cause, resume the Session through the control plane, and call `attach_session()` to restart lease renewal. `on_lease_lost` remains reserved for terminal loss.\n\n```python\nsvc = await caracal.start_session(\n    labels=[\"voice-worker\"],\n    on_lease_lost=lambda exc: shutdown(),\n)\n```\n\nA renewal cannot run while the event loop is blocked synchronously (CPU-bound work with no `await`); in that case the lease correctly lapses, which is the liveness signal working as intended.\n\nA worker that restarts does not need a fresh session: persist `svc.session_id` and re-attach with `attach_session()`. Attach atomically acquires a new lease generation and renews the deadline, fencing every older handle. A Session the Coordinator no longer holds live fails with `CoordinatorError`; a suspended Session must be resumed through the control plane before attachment. The returned handle behaves like one "},{"id":"/v0.2/guides/sdk-go","title":"Integrate the Go SDK","url":"https://docs.caracal.run/v0.2/guides/sdk-go/","concepts":"","body":"Use the Go SDK in an application that must carry governed authority in `context.Context`, create Sessions, narrow Delegation, or route HTTP through Gateway. A resource server that only verifies inbound mandates should use [the net/http adapter](/guides/protect-nethttp/).\n\n## Prerequisites\n\n- A managed application credential, active policy, resource/provider binding, and Gateway route.\n- Request deadlines propagated through `context.Context`.\n- A destination idempotency strategy for mutating requests.\n\n## Install\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/sdk/go\n```\n\n## Connect\n\n```go\npackage main\n\nimport (\n\t\"context\"\n\t\"log\"\n\n\tcaracal \"github.com/garudex-labs/caracal/packages/sdk/go\"\n)\n\nfunc main() {\n\tclient, err := caracal.New()\n\tif err != nil {\n\t\tlog.Fatal(err)\n\t}\n\n\tif err := client.Session(context.Background(), func(ctx context.Context) error {\n\t\theaders, err := client.Headers(ctx)\n\t\tif err != nil {\n\t\t\treturn err\n\t\t}\n\t\t_ = headers\n\t\treturn nil\n\t}); err != nil {\n\t\tlog.Fatal(err)\n\t}\n}\n```\n\n`New()` loads exactly the profile named by `CARACAL_CONFIG` when set; otherwise it loads `CARACAL_*` environment variables. It does not search default profile or credential paths.\n\n## Use an HTTP client\n\n```go\nhttpClient := client.Transport(nil, caracal.CallOptions{\n\tScopes:      []string{\"pipernet:read\"},\n\tPropagation: caracal.PropagationGatewayOnly,\n})\n\nerr := client.Session(context.Background(), func(ctx context.Context) error {\n\treq, err := http.NewRequestWithContext(ctx, http.MethodGet, \"https://api.pipernet.example/reports\", nil)\n\tif err != nil {\n\t\treturn err\n\t}\n\t_, err = httpClient.Do(req)\n\treturn err\n})\n```\n\nThe transport mints the scoped Gateway-ingress mandate and injects Caracal envelope headers from `context.Context`. For explicit routing metadata, use `GatewayRequest()` and send the resulting request through the scoped transport; the helper alone does not authenticate it.\n\n## Long-lived Sessions\n\nDaemons and workers that outlive a single request use `StartSession` instead of `Session`. It returns a handle you own: the SDK renews the lease from a background goroutine by default, and you retire the Session with `Close`. If heartbeats stop before the lease expires, Coordinator suspends the Session for explicit recovery or termination; its stored protocol lifecycle is `service`.\n\n```go\nsvc, err := client.StartSession(context.Background(), caracal.StartSessionOptions{\n\tLabels: []string{\"fiona-worker\"},\n})\nif err != nil {\n\treturn err\n}\ndefer svc.Close(context.Background())\n\nfor running {\n\tdoWork(svc.Context) // lease renews in the background\n}\n```\n\nThe renewal cadence follows the server lease, renewing at roughly a third of the remaining lease with jitter. Set `StartSessionOptions.HeartbeatInterval` to a positive duration to fix the cadence, or a negative one to disable the background goroutine and call `Heartbeat` yourself. `LeaseGeneration()` exposes the monotonic ownership token sent by every heartbeat and `Close`, so an earlier holder cannot renew or terminate the Session after another process takes ownership. Transient renewal errors are logged and retried on the next tick; if the Coordinator reports the Session permanently gone or fenced, the goroutine stops and `StartSessionOptions.OnLeaseLost` fires once so the worker can resign instead of spinning.\n\n`Status()` follows Coordinator heartbeat responses. If it becomes `suspended`, automatic heartbeat stops and `OnStateChange` reports the transition. Stop taking work, resolve the cause, resume the Session through the control plane, and call `AttachSession` to restart lease renewal. `OnLeaseLost` remains reserved for terminal loss.\n\nA worker that restarts does not need a fresh session: persist `svc.SessionID()` and re-attach with `AttachSession`. Attach atomically acquires a new lease generation and renews the deadline, fencing every older handle. A Session the Coordinator no longer holds live fails with `*CoordinatorError`; a suspended Session must be resumed through the control plane before attachment. The returned handle behaves like one from `StartSession`:\n\n```go\nsvc, err := client.AttachSession(ctx, persistedSessionID, caracal.AttachSessionOptions{\n\tOnLeaseLost: func(error) { shutdown() },\n})\n```\n\nThe rebuilt context carries the Session identity only; Delegations bound by the previous holder are re-presented with `AcceptDelegation`.\n\n## Start a narrowed child\n\n```go\nerr := client.Session(context.Background(), func(ctx context.Context) error {\n\treturn client.Session(ctx, func(child context.Context) error {\n\t\t_, _ = client.Current(child)\n\t\treturn nil\n\t}, caracal.SessionOptions{\n\t\tAuthority: caracal.AuthorityNarrow([]string{\"pipernet:read\"}, caracal.NarrowOptions{\n\t\t\tConstraints: &caracal.DelegationConstraints{MaxHops: 1},\n\t\t\tTTLSeconds:  600,\n\t\t}),\n\t})\n})\n```\n\nA plain `client.Session` runs the child under its parent's effective authority - the application's authority for a root parent, or the parent's narrowed slice when the parent was itself narrowed (transitive least-privilege). Set `SessionOptions.Authority` to `caracal.AuthorityNarrow([]string{...})` only when the child should hold a least-privilege subset, or `caracal.AuthorityNone()` for a child with no inherited authority. Use `client.Delegate` to grant authority to a Session that already exists, typically in another application: it returns the edge, and the receiving session presents it with `client.AcceptDelegation(ctx, edgeID)`. Use `Current(ctx)` to inspect the bound Caracal context and `Headers(ctx)` to project it to outbound HTTP headers.\n\n## Troubleshooting\n\n| Symptom                         | Check                                                                                        |\n| ------------------------------- | -------------------------------------------------------------------------------------------- |\n| Missing config error            | Confirm the runtime profile or required environment variables.                               |\n| `Headers` witho"},{"id":"/v0.2/guides/runtime-run","title":"Run an Agent with caracal run","url":"https://docs.caracal.run/v0.2/guides/runtime-run/","concepts":"","body":"`caracal run` starts a local subprocess with scoped, short-lived credentials injected as environment variables. The workload carries only its workload ID and secret; the credential bindings live in the web console. Use it for development, demos, and controlled local runs.\n\nDo not use it for a daemon that must renew credentials, for request-level Gateway enforcement, or as a process supervisor. Use an SDK for long-lived work.\n\n## Prerequisites\n\n- A ready runtime, Launcher workload, owner-only workload secret, and at least one launch binding.\n- A provider with `allow_runtime_injection=true` and an active policy permitting binding scopes.\n- A child process that reads the configured environment variable and can finish before credential expiry.\n\n## Prepare the workload\n\n1. Run `caracal up`.\n2. Sign in to the web console and use **Guided setup** to create the zone, provider, resource, and policy.\n3. On the console's **Launcher** page, create a workload. Store its secret in the owner-only file at `<Caracal config dir>/runtime/<workload_id>/secret`, or export `CARACAL_WORKLOAD_SECRET`; the secret stays retrievable from the Launcher page, with every reveal audited.\n4. On the same page, bind an environment variable to each resource the workload needs and select the scopes each credential should carry. The page then shows the exact launch commands. See [Configure Workloads](/runtime-console/config-file/).\n\n## Run a command\n\n```bash\nexport CARACAL_WORKLOAD_ID=<workload_id>\ncaracal run -- npm start\n```\n\nThe launcher fetches the workload's launch bindings from STS, mints a short-lived credential for each binding under only its selected scopes, and injects the results into the configured environment variables. The child environment is otherwise scrubbed: `CARACAL_*` configuration variables stay with the launcher, and only a small allowlist such as `PATH`, `HOME`, locale, and `XDG_*` directories is inherited. Credentials are minted once at launch and never renewed; long-running workloads should use an SDK. If Policy requires Approval for a binding, the launch pauses and emits an `approval_required` line on stderr until the hold is decided. See [Run Workloads](/runtime-console/runtime/) for the full contract.\n\n## Validate the run\n\n| Check | Command or surface |\n| --- | --- |\n| Runtime is ready | `caracal status --ready` |\n| Credential is injected | `caracal run -- printenv OPENAI_API_KEY` (use your configured `env` name) |\n| First request succeeds | Send a request to a configured resource path. |\n| Audit captured the request | web console **Audit**. |\n\nExpected result: only configured binding variables and optional `<ENV>_EXPIRES_AT` values enter the child; workload identity and other `CARACAL_*` variables do not. The launcher exits with the child process's exit code.\n\n## Troubleshooting\n\n| Symptom | Fix |\n| --- | --- |\n| `workload identity not found` | Set `CARACAL_WORKLOAD_ID` and a workload secret source. |\n| `invalid workload credentials` | The workload ID or secret is wrong or was rotated; copy the current values from the **Launcher** page. |\n| `no credential bindings configured` | Define launch bindings for this workload on the console's **Launcher** page. |\n| Secret file rejected | Restrict file permissions and avoid setting both inline and file secrets. |\n| Launch pauses on `approval_required` | Decide the Approval in the web console, or adjust the Policy tier. |\n| Request denied | Check policy-set activation, scopes, and audit diagnostics. |\n\n:::caution[Failure point: scope versus provider credential]\nBinding scopes govern whether STS releases a credential. A static upstream API key may still carry broader provider-native power. Prefer Gateway brokering when the upstream cannot issue a truly scoped credential.\n:::\n\n## Next step\n\nRun [Launch Research Agent](/examples/research-agent/) for the fixed example, or migrate a long-lived process to the matching [SDK guide](/guides/)."},{"id":"/v0.2/guides/protect-gateway-http","title":"Protect a Gateway-Routed HTTP API","url":"https://docs.caracal.run/v0.2/guides/protect-gateway-http/","concepts":"","body":"Use Gateway-routed protection when the upstream is HTTP-routable and you want Caracal to enforce every request before it reaches the target. The agent receives a short-lived Caracal mandate; Gateway verifies it, resolves the resource route, attaches the configured upstream credential when needed, forwards the request, and writes action-result audit.\n\n## Prerequisites\n\n- Gateway, STS, Redis revocation state, audit ingestion, and the upstream report ready.\n- A resource with stable identifier, Gateway application, provider, scopes, and declared operations.\n- An active policy granting only the calling role and an application SDK configured for the same resource.\n\n## When to Use Gateway\n\n| Use Gateway when                                                               | Use another guide when                                                      |\n| ------------------------------------------------------------------------------ | --------------------------------------------------------------------------- |\n| The upstream is HTTP, REST, MCP-over-HTTP, or another Gateway-routable target. | The service must verify mandates inside its own process.                    |\n| You want provider credentials to stay inside the trusted Gateway/STS boundary. | The application must call the provider directly and only needs attribution. |\n| You want action-result audit from the central Gateway.                         | The resource server owns result audit after adapter verification.           |\n| Responses are request/response or streamed HTTP, such as SSE.                  | The upstream requires WebSocket connections, which Gateway does not proxy.  |\n\nFor in-process enforcement, use [Protect an Express App](/guides/protect-express/), [Protect a FastMCP App](/guides/protect-fastmcp/), [Protect a Go net/http Service](/guides/protect-nethttp/), or [Protect an MCP Server](/guides/protect-mcp/).\n\n## Create or Select the Resource\n\nOpen the packaged web console at `http://localhost:3001` after `caracal up`.\n\nCreate or select a resource with:\n\n| Field                        | Guidance                                                                                                            |\n| ---------------------------- | ------------------------------------------------------------------------------------------------------------------- |\n| Resource identifier          | Stable audience URI, such as `resource://pipernet`.                                                                 |\n| Scopes                       | Action-oriented scopes, such as `pipernet:read` and `pipernet:submit`.                                              |\n| Upstream URL                 | Network URL the Gateway can reach.                                                                                  |\n| Gateway application          | The Caracal application identity Gateway uses for upstream exchanges.                                               |\n| Upstream credential provider | The provider record Gateway uses to attach no credential, a Caracal mandate, OAuth token, API key, or bearer token. |\n\nKeep the resource identifier stable even when upstream hosts or provider bindings change.\n\n## Choose Provider Auth Mode\n\n| Provider type            | Use when                                                                |\n| ------------------------ | ----------------------------------------------------------------------- |\n| None                     | Gateway enforces Caracal access and the upstream expects no credential. |\n| Caracal mandate          | The upstream verifies Caracal mandates directly.                        |\n| OAuth client credentials | Gateway needs a machine OAuth token.                                    |\n| OAuth authorization code | Gateway needs a connected upstream account (delegated OAuth consent).   |\n| API key                  | Gateway attaches a sealed static API key in a configured header.        |\n| Bearer token             | Gateway attaches a sealed pre-issued bearer token.                      |\n\nUse [Define Resources and Providers](/guides/resources-providers/) for field definitions and [Provider Recipes](/guides/provider-recipes/) for concrete OpenAI, Google, GitHub, Slack, and internal API examples.\n\n## Route Calls Through Gateway\n\nGateway-routed requests include:\n\n| Request part         | Value                                                                               |\n| -------------------- | ----------------------------------------------------------------------------------- |\n| URL                  | Gateway URL from your runtime profile (`http://localhost:8081` on the local stack). |\n| `Authorization`      | `Bearer <Caracal mandate>`.                                                         |\n| `X-Caracal-Resource` | Resource identifier.                                                                |\n| Path                 | The protected path you configured for the upstream.                                 |\n\nSDKs can build the Gateway request and inject headers for you:\n\n- TypeScript: `caracal.gatewayRequest()` plus `caracal.transport({ scopes })`\n- Python: `caracal.gateway_request()` plus `caracal.transport(scopes=[...])`\n- Go: `GatewayRequest()` plus `Transport(nil, CallOptions{Scopes: ...})`\n\nThe request helper supplies only the Gateway URL and `X-Caracal-Resource`; the scoped transport mints and attaches the required `use=gateway` mandate. Do not combine the helper with raw lifecycle headers.\n\n## Verify Authorization and Audit\n\nRun these checks before treating the route as ready:\n\n| Check                 | Expected result                                               |\n| --------------------- | ------------------------------------------------------------- |\n| Allowed request       | Gateway forwards to upstream and records action-result audit. |\n| Missing mandate       | Gateway rejects the request.                                  |\n| Missing scope         | STS or Gateway rejects the "},{"id":"/v0.2/guides/protect-express","title":"Protect an Express App","url":"https://docs.caracal.run/v0.2/guides/protect-express/","concepts":"","body":"Use `@caracalai/express` when an Express app should verify Caracal mandates before route handlers run.\n\n## Prerequisites\n\n- A Caracal-mandate resource whose audience matches this service.\n- The zone STS issuer, zone ID, and a shared production revocation store.\n- A route-by-route scope and target map.\n\n## Install\n\n```bash\nnpm install express @caracalai/express @caracalai/verify @caracalai/revocation\n```\n\nUse a Redis-backed revocation store in production. The in-memory store is only suitable for local development and tests.\n\n## Add middleware\n\n```ts\nimport express from 'express'\nimport { caracalAuth, type CaracalRequest } from '@caracalai/express'\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst app = express()\n\nconst verifier = createMandateVerifier({\n  issuer: process.env.CARACAL_ISSUER!,\n  audience: 'resource://pipernet',\n  zoneId: process.env.CARACAL_ZONE_ID!,\n  revocations: new InMemoryRevocationStore(),\n})\n\nawait verifier.warmup()\n\napp.use(\n  '/mcp',\n  caracalAuth(\n    { verifier },\n    {\n      requiredScopes: ['mcp:tool:call'],\n      requiredTargets: ['resource://pipernet'],\n    },\n  ),\n)\n\napp.post('/mcp/tools/list', (req: CaracalRequest, res) => {\n  res.json({\n    principal: req.caracalClaims?.sub,\n    tools: ['search', 'summarize'],\n  })\n})\n```\n\nThe middleware attaches verified claims to `req.caracal` and `req.caracalClaims`, plus the propagation context at `req.caracalContext`.\n\n## Enforce the right boundary\n\n| Option              | Use it for                        |\n| ------------------- | --------------------------------- |\n| `requiredScopes`    | Tool or route-level scope checks. |\n| `requiredTargets`   | Resource-target checks.           |\n| `requireSession`    | Require a governed Session.       |\n| `requireDelegation` | Require delegated authority.      |\n| `maxHopCount`       | Limit delegation depth.           |\n\n## Validate\n\n1. Exchange for a mandate that targets the resource.\n2. Call the protected route with `Authorization: Bearer <mandate>`.\n3. Remove a required scope and confirm the route returns `403`.\n4. Revoke the session and confirm the route rejects the old mandate.\n\nExpected result: missing or invalid credentials return `401`, valid but insufficient authority returns `403`, and protected handlers run only after `req.caracalClaims` and `req.caracalContext` are populated.\n\n:::caution[Failure point: revocation]\n`InMemoryRevocationStore` proves local behavior only. Every production replica must share the Redis-backed store and consume revocation events; otherwise one replica can continue accepting a revoked Session.\n:::\n\nFor middleware option signatures, use [Express Adapter reference](/sdks/adapters/express/).\n\n## Next step\n\nAdd route-specific requirements, then test the boundary with [Test Caracal Integrations](/guides/testing/)."},{"id":"/v0.2/guides/protect-fastapi","title":"Protect a FastAPI App","url":"https://docs.caracal.run/v0.2/guides/protect-fastapi/","concepts":"","body":"Use `caracalai-asgi` when a FastAPI, Starlette, or other ASGI app should verify Caracal mandates before request handlers run. This is the provider-side boundary: a partner serving Caracal-governed customers verifies each inbound mandate against the zone's keys before doing any work.\n\n## Prerequisites\n\n- A Caracal-mandate resource whose audience is `resource://pipernet` or another stable resource ID.\n- STS issuer and zone configuration plus a shared production revocation store.\n- Public health paths identified before middleware is enabled.\n\n## Install\n\n```bash\npip install caracalai-asgi caracalai-revocation\n```\n\nUse a Redis-backed revocation store in production. The in-memory store is only suitable for local development and tests.\n\n## Add middleware\n\n```python\nfrom caracalai_asgi import CaracalASGIAuth\nfrom caracalai_revocation import InMemoryRevocationStore\nfrom fastapi import FastAPI, Request\n\napp = FastAPI()\napp.add_middleware(\n    CaracalASGIAuth,\n    audience=\"resource://pipernet\",\n    revocations=InMemoryRevocationStore(),\n    required_scopes=[\"pipernet:read\"],\n    routes={\n        \"/payouts\": {\"required_scopes\": [\"pipernet:payout\"], \"require_delegation\": True},\n    },\n    exclude=[\"/healthz\"],\n)\n\n@app.post(\"/payouts/create\")\nasync def create_payout(request: Request):\n    principal = request.state.caracal\n    return {\"subject\": principal.sub}\n```\n\nWith `CARACAL_STS_URL` and `CARACAL_ZONE_ID` set - the standard Caracal workload variables - the middleware resolves the issuer and zone itself; you state only your own audience and revocation store. Requests reach your handlers only after the mandate's signature, issuer, audience, zone, token use, scopes, and revocation anchors all verify.\n\n## Enforce the right boundary\n\n| Option               | Use it for                                                                |\n| -------------------- | ------------------------------------------------------------------------- |\n| `required_scopes`    | Route-level scope checks.                                                 |\n| `required_targets`   | Resource-target checks.                                                   |\n| `require_session`    | Require a governed Session.                                               |\n| `require_delegation` | Require delegated authority.                                              |\n| `max_hop_count`      | Limit delegation depth.                                                   |\n| `routes`             | Apply any of the above per path prefix; the longest matching prefix wins. |\n\n## Validate\n\n1. Exchange for a mandate that targets the resource.\n2. Call the protected route with `Authorization: Bearer <mandate>`.\n3. Remove a required scope and confirm the route returns `403`.\n4. Revoke the session and confirm the route rejects the old mandate.\n\nExpected result: missing or invalid credentials return `401`, valid but insufficient authority returns `403`, excluded probes remain reachable, and handlers receive `request.state.caracal` only after verification.\n\n:::caution[Failure point: prefix matching]\nRoute configuration uses path prefixes and the longest match wins. Test overlapping prefixes so a broad rule cannot hide a stricter operation rule.\n:::\n\nFor every middleware field, use [ASGI Adapter reference](/sdks/adapters/asgi/).\n\n## Next step\n\nReplace the in-memory store with the Redis backend and run the revocation case in [Test Caracal Integrations](/guides/testing/)."},{"id":"/v0.2/guides/protect-fastmcp","title":"Protect a FastMCP App","url":"https://docs.caracal.run/v0.2/guides/protect-fastmcp/","concepts":"","body":"Use the FastMCP adapter when a FastMCP app should reject invalid, expired, insufficient, or revoked Caracal mandates. Python uses `caracalai-fastmcp`; TypeScript uses `@caracalai/fastmcp`.\n\n## Prerequisites\n\n- A mandate-aware MCP resource, stable audience, tool scopes, issuer, and zone ID.\n- A shared production revocation store.\n- A FastMCP hook that can reject before tool code runs; verification after dispatch is too late.\n\n## Install\n\n```bash\npip install \"caracalai-fastmcp[fastmcp]\" caracalai-revocation\n```\n\n```bash\nnpm install @caracalai/fastmcp @caracalai/verify @caracalai/revocation\n```\n\nUse `caracalai-revocation-redis` (Python) or the Redis store in `@caracalai/revocation` (TypeScript) for shared production revocation. The in-memory store is only for local development and tests.\n\n## Create an authenticator (Python)\n\n```python\nfrom caracalai_fastmcp import CaracalAuth\nfrom caracalai_revocation import InMemoryRevocationStore\n\nauth = CaracalAuth(\n    issuer=\"https://sts.pipernet.example\",\n    audience=\"resource://pipernet\",\n    expected_zone_id=\"pied-piper-production\",\n    required_scopes=[\"mcp:tool:call\"],\n    required_targets=[\"resource://pipernet\"],\n    revocations=InMemoryRevocationStore(),\n    require_session=True,\n)\n\nasync def startup():\n    await auth.warmup()\n```\n\n## Verify before running a tool (Python)\n\n```python\nfrom caracalai_fastmcp import CaracalAuthError\n\nasync def handle_tool_call(token: str, payload: dict):\n    try:\n        claims = await auth(token)\n    except CaracalAuthError as err:\n        return {\"error\": err.code, \"error_description\": err.description, \"error_hint\": err.hint}\n\n    return {\n        \"subject\": claims.sub,\n        \"result\": await run_tool(payload),\n    }\n```\n\nWire this check into the FastMCP auth or request hook used by your server. The important boundary is that the mandate is verified before the tool handler performs work.\n\n## Verify before running a tool (TypeScript)\n\n```ts\nimport { extractBearer, verifyFastMcpToken, FastMcpAuthError } from '@caracalai/fastmcp'\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.pipernet.example',\n  audience: 'resource://pipernet',\n  zoneId: 'pied-piper-production',\n  revocations: new InMemoryRevocationStore(),\n})\n\nasync function handleToolCall(authorization: string, payload: unknown) {\n  const token = extractBearer(authorization)\n  if (!token) return { error: 'missing_token' }\n\n  try {\n    const context = await verifyFastMcpToken(token, verifier, {\n      requiredScopes: ['mcp:tool:call'],\n      requiredTargets: ['resource://pipernet'],\n      requireSession: true,\n    })\n    return { subject: context.sub, result: await runTool(payload) }\n  } catch (err) {\n    if (err instanceof FastMcpAuthError) return { error: err.code }\n    throw err\n  }\n}\n```\n\nThe boundary is the same in both languages: verify the mandate before the tool handler performs work.\n\n## Validate\n\n| Test                                                             | Expected result                   |\n| ---------------------------------------------------------------- | --------------------------------- |\n| Missing bearer token                                             | `missing_token` or framework 401. |\n| Wrong audience                                                   | `invalid_token`.                  |\n| Missing scope                                                    | `insufficient_scope`.             |\n| Revoked session                                                  | `session_revoked`.                |\n| Mandate without a Session with `require_session=True` / `requireSession: true` | `session_required`.               |\n\nExpected result: the tool handler runs only for a mandate matching issuer, audience, zone, target, scope, Session requirements, and current revocation state.\n\n:::caution[Failure point: adapter scope]\nThe adapter verifies one token; it does not start a Session, mint authority, or authorize a later outbound call. Use the SDK inside verified context for governed downstream work.\n:::\n\nFor exact call signatures, use [FastMCP Adapter reference](/sdks/adapters/fastmcp/).\n\n## Next step\n\nUse [Protect an MCP Server](/guides/protect-mcp/) only when the framework hook cannot use this dedicated adapter."},{"id":"/v0.2/guides/protect-nethttp","title":"Protect a Go net/http Service","url":"https://docs.caracal.run/v0.2/guides/protect-nethttp/","concepts":"","body":"Use the Go net/http adapter when a Go service should verify Caracal mandates at the handler boundary.\n\n## Prerequisites\n\n- A mandate-aware resource with a stable audience and route scopes.\n- STS issuer, zone ID, and a production Redis revocation consumer.\n- Request deadlines on inbound handlers so JWKS or revocation work is cancelable.\n\n## Install\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/adapters/nethttp/go\ngo get github.com/garudex-labs/caracal/packages/revocation/go\n```\n\n## Wrap a handler\n\n```go\npackage main\n\nimport (\n\t\"encoding/json\"\n\t\"net/http\"\n\t\"time\"\n\n\tnethttp \"github.com/garudex-labs/caracal/packages/adapters/nethttp/go\"\n\trevocation \"github.com/garudex-labs/caracal/packages/revocation/go\"\n\tverify \"github.com/garudex-labs/caracal/packages/verify/go\"\n)\n\nfunc main() {\n\trevocations := revocation.NewInMemoryStore(24 * time.Hour)\n\n\tverifier := verify.NewVerifier(verify.Options{\n\t\tIssuer:      \"https://sts.pipernet.example\",\n\t\tAudience:    \"resource://pipernet\",\n\t\tZoneID:      \"pied-piper-production\",\n\t\tRevocations: revocations,\n\t})\n\n\tprotected := nethttp.VerifierMiddleware(verifier.Require(verify.Options{\n\t\tRequiredScopes:  []string{\"pipernet:read\"},\n\t\tRequiredTargets: []string{\"resource://pipernet\"},\n\t}))(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n\t\tclaims, ok := nethttp.ClaimsFromContext(r.Context())\n\t\tif !ok {\n\t\t\thttp.Error(w, \"missing claims\", http.StatusUnauthorized)\n\t\t\treturn\n\t\t}\n\t\t_ = json.NewEncoder(w).Encode(map[string]string{\"subject\": claims.Sub})\n\t}))\n\n\thttp.Handle(\"/reports\", protected)\n\t_ = http.ListenAndServe(\":8080\", nil)\n}\n```\n\n## Enforce constraints\n\n| Option                 | Use it for                     |\n| ---------------------- | ------------------------------ |\n| `RequiredScopes`       | Route or operation permission. |\n| `RequiredTargets`      | Resource target matching.      |\n| `RequireSession`       | Session-bound endpoints.       |\n| `RequireDelegation`    | Delegated-only endpoints.      |\n| `RequireChainContains` | Application path requirements. |\n| `MaxHopCount`          | Delegation depth limit.        |\n\n## Production revocation\n\nThe in-memory store does not share revocations across instances. Use the Redis revocation backend and consume `caracal.sessions.revoke` for production resource servers.\n\n## Validate\n\n1. Call without a bearer token and expect `401`.\n2. Call with a valid mandate and expect the handler response.\n3. Remove a required scope and expect `403`.\n4. Mark the session revoked and expect `session_revoked`.\n\nExpected result: `401` identifies an unaccepted credential, `403` identifies accepted but insufficient authority, and `ClaimsFromContext` succeeds only inside protected handlers.\n\n:::caution[Failure point: middleware order]\nPlace Caracal verification before handlers that read bodies or perform work. Logging may wrap it, but authentication-dependent middleware must run after it.\n:::\n\nFor exact Go fields, use [net/http Adapter reference](/sdks/adapters/nethttp/).\n\n## Next step\n\nConnect the Redis revocation consumer and validate it with [Test Caracal Integrations](/guides/testing/)."},{"id":"/v0.2/guides/protect-mcp","title":"Protect an MCP Server","url":"https://docs.caracal.run/v0.2/guides/protect-mcp/","concepts":"","body":"Use the verify packages when your MCP framework does not have a dedicated Caracal adapter or when you want to build your own boundary.\n\n## Prerequisites\n\n- A resource mandate audience and tool-scope map.\n- Issuer, zone ID, shared revocation storage, and a hook that runs before tool dispatch.\n- A consistent mapping from verification errors to the framework's unauthorized/forbidden response.\n\n## TypeScript verifier\n\n```bash\nnpm install @caracalai/verify @caracalai/revocation\n```\n\n```ts\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.pipernet.example',\n  audience: 'resource://pipernet',\n  zoneId: 'pied-piper-production',\n  revocations: new InMemoryRevocationStore(),\n})\n\nexport async function verifyToolRequest(authorization: string | undefined) {\n  const result = await verifier.authorization(authorization, {\n    requiredScopes: ['mcp:tool:call'],\n    requiredTargets: ['resource://pipernet'],\n    requireSession: true,\n  })\n\n  if (!result.ok) {\n    throw new Error(`${result.error.code}: ${result.error.description}`)\n  }\n\n  return result.principal\n}\n```\n\n## Python verifier\n\n```bash\npip install caracalai-verify caracalai-revocation\n```\n\n```python\nfrom caracalai_verify import authenticate, extract_bearer\nfrom caracalai_revocation import InMemoryRevocationStore\n\nrevocations = InMemoryRevocationStore()\n\nasync def verify_tool_request(authorization: str | None):\n    token = extract_bearer(authorization)\n    result = await authenticate(\n        token or \"\",\n        issuer=\"https://sts.pipernet.example\",\n        audience=\"resource://pipernet\",\n        required_scopes=[\"mcp:tool:call\"],\n        expected_zone_id=\"pied-piper-production\",\n        revocations=revocations,\n        require_session=True,\n        required_targets=[\"resource://pipernet\"],\n    )\n    if result.error is not None:\n        raise RuntimeError(f\"{result.error.code}: {result.error.description}\")\n    return result.principal\n```\n\n## Verification checklist\n\n| Check                            | Why it matters                                             |\n| -------------------------------- | ---------------------------------------------------------- |\n| Issuer and audience              | Prevents accepting mandates from the wrong zone or target. |\n| Required scopes                  | Enforces tool-level authority.                             |\n| Required targets                 | Prevents cross-resource token reuse.                       |\n| Session or Delegation requirements | Keeps application-root and delegated calls separate.       |\n| Revocation store                 | Rejects revoked sessions and Delegations.                  |\n\n## Production revocation\n\nUse Redis-backed revocation packages for multi-instance MCP servers:\n\n- TypeScript: `@caracalai/revocation-redis`\n- Python: `caracalai-revocation-redis`\n- Go: `github.com/garudex-labs/caracal/packages/backends/redis/go`\n\nRun a consumer for the `caracal.sessions.revoke` stream so every resource server instance learns about revoked anchors.\n\n## Validate the boundary\n\nTest missing token, wrong audience, wrong target, missing scope, required Session, required Delegation, hop limit, and revoked Session before enabling traffic. Expected result: no rejected call reaches tool code and every allowed principal is derived from verified claims rather than request metadata.\n\n:::caution[Failure point: unsigned context]\nNever authorize from baggage, trace headers, tool arguments, or a caller-provided subject. Verified mandate claims are authoritative; other fields are correlation only.\n:::\n\nFor exact verifier functions and error types, use [Verify Package reference](/sdks/verify/).\n\n## Next step\n\nWrap the framework hook, then run [Test Caracal Integrations](/guides/testing/) with a revoked-anchor case."},{"id":"/v0.2/guides/audit-stream","title":"Tail and Query the Audit Stream","url":"https://docs.caracal.run/v0.2/guides/audit-stream/","concepts":"","body":"Use Audit when a request is denied, Approval is required, Delegation behaves unexpectedly, or an operator needs evidence for a run.\n\n## Prerequisites\n\n- A zone and known time window or request ID.\n- A trusted Admin client for automation; application credentials cannot read zone audit.\n- Durable event-ID deduplication if exporting continuously.\n\n## Web Console Workflow\n\n1. Open the packaged web console at `http://localhost:3001`.\n2. Select **Audit**.\n3. Filter by zone, decision, event type, or request ID.\n4. Open the event detail to inspect metadata, determining policies, and diagnostics.\n5. Open the decision trace from the event for a full request-level view.\n\n## Automation workflow\n\n```ts\nimport { AdminClient } from '@caracalai/admin'\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n})\n\nconst events = await admin.audit.list(process.env.CARACAL_ZONE_ID!, {\n  decision: 'deny',\n  limit: 25,\n})\n\nconst trace = await admin.audit.explain(process.env.CARACAL_ZONE_ID!, events[0].request_id!)\nconsole.log(trace.final_decision, trace.denied)\n```\n\n## Useful filters\n\n| Filter      | Use it for                                                       |\n| ----------- | ---------------------------------------------------------------- |\n| Request ID  | Stitch together STS, Gateway, adapter, and explain events.       |\n| Decision    | Find denies, allows, or partial decisions.                       |\n| Event type  | Focus on Policy, Session, Delegation, Approval, or admin changes. |\n| Time window | Investigate a run, incident, or rollout.                         |\n\n## Ship events to a SIEM\n\nPoll `admin.audit.list` with a bounded time window and persist the last processed event identity. Caracal does not expose a supported external audit-stream subscription API; Redis topics are runtime internals, not an application integration contract. Use Admin API list/export surfaces and forward events after deduplication.\n\n| Field                         | Type           | Meaning                                                                           |\n| ----------------------------- | -------------- | --------------------------------------------------------------------------------- |\n| `id`                          | string         | Unique event id; use as the dedup key.                                            |\n| `zone_id`                     | string         | Zone the event belongs to.                                                        |\n| `event_type`                  | string         | What happened: exchange, decision, replay_detected, revocation, lifecycle events. |\n| `request_id`                  | string \\| null | Correlates every event from one request across STS, Gateway, and adapters.        |\n| `decision`                    | string \\| null | `allow`, `deny`, or `partial` for decision-bearing events.                        |\n| `evaluation_status`           | string \\| null | How policy evaluation concluded.                                                  |\n| `metadata_json`               | object \\| null | Event-specific attributes: session, resource, scopes, client identity.            |\n| `occurred_at` / `ingested_at` | string         | Event time and audit-service ingest time (RFC 3339).                              |\n\nEvent detail (`admin.audit.explain`) adds the determining policies, policy-set version, manifest hash, and diagnostics for decision events - fetch it lazily from the SIEM for alerts rather than shipping it wholesale. Alert first on `decision: \"deny\"` spikes, `replay_detected`, and delegation events for sessions outside your expected label set.\n\n## What to capture in incidents\n\n- request ID;\n- zone ID;\n- application ID;\n- resource identifier;\n- requested scopes;\n- final decision;\n- determining policies;\n- diagnostics;\n- session or Delegation IDs when present.\n\nRelated page: [Audit and Request Traces](/concepts/audit-ledger/).\n\n## Validate the export\n\nGenerate one allow, deny, approval, Delegation, and Gateway result. Confirm request correlation, ordering in the destination, replay-safe deduplication by event ID, and credential redaction. Expected result: an operator can move from a SIEM alert to the Caracal request trace without querying internal Redis.\n\n:::caution[Failure point: polling cursor]\nDo not use wall-clock time alone as an exactly-once cursor. Re-read an overlap window and deduplicate by event ID so late ingestion does not create gaps.\n:::\n\n## Next step\n\nUse [Debug Authorization Decisions](/guides/authorize-access/) to turn a correlated deny into a focused correction."},{"id":"/v0.2/guides/delegation","title":"Implement Multi-Agent Delegation","url":"https://docs.caracal.run/v0.2/guides/delegation/","concepts":"","body":"Use Delegation when one Session needs to hand a narrower slice of authority to another Session. The SDK creates the Delegation and carries the context; the web console shows the graph and impact.\n\n## Prerequisites\n\n- Two live Sessions and a parent that already holds the resource authority being delegated.\n- A specific resource, scope subset, positive TTL, and intended maximum hop count.\n- An authenticated work channel for transferring the opaque Delegation ID to the target Session.\n\n`session()` always starts the child under the **same application** as the parent. Narrow the child's authority only when it should hold less than the parent. To hand authority across applications, use `delegate()` with a peer Session under the other application; it returns the Delegation, and the receiver presents it with `acceptDelegation()`.\n\n## Implementation flow\n\nPython uses `Authority.narrow(...)`, `await caracal.delegate(...)`, and `async with caracal.accept_delegation(edge_id, validate=True)`. Go uses `AuthorityNarrow(...)`, `Delegate`, and `AcceptDelegation`. The [language SDK references](/sdks/) define exact option types; the workflow and wire authority are equivalent.\n\n## TypeScript example\n\n```ts\nimport { Caracal, Authority } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\n\nawait caracal.session(async () => {\n  await caracal.session(\n    async () => {\n      const headers = await caracal.headersAsync()\n      await fetch('https://api.pipernet.example/reports', { headers })\n    },\n    {\n      authority: Authority.narrow(['pipernet:read'], {\n        resourceId: 'resource://pipernet',\n        constraints: { maxHops: 1 },\n        ttlSeconds: 600,\n      }),\n    },\n  )\n})\n```\n\n## Review the graph\n\n1. Open the packaged web console at `http://localhost:3001`.\n2. Select **Delegation**.\n3. Inspect active edges, inbound edges, outbound edges, and traversal.\n4. Use impact before revoking an edge.\n5. Check **Audit** for the delegated exchange and resource decision.\n\n## Hand Over the Delegation ID Safely\n\nThe issuer and receiver share the opaque Delegation ID over their authenticated work channel, such as a queue message, RPC field, or task payload. Creation is only an offer and does not mutate receiver context. Possessing and presenting this target-Session-bound ID through `acceptDelegation()` is receiver consent; STS also checks the receiving application and live target Session. Use `acceptDelegation(delegationId, fn, { validate: true })` to confirm that exact ID with Coordinator before work runs under it. Every presentation emits a `delegation.accept` event.\n\n## Safe constraints\n\n| Constraint | Good default                              |\n| ---------- | ----------------------------------------- |\n| Scopes     | Small subset of parent authority.         |\n| TTL        | Required; use minutes, not days.          |\n| Hop count  | `1` unless a deeper graph is intentional. |\n| Resource   | Single resource whenever possible.        |\n\n## Troubleshooting\n\n| Symptom                               | Check                                                                |\n| ------------------------------------- | -------------------------------------------------------------------- |\n| `Delegate requires an active Session` | Call delegation inside `session()` or a bound Caracal context.       |\n| Resource denies delegated request     | Confirm the edge, ancestry, policy grant, and required scopes remain live. |\n| Chain validation fails                | Confirm authoritative parent links are continuous; configure `requireChainContains` separately at verifiers when needed. |\n| Revocation did not affect child       | Confirm cascade revocation and resource-server revocation consumers. |\n\n## Validate the delegation\n\nMint the allowed subset, attempt one broader scope, revoke the edge, and retry. The subset must succeed; broadening must fail server-side; after revocation neither the child nor descendants may mint or pass resource verification.\n\n:::caution[Failure point: lifetime]\nA Delegation can expire before its target Session. The Session may remain observable but cannot mint through the expired edge. Size the edge TTL to the work and handle terminal authorization failure rather than widening authority.\n:::\n\n## Next step\n\nInspect the graph and request trace in [Tail and Query the Audit Stream](/guides/audit-stream/)."},{"id":"/v0.2/guides/step-up","title":"Human Approval","url":"https://docs.caracal.run/v0.2/guides/step-up/","concepts":"","body":"Human approval pauses a token exchange until a person decides it. A policy data document classifies scopes into risk tiers and declares which tiers need a human decision; when an agent requests a gated scope, the STS parks the exchange on a durable hold and returns `interaction_required` instead of a mandate. The gate is optional: a zone that declares no approval tiers never sees a hold.\n\n## When to use approval\n\nUse it for infrequent high-impact authority where waiting is safer than automatic issuance. Do not use approval as user authentication, a substitute for least privilege, or a repair for an overbroad provider credential.\n\n## Prerequisites\n\n- Valid grant data for the underlying scope; approval can gate authority but cannot create it.\n- An operator approval path, or a registered subject issuer plus implemented federation for subject-only decisions.\n- A durable place to persist `approvalId` and operation identity across worker restarts.\n\n## Flow\n\n## Declare the gate in policy data\n\nApproval is declared as data, like every other policy input. `risk` names a tier for each sensitive scope; `approval_tiers` declares which tiers hold a mint for a decision:\n\n```text\n# caracal:data-document\npackage caracal.authz\n\nimport rego.v1\n\nrisk := [\n  {\"scope\": \"pipernet:refund\", \"tier\": \"high\"},\n]\n\napproval_tiers := [\n  {\"tier\": \"high\", \"approver\": \"operator\", \"ttl_seconds\": 1800, \"privacy\": \"identified\"},\n]\n```\n\n| Field         | Meaning                                                                                                                                                            |\n| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `tier`        | Your tier name; the platform fixes no taxonomy. Required - a declaration without a tier fails the mint closed.                                                     |\n| `approver`    | Who may decide: `operator` (approve-capable admin credential), `subject` (the application's own federated end user - see below), or `any`. Defaults to `operator`. |\n| `ttl_seconds` | How long the hold stays decidable, clamped between 60 seconds and 7 days. Defaults to 1800.                                                                        |\n| `privacy`     | How much approver identity the decision record retains: `identified`, `pseudonymous`, or `anonymous`. Defaults to `identified`.                                    |\n\nWhen one mint matches several compatible tiers, the shortest window and most protective privacy mode win, and a specific `operator` or `subject` requirement wins over `any`. A request that combines an operator-only tier with a subject-only tier is denied and must be split into separate mints: one decision cannot satisfy two independent approver roles. Invalid approver, privacy, or TTL declarations fail the mint closed.\n\nTune `ttl_seconds` to the tier's real decision window: it bounds how long an approved-but-unconsumed hold stays spendable, so a shorter TTL shrinks the window in which a leaked approval could be consumed, while a TTL shorter than your approvers' actual response time just converts legitimate requests into expiries. High-value, rarely exercised operations warrant minutes; routine operator flows can keep the 30-minute default.\n\n## Handle the hold in the agent\n\nThe `interaction_required` error carries the approval id, tier, expiry, and an opaque `binding`. The binding covers the principal, Authority record, governed Session, Delegation, application, canonical resource and scope sets, active policy-set manifest, and platform decision contract. A retry after policy or execution context changes cannot spend an earlier decision. The SDK runs the whole flow - catch the hold, wait for the decision, retry with the approval id so the retried mint consumes it - in one call:\n\n```ts\nimport { Caracal } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\n\nconst mandate = await caracal.withApproval((approvalId) => caracal.mintMandate('resource://pipernet', ['pipernet:refund'], { approvalId }))\n```\n\nWhen the wait outcome is anything but `approved` - rejected, expired, already consumed, or the wait timed out - `withApproval` rethrows the original `ApprovalRequiredError`. Its `approvalId` is durable: persist it and resume later with `waitForApproval`, which long-polls and returns the typed final state without re-raising the hold:\n\n```ts\nconst state = await caracal.waitForApproval(persistedApprovalId, { timeoutMs: 600_000 })\n// 'approved'  -> retry the mint with { approvalId } to consume the decision\n// 'pending'   -> nobody has decided; waiting again is safe\n// 'rejected' | 'expired' | 'consumed' -> terminal; re-request the operation\n```\n\nAn approval is **single-use**: the first mint that presents the approval id spends it. When two workers race one approval - a retried queue message, a redeployed worker resuming the same persisted id - one wins and the loser receives the typed `approval_consumed` error. This is not a new `ApprovalRequired` hold and is not an error to wait out: check whether the intended effect already happened before requesting another approval.\n\nThe Python and Go clients expose the same surface as `with_approval`/`wait_for_approval` and `WithApproval`/`WaitForApproval`. The lower-level `@caracalai/oauth` client offers `waitForApproval` directly for integrations that drive the exchange themselves. Under `caracal run` none of this is hand-written: the engine prints an `approval_required` notice with the approval id and binding, waits on the hold, and retries the exchange itself.\n\n## Decide as an operator\n\nOpen the zone's **Approvals** page in the Console to review pending holds - each shows the requesting principal, the tier, the binding to cross-check against the agent's notice, and the approval window - and approve or reject with an optional reason. For automation, the Admin API exposes the same de"},{"id":"/v0.2/guides/approval-notifications","title":"Approval Notifications","url":"https://docs.caracal.run/v0.2/guides/approval-notifications/","concepts":"","body":"Approval holds are decided fastest when the right person hears about them immediately. A notification sink is a zone-scoped webhook endpoint that Caracal pushes approval lifecycle events to: your relay turns the delivery into a page, a chat message, or a ticket in whatever system your team watches. Sinks are optional - a zone without one loses nothing except the push, since the Console badges pending holds and the [Approvals page](/guides/step-up/#decide-as-an-operator) remains the decision surface.\n\n## Prerequisites\n\n- A public HTTPS receiver with raw-body access, durable delivery-ID deduplication, and a secret manager.\n- An operational owner for retries, abandoned deliveries, and secret rotation.\n- Approval tiers already validated end to end without notifications.\n\n## Create a sink\n\nOpen **Settings → Notifications** in the Console and add a sink with a name, an HTTPS endpoint, and the event types it should receive. The response reveals the signing secret exactly once - store it in your receiver's secret manager before closing the dialog, because Caracal keeps only a sealed copy it cannot show again. For automation, the Admin API exposes the same lifecycle:\n\n```bash\ncurl -X POST \"$CARACAL_API_URL/v1/zones/$CARACAL_ZONE_ID/notification-sinks\" \\\n  -H \"Authorization: Bearer $CARACAL_ADMIN_TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"name\": \"Pied Piper on-call relay\",\n    \"url\": \"https://hooks.hooli.example/caracal-approvals\",\n    \"event_types\": [\"step_up_issued\", \"step_up_decided\"]\n  }'\n```\n\nEndpoints must be HTTPS; plain HTTP is accepted only for loopback addresses during local development, and URLs may never embed credentials. Delivery resolves and pins DNS for each connection and rejects loopback, private, metadata, link-local, CGNAT, multicast, mapped, and NAT64-embedded restricted addresses. Authentication is the signature, not the URL. A zone holds at most 20 sinks.\n\n| Event type         | Fires when                                                                                              |\n| ------------------ | ------------------------------------------------------------------------------------------------------- |\n| `step_up_issued`   | A hold is created and waits for a decision. This is the event to page on.                               |\n| `step_up_decided`  | An approver settles the hold - the payload's `decision` field says whether it was approved or rejected. |\n| `step_up_consumed` | The approved hold releases its mandate to the retrying agent.                                           |\n\n## Verify every delivery\n\nEach delivery is an HTTP `POST` with a JSON body and these headers:\n\n| Header                | Content                                                                             |\n| --------------------- | ----------------------------------------------------------------------------------- |\n| `X-Caracal-Event`     | The event type, for routing before parsing.                                         |\n| `X-Caracal-Delivery`  | Unique delivery id. Deliveries are at-least-once; use this id to deduplicate.       |\n| `X-Caracal-Sink`      | The sink id, so one receiver can serve several sinks.                               |\n| `X-Caracal-Timestamp` | Unix seconds when the delivery was signed. Reject stale timestamps to stop replays. |\n| `X-Caracal-Signature` | `v1=` followed by hex HMAC-SHA256 of `timestamp.body` under the sink secret.        |\n\nVerify the signature over the raw request body before trusting anything in it:\n\n```ts\nimport { createHmac, timingSafeEqual } from 'node:crypto'\n\nfunction verifySink(secret: string, headers: Record<string, string>, rawBody: string): boolean {\n  const timestamp = headers['x-caracal-timestamp']\n  if (Math.abs(Date.now() / 1000 - Number(timestamp)) > 300) return false\n  const expected = `v1=${createHmac('sha256', secret).update(`${timestamp}.${rawBody}`).digest('hex')}`\n  const received = headers['x-caracal-signature'] ?? ''\n  return received.length === expected.length && timingSafeEqual(Buffer.from(received), Buffer.from(expected))\n}\n```\n\nThe body carries the audit event verbatim - `data` is the same metadata the zone audit stream records for the hold, so a relay can render a complete approval prompt without calling back:\n\n```json\n{\n  \"id\": \"0195f2aa-7c31-7d4e-a01b-3f6c2d9e8b10\",\n  \"type\": \"step_up_issued\",\n  \"zone_id\": \"0195f2a9-1b22-7c3d-9e4f-5a6b7c8d9e0f\",\n  \"decision\": \"pending\",\n  \"occurred_at\": \"2026-01-01T12:00:00.000Z\",\n  \"data\": {\n    \"challenge_id\": \"0195f2aa-6d20-7b3c-8e1a-2f4d6c8b0a9e\",\n    \"tier\": \"high\",\n    \"approver_class\": \"operator\",\n    \"privacy_mode\": \"identified\",\n    \"binding\": \"9f3c…\",\n    \"expires_at\": \"2026-01-01T12:30:00Z\",\n    \"application_id\": \"0195f2a9-4e55-7a1b-bc2d-3e4f5a6b7c8d\",\n    \"session_id\": \"0195f2aa-2c11-7e9f-8a0b-1c2d3e4f5a6b\"\n  }\n}\n```\n\nRespond with any `2xx` status within ten seconds. The response body is ignored - do your downstream work asynchronously and acknowledge fast.\n\n## Delivery semantics\n\nDelivery is at-least-once with per-sink ordering by the zone audit sequence. A non-`2xx` response or a timeout schedules a retry with exponential backoff from 30 seconds to a 15-minute ceiling; after 8 attempts the delivery is abandoned and the failure is visible on the sink's delivery record in the Console. Pausing a sink stops deliveries without losing anything: the sink's cursor into the audit stream holds its place, and resuming catches up from where it stopped. A run of consecutive failures flags the sink as failing in the Console but never disables it - fix the receiver and the backlog drains on its own.\n\nRotating the secret (Console or `POST …/notification-sinks/{id}/rotate-secret`) invalidates the old value immediately and reveals the replacement once; update the receiver in the same change window. Sink creation, edits, rotations, and deletions are themselves recorded in the zone audit stream, with secret material redacted.\n\nRelated pages: [Human Approval](/guides/ste"},{"id":"/v0.2/guides/production-patterns","title":"Production Integration Patterns","url":"https://docs.caracal.run/v0.2/guides/production-patterns/","concepts":"","body":"Use this review after one application and one protected resource work end to end. Production deployments combine a primary enforcement boundary with SDK lifecycle, shared revocation, policy rollout, audit export, and operational ownership.\n\n## Prerequisites\n\n- A tested allow, deny, revoke, and request-trace path.\n- Named owners for application identity, resource verification, policy rollout, revocation, audit, and incidents.\n- TLS, secret storage, shared revocation state, and timeout budgets appropriate to the deployment.\n\n## Choose the Enforcement Boundary\n\n| Boundary | Use when | Enforcement and result evidence |\n| --- | --- | --- |\n| Gateway-routed HTTP | Caracal can route the request before it reaches the upstream. | Gateway verifies, exchanges, routes, and records the upstream result. |\n| Framework adapter | You own the resource server and must verify inside Express, ASGI, FastMCP, or Go `net/http`. | The service verifies before work and must emit its action result. |\n| Verify engine | No supported adapter fits the service boundary. | Custom middleware uses the shared verifier and owns result evidence. |\n| Runtime credential injection | An existing process needs a credential once at launch. | `caracal run` gates launch; it does not renew or enforce later provider calls. |\n\nUse [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/) for the default HTTP path. Use the framework-specific protection guides when enforcement must live in the resource server.\n\n## Reference Flow\n\n## Identity and Configuration Boundaries\n\nOne SDK client represents one `(zone, application)` identity during concurrent use. Keep one client and one credential source per application boundary. Do not swap identities through a shared resolver while Sessions or transports are active.\n\nOn a shared host, give each service its own application, profile, and owner-only secret file:\n\n```toml\nzone_id = \"pied-piper-production\"\napplication_id = \"son-of-anton\"\napp_client_secret_file = \"/etc/caracal/son-of-anton.secret\"\nsts_url = \"https://sts.pipernet.example\"\ngateway_url = \"https://gateway.pipernet.example\"\n\n[[credentials]]\nresource = \"resource://pipernet\"\n```\n\nUse a dynamic credential resolver only when one application credential is rotated or resolved externally. The next exchange can use the replacement credential; active identities still remain separate clients.\n\n## Propagation into Other Protocols\n\nCaracal SDKs project authority and trace context into HTTP headers. When another protocol carries equivalent metadata, inject the SDK-produced fields per call and verify the Mandate before the server performs work.\n\nFor gRPC, use a language-native client interceptor to copy the authorization and Caracal metadata into call metadata, then build server middleware around the verify package. Caracal does not ship gRPC middleware, service-mesh identity, or protocol-specific result audit.\n\nA service mesh authenticates network peers; it does not replace Caracal resource scopes, policy decisions, Mandate verification, or revocation.\n\n## OpenTelemetry Correlation\n\nSDK transports preserve valid W3C trace context and add Caracal baggage. Gateway audit includes trace and request correlation. Validate that one APM trace can pivot to the corresponding Caracal request trace without treating tracing as an authorization control.\n\n## Queue and Batch Work\n\nUse a Session per governed unit of work. Supply an explicit idempotency key only when a durable source provides a stable delivery identifier. Caracal makes supported Coordinator creations replay-safe; it does not make arbitrary callbacks, queue consumption, or upstream mutations exactly once.\n\nFor long-running workers, use SDK-managed service Sessions and lease callbacks. Do not use `caracal run` credentials beyond their issued lifetime because the launcher never renews them.\n\n## Production Checklist\n\n| Area | Required evidence |\n| --- | --- |\n| Enforcement | A request cannot reach the protected action without Gateway or verifier acceptance. |\n| Identity | Each application boundary has distinct credentials, clients, policy attribution, and rotation ownership. |\n| Revocation | Shared consumers receive Session and Delegation invalidation; stale state fails according to the documented boundary. |\n| Policy | Candidate versions validate and simulate, activation reaches every STS replica, and rollback is rehearsed. |\n| Audit | Authorization and action-result evidence correlate by request or trace ID. |\n| Approval | Gated scopes reach the intended operator or federated Subject decision plane. |\n| Recovery | Dependency outages, audit replay, restore, and credential rotation are tested. |\n\n## Validate After Rollout\n\nRun a successful request, policy denial, verifier denial, revoked-Session request, replayed Gateway request, and Approval-gated request. Confirm expected HTTP or SDK errors and complete audit evidence.\n\nIn staging, interrupt STS, Redis revocation freshness, Audit delivery, and the upstream. Confirm access fails closed where authority cannot be proven and application retries remain bounded and mutation-safe.\n\n## Next Action\n\nComplete the focused guide for the selected enforcement boundary, then use [Operate Caracal](/operations/) to deploy and monitor it."},{"id":"/v0.2/concepts","title":"Understand the Model","url":"https://docs.caracal.run/v0.2/concepts/","concepts":"","body":"Read this section after Get Started when you need to design an integration or explain a decision. It builds one external model: an Application acts in a Zone, optional Subject identity adds attribution, a Session bounds execution, Delegation narrows authority, policy approves a Resource request, and a Mandate carries the result.\n\n## Read This Section in Order\n\n| Start here                                                | Use it to understand                                                          |\n| --------------------------------------------------------- | ----------------------------------------------------------------------------- |\n| [Caracal Mental Model](/concepts/model-overview/)         | The smallest useful picture of Caracal.                                       |\n| [Authority and Enforcement](/concepts/authority-model/)   | Where decisions happen before requests reach a target.                        |\n| [Zones](/concepts/zone/)                                  | The tenant boundary that owns keys, policies, resources, sessions, and audit. |\n| [Identities and Applications](/concepts/principal/)       | Application credentials, optional Subjects, Authority records, and Sessions.  |\n| [Resources and Grants](/concepts/resource-grant/)         | What can be accessed and which scopes are granted.                            |\n| [Providers](/concepts/provider/)                          | The credential Caracal attaches to the upstream target.                       |\n| [Policies and Policy Sets](/concepts/policy/)             | Rego rules evaluated by the STS during token exchange.                        |\n| [Mandates](/concepts/mandate/)                            | The short-lived JWT that carries approved authority.                          |\n| [Approvals](/concepts/step-up/)                           | How sensitive actions are held for a human decision.                          |\n| [Session Delegation](/concepts/delegation/)               | How one agent passes bounded authority to another.                            |\n| [Delegation Constraints](/concepts/constraint/)           | The limits attached to delegated authority.                                   |\n| [Sessions and Revocation](/concepts/sessions-revocation/) | How active authority is ended and propagated.                                 |\n| [Audit and Request Traces](/concepts/audit-ledger/)       | The event trail behind decisions and runs.                                    |\n| [Caracal Operator](/concepts/operator/)                   | An optional governed assistant for reviewed console changes.                  |\n\n## Core Flow\n\nThe same model appears across the product:\n\n- Onboarding uses the web console guided setup to create the first zone, application, resource, and policy, then you create a workload with launch bindings on the console's Launcher page and give it its workload ID and secret.\n- Guides use the SDKs, Console, Admin API, and adapters to build repeatable integrations.\n- Operations pages use the same terms when explaining keys, revocation, audit, and runtime health.\n\n## Term Map\n\n| Term              | Short definition                                                                                         | Canonical page                                            |\n| ----------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |\n| Zone              | Tenant boundary for authority data and signing keys.                                                     | [Zones](/concepts/zone/)                                  |\n| Application       | Registered client or agent workload.                                                                     | [Identities and Applications](/concepts/principal/)       |\n| Subject           | Optional identity federated from your identity provider for attribution and supported approval flows. | [Identities and Applications](/concepts/principal/)       |\n| Authority record  | Immutable record of identity and authority context created by an exchange.                             | [Identities and Applications](/concepts/principal/)       |\n| Resource          | Protected API, MCP server, tool group, or upstream target.                                               | [Resources and Grants](/concepts/resource-grant/)         |\n| Grant             | Policy data that describes which Application roles may request Resource scopes.                         | [Resources and Grants](/concepts/resource-grant/)         |\n| Provider          | Credential mode Caracal uses toward the upstream target.                                                 | [Providers](/concepts/provider/)                          |\n| Policy            | Versioned Rego logic evaluated at token exchange.                                                        | [Policies and Policy Sets](/concepts/policy/)             |\n| Policy set        | Versioned bundle of policies activated for a zone.                                                       | [Policies and Policy Sets](/concepts/policy/)             |\n| Mandate           | Short-lived JWT issued by the STS after policy approval.                                                 | [Mandates](/concepts/mandate/)                            |\n| Delegation        | Bounded authority transfer between Sessions.                                                             | [Session Delegation](/concepts/delegation/)               |\n| Revocation anchor | Authority record ID, Root authority record ID, Session ID, or Delegation ID checked by resource servers. | [Sessions and Revocation](/concepts/sessions-revocation/) |\n| Caracal Operator  | Governed natural-language assistant that turns intent into reviewed control-plane changes.               | [Caracal Operator](/concepts/operator/)                   |\n| System zone       | Reserved `caracal.sys/` z"},{"id":"/v0.2/concepts/model-overview","title":"Caracal Mental Model","url":"https://docs.caracal.run/v0.2/concepts/model-overview/","concepts":"","body":"Caracal answers one question: **should this Application receive scoped authority for this Resource right now?**\n\nIt answers that question during token exchange, records the decision, and returns a mandate only when the active policy set allows the request.\n\n## Before You Begin\n\nKnow the Application and Resource from Get Started. No protocol knowledge is required.\n\n## Seven Core Nouns\n\n| Noun        | What it means                                                                      |\n| ----------- | ---------------------------------------------------------------------------------- |\n| Zone        | The trust boundary for configuration, signing keys, policy, Sessions, and audit. |\n| Application | Registered software that authenticates to Caracal.                                |\n| Subject     | An optional identity federated from your identity provider for attribution.        |\n| Session     | One governed execution under an Application.                                       |\n| Resource    | The protected target: API, MCP server, tool group, or upstream service.            |\n| Policy      | Versioned data the platform decision contract evaluates during exchange.           |\n| Mandate     | The short-lived signed proof accepted by the Gateway or a verified service.        |\n\nMost of these are configured directly. A _grant_ is a permission binding for resource scopes that policy can read during evaluation; it is not the mandate a resource server verifies.\n\n## One Application, Many Sessions\n\nAn application and a Session are different layers, and they scale differently:\n\n- An **application** is the credentialed security boundary - operator-provisioned or dynamically registered, holding the secret Caracal authenticates. It is created deliberately.\n- A **Session** is the governed execution unit - started the moment a workload acts, with no secret and no registration step.\n\nOne application backs many Sessions. A durable workload registers **one managed application**, then starts, delegates, and fans out as many Sessions as it needs under that single credential. You do not create an application per AI agent; per-execution attribution comes from the Session, not a new application. See [Should I create one application per agent?](/reference/faq/#faq-006).\n\n## Three Runtime Verbs\n\n| Verb     | Meaning                                                                                                    |\n| -------- | ---------------------------------------------------------------------------------------------------------- |\n| Exchange | Ask the STS to convert existing identity into a resource mandate.                                          |\n| Start    | Open a governed Session, optionally attaching a Subject authority record ID for attribution and lifecycle. |\n| Delegate | Pass constrained authority from one Session to another.                                                    |\n\n## One Decision Point\n\nCaracal evaluates the active policy with the Application, Authority record, Session, Resource, scopes, Delegation, and Approval context. If policy allows the request, Caracal signs a Mandate. If policy denies it, no Mandate is issued.\n\nA federated Subject can be attached for attribution and supported Subject approval flows. Federation does not create a separate per-Subject permission system: Resource authority still comes from the Application, policy, and any Delegation.\n\nResource servers still verify mandates locally through the Gateway or adapters. That keeps every request protected even after token exchange succeeds.\n\n## Why This Model Matters\n\n- Long-lived provider secrets stay out of agents.\n- Authority expires quickly and can be revoked.\n- Delegation carries typed constraints instead of informal trust.\n- Every allow, deny, Approval, and revocation path is auditable.\n\n## Outcome\n\nYou should be able to separate durable Application identity, optional Subject attribution, temporary Session execution, and short-lived Mandates.\n\nNext, read [Authority and Enforcement](/concepts/authority-model/) to see where each enforcement layer fits."},{"id":"/v0.2/concepts/authority-model","title":"Authority and Enforcement","url":"https://docs.caracal.run/v0.2/concepts/authority-model/","concepts":"","body":"Use this page to choose where Caracal makes and enforces an authorization decision. First understand [Caracal Mental Model](/concepts/model-overview/).\n\nCaracal separates **granting authority** from **using authority**.\n\n- The STS grants authority by issuing a short-lived mandate after policy evaluation.\n- The Gateway or adapter uses authority by verifying the mandate before forwarding a request or running a tool.\n\n## Enforcement Flow\n\n## Control Layers\n\n| Layer              | Responsibility                                                   | Source of truth                    |\n| ------------------ | ---------------------------------------------------------------- | ---------------------------------- |\n| Zone               | Owns keys, policies, resources, sessions, and audit data.        | Console or Admin API               |\n| Grant data         | Declares which Application roles may request Resource scopes.    | Console or Admin API               |\n| Policy set         | Makes the final allow, deny, or Approval decision.               | Rego policy versions               |\n| Mandate            | Carries approved authority as a short-lived signed JWT.          | STS                                |\n| Gateway or adapter | Verifies mandate claims and revocation before use.               | Runtime and resource server config |\n| Audit ledger       | Records decisions, diagnostics, and request correlation.         | API, Console, and storage          |\n\n## Where Resource Authority Comes From\n\nA Session does not receive Resource authority merely because it exists. Application-owned calls use the SDK's bounded call path. Agent work receives a narrowed Delegation. Both paths still require active policy approval before Caracal issues a Mandate.\n\nCommon mistake: treating a Session as a permission grant. A Session supplies lifecycle and attribution. Use an Application-owned call for ordinary app work, or attach a narrowed Delegation when a child or peer must hold less authority.\n\n## Default Posture\n\nCaracal should be treated as deny-by-default:\n\n1. A resource must be registered.\n2. The Application must have an applicable grant-data or Delegation path.\n3. The active policy set must allow the requested resource and scopes.\n4. The resource server must verify the mandate and revocation anchors.\n\nMissing configuration, invalid signatures, expired mandates, insufficient scopes, revoked sessions, and failed delegation checks all stop the request.\n\n## Gateway and Adapter Roles\n\nUse the Gateway when you want Caracal to front an HTTP upstream and mediate requests centrally. Use an adapter when the resource server should verify mandates inside its own framework, such as Express, FastMCP, or net/http, or use the verify engine directly for custom boundaries.\n\nBoth patterns share the same authority model. The difference is only where mandate verification runs.\n\n## Enforce, Propagate, or Attribute\n\nThe SDK exposes several call paths. They are not interchangeable: each does a different job, and only some **enforce** authority. Pick by what you need at that boundary.\n\n| Call path                                                                                    | Role          | Verifies the mandate?                                              | Use when                                                                         |\n| -------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------- |\n| SDK request through the Gateway                                                             | **Enforce**   | Yes - the Gateway verifies claims and revocation before forwarding | Caracal fronts an HTTP upstream and mediates centrally                           |\n| Direct Gateway call (`curl` to the Gateway URL)                                              | **Enforce**   | Yes - same Gateway verification                                    | Non-SDK clients or debugging the enforced path                                   |\n| Adapter verify (`context_middleware(verifier=…)`, Express, FastMCP, net/http, verify engine) | **Enforce**   | Yes - the adapter verifies inside the resource server              | The resource server should enforce in its own framework                          |\n| `caracal.transport` / `context_middleware()` (no verifier)                                   | **Propagate** | No - carries and binds the envelope only                           | A Gateway or adapter already enforced upstream and you only need context to flow |\n| App-managed provider call (your code calls the provider SDK directly)                        | **Attribute** | No - Caracal records who acted, but does not gate the call         | You want audit attribution without routing through the Gateway                   |\n\nRule of thumb: **use the Gateway or an adapter verifier to enforce; use transport/propagation to carry identity after enforcement; treat app-managed provider calls as attribution only.** If a path says \"no\" under _verifies the mandate_, something upstream must have already enforced it.\n\n## Outcome\n\nYou should be able to name the decision point, the enforcement boundary, and whether a proposed call path enforces, propagates, or only attributes authority.\n\n## Next Step\n\nRead [Zones](/concepts/zone/) to understand the tenant boundary that owns authority data.\n\n## Related Pages\n\n- [Policies and Policy Sets](/concepts/policy/) explains the decision contract.\n- [Mandates](/concepts/mandate/) explains the issued token.\n- [Sessions and Revocation](/concepts/sessions-revocation/) explains how active authority ends."},{"id":"/v0.2/concepts/zone","title":"Zones","url":"https://docs.caracal.run/v0.2/concepts/zone/","concepts":"","body":"Use this page when deciding what must be isolated from what. A Zone is Caracal's main trust boundary: it groups the identities, policy, Resources, keys, runtime authority, and audit evidence that must share one administrative boundary.\n\n## What a Zone Owns\n\n| Area          | Zone-owned data                                                              |\n| ------------- | ---------------------------------------------------------------------------- |\n| Identity      | Applications, credentials, Authority records, and Sessions.                  |\n| Authorization | Resources, Providers, grants, policies, policy sets, and Approvals.           |\n| Cryptography  | Zone signing keys and JWKS used to verify mandates.                          |\n| Delegation    | Delegations, constraints, depth limits, and cascade revocation state.        |\n| Audit         | Decision events, diagnostics, request IDs, and explain traces.               |\n\n## Why Zones Exist\n\nZones let teams run separate environments, tenants, or trust domains without mixing authority data.\n\nCommon zone boundaries include:\n\n- production, staging, and development environments;\n- separate customers in a hosted deployment;\n- isolated product areas with different policy owners;\n- high-sensitivity resources that need distinct keys and audit trails.\n\nWhen several boundaries could apply at once, use [Model Your Application in Caracal](/guides/modeling-recipes/) to choose between a separate zone, a shared zone with separate resources, and a customer attribute in policy input. To serve many of your own customers from one zone, follow [Serve Your Own Customers](/guides/serve-customers/).\n\n:::note[FAQ]\n[What should a zone represent?](/reference/faq/#faq-003)\n:::\n\n## Zone Lifecycle\n\nZone setup is normally managed through the web console. The Admin API exposes the same objects for automation.\n\n## Key and Policy Isolation\n\nEach zone has its own signing-key and JWKS context. Resource servers verify mandates against the issuer, audience, and expected zone. Policy activation is also zone-scoped: activating a policy set in one zone does not affect another zone.\n\n## Advanced Application Registration\n\nA Zone can allow programmatic registration of auto-expiring Applications. This is off by default and is for separate credential boundaries, not ordinary Session fan-out. The console does not create these Applications.\n\nDisabling registration while live auto-expiring Applications exist requires an explicit decision:\n\n| Choice      | Effect                                                                                                                                 |\n| ----------- | -------------------------------------------------------------------------------------------------------------------------------------- |\n| Keep live   | Blocks new registrations; existing DCR applications stay valid until their own expiry.                                                 |\n| Revoke live | Blocks new registrations and immediately archives live DCR applications, revoking their sessions and terminating related agent access. |\n\nThe console prompts for this choice. Use the Admin API reference when automating it.\n\n## System Zone\n\nCaracal reserves one **system zone** for the infrastructure that runs the platform itself, distinct from the tenant zones you create. It carries the reserved `caracal.sys/` namespace and is provisioned by the bootstrap admin identity, so a tenant can never create, author, or impersonate the objects inside it.\n\nThe [Caracal Operator](/concepts/operator/) self-governs through this zone: its reserved control identity, least-privilege control grants, and the providers and resources that route its model calls all live there. The Operator will not open a session in, or execute against, the system zone, so its delegated authority stays away from the infrastructure that runs Caracal. An administrator can also mark additional zones as system zones to place them out of the Operator's reach.\n\n## Operational Guidance\n\n- Keep production and non-production authority in separate zones.\n- Name zones after the trust boundary, not a single service.\n- Keep resource identifiers stable because policies, grants, and audit traces refer to them.\n- Rotate zone signing keys using the operations workflow, then confirm resource servers load the current JWKS.\n\n## Next Step\n\nRead [Identities and Applications](/concepts/principal/) to understand who acts inside a zone.\n\n## Related Pages\n\n- [Resources and Grants](/concepts/resource-grant/)\n- [Model Your Application in Caracal](/guides/modeling-recipes/)\n- [Audit and Request Traces](/concepts/audit-ledger/)"},{"id":"/v0.2/concepts/principal","title":"Identities and Applications","url":"https://docs.caracal.run/v0.2/concepts/principal/","concepts":"","body":"Use this page to decide which identity is durable, which identity is optional, and which record identifies one execution.\n\nAn **Application** is registered software that authenticates to Caracal. A **Subject** is an optional identity federated from your identity provider. An **Authority record** is an immutable record of identity and authority context. A **Session** is one governed execution under an Application.\n\n## Identity and Execution Objects\n\n| Public object    | Typical source                                | What it controls                                                                                                           |\n| ---------------- | --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |\n| Application      | Workload credential or Application secret     | The durable credential and policy boundary for software.                                                                  |\n| Subject          | Token from your registered identity provider  | Attribution, provider connection ownership, revocation anchor, and supported Subject approvals; not scope authority alone. |\n| Authority record | Successful identity or authority exchange     | Immutable audit and revocation context.                                                                                    |\n| Session          | SDK runtime primitive                         | The lifetime and exact attribution of one governed execution.                                                              |\n\nNo identity object authorizes a Resource by itself. A request still needs Resource scopes, applicable policy, and any required Delegation or Approval.\n\n## Application Roles\n\nApplications represent software that can participate in Caracal flows:\n\n- an agent runtime that starts child Sessions;\n- a backend service that requests mandates;\n- a Gateway application that fronts protected upstreams;\n- an adapter-protected resource server;\n- a managed or dynamically registered client.\n\nApplications have registration metadata, a server-owned credential, and a registration method. Durable Applications are operator-provisioned for known software, including runtimes that start child Sessions. Auto-expiring Applications are created programmatically when a separate temporary credential boundary is needed, not for ordinary Session fan-out.\n\n## Applications Are the Credential Boundary; Sessions Are the Runtime Unit\n\nAn application is registered, holds a server-owned secret, and is the identity Caracal authenticates. A Session is started at runtime by the workload that already holds that secret; it carries parent, Subject authority record ID, labels, and delegation context. One application backs many Sessions, so a durable workload uses **one managed application** and starts, delegates, and fans out as many Sessions as it needs. You do not register an application per AI agent - see [Should I create one application per agent?](/reference/faq/#faq-006).\n\n## Durable and Auto-Expiring Applications\n\n| Kind          | Use it for                                                        | Rule                                                                                                   |\n| ------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |\n| Durable       | A service, orchestrator, Gateway, or agent runtime                 | Create once and reuse across many Sessions for the same workload.                                      |\n| Auto-expiring | An isolated credential boundary for a tenant or integration       | Create programmatically; it expires, binds to one task Session, and cannot participate in a child tree. |\n\nSessions use one of two lifecycles: a **task Session** ends with a bounded unit of work, while a **service Session** stays active through a heartbeat lease. Lifecycle describes time, not actor type or authority.\n\nA task Session may have a wall-clock TTL. A service Session ends when its heartbeat lease lapses or it is closed. Keeping the lease alive does not widen authority.\n\nTwo structural rules prevent invalid trees:\n\n- A `task` parent cannot start a `service` child. The protocol reports `task_session_cannot_start_service`. A `service` parent may start either lifecycle.\n- An auto-expiring Application's Session is a **leaf**: it cannot parent or be a child Session.\n\nA short-lived worker is an ordinary task Session with a TTL. Model an orchestrator and its workers under one durable Application unless they need separate credentials or mutual distrust boundaries.\n\n- the orchestrator is the top-level Session, or a [`start_session()`](/sdks/python/) handle when it needs a heartbeat lease,\n- each manager is a plain `session()` that inherits the application's authority,\n- each task worker is `session(authority=Authority.narrow([...]), ttl_seconds=…)` - least-privilege and auto-terminated on block exit, with the TTL sweeper as a backstop.\n\nUse an auto-expiring Application for credential isolation, not Session fan-out. It authenticates independently; it is not started as a child Session.\n\n:::note[FAQ]\n[What is the difference between an application, principal, and Session?](/reference/faq/#faq-005) and [when should I use a managed application versus DCR?](/reference/faq/#faq-007)\n:::\n\nPolicy and audit can distinguish Application kind, Session lifecycle, labels, parentage, and Delegation context.\n\n## Telling Sessions Apart\n\nEvery Session has one canonical Session ID. SDK context exposes it as `sessionId`, `session_id`, or `SessionID`. It is returned when `session()` or `startSession()` starts the Session and is stamped onto its token exchanges and audit events.\n\n`labels` are descriptors, not identity. Many Sessions under one application can intentionally share labels. Use labels, metadata,"},{"id":"/v0.2/concepts/resource-grant","title":"Resources and Grants","url":"https://docs.caracal.run/v0.2/concepts/resource-grant/","concepts":"","body":"Use this page to separate the protected target from the policy data used to evaluate access. A Resource is something Caracal protects. Grant data maps an Application role to scopes for that Resource.\n\n## Resources\n\nResources describe protected targets such as:\n\n- HTTP APIs behind the Gateway;\n- MCP servers and tool groups;\n- internal services protected by Express, FastMCP, or net/http adapters;\n- provider-backed targets that need credential mediation.\n\n| Resource field | Purpose |\n| --- | --- |\n| Identifier | Stable policy and token audience target. Always use the `resource://<slug>` convention, such as `resource://pipernet`; keep it stable even when the upstream URL changes. |\n| Upstream URL | Gateway forwarding target. |\n| Scopes | Named Caracal resource actions that policies and mandates can constrain. |\n| Gateway application | Managed application identity used by Gateway-mediated resources. |\n| Upstream credential provider | Resource binding to the provider record used when Gateway attaches no credential, a Caracal mandate, OAuth tokens, API keys, or bearer tokens. |\n\n:::note[FAQ]\n[What is the difference between a resource and a provider?](/reference/faq/#faq-009) and [why must the resource identifier stay stable?](/reference/faq/#faq-010)\n:::\n\n## Grants\n\nA grant data entry binds an Application key and role to a Resource and one or more scopes. It is not a Mandate and it is not a per-Subject permission assignment.\n\nGrants are not the final decision. They are one input to Policy. The active Policy set can still deny, require Approval, or constrain the exchange.\n\nCaracal also stores administrative records associated with Subjects for lifecycle and revocation workflows. Those records do not feed per-exchange scope decisions. Subject-specific upstream accounts are credential Provider connections, not Caracal authorization grants.\n\n## Exchange Relationship\n\n## Scope Design\n\nPrefer small, action-oriented scopes:\n\n| Good | Avoid |\n| --- | --- |\n| `pipernet:read` | `admin` |\n| `piperchat:comment` | `write_all` |\n| `nucleus:tool:call` | `tools` |\n\nUse Resource identifiers for targets and scopes for actions. Do not encode environment, tenant, or Subject identity into scope names when those belong in the Zone or decision context.\n\n:::note[FAQ]\n[How should I design scopes?](/reference/faq/#faq-011) and [do I manage grants directly?](/reference/faq/#faq-012)\n:::\n\n## Next Step\n\nRead [Providers](/concepts/provider/) to understand the credential Caracal attaches when it calls the upstream target.\n\n## Related Pages\n\n- [Define Resources and Providers](/guides/resources-providers/)\n- [Model Your Application in Caracal](/guides/modeling-recipes/)\n- [Debug Authorization Decisions](/guides/authorize-access/)"},{"id":"/v0.2/concepts/provider","title":"Providers","url":"https://docs.caracal.run/v0.2/concepts/provider/","concepts":"","body":"A credential Provider answers one question: after Caracal approves a call, what credential does the upstream target receive? Read [Resources and Grants](/concepts/resource-grant/) first. Every Gateway-routed Resource binds one Provider; one Provider can serve many Resources.\n\n## Why Providers Exist\n\nCallers never hold upstream credentials. The agent presents a Caracal mandate; Gateway verifies it, then attaches the upstream credential the provider describes. Provider secrets are sealed at creation and are not returned by list or detail APIs.\n\n## Auth Modes\n\n| Mode | Upstream receives | Use when |\n| --- | --- | --- |\n| None | No credential. | Gateway is the enforcement point and the upstream expects nothing. |\n| Caracal mandate | The Caracal mandate as a bearer token. | The upstream verifies Caracal tokens itself with a verifier or adapter. |\n| OAuth 2.0 authorization code | A consented upstream account's token. | The upstream needs delegated account consent. |\n| OAuth 2.0 client credentials | A service-to-service token. | The upstream uses machine-to-machine OAuth, via the standard grant or an RFC 7523 signed-assertion grant such as a Google service account. |\n| API key | A static key in a configured header. | The upstream uses vendor API keys. |\n| Bearer token | A static pre-issued token. | The upstream expects a fixed bearer credential Caracal does not mint. |\n| HTTP Basic | `Authorization: Basic` from a username and sealed password. | The upstream authenticates with a username/password or username/API-token pair. |\n\n## How a Provider Is Used\n\nGateway strips the caller's authorization and forwards the provider credential instead. For OAuth modes, STS obtains and refreshes the upstream tokens; delegated consent is stored as a provider connection - one authenticated upstream account per subject and provider, distinct from Caracal grants, which express authorization.\n\n## Common Mistakes\n\n- A resource says what is protected; its provider says how the upstream is authenticated. Keep credential detail on the provider and target detail on the resource.\n- Name providers with stable `provider://` identifiers, such as `provider://hooli-oidc`.\n- OAuth providers support a real connectivity check before creation. The other modes are validated at creation and exercised when a resource first uses them.\n- A Provider controls upstream authentication, not whether Caracal issues a Mandate.\n- A Subject's Provider connection identifies its upstream account; it does not grant Caracal scopes.\n\n:::note[FAQ]\n[What is the difference between a resource and a provider?](/reference/faq/#faq-009) and [is an application secret the same as a provider credential?](/reference/faq/#faq-013)\n:::\n\n## Next Step\n\nRead [Policies and Policy Sets](/concepts/policy/) to understand how requests against a Resource are allowed, denied, or held for Approval.\n\n## Related Pages\n\n- [Define Resources and Providers](/guides/resources-providers/)\n- [Provider Recipes](/guides/provider-recipes/)\n- [Resources and Grants](/concepts/resource-grant/)"},{"id":"/v0.2/concepts/policy","title":"Policies and Policy Sets","url":"https://docs.caracal.run/v0.2/concepts/policy/","concepts":"","body":"Use this page to understand what policy owners configure and what Caracal guarantees. Caracal owns the decision contract. You author versioned policy data describing Application bindings, Resource grants, confinement, restrictions, risk, and Approval tiers. One policy-set version is active per Zone.\n\n## Policy Objects\n\n| Object                    | Purpose                                             |\n| ------------------------- | --------------------------------------------------- |\n| Policy                    | Named policy data document with immutable versions. |\n| Policy version            | A specific content hash and schema version.         |\n| Policy set                | A named bundle of policy versions.                  |\n| Policy set version        | A specific manifest of policy versions.             |\n| Active policy set version | The version the STS evaluates for a zone.           |\n\n## Decision Contract\n\nThe decision contract is deny by default. You never replace its decision result; you supply the data it reads. This keeps structural checks, Delegation narrowing, and Approval behavior consistent across Zones.\n\n```text\n# caracal:data-document\npackage caracal.authz\n\nimport rego.v1\n\ngrants := {\n  \"resource://pipernet\": {\n    \"application\": \"son-of-anton\",\n    \"roles\": {\"operator\": [\"pipernet:read\"]},\n  },\n}\n```\n\nThe contract reads four adopter documents:\n\n| Document      | Shape                                                | Effect                                                                                                                           |\n| ------------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |\n| `app_ids`     | `{binding_key: application_id}`                      | Binds a readable Application key to its registered ID.                                                                           |\n| `grants`      | `{resource: {application, roles: {role: [scopes]}}}` | Declares which application owns a resource view and which scopes each role may hold.                                             |\n| `confinement` | `[{label_prefix, scopes}]`                           | Caps every Session with a matching label prefix to a fixed scope set. Optional; omitting it confines nothing.                   |\n| `restrict`    | set of reasons                                       | Deny overlay. Any entry denies every exchange in the zone. Optional; keep it empty to authorize normally.                        |\n\nA zone that supplies no data authorizes nothing: every allow rule collapses to the default deny.\n\n## Policy Input Contract\n\nEvery evaluation receives documented Application, Resource, action, Session, Delegation, requested-scope, optional Subject-claim, Approval, and trace context. Use [Author Policy Data](/guides/author-policy/) for the current field-level contract.\n\n| Context area | What policy can reason about |\n| --- | --- |\n| Application | Registered ID, kind, and Zone. |\n| Session | ID, task or service lifecycle, and labels. |\n| Resource and action | Stable Resource identifier, declared scopes, requested scopes, and Gateway HTTP operation when present. |\n| Delegation | Source, target, narrowed Resource and scopes, and verified chain context. |\n| Subject | Verified claims for structural binding and audit, not an independent scope grant. |\n| Approval and trace | Whether the matching Approval was resolved and the request trace ID. |\n\nVerified Subject claims are available for structural binding and audit. The built-in allow rules do not turn those claims into per-Subject scope grants. If a product needs user-specific business authorization, keep that in the product's own authorization system rather than claiming Caracal federation provides it.\n\n## Policy Outcomes\n\n| Outcome       | Effect                                                                     |\n| ------------- | -------------------------------------------------------------------------- |\n| `allow`       | STS signs a mandate if token and session checks also pass.                 |\n| `deny`        | STS refuses the exchange and records diagnostics.                          |\n| approval gate | STS holds the mint and returns `interaction_required` with a challenge ID. |\n\nApproval gates are declared as data: `risk` names a tier for each sensitive scope and `approval_tiers` declares which tiers require a human decision. When a mint requests a gated scope, STS creates a durable hold and releases the Mandate only after an authorized approver decides it. See [Approvals](/concepts/step-up/).\n\n## Authoring Rules\n\n- Author data documents only; mark each with `# caracal:data-document`. The platform decision contract owns every `result`.\n- Validate through the web console or Admin API before activation - a data document must define data and must not define `result`.\n- Activate through a policy set version, not by editing active content in place.\n- Keep `grants`, `app_ids`, `confinement`, and `restrict` in separate documents so ownership and review stay clear.\n- Tighten authority through `confinement` and `restrict`; neither can widen what the contract already allows.\n\n## Next Step\n\nRead [Approvals](/concepts/step-up/) to understand how policies hold sensitive exchanges for a human decision.\n\n## Related Pages\n\n- [Author Policy Data](/guides/author-policy/)\n- [Activate a Policy Set](/guides/activate-policy-set/)"},{"id":"/v0.2/concepts/step-up","title":"Approvals","url":"https://docs.caracal.run/v0.2/concepts/step-up/","concepts":"","body":"Read this page after [Policies and Policy Sets](/concepts/policy/). An Approval pauses a sensitive authority request until an eligible human decides it. Policy data maps scopes to risk tiers and identifies whether an operator, the federated Subject, or either may decide.\n\nApproval is an optional security primitive. A zone that declares no `approval_tiers` data never creates a hold, and nothing in the platform requires one.\n\n## Approval Flow\n\n## Two Decision Planes\n\n| Plane    | Approver                                                  | Surface                                                                                                 |\n| -------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |\n| Operator | A control-plane admin holding an `approve`-capable token. | Console Approvals page or `POST /v1/zones/{zone}/step-up-challenges/{id}/approve` / `/reject`.          |\n| Subject  | The requesting application's own authenticated end user.  | `POST /step-up/{id}/decision` on the STS, requiring a user-type session mandate and the hold's binding. |\n\nThe tier's `approver` declaration picks the plane: `operator`, `subject`, or `any`. On the operator plane, approval authority is a distinct admin capability - a `write` token cannot decide a hold. An `any` hold admits either plane, so operators can always decide it.\n\nA Subject-only Approval requires the Application to federate its end user through a registered Subject issuer. Without that federation, the Approval can only expire. This is a decision mechanism, not per-Subject Resource authorization: the original Application, policy, scopes, and Delegation still bound the resulting Mandate.\n\n## Components\n\n| Component            | Responsibility                                                                                                                         |\n| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |\n| Policy data          | Declares `risk` tiers per scope and `approval_tiers` gates; the platform fixes no tier taxonomy.                                       |\n| STS                  | Creates the hold, serves its state to long-polling agents, records decisions, verifies the binding, and consumes the approval at mint. |\n| Console or Admin API | Lists, inspects, and decides operator-plane holds.                                                                                     |\n| Application          | Relays subject-plane holds to its own federated end user and posts the decision with the user's session mandate.                       |\n| SDK or OAuth client  | Surfaces `interaction_required` and waits on the hold (`waitForApproval`); `caracal run` parks and retries automatically.              |\n\n## Challenge Lifecycle\n\n| State      | Meaning                                                                                  |\n| ---------- | ---------------------------------------------------------------------------------------- |\n| `pending`  | The hold is live and awaiting a decision.                                                |\n| `approved` | An approver granted the hold; the next matching exchange mints.                          |\n| `rejected` | An approver refused the hold; terminal.                                                  |\n| `expired`  | The approval window closed without a decision, or an approval lapsed before consumption. |\n| `consumed` | The approval released its one mandate; terminal.                                         |\n\nAn Approval releases at most one Mandate. Its binding covers the Application, Authority record, Session, Delegation, Resource, scopes, and active policy version. A policy rollout or execution-context change therefore requires a fresh decision. Consumed and rejected are terminal.\n\n## Privacy Modes\n\nThe tier's `privacy` declaration controls what the decision record retains of a Subject-plane approver: `identified` stores the Subject verbatim, `pseudonymous` a stable zone-scoped pseudonym, and `anonymous` a redaction marker. The approver's Authority record ID is always kept as the forensic and revocation anchor. Operator-plane decisions always record the deciding admin identity. Caracal stores authorization facts, never business context.\n\n## Design Guidance\n\n- Gate high-risk scopes, not everything: approval latency is a person, so reserve it for authority worth a pause.\n- Keep the decision outside policy; policy declares that a decision is needed, never performs it.\n- Use `subject` tiers when the risk belongs to the application's end user and the application federates its users through a zone subject issuer; use `operator` tiers when the risk belongs to the zone.\n- Give automation credentials `write` without `approve`, so no pipeline can silently settle a hold.\n- Cross-check the binding: the agent prints it beside the challenge id, and the Console shows it on the hold.\n\n## Next Step\n\nRead [Session Delegation](/concepts/delegation/) to understand how approved authority can be narrowed for another Session.\n\n## Related Pages\n\n- [Human Approval](/guides/step-up/)\n- [Policies and Policy Sets](/concepts/policy/)\n- [Audit and Request Traces](/concepts/audit-ledger/)"},{"id":"/v0.2/concepts/mandate","title":"Mandates","url":"https://docs.caracal.run/v0.2/concepts/mandate/","concepts":"","body":"Read this page after [Policies and Policy Sets](/concepts/policy/). A Mandate is the short-lived signed proof Caracal issues after policy allows a request. The Gateway or a verified service checks it before the action runs.\n\n## What a Mandate Proves\n\nA valid mandate proves:\n\n- which zone issued it;\n- which Application is acting and which optional Subject context is present;\n- which session anchors are active;\n- which resource targets and scopes were approved;\n- which Authority record, Session, and Delegation supplied the authority;\n- when the authority expires.\n\n## Issuance Path\n\n## Mandate Use\n\n| Use                | Verification focus                                                             |\n| ------------------ | ------------------------------------------------------------------------------ |\n| Gateway request    | Issuer, audience, zone, resource, scopes, expiry, revocation.                  |\n| MCP tool call      | Bearer token, required scopes, required targets, Session and Delegation constraints. |\n| SDK outbound call  | Context propagation and mandate header injection.                              |\n| Delegated exchange | Session, Delegation, scopes, hop count, and constraints.                       |\n\n## Mandates Are Not Credentials to Store\n\nMandates are intentionally short lived and context bound. Do not store them as durable credentials, copy them into configuration files, or reuse them across unrelated Resources.\n\nResource servers should always verify a mandate at request time. Verification includes signature and claim checks plus revocation checks for the Authority record ID, Root authority record ID, Session ID, and Delegation ID when those claims are present. See the [parsed claim mapping](/sdks/identity/#parsed-claim-names) for language-level and raw JWT names.\n\n## Failure Modes\n\n| Failure                   | Meaning                                                                   |\n| ------------------------- | ------------------------------------------------------------------------- |\n| `invalid_token`           | Signature, issuer, audience, required claim, or expiry validation failed. |\n| `scope_insufficient`      | The mandate does not contain a required scope.                            |\n| `session_revoked`         | One of the mandate revocation anchors has been revoked.                   |\n| `session_required`        | The resource requires authority from a governed Session.                  |\n| `delegation_required`     | The resource requires delegated authority.                                |\n| `chain_mismatch`          | The delegation chain does not include the required application.           |\n| `hop_count_exceeded`      | The delegation path exceeds the configured hop limit.                     |\n\n## Next Step\n\nRead [Session Delegation](/concepts/delegation/) to understand how Sessions pass bounded authority.\n\n## Related Pages\n\n- [Sessions and Revocation](/concepts/sessions-revocation/)\n- [Protect an MCP Server](/guides/protect-mcp/)\n- [Run an Agent with caracal run](/guides/runtime-run/)"},{"id":"/v0.2/concepts/delegation","title":"Session Delegation","url":"https://docs.caracal.run/v0.2/concepts/delegation/","concepts":"","body":"Read this page when one Session must give a child or peer less authority than the Application could otherwise request. A Delegation is a revocable, expiring authority relationship between Sessions.\n\nAuthority follows the **application**. Sessions started under the same application already act under that application's authority. Create a Delegation in exactly two cases:\n\n- To **narrow** authority, so a child holds only a subset of what its parent can do (least privilege).\n- To carry authority **across applications**, when a receiving Session belongs to a different application and explicitly presents the offered Delegation ID.\n\nIt is represented as a graph of directed edges. Each edge connects a source session to a target session, carries scopes and constraints, and can be revoked independently.\n\n## Graph Model\n\n## Delegation Fields\n\n| Field                | Purpose                                                          |\n| -------------------- | ---------------------------------------------------------------- |\n| Source session       | The session that delegates authority.                            |\n| Target session       | The child or receiving Session.                                  |\n| Issuer application   | Application creating the delegation.                             |\n| Receiver application | Application receiving authority.                                 |\n| Resource             | Optional resource boundary for the edge.                         |\n| Scopes               | Subset of authority being delegated.                             |\n| Constraints          | Typed resource, scope, TTL, and hop limits plus audit metadata. |\n| Status               | Active, expired, or revoked lifecycle state.                     |\n\n## Rules\n\n- Delegation should narrow authority, not expand it.\n- Every Delegation must have a positive TTL; unbounded edges are rejected before creation.\n- Delegation paths must not cycle.\n- Hop count should be bounded.\n- Revoking an upstream Delegation should invalidate downstream authority.\n- Resource servers should verify delegation claims when they require delegated access.\n\nExpiry removes the edge's authority without terminating either endpoint Session. STS rejects the expired edge and caps every mandate minted through it so the mandate cannot outlive the edge. The target Session remains active but cannot use that Delegation again; it must receive another live Delegation before it can mint delegated authority. Revocation is different: it is an explicit monotonic action that cascades through downstream Delegations and affected Session subtrees.\n\n## What Delegation Bounds\n\nA Delegation bounds every Mandate issued through it and every later Delegation chained from it. It never raises the Application's policy ceiling.\n\n- A narrowed child receives no more scopes, Resources, lifetime, or hop budget than its parent.\n- Inheriting from a narrowed parent preserves that narrowing for descendants.\n- Inheriting from an Application-root Session does not manufacture delegated Resource authority.\n\nWorked example: A starts B with `Authority.narrow([tickets:read])`, then B starts C.\n\n- If B starts C with **inherit**, Coordinator records a `B → C` edge mirroring B's `tickets:read` slice, so C remains bounded by B's narrowing.\n- If B starts C with narrower authority, Coordinator records a `B → C` edge and rejects it unless `C ⊆ B`.\n\nThe Application and active policy remain the hard ceiling. Use a separate Application when the receiver needs a separate credential or trust boundary. A cross-Application receiver must explicitly accept the offered Delegation before using it.\n\n## SDK Relationship\n\nThe SDKs expose one primitive for creating children, one for granting a peer, and one for presenting a received grant:\n\n| Language   | Start a child                            | Delegate to an existing peer | Present a received Delegation |\n| ---------- | ---------------------------------------- | ---------------------------- | ----------------------------- |\n| TypeScript | `session()` / `session({ authority })`   | `delegate()` / `revokeDelegation()` | `acceptDelegation()` |\n| Python     | `session()` / `session(authority=…)`     | `delegate()` / `revoke_delegation()` | `accept_delegation()` |\n| Go         | `Session()` / `Session` with `Authority` | `Delegate()` / `RevokeDelegation()` | `AcceptDelegation()` |\n\n`session()` returns a child running under the **same application's** authority; it never moves the child into another application. Pass `Authority.narrow(...)` to bind a least-privilege Delegation or `Authority.none()` for no inherited authority. To hand authority across applications, use `delegate()` with an existing peer Session. The issuer's context is unchanged, and the receiver presents the Delegation with `acceptDelegation()`.\n\nThese helpers propagate session and delegation context so later token exchanges include the correct graph proof.\n\n## Where You Interact With Delegation\n\nCreate Delegations at runtime through the SDK. The console is an inspection and revocation surface, not a Delegation authoring form. Audit records the Delegation ID, chain, scopes, and outcome.\n\n## Next Step\n\nRead [Delegation Constraints](/concepts/constraint/) to understand the limits carried by each edge.\n\n## Related Pages\n\n- [Implement Multi-Agent Delegation](/guides/delegation/)\n- [Audit and Request Traces](/concepts/audit-ledger/)"},{"id":"/v0.2/concepts/constraint","title":"Delegation Constraints","url":"https://docs.caracal.run/v0.2/concepts/constraint/","concepts":"","body":"Read [Session Delegation](/concepts/delegation/) first. Constraints state how far, how long, and for what a Delegation may be used. Caracal validates them when the Delegation is created and again before issuing a Mandate through it.\n\n## Constraint Types\n\n| Constraint       | Use it to limit                                             |\n| ---------------- | ----------------------------------------------------------- |\n| Resource         | Which protected target can be reached.                      |\n| Scopes           | Which actions can be requested.                             |\n| TTL              | How long the delegated edge remains useful.                 |\n| Hop count        | How deep the delegation chain may become.                   |\n| Approval metadata | Audit/display annotation; it does not authorize the edge.    |\n| Broad reason      | Audit/display note for an elevated resource-unbounded edge.  |\n\n## Example Shape\n\n```json\n{\n  \"resource\": \"resource://piperchat\",\n  \"scopes\": [\"piperchat:read\", \"piperchat:comment\"],\n  \"max_hops\": 2,\n  \"expires_at\": \"2026-06-01T12:00:00Z\",\n  \"policy_approved\": true\n}\n```\n\nDelegation constraints bound authority, lifetime, and propagation. They are not quotas. Use a domain-specific store when calls, work, or cost must be consumed atomically. Approval metadata and broad-reason fields are audit annotations; they do not authorize the Delegation.\n\nApplication-chain requirements are verifier options rather than edge constraints. Use `requireChainContains` at a resource boundary when a specific application must appear in the signed delegation chain.\n\n## Where Constraints Are Enforced\n\n## Design Guidance\n\n- Put durable business rules in policy data documents.\n- Put per-edge runtime limits in constraints.\n- Prefer positive allowlists over open-ended deny lists.\n- Keep constraints small enough to review in audit traces.\n- Use consistent field names across agents so policies stay readable.\n\n## Next Step\n\nRead [Sessions and Revocation](/concepts/sessions-revocation/) to understand how active authority ends.\n\n## Related Pages\n\n- [Session Delegation](/concepts/delegation/)\n- [Policies and Policy Sets](/concepts/policy/)\n- [Implement Multi-Agent Delegation](/guides/delegation/)"},{"id":"/v0.2/concepts/sessions-revocation","title":"Sessions and Revocation","url":"https://docs.caracal.run/v0.2/concepts/sessions-revocation/","concepts":"","body":"Read this page after [Delegation Constraints](/concepts/constraint/). Authority records, Sessions, and Delegations make authority temporary and revocable. A Mandate refers to these records so the Gateway or verified service can reject authority that has ended.\n\n## Identity and Execution Records\n\n| Record                | Role                                                                                                                                                            |\n| --------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Subject               | Opaque JWT `sub` identity. The Console's **Subjects** page groups Authority records by this value.                                                              |\n| Authority record      | Immutable record created by an identity or authority exchange; an audit and revocation anchor.                                                                  |\n| Root Authority record | First record in an authority ancestry; revoking it can end the descendant authority chain.                                                                      |\n| Session               | Governed execution. A delegated Session is still a Session, with an inbound Delegation.                                                                          |\n\n## Subject Identity Is Federated\n\nCaracal does not generate Subjects, and **Subjects** is not a login surface. The `sub` on an Authority record is taken verbatim from the token the application exchanges. To federate end users, a zone registers the identity system as a Subject issuer and the application exchanges each identity token for an Authority record with no resource authority.\n\nA Session may attach the federated Subject's Authority record only while presenting proof of control. That Subject remains immutable attribution and a revocation anchor for the Session. It does not automatically replace the Application identity on later Resource Mandates and does not create per-Subject scopes.\n\n## Revocation Anchors\n\nResource servers check every relevant anchor: Authority record ID, Root authority record ID, Session ID, and Delegation ID. The [parsed claim mapping](/sdks/identity/#parsed-claim-names) lists the canonical language-level names and raw JWT fields.\n\nIf any anchor is revoked, the mandate should be rejected as `session_revoked`.\n\n## Revocation Flow\n\nSuspension is reversible Session state, not permanent revocation. Gateway-routed requests perform a fresh STS exchange and reject a suspended Session immediately through authoritative Session validation. Already issued mandates checked directly by a resource verifier can remain usable until their mandate TTL expires; keep mandate TTLs within the documented 15-minute cap when suspension latency matters. Termination and Delegation revocation remain monotonic revocation events.\n\n## Cascade Behavior\n\nRevocation should follow authority:\n\n- revoking an Authority record invalidates authority descended from it;\n- revoking a Session invalidates its child Delegations;\n- revoking a Delegation invalidates downstream delegated authority;\n- revoking a grant prevents future exchange and can invalidate active Authority records and Sessions depending on workflow.\n\n## Resource-Server Responsibility\n\nThe Gateway and adapters must be configured with a revocation store. For development, an in-memory store can be useful. For production, use a shared store and stream consumer so revocations propagate across resource-server instances.\n\n## Next Step\n\nRead [Audit and Request Traces](/concepts/audit-ledger/) to understand how decisions and requests are explained.\n\n## Related Pages\n\n- [Mandates](/concepts/mandate/)\n- [Protect an MCP Server](/guides/protect-mcp/)\n- [Tail and Query the Audit Stream](/guides/audit-stream/)"},{"id":"/v0.2/concepts/audit-ledger","title":"Audit and Request Traces","url":"https://docs.caracal.run/v0.2/concepts/audit-ledger/","concepts":"","body":"Use this page when you need to explain one decision or prove one action. Audit records what Caracal decided, the context used, and which request or Session produced the event.\n\n## What Gets Audited\n\n| Event area              | Examples                                                              |\n| ----------------------- | --------------------------------------------------------------------- |\n| Token exchange          | Allow, deny, Approval required, and policy diagnostics.               |\n| Gateway and adapter use | Resource decision, mandate verification failure, request correlation. |\n| Policy lifecycle        | Policy creation, validation, policy-set activation, simulation.       |\n| Delegation              | Creation, traversal, impact, and revocation cascade.                  |\n| Sessions                | Start, terminate, revoke, expire.                                     |\n| Administration          | Zone, application, resource, provider, grant, and challenge changes.  |\n\n## Evidence Flow\n\n## How to Use Audit\n\n| Question                                      | Where to look                                                        |\n| --------------------------------------------- | -------------------------------------------------------------------- |\n| Why was a request denied?                     | Console `request trace` or Admin API explain endpoint by request ID. |\n| Which policy caused the decision?             | Determining policies and diagnostics.                                |\n| Did revocation propagate?                     | Session, delegation, and resource decision events.                   |\n| Which run made a request?                     | Request ID, Authority record ID, Session ID, and trace context.      |\n| Was an Approval required, and who decided it? | Approval issuance, decision, and consumption events.                |\n\n## Request IDs\n\nRequest IDs tie multiple events together. Keep the request ID from an SDK, Gateway, STS error, or Console trace whenever debugging. The explain view uses it to collect related decision events and diagnostics.\n\n## Integrity and Retention\n\nAudit events should be treated as operational evidence. Configure retention, export, and SIEM forwarding according to your deployment requirements. Do not rely on local process logs as the only authority trail.\n\n## Outcome\n\nFor one request ID, you should be able to identify the Application, optional Subject attribution, Session, Resource, scopes, policy version, Approval or Delegation context, decision, and enforcement result.\n\n## Next Step\n\nUse [Guides](/guides/) when you are ready to apply the model.\n\n## Related Pages\n\n- [Tail and Query the Audit Stream](/guides/audit-stream/)\n- [Trace One Protected Request](/tutorials/inspect-a-run/)\n- [Operations](/operations/)"},{"id":"/v0.2/concepts/operator","title":"Caracal Operator","url":"https://docs.caracal.run/v0.2/concepts/operator/","concepts":"","body":"Read this page only if you plan to use the optional Caracal Operator in the web console. It turns natural-language intent into a previewed plan and applies that plan through the same guarded APIs available to the human operator. It introduces no new authority.\n\n## Why It Exists\n\nMost control-plane work is a sequence of small, related changes: register an application, connect a provider, define a resource and its scopes, then activate the policy that ties them together. The Operator collapses that into a described outcome while keeping every safety property - validation, least privilege, approval, and audit - in the platform rather than the model.\n\nThe Operator works against a **capability catalog**: the set of control-plane actions it can take, grouped by the objects you operate - zones, applications, providers, resources, access, and policy. Each capability is classified as read-only or state-changing, so a request that only inspects state is always distinguishable from one that changes it.\n\n## The Governed Lifecycle\n\nA change never applies directly from natural language. Within a session the Operator follows a fixed lifecycle, and the language model only ever produces a draft that enters it:\n\n1. **Propose.** Intent becomes a plan whose every step is validated against the capability catalog. A step that names an unknown action or invalid arguments is rejected before anything runs.\n2. **Preview.** The plan is resolved against your live state as a read-only dry run, so each step is marked as a create, an update, a no-op, or blocked when a referenced object is missing. Nothing is written.\n3. **Decide.** You approve or reject the plan. A plan is decided once, and only an approved plan is eligible to apply.\n4. **Apply.** An approved plan is re-validated and re-previewed, then executed step by step. A plan applies only once, and any secret it produces is surfaced in the apply response, never written to the conversation or the audit log; issued credentials stay retrievable from Secret Store custody through the owning object's audited reveal.\n\n## Authority and Isolation\n\nThe Operator runs as a reserved Application, distinct from the human operator who approves a plan. Audit records both the human decision and the Operator Application that executes the change.\n\nThat delegated authority is least-privilege: the Operator may execute only the capabilities explicitly granted to it. A plan that asks for a capability outside its grant is refused as forbidden, before execution. The Operator is also bounded by zone isolation - it will not open a session in, or execute against, a [system zone](/concepts/zone/#system-zone), keeping its authority away from the infrastructure that runs Caracal itself.\n\n## Ask and Agent Modes\n\nEvery conversation runs in one of two modes, enforced by Caracal and never chosen by the model:\n\n| Mode | What the Operator can do |\n| --- | --- |\n| Agent | Answer questions, read state, and propose plans that apply changes after your approval. |\n| Ask | Strictly read-only: explain, investigate, and diagnose. It never produces a plan and cannot apply anything. |\n\nAsk mode is enforced in two independent places - the planning skill is never selected, and the change endpoints refuse outright - so a read-only conversation is provably write-incapable.\n\n## Autopilot\n\nIn agent mode you can engage **autopilot**, which lets Caracal auto-satisfy the approval step for every plan in the conversation. Engaging it is an explicit opt-in to acting without a human in the loop; it is off by default and per conversation, and a platform-level master switch must also be on. Auto-approval never widens authority - the governed execute path still enforces the capability allowlist, the least-privilege executor token, and zone isolation on every apply. Autopilot defers while a plan still needs credentials from the console's secure prompt and stops when a preview shows the plan cannot apply. A deployment can also bound an engaged conversation with a **write budget**: once the cumulative auto-approved write operations would exceed it, autopilot pauses, records the pause in the conversation ledger, and the plan waits for explicit human approval. The master switch is a single kill switch that stops all auto-approval on the next turn.\n\n## Authoring Policy\n\nPolicy is the densest control-plane object to write by hand: a decision rests on grant, binding, and confinement data that must parse as valid Rego and satisfy the platform decision contract. The Operator includes a dedicated policy author for exactly this. Describe the access you want - which application owns a resource, which roles hold which scopes, how to confine a label - and it drafts the matching [data documents](/concepts/policy/), explains each one, and reports the least-privilege posture, the risks it detected, ready-to-run simulations, and activation readiness.\n\nEvery draft is validated and previewed against the same contract the platform enforces, so a document the Operator emits is already contract-valid; if it cannot produce a valid document it fails closed rather than returning broken Rego. A draft is not a change. Turning one into a policy runs the ordinary [governed lifecycle](#the-governed-lifecycle) - you review the proposed create, approve it, and the create is re-validated on apply and attributed to you. Policies authored this way carry provenance marking them AI-assisted, and their later version, simulation, and activation steps stay under the same review and audit as any other policy work.\n\n## Natural-Language Provider\n\nTurning words into a plan requires a model endpoint supplied by the operator. Open **Settings → AI Operator → Models** to add an OpenAI chat-completions-compatible endpoint, model IDs, optional context window, and key placement. The API key is accepted only when the provider is created or rotated, sealed into a credential Provider in the reserved `caracal.sys` Zone, and never returned to the browser. Operator model calls use t"},{"id":"/v0.2/operations","title":"Operate Caracal","url":"https://docs.caracal.run/v0.2/operations/","concepts":"","body":"Use this section when you own a Caracal deployment. These runbooks describe behavior present in this repository; they do not promise an availability level, managed service, or compliance outcome.\n\n## Choose a Runbook\n\n| Need | Start here |\n| --- | --- |\n| Choose a substrate | [Choose a Deployment Profile](/operations/deployment-profiles/) |\n| Run one Docker host | [Deploy with Docker Compose](/operations/docker-compose/) |\n| Deploy the repository chart | [Deploy with Helm](/operations/kubernetes-helm/) |\n| Diagnose a failed request | [Troubleshoot by Symptom](/operations/troubleshooting/) |\n| Diagnose unhealthy infrastructure | [Debug Infrastructure Issues](/operations/debugging/) |\n| Recover a known failure | [Recover from Failures](/operations/failure-modes/) |\n| Back up or restore Compose state | [Back Up and Retain Data](/operations/backup-retention/) |\n| Change versions | [Upgrade Caracal](/operations/upgrade/) |\n\n## Operating Invariants\n\n- Postgres is the durable system of record. Redis carries streams and correctness-critical revocation state.\n- `dev` is local-development posture. `rc` and `stable` enforce published-mode configuration; `rc` is not a production stability claim.\n- Compose publishes service ports on loopback. Expose them only through an operator-owned TLS proxy.\n- Helm and OpenTofu are deployment assets, not a managed Kubernetes, HA, backup, or SLO service.\n- Runtime lifecycle belongs to `caracal up`, `down`, `status`, `upgrade`, and `purge`. Product administration belongs to the web console, Admin SDK, or Control API.\n\n## Baseline Verification\n\nFor Compose, run `caracal status --ready`. For Kubernetes, inspect Jobs and pod readiness, then test required service endpoints. `/health` proves liveness; only `/ready` is a traffic gate.\n\n## Recovery Boundary\n\nBefore a risky change, retain the current version, configuration, data backup, and secrets backup. Database migrations are forward-only; application rollback is safe only when the older version accepts the migrated schema.\n\n## Next Action\n\nChoose the substrate in [Choose a Deployment Profile](/operations/deployment-profiles/)."},{"id":"/v0.2/operations/deployment-profiles","title":"Choose a Deployment Profile","url":"https://docs.caracal.run/v0.2/operations/deployment-profiles/","concepts":"","body":"Choose a profile from requirements you can verify. Caracal ships deployment mechanics; it does not certify capacity, high availability, multi-region operation, or a cloud service.\n\n## Decision\n\n| Requirement | Use | Do not infer |\n| --- | --- | --- |\n| Local development from source | `caracal up` with development Compose | Production hardening |\n| One Docker host with bundled stores | Installed runtime and packaged Compose | Host redundancy or zero downtime |\n| Kubernetes 1.30+ with operator dependencies | Helm chart | A tested SLO or managed stores |\n| Declarative chart installation | `caracalStack` OpenTofu module | Resources beyond namespace, optional Secret, and Helm release |\n| Provider-neutral VM bootstrap | `caracalHost` OpenTofu module | VM, firewall, TLS, backup, or monitoring creation |\n\n## Prerequisites\n\nDefine ingress, recovery objectives, storage ownership, secret delivery, monitoring, and maintenance policy. If availability matters, prove it in your environment; replicas, PDBs, HPAs, and atomic upgrades are mechanisms, not guarantees.\n\n## Safe Procedure\n\n1. Use `dev` only on a local development host.\n2. Pin a release and use `stable` for production evaluation.\n3. Keep Compose ports loopback-bound; add an operator-owned TLS proxy for remote access.\n4. For Helm, provide Postgres, Redis, runtime Secret, ingress, and network egress explicitly.\n5. Establish backup, restore, metrics, alerts, and incident ownership before production traffic.\n\n## Verify\n\nConfirm assets render, secrets resolve, migrations finish, and `/ready` passes. Run a canary token exchange and Gateway request, then locate its audit evidence.\n\n## Rollback or Recovery\n\nKeep the prior release and values. Restore data only from a tested backup and restore secrets separately. Helm rollback never reverses database migrations.\n\n## Next Action\n\nUse [Deploy with Docker Compose](/operations/docker-compose/) or [Deploy with Helm](/operations/kubernetes-helm/)."},{"id":"/v0.2/operations/docker-compose","title":"Deploy with Docker Compose","url":"https://docs.caracal.run/v0.2/operations/docker-compose/","concepts":"","body":"Use Compose for local development or one Docker host. It is not a multi-host or HA deployment.\n\n## Prerequisites\n\n- Docker with Compose support.\n- For repository development, installed workspace dependencies.\n- For a released runtime, an installed Caracal binary and writable `CARACAL_HOME`.\n- Host backup and TLS exposure plans before production use.\n\n## Procedure\n\n```bash\ncaracal up\ncaracal status --ready\n```\n\nDevelopment builds `infra/docker/docker-compose.yml`; installed `rc` and `stable` runtimes use the embedded release topology. API `3000`, STS `8080`, Gateway `8081`, Audit `9090`, Coordinator `4000`, and web `3001` bind to `127.0.0.1`. The web container listens internally on `3002`.\n\n`caracal up` creates missing managed secret files and preserves non-empty values. Keep `$CARACAL_HOME/secrets` or `CARACAL_SECRETS_DIR` outside source and agent workspaces. Put overrides in `$CARACAL_HOME/caracal.env`; do not edit the installed compose file.\n\n## Verify\n\n`caracal status` checks `/health`; `caracal status --ready` checks dependencies. From a checkout, `bash infra/scripts/smokeTest.sh` probes the five core services on loopback. Open `http://localhost:3001` only after readiness passes.\n\n## Stop and Recover\n\nUse `caracal down` to stop services while retaining volumes.\n\n:::danger[Data loss]\n`caracal down -v` and purge operations can remove state. Do not use them until Postgres, Redis, replay state, and separately stored secrets have recoverable backups.\n:::\n\nIf startup fails, preserve volumes and secrets, inspect `caracal status --json` and container logs, then use [Debug Infrastructure Issues](/operations/debugging/).\n\n## Next Action\n\nReview [Configure Service Environment](/operations/env-vars/) before exposing an endpoint."},{"id":"/v0.2/operations/kubernetes-helm","title":"Deploy with Helm","url":"https://docs.caracal.run/v0.2/operations/kubernetes-helm/","concepts":"","body":"Use the chart when you already operate Kubernetes, Postgres, Redis, secrets, ingress, and monitoring. It does not provision a cluster or managed dependencies and makes no availability claim.\n\n## Prerequisites\n\n- Kubernetes `>=1.30`, Helm, and namespace privileges.\n- A pinned chart version or reviewed checkout.\n- External Postgres and Redis for `stable` mode.\n- A complete runtime Secret; production values set `secrets.create=false`.\n- Explicit DNS, ingress-controller, dependency, provider, and object-store network rules.\n\n## Safe Procedure\n\n1. Create an environment-owned values file from `values.production.yaml`.\n2. Set the image tag, service URLs, `services.web.publicUrl`, storage hosts, Secret name, and only required ingress.\n3. Render before apply:\n\n```bash\nhelm lint infra/helm/caracal\nhelm template caracal infra/helm/caracal --namespace caracal --values <environment-values> > caracal.rendered.yaml\n```\n\n4. Review Secret references, NetworkPolicies, Ingresses, migration Jobs, image tags, PVCs, and security contexts.\n5. Install:\n\n```bash\nhelm upgrade --install caracal infra/helm/caracal --namespace caracal --create-namespace --atomic --wait --wait-for-jobs --values <environment-values>\n```\n\nSTS and Gateway render as StatefulSets when replay persistence is enabled. PDBs, HPAs, ServiceMonitor, and PrometheusRule require compatible controllers and do not prove HA.\n\n## Verify\n\nCheck migration Jobs, pods, services, and rollout status. Confirm `/ready`, run a canary token exchange and Gateway call, and locate audit evidence. Verify the scraper authenticates with `METRICS_BEARER`.\n\n## Rollback and Recovery\n\n```bash\nhelm -n caracal history caracal\nhelm -n caracal rollback caracal <revision>\n```\n\nRollback does not reverse Postgres migrations. Verify schema compatibility or roll forward. Preserve replay PVCs and the runtime Secret.\n\n## Next Action\n\nUse [Choose a Cloud Profile](/operations/cloud-native-profiles/) to integrate operator-owned dependencies."},{"id":"/v0.2/operations/cloud-native-profiles","title":"Choose a Cloud Profile","url":"https://docs.caracal.run/v0.2/operations/cloud-native-profiles/","concepts":"","body":"Caracal does not ship provider resources or a managed-cloud service. Use this page to connect services you operate to standard Helm interfaces.\n\n## Use Criteria\n\nProceed only when the platform provides Kubernetes, TLS ingress, Postgres, Redis with Streams, secret projection, replay storage, and metrics collection. Provider product names are examples, not certifications.\n\n## Dependency Contract\n\n| Dependency | Required behavior |\n| --- | --- |\n| Postgres | TLS connection, migration privileges, capacity, and operator-owned backup/restore |\n| Redis | Streams, authentication, persistence, `noeviction`, and observable pending entries |\n| Secrets | Complete runtime Secret; no plaintext in Git or plans |\n| Ingress | HTTPS, exact origins and paths, explicit NetworkPolicy ingress |\n| Monitoring | Authenticated metrics scraping and actionable alert routing |\n\n## Procedure\n\n1. Provision dependencies with platform tooling.\n2. Project runtime credentials into the namespace.\n3. Set external hosts and internal URLs in environment values.\n4. Allow only required DNS, storage, identity-provider, upstream, and export egress.\n5. Render and review the chart before installation.\n\n## Verify and Recover\n\nConfirm secret synchronization, TLS, network reachability, migrations, `/ready`, metrics auth, canary authorization, revocation, and audit capture. Test provider-native backup restore. Retain the prior values and release; never rely on provider marketing or chart defaults for recovery behavior.\n\n## Next Action\n\nUse [Deploy on Managed Kubernetes](/operations/cloud-reference-deployments/) for the repository examples and their limits."},{"id":"/v0.2/operations/cloud-reference-deployments","title":"Deploy on Managed Kubernetes","url":"https://docs.caracal.run/v0.2/operations/cloud-reference-deployments/","concepts":"","body":"Files under `infra/helm/caracal/examples` demonstrate External Secrets Operator and Helm interfaces. They are templates, not complete or certified AWS, Azure, or Google Cloud deployments.\n\n## Prerequisites\n\nOperate Kubernetes 1.30+, Postgres, Redis with Streams, External Secrets Operator, a TLS issuer, ingress controller, and Prometheus Operator when using chart monitoring resources.\n\n## Procedure\n\n1. Adapt one `external-secrets/secretstore-*.yaml`; replace every identity and store reference.\n2. Store every value referenced by `externalsecret-runtime.yaml` in the provider secret manager.\n3. Apply the adapted SecretStore and ExternalSecret.\n4. Copy `values.cloud-managed.yaml`; replace hosts, domains, storage class, ingress class, issuer, and egress selectors.\n5. Render. Reject plaintext Secret data, default hosts, broad ingress, and unintended public API, Gateway, or STS endpoints.\n6. Install the reviewed overlay.\n\n## Verify\n\nConfirm Secret synchronization, migration Jobs, pod readiness, HTTPS origins, STS issuer, NetworkPolicy paths, metrics collection, alerts, and a canary protected call with audit evidence.\n\n## Rollback and Recovery\n\nRetain the previous Helm revision and values. Roll back only when schema-compatible. Use provider-native storage recovery and restore runtime secrets separately; examples do not create or test backups.\n\n## Next Action\n\nApply [Harden Production](/operations/tls-hardening/) before admitting traffic."},{"id":"/v0.2/operations/opentofu","title":"Provision with OpenTofu","url":"https://docs.caracal.run/v0.2/operations/opentofu/","concepts":"","body":"`caracalHost` renders cloud-init; it does not create a VM. `caracalStack` creates a namespace, optionally a Secret, and a Helm release; it does not create a cluster, Postgres, Redis, ingress controller, or secret manager.\n\n## Prerequisites\n\nUse OpenTofu `>=1.8`. Kubernetes also requires version `>=1.30`, kubeconfig access, external dependencies, and a complete runtime Secret.\n\n## Kubernetes Procedure\n\n1. Copy the production tfvars example outside source control.\n2. Pin `chartVersion`, set database and Redis hosts, and deliver `caracal-runtime` before apply.\n3. Add reviewed chart overlays through `extraValues`.\n4. Run:\n\n```bash\ncd infra/tofu/envs/production\ntofu init\ntofu plan -out caracal.plan\ntofu apply caracal.plan\n```\n\nThe module waits for Jobs and uses atomic Helm behavior by default. Remote state and locking are operator responsibilities; never put plaintext runtime secrets in variables or state.\n\n## VM Procedure\n\nAttach `caracalHost.userData` or `userDataBase64` to an operator-owned VM. It installs Docker when absent, installs a pinned release, writes non-secret overrides, and starts Caracal. Runtime secrets are generated on the host.\n\n## Verify and Recover\n\nRun `bash infra/tofu/scripts/validate.sh` in a checkout. After apply, verify Jobs, readiness, and audit evidence; on a VM run `caracal status --ready`. Use Helm revision rollback only when schema-compatible. Restore VM data and secrets separately before rebuilding.\n\n## Next Action\n\nRead [Deploy with Helm](/operations/kubernetes-helm/) or [Deploy with Docker Compose](/operations/docker-compose/)."},{"id":"/v0.2/operations/install-kit","title":"Package an Install Kit","url":"https://docs.caracal.run/v0.2/operations/install-kit/","concepts":"","body":"Use an install kit when another team needs a reviewed, reproducible handoff for a self-hosted Caracal deployment. The kit packages artifacts already implemented in this repository; it does not create a managed service, support entitlement, availability guarantee, or certification.\n\n## Kit Contents\n\n| Artifact | Required content |\n| --- | --- |\n| Release record | Product version, image digests, chart version, checksums, and verification output. |\n| Deployment values | Environment-specific Compose or Helm inputs with no secret data. |\n| Secret inventory | Every required key, owning team, external location, delivery method, and rotation procedure. |\n| Network record | Public endpoints, TLS termination, private dependencies, ingress, egress, and firewall or NetworkPolicy rules. |\n| Runbooks | Install, readiness, canary, monitoring, backup, restore, upgrade, rollback or roll-forward, and incident response. |\n| Acceptance evidence | Rendered output, migration result, readiness, negative tests, audit trace, alerts, restore, and owner approval. |\n\nKeep customer values and all secret material outside the repository and outside the kit archive unless the receiving team's approved secret-delivery system encrypts and controls them.\n\n## Procedure\n\n1. Pin the release, chart, image digests, and required package versions.\n2. Verify the release and retain the output.\n3. Render the exact deployment configuration the receiving team will use.\n4. Review public exposure, service identity, resource requests, persistence, secret references, and published mode.\n5. Build the secret inventory without recording secret values.\n6. Include the relevant operations and incident procedures, with named owners and escalation paths.\n7. Install the kit in staging and run readiness, canary allow/deny/revoke, audit, alert, backup, and isolated restore checks.\n8. Record unsupported or adopter-owned requirements explicitly.\n9. Obtain platform and security acceptance for the immutable kit revision.\n\n## Acceptance Criteria\n\n- A clean environment can reproduce the rendered deployment and artifact verification.\n- No plaintext secret appears in values, manifests, logs, or the kit archive.\n- The migration job and every required readiness endpoint pass.\n- A protected canary request produces authorization and action-result evidence.\n- A revoked Session is rejected at the chosen enforcement boundary.\n- Alert routing reaches the documented owner.\n- The data backup and separately protected key material restore successfully together.\n- The previous kit revision and recovery decision remain available.\n\n## Recovery\n\nKeep every accepted kit immutable. If validation fails, return the candidate to review instead of patching the deployed values out of band. After a production change, issue a new kit revision with updated render, evidence, and recovery notes.\n\n## Next Action\n\nUse [Plan a Platform Rollout](/operations/platform-rollout-kit/) for deployment and [Hand Off to Platform Teams](/operations/platform-team-handoff/) for ownership acceptance."},{"id":"/v0.2/operations/env-vars","title":"Configure Service Environment","url":"https://docs.caracal.run/v0.2/operations/env-vars/","concepts":"","body":"Use this page before first start or after a release adds configuration. Service variables and workload SDK variables are separate concerns.\n\n## Precedence and Locations\n\nProcess environment overrides Compose env files. An installed runtime reads `$CARACAL_HOME/caracal.env`; repository development also uses `infra/docker/local.env` over development defaults. For a supported secret, `NAME_FILE` is resolved before `NAME`.\n\nUse `CARACAL_MODE=dev` only locally. `rc` and `stable` share fail-closed configuration checks; `rc` denotes release maturity, not weaker security.\n\n## Fixed Endpoints\n\n| Service | Port | Health | Readiness | Metrics |\n| --- | ---: | --- | --- | --- |\n| API | 3000 | `/health` | `/ready` | `/metrics` |\n| STS | 8080 | `/health` | `/ready` | `/metrics`, `/metrics.json` |\n| Gateway | 8081 | `/health` | `/ready` | `/metrics`, `/metrics.json` |\n| Audit | 9090 | `/health` | `/ready` | `/metrics`, `/metrics.json` |\n| Coordinator | 4000 | `/health` | `/ready` | `/metrics` |\n| Web BFF | 3002 | deployment probe | deployment probe | none documented |\n\nCompose publishes web on host port `3001` and other services on their service ports, all on loopback.\n\n## Required Secret Classes\n\nPublished deployments require storage URLs, admin and Coordinator credentials, `SECRET_STORE_KEK`, `AUDIT_HMAC_KEY`, `STREAMS_HMAC_KEY`, `IDEMPOTENCY_HMAC_KEY`, `GATEWAY_STS_HMAC_KEY`, and `METRICS_BEARER` where consumed. Use the release-matched Compose/chart secret mapping rather than guessing `_FILE` support.\n\n## Web Console BFF\n\n| Variable | Purpose |\n| --- | --- |\n| `CARACAL_AUTH_URL` | Public auth/Web BFF URL; packaged local default is `http://localhost:3001`. |\n| `CARACAL_OPEN_REGISTRATION` | Opens registration beyond the host allowlist posture when explicitly enabled. |\n| `CARACAL_PASSWORD_SIGNUP` | Enables email/password signup; published mode also requires working verification mail. |\n| `CARACAL_SMTP_URL`, `CARACAL_SMTP_FROM` | SMTP transport and sender for verification and reset messages. |\n| `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` | Enables Google sign-in when both values are present. |\n| `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET` | Enables GitHub sign-in when both values are present. |\n| `CARACAL_AUTH_TRUST_PROXY` | Trusts forwarded client/protocol headers only when the deployment has an approved proxy boundary. |\n\nProvider credentials and SMTP URLs support their implemented `_FILE` variants. Registration admission and authentication method are separate: an allowlisted email still needs one configured sign-in method.\n\n## API Operator and Control\n\n| Variable | Purpose |\n| --- | --- |\n| `API_OPERATOR_ENABLED` | Registers the Operator capability and routes; default is enabled. |\n| `API_OPERATOR_ALLOWED_CAPABILITIES` | Optional comma-separated ceiling over executable Operator capabilities. |\n| `API_OPERATOR_AUTOPILOT_ENABLED` | Master switch that permits conversation-level automatic plan Approval. |\n| `API_OPERATOR_AUTOPILOT_WRITE_BUDGET` | Optional cumulative write-operation budget for an autopilot conversation. |\n| `API_OPERATOR_AI_MAX_OUTPUT_TOKENS` | Per-call model output ceiling. |\n| `API_OPERATOR_AI_MAX_CALLS_PER_TURN` | Per-turn model-call budget. |\n| `CARACAL_CONTROL_ENABLED`, `CONTROL_GATE_FILE` | Build-time Control mount and runtime invoke gate. |\n\nThe API process also accepts `API_OPERATOR_AI_PROVIDERS` plus per-ID `API_OPERATOR_AI_<ID>_BASE_URL`, `_MODEL`, optional `_API_KEY`, `_TIMEOUT_MS`, and `_CONTEXT_WINDOW`. IDs are tried in listed order before console-managed providers. Those variables are a direct API-process configuration path; the installed-runtime Compose and Helm surfaces do not forward provider-specific entries. For the packaged workflow, configure providers under **Settings → AI Operator → Models**, where keys are sealed into `caracal.sys` and never returned.\n\n## Safe Procedure\n\n1. Start from the shipped env template or chart values for the same release.\n2. Put non-secret overrides in `caracal.env` or environment-owned Helm values.\n3. Put secrets in owner-only files or a Kubernetes Secret projection.\n4. Set external origins and issuer to exact HTTPS URLs.\n5. Enable Control, public ingress, private egress exceptions, password signup, or proxy trust only when required.\n6. Restart changed services and gate on `/ready`.\n\n## Verify and Recover\n\nRun `caracal status --ready` or inspect Kubernetes readiness. Published metrics must reject a missing bearer and accept the configured one. If startup fails, revert the last override; do not replace generated secrets merely to clear validation errors.\n\n## Next Action\n\nChoose a [Secret Backend](/operations/secret-backends/) and apply [Harden Production](/operations/tls-hardening/)."},{"id":"/v0.2/operations/tls-hardening","title":"Harden Production","url":"https://docs.caracal.run/v0.2/operations/tls-hardening/","concepts":"","body":"Use this checklist before a `stable` deployment receives real credentials or traffic.\n\n## Prerequisites\n\nOwn the TLS endpoint, DNS, runtime Secret, ingress or proxy, dependency firewall rules, and monitoring path. Caracal does not configure host firewalls or issue Compose certificates.\n\n## Procedure\n\n1. Set `CARACAL_MODE=stable`; never expose `dev`.\n2. Keep Postgres, Redis, API, Coordinator, Audit, and Control private unless explicitly required.\n3. Expose the same-origin web BFF through HTTPS and set its exact public origin. Trust proxy headers only from a controlled direct proxy.\n4. Deliver secrets through files/projections and deny agents access to runtime secrets and Docker socket.\n5. Retain non-root, dropped-capability, read-only-filesystem, and `no-new-privileges` settings.\n6. Permit only required ingress and egress. Pin Gateway destinations with `UPSTREAM_HOST_ALLOWLIST` when appropriate.\n7. Require authenticated metrics and route critical audit/revocation alerts.\n\nGateway blocks dangerous address classes and does not follow redirects, but operator-provisioned private upstreams are intentionally supported. Network policy remains the outer boundary.\n\n## Verify\n\nTest HTTPS and issuer/JWKS identity, denied direct storage access, rejected cross-origin browser writes, rejected unauthenticated metrics, blocked upstreams, and revoked sessions denied before dispatch.\n\n## Rollback or Recovery\n\nRevert the narrowest policy change while keeping services private. Never recover by switching to `dev`, disabling revocation safety, publishing storage, or placing secrets inline.\n\n## Next Action\n\nComplete [Rotate Keys and Secrets](/operations/key-management/) and [Configure Alerts](/operations/alerts/)."},{"id":"/v0.2/operations/key-management","title":"Rotate Keys and Secrets","url":"https://docs.caracal.run/v0.2/operations/key-management/","concepts":"","body":"Rotate one class at a time. Inventory the KEK, zone signing keys, integrity HMACs, idempotency HMAC, Gateway-STS HMAC, service tokens, storage credentials, and web auth secret.\n\n## Prerequisites\n\nKeep current data and secret backups, owners for every producer/consumer, canary requests, and a documented overlap window where supported.\n\n## Safe Procedure\n\n1. Generate replacement material cryptographically.\n2. Use `SECRET_STORE_KEK_PREVIOUS` for envelope re-sealing and `IDEMPOTENCY_HMAC_KEY_PREVIOUS` for receipt overlap.\n3. Update every producer and consumer of a shared HMAC before retirement.\n4. Roll affected services and wait for readiness.\n5. Check audit, stream, replay, revocation, JWKS, and canary signals.\n6. Retire old material only after applicable token, cache, message, replay, or receipt windows.\n\nKeep the old idempotency key for at least `IDEMPOTENCY_RETENTION_SECONDS`.\n\n## Verify\n\nConfirm retired credentials fail, envelopes decrypt, JWKS is expected, new stream/audit messages verify, pending entries drain, and Gateway-to-STS exchange succeeds.\n\n## Recovery\n\nRestore the retiring value before deletion if verification fails. Preserve logs, DLQ, and replay state. Never regenerate a lost KEK; restore its backup.\n\n## Next Action\n\nVerify [PostgreSQL](/operations/postgres/) and [Redis Streams](/operations/redis/)."},{"id":"/v0.2/operations/postgres","title":"Operate PostgreSQL","url":"https://docs.caracal.run/v0.2/operations/postgres/","concepts":"","body":"Postgres holds product, authority, policy, session, delegation, outbox, key-reference, and audit state.\n\n## Prerequisites\n\nUse release-matched migrations, an administrative migration role, service-specific credentials, and a tested backup. Do not grant service roles ownership or `BYPASSRLS`.\n\n## Procedure\n\nCompose and Helm run `infra/postgres/scripts/migrate.sh` in a one-shot workload. Files apply in order, one transaction each, under an advisory lock. Production operation is forward-only.\n\nIn a disposable database, run:\n\n```bash\nbash infra/postgres/scripts/validateMigrations.sh\n```\n\nMonitor pools, long transactions, timeouts, storage, audit partitions, and outbox age. Increase database capacity before aggregate service pools.\n\n## Verify\n\nConfirm migrations, `schema_migrations`, service roles, RLS, append-only audit permissions, and dependent readiness.\n\n## Recovery\n\nRestore connectivity/capacity, then verify migrations, pools, outboxes, and readiness. For data loss, use complete restore; do not reconstruct authority or audit rows manually.\n\n:::danger[Schema and evidence damage]\nDo not run down migrations, drop databases, disable RLS, or mutate audit rows as an outage shortcut.\n:::\n\n## Next Action\n\nVerify event delivery with [Operate Redis Streams](/operations/redis/)."},{"id":"/v0.2/operations/redis","title":"Operate Redis Streams","url":"https://docs.caracal.run/v0.2/operations/redis/","concepts":"","body":"Redis carries audit, invalidation, revocation, agent, invocation, and delegation streams. It is correctness-critical, not an evictable cache.\n\n## Prerequisites\n\nUse Streams, authentication, persistence, `maxmemory-policy noeviction`, and enough memory. The bundled image uses AOF with `appendfsync everysec`; validate external services separately.\n\n## Procedure\n\nProvisioning is automatic in the bundled stack. From a checkout:\n\n```bash\nbash infra/redis/provision-streams.sh\nbash infra/redis/scripts/verify.sh\n```\n\nProvide Redis host, port, and password file when needed. Monitor stream length, groups, pending entries, reclaim/retry, DLQ, memory, persistence, and latency.\n\n## Verify\n\nConfirm expected streams/groups, idempotent provisioning, `noeviction`, persistence, and policy/revocation/audit delivery.\n\n## Recovery\n\nRestore Redis, verify groups, then drain consumers and replay. Reconcile Postgres outboxes and replay directories. Never delete pending entries or reset groups until durable handling is proven.\n\n## Next Action\n\nUse [Scale Capacity](/operations/scale-capacity/) when lag or memory grows."},{"id":"/v0.2/operations/scale-capacity","title":"Scale Capacity","url":"https://docs.caracal.run/v0.2/operations/scale-capacity/","concepts":"","body":"Caracal publishes signals and Helm controls, but no universal throughput, latency, or availability figure. Establish a baseline with your workload.\n\n## Decision Criteria\n\n| Signal | Investigate first |\n| --- | --- |\n| Database pool pressure | Slow queries, transactions, connections, I/O |\n| Audit lag, DLQ, replay age | Audit write path, Redis, Postgres, replay storage |\n| Gateway STS circuit | STS readiness and exchange latency |\n| Revocation lag | Redis latency, pending entries, snapshot refresh |\n| Readiness flapping | CPU, memory, dependencies, probes |\n\n## Prerequisites\n\nCollect authenticated metrics, latency, dependency metrics, logs, and repeatable load. Know storage connection/memory ceilings.\n\n## Safe Procedure\n\n1. Record baseline and first saturated resource.\n2. Correct dependencies or slow queries before replicas.\n3. Change one limit or replica count at a time.\n4. Keep aggregate pools within database capacity.\n5. Repeat load and compare readiness, errors, lag, DLQ, replay, and revocation.\n6. Set environment thresholds from measured bounds.\n\nChart replica/HPA defaults are not capacity recommendations.\n\n## Rollback\n\nRestore the previous value if pressure or errors increase and retain both measurements.\n\n## Next Action\n\nBuild gates in [Monitor Health and Metrics](/operations/observability/)."},{"id":"/v0.2/operations/observability","title":"Monitor Health and Metrics","url":"https://docs.caracal.run/v0.2/operations/observability/","concepts":"","body":"Use `/health` for liveness and `/ready` for traffic and rollout gates.\n\n## Endpoint Map\n\n| Service | Port | Health | Readiness | Metrics |\n| --- | ---: | --- | --- | --- |\n| API | 3000 | `/health` | `/ready` | `/metrics` |\n| STS | 8080 | `/health` | `/ready` | `/metrics`, `/metrics.json` |\n| Gateway | 8081 | `/health` | `/ready` | `/metrics`, `/metrics.json` |\n| Audit | 9090 | `/health` | `/ready` | `/metrics`, `/metrics.json` |\n| Coordinator | 4000 | `/health` | `/ready` | `/metrics` |\n\nPublished metrics require `Authorization: Bearer <METRICS_BEARER>`. Keep them private even when authenticated.\n\n## Procedure\n\n1. Scrape every enabled service with the metrics secret.\n2. Track readiness, DB pool, policy age/compile errors, provider refresh, audit lag/DLQ/tamper/replay, outbox, STS circuit, and revocation freshness.\n3. Correlate logs and audit by request ID.\n4. Gate deployment on readiness plus canary exchange, Gateway call, and audit lookup.\n\nFor Compose run `caracal status --ready`; from a checkout use `bash infra/scripts/smokeTest.sh`. For Helm, verify Jobs, workloads, ServiceMonitor discovery, and rules.\n\n## Recovery\n\nIf health passes but readiness fails, preserve the readiness reason and inspect its dependency/safety latch. Restore the cause and drain backlogs. Preserve evidence before replacing an `audit_evidence_lost` process.\n\n## Next Action\n\nRoute signals with [Configure Alerts](/operations/alerts/)."},{"id":"/v0.2/operations/alerts","title":"Configure Alerts","url":"https://docs.caracal.run/v0.2/operations/alerts/","concepts":"","body":"Use the chart PrometheusRule only with a compatible rule importer. Tune thresholds from measured behavior.\n\n## Required Routes\n\n| Alerts | Route and first action |\n| --- | --- |\n| Audit tamper/evidence loss | Security incident; stop risky traffic and preserve evidence |\n| Revocation freshness/reload/lag | Access-safety incident; remove affected Gateway from traffic |\n| Gateway STS circuit/exchange | Restore STS exchange before protected traffic |\n| Audit DLQ/lag/replay or API outbox | Freeze changes; restore Redis/Postgres/Audit path |\n| Policy/provider alerts | Restore last known-good policy/provider behavior |\n| Pool saturation/readiness flapping | Platform on-call; diagnose capacity/dependencies |\n\n## Procedure\n\n1. Render PrometheusRule and inspect expressions, thresholds, `for` durations, and labels.\n2. Confirm every metric is scraped.\n3. Route critical safety alerts to staffed security/on-call and warnings to owners.\n4. Link rules to incident or recovery runbooks.\n5. Fire each rule safely in non-production and record detection time.\n\n## Verify\n\nConfirm loading, firing, intended receiver, resolution, and valid runbook links.\n\n## Rollback\n\nRevert only a noisy threshold/route, not collection. Keep audit, revocation, and policy safety alerts enabled.\n\n## Next Action\n\nStart [Symptom Triage](/operations/troubleshooting/) or rehearse [Failure Drills](/operations/failure-drills/)."},{"id":"/v0.2/operations/troubleshooting","title":"Troubleshoot by Symptom","url":"https://docs.caracal.run/v0.2/operations/troubleshooting/","concepts":"","body":"Use this when an SDK, HTTP request, console action, or protected call failed. It identifies the failed surface; it does not repair infrastructure.\n\n## Prerequisites\n\nCapture timestamp, request ID, zone, application, resource, operation, status, error code, and version. Redact credentials.\n\n## Triage Procedure\n\n1. Check readiness. If any service is NotReady, use [Debug Infrastructure Issues](/operations/debugging/).\n2. For client startup failure, verify explicit profile, endpoints, IDs, and credential file.\n3. For `401`, verify credential type, issuer, expiry, and intended surface.\n4. For STS `403`, inspect Grants, Resource/scopes, Policy set, Session, Delegation, and Approval.\n5. For resource/Gateway `403`, verify mandate issuer, audience, scope, `X-Caracal-Resource`, binding, revocation, and verifier.\n6. Search Audit by request ID. If absent, confirm the request reached enforcement, then inspect the audit path.\n\n## Verification\n\nRepeat one safe request and confirm its expected status and audit subject/resource/scopes/policy/result.\n\n## Recovery Boundary\n\nDo not widen policy, bypass Gateway, disable revocation, or replace credentials until the surface is identified.\n\n## Diagnostic Bundle\n\nCaracal exposes diagnosis through existing supported surfaces rather than a separate `doctor` command:\n\n| Evidence | Surface |\n| --- | --- |\n| Runtime health and readiness | `caracal status --json` and `caracal status --ready --json` |\n| Service, Zone, and provider checks | Web console **Diagnostics** |\n| Recent decisions and operational events | Web console **Audit** or the Admin API audit list |\n| One correlated authorization path | Request trace by request ID |\n\nCapture those outputs with the timestamp, version, Zone, and request ID. Redact credentials before attaching the bundle to an incident.\n\n## Next Action\n\nUse [Infrastructure Diagnosis](/operations/debugging/) or [Authorization Diagnosis](/guides/authorize-access/)."},{"id":"/v0.2/operations/debugging","title":"Debug Infrastructure Issues","url":"https://docs.caracal.run/v0.2/operations/debugging/","concepts":"","body":"Use this after readiness, rollout, or dependency failure. It diagnoses infrastructure; recovery is separate.\n\n## Prerequisites\n\nPreserve the first failing readiness response, deployment/config diff, workload status, logs, and alert timeline before restart.\n\n## Diagnosis Procedure\n\n1. Inspect Compose containers or Kubernetes pods, Jobs, events, and rollout.\n2. Compare mode, Secret keys, URLs, ports, origins, and release pin with the release.\n3. Test Postgres connectivity, migrations, pools, and outbox age.\n4. Test Redis connectivity, `noeviction`, persistence, groups, and pending entries.\n5. Read exact readiness reason and correlated logs.\n6. Inspect policy age, STS circuit, revocation snapshot, audit DLQ/replay, and evidence-loss latch.\n\nStart with `caracal status --json`, Compose status/logs, or Kubernetes get/describe/logs.\n\n## Verification\n\nDiagnosis is complete when one dependency, value, ceiling, or safety invariant explains readiness with log/metric evidence.\n\n## Recovery Boundary\n\nDo not delete volumes, reset streams, restore data, or clear evidence before selecting recovery.\n\n## Next Action\n\nApply [Recover from Failures](/operations/failure-modes/)."},{"id":"/v0.2/operations/failure-modes","title":"Recover from Failures","url":"https://docs.caracal.run/v0.2/operations/failure-modes/","concepts":"","body":"Use this only after diagnosis. Restore durable state, then services, then evidence and access-safety freshness.\n\n## Recovery Order\n\n1. Freeze rollouts and risky traffic.\n2. Restore Postgres, then Redis.\n3. Restore API, STS, Gateway, Audit, Coordinator, and web readiness.\n4. Drain outboxes, pending entries, audit replay, and DLQ.\n5. Prove policy and revocation freshness.\n6. Run canary allow/deny and locate audit evidence.\n\n## Failure Procedures\n\n| Failure | Recovery |\n| --- | --- |\n| Postgres | Restore connectivity/capacity, migrations and pools, then outboxes |\n| Redis | Restore persistence/`noeviction`, groups and pending entries, then replay |\n| STS | Restore stores, policy bundle, signing/KEK, JWKS, Gateway HMAC |\n| Gateway | Restore STS exchange, binding, revocation, egress, replay |\n| Audit | Restore stores/HMAC, drain replay/DLQ, check tamper state |\n| Coordinator | Restore stores, service token, outbox, workers |\n\n`audit_replay_unavailable` can clear after storage recovers. `audit_evidence_lost` is latched: preserve evidence, scope the interval, then replace the process as an incident action.\n\n## Verification\n\nReadiness passes; queues are drained/understood; revoked sessions deny; current policy applies; audit records canaries.\n\n## Rollback\n\nReturn to preserved config/dependency snapshot if repair worsens state. Database restore is separate destructive recovery.\n\n## Next Action\n\nRehearse [Failure Drills](/operations/failure-drills/)."},{"id":"/v0.2/operations/failure-drills","title":"Run Failure Drills","url":"https://docs.caracal.run/v0.2/operations/failure-drills/","concepts":"","body":"Run only in isolated non-production with restorable data and secrets.\n\n## Prerequisites\n\nEstablish green readiness, understood backlogs, tested alert routing, recovery owner, stop condition, backup, and baseline canary/audit results.\n\n## Procedure\n\n1. Select one fault: block Redis/Postgres, isolate Gateway from STS, pause Audit, delay revocation, or activate invalid test policy.\n2. Inject only that fault with a time limit.\n3. Record readiness, traffic, alerts, logs, lag, and detection time.\n4. Remove the fault.\n5. Execute [Recover from Failures](/operations/failure-modes/).\n6. Record readiness, drain, freshness, canary, and audit recovery times.\n\n:::danger[Destructive drill]\nAudit tampering, restore, Redis replacement, volume deletion, and key loss require disposable infrastructure and explicit approval.\n:::\n\n## Pass Criteria\n\nAlert reaches the owner; services fail closed where expected; recovery follows the runbook; readiness and canary evidence return without unexplained queues.\n\n## Recovery\n\nIf a stop condition is exceeded, terminate injection, preserve evidence, restore last known-good, and open an incident.\n\n## Next Action\n\nUpdate thresholds/runbooks, then validate [Backups](/operations/backup-retention/)."},{"id":"/v0.2/operations/backup-retention","title":"Back Up and Retain Data","url":"https://docs.caracal.run/v0.2/operations/backup-retention/","concepts":"","body":"Repository scripts support Compose only. Kubernetes/external stores require operator tooling.\n\n## Bundle Scope\n\n`backup.sh` writes Postgres globals/database dumps, Redis AOF after rewrite, and available STS/Gateway replay state. It excludes secrets.\n\n## Backup Procedure\n\n```bash\nCARACAL_BACKUP_DIR=/var/backups/caracal CARACAL_BACKUP_RETAIN=7 bash infra/scripts/backup.sh\n```\n\nSet the Compose project when non-default. Encrypt/copy off-host and back up matching secrets separately.\n\n## Verify Backup\n\nInspect manifest/dumps/AOF/replay, then restore with copied secrets into an isolated project and verify readiness, policy, sessions, revocation, authorization, and audit.\n\n## Restore Procedure\n\n:::danger[Destructive restore]\nRestore stops apps, drops/recreates databases, replaces Redis, and overwrites replay state.\n:::\n\n```bash\nCARACAL_COMPOSE_PROJECT=<isolated-project> CARACAL_RESTORE_CONFIRM=yes bash infra/scripts/restore.sh <bundle.tar.gz>\n```\n\nPostgres/Redis containers and matching secrets must exist.\n\n## Recovery\n\nOn failure keep isolated, preserve output/state, recreate clean volumes, and retry from original bundle/secrets. Never reopen partial restore.\n\n## Next Action\n\nUse [Incident Response](/operations/incident-response/) after data loss or tampering."},{"id":"/v0.2/operations/incident-response","title":"Respond to Incidents","url":"https://docs.caracal.run/v0.2/operations/incident-response/","concepts":"","body":"Use for policy bypass, credential exposure, unsafe routing, audit integrity/loss, malicious release, stale revocation, or serious availability failure. Canonical detail is in `governance/INCIDENT_RESPONSE.md`.\n\n## Prerequisites\n\nAssign Incident Lead, Driver, and Reviewer. Use a private advisory for OSS security issues and approved private evidence storage.\n\n## Procedure\n\n1. Record source, time, version, mode, boundary, assets, and severity.\n2. Contain severe incidents first: revoke/rotate, deny, block, disable, or remove traffic.\n3. Preserve logs, metrics, audit, Redis pending/DLQ, replay, config, images, and request IDs.\n4. Reproduce minimally in isolation and identify the missing guard/failure.\n5. Remove the path, validate reproduction and a negative variant, and search sibling boundaries.\n6. Recover and reconcile Postgres, Redis, outbox, revocation, sessions, and audit.\n7. Communicate affected versions/actions after containment or fix.\n\n## Verification\n\nClose only when behavior fails safely, targeted validation passes, readiness/safety signals recover, communication completes, and follow-ups have owners.\n\n## Recovery Boundary\n\nKeep containment until validation. Do not delete evidence or publish exploit detail to accelerate closure.\n\n## Next Action\n\nUse [Plan a Platform Rollout](/operations/platform-rollout-kit/) for correction."},{"id":"/v0.2/operations/platform-rollout-kit","title":"Plan a Platform Rollout","url":"https://docs.caracal.run/v0.2/operations/platform-rollout-kit/","concepts":"","body":"Use this for infrastructure changes. Policy activation has a separate [Policy Deployment](/operations/policy-deployment/) procedure.\n\n## Prerequisites\n\nRetain current version, rendered config, values, secrets, data backup, and schema-compatibility decision. Prepare readiness, authorization, revocation, and audit checks.\n\n## Procedure\n\n1. Verify provenance and render exact configuration.\n2. Review migrations, Secret keys, endpoints, NetworkPolicies, volumes, resources, and alerts.\n3. Confirm storage/event health.\n4. Apply migrations, then roll via supported runtime or Helm path.\n5. Stop on migration, readiness, audit, outbox, policy, revocation, or STS-exchange failure.\n6. Resume only after canary authorization and audit evidence.\n\n## Verify\n\nRecord migration, rollout, `/ready`, queues/replay, metrics, and allow/deny canary request IDs.\n\n## Rollback or Recovery\n\nUse prior app/config only when schema-compatible; otherwise roll forward. Restore data only for declared disaster recovery, not ordinary rollback.\n\n## Next Action\n\nComplete [Platform Handoff](/operations/platform-team-handoff/)."},{"id":"/v0.2/operations/policy-deployment","title":"Deploy Policy Changes","url":"https://docs.caracal.run/v0.2/operations/policy-deployment/","concepts":"","body":"Use web console, Admin SDK, or API. Runtime CLI does not manage policy.\n\n## Prerequisites\n\nRecord active version, allow/deny cases, Resources/scopes, Grants, Approval behavior, rollback version, and STS/Audit readiness.\n\n## Procedure\n\n1. Create version and compile merged bundle.\n2. Simulate allow, deny, missing-scope, revoked-Session, and Approval cases.\n3. Activate in a test zone/cohort.\n4. Poll activation status until `propagation_status` is `loaded` and STS version matches.\n5. Run canaries and inspect audit.\n6. Expand only after pass.\n\nAllow the configured STS poll interval for multi-replica convergence.\n\n## Verify\n\nCanaries match simulation, STS reports intended bundle, no compile/staleness alert fires, Gateway matches, and audit identifies determining policy.\n\n## Rollback or Recovery\n\nActivate the last known-good version and repeat convergence/canaries. Do not edit stored versions or weaken unrelated grants.\n\n## Next Action\n\nUse [Upgrade Caracal](/operations/upgrade/) only for runtime/chart/schema changes."},{"id":"/v0.2/operations/upgrade","title":"Upgrade Caracal","url":"https://docs.caracal.run/v0.2/operations/upgrade/","concepts":"","body":"Migrations are forward-only. Runtime stages images, migrates, rolls services, then waits up to two minutes for readiness. This is not a zero-downtime guarantee.\n\n## Prerequisites\n\nRead release notes, use supported sequential upgrades, back up data/secrets, record current config, verify audit/streams, and prepare canaries. Install the new runtime binary before upgrading.\n\n## Runtime Procedure\n\n```bash\ncaracal upgrade\n```\n\nIt refreshes assets, preserves env/non-empty secrets, stages, migrates, rolls, and gates readiness. `--no-pull` uses staged images. Attempts append to `$CARACAL_HOME/upgrade.log` outside dev. Re-run after interruption.\n\n## Helm Procedure\n\nRender/diff exact values, run pinned Helm upgrade with wait/atomic behavior, and inspect migration Jobs/rollout. Use a diff plugin only if installed.\n\n## Verify\n\nConfirm migrations, readiness, queues/replay, exchange, protected request, revocation denial, and audit evidence.\n\n## Rollback\n\nPrefer roll forward. Use older apps only after schema/config compatibility review. Never delete secrets or bypass the runtime version guard casually.\n\n## Next Action\n\nRecord result in [Platform Rollout](/operations/platform-rollout-kit/)."},{"id":"/v0.2/operations/compliance-audit-integration","title":"Export Audit Evidence","url":"https://docs.caracal.run/v0.2/operations/compliance-audit-integration/","concepts":"","body":"Audit records are technical evidence, not certification or a complete organizational trail. Audit consumes Redis, writes append-only Postgres rows, checks tamper state, and can export complete hourly Parquet partitions.\n\n## Prerequisites\n\nDefine evidence scope, retention, access, encryption, legal hold, and archive ownership. Configure `AUDIT_EXPORT_S3_*` only when needed and size `AUDIT_EXPORT_TMP_DIR` for the largest hourly partition.\n\n## Procedure\n\n1. Verify Audit readiness, HMAC, stream group, DLQ, and tamper metrics.\n2. Configure an operator-owned S3-compatible endpoint and least-privilege credentials.\n3. Enable export; monitor watermark, scratch space, upload failures, and objects.\n4. Correlate a canary request across producer, stream, Postgres, and export.\n5. Retain applicable HMAC material with verification evidence.\n\nThe watermark advances only after successful upload.\n\n## Verify and Recover\n\nCompare counts for a closed interval, inspect DLQ/lag, and verify intended reader access. On failure preserve Postgres, pending entries, DLQ, replay, scratch output, and logs before replay or deletion.\n\n## Next Action\n\nGenerate an [Evidence Pack](/security/evidence-pack/) or complete [Platform Handoff](/operations/platform-team-handoff/)."},{"id":"/v0.2/operations/platform-team-handoff","title":"Hand Off to Platform Teams","url":"https://docs.caracal.run/v0.2/operations/platform-team-handoff/","concepts":"","body":"This transfers operational responsibility; it does not create support or availability guarantees.\n\n## Prerequisites\n\nName service, storage, security, release, and on-call owners. Provide deployed values, secret inventory, monitoring, backups, and private incident access.\n\n## Acceptance Procedure\n\n1. Reproduce release verification and rendering.\n2. Start or upgrade staging.\n3. Verify migrations, health/readiness, metrics, authorization, revocation, and audit.\n4. Trigger alerts and confirm routing.\n5. Produce data and separate secret backups; restore both in isolation.\n6. Rehearse dependency failure and security intake.\n7. Record known limits and absent enterprise assurances.\n\n## Acceptance Evidence\n\nRetain version/digests, values, migration/readiness output, canary IDs, alert tests, restore results, owners, escalation, and risks.\n\n## Recovery\n\nReject handoff when an owner, secret, backup, alert, or recovery test is missing. Continue staging ownership until closed.\n\n## Next Action\n\nUse [Respond to Incidents](/operations/incident-response/) as the on-call entry point."},{"id":"/v0.2/architecture","title":"Understand Architecture","url":"https://docs.caracal.run/v0.2/architecture/","concepts":"","body":"You do not need this section to complete normal console setup. Use it when selecting an enforcement boundary, tracing a failed request, planning dependencies, or recovering state.\n\n## Choose the Flow You Need\n\n| Question                                                    | Read                                                  |\n| ----------------------------------------------------------- | ----------------------------------------------------- |\n| Which clients may call which surfaces?                      | [Map the System](/architecture/system-topology/)      |\n| What happens before a mandate is issued?                    | [Exchange Tokens](/architecture/token-exchange-flow/) |\n| How do Sessions and Delegations affect authority?           | [Coordinate Sessions](/architecture/delegation-flow/) |\n| Why can state be current in Postgres but delayed elsewhere? | [Propagate Events](/architecture/event-streams/)      |\n| What must be backed up or restored first?                   | [Store State](/architecture/storage-model/)           |\n| Which key protects which trust transition?                  | [Manage Keys](/architecture/crypto-keys/)             |\n| Where must a deployment fail closed?                        | [Enforce Boundaries](/architecture/trust-boundaries/) |\n\n## Operational Model\n\nPostgres is durable state. Redis Streams propagates events and invalidations. STS decides and issues authority. Gateway enforces before an upstream call. Coordinator makes execution lineage explicit. Audit turns signed events into evidence.\n\nThat separation creates useful failure boundaries: management can be unavailable without changing already-issued token expiry; Audit can lag while requests continue to emit replayable evidence; Redis can recover propagation from durable outboxes; Gateway denies when it cannot establish current authority.\n\n## Supported Integration Surfaces\n\nIntegrators call the SDKs, STS token endpoint, Gateway proxy, documented Coordinator API, Admin API, or optional Control API according to the task. Internal endpoints, databases, Redis topics, replay files, and retained schema names are implementation boundaries, not application APIs.\n\n## Next Step\n\n[Map the System](/architecture/system-topology/)."},{"id":"/v0.2/architecture/system-topology","title":"Map the System","url":"https://docs.caracal.run/v0.2/architecture/system-topology/","concepts":"","body":"Use this map before exposing a service or diagnosing a cross-service failure.\n\n## Caller Map\n\n| Caller                        | Supported destination                                         | Avoid                                                              |\n| ----------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------ |\n| Human operator                | Web console on local `3001` or deployed console URL           | Direct database, Redis, or internal routes                         |\n| Trusted management automation | Admin API/Admin SDK, or optional Control API on API `3000`    | Runtime CLI product-management commands                            |\n| Workload needing mandates     | STS `POST /oauth/2/token` through an SDK or documented client | API admin credentials                                              |\n| Protected HTTP client         | Gateway `8081` with a mandate and resource header             | Direct protected upstream when Gateway is the enforcement boundary |\n| Session-aware application     | Coordinator `4000` through an SDK or documented API           | Coordinator operator tokens in workload source                     |\n| Verifier                      | STS JWKS and revocation backend through verification packages | Private signing keys                                               |\n\nPorts are local Compose defaults, bound to loopback. A production deployment normally places supported public surfaces behind TLS and keeps internal and operator-only routes private.\n\n## Dependency Map\n\n| Visible failure                               | Dependency to check                                                         |\n| --------------------------------------------- | --------------------------------------------------------------------------- |\n| Console cannot list or mutate product objects | API auth, API readiness, Postgres, then Redis/outbox                        |\n| Exchange denies or cannot load policy         | STS, Postgres policy/product state, Redis invalidation, signing/secret keys |\n| Gateway denies before reaching upstream       | Inbound mandate, resource binding, revocation, STS, or upstream safety      |\n| Sessions or Delegations appear stale          | Coordinator, Postgres, Redis, outbox, leases/sweepers                       |\n| Audit search lags behind requests             | Redis consumer state, Audit readiness, DLQ, replay volumes, Postgres        |\n\n## Deployment Implications\n\n- API, STS, Gateway, Audit, and Coordinator all need Postgres and Redis in the packaged topology; readiness captures more than process liveness.\n- Gateway also depends synchronously on STS for per-request exchange.\n- Console product views depend on API and Coordinator through the auth backend-for-frontend.\n- Control is an optional API plugin, not a separate service or port.\n\nUse [Choose a Deployment Profile](/operations/deployment-profiles/) for deployment choices and [Understand Services](/services/) for service-specific failure posture.\n\n## Next Step\n\n[Exchange Tokens](/architecture/token-exchange-flow/)."},{"id":"/v0.2/architecture/token-exchange-flow","title":"Exchange Tokens","url":"https://docs.caracal.run/v0.2/architecture/token-exchange-flow/","concepts":"","body":"Use this flow when an SDK, `caracal run`, or Gateway cannot obtain authority.\n\n## Request Flow\n\n## What the Integrator Supplies\n\nThe caller authenticates an application or workload, identifies the zone and target resource, and requests scopes. A subject token can contribute a federated `sub`; a prior Caracal mandate can be narrowed. Session and Delegation identifiers supply execution lineage when the SDK flow created them.\n\nThe protocol fields `session_id`, `agent_session_id`, and `delegation_edge_id` refer respectively to an Authority record, a Coordinator Session, and a Delegation. They are not interchangeable IDs.\n\n## Approval Branch\n\nAn Approval is a hold, not a credential. An eligible operator or end user decides it, then the waiting client retries with the hold ID. STS consumes an approved hold once while issuing the credential. Rejected, expired, undecided, or already-consumed holds deny.\n\n`caracal run` waits and retries once. SDK applications should follow their language SDK's interaction-required contract. See [Approvals](/concepts/step-up/) for the canonical model.\n\n## Failure and TTL Implications\n\n- Resource mandates are capped at 15 minutes; Session mandates are capped at 60 minutes.\n- Gateway rejects an inbound token that is too close to expiry before proxying.\n- Invalid client proof, subject issuer, resource, policy, Session, Delegation, operation, scope, or approval fails before issuance.\n- Gateway-authenticated exchanges are signed over the exact form body with a timestamp and nonce; query ambiguity and duplicate singleton fields are rejected.\n- Audit delivery is asynchronous, but STS and Gateway use replay storage when immediate Redis publication is unavailable.\n\nFor exact request fields and errors, use [Use STS Endpoint](/api/sts/). Do not call STS internal policy or key-rotation routes; the API service owns those calls.\n\n## Next Step\n\n[Coordinate Sessions](/architecture/delegation-flow/)."},{"id":"/v0.2/architecture/delegation-flow","title":"Coordinate Sessions","url":"https://docs.caracal.run/v0.2/architecture/delegation-flow/","concepts":"","body":"Use this flow when a request depends on execution lineage, long-lived leases, or authority delegated between Sessions.\n\n## Session and Delegation Flow\n\n## Choose a Session Lifecycle\n\nTask Sessions use a TTL and expire. Service Sessions use heartbeat leases and become unhealthy when renewal stops. The process that owns a service Session must continue heartbeating; container liveness alone does not renew authority.\n\nA Session tree records parent-child execution. Suspending or terminating a subtree affects governed execution state. A Delegation separately records exactly which resource, scopes, constraints, and expiry cross from one Session to another.\n\n## Consistency and Failure\n\nCoordinator writes state and outbox records durably in Postgres, then publishes lifecycle and invalidation events through Redis. A short propagation delay is possible. STS validates authoritative Session and Delegation state before issuing delegated authority, so a stale consumer view does not grant authority by itself.\n\nOperational jobs expire stale task Sessions and Delegations, detect missed service leases, enforce invocation deadlines, publish outbox rows, and clean retained terminal data. Backlogged or dead outbox rows mean downstream views and revocation consumers can lag; surface that in Diagnostics before assuming the SDK failed.\n\n## Operator and Integrator Surfaces\n\nApplications use an SDK or the documented Coordinator API. Human operators use console **Sessions**, Delegation views, and Audit. Do not mutate Coordinator tables or publish lifecycle topics directly. Top-level `caracal` commands do not manage Sessions or Delegations.\n\n## Next Step\n\n[Propagate Events](/architecture/event-streams/)."},{"id":"/v0.2/architecture/event-streams","title":"Propagate Events","url":"https://docs.caracal.run/v0.2/architecture/event-streams/","concepts":"","body":"Redis Streams carries propagation, not the durable product model. This distinction explains why a committed change can exist before every consumer has observed it.\n\n## Delivery Paths\n\nAPI and Coordinator changes enqueue outbox rows in the same Postgres transaction as state. A dispatcher retries publication. Consumers use groups, pending-entry recovery, deduplication, and signed messages in published modes.\n\nSTS and Gateway cannot put audit evidence in the same database transaction as every decision. They write replay files when Redis delivery is unavailable and drain them after recovery.\n\n## What Lag Means\n\n| Observation                                                | Interpretation                                                                             |\n| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------ |\n| Product read shows a change but a consumer has not reacted | Outbox or stream propagation is delayed.                                                   |\n| Gateway or verifier still accepts revoked authority        | Check revocation snapshot, consumer lag, readiness, and verifier fail posture immediately. |\n| Audit event appears later                                  | Check Audit consumer lag and replay backlog; do not assume evidence was never emitted.     |\n| Dead outbox rows or old pending entries                    | Recovery needs operator action; readiness thresholds may fail.                             |\n\nPostgres remains the recovery anchor. Redis is operationally important but is not a replacement for database backups.\n\n## Canonical Topic Reference\n\n[Use Event Topics](/api/event-topics/) owns the topic, producer, and consumer-group list. Do not publish those topics from application code. They are service integration contracts, not a public event bus for workloads.\n\n## Next Step\n\n[Store State](/architecture/storage-model/)."},{"id":"/v0.2/architecture/storage-model","title":"Store State","url":"https://docs.caracal.run/v0.2/architecture/storage-model/","concepts":"","body":"## Data Classes\n\n| State                                                                            | Store                                   | Operational implication                                                      |\n| -------------------------------------------------------------------------------- | --------------------------------------- | ---------------------------------------------------------------------------- |\n| Product, policy, authority, Session, Delegation, admin-audit, and audit evidence | Postgres                                | Primary durable backup and restore target.                                   |\n| Invalidation, revocation, lifecycle, audit delivery, consumer coordination       | Redis Streams                           | Preserve when possible; recover propagation from durable state and outboxes. |\n| Undelivered STS/Gateway audit events                                             | Replay volumes                          | Restore before discarding Redis state so evidence can drain.                 |\n| Provider, application, workload, signing, HMAC, and encryption secrets           | Secret backend and runtime secret files | Restore with matching key material; ciphertext alone is insufficient.        |\n\n## Ownership Boundaries\n\nThe API owns product and policy data. STS owns issuance behavior and Authority records. Coordinator owns Sessions and Delegations. Audit owns append-only evidence ingestion. Shared Postgres does not make one service's tables a supported API for another client.\n\nSome schema names retain protocol history, including `agent_services`, `agent_invocations`, and `delegation_edges`. Public surfaces expose Sessions, invocations, and Delegations. Integrations must use public names and APIs rather than retained table names.\n\n## Integrity Guarantees\n\n- Production migrations move forward and are recorded in `schema_migrations`.\n- Policy versions are immutable.\n- Zone-scoped reads use fail-closed row-level security.\n- The Audit database role cannot update or delete evidence rows.\n- Outboxes couple a state change to eventual event publication.\n- Secret envelopes bind encrypted values to key fingerprints and purposes.\n\n## Restore Order\n\n1. Restore Postgres and the runtime/secret-backend keys needed to decrypt it.\n2. Restore STS and Gateway audit replay volumes.\n3. Restore Redis streams and pending entries when available, or allow durable publishers and snapshots to rebuild propagation state.\n4. Reconnect audit exports and verify retention watermarks.\n5. Run readiness, Diagnostics, a policy simulation, a protected request, and an Audit trace.\n\nUse [Back Up and Retain Data](/operations/backup-retention/) for procedures. Do not use direct SQL as a management or migration shortcut.\n\n## Next Step\n\n[Manage Keys](/architecture/crypto-keys/)."},{"id":"/v0.2/architecture/crypto-keys","title":"Manage Keys","url":"https://docs.caracal.run/v0.2/architecture/crypto-keys/","concepts":"","body":"Caracal separates keys so compromise or rotation at one boundary does not silently authorize another.\n\n## Key-to-Boundary Map\n\n| Material                              | Boundary                                                                                  | Failure implication                                                                      |\n| ------------------------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |\n| Zone signing keys                     | STS mandate issuance to verifiers                                                         | Missing private material blocks issuance; stale JWKS caches can reject a rotated signer. |\n| `SECRET_STORE_KEK`                    | Services to sealed provider, signing, connection, sink, application, and workload secrets | Wrong key fingerprint prevents decryption; restore ciphertext and keyring together.      |\n| `AUDIT_HMAC_KEY`                      | Producers to Audit evidence                                                               | Mismatch creates HMAC failures and blocks trusted ingestion.                             |\n| `STREAMS_HMAC_KEY`                    | Stream producers to consumers                                                             | Mismatch blocks trusted propagation in published modes.                                  |\n| `GATEWAY_STS_HMAC_KEY`                | Gateway to STS exchange                                                                   | Mismatch denies every Gateway-authenticated exchange.                                    |\n| Admin/Coordinator/Control credentials | Operator clients to management surfaces                                                   | Missing or insufficient scope yields `401` or `403`; do not distribute to workloads.     |\n\n## Mandate Verification Flow\n\nKeep old public keys available until verifier caches and outstanding token TTLs no longer need them. Never copy private signing material to a verifier.\n\n## Secret Envelope Rotation\n\nSecret Store uses a fresh data-encryption key per value and wraps it with the configured KEK. The envelope records a key fingerprint and purpose-specific associated data. During KEK rotation, `SECRET_STORE_KEK_PREVIOUS` allows reads under the retiring key while data is rewrapped. Removing it too early makes old envelopes unreadable.\n\nPublished `rc` and `stable` modes require validated HMAC material rather than silently disabling integrity checks. A missing or short required key fails startup or readiness.\n\nUse [Rotate Keys and Secrets](/operations/key-management/) for the operational procedure and [Configure Secret Backends](/operations/secret-backends/) for custody options.\n\n## Next Step\n\n[Enforce Boundaries](/architecture/trust-boundaries/)."},{"id":"/v0.2/architecture/trust-boundaries","title":"Enforce Boundaries","url":"https://docs.caracal.run/v0.2/architecture/trust-boundaries/","concepts":"","body":"## Boundary Decisions\n\n| Boundary        | Accepted caller                                                   | Must remain outside                                                    |\n| --------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------- |\n| Runtime CLI     | Local operator managing lifecycle or launching a workload         | Product-management state and admin credentials                         |\n| Web console/BFF | Authenticated, admitted operator with zone access                 | Anonymous callers and direct browser possession of root service tokens |\n| Admin API       | Trusted Admin SDK or console backend                              | Workload code                                                          |\n| Control API     | Enabled gate plus short-lived, scoped, zone-bound Control token   | Replayed tokens, root admin token distribution, lifecycle commands     |\n| STS             | Valid application/workload/Gateway proof and well-formed exchange | End-user password authentication and unsupported token fields          |\n| Gateway         | Mandate, `X-Caracal-Resource`, binding, safe path and upstream    | Caller-selected upstreams and caller-provided upstream credentials     |\n| Coordinator     | SDK authority or operator credential appropriate to the route     | Direct table mutation and unsigned lifecycle publication               |\n| Audit           | Signed events and authenticated operator search                   | Mutable evidence and untrusted stream payloads                         |\n\n## Protected Request Boundary\n\nGateway denies before upstream dispatch when it cannot establish a valid, unexpired, unreplayed, unrevoked mandate; a resource header and binding; an allowed operation and scope; a safe path and host; and a successful STS exchange. It strips caller authorization before applying the configured upstream credential.\n\nGateway is an HTTP proxy. It streams HTTP responses, including SSE, while rechecking revocation. It strips hop-by-hop upgrade headers and does not proxy WebSockets. Protect WebSockets in process with the verification engine or an adapter.\n\n## Management Boundary\n\nThe console is the human management surface. Admin SDK and Control API are automation surfaces. The runtime CLI remains local lifecycle and workload launch only. A Control gate or API outage must not prevent `caracal status`, `down`, or `up` from operating locally.\n\nThe system-zone viewer adds a console-side read-only boundary: mutation controls are disabled and non-read requests are blocked. It is a transparency surface, not an alternative management route.\n\n## Fail-Closed Expectations\n\n- Gateway and STS deny when authority, revocation, policy, or service proof cannot be established.\n- Published modes do not permit `JTI_FAIL_OPEN` and require integrity keys.\n- Postgres row-level security denies cross-zone access without valid context.\n- Control denies when disabled, unauthenticated, replayed, rate-limited, out of scope, or unable to record required audit evidence.\n- Audit rejects integrity failures rather than treating them as trustworthy evidence.\n\nFor deployment controls, use [Harden Production](/operations/tls-hardening/). For exact Gateway behavior, use [Proxy Through Gateway](/api/gateway/)."},{"id":"/v0.2/runtime-console","title":"Operate Runtime and the Web Console","url":"https://docs.caracal.run/v0.2/runtime-console/","concepts":"","body":"Use this section after installation. It follows the work in the order an operator normally performs it, without mixing runtime lifecycle with product management.\n\n## Complete an Operator Journey\n\n1. [Choose the Right Surface](/runtime-console/cli-and-console/). Use `caracal` for local lifecycle and process launch, the web console for human management, and the Control API or Admin SDK for automation.\n2. [Start and Check the Stack](/runtime-console/stack/). Start services, wait for readiness, and open the packaged console.\n3. [Control Console Access](/runtime-console/console-access/). Admit the people who may sign in.\n4. [Use the Web Console](/runtime-console/console/). Finish account onboarding, create a zone, and complete guided setup.\n5. [Configure Workloads](/runtime-console/config-file/). Create a Launcher workload and credential bindings, or configure an SDK profile.\n6. [Run Workloads](/runtime-console/runtime/). Launch a command with short-lived credentials.\n7. [Inspect Diagnostics and Audit](/runtime-console/observability/). Read dashboard posture, health, decisions, and request traces.\n8. [Manage Product Objects](/runtime-console/admin/) and [Manage Runtime Authority](/runtime-console/agents/). Maintain configuration and intervene in Subjects, Authority records, Sessions, Delegations, and Approvals.\n\n## Keep the Boundary Clear\n\n| Goal                                                                                             | Surface                  |\n| ------------------------------------------------------------------------------------------------ | ------------------------ |\n| Start, stop, check, upgrade, or purge the local stack                                            | `caracal`                |\n| Launch one process with injected credentials                                                     | `caracal run`            |\n| Manage access to the console host                                                                | `caracal allowlist`      |\n| Create or change zones, applications, providers, resources, policies, workloads, or Control keys | Web console              |\n| Inspect and intervene in runtime authority                                                       | Web console              |\n| Automate product management                                                                      | Control API or Admin SDK |\n\nThe runtime CLI does not provide zone, policy, session, delegation, approval, audit, or Control management commands. This keeps local lifecycle independent of admin tokens, selected zones, and product credentials.\n\n## Local Addresses\n\nAfter `caracal up`, open the packaged console at [http://localhost:3001](http://localhost:3001). Local services bind to loopback: API `3000`, Coordinator `4000`, STS `8080`, Gateway `8081`, and Audit `9090`.\n\nDo not call those service ports merely because they are reachable locally. The console, SDKs, Gateway, Control API, and documented API surfaces provide the supported paths for their respective tasks.\n\n## Next Step\n\nStart with [Choose the Right Surface](/runtime-console/cli-and-console/)."},{"id":"/v0.2/runtime-console/cli-and-console","title":"Choose the Right Surface","url":"https://docs.caracal.run/v0.2/runtime-console/cli-and-console/","concepts":"","body":"Choose a surface from the outcome you need, not from where a similarly named object happens to appear.\n\n## Decision Table\n\n| You need to                                             | Use                      | Why                                                                                                                      |\n| ------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------------------------ |\n| Start, stop, inspect, upgrade, or reset a local runtime | `caracal`                | These operations concern local processes and storage.                                                                    |\n| Launch a command with scoped credentials                | `caracal run`            | It authenticates a Launcher workload and injects its configured bindings.                                                |\n| Admit, suspend, or remove a console user                | `caracal allowlist`      | Access is host-level auth configuration, not zone product state.                                                         |\n| Create or change product objects                        | Web console              | It provides selected-zone context, validation, guided setup, and audited secret handling.                                |\n| Investigate or intervene in live authority              | Web console              | Subjects, Authority records, Sessions, Delegations, Approvals, Audit, and Diagnostics are structured operator workflows. |\n| Manage product state from CI or another trusted host    | Control API or Admin SDK | These are authenticated automation surfaces.                                                                             |\n\n## Runtime CLI Boundary\n\nThe top-level runtime commands are lifecycle and launch commands:\n\n```text\nup  down  status  upgrade  purge  allowlist  run  web\n```\n\n`web` is a development launcher for the console UI and auth backend. It is not a product-management command. The packaged console already runs as part of `caracal up` and is opened in a browser at `http://localhost:3001`.\n\nDo not look for top-level commands to create zones, policies, applications, resources, workloads, Sessions, Delegations, Approvals, or Control keys. Use the console for human work. Use the Admin SDK or Control API for automation.\n\n## Console Versus Automation\n\nUse the web console when a person needs to follow guided setup, review selected-zone validation, reveal a secret with an audit record, activate policy, inspect a trace, decide an eligible Approval, or intervene in a Session.\n\nUse automation only from a trusted operator environment. The Admin SDK provides direct management APIs. The optional Control API provides zone-bound, scoped, replay-protected remote invocation. Neither surface starts Docker or launches workload processes.\n\nNever give workload code root admin, Coordinator, Control, or secret-store credentials. Workloads authenticate with application, SDK, or Launcher workload credentials.\n\n## If the Wrong Surface Seems Necessary\n\n| Symptom                                                | Action                                                                              |\n| ------------------------------------------------------ | ----------------------------------------------------------------------------------- |\n| A zone or policy command is absent from `caracal`      | Open the web console; the absence is intentional.                                   |\n| A lifecycle command asks for a zone or admin token     | Treat it as a boundary violation.                                                   |\n| CI needs repeatable configuration                      | Use the Admin SDK or Control API, not browser scripting.                            |\n| The console is unavailable but lifecycle must continue | Use `caracal status`, `down`, or `up`; lifecycle does not depend on console access. |\n\n## Next Step\n\n[Start and Check the Stack](/runtime-console/stack/)."},{"id":"/v0.2/runtime-console/stack","title":"Start and Check the Stack","url":"https://docs.caracal.run/v0.2/runtime-console/stack/","concepts":"","body":"## Start\n\n```bash\ncaracal up\ncaracal status --ready\n```\n\n`caracal up` starts the stack in the background. Development mode builds local images; release modes use the installed runtime assets. Continue only when `status --ready` exits `0`.\n\n`caracal status` checks liveness. `caracal status --ready` also checks service dependencies. A service can be healthy while Postgres, Redis, policy state, or another required dependency is not ready.\n\nFor machine-readable checks:\n\n```bash\ncaracal status --ready --json\n```\n\n## Open the Packaged Console\n\nOpen [http://localhost:3001](http://localhost:3001). Compose maps loopback port `3001` to the combined web/auth service on container port `3002`.\n\nThe other loopback bindings are API `3000`, Coordinator `4000`, STS `8080`, Gateway `8081`, and Audit `9090`. They are useful for documented clients and probes, not substitutes for console workflows.\n\n## Stop or Reset\n\n```bash\ncaracal down\n```\n\nThis stops the stack while preserving volumes. `caracal down -v` also removes Compose volumes. Use `caracal purge` only when intentionally removing selected local artifacts or all local runtime state. A destructive reset can invalidate generated tokens and remove product data; rebuild zones and product objects through the console afterward.\n\n## Recover Common Startup Failures\n\n| Result                                     | Meaning                                                              | Next action                                                                   |\n| ------------------------------------------ | -------------------------------------------------------------------- | ----------------------------------------------------------------------------- |\n| `status` fails                             | At least one service is not live.                                    | Inspect container/service logs and port conflicts.                            |\n| `status` passes but `status --ready` fails | A live service cannot satisfy a dependency or readiness threshold.   | Open Diagnostics if available; otherwise inspect the failing readiness probe. |\n| Console does not open but readiness passes | Host port `3001` is occupied or the web service failed separately.   | Check the web service and local listener.                                     |\n| Credentials fail after reset               | A shell variable or file contains a credential from the prior state. | Use freshly generated local secret files or rotate the affected credential.   |\n\nFor infrastructure diagnosis, use [Debug Infrastructure Issues](/operations/debugging/).\n\n## Development Console Launcher\n\n`caracal web` is for console development. It stops the packaged web container to avoid a port collision, starts the local UI and auth backend, and leaves the packaged container stopped when it exits. Run `caracal up` afterward to restore the packaged console.\n\n`caracal web --allow-offline` permits UI or sign-in development while runtime services are unavailable. Product views still cannot perform their backend work.\n\n## Next Step\n\n[Control Console Access](/runtime-console/console-access/)."},{"id":"/v0.2/runtime-console/console","title":"Use the Web Console","url":"https://docs.caracal.run/v0.2/runtime-console/console/","concepts":"","body":"## Open the Correct Console\n\nFor normal local operation, open [http://localhost:3001](http://localhost:3001) after `caracal up`. This is the packaged console served with its session-guarded backend-for-frontend.\n\nUse `caracal web` only while developing the console. That development launcher is unrelated to the console's **Launcher** workload page:\n\n| Name                    | Purpose                                                                                                                |\n| ----------------------- | ---------------------------------------------------------------------------------------------------------------------- |\n| `caracal web`           | Starts local web-development processes. It does not create or run governed workloads.                                  |\n| **Services → Launcher** | Creates workload identities and credential bindings consumed by `caracal run`. It does not start a development server. |\n\n## Sign In and Finish Account Onboarding\n\nAfter admission through the host allowlist, sign in with a configured method. A first account visit collects the operator profile and creates or selects the first ordinary zone. This account onboarding is distinct from in-zone guided setup.\n\nThe console keeps the active zone in the URL and profile state. Zone-scoped pages require a selected zone.\n\n## Complete Guided Setup\n\nGuided setup reads live zone inventory and opens the real product forms. It teaches one deny-by-default path:\n\n1. **Register an application** such as Son of Anton. This is the identity requesting access.\n2. **Connect a provider** such as Hooli OIDC. This supplies upstream credentials at runtime.\n3. **Define a resource** such as `resource://pipernet`, including its scopes and upstream.\n4. **Activate a policy** that authorizes the intended application and scopes.\n5. **Verify** from the dashboard, policy simulation, Sessions, and Audit.\n\nThe guide marks a step complete from actual zone state. It does not create a Launcher workload. After access is enforcing, open **Services → Launcher** to configure `caracal run`.\n\nFor field-level guidance, use [Define Resources and Providers](/guides/resources-providers/) and [Activate a Policy Set](/guides/activate-policy-set/).\n\n## Read the Console by Task\n\n| Task                                             | Console area                  |\n| ------------------------------------------------ | ----------------------------- |\n| Review posture and recent activity               | **Dashboard**                 |\n| Configure applications, providers, and resources | **Access**                    |\n| Author, simulate, and activate policy            | **Policy**                    |\n| Investigate Subjects and authority               | **Subjects**                  |\n| Intervene in Sessions, Delegations, or Approvals | **Runtime**                   |\n| Search decisions and run health checks           | **Audit** and **Diagnostics** |\n| Configure workload launch                        | **Services → Launcher**       |\n| Configure scoped remote automation               | **Services → Control**        |\n\n## System-Zone Read-Only View\n\nCaracal's reserved system zone is not offered in the normal active-zone switcher. The Settings entry opens it in a separate transparency view. In that tab, editable fields and mutating controls are disabled, every non-GET/HEAD console API request is blocked with `system_zone_read_only`, and settings, Operator, and other hide-locked routes are unavailable.\n\nDo not use the system-zone viewer for customer or application configuration. Return to an ordinary zone to make changes.\n\n## Secret Handling\n\nApplication and Launcher workload secrets are held in Secret Store custody. Copy a secret at creation or use the object's reveal action later. Every reveal is audited. Rotation invalidates the current secret, so update the consuming secret store or file immediately.\n\nProvider secrets are entered during supported create or rotation flows and remain masked. The console does not write revealed values to the workload host.\n\n## Caracal Operator\n\nOpen **Caracal Operator** from the utility rail or command palette. Each conversation runs in **Ask** mode for read-only investigation or **Agent** mode for answers, live reads, and change plans. A mutating plan is validated and previewed against current state, then waits for Approval before it is revalidated and applied. Conversation memory records activity; live reads and execution previews remain the source of truth for current state.\n\nConfigure natural-language models under **Settings → AI Operator → Models**. The page can add, edit, test, rotate, and delete OpenAI-compatible providers. A new or rotated key is sent once, sealed into the reserved `caracal.sys` Zone, and never returned. Caracal routes model calls through the governed Gateway. The Operator can still expose deterministic catalog and plan behavior when no model provider is configured, but natural-language assistance remains unavailable.\n\n## Next Step\n\n[Configure Workloads](/runtime-console/config-file/)."},{"id":"/v0.2/runtime-console/console-access","title":"Control Console Access","url":"https://docs.caracal.run/v0.2/runtime-console/console-access/","concepts":"","body":"The runtime host controls who may register and sign in. Zone roles and product authority are separate concerns applied after authentication.\n\n## Admit a Person or Domain\n\n```bash\ncaracal allowlist add richard.hendricks@piedpiper.example\ncaracal allowlist add @piedpiper.example\ncaracal allowlist list\n```\n\nEntries are exact email addresses or `@domain` suffixes. An exact entry takes precedence over a domain entry. While any active or locked entries exist, the allowlist is the sign-in authority. With an empty list, registration follows deployment configuration: open in development and closed in production unless configured otherwise.\n\n## Suspend, Restore, or Remove Access\n\n```bash\ncaracal allowlist lock monica.hall@piedpiper.example\ncaracal allowlist unlock monica.hall@piedpiper.example\ncaracal allowlist remove gavin.belson@hooli.example\n```\n\n| Action              | Sign-in effect                              | Account effect                                                                                                 |\n| ------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |\n| `lock`              | Blocks sign-in and console requests.        | Revokes sessions; retains the account and product data.                                                        |\n| `unlock`            | Restores access.                            | Retains the existing account.                                                                                  |\n| `remove`            | Blocks access until explicitly re-admitted. | Revokes sessions and erases sign-in records on the person's next contact; zone data and audit evidence remain. |\n| `add` after removal | Allows registration again.                  | Starts a fresh admission.                                                                                      |\n\nRemoval is stored as an explicit marker. A missing, empty, or unreadable allowlist may deny access, but it does not trigger account erasure.\n\n## Interpret Access Denial\n\nThe browser deliberately shows the same access-denied page when an address was never admitted, is locked, or was removed. This prevents the UI from revealing account status. Runtime auth logs retain the concrete reason for operators.\n\nTo resolve a denial:\n\n1. Run `caracal allowlist list` on the runtime host.\n2. Unlock a locked entry, or add an intended address.\n3. Check the configured email/password or OAuth sign-in method.\n4. If the allowlist is empty, check `CARACAL_OPEN_REGISTRATION` and the deployment environment.\n\nBeing admitted to the console does not make a person a workload Subject. Console accounts authenticate operators. Subjects are identities that applications act for and are observed inside a selected zone.\n\n## Next Step\n\n[Use the Web Console](/runtime-console/console/)."},{"id":"/v0.2/runtime-console/config-file","title":"Configure Workloads","url":"https://docs.caracal.run/v0.2/runtime-console/config-file/","concepts":"","body":"Caracal supports two configuration paths. Choose one from the credential lifecycle your process needs.\n\n| Need                                                                | Configure                                          |\n| ------------------------------------------------------------------- | -------------------------------------------------- |\n| Start a bounded command with environment-injected credentials       | A workload and bindings on **Services → Launcher** |\n| Renew credentials during a long-running process or use SDK Sessions | An SDK profile or SDK environment configuration    |\n\n## Configure a Launcher Workload\n\n1. Open **Services → Launcher** in the intended zone.\n2. Create a workload identity, for example Son of Anton.\n3. Add bindings. Each binding selects an environment variable, resource, scopes, credential type, and unavailable behavior.\n4. Reveal the workload secret. The reveal is recorded in admin audit.\n5. Store the secret on the host that will run the command.\n6. Copy the exact launch command shown by the page.\n\nThe launching host carries only workload identity and proof:\n\n| Variable                       | Meaning                                        |\n| ------------------------------ | ---------------------------------------------- |\n| `CARACAL_WORKLOAD_ID`          | Required workload identifier.                  |\n| `CARACAL_WORKLOAD_SECRET`      | Inline secret for local development.           |\n| `CARACAL_WORKLOAD_SECRET_FILE` | Explicit mounted secret-file path.             |\n| `CARACAL_STS_URL`              | STS override for custom or remote deployments. |\n\nSet only one secret source. In local dev and stable modes, omitting both secret variables makes the runtime read `<Caracal config dir>/runtime/<workload_id>/secret`. Production launches require an explicit secret source.\n\n```bash\nmkdir -p ~/.config/caracal/runtime/<workload_id>\nprintf '%s' '<workload secret>' > ~/.config/caracal/runtime/<workload_id>/secret\nchmod 600 ~/.config/caracal/runtime/<workload_id>/secret\n```\n\nThe config directory defaults to `$XDG_CONFIG_HOME/caracal` or `~/.config/caracal` on Linux, `~/Library/Application Support/Caracal` on macOS, and `%APPDATA%\\Caracal` on Windows. `CARACAL_CONFIG_HOME` overrides it.\n\nA binding with no selected scopes requests the resource's configured scope set. Required binding failure prevents launch. Configure an optional binding's unavailable behavior on the Launcher page when the process may continue without it.\n\n## Configure an SDK Profile\n\nSDK loaders use `CARACAL_CONFIG` when it names an existing file; otherwise they read explicit runtime environment configuration. They do not search the current directory or home directory for a profile.\n\n```toml\nzone_id = \"<zone-id>\"\napplication_id = \"<application-id>\"\ndefault_ttl_seconds = 900\n\n[[credentials]]\nresource = \"resource://pipernet\"\nupstream_prefix = \"https://api.pipernet.example/v1\"\n```\n\nCommon profile fields are `sts_url`, `gateway_url`, `coordinator_url`, `zone_id`, `application_id`, `app_client_secret_file`, `app_client_secret`, `default_ttl_seconds`, `credentials`, and `optional_credentials`.\n\nLauncher binding fields such as environment name and unavailable behavior do not belong in an SDK profile. For language-specific setup, use [Choose an SDK or Package](/sdks/).\n\n## Security Checks\n\n- Keep workload and application secret files owner-only and explicitly mounted in production.\n- Do not mount operator secret directories into workload containers.\n- Non-local insecure STS URLs are rejected unless the deployment explicitly permits them.\n- Binding targets cannot be process-loader variables such as `NODE_OPTIONS`, `LD_PRELOAD`, `LD_LIBRARY_PATH`, or `DYLD_*`.\n\n## Next Step\n\n[Run Workloads](/runtime-console/runtime/)."},{"id":"/v0.2/runtime-console/runtime","title":"Run Workloads","url":"https://docs.caracal.run/v0.2/runtime-console/runtime/","concepts":"","body":"`caracal run` authenticates a Launcher workload, fetches its bindings, mints credentials, injects them into a child environment, and returns the child's result.\n\n```bash\ncaracal run -- python3 agent.py\n```\n\nUse `--` before a child command that accepts flags.\n\n## What Happens Before Spawn\n\n```text\nworkload proof → fetch bindings → mint credentials → inject environment → spawn child\n```\n\nFor each binding, the runtime validates the environment name and secret-file permissions, then requests only the configured resource and scopes. A required failure prevents the child from starting. Optional bindings follow their configured unavailable behavior.\n\nStatic upstream credentials such as API keys remain as powerful as the upstream value itself even when the Caracal mint decision was scope-bounded. Prefer short-lived provider tokens or Caracal mandates where supported.\n\n## Approval Holds\n\nIf STS returns `interaction_required`, the runtime prints an `approval_required` notice containing the hold and binding, long-polls the hold, and retries the mint once after approval. When the response has no usable expiry, it uses a five-minute fallback. Rejection, expiry, timeout, or consumption by another request prevents spawn.\n\nAn operator can inspect eligible holds in the console **Approvals** page. Subject-only holds are visible there but can be decided only by the application's end user.\n\n## Child Environment\n\nThe child receives configured credential variables and, when the provider reports expiry, `<ENV>_EXPIRES_AT` in epoch seconds. It also receives a narrow allowlist of ordinary process variables such as `PATH`, home, temporary-directory, locale, terminal, CI, environment name, and OS system variables.\n\nLauncher configuration variables are removed. In particular, `CARACAL_WORKLOAD_SECRET`, admin credentials, and most `CARACAL_*` values do not propagate. `CARACAL_ENV` is retained because it identifies the deployment environment.\n\n## Credential Lifetime\n\n`caracal run` mints once at startup and does not renew. Use it for bounded processes whose work completes before the injected credentials expire. Use a Caracal SDK when a service must re-exchange credentials, manage Sessions, or continue indefinitely.\n\nEvery manifest fetch and mint is correlated in audit. Rotating the workload secret blocks later launches that still use the prior value. Revocation blocks Gateway-routed use immediately while already injected provider credentials remain subject to their own expiry and provider behavior.\n\n## Signals and Exit Results\n\nThe runtime forwards `SIGINT`, `SIGTERM`, `SIGHUP`, and `SIGQUIT`, waits for the child, and does not leave it running.\n\n| Exit result     | Meaning                                                               |\n| --------------- | --------------------------------------------------------------------- |\n| Child exit code | The child started and its result is returned unchanged.               |\n| `1`             | Configuration, exchange, validation, or Approval failed before spawn. |\n| `127`           | The command could not be spawned.                                     |\n| `128 + N`       | The child ended because of signal number `N`.                         |\n\n## Next Step\n\n[Inspect Diagnostics and Audit](/runtime-console/observability/)."},{"id":"/v0.2/runtime-console/admin","title":"Manage Product Objects","url":"https://docs.caracal.run/v0.2/runtime-console/admin/","concepts":"","body":"Use the console for human management after guided setup. Keep work inside the active ordinary zone.\n\n## Maintain the Access Chain\n\n```text\napplication + provider + resource + active policy → authorized access\n```\n\n| Area                    | Operator task                                                                                                            |\n| ----------------------- | ------------------------------------------------------------------------------------------------------------------------ |\n| **Applications**        | Register managed application identities and rotate or reveal client secrets.                                             |\n| **Providers**           | Configure the upstream credential mode and rotate provider secrets.                                                      |\n| **Resources**           | Define `resource://` identifiers, operations/scopes, upstream URLs, Gateway application bindings, and provider bindings. |\n| **Policies**            | Author and validate policy versions.                                                                                     |\n| **Policy Sets**         | Compose, simulate, and activate the policy that STS evaluates.                                                           |\n| **Services → Launcher** | Create workload identities and launch bindings.                                                                          |\n| **Services → Control**  | Enable scoped remote automation and manage Control credentials.                                                          |\n\nFollow [Author Policy Data](/guides/author-policy/) and [Activate a Policy Set](/guides/activate-policy-set/) for the canonical policy workflow rather than duplicating it here.\n\n## Treat Secrets as Audited Operations\n\nApplication and workload secrets are sealed after creation. A reveal action retrieves the current value from Secret Store custody and writes an admin-audit record. A rotation invalidates the previous value. Copy the replacement directly into the consuming secret store; do not place it in source, screenshots, chat, or audit annotations.\n\nProvider secret fields remain masked and are accepted only by the supported credential create or rotation flow.\n\n## Hand Off to Automation\n\nUse the Admin SDK for trusted direct management clients. Use **Services → Control** when remote automation needs a zone-bound credential and the optional Control endpoint is enabled.\n\nControl credentials are scoped to management nouns and verbs. Control calls are replay-protected, rate-limited, and audited. They do not receive the root admin token. Control is not a top-level `caracal` command and does not manage stack lifecycle.\n\nSee [Automate Management](/services/control/) for dependency and failure implications, and [Use the Admin API](/api/control-plane/) for endpoint reference.\n\n## System Zone\n\nThe reserved system-zone viewer is read-only. It exists to expose Caracal-owned state for transparency, not as a management target. Return to an ordinary selected zone before creating, editing, revealing, rotating, deciding, or deleting anything.\n\n## Next Step\n\n[Manage Runtime Authority](/runtime-console/agents/) for Subjects, Authority records, Sessions, Delegations, and Approvals."},{"id":"/v0.2/runtime-console/observability","title":"Inspect Diagnostics and Audit","url":"https://docs.caracal.run/v0.2/runtime-console/observability/","concepts":"","body":"Start at the dashboard, then choose Diagnostics for platform posture or Audit for a specific decision.\n\n## Read the Dashboard\n\nThe selected-zone dashboard summarizes product readiness, pending Approvals, object counts, and recent activity. Use it to decide which detail workspace to open next. A count is navigation context, not proof of correctness; verify policy with simulation and verify a real request in Audit.\n\n## Run Diagnostics\n\n**Diagnostics** runs shared Doctor checks across:\n\n| Group     | What it establishes                                                              |\n| --------- | -------------------------------------------------------------------------------- |\n| Health    | API reachability, management authentication, and clock alignment.                |\n| Readiness | Service readiness and operator metrics for STS, Gateway, Audit, and Coordinator. |\n| Zones     | Visible zones, resources, active policy state, and audit queryability.           |\n| Preflight | Local secrets, keys, TLS material, Postgres, and Redis reachability.             |\n\n`caracal status --ready` answers whether the stack can accept work. Diagnostics goes deeper: policy compilation, audit integrity, event backlog, and clock skew. Treat audit chain mismatch as a security incident. Treat dead outbox rows, DLQ entries, stale pending events, and large lag as propagation failures even if HTTP health still passes.\n\n## Trace a Request\n\n1. Open **Audit** in the correct zone.\n2. Choose **Activity** for authority/resource events or **Admin** for management changes.\n3. Filter by request ID when one is available. Otherwise narrow by time, decision, event type, application, Authority record, or Session.\n4. Open the event group or decision trace.\n5. Read the resource, Subject, requested scopes, determining policy, diagnostics, and result.\n\nAudit evidence and live state answer different questions. Audit shows what happened and why. Subjects, Sessions, Delegations, and Approvals show what authority remains live.\n\nSecret reveal and rotation actions appear in admin audit. Secret values do not belong in the event payload.\n\n## Follow Dependency Failures\n\n| Symptom                        | First check                     | Then                                           |\n| ------------------------------ | ------------------------------- | ---------------------------------------------- |\n| Management pages fail          | API readiness and auth          | Postgres, Redis, and API outbox                |\n| Mandate issuance fails         | STS readiness and policy state  | Postgres, Redis invalidation, keys             |\n| Gateway denies before upstream | Gateway trace and STS readiness | Binding, mandate, revocation, upstream safety  |\n| Audit event is delayed         | Audit readiness                 | Redis lag, pending entries, DLQ, replay volume |\n| Session state is stale         | Coordinator readiness           | Postgres, Redis, outbox, lease sweepers        |\n\nUse [Troubleshoot by Symptom](/operations/troubleshooting/) for the canonical symptom-first workflow.\n\n## Next Step\n\n[Manage Runtime Authority](/runtime-console/agents/)."},{"id":"/v0.2/runtime-console/agents","title":"Manage Runtime Authority","url":"https://docs.caracal.run/v0.2/runtime-console/agents/","concepts":"","body":"Use these console workspaces when access is already configured and a person needs to understand or interrupt live authority. They are not runtime CLI workflows.\n\n## Keep the Identities Distinct\n\n| Object           | Meaning                                            | Identifier relationship                                                         |\n| ---------------- | -------------------------------------------------- | ------------------------------------------------------------------------------- |\n| Console account  | A human authenticated to operate the console.      | Not a workload Subject.                                                         |\n| Application      | A confidential client that asks STS for authority. | May act for itself or a federated Subject.                                      |\n| Subject          | The stable JWT `sub` an application acts for.      | Grouping key in the Subjects workspace.                                         |\n| Authority record | One STS-issued authority anchor.                   | Its ID is carried as protocol `session_id`; it is not a Coordinator Session ID. |\n| Session          | A governed execution owned by Coordinator.         | Its ID is carried as `agent_session_id`.                                        |\n| Delegation       | Bounded authority from one Session to another.     | Anchored by source and target Session IDs.                                      |\n| Approval         | A human-decision hold raised for a requested action. | May link to Subject, Authority record, Session, Resource, and scopes.           |\n\n## Investigate a Subject\n\nOpen **Subjects** and search by identity or kind. A Subject detail groups Authority record history, governed Sessions, pending Approvals, and provider connections.\n\nUse the Subject kill switch for credential compromise or offboarding when individual cleanup would be unsafe. The implemented cascade revokes live Authority records, terminates linked Sessions, revokes Delegations, and revokes provider connections. Confirm the selected Subject and zone before acting.\n\nSubjects are supplied by the application's identity system or fall back to application identity when no accepted subject token is exchanged. The console does not authenticate those end users.\n\n## Intervene in Sessions\n\nOpen **Sessions** to filter active, suspended, or terminated executions and inspect their tree, lifecycle, leases, invocations, Authority linkage, and inbound or outbound Delegations.\n\n- **Suspend** pauses an active Session subtree.\n- **Resume** returns a suspended subtree to active operation.\n- **Terminate** is terminal and stops the Session subtree.\n\nTask Sessions expire by TTL. Service Sessions use heartbeat leases. A Session can reference an Authority record for attribution without changing the IDs into the same object.\n\n## Review and Revoke Delegation\n\nThe Sessions workspace exposes Delegation views and lineage. Inspect source and target Sessions, resource and scope bounds, constraints, expiry, and traversal state before revocation.\n\nRevocation ends the delegated authority represented by that edge. Requests must still pass the consuming verifier's revocation checks; use Audit to confirm denial after intervention.\n\n## Decide Approvals\n\n**Approvals** lists holds in these implemented states:\n\n| State      | Meaning                                                                          |\n| ---------- | -------------------------------------------------------------------------------- |\n| `pending`  | Waiting for an eligible approver until expiry.                                   |\n| `approved` | An approver satisfied the hold; the requesting exchange has not consumed it yet. |\n| `consumed` | A retry used the approval; it cannot be reused.                                  |\n| `rejected` | An approver denied the request.                                                  |\n| `expired`  | The decision window ended.                                                       |\n\nA pending hold is decidable in the console only when its approver class is **Zone operator** or **Operator or end user**. An **End user only** hold remains visible but has no console approve/reject action. Before deciding, inspect the requester, privacy mode, resource, scopes, Session lineage, expiry, and recent matching decisions.\n\nApproval does not itself issue a credential. The waiting client retries the mint, which consumes the hold exactly once. A second consumer receives an already-consumed failure.\n\n## Correlate Every Intervention\n\nUse links from Subject, Session, Delegation, and Approval details into Audit. Filter by request ID, Authority record ID, or Session ID to confirm the original decision and the effect of a revoke, suspend, terminate, approve, or reject action.\n\n## Related Pages\n\n- [Sessions and Revocation](/concepts/sessions-revocation/)\n- [Session Delegation](/concepts/delegation/)\n- [Approvals](/concepts/step-up/)\n- [Inspect Diagnostics and Audit](/runtime-console/observability/)"},{"id":"/v0.2/sdks","title":"Choose an SDK or Package","url":"https://docs.caracal.run/v0.2/sdks/","concepts":"","body":"Use SDKs when you need package-level details after a guide. The [Guides](/guides/) section shows task workflows; this section explains package names, install commands, public APIs, runtime requirements, and where each package fits.\n\nCaracal has three separate integration boundaries: application SDKs create and carry authority, the Admin package changes product state, and verification packages enforce inbound mandates. Do not use an application credential for administration or treat context propagation as authentication.\n\n## Choose by What You Are Building\n\n| I am building...                     | Start here                                           | Then read                                                                                             |\n| ------------------------------------ | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |\n| TypeScript app or agent workflow     | [TypeScript SDK](./typescript/)                      | [OAuth Package](./oauth/), [Verify Package](./verify/)                                                |\n| Python app, agent, or ASGI service   | [Python SDK](./python/)                              | [ASGI Adapter](./adapters/asgi/), [FastMCP Adapter](./adapters/fastmcp/), [Verify Package](./verify/) |\n| Go service or agent workflow         | [Go SDK](./go/)                                      | [Go net/http Adapter](./adapters/nethttp/)                                                            |\n| Express resource server              | [Express Adapter](./adapters/express/)               | [Verify Package](./verify/), [Redis Revocation Store](./backends/redis/)                              |\n| FastAPI or Starlette resource server | [ASGI Adapter](./adapters/asgi/)                     | [Verify Package](./verify/), [Redis Revocation Store](./backends/redis/)                              |\n| FastMCP server                       | [FastMCP Adapter](./adapters/fastmcp/)               | [Verify Package](./verify/)                                                                           |\n| Custom verification boundary         | [Verification Layer Overview](./verification-layer/) | [Identity Package](./identity/), [Revocation Package](./revocation/)                                  |\n| Admin or provisioning automation     | [Admin Package](./admin/)                            | [Use the Admin API](/api/control-plane/)                                                              |\n| Shared revocation storage            | [Redis Revocation Store](./backends/redis/)          | [Revocation Package](./revocation/)                                                                   |\n\n## Package Map\n\n| Area                   | TypeScript / Node             | Python                       | Go                                                             |\n| ---------------------- | ----------------------------- | ---------------------------- | -------------------------------------------------------------- |\n| App SDK                | `@caracalai/sdk`              | `caracalai-sdk`              | `github.com/garudex-labs/caracal/packages/sdk/go`              |\n| Identity verification  | `@caracalai/identity`         | `caracalai-identity`         | `github.com/garudex-labs/caracal/packages/identity/go`         |\n| OAuth token exchange   | `@caracalai/oauth`            | `caracalai-oauth`            | `github.com/garudex-labs/caracal/packages/oauth/go`            |\n| Revocation store       | `@caracalai/revocation`       | `caracalai-revocation`       | `github.com/garudex-labs/caracal/packages/revocation/go`       |\n| Verification engine    | `@caracalai/verify`           | `caracalai-verify`           | `github.com/garudex-labs/caracal/packages/verify/go`           |\n| Express adapter        | `@caracalai/express`          | Not applicable               | Not applicable                                                 |\n| FastMCP adapter        | `@caracalai/fastmcp`          | `caracalai-fastmcp`          | Not applicable                                                 |\n| ASGI adapter           | Not applicable                | `caracalai-asgi`             | Not applicable                                                 |\n| net/http adapter       | Not applicable                | Not applicable               | `github.com/garudex-labs/caracal/packages/adapters/nethttp/go` |\n| Redis revocation store | `@caracalai/revocation-redis` | `caracalai-revocation-redis` | `github.com/garudex-labs/caracal/packages/backends/redis/go`   |\n\n## Runtime Requirements\n\n| Ecosystem | Current package target                                                                                                |\n| --------- | --------------------------------------------------------------------------------------------------------------------- |\n| Node.js   | Node `>=22` for published TypeScript packages that declare an engine.                                                 |\n| Python    | Python `>=3.12` for published Python packages.                                                                        |\n| Go        | Module paths under `github.com/garudex-labs/caracal/packages/...`; current modules declare Go `1.26` where specified. |\n\nGo submodules may resolve to pseudo-versions because repository CalVer release tags are not semantic-version tags for nested Go modules. Adapter availability is intentionally ecosystem-specific; parity covers behavior exposed by all three application facades, not identical framework packages.\n\n## Related Guides\n\n- [Add SDK to Your App](/get-started/add-sdk-to-your-app/)\n- [Connect Your App with the SDK](/tutorials/connect-an-agent/)\n- [Protect a Gateway-Routed HTTP API](/guides/protect-gateway-http/)\n- [Implement Multi-Agent Delegation](/guides/delegation/)"},{"id":"/v0.2/sdks/typescript","title":"TypeScript SDK","url":"https://docs.caracal.run/v0.2/sdks/typescript/","concepts":"","body":"`@caracalai/sdk` is the main TypeScript package for Session lifecycle, Delegation, Gateway routing, and Caracal context propagation.\n\nUse it in application code. Do not use it to create zones, applications, policies, resources, or grants; those operations belong to [Admin Package](/sdks/admin/).\n\n## Install\n\n```bash\nnpm install @caracalai/sdk\n```\n\nThe package is ESM-only and targets Node `>=22`. To consume it from a CommonJS project, use a dynamic `await import('@caracalai/sdk')` or set `\"type\": \"module\"` in your `package.json`.\n\n## Connect and Configure\n\n| API                                 | Use it when                                                                                              |\n| ----------------------------------- | -------------------------------------------------------------------------------------------------------- |\n| `new Caracal()`                     | Use normal deployment configuration: exactly `CARACAL_CONFIG` when set, otherwise `CARACAL_*` variables. |\n| `Caracal.fromClientSecret(options)` | Supply one complete static client-secret configuration directly.                                         |\n\n```ts\nimport { Caracal } from '@caracalai/sdk'\n\nconst caracal = new Caracal()\n```\n\nThe constructor never searches home directories or default profile paths. Multiple credential modes fail at startup instead of using precedence. Explicit environment mappings, profile loading, dynamic credential resolvers, custom transports, and raw configuration live in `@caracalai/sdk/advanced`.\n\n`Caracal.fromClientSecret` requires the static `zoneId`/`applicationId`/`clientSecret` triple. Resources are optional for per-resource application transports; Session and lifecycle operations fail clearly when no lifecycle resource audience is configured. It refreshes application subject tokens automatically.\n\n## Core methods\n\n| Method                                         | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |\n| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `session(fn, options?)`                        | Run `fn` inside a governed Session; `fn` receives the bound context, including its Session ID. Pass `authority: Authority.narrow(...)` to bound authority and `task` to describe the work. Retry protection is automatic; ordinary code should omit `idempotencyKey`. See [Safe Retries and Idempotency](/guides/idempotency/) for durable-source redelivery.                                                                                                                                  |\n| `startSession(options?)`                       | Start a governed Session that outlives a block; auto-renews its generation-fenced lease and returns a handle with `heartbeat()`, `deadlineAt`, `leaseGeneration`, and `close()`. Service lifetime is lease-only. Pass `authority: Authority.narrow(...)` with a positive Delegation TTL to bound the handle's authority. Retry protection is automatic.                                                                                                                                                  |\n| `attachSession(sessionId, options?)`           | Re-attach to an active persisted long-lived Session after a restart: atomically acquires a new generation, fences older holders, and returns the same handle `startSession` does.                                                                                                                                                                                                                                                                                                                  |\n| `delegate(options)`                            | Delegate a slice of the current Session's authority to an existing peer Session; returns the Delegation for the receiver to accept. A transient Coordinator failure is retried once under an idempotency key, so no duplicate Delegation is issued.                                                                                                                                                                                                                                            |\n| `revokeDelegation(delegationId)`               | Revoke a Delegation issued by this application.                                                                                                                                                                                                                                                                                                                                                                                    |\n| `acceptDelegation(delegationId, fn, options?)` | Present a received Delegation: bind a derived context carrying it while `fn` runs. Pass `{ validate: true }` to confirm with the Coordinator that the Delegation is live for the bound Session first.                                                                                                                                                                                                                   "},{"id":"/v0.2/sdks/python","title":"Python SDK","url":"https://docs.caracal.run/v0.2/sdks/python/","concepts":"","body":"`caracalai-sdk` is the main Python package for async Session lifecycle, Delegation, Gateway routing, and ASGI context propagation.\n\nUse it in application code. Product management belongs to [Admin Package](/sdks/admin/), and direct inbound enforcement belongs to an adapter or the verify package.\n\n## Install\n\n```bash\npip install caracalai-sdk\n```\n\nThe package requires Python `>=3.12`.\n\n## Connect and Configure\n\n| API                               | Use it when                                                                                              |\n| --------------------------------- | -------------------------------------------------------------------------------------------------------- |\n| `Caracal()`                       | Use normal deployment configuration: exactly `CARACAL_CONFIG` when set, otherwise `CARACAL_*` variables. |\n| `Caracal.from_client_secret(...)` | Supply one complete static client-secret configuration directly.                                         |\n\n```python\nfrom caracalai import Caracal\n\ncaracal = Caracal()\n```\n\nThe constructor never searches home directories or default profile paths. Multiple credential modes fail at startup. Explicit environment mappings, profile loading, dynamic credential resolvers, and raw configuration live in `caracalai.advanced`.\n\n## Core methods\n\n| Method                                                                                                      | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |\n| ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `async with caracal.session(...)`                                                                           | Run the block inside a governed Session - a bounded identity Caracal establishes around whatever the block executes; pass `authority=Authority.narrow(...)` to bound its authority and `task=` to record what the Session is for. Retry protection is automatic; ordinary code should omit `idempotency_key`. See [Safe Retries and Idempotency](/guides/idempotency/) for durable-source redelivery.                                                                                                                                                                          |\n| `await caracal.start_session(...)`                                                                          | Start a governed Session that outlives a block; auto-renews its generation-fenced lease and returns a handle with `heartbeat()`, `heartbeat_deadline_at`, `lease_generation`, and `aclose()`. Service lifetime is lease-only. Pass `authority=Authority.narrow(...)` with a positive Delegation TTL to bound the handle's authority. Retry protection is automatic.                                                                                                                                                  |\n| `await caracal.attach_session(session_id, ...)`                                                             | Re-attach to an active persisted long-lived Session after a restart: atomically acquires a new generation, fences older holders, and returns the same handle `start_session` does.                                                                                                                                                                                                                                                                                                                                                                                              |\n| `await caracal.delegate(...)`                                                                               | Delegate a slice of the bound Session's authority to an existing peer Session; returns the Delegation for the receiver to accept. A transient Coordinator failure is retried once under an idempotency key, so no duplicate Delegation is issued.                                                                                                                                                                                                                                                                                                                              |\n| `await caracal.revoke_delegation(delegation_id)`                                                            | Revoke a Delegation issued by this application.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |\n| `async with caracal.accept_delegation(delegation_id, validate=False)`           "},{"id":"/v0.2/sdks/go","title":"Go SDK","url":"https://docs.caracal.run/v0.2/sdks/go/","concepts":"","body":"The Go SDK provides Session lifecycle, Delegation, Gateway routing, and context propagation for Go services.\n\nUse it for application authority, not management CRUD. Use [Admin Package](/sdks/admin/) for control-plane automation.\n\n## Install\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/sdk/go\n```\n\n## Connect and Configure\n\n```go\nimport caracal \"github.com/garudex-labs/caracal/packages/sdk/go\"\n\nclient, err := caracal.New()\nif err != nil {\n\treturn err\n}\n```\n\n`New()` loads exactly `CARACAL_CONFIG` when set; otherwise it loads `CARACAL_*` environment variables. It never searches home directories or default profile paths. Multiple credential modes fail at startup.\n\n## Core methods\n\n| API                                                    | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |\n| ------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `New()`                                                | Use normal deployment configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |\n| `FromClientSecret(options)`                            | Supply one complete static client-secret configuration directly.                                                                                                                                                                                                                                                                                                                                                                                                                                         |\n| `FromEnv()`, `FromConfig(path)`, `FromCredentials(...)` | Advanced explicit loading, profile, and dynamic credential paths.                                                                                                                                                                                                                                                                                                                                                                                                                                         |\n| `client.Session(ctx, fn, opts...)`                     | Run `fn` inside a governed Session with a bound `context.Context`; set `SessionOptions.Authority` to bound its authority and `Task` to record what the Session is for. Retry protection is automatic; ordinary code should leave `IdempotencyKey` empty. See [Safe Retries and Idempotency](/guides/idempotency/) for durable-source redelivery.                                                                                                                                                         |\n| `client.StartSession(ctx, opts...)`                    | Start a governed Session that outlives a block; auto-renews its generation-fenced lease and returns a `*SessionHandle` with `Heartbeat`, `DeadlineAt`, `LeaseGeneration`, and `Close`. Retry protection is automatic.                                                                                                                                                                                                                                                                                    |\n| `client.AttachSession(ctx, sessionID, opts...)`        | Re-attach to an active persisted long-lived Session after a restart: atomically acquires a new generation, fences older holders, and returns the same handle `StartSession` does.                                                                                                                                                                                                                                                                                                                         |\n| `client.Delegate(ctx, opts)`                           | Delegate a slice of the current Session's authority to a peer Session; returns the Delegation for the receiver to accept. A transient Coordinator failure is retried once under an idempotency key, so no duplicate Delegation is issued.                                                                                                                                                                                                                                                                 |\n| `client.RevokeDelegation(ctx, delegationID)`           | Revoke a Delegation issued by this application.                                                                                                                                                                                                                                                   "},{"id":"/v0.2/sdks/verification-layer","title":"Verification Layer Overview","url":"https://docs.caracal.run/v0.2/sdks/verification-layer/","concepts":"","body":"Use this page when you are protecting an inbound resource-server boundary and need to choose the right package layer. Start with the highest-level adapter that fits your framework; use lower-level packages only when you are building a custom boundary.\n\nThe verification layer consumes `use=resource` mandates at a resource server. Gateway ingress consumes `use=gateway` mandates instead. A Session or lifecycle mandate must not be accepted at either boundary.\n\n## Which Layer Should I Use?\n\n| Need                                                      | Use                                                                                  |\n| --------------------------------------------------------- | ------------------------------------------------------------------------------------ |\n| Express route middleware                                  | [Express Adapter](/sdks/adapters/express/)                                           |\n| FastAPI/Starlette (ASGI) middleware                       | [ASGI Adapter](/sdks/adapters/asgi/)                                                 |\n| FastMCP server or tool authentication                     | [FastMCP Adapter](/sdks/adapters/fastmcp/)                                           |\n| Go `net/http` middleware                                  | [Go net/http Adapter](/sdks/adapters/nethttp/)                                       |\n| Framework-neutral bearer parsing and mandate verification | [Verify Package](/sdks/verify/)                                                      |\n| Custom JWT claim verification                             | [Identity Package](/sdks/identity/)                                                  |\n| Shared revocation checks                                  | [Revocation Package](/sdks/revocation/) plus [Redis Revocation Store](/sdks/backends/redis/) |\n\n## Framework Adapters\n\nAdapters bind the shared verify and identity packages to common server frameworks. They should reject failed requests before your handler or tool runs, attach verified claims to framework context, and preserve the same 401/403 behavior across languages.\n\nUse adapters first when your framework is supported. They reduce boilerplate and keep error mapping consistent with the rest of Caracal.\n\n## Verify Package\n\n[Verify Package](/sdks/verify/) is the reusable verification engine under the adapters. Use it when your framework is unsupported or when you need direct control over bearer parsing, verifier defaults, route-level scopes, targets, Session requirements, Delegation requirements, hop limits, and safe error hints.\n\n## Identity Package\n\n[Identity Package](/sdks/identity/) verifies mandate JWT claims directly. Use it when you are composing a custom verifier or adapter. It does not provide the full verify-engine error mapping or revocation-store integration by itself.\n\n## Revocation and Shared State\n\nResource servers must reject mandates anchored to a revoked Authority record ID, Root authority record ID, Session ID, or Delegation ID. Use in-memory revocation stores for local development only. Use [Redis Revocation Store](/sdks/backends/redis/) for multi-instance resource servers that consume the protocol stream `caracal.sessions.revoke`.\n\n## Failure Behavior\n\nAll HTTP verification layers should preserve the shared status mapping:\n\n| Status | Meaning                                                                                            |\n| ------ | -------------------------------------------------------------------------------------------------- |\n| `401`  | The credential was missing, invalid, expired, revoked, or from the wrong zone.                     |\n| `403`  | The mandate verified but lacks required scope, target, Session, Delegation, chain, or hop authority. |\n\nJWT verification establishes signed claims; revocation checks establish whether their authority remains active. Both are required where revocation-before-expiry is part of the resource's security contract.\n\n## Related Pages\n\n- [Protect an MCP Server](/guides/protect-mcp/)\n- [Protect an Express App](/guides/protect-express/)\n- [Protect a FastAPI App](/guides/protect-fastapi/)\n- [Protect a FastMCP App](/guides/protect-fastmcp/)\n- [Protect a Go net/http Service](/guides/protect-nethttp/)"},{"id":"/v0.2/sdks/adapters","title":"Framework Adapters","url":"https://docs.caracal.run/v0.2/sdks/adapters/","concepts":"","body":"Framework adapters bind the lower-level identity and verify packages to common server frameworks. Use them before reaching for lower-level verification APIs.\n\nAdapters are inbound enforcement only. They do not start Sessions, mint mandates, proxy to Gateway, or manage product state.\n\n## Adapter Map\n\n| Adapter | Package | Use it for |\n| --- | --- | --- |\n| [Express](/sdks/adapters/express/) | `@caracalai/express` | Protecting Express 5 routes with Caracal mandate verification. |\n| [ASGI](/sdks/adapters/asgi/) | `caracalai-asgi` | Protecting FastAPI, Starlette, and other Python ASGI apps with Caracal mandate verification. |\n| [FastMCP](/sdks/adapters/fastmcp/) | `@caracalai/fastmcp`, `caracalai-fastmcp` | Verifying FastMCP bearer tokens before tool execution. |\n| [Go net/http](/sdks/adapters/nethttp/) | `github.com/garudex-labs/caracal/packages/adapters/nethttp/go` | Protecting Go HTTP handlers. |\n\n## Choose a Verification Layer\n\n| Need | Use |\n| --- | --- |\n| Framework-neutral verification | [Verify Package](/sdks/verify/) |\n| Express route middleware | [Express Adapter](/sdks/adapters/express/) |\n| FastAPI/Starlette (ASGI) middleware | [ASGI Adapter](/sdks/adapters/asgi/) |\n| FastMCP tool/server authentication | [FastMCP Adapter](/sdks/adapters/fastmcp/) |\n| Go HTTP middleware | [Go net/http Adapter](/sdks/adapters/nethttp/) |\n| Shared revocation store | [Redis Revocation Store](/sdks/backends/redis/) |\n\n## Boundary Semantics\n\nEvery HTTP adapter maps verification failures through one canonical status function in `@caracalai/verify` (`httpStatusForAuthError` in TypeScript, `verify.HTTPStatus` in Go), so the boundary behaves identically across frameworks and languages:\n\n- **401** - the credential itself was not accepted: `missing_token`, `invalid_token`, `invalid_zone`, `session_revoked`, `delegation_stale`.\n- **403** - the mandate verified but its authority is insufficient for the route: `insufficient_scope`, `session_required`, `delegation_required`, `chain_mismatch`, `hop_count_exceeded`.\n\nAdapters never re-derive these status codes; they consume the shared mapping.\n\nAll adapters require trusted issuer, audience, zone, and route-authority requirements. In-memory revocation is suitable only for tests or one process; replicated deployments need a shared backend.\n\n## Related State Backends\n\n- [Redis Revocation Store](/sdks/backends/redis/)\n\n## Related Guides\n\n- [Verification Layer Overview](/sdks/verification-layer/)\n- [Protect an MCP Server](/guides/protect-mcp/)\n- [Protect an Express App](/guides/protect-express/)\n- [Protect a FastAPI App](/guides/protect-fastapi/)\n- [Protect a FastMCP App](/guides/protect-fastmcp/)\n- [Protect a Go net/http Service](/guides/protect-nethttp/)"},{"id":"/v0.2/sdks/adapters/express","title":"Express Adapter","url":"https://docs.caracal.run/v0.2/sdks/adapters/express/","concepts":"","body":"`@caracalai/express` protects Express routes by parsing the bearer token, verifying the mandate through `@caracalai/verify`, and attaching Caracal claims to the request.\n\nUse it for Express 5 ingress. Do not use it as outbound SDK middleware or as a replacement for Gateway.\n\n## Install\n\n```bash\nnpm install @caracalai/express @caracalai/verify @caracalai/revocation-redis\n```\n\nThe adapter has an Express `^5.0.0` peer dependency and targets Node `>=22`.\n\n## Middleware\n\n```ts\nimport express from 'express'\nimport { caracalAuth } from '@caracalai/express'\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { RedisRevocationStore } from '@caracalai/revocation-redis'\n\nconst app = express()\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.example.com',\n  audience: 'https://mcp.example.com',\n  zoneId: 'zone_prod',\n  revocations: new RedisRevocationStore(redis),\n})\n\napp.use(\n  '/mcp',\n  caracalAuth(\n    { verifier },\n    {\n      requiredScopes: ['mcp:tool:call'],\n      requiredTargets: ['https://mcp.example.com'],\n      requireSession: true,\n    },\n  ),\n)\n```\n\n## Request shape\n\nThe middleware attaches Caracal claims to `req.caracal` and `req.caracalClaims` when verification succeeds. Use the exported `CaracalRequest` type when a handler needs typed access.\n\n`caracalAuth(options, routeOverrides?)` is the public middleware entry point. Pass either verify dependencies or `{ verifier }`. `bindContext` defaults to true and also sets `req.caracalContext`; set it false only when ambient SDK context is intentionally unwanted.\n\n```ts\nimport type { CaracalRequest } from '@caracalai/express'\n\napp.post('/mcp/tools/search', (req: CaracalRequest, res) => {\n  res.json({ subject: req.caracal?.sub })\n})\n```\n\n## Failure behavior\n\nFailed verification returns a verify-engine error code such as `missing_token`, `invalid_token`, `insufficient_scope`, or `session_revoked`, plus a safe `error_hint` field. Pair the adapter with a shared revocation store when multiple resource-server instances serve the same resource.\n\n## Related pages\n\n- [Protect an Express App](/guides/protect-express/)\n- [Verify Package](/sdks/verify/)\n- [Redis Revocation Store](/sdks/backends/redis/)"},{"id":"/v0.2/sdks/adapters/asgi","title":"ASGI Adapter","url":"https://docs.caracal.run/v0.2/sdks/adapters/asgi/","concepts":"","body":"The ASGI adapter protects any Python ASGI application - FastAPI, Starlette, Quart, Django ASGI - with fail-closed Caracal mandate verification. It is pure ASGI: it imports no web framework and delegates every check to [Verify Package](/sdks/verify/).\n\n`CaracalASGIAuth` is the only public package entry point. Use it for inbound ASGI HTTP or WebSocket authentication, not outbound SDK calls.\n\n## Install\n\n```bash\npip install caracalai-asgi\n```\n\n## Add middleware\n\n```python\nfrom caracalai_asgi import CaracalASGIAuth\nfrom caracalai_revocation import InMemoryRevocationStore\nfrom fastapi import FastAPI\n\napp = FastAPI()\napp.add_middleware(\n    CaracalASGIAuth,\n    audience=\"resource://billing-api\",\n    revocations=InMemoryRevocationStore(),\n    required_scopes=[\"billing:read\"],\n    routes={\n        \"/payouts\": {\"required_scopes\": [\"billing:payout\"], \"require_delegation\": True},\n    },\n    exclude=[\"/healthz\"],\n)\n\n@app.get(\"/balances\")\nasync def balances(request):\n    principal = request.state.caracal\n    return {\"sub\": principal.sub, \"scopes\": principal.scope}\n```\n\n`issuer` defaults to `CARACAL_STS_URL` and `expected_zone_id` to `CARACAL_ZONE_ID`, so a provider deployed with the standard Caracal workload variables only states its own audience and revocation store. Construction fails if no issuer or zone can be resolved.\n\n## Options\n\n| Option                                                   | Use it for                                                                                                      |\n| -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |\n| `audience`                                               | The provider's own resource identifier; mandates minted for other resources are rejected.                       |\n| `revocations`                                            | Revocation store consulted on every request. Use [Redis Revocation Store](/sdks/backends/redis/) in production. |\n| `required_scopes` / `required_targets`                   | Default scope and target requirements for every route.                                                          |\n| `require_session` / `require_delegation` / `max_hop_count` | Session identity, delegated authority, and Delegation-depth requirements.                                      |\n| `routes`                                                 | Per-route overrides by path prefix; the longest matching prefix wins. Any option above can be overridden.       |\n| `exclude`                                                | Path prefixes served without verification (health and readiness probes).                                        |\n\n`required_use` defaults to `resource`; `require_session` and `require_delegation` default to false. Exclusions and route prefixes match path-segment boundaries, and the longest route prefix wins.\n\n## Behavior\n\n- Verified claims are stored as `scope[\"state\"][\"caracal\"]`, surfaced as `request.state.caracal` in Starlette and FastAPI.\n- Failed verification answers with the shared status mapping (`http_status_for_auth_error`): 401 for credential failures, 403 for insufficient authority, with the standard `error`/`error_description` JSON body.\n- WebSocket connections that fail verification are closed with policy code `1008`; lifespan events pass through.\n- Call `await middleware.warmup()` at startup to prefetch the zone JWKS before the first request.\n\n## Boundary\n\nThe adapter verifies inbound mandates; it does not create Sessions or Delegations. Use the [Python SDK](/sdks/python/) to create Caracal context before making outbound calls.\n\n## Related pages\n\n- [Protect a FastAPI App](/guides/protect-fastapi/)\n- [Verify Package](/sdks/verify/)\n- [Sessions and Revocation](/concepts/sessions-revocation/)"},{"id":"/v0.2/sdks/adapters/fastmcp","title":"FastMCP Adapter","url":"https://docs.caracal.run/v0.2/sdks/adapters/fastmcp/","concepts":"","body":"FastMCP adapters expose small verifier APIs that delegate to the verify packages. Use them when your FastMCP integration needs to authenticate a bearer token before running a tool.\n\nThey verify one token and return a small principal projection; they do not install server lifecycle hooks, create Sessions, or mint outbound mandates.\n\n## Install\n\n| Ecosystem  | Package                                                                  |\n| ---------- | ------------------------------------------------------------------------ |\n| TypeScript | `npm install @caracalai/fastmcp @caracalai/verify @caracalai/revocation` |\n| Python     | `pip install caracalai-fastmcp`                                          |\n\n## TypeScript verifier\n\n```ts\nimport { extractBearer, verifyFastMcpToken } from '@caracalai/fastmcp'\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.example.com',\n  audience: 'https://mcp.example.com',\n  zoneId: 'zone_prod',\n  revocations: new InMemoryRevocationStore(),\n})\n\nconst token = extractBearer(request.headers.get('authorization') ?? '')\nif (!token) throw new Error('missing bearer token')\n\nconst context = await verifyFastMcpToken(token, verifier, {\n  requiredScopes: ['mcp:tool:call'],\n  requiredTargets: ['https://mcp.example.com'],\n  requireSession: true,\n})\n\nconsole.log(context.sub, context.zoneId, context.scope)\n```\n\n`verifyFastMcpToken()` returns `{ sub, zoneId, scope }` or throws `FastMcpAuthError`.\n\nThe TypeScript public entries are `verifyFastMcpToken`, `extractBearer`, and `FastMcpAuthError`. The Python entries are `CaracalAuth` and `CaracalAuthError`. There is no Go FastMCP adapter; use the framework-neutral Go verifier or net/http adapter.\n\n## Python verifier\n\n```python\nfrom caracalai_fastmcp import CaracalAuth, CaracalAuthError\n\nauth = CaracalAuth(\n    issuer=\"https://sts.example.com\",\n    audience=\"https://mcp.example.com\",\n    zone_id=\"zone_prod\",\n    required_scopes=[\"mcp:tool:call\"],\n    required_targets=[\"https://mcp.example.com\"],\n    require_session=True,\n    revocations=revocations,\n)\n\ntry:\n    context = await auth.verify_token(token)\nexcept CaracalAuthError as exc:\n    raise RuntimeError(exc.code) from exc\n```\n\n## Boundary\n\nThe adapter verifies tokens; it does not create Sessions or Delegations. Use the [Python SDK](/sdks/python/) or [TypeScript SDK](/sdks/typescript/) to create Caracal context before making outbound calls.\n\n## Related pages\n\n- [Protect a FastMCP App](/guides/protect-fastmcp/)\n- [Verify Package](/sdks/verify/)\n- [Sessions and Revocation](/concepts/sessions-revocation/)"},{"id":"/v0.2/sdks/adapters/nethttp","title":"Go net/http Adapter","url":"https://docs.caracal.run/v0.2/sdks/adapters/nethttp/","concepts":"","body":"The Go net/http adapter wraps handlers with verify-engine verification and stores verified claims in the request context.\n\nUse it for inbound `net/http` handlers. It does not create outbound Caracal context or replace the Go application SDK.\n\n## Install\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/adapters/nethttp/go\n```\n\n## Middleware\n\n```go\nimport (\n\t\"net/http\"\n\t\"time\"\n\n\tnethttp \"github.com/garudex-labs/caracal/packages/adapters/nethttp/go\"\n\trevocation \"github.com/garudex-labs/caracal/packages/revocation/go\"\n\tverify \"github.com/garudex-labs/caracal/packages/verify/go\"\n)\n\nrevocations := revocation.NewInMemoryStore(24 * time.Hour)\n\nverifier := verify.NewVerifier(verify.Options{\n\tIssuer:          \"https://sts.example.com\",\n\tAudience:        \"https://api.example.com\",\n\tZoneID:          \"zone_prod\",\n\tRevocations:     revocations,\n})\n\nhandler := nethttp.VerifierMiddleware(verifier.Require(verify.Options{\n\tRequiredScopes:  []string{\"tickets:read\"},\n\tRequiredTargets: []string{\"https://api.example.com/tickets\"},\n\tRequireSession:  true,\n}))(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n\tclaims, ok := nethttp.ClaimsFromContext(r.Context())\n\tif !ok {\n\t\thttp.Error(w, \"missing claims\", http.StatusUnauthorized)\n\t\treturn\n\t}\n\t_, _ = w.Write([]byte(claims.Sub))\n}))\n```\n\n## APIs\n\n| API | Purpose |\n| --- | --- |\n| `Middleware(opts)` | Return middleware that verifies the bearer token and rejects failed requests. |\n| `VerifierMiddleware(verifier)` | Return middleware backed by a reusable verifier with shared defaults. |\n| `ClaimsFromContext(ctx)` | Retrieve verified Caracal claims inside a handler. |\n\n`Middleware` constructs a verifier from `verify.Options`; `VerifierMiddleware` reuses an existing verifier. A nil verifier produces a verifier with empty options and therefore cannot establish a properly configured production boundary; always pass trusted issuer, audience, zone, and revocation settings.\n\n## Failure behavior\n\nThe middleware maps verification errors to HTTP failures before the handler runs and includes a safe `error_hint` in JSON failures. Use a shared revocation store through `verify.Options` in production so revoked sessions are rejected consistently across service instances.\n\n## Related pages\n\n- [Protect a Go net/http Service](/guides/protect-nethttp/)\n- [Go SDK](/sdks/go/)\n- [Verify Package](/sdks/verify/)"},{"id":"/v0.2/sdks/verify","title":"Verify Package","url":"https://docs.caracal.run/v0.2/sdks/verify/","concepts":"","body":"The verify packages authenticate Caracal mandates without tying the check to Express, FastMCP, or Go net/http. Framework adapters build on this engine.\n\nUse one long-lived verifier per trust boundary so JWKS and revocation state are reused. Do not use it to mint mandates or create Sessions.\n\n## Install\n\n| Ecosystem  | Package                                                     |\n| ---------- | ----------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/verify`                             |\n| Python     | `pip install caracalai-verify`                              |\n| Go         | `go get github.com/garudex-labs/caracal/packages/verify/go` |\n\n## Core APIs\n\n| API                                                                        | Purpose                                                                                                      |\n| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |\n| `extractBearer` / `extract_bearer` / `ExtractBearer`                       | Parse a bearer token from an `Authorization` header.                                                         |\n| `createMandateVerifier` / `NewVerifier`                                    | Build a reusable verifier with secure defaults, JWKS caching, revocation checks, and per-route overrides.    |\n| `create_mandate_verifier`                                                  | Python reusable verifier with the same defaults, warmup, and per-route override model.                       |\n| `authenticate` / `Authenticate`                                            | Verify one token against identity claims and revocation anchors.                                             |\n| `authenticateRequest` / `unauthorizedResponse`                             | Authenticate a fetch-standard `Request` and render an `AuthError` as a JSON `Response` in WinterTC runtimes. |\n| `checkActiveAuthority` / `check_active_authority` / `CheckActiveAuthority` | Check expiry and revoked anchors for verified claims.                                                        |\n\n## TypeScript example\n\n```ts\nimport { createMandateVerifier } from '@caracalai/verify'\nimport { InMemoryRevocationStore } from '@caracalai/revocation'\n\nconst verifier = createMandateVerifier({\n  issuer: 'https://sts.example.com',\n  audience: 'https://mcp.example.com',\n  zoneId: 'zone_prod',\n  revocations: new InMemoryRevocationStore(),\n})\n\nconst result = await verifier.authorization(req.headers.authorization, {\n  requiredScopes: ['mcp:tool:call'],\n  requiredTargets: ['https://mcp.example.com'],\n  requireSession: true,\n})\n\nif (!result.ok) {\n  throw new Error(`${result.error.code}: ${result.error.hint}`)\n}\n```\n\nUse one verifier per resource server and add route-level scopes or targets through `verifier.authorization(..., overrides)` or `verifier.require(overrides)`. The verifier defaults to resource mandates, checks the STS issuer and audience, enforces zone, scope, target, Session, Delegation, and hop constraints, checks expiry, and queries the Authority record ID, Root authority record ID, Session ID, and Delegation ID parsed claims as revocation anchors. See the [parsed claim mapping](/sdks/identity/#parsed-claim-names) for language-level and raw JWT names.\n\n### Fetch-standard runtimes\n\nRuntimes built on WinterTC `Request`/`Response` globals — Node 18+, Deno, Bun, and edge workers — can authenticate without a framework adapter:\n\n```ts\nimport { authenticateRequest, unauthorizedResponse } from '@caracalai/verify'\n\nexport default {\n  async fetch(request: Request): Promise<Response> {\n    const result = await authenticateRequest(request, deps)\n    if (!result.ok) {\n      return unauthorizedResponse(result.error)\n    }\n    return handle(request, result.principal)\n  },\n}\n```\n\n`unauthorizedResponse` maps error codes to 401 or 403 and renders the same JSON error body as the framework adapters. The same pair drops into any fetch-based framework. In Hono:\n\n```ts\napp.use('/api/*', async (c, next) => {\n  const result = await authenticateRequest(c.req.raw, deps)\n  if (!result.ok) return unauthorizedResponse(result.error)\n  c.set('principal', result.principal)\n  await next()\n})\n```\n\nAnd in a Next.js route handler:\n\n```ts\nexport async function GET(request: Request): Promise<Response> {\n  const result = await authenticateRequest(request, deps)\n  if (!result.ok) return unauthorizedResponse(result.error)\n  return Response.json(await loadTickets(result.principal))\n}\n```\n\nPython and Go services cover this role with the [ASGI adapter](/sdks/adapters/asgi/) and the [net/http adapter](/sdks/adapters/nethttp/), which wrap their ecosystems' native request types.\n\n## Python example\n\n```python\nfrom caracalai_revocation import InMemoryRevocationStore\nfrom caracalai_verify import AuthOptions, create_mandate_verifier\n\nverifier = create_mandate_verifier(\n    AuthOptions(\n        issuer=\"https://sts.example.com\",\n        audience=\"https://api.example.com\",\n        expected_zone_id=\"zone_prod\",\n        revocations=InMemoryRevocationStore(),\n    )\n)\n\nawait verifier.warmup()\n\nresult = await verifier.authorization(\n    request.headers.get(\"authorization\"),\n    required_scopes=[\"tickets:read\"],\n    required_targets=[\"https://api.example.com/tickets\"],\n)\n\nif not result.ok:\n    raise PermissionError(f\"{result.error.code}: {result.error.hint}\")\n```\n\n## Error codes\n\n`authenticate` and reusable verifiers normalize failures into typed error codes: `missing_token`, `invalid_token`, `invalid_zone`, `insufficient_scope`, `session_revoked`, `delegation_stale`, `session_required`, `delegation_required`, `chain_mismatch`, and `hop_count_exceeded`. TypeScript, Python, and Go reusable verifiers include a safe debugging hint for operator logs and API error bodies.\n\nAuthentication performs no retry of the protected operation. A JWKS or revocation-store fai"},{"id":"/v0.2/sdks/identity","title":"Identity Package","url":"https://docs.caracal.run/v0.2/sdks/identity/","concepts":"","body":"The identity packages verify Caracal mandate JWTs. Use them when you are building an adapter or custom resource-server boundary that accepts mandates directly.\n\nDo not use identity verification alone as a production authorization boundary when revocation must take effect before token expiry; compose it through [Verify Package](/sdks/verify/) with a shared revocation store.\n\n## Install\n\n| Ecosystem  | Package                                                       |\n| ---------- | ------------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/identity`                             |\n| Python     | `pip install caracalai-identity`                              |\n| Go         | `go get github.com/garudex-labs/caracal/packages/identity/go` |\n\nNode packages target Node `>=22`; Python packages require Python `>=3.12`.\n\n## Verification inputs\n\n| Option                                                                     | Meaning                                                                                                                                                                                                                             |\n| -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `issuer` / `Issuer`                                                        | Expected STS issuer.                                                                                                                                                                                                                |\n| `audience` / `Audience`                                                    | Expected audience for the mandate.                                                                                                                                                                                                  |\n| `zoneId` / `expected_zone_id` / `ZoneID`                                   | Required zone trust anchor. It fixes which zone's signing keyset verifies the token and must equal the `zone_id` claim, so key selection is never influenced by the unverified token. Verification fails closed when it is missing. |\n| `requiredScopes` / `required_scopes` / `RequiredScopes`                    | Scopes every accepted mandate must contain.                                                                                                                                                                                         |\n| `requiredTargets` / `required_targets` / `RequiredTargets`                 | Target resources every accepted mandate must include.                                                                                                                                                                               |\n| `requiredUse` / `required_use` / `RequiredUse`                             | Token use, usually `resource`.                                                                                                                                                                                                      |\n| `requireSession` / `require_session` / `RequireSession`                    | Require a governed Session identity.                                                                                                                                                                                                   |\n| `requireDelegation` / `require_delegation` / `RequireDelegation`           | Require a Delegation claim (`delegation_edge_id` on the wire).                                                                                                                                                                      |\n| `requireChainContains` / `require_chain_contains` / `RequireChainContains` | Require an application in the delegation chain.                                                                                                                                                                                     |\n| `maxHopCount` / `max_hop_count` / `MaxHopCount`                            | Cap delegation chain depth.                                                                                                                                                                                                         |\n\n## TypeScript example\n\n```ts\nimport { verify } from '@caracalai/identity'\n\nconst claims = await verify(token, {\n  issuer: 'https://sts.example.com',\n  audience: 'https://api.example.com',\n  zoneId: 'zone_prod',\n  requiredScopes: ['tickets:read'],\n  requiredTargets: ['https://api.example.com/tickets'],\n})\n```\n\n## Parsed claim names\n\nThe identity packages map raw JWT claims to canonical language-level fields:\n\n| Meaning                  | TypeScript              | Python                     | Go                      | Raw JWT claim        |\n| ------------------------ | ----------------------- | -------------------------- | ----------------------- | -------------------- |\n| Authority record ID      | `authorityRecordId`     | `authority_record_id`      | `AuthorityRecordID`     | `sid`                |\n| Root authority record ID | `rootAuthorityRecordId` | `root_authority_record_id` | `RootAuthorityRecordID` | `root_sid`           |\n| Session ID               | `sessionId`             | `session_id`               | `SessionID`             | `agent_session_id`   |\n| Delegation ID            | `delegationId`          | `delegation_id`            | `DelegationID`          | `delegation_edge_id` |\n\n## JWKS caching\n\nSTS signing keysets are zone-scoped: every fetch hits `{issuer}/.well-known/jwks.json?zone_id={zone}` and is cached per issuer and zone. TypeScript verifiers use an in-memory "},{"id":"/v0.2/sdks/revocation","title":"Revocation Package","url":"https://docs.caracal.run/v0.2/sdks/revocation/","concepts":"","body":"The revocation packages define the store contract resource servers use to reject mandates after an Authority record, Root authority record, Session, or Delegation has been revoked.\n\nThey do not consume streams, verify JWTs, or revoke product objects by themselves. Use the Redis backend and its consumers for distributed enforcement; use the Admin API to initiate revocation.\n\n## Install\n\n| Ecosystem  | Package                                                         |\n| ---------- | --------------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/revocation`                             |\n| Python     | `pip install caracalai-revocation`                              |\n| Go         | `go get github.com/garudex-labs/caracal/packages/revocation/go` |\n\n## Contract\n\n| Operation                                                                    | Meaning                                                                                                                                      |\n| ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |\n| `isRevoked(anchorId)` / `is_revoked(anchor_id)` / `IsRevoked(anchorID)`                      | Return whether an Authority record, Root authority record, Session, or Delegation anchor is revoked. |\n| `markRevoked(anchorId, ttl)` / `mark_revoked(anchor_id, ttl)` / `MarkRevoked(anchorID, ttl)` | Record a revocation anchor for a TTL.                                                                |\n| `currentDelegationEpoch(zoneId)` / idiomatic equivalent | Return the newest observed Delegation graph epoch when the backend supports stale-edge detection. |\n| `markDelegationEpoch(zoneId, epoch, ttl)` / idiomatic equivalent | Advance that epoch without allowing delayed messages to regress it. |\n\n## In-memory stores\n\nUse in-memory stores for local development, tests, and single-process examples:\n\n| Ecosystem  | In-memory API                                               |\n| ---------- | ----------------------------------------------------------- |\n| TypeScript | `new InMemoryRevocationStore({ defaultTtlMs, maxEntries })` |\n| Python     | `InMemoryRevocationStore(default_ttl_ms=...)`               |\n| Go         | `revocation.NewInMemoryStore(defaultTTL)`                   |\n\nIn-memory stores do not share revocation state across processes. Production resource servers should use a shared backend and consume `caracal.sessions.revoke`.\n\n## Production path\n\nUse [Redis Revocation Store](/sdks/backends/redis/) for multi-instance resource servers. The Redis backend reads signed revocation stream messages, marks every revocation anchor, and lets verifiers fail closed when Redis is unavailable.\n\n## Related pages\n\n- [Sessions and Revocation](/concepts/sessions-revocation/)\n- [Verify Package](/sdks/verify/)\n- [Protect an MCP Server](/guides/protect-mcp/)"},{"id":"/v0.2/sdks/oauth","title":"OAuth Package","url":"https://docs.caracal.run/v0.2/sdks/oauth/","concepts":"","body":"The OAuth packages call the STS `/oauth/2/token` endpoint for RFC 8693 exchanges. The returned mandate class depends on the authenticated flow: lifecycle bootstrap returns `use=session`, a bound direct mint returns `use=gateway`, and Gateway's authenticated exchange returns `use=resource` privately to Gateway.\n\nUse this package only when composing a custom authority client. Normal applications should use the language SDK facade, which supplies the correct Authority-record, Session, and Delegation identifiers.\n\n## Install\n\n| Ecosystem  | Package                                                    |\n| ---------- | ---------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/oauth`                             |\n| Python     | `pip install caracalai-oauth`                              |\n| Go         | `go get github.com/garudex-labs/caracal/packages/oauth/go` |\n\n## Exchange inputs\n\n| Option                     | Meaning                                                                                                                       |\n| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |\n| Subject token              | Existing session mandate or registered external identity token; omit for application-principal exchanges.                    |\n| Resource                   | One or more resource identifiers. Clients trim, exact-deduplicate, and sort the set.                                           |\n| Client secret              | Application authentication. Public client assertions are not supported.                                                       |\n| Authority record ID        | Sent in the STS form field `session_id`. It is distinct from a governed Session ID.                                           |\n| Session ID                 | Sent in the STS form field `agent_session_id`.                                                                                |\n| Delegation ID              | Sent in the STS form field `delegation_edge_id`.                                                                              |\n| Scopes                     | Requested resource scopes.                                                                                                    |\n| TTL seconds                | Requested mandate lifetime.                                                                                                   |\n\n## TypeScript example\n\n```ts\nimport { OAuthClient, ApprovalRequiredError } from '@caracalai/oauth'\n\nconst oauth = new OAuthClient(stsUrl, zoneId, applicationId)\n\ntry {\n  const token = await oauth.exchange(subjectToken, 'https://api.example.com/tickets', {\n    scopes: ['tickets:read'],\n    clientSecret: process.env.CARACAL_APP_CLIENT_SECRET,\n  })\n  console.log(token.accessToken, token.expiresIn)\n} catch (error) {\n  if (error instanceof ApprovalRequiredError) {\n    console.log('approval required', error.approvalId)\n  } else {\n    throw error\n  }\n}\n```\n\n## Behavior\n\n- Successful responses are validated for `access_token`, `token_type`, and `expires_in`.\n- Public responses expose the granted `target_resources` subset; private Gateway upstream directives are not part of the SDK response type.\n- Cache-enabled responses are isolated by identity, canonical resources, scopes, TTL, and credential context. One-shot, approval-bearing, and cache-disabled exchanges bypass both cache and single-flight sharing.\n- Issuance uses one network attempt. A lost response may hide a successfully minted token, so the clients never retry STS exchange automatically.\n- STS `interaction_required` responses surface as `ApprovalRequiredError`.\n- Default exchange timeout is 30 seconds. `invalidate()` / `invalidate` clears the client cache; it does not revoke already issued mandates or cancel exchanges already in flight.\n- Never retry an exchange merely because the response was lost. If the surrounding business operation is replayable, mint a fresh mandate for that new attempt.\n\n## Subject Federation\n\nWhen the zone registers the application's identity system as a Subject issuer, the client exchanges an end user's identity token for a Caracal Authority record and can post that user's decision on a Subject-reserved approval hold:\n\n- `federateSubject(idToken, opts)` / `federate_subject(id_token, ...)` / `FederateSubject(ctx, idToken, opts)` - creates the Subject's Authority record and returns the Subject mandate response. Never cached: each federation is an explicit identity event, and the record carries no resource authority. The high-level SDK wrappers also return the Authority record ID as `subjectAuthorityRecordId`, `subject_authority_record_id`, or `SubjectAuthorityRecordID`.\n- `decideApproval({...})` / `decide_approval(...)` / `DecideApproval(ctx, input)` - posts the user's decision with the session mandate, echoing the hold's exact binding.\n\nSee [Human Approval](/guides/step-up/#decide-as-the-applications-end-user) for the end-to-end flow.\n\n## Related pages\n\n- [Human Approval](/guides/step-up/)\n- [Mandates](/concepts/mandate/)\n- [Use STS Endpoint](/api/sts/)"},{"id":"/v0.2/sdks/admin","title":"Admin Package","url":"https://docs.caracal.run/v0.2/sdks/admin/","concepts":"","body":"The admin package is the automation client for control-plane objects. It mirrors Console management workflows for scripts and services, and ships in three languages: `@caracalai/admin` (TypeScript), `caracalai-admin` (Python), and `github.com/garudex-labs/caracal/packages/admin/go` (Go).\n\nUse it from trusted operator automation. Do not embed admin tokens in agent or resource-server processes, and do not use this client for the SDK Session lifecycle.\n\n## Install\n\n```bash\nnpm install @caracalai/admin\n```\n\n```bash\npip install caracalai-admin\n```\n\n```bash\ngo get github.com/garudex-labs/caracal/packages/admin/go\n```\n\n## Create a client\n\n```ts\nimport { AdminClient } from '@caracalai/admin'\n\nconst admin = new AdminClient({\n  apiUrl: process.env.CARACAL_API_URL!,\n  coordinatorUrl: process.env.CARACAL_COORDINATOR_URL,\n  adminToken: process.env.CARACAL_ADMIN_TOKEN!,\n  coordinatorToken: process.env.CARACAL_COORDINATOR_TOKEN,\n})\n```\n\n`apiUrl` and `adminToken` are required for API-backed resources. `coordinatorUrl` and `coordinatorToken` are required for Session lifecycle and Delegation methods.\n\nPython:\n\n```python\nfrom caracalai_admin import AdminClient\n\nadmin = AdminClient(\n    api_url=os.environ[\"CARACAL_API_URL\"],\n    admin_token=os.environ[\"CARACAL_ADMIN_TOKEN\"],\n    coordinator_url=os.environ.get(\"CARACAL_COORDINATOR_URL\"),\n    coordinator_token=os.environ.get(\"CARACAL_COORDINATOR_TOKEN\"),\n)\n```\n\nGo:\n\n```go\nimport admin \"github.com/garudex-labs/caracal/packages/admin/go\"\n\nclient := admin.NewAdminClient(admin.AdminClientOptions{\n    APIURL:           os.Getenv(\"CARACAL_API_URL\"),\n    AdminToken:       os.Getenv(\"CARACAL_ADMIN_TOKEN\"),\n    CoordinatorURL:   os.Getenv(\"CARACAL_COORDINATOR_URL\"),\n    CoordinatorToken: os.Getenv(\"CARACAL_COORDINATOR_TOKEN\"),\n})\n```\n\n## API groups\n\n| Group              | Methods                                                                                                                                                                                                                                                       |\n| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `zones`            | `list`, `get`, `dcrStatus`, `create`, `patch`, `delete`                                                                                                                                                                                                       |\n| `applications`     | `list`, `get`, `create`, `patch`, `rotateSecret`, `getClientSecret`, `delete`, `dcr`                                                                                                                                                                        |\n| `resources`        | `list`, `get`, `create`, `patch`, `delete`                                                                                                                                                                                                                    |\n| `providers`        | `list`, `get`, `create`, `patch`, `delete`                                                                                                                                                                                                                    |\n| `policies`         | `list`, `get`, `create`, `validate`, `addVersion`, `delete`                                                                                                                                                                                                   |\n| `policyTemplates`  | `list`, `get`                                                                                                                                                                                                                                                 |\n| `policySets`       | `list`, `get`, `create`, `addVersion`, `listVersions`, `simulate`, `activate`, `activationStatus`, `delete`                                                                                                                                                   |\n| `grants`           | `list`, `get`, `create`, `revoke`                                                                                                                                                                                                                             |\n| `subjectIssuers`   | `list`, `get`, `create`, `patch`, `delete`                                                                                                                                                                                                                    |\n| `providerConnections` | `create`, `authorizeOAuth`, `revoke`                                                                                                                                                                                                                       |\n| `workloads`        | `list`, `get`, `create`, `update`, `rotateSecret`, `getSecret`, `delete`                                                                                                                                                                                     |\n| `authorityRecords` | `list`                                                                                                                                                                                                                                                        |\n| `subjects`         | `revoke` - the kill switch: one call revokes every live Authority record for the Subject, terminates linked Sessions, revokes their Delegations and provider connections, and feeds the revocation stream so in-flight mandates die before `exp`. Idempotent. |\n| `sessions`      "},{"id":"/v0.2/sdks/backends/redis","title":"Redis Revocation Store","url":"https://docs.caracal.run/v0.2/sdks/backends/redis/","concepts":"","body":"The Redis backends provide shared revocation state for resource servers. They let multiple service instances reject mandates after Authority records, root Authority records, Sessions, or Delegations are revoked.\n\nUse this backend only for verification state. It is not a general Caracal data client and must not receive Admin API credentials.\n\nThe resource server does not need to share a network with Caracal services. For separate VPC, cluster, or cloud deployments, give the resource workload private TLS connectivity to the managed Redis endpoint that carries Caracal streams, authenticate it with a dedicated least-privilege Redis identity, and run the consumer beside the verifier. Do not expose Redis publicly or distribute Caracal administrative tokens to resource applications.\n\n## Install\n\n| Ecosystem  | Package                                                             |\n| ---------- | ------------------------------------------------------------------- |\n| TypeScript | `npm install @caracalai/revocation-redis`                           |\n| Python     | `pip install caracalai-revocation-redis`                            |\n| Go         | `go get github.com/garudex-labs/caracal/packages/backends/redis/go` |\n\n## Defaults\n\n| Setting           | Default                                         |\n| ----------------- | ----------------------------------------------- |\n| Revocation stream | `caracal.sessions.revoke`                       |\n| Consumer group    | `resource-revocation`                           |\n| Revocation TTL    | 24 hours                                        |\n| Signature support | Optional HMAC verification for stream messages. |\n| Dead-letter bound | Approximately 10,000 poison entries per `.dead` stream. |\n\nThe store and consumers are long-lived resources. Start stream consumers with service startup, use unique consumer names, stop polling on shutdown, and close the caller-owned Redis connection. Verification should fail closed when the backend is unavailable.\n\n## TypeScript store\n\n```ts\nimport { RedisRevocationStore } from '@caracalai/revocation-redis'\n\nconst revocations = new RedisRevocationStore(redis, {\n  defaultTtlMs: 24 * 60 * 60 * 1000,\n})\n\nawait revocations.markRevoked('sess_123')\nconst blocked = await revocations.isRevoked('sess_123')\n```\n\n## Stream consumer\n\nUse the stream consumer when your resource server should learn revocations from the audit/control plane instead of marking them locally.\n\n```ts\nimport { RedisRevocationConsumer } from '@caracalai/revocation-redis'\n\nconst consumer = new RedisRevocationConsumer(redis, revocations, {\n  consumer: 'api-1',\n  streamHmacKey: Buffer.from(process.env.CARACAL_REVOCATION_HMAC_SECRET!, 'hex'),\n  requireSignature: true,\n})\n\nawait consumer.ensureGroup()\nawait consumer.pollOnce()\n```\n\nStream events can revoke multiple anchors. Invalid signatures and permanently malformed messages are copied to the bounded `<stream>.dead` stream before acknowledgement. If the dead-letter write fails, the source message remains pending for recovery; transient application failures also remain pending.\n\nDelegated verification also needs the `caracal.delegations.invalidate` stream. Start `RedisDelegationInvalidationConsumer` beside `RedisRevocationConsumer` so the shared store receives the zone's current graph epoch; without it, signature and revocation-anchor checks still run, but `delegation_stale` cannot detect a mandate issued against an older graph epoch. Epoch writes use one atomic max-with-TTL operation, so delayed or concurrent messages cannot regress the recorded security state. The Python and Go Redis packages expose equivalent behavior.\n\nEach independently deployed resource instance uses a unique consumer name under deployment-specific groups for both streams. Supply `STREAMS_HMAC_KEY` through that workload's secret manager and require stream signatures in published deployments. Network policy should permit only DNS and the exact private Redis endpoint; the application needs no Postgres, API, Coordinator, or internal service access for revocation.\n\n## Related pages\n\n- [Revocation Package](/sdks/revocation/)\n- [Sessions and Revocation](/concepts/sessions-revocation/)\n- [Verify Package](/sdks/verify/)"},{"id":"/v0.2/security","title":"Secure Caracal","url":"https://docs.caracal.run/v0.2/security/","concepts":"","body":"This section covers OSS code and assets in this repository, not enterprise code, customer infrastructure, external identity providers/upstreams, or model behavior.\n\n## Choose a Review\n\n| Goal | Page |\n| --- | --- |\n| Assets, boundaries, threats, residual risk | [Threat Model](/security/threat-model/) |\n| Deployed environment review | [Hardening](/security/hardening/) |\n| Artifact verification | [Verify a Release](/security/verify-releases/) |\n| Repository evidence | [Evidence Pack](/security/evidence-pack/) |\n| Private reporting | [Report a Vulnerability](/security/disclosure/) |\n| Adoption controls and non-claims | [Review OSS Adoption Readiness](/security/adoption-review/) |\n\n## Security Invariants\n\n- STS fails closed on Policy, key, replay, revocation, Session, Approval, and signing failures.\n- Gateway authorizes before dispatch and applies binding, egress, redirect, replay, and revocation controls.\n- Published modes require integrity keys and authenticated metrics.\n- Audit is append-only/tamper-evident with replay and DLQ paths.\n- Runtime secrets remain outside untrusted application/agent workspaces.\n- OSS behavior does not depend on enterprise code.\n\n## Review Completion\n\nTie every claim to version, config, source/test evidence, and observed result. Repository controls do not replace host hardening, identity governance, recovery testing, or compliance programs.\n\n## Next Action\n\nStart with [Review the Threat Model](/security/threat-model/)."},{"id":"/v0.2/security/threat-model","title":"Review the Threat Model","url":"https://docs.caracal.run/v0.2/security/threat-model/","concepts":"","body":"The canonical model is `governance/THREAT_MODEL.md`. This is a review procedure, not a duplicate inventory.\n\n## Use Criteria\n\nReview before adoption and whenever auth, policy, tokens, keys, revocation, audit, streams, egress, services, ports, secrets, installers, releases, or product boundaries change.\n\n## Prerequisites\n\nPin source/deployed release. Gather rendered deployment, endpoints, secret flow, dependency topology, features, and targeted tests.\n\n## Review Procedure\n\n1. Confirm deployment fits the documented OSS scope.\n2. Walk each boundary: untrusted input, mediation, credential, fail-closed behavior, evidence, owner.\n3. Map assets to T1-T14 and run relevant negative checks.\n4. Review every canonical known limit against deployment.\n5. Record operator controls: TLS, host isolation, IdP, backup, monitoring, incident process.\n6. Reject claims relying on enterprise-only or unverified platform controls.\n\n## Verification\n\nRetain references/results for accepted threats and owner/containment/acceptance/date for residual risks.\n\n## Recovery\n\nDisable or isolate unsupported boundaries. Report exploitable defects privately.\n\n## Next Action\n\nApply [Harden Security Posture](/security/hardening/)."},{"id":"/v0.2/security/hardening","title":"Harden Security Posture","url":"https://docs.caracal.run/v0.2/security/hardening/","concepts":"","body":"Use after operational hardening and before production traffic.\n\n## Prerequisites\n\nUse a pinned verified release, stable mode, complete Secret, private storage, TLS ingress, authenticated metrics, and tested backup/incident paths.\n\n## Procedure\n\n1. Expose only required endpoints; keep storage, Audit, Coordinator, and Control private.\n2. Retain non-root, read-only, dropped-capability, seccomp, and no-new-privileges settings.\n3. Confirm secrets are projected, restricted, separately backed up, and inaccessible to agents.\n4. Verify browser origins, cookies, registration allowlist, password/SMTP policy, and proxy trust.\n5. Verify STS deny cases for credentials, scope, revocation, replay, and Approval.\n6. Verify Gateway binding/header, egress, dangerous addresses, redirects, revocation, and pre-dispatch denial.\n7. Verify stream/audit HMAC, tamper, DLQ/replay, and metrics auth.\n8. Reserve bootstrap admin credentials for break-glass.\n\n## Verification\n\nRetain negative-test IDs, readiness, rendered settings, network results, provenance, alert test, and restore evidence.\n\n## Recovery\n\nRemove affected endpoint/workload from traffic, restore verified config, and open an incident for possible exposure. Never switch to dev or disable safety checks.\n\n## Next Action\n\nVerify artifacts with [Verify a Release](/security/verify-releases/)."},{"id":"/v0.2/security/verify-releases","title":"Verify a Release","url":"https://docs.caracal.run/v0.2/security/verify-releases/","concepts":"","body":"The release workflow publishes archive checksums and GitHub Artifact Attestations; container builds request provenance and SBOM attestations. Verify only artifacts present for the selected release.\n\n## Prerequisites\n\nDownload from official GitHub/GHCR. Install/authenticate GitHub CLI. Record tag and digest/hash.\n\n## Archive Procedure\n\n```bash\nsha256sum --check SHA256SUMS\ngh attestation verify caracal-runtime-<platform>-<version>.tar.gz --repo Garudex-Labs/caracal\n```\n\nUse `shasum -a 256 --check` on macOS or `Get-FileHash` on PowerShell. Installers always verify checksum; provenance is opportunistic unless `CARACAL_REQUIRE_PROVENANCE=1`.\n\n## Container Procedure\n\n```bash\ngh attestation verify oci://ghcr.io/garudex-labs/caracal-go:<tag> --repo Garudex-Labs/caracal\ndocker buildx imagetools inspect ghcr.io/garudex-labs/caracal-go:<tag>\n```\n\nRepeat for every deployed image and pin digests. Inspect available attestations; do not infer SBOM presence from provenance alone.\n\n## Failure Recovery\n\nDo not run failed/unverifiable artifacts. Re-download and confirm tag/repository. Preserve hashes/output and report privately if persistent.\n\n## Next Action\n\nCapture deployment evidence with [Generate an Evidence Pack](/security/evidence-pack/)."},{"id":"/v0.2/security/evidence-pack","title":"Generate an Evidence Pack","url":"https://docs.caracal.run/v0.2/security/evidence-pack/","concepts":"","body":"`evidencePack.sh` captures image provenance, schema validation, runtime smoke readiness, and the threat model. It is not compliance, penetration testing, an SBOM archive, config audit, or HA proof.\n\n## Prerequisites\n\nProvide checkout, release version/authenticated `gh`, database credentials/`psql`, reachable runtime, and protected output as applicable.\n\n## Procedure\n\n```bash\nexport CARACAL_VERSION=<tag>\nexport PGHOST=<host> PGPORT=5432 PGUSER=<user> PGDATABASE=<database> PGPASSWORD=<password>\nexport CARACAL_SMOKE_HOST=<host>\nbash infra/scripts/evidencePack.sh\n```\n\nOutput is under `evidence/caracal-evidence-<UTC>/`. Executed failures exit non-zero; missing inputs produce non-failing `SKIPPED`.\n\n## Verify\n\nRequire relevant checks to be `PASS`; skipped means missing evidence. Add rendered deployment, digests, config review, canaries, alerts, and restore evidence separately.\n\n## Recovery\n\nPreserve raw failure output, correct the boundary, and generate a new pack. Never edit generated status.\n\n## Next Action\n\nUse [Review OSS Adoption Readiness](/security/adoption-review/)."},{"id":"/v0.2/security/adoption-review","title":"Review OSS Adoption Readiness","url":"https://docs.caracal.run/v0.2/security/adoption-review/","concepts":"","body":"Use this review when security, platform, compliance, procurement, or operations teams need a decision record for the product implemented in this repository. Caracal is a technical authority and audit component, not a compliance program or contractual assurance.\n\nThis repository does not establish a managed service, service-level agreement, support entitlement, commercial edition, certification, regulatory conformity, or FIPS validation. Treat any requirement without implementation and evidence here as unavailable until a separate authoritative source is reviewed.\n\n## What You Can Evaluate\n\n| Area | Evidence in this repository | Adopter-owned decision |\n| --- | --- | --- |\n| Authority enforcement | STS exchange, policy evaluation, Gateway and adapter verification, revocation, replay controls, tests | Which resources and actions require Caracal enforcement |\n| Deployment | Versioned Compose, Helm, and OpenTofu assets | Availability topology, infrastructure provider, ingress, and capacity |\n| Secrets and cryptography | Secret backends, envelope encryption, zone signing, HMAC boundaries, rotation procedures | Secret manager, key custody, rotation cadence, and required cryptographic validation |\n| Evidence | Request IDs, audit ingestion, tamper checks, replay, DLQ, export paths | Retention, legal hold, SIEM ownership, and evidence access |\n| Operations | Readiness, metrics, alerts, backup/restore, failure drills, upgrade flow | SLOs, RTO/RPO, on-call coverage, and escalation |\n| Supply chain | Lockfiles, checksums, release verification, build and publishing workflows | Artifact allowlisting, vulnerability acceptance, and deployment admission |\n| Human identity | Self-hosted Console sign-in and host-managed admission | Identity provider configuration, account lifecycle, privileged access review |\n\n## Prerequisites\n\nBefore the review, collect:\n\n- the exact Caracal release, image digests, chart or Compose assets, and configuration under review;\n- the protected resources, workload identities, Subjects, operators, and data classes in scope;\n- authoritative security and compliance requirements;\n- availability, recovery, retention, privacy, and incident objectives;\n- named owners for infrastructure, identity, secrets, policy, audit, and application integration.\n\n## Review Procedure\n\n1. **Verify the release.** Validate checksums, provenance, signatures or attestations that are actually present, image digests, and version alignment across packages.\n2. **Review the threat model.** Map in-scope assets and trust boundaries to the deployed topology. Record assumptions the repository cannot enforce, including host and cloud controls.\n3. **Render the deployment.** Inspect the exact Compose or Helm output for public endpoints, TLS termination, service accounts, NetworkPolicy, storage, secret mounts, resource limits, and published mode.\n4. **Exercise authority failures.** Test expected allow, deny, insufficient scope, wrong audience, replay, expired Mandate, revoked Authority record, revoked Session, revoked Delegation, and unsafe upstream cases.\n5. **Exercise human and administrative controls.** Verify Console admission, operator credential scope, Subject federation where used, Approval decisions, and administrative audit attribution.\n6. **Exercise evidence paths.** Trace request IDs from authorization through action result, test audit interruption and replay, inspect DLQ handling, and verify export or SIEM ingestion.\n7. **Exercise operations.** Verify readiness, authenticated metrics, alerts, capacity assumptions, upgrade behavior, data-plus-secret restore, failure drills, and incident intake.\n8. **Record every gap.** For each requirement, record Caracal evidence, deployment evidence, missing control, compensating control, owner, due date, and retest trigger.\n\nFramework mappings are review aids, not automatic coverage. A technical control can contribute evidence to an organizational requirement without satisfying the requirement by itself.\n\n## Minimum Evidence Set\n\nRetain:\n\n- artifact verification output and immutable version identifiers;\n- rendered deployment manifests or resolved Compose configuration;\n- threat-model findings and accepted assumptions;\n- positive and negative test results with request IDs;\n- evidence-pack output, including the reason for every skipped check;\n- alert routing and failure-drill results;\n- backup and secret-custody evidence plus a successful isolated restore;\n- upgrade rehearsal and rollback or roll-forward decision;\n- final risks, owners, approval, expiry date, and review triggers.\n\n## Decision Rules\n\nApprove only the exact release and environment reviewed. Do not convert the presence of a workflow, test, manifest, or documentation page into a guarantee about a deployed environment.\n\nReject or conditionally approve when:\n\n- a required enforcement path can bypass Gateway or verifier checks;\n- revocation or audit freshness cannot be observed;\n- operator secrets can reach workload or agent environments;\n- a restore cannot recover both durable data and required key material;\n- critical alerts have no owner or tested response;\n- an organizational, contractual, or certification requirement is being inferred from repository intent alone.\n\n## Review Triggers\n\nRepeat the review when a release, service boundary, public endpoint, identity flow, secret backend, storage platform, deployment substrate, protected data class, policy model, recovery objective, or authoritative requirement changes.\n\nMissing evidence means **unverified**. It does not mean planned, inherited, or satisfied.\n\n## Next Action\n\nComplete [Harden Security Posture](/security/hardening/), [Generate an Evidence Pack](/security/evidence-pack/), and [Hand Off to Platform Teams](/operations/platform-team-handoff/)."},{"id":"/v0.2/security/disclosure","title":"Report a Vulnerability","url":"https://docs.caracal.run/v0.2/security/disclosure/","concepts":"","body":"Do not open public issues or pull requests for credential exposure, policy bypass, unsafe execution/routing, audit compromise, malicious artifacts, or exploitable failures.\n\n## Choose a Channel\n\n| Scope | Channel |\n| --- | --- |\n| OSS code | `https://github.com/Garudex-Labs/caracal/security/advisories/new` |\n| Sensitive attachments/context | `support@caracal.run` |\n| Enterprise-only/private customer context | Email only; outside this workspace |\n\n## Report Procedure\n\nInclude summary, version/commit, boundary, prerequisites, minimal reproduction, observed/expected, impact, and mitigation if known. Redact credentials/customer data.\n\nMaintainers aim to respond within up to seven days; this is not a resolution SLA.\n\n## Verify Submission\n\nRetain identifier and sanitized copy. Continue through the same private channel.\n\n## Recovery and Disclosure\n\nKeep private until fix/mitigation or coordinated outcome. For active exploitation, contain with [Incident Response](/operations/incident-response/).\n\n## Next Action\n\nReview [Threat Model](/security/threat-model/)."},{"id":"/v0.2/examples","title":"Use Examples","url":"https://docs.caracal.run/v0.2/examples/","concepts":"","body":"Examples show Caracal integrated into concrete applications and automation scripts. Use them after the first tutorials when you want runnable code that matches a specific integration job. All examples live in the dedicated [Caracal examples repository](https://github.com/Garudex-Labs/examples); clone it once and every guide below runs from that checkout:\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\n```\n\n## Prerequisites\n\n- Complete [First Protected Call](/get-started/first-protected-call/) and know whether you are integrating an application or a resource server.\n- Use an isolated development zone and synthetic credentials.\n- Run the example's offline tests before connecting it to a live runtime.\n\n## Choose an example\n\n| Goal                                                                                        | Start here                                                | Code path                                                                                 |\n| ------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------- |\n| Need a local protected target for the first Gateway call.                                   | [Run Echo Upstream](/examples/echo-upstream/)             | [echoUpstream](https://github.com/Garudex-Labs/examples/tree/main/echoUpstream)           |\n| Want to automate zone setup from a script or pipeline through the Control API.              | [Bootstrap Control State](/examples/control-bootstrap/)   | [controlBootstrap](https://github.com/Garudex-Labs/examples/tree/main/controlBootstrap)   |\n| Need to prove a provider-backed resource is ready.                                          | [Check Provider Readiness](/examples/provider-preflight/) | [providerPreflight](https://github.com/Garudex-Labs/examples/tree/main/providerPreflight) |\n| Need to turn a denial into a safe policy fix.                                               | [Iterate Policy Safely](/examples/policy-iterate/)        | [policyIterate](https://github.com/Garudex-Labs/examples/tree/main/policyIterate)         |\n| Want to launch a plain CLI agent with injected provider credentials.                        | [Launch Research Agent](/examples/research-agent/)        | [ResearchAgent](https://github.com/Garudex-Labs/examples/tree/main/ResearchAgent)         |\n| Want a full app reference lab with agents, providers, Gateway, STS, and Console inspection. | [Run Lynx Capital](/examples/lynx-capital/)               | [lynxCapital](https://github.com/Garudex-Labs/examples/tree/main/lynxCapital)             |\n\n## Recommended order\n\n1. Start with [Run Echo Upstream](/examples/echo-upstream/) if you have not completed a Gateway-mediated request yet.\n2. Use [Bootstrap Control State](/examples/control-bootstrap/) when automation should own zone setup instead of manual Console clicks.\n3. Run [Check Provider Readiness](/examples/provider-preflight/) before the first real provider-backed Gateway call.\n4. Use [Iterate Policy Safely](/examples/policy-iterate/) when an audit denial needs to become a tested policy change.\n5. Try [Launch Research Agent](/examples/research-agent/) to see `caracal run` inject provider-native credentials into an existing-style CLI process.\n6. Study [Run Lynx Capital](/examples/lynx-capital/) when you need a full app topology and live Console inspection path.\n\n## Use examples safely\n\n- Start Caracal through the released runtime and Console path described by the example.\n- Use Console for zones, applications, providers, resources, policies, control keys, and launch bindings.\n- Keep example fixtures inside their own example directory in the examples repository.\n- Run each example's offline tests before adapting it.\n- Do not commit provider secrets, admin tokens, or real third-party credentials.\n\nExpected result: each example proves one job and leaves a request ID, test output, or drift report. Examples are not production libraries and must be adapted with your own identity, secret, retry, timeout, and deployment controls.\n\n:::caution[Fixed example names]\n`ResearchAgent` and `lynxCapital` are checked-in source names and remain unchanged. New configuration around them should use approved Pied Piper/Hooli names.\n:::\n\n## Related sections\n\n- [Get Started](/get-started/)\n- [Tutorials](/tutorials/)\n- [Guides](/guides/)\n- [SDKs](/sdks/)\n- [Runtime and Console](/runtime-console/)\n\n## Next step\n\nChoose one row in **Choose an example**; do not run the full reference lab when a smaller example proves the required boundary."},{"id":"/v0.2/examples/echo-upstream","title":"Run Echo Upstream","url":"https://docs.caracal.run/v0.2/examples/echo-upstream/","concepts":"","body":"Echo Upstream is a zero-dependency HTTP service in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `echoUpstream/`. It stands in for the API you would put behind Caracal, so you can verify a Gateway-mediated request end to end without hosting your own upstream. Every response states whether the call was brokered by the Gateway or hit the service directly, and shows the evidence behind that verdict.\n\n## When to use it\n\nUse it as a disposable resource server while validating Gateway routing, provider injection, headers, and audit. It does not verify Caracal mandates itself and is not a production authorization boundary.\n\n## Prerequisites\n\n- A ready Caracal runtime and Docker network access from Gateway.\n- A `resource://pipernet` resource, provider, operations, active policy, and scoped caller mandate.\n\n## What it demonstrates\n\n| Area | Behavior |\n| --- | --- |\n| Brokered-call proof | Reports `viaGateway` plus the request ID, trace context, and forwarding metadata the Gateway stamped on the request. |\n| Credential handling | Confirms the Gateway injected the brokered credential and redacts credential values from the echoed headers. |\n| Gateway reachability | Joins the `caracalData` Docker network so Gateway can reach `http://echoUpstream:8088`. |\n| Local debugging | Exposes `http://127.0.0.1:8088` on the host and logs one line per request, marked `[gateway]` or `[direct]`. |\n\n## Run with Docker\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/echoUpstream\ndocker compose -f compose.yml up --build\n```\n\nCheck the local health endpoint:\n\n```bash\ncurl http://127.0.0.1:8088/healthz\n```\n\n## Run with Node\n\n```bash\ncd caracal-examples/echoUpstream\nnpm start\n```\n\nThe server listens on port `8088`. Set `ECHO_PORT` when you need a different local port.\n\n## Use as the protected upstream\n\nIn web console guided setup or through the Control API, create a resource with this upstream URL:\n\n```text\nhttp://echoUpstream:8088\n```\n\nThen send a request to the Gateway with the Caracal access token and resource ID from guided setup. The command has this shape:\n\n```bash\ncurl http://localhost:8081/v1/hello \\\n  -H \"Authorization: Bearer $CARACAL_RESOURCE_PIPERNET_TOKEN\" \\\n  -H \"X-Caracal-Resource: resource://pipernet\"\n```\n\n## Read the response\n\nA brokered call returns `\"viaGateway\": true` with a `gateway` section:\n\n| Field | What it proves |\n| --- | --- |\n| `viaGateway` | The request carried the Gateway's request ID and forwarding metadata. |\n| `gateway.requestId` | Audit handle - filter web console **Audit** by it to trace the policy decision. |\n| `gateway.credentialInjected` | The Gateway brokered a credential the client never held. |\n| `request.headers` | The headers the upstream actually received, with credentials redacted. |\n\nCalling `http://127.0.0.1:8088/` directly returns `\"viaGateway\": false`, which makes the difference between a protected and an unprotected path visible.\n\n## Test\n\n```bash\ncd caracal-examples/echoUpstream\nnpm test\n```\n\nExpected result: direct traffic reports `viaGateway: false`; the Gateway path reports `viaGateway: true`, redacts credentials, and provides a request ID that resolves to STS and Gateway evidence.\n\n:::caution[Failure point: direct port]\nHost port `8088` deliberately bypasses Gateway. Never interpret a successful direct curl as proof of authorization, and do not expose an equivalent bypass in production.\n:::\n\n## Next step\n\nContinue to [Bootstrap Control State](/examples/control-bootstrap/) when you want repeatable setup for demo resources and policies."},{"id":"/v0.2/examples/control-bootstrap","title":"Bootstrap Control State","url":"https://docs.caracal.run/v0.2/examples/control-bootstrap/","concepts":"","body":"Control Bootstrap is the canonical Control API automation example in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `controlBootstrap/`. A small CI/CD-style pipeline keeps one workload environment—Application, Provider, Resource, and Policy—matching a declared plan without adding product-management verbs to the runtime CLI.\n\n## When to use it\n\nUse it when CI must reconcile objects inside one existing zone through scoped Control credentials. Use the Admin SDK from a trusted operator environment when automation must create zones or use management surfaces not exposed by Control.\n\n## Prerequisites\n\n- A ready runtime and existing target zone.\n- Separate Control keys for apply, verify, and teardown with only documented scopes.\n- A reviewed plan containing no production secrets.\n\n## What it demonstrates\n\n| Area               | Behavior                                                                                                    |\n| ------------------ | ----------------------------------------------------------------------------------------------------------- |\n| Automation surface | Calls `/v1/control/invoke` instead of using a root admin token.                                             |\n| Identity model     | Uses scoped, short-lived control keys created in the web console, one scope tier per pipeline stage.        |\n| Reconciliation     | `apply` creates missing objects, patches drifted ones, and publishes a new policy version on content drift. |\n| CI gating          | `verify` is a read-only drift check that exits non-zero when the zone does not match the plan.              |\n| Safety model       | Uses replay protection, rate limits, scoped control permissions, and audit.                                 |\n\nThe plan describes the PiperNet reporter's environment: its Application, the `provider://pipernet-mandate` Provider, the `resource://pipernet` Resource wired to it, and the baseline Policy that allows `read`.\n\n## Web Console Setup\n\n1. Start the runtime and open the web console:\n\n   ```bash\n   caracal up\n   caracal status --ready\n   ```\n\n   Open the packaged web console at `http://localhost:3001`.\n\n2. Create or select the target zone.\n3. Create a control key with only the scopes the stage needs: read/write on app, provider, resource, and policy for `apply`; read for `verify`; read/delete for `teardown`.\n4. Save the `client_id` and `client_secret`. STS resolves the zone from the bound control key; the secret stays retrievable from the Applications page through an audited reveal.\n\n## Run the pipeline\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/controlBootstrap\ncp env.example .env\n$EDITOR .env\n. .env\nnpm run apply\nnpm run verify\n```\n\n`apply` is idempotent: rerunning it against an in-sync zone changes nothing, and rerunning it against a drifted zone converges the drift. Run teardown when you want to remove the environment:\n\n```bash\nnpm run teardown\n```\n\n## Files to study\n\n| File                | Purpose                                                                             |\n| ------------------- | ----------------------------------------------------------------------------------- |\n| `controlClient.mjs` | Exchanges client credentials at STS and calls the Control API.                      |\n| `plan.mjs`          | Declares the desired environment, drift checks, scope tiers, and env-driven config. |\n| `apply.mjs`         | Reconciles the live zone with the plan.                                             |\n| `verify.mjs`        | Read-only drift gate for CI.                                                        |\n| `teardown.mjs`      | Removes the environment in reverse dependency order.                                |\n\n## Test\n\n```bash\ncd caracal-examples/controlBootstrap\nnpm test\n```\n\nThe tests use a fake zone and mock transport and do not call a live Caracal stack.\n\n## Validate the workflow\n\nRun `apply` twice, then `verify`, introduce one safe drift, and run `verify` again. Expect no second write on unchanged apply, a clean verification, then a non-zero drift result. Teardown must remove objects in dependency order without affecting objects outside the plan.\n\n:::caution[Failure point: credential boundary]\nNever give the example a root admin token or application credential. Control keys are zone-bound automation credentials; keep each pipeline stage least-privileged and short-lived.\n:::\n\n## Next step\n\nContinue to [Check Provider Readiness](/examples/provider-preflight/) before sending traffic through a provider-backed Gateway resource."},{"id":"/v0.2/examples/provider-preflight","title":"Check Provider Readiness","url":"https://docs.caracal.run/v0.2/examples/provider-preflight/","concepts":"","body":"Provider Preflight is an automatable readiness check in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `providerPreflight/`. Use it after creating a provider-backed resource, before cutting traffic over, and from CI before each deploy. It walks the same chain a real Gateway request depends on - control plane, Gateway dependencies, application identity, provider binding and configuration, network paths, and the active policy decision - and reports exactly which link is broken and how to fix it.\n\n## Prerequisites\n\n- A provisioned application, provider, resource, operation set, and active policy.\n- A read-capable Admin token stored only in the CI secret environment.\n- Network placement comparable to Gateway for meaningful reachability checks.\n\n## What it checks\n\nChecks run in five phases:\n\n| Phase         | Check                  | What it confirms                                                                                                       |\n| ------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------- |\n| readiness     | Admin API readiness    | The control plane responds ready on `GET /ready`.                                                                      |\n| readiness     | Gateway readiness      | The Gateway responds ready, which verifies its bindings, Postgres, Redis, revocation freshness, audit replay, and STS. |\n| dependencies  | Resource binding       | The resource resolves to exactly one credential provider and a Gateway application.                                    |\n| dependencies  | Application            | The application exists, is not expired, and matches the resource's routing binding.                                    |\n| configuration | Provider configuration | Kind-required fields are present and `allowed_token_hosts` covers the token endpoint.                                  |\n| configuration | Scope coverage         | Every requested scope is declared on the resource.                                                                     |\n| configuration | Runtime injection      | Runtime-injection eligibility is enabled when required.                                                                |\n| connectivity  | Token endpoint host    | OAuth token endpoints are HTTPS and publicly routable, or explicitly granted as private egress.                        |\n| connectivity  | Callback reachability  | Authorization-code callback origins are HTTPS and reachable.                                                           |\n| connectivity  | Upstream reachability  | The protected upstream is reachable, with a warning when it resolves to a private address.                             |\n| authorization | Policy authorization   | Simulating the active policy set with the input shape STS uses for real token exchanges returns `allow`.               |\n\nThe preflight fails closed. Any failed check exits non-zero, and every failure or warning prints a `fix:` line with the concrete remediation.\n\n## Run the preflight\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/providerPreflight\nCARACAL_API_URL=http://127.0.0.1:3000 \\\nCARACAL_ADMIN_TOKEN=<admin-token> \\\nPREFLIGHT_ZONE_ID=<zone-id> \\\nPREFLIGHT_RESOURCE_ID=<resource-id> \\\nPREFLIGHT_APPLICATION_ID=<app-id> \\\nPREFLIGHT_SCOPES=pipernet:read,pipernet:write \\\nnpm run preflight\n```\n\nOptional settings:\n\n| Variable                                   | Purpose                                                                                                       |\n| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |\n| `PREFLIGHT_GATEWAY_URL`                    | Probes the Gateway's `/ready` endpoint; without it the Gateway phase reports a warning instead of validating. |\n| `PREFLIGHT_REQUIRE_RUNTIME_INJECTION=true` | Requires providers to allow runtime injection.                                                                |\n| `PREFLIGHT_OUTPUT=json`                    | Emits the full report as JSON for CI pipelines and dashboards.                                                |\n\nExit codes: `0` when all checks pass, `1` when any check fails, `2` when the preflight itself cannot run.\n\n## Network position\n\n`Upstream reachability` and `Callback reachability` run from the host executing the script. Run it from a network position comparable to Gateway, or inside the cluster, when you need the reachability result to match production routing.\n\n## Test\n\n```bash\ncd caracal-examples/providerPreflight\nnpm test\n```\n\nThe tests inject network, DNS, and policy responses and do not call live systems.\n\n## Validate the preflight\n\nRun once with valid config and once with a deliberately missing scope. Expect exit `0` and a clean summary, then exit `1` with a specific `fix:` line. JSON mode must remain parseable while narration stays off stdout.\n\n:::caution[Failure point: simulation is not traffic]\nA passing policy simulation and host probe do not prove credential injection or upstream behavior. Follow preflight with one real Gateway request and audit trace.\n:::\n\n## Next step\n\nContinue to [Iterate Policy Safely](/examples/policy-iterate/) when a denied request needs to become a tested policy change."},{"id":"/v0.2/examples/policy-iterate","title":"Iterate Policy Safely","url":"https://docs.caracal.run/v0.2/examples/policy-iterate/","concepts":"","body":"Policy Iterate is an audit-driven policy rollout loop in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `policyIterate/`. Use it when a real request is denied and you want to test a candidate policy change against the exact redaction-safe audit input - and prove it does not change other decisions - before activation.\n\n## Prerequisites\n\n- A denied request ID with an explainable audit trace.\n- A staged immutable policy-set version and current active version recorded for rollback.\n- Regression cases covering nearby allow and deny behavior.\n\n## Loop\n\n1. **Diagnose** - a request is denied and you capture its audit `request_id`. The audit explain endpoint reconstructs the denied `policy_input` along with the diagnostics and determining policies.\n2. **Simulate** - you edit the policy, stage a candidate policy-set version, and the example replays the denied input against it through the same OPA engine that serves live traffic.\n3. **Regress** - the example replays your expected-decision cases against the same candidate, so loosening a policy for one caller cannot silently change decisions for everyone else.\n4. **Decide** - activation is gated on evidence: the candidate allows the denied input, the rollout contract validates, simulation produced no warnings, and every regression case keeps its expected decision.\n5. **Activate** - with `ACTIVATE=true` and a clean verdict, the example activates the version and polls activation status until the STS runtime reports it loaded.\n\n## Run the iteration\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/policyIterate\nCARACAL_API_URL=http://127.0.0.1:3000 \\\nCARACAL_ADMIN_TOKEN=<admin-token> \\\nCARACAL_ZONE_ID=<zone-id> \\\nDENIED_REQUEST_ID=<denied-request-id> \\\nPOLICY_SET_ID=<policy-set-id> \\\nCANDIDATE_VERSION_ID=<staged-version-id> \\\nREGRESSION_FILE=./regressions.json \\\nnpm run iterate\n```\n\nThe run is a dry run by default: it narrates each phase on stderr, prints a JSON report on stdout, and exits `0` only when the verdict is clean. Re-run with `ACTIVATE=true` to roll out the version and wait for propagation.\n\n`REGRESSION_FILE` is optional and points to a JSON array of `{ name, expect, input }` cases - see `regressions.example.json` in the example directory for the format.\n\n## Claims note\n\nActor and subject claims are not written to audit, so reconstructed input does not contain them. For a claim-dependent denial, add the relevant `context.actor_claims` to a regression case built from the printed `policyInput` and iterate with that case instead.\n\n## Test\n\n```bash\ncd caracal-examples/policyIterate\nnpm test\n```\n\nThe tests inject the Admin API transport and do not call a live Caracal stack.\n\n## Validate the iteration\n\nKeep `ACTIVATE` unset first. Expect the candidate to repair the target denial while every regression retains its decision. Only then activate and wait for `loaded`; verify the first real exchange names the candidate manifest.\n\n:::caution[Failure point: reconstructed input]\nAudit reconstruction is redaction-safe and omits actor/subject claims. If a decision depends on implemented federated claims, supply them explicitly in a reviewed regression case; never guess them from a display name.\n:::\n\n## Next step\n\nContinue to [Launch Research Agent](/examples/research-agent/) to see runtime credential injection for a plain CLI agent."},{"id":"/v0.2/examples/research-agent","title":"Launch Research Agent","url":"https://docs.caracal.run/v0.2/examples/research-agent/","concepts":"","body":"Research Agent is a `caracal run` example in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `ResearchAgent/`. It launches a normal Node.js CLI agent and injects provider-native credentials only into the child process after Caracal authorizes the run.\n\n## When to use it\n\nUse it to evaluate one-shot runtime credential injection for an existing CLI that cannot accept an SDK transport. Do not use it as the pattern for a long-running service or as proof of request-level Gateway enforcement.\n\n## Prerequisites\n\n- Three provider-backed resources with runtime injection enabled and least-privilege binding scopes.\n- A Launcher workload and owner-only workload secret.\n- Synthetic or development provider credentials; the offline test does not call third parties.\n\n## What it demonstrates\n\n| Resource | Injected env | Used for |\n| --- | --- | --- |\n| `resource://google-drive` | `GOOGLE_DRIVE_ACCESS_TOKEN` | Searching and exporting Google Drive documents. |\n| `resource://google-calendar` | `GOOGLE_CALENDAR_ACCESS_TOKEN` | Reading relevant Calendar events. |\n| `resource://openai` | `OPENAI_API_KEY` | Answering the terminal question with model context. |\n\nThe agent has no Caracal SDK dependency. It reads provider-native environment variables and behaves like an existing third-party terminal tool.\n\n## Console setup\n\nUse Console to create or select:\n\n| Object | Purpose |\n| --- | --- |\n| Zone | Owns the workload, providers, resources, and policies. |\n| Workload | Identity used by `caracal run` to call STS; created on the **Launcher** page. |\n| Google Drive provider | Returns a Drive read token. |\n| Google Calendar provider | Returns a Calendar read token. |\n| OpenAI provider | Returns an OpenAI-compatible credential. |\n| Resources | Map `resource://google-drive`, `resource://google-calendar`, and `resource://openai` to providers. |\n| Policy | Allows the workload to request all three resources. |\n\nEnable runtime injection on each provider.\n\nThen open the console's **Launcher** page, create a workload named `research agent`, and add three launch bindings:\n\n| Environment variable | Resource |\n| --- | --- |\n| `GOOGLE_DRIVE_ACCESS_TOKEN` | `resource://google-drive` |\n| `GOOGLE_CALENDAR_ACCESS_TOKEN` | `resource://google-calendar` |\n| `OPENAI_API_KEY` | `resource://openai` |\n\nSelect the read-only scopes on the Drive and Calendar bindings so each injected credential carries only what the agent needs. The Launcher page then shows the exact launch commands.\n\n## Store the workload secret\n\nThe launcher needs one local file: the workload's secret at the runtime secret path. Copy it from the Launcher page - it stays retrievable there, with every reveal audited.\n\n```bash\nexport CARACAL_WORKLOAD_ID=\"<workload ID from the Launcher page>\"\nmkdir -p ~/.config/caracal/runtime/$CARACAL_WORKLOAD_ID\nprintf '%s' '<workload secret>' > ~/.config/caracal/runtime/$CARACAL_WORKLOAD_ID/secret\nchmod 600 ~/.config/caracal/runtime/$CARACAL_WORKLOAD_ID/secret\n```\n\nDo not export `GOOGLE_DRIVE_ACCESS_TOKEN`, `GOOGLE_CALENDAR_ACCESS_TOKEN`, or `OPENAI_API_KEY` yourself. Caracal injects them into the child process after STS authorization.\n\n## Launch the agent\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/ResearchAgent\nexport CARACAL_WORKLOAD_ID=\"<workload ID from the Launcher page>\"\ncaracal run -- node agent.mjs\n```\n\nThe agent confirms the injected credentials (values masked) and opens an interactive prompt:\n\n```text\n[agent] credential preflight (values masked, injected by launcher):\n[agent]   GOOGLE_DRIVE_ACCESS_TOKEN  present  -> Google Drive (read-only scope)\n[agent]   GOOGLE_CALENDAR_ACCESS_TOKEN  present  -> Google Calendar (read-only scope)\n[agent]   OPENAI_API_KEY  present  -> OpenAI\nCaracal run research agent ready. Ask about Drive docs or Calendar events. Type \"exit\" to quit.\n>\n```\n\nStarted directly with `node agent.mjs`, the preflight fails with exit code 2 before any network call. Credentials disappear with the child-process environment when the process exits.\n\n## Test\n\n```bash\ncd caracal-examples/ResearchAgent\npnpm test\n```\n\nThe tests do not contact Google, OpenAI, or Caracal.\n\n## Validate the launch\n\nRun the agent directly and expect exit `2`, then run it through `caracal run` and expect all three masked preflight entries. Confirm binding fetch and credential mints in Audit, then exit and verify credentials are absent from the parent shell.\n\n:::caution[Failure point: static authority]\nInjected API keys or bearer tokens can carry more upstream authority than Caracal binding scopes suggest. Scopes gate release; they cannot narrow a static provider credential. Prefer Gateway brokering for strong per-request enforcement.\n:::\n\n## Next step\n\nContinue to [Run Lynx Capital](/examples/lynx-capital/) when you want a full app reference lab with live Console inspection."},{"id":"/v0.2/examples/lynx-capital","title":"Run Lynx Capital","url":"https://docs.caracal.run/v0.2/examples/lynx-capital/","concepts":"","body":"Lynx Capital is a runnable reference in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) under `lynxCapital/`. It models a\nfinance-operations platform: an LLM swarm of orchestrators, regional workflows, and\nthousands of ephemeral domain workers executes payout cycles across twenty partner\nproviders, with every agent and every provider call governed by Caracal. It is the primary\nreference for modelling permission boundaries, Sessions for agents, providers, resources, and\npolicies on Caracal.\n\n## When to use it\n\nUse this fixed-name reference only after a smaller example works. Study it for multi-application permission boundaries, per-agent Sessions, provider views, policy data, and test organization; do not copy its finance domain or dependency pins blindly.\n\n## Prerequisites\n\n- Python, Docker, OPA, an isolated development zone, and a scoped provisioning Control key.\n- Capacity to run offline policy and Python suites before live provisioning.\n- Separate secrets for every managed application boundary.\n\n## Architecture\n\n| Building block | Role                                                                                                                                                                                                                             |\n| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Applications   | `lynx-operations`, `lynx-intake`, `lynx-ledger`, `lynx-compliance`, `lynx-treasury`, `lynx-payments`, `lynx-audit` - one **managed application** per permission boundary, each holding only its own partner authority.           |\n| Agents         | Every agent execution - orchestrator or worker - is its own **Session** under its role's application, labeled `[role, lynx-swarm]` with run and agent metadata, narrowed by a Delegation to its role's scopes and views.         |\n| Providers      | Twenty partner **credential providers** (`provider://<slug>`), each registered in the exact config shape its kind supports: API key, bearer token, OAuth client credentials, OAuth authorization code, Caracal mandate, or none. |\n| Resources      | Per-application **resource views** (`resource://<app>-<provider>`). The Gateway binds each view to exactly one application, so shared partners expose one view per boundary, each carrying only that boundary's scopes.          |\n| Policy set     | `lynx-finance-ops`: default-deny base, generated bindings and grants data, and one shared decisions policy allowing exactly each application's role-granted mandate mints and gateway calls.                                     |\n\nThe model is declared once in `config/tenancy.yaml`; the SDK seam, agent runner,\nprovisioning, and policy all read from it.\n\n### Why one application per permission boundary\n\nA Caracal application is a credential and trust boundary, and the Gateway binds each\nresource to exactly one application. Splitting the swarm by permission boundary means a\npayments worker and an audit worker can both reach the same partner - through different\nviews, with different scopes - while a compromised intake agent can never present payment\nauthority. Agent executions are Sessions, not applications: each started Session gets its own identity, labels,\nDelegation, and audit trail without minting new application credentials.\n\n### Sessions for agents\n\nThe swarm's runner gives every agent its own session via the SDK's `session()`:\norchestrators inherit under the operations boundary; each domain worker starts under its\napplication's per-run dispatcher root with `Authority.narrow(role scopes, views, max_hops=1,\nrun TTL)`. Ad-hoc partner-integration workers resolve their boundary, scope, and view\ndynamically from the requested provider operation. Logs and policy decisions identify\nexactly which agent did what.\n\n### Customer attribution and confinement\n\nWorkers acting on one customer's records - invoicing, dunning, payment application - start Sessions\nwith a `customer:<id>` label and a `customer_id` metadata key. The metadata key makes\nper-customer audit a direct filter over the shared zone trail; the label is policy input,\nand the base policy confines customer-labeled agents to the customer-record scopes, so a\nworker dunning one customer can never mint treasury or payment-rail authority. This is the\n[Serve Your Own Customers](/guides/serve-customers/) pattern applied to app-only work: one\nzone, customer separation carried by sessions, labels, metadata, and policy.\n\n## Setup flow\n\n## Commands\n\n```bash\ngit clone https://github.com/Garudex-Labs/examples.git caracal-examples\ncd caracal-examples/lynxCapital\npython -m venv .venv\nsource .venv/bin/activate\npip install -e \".[dev]\"\ncp -n .env.example .env\n```\n\nThe workload `.env` carries the zone and one `LYNX_CARACAL_<APP>_APPLICATION_ID` /\n`_CLIENT_SECRET` pair per boundary. Provisioning uses a separate operator file and a\nscoped Control key created once in Console.\n\n```bash\ncp -n .env.provision.example .env.provision   # set CONTROL_CLIENT_ID / _SECRET\n. .env.provision\npython scripts/provision.py     # applications, providers, views, policy set (idempotent)\npython scripts/reference.py     # SDK walkthrough: labeled Sessions, narrowed authority, mandates\npython scripts/teardown.py      # remove the provisioned objects\n```\n\n`provision.py` prints the per-application credential exports as it creates each\napplication; each client secret is returned exactly once. It also renders the\napplication-id bindings into the policy library before authoring it, so policy decisions\nkey on the real control-plane UUIDs.\n\n## Policies\n\n`policies/` is an importable, OPA-tested library. The base policy default-denies and owns\nthe decision contract; generated data documents carry the application bindings and the\nresource-view grants; one shared decisions policy allows ma"},{"id":"/v0.2/api","title":"Use API Reference","url":"https://docs.caracal.run/v0.2/api/","concepts":"","body":"Use this section when you need wire-level behavior. For task workflows, start in [Guides](/guides/). For service ownership and operations, use [Understand Services](/services/) and [Operations](/operations/).\n\nMost integrations should use an SDK or the web console. Only the Admin API is a general external management API. Coordinator is a wire-facing SDK protocol, STS is an OAuth endpoint, Gateway is a protected reverse proxy, and event topics are deployment infrastructure rather than end-user HTTP APIs.\n\n## API Surfaces\n\n| Surface                                  | Base                | Purpose                                                                                                            |\n| ---------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------ |\n| [Use the Admin API](/api/control-plane/) | API service `/v1`   | Zones, applications, providers, resources, policies, policy sets, grants, Authority records, Sessions, audit, approvals, and templates. |\n| [Use Coordinator API](/api/coordinator/) | Coordinator service | Sessions, long-lived Session services, invocations, Delegations, and SDK lifecycle endpoints.                                   |\n| [Use STS Endpoint](/api/sts/)            | STS service         | OAuth token exchange, JWKS, Approval status, and internal policy operations.                                       |\n| [Proxy Through Gateway](/api/gateway/)   | Gateway service     | Protected reverse-proxy behavior rather than CRUD endpoints.                                                       |\n| [Use Event Topics](/api/event-topics/)   | Redis Streams       | Audit, invalidation, revocation, Session, invocation, and Delegation topics.                                       |\n\n## Service Ports\n\n| Service     | Local port              |\n| ----------- | ----------------------- |\n| API         | `3000`                  |\n| Coordinator | `4000`                  |\n| STS         | `8080`                  |\n| Gateway     | `8081`                  |\n| Audit       | `9090`                  |\n| Control     | API `3000` when enabled |\n\n## Error Shape\n\nCaracal service errors use the shared OAuth-compatible shape:\n\n```json\n{\n  \"error\": \"access_denied\",\n  \"error_description\": \"policy denied request\",\n  \"requestId\": \"018f...\"\n}\n```\n\nSee [Error Codes](/reference/errors/) for canonical codes.\n\nDo not assume one status code across surfaces: STS follows OAuth semantics, adapters distinguish 401 from 403, and Gateway can return either a Caracal preflight error or the upstream response. Branch on the documented machine-readable `error` field where a Caracal JSON error is present.\n\n## Stability\n\nThe public Admin `/v1` routes and STS RFC 8693 exchange are supported integration surfaces. Coordinator field names such as `agents`, `agent_session_id`, and `delegation_edge_id` are protocol names retained for SDK interoperability; applications should use facade names. `/internal/*`, operator routes, private STS directives, and raw Redis topics are not general application APIs.\n\n## Reading Path\n\n| Need                                      | Page                                     |\n| ----------------------------------------- | ---------------------------------------- |\n| Manage product objects over HTTP          | [Use the Admin API](/api/control-plane/) |\n| Manage Session and Delegation runtime state | [Use Coordinator API](/api/coordinator/) |\n| Exchange authority for mandates           | [Use STS Endpoint](/api/sts/)            |\n| Understand Gateway proxy requirements     | [Proxy Through Gateway](/api/gateway/)   |\n| Consume or verify stream contracts        | [Use Event Topics](/api/event-topics/)   |\n\n## Next Step\n\nStart with [Use the Admin API](/api/control-plane/) when automating product setup."},{"id":"/v0.2/api/control-plane","title":"Use the Admin API","url":"https://docs.caracal.run/v0.2/api/control-plane/","concepts":"","body":"The Admin API is served by the API service on port `3000`. Management routes are registered under `/v1` and are protected by admin authentication.\n\nThis is the supported HTTP surface for trusted automation. Prefer [Admin Package](/sdks/admin/) for typed pagination and errors. Never expose an admin token to an agent, browser, or protected upstream.\n\n## Authentication\n\nSend an admin token as a bearer credential on every `/v1` request:\n\n```\nAuthorization: Bearer <admin token>\n```\n\n| Status | Error                                         | Meaning                                           |\n| ------ | --------------------------------------------- | ------------------------------------------------- |\n| `401`  | `unauthorized`, `invalid_admin_token`         | Token is missing, malformed, expired, or revoked. |\n| `403`  | `admin_token_read_only`                       | Write attempted with a read-only token.           |\n| `403`  | `admin_token_zone_mismatch`, `zone_forbidden` | Token is scoped to a different zone.              |\n| `403`  | `system_zone_read_only`                       | Write attempted against the reserved system zone. |\n\n## Error Shape\n\nEvery non-2xx `/v1` response carries a single JSON envelope:\n\n```json\n{\n  \"error\": \"invalid_body\",\n  \"error_description\": \"Request body failed validation\",\n  \"details\": { \"issues\": [{ \"path\": [\"name\"], \"message\": \"Required\" }] }\n}\n```\n\n`error` is the stable machine code to branch on. `error_description`, when present, is a human-readable summary of the failure. `details`, when present, carries structured context: for example, the validation issues on `invalid_body`, the failed connectivity check on `provider_check_failed`, or the live dynamic-client count on `dcr_shutdown_required`. See [Error Codes](/reference/errors/) for the shared code catalog.\n\nMalformed JSON, unknown fields in strict schemas, wrong types, missing required values, invalid identifiers, and zone mismatches are rejected before mutation. Success status depends on the operation: reads and updates return `200`, creates commonly return `201`, asynchronous policy activation returns `202`, and deletes commonly return `204`. Treat each endpoint response as authoritative rather than assuming every write returns an object.\n\n## Health and Diagnostics\n\n| Method | Path       | Purpose                           |\n| ------ | ---------- | --------------------------------- |\n| `GET`  | `/health`  | Liveness check.                   |\n| `GET`  | `/ready`   | Dependency and service readiness. |\n| `GET`  | `/metrics` | Service metrics.                  |\n| `GET`  | `/docs`    | Optional API docs when enabled.   |\n\n## Core Resources\n\n| Resource     | Collection                                   | Item                                                        | Extra                                                                                                                                                      |\n| ------------ | -------------------------------------------- | ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Zones        | `GET`, `POST /v1/zones`                      | `GET`, `PATCH`, `DELETE /v1/zones/:id`                      | `GET /v1/zones/:id/dcr-status`, `GET /v1/zones/:id/overview`                                                                                               |\n| Applications | `GET`, `POST /v1/zones/:zoneId/applications` | `GET`, `PATCH`, `DELETE /v1/zones/:zoneId/applications/:id` | `POST /v1/zones/:zoneId/applications/dcr`, `POST /v1/zones/:zoneId/applications/:id/rotate-secret`, `GET /v1/zones/:zoneId/applications/:id/client-secret` |\n| Providers    | `GET`, `POST /v1/zones/:zoneId/providers`    | `GET`, `PATCH`, `DELETE /v1/zones/:zoneId/providers/:id`    | -                                                                                                                                                          |\n| Resources    | `GET`, `POST /v1/zones/:zoneId/resources`    | `GET`, `PATCH`, `DELETE /v1/zones/:zoneId/resources/:id`    | -                                                                                                                                                          |\n| Workloads    | `GET`, `POST /v1/zones/:zoneId/workloads`    | `GET`, `PUT`, `DELETE /v1/zones/:zoneId/workloads/:id`      | `POST /v1/zones/:zoneId/workloads/:id/rotate-secret`, `GET /v1/zones/:zoneId/workloads/:id/secret`                                                         |\n\nWorkloads are the launcher identities `caracal run` uses. Create a workload, deliver its secret to the launch environment, and rotate the secret here when you automate runtime setup; the secret stays retrievable from Secret Store custody through an audited reveal endpoint.\n\n## Policy and Access\n\n| Area                 | Read                                                                                                                                                                                                 | Write                                                                                                                                                                                                                                     |\n| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Policy validation    | -                                                                                                     "},{"id":"/v0.2/api/coordinator","title":"Use Coordinator API","url":"https://docs.caracal.run/v0.2/api/coordinator/","concepts":"","body":"Coordinator is served on port `4000`. It owns governed Session and Delegation graph state. This page documents the raw protocol, whose stable paths and fields retain `agents`, `agent_session_id`, and `delegation_edge_id` names.\n\n**Wire-facing API:** application code normally should not call Coordinator directly. The SDK owns credential selection, idempotency, retries, context binding, generation fencing, and cleanup.\n\n## Authentication and Errors\n\nRuntime routes require a Caracal bearer mandate whose zone matches `:zoneId`, whose application is active, and whose scope authorizes the operation. The configured operator token is accepted only on an allowlisted management subset; traversal and impact require `coordinator.admin`. `/v1/verify`, health, and readiness are exceptions documented below.\n\nProtocol errors are JSON with at least `error`; common statuses are `400` validation, `401` missing/invalid/expired bearer, `403` ownership or scope failure, `404` missing object, `409` lifecycle/idempotency conflict, `429` capacity or receipt limit, and `5xx` dependency failure.\n\n## Health and Metrics\n\n| Method | Path       | Purpose                       |\n| ------ | ---------- | ----------------------------- |\n| `GET`  | `/health`  | Liveness check.               |\n| `GET`  | `/ready`   | Dependency and job readiness. |\n| `GET`  | `/metrics` | Service metrics.              |\n\n## Session Management\n\n| Method   | Path                                 | Purpose                                          |\n| -------- | ------------------------------------ | ------------------------------------------------ |\n| `POST`   | `/zones/:zoneId/agents`              | Start a Session. `/agents` is the protocol path. |\n| `GET`    | `/zones/:zoneId/agents`              | List Sessions.                                   |\n| `GET`    | `/zones/:zoneId/agents/:id`          | Inspect one Session.                             |\n| `GET`    | `/zones/:zoneId/agents/:id/children` | List child Sessions.                             |\n| `PATCH`  | `/zones/:zoneId/agents/:id/suspend`  | Suspend a Session subtree.                       |\n| `PATCH`  | `/zones/:zoneId/agents/:id/resume`   | Resume a suspended Session subtree.              |\n| `DELETE` | `/zones/:zoneId/agents/:id`          | Terminate a Session.                             |\n\n`POST /zones/:zoneId/agents` accepts `application_id`, protocol field `subject_session_id` (the Subject Authority-record ID), optional `parent_id`, `lifecycle` (`task` or `service`), `labels`, `ttl_seconds`, `parent_authority` (`inherit` or `none`), `inherit_parent_edge_id`, and `metadata`. Task TTL defaults to 3600 seconds and is bounded to 1–86400. Service lifetime is lease-based. The response carries protocol `agent_session_id`, optional inherited `delegation_edge_id`, and lifecycle/lease data.\n\nLists accept `limit` (default 100, maximum 500), opaque `cursor`, and route-specific filters. Responses use `{ \"items\": [...], \"next_cursor\": string | null }`.\n\n## Long-lived Sessions and Invocations\n\n| Method  | Path                                      | Purpose                                                                               |\n| ------- | ----------------------------------------- | ------------------------------------------------------------------------------------- |\n| `POST`  | `/zones/:zoneId/agent-services`           | Register or update a long-lived Session lease. `agent-services` is the protocol path. |\n| `GET`   | `/zones/:zoneId/agent-services`           | List long-lived Session leases.                                                       |\n| `POST`  | `/zones/:zoneId/agents/:id/heartbeat`     | Refresh a long-lived Session lease.                                                   |\n| `POST`  | `/zones/:zoneId/invocations`              | Create an invocation.                                                                 |\n| `GET`   | `/zones/:zoneId/invocations/:id`          | Inspect an invocation.                                                                |\n| `PATCH` | `/zones/:zoneId/invocations/:id/start`    | Mark an invocation running.                                                           |\n| `PATCH` | `/zones/:zoneId/invocations/:id/cancel`   | Cancel an invocation.                                                                 |\n| `PATCH` | `/zones/:zoneId/invocations/:id/complete` | Complete an invocation.                                                               |\n\n`POST /zones/:zoneId/agents/:id/lease` acquires a new generation before an SDK attaches. Heartbeat must carry the current generation; an older holder is fenced with conflict. Terminal missing, expired, or fenced results stop SDK auto-heartbeat.\n\n## Delegation\n\nRuntime credentials see only Delegations where their application is the issuer or receiver. Recursive traversal and impact analysis are operator-only because descendants can cross application boundaries and expose another application's topology or Authority record IDs. The managed operator token retains zone-wide inspection for Console and Admin SDK workflows.\n\n| Method  | Path                                                   | Purpose                                                                                              |\n| ------- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- |\n| `POST`  | `/zones/:zoneId/delegations`                           | Create a Delegation. The response field `delegation_edge_id` is the protocol name for Delegation ID. |\n| `GET`   | `/zones/:zoneId/delegations/active`                    | List active Delegations.                                                                             |\n| `GET`   | `/zones/:zoneId/delegations/inbound/:sessionId`        | List inbound edges for a session.                                                                    |\n| `GET`   | `/zones/:zoneId"},{"id":"/v0.2/api/sts","title":"Use STS Endpoint","url":"https://docs.caracal.run/v0.2/api/sts/","concepts":"","body":"STS is served on port `8080` and issues scoped Caracal mandate JWTs.\n\nApplications normally call it through the OAuth or application SDK. The public exchange follows RFC 8693 framing with Caracal parameters; internal policy and signing-key endpoints are service-to-service only.\n\n## Public Endpoints\n\n| Method | Path                                    | Purpose                                                                               |\n| ------ | --------------------------------------- | ------------------------------------------------------------------------------------- |\n| `POST` | `/oauth/2/token`                        | OAuth token exchange for resource, session, Gateway, or delegated mandates.           |\n| `GET`  | `/.well-known/jwks.json?zone_id={zone}` | Public signing keys for mandate verification, scoped per zone.                        |\n| `GET`  | `/step-up/{id}`                         | Approval hold state; `?wait={seconds}` long-polls until the state changes.            |\n| `POST` | `/step-up/{id}/decision`                | Subject-plane approval decision, authenticated with a federated user session mandate. |\n| `GET`  | `/health`                               | Liveness check.                                                                       |\n| `GET`  | `/ready`                                | Readiness check.                                                                      |\n| `GET`  | `/metrics`                              | Prometheus metrics.                                                                   |\n| `GET`  | `/metrics.json`                         | JSON metrics.                                                                         |\n\n## Token Exchange Request\n\n`POST /oauth/2/token` accepts body-only `application/x-www-form-urlencoded` parameters. Query parameters, duplicate singleton fields, and other media types are rejected. Repeated `resource` fields are allowed; STS trims, exact-deduplicates, and sorts them before evaluation.\n\n| Parameter                                                    | Purpose                                                                                               |\n| ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- |\n| `grant_type`                                                 | OAuth grant type.                                                                                     |\n| `subject_token`, `subject_token_type`                        | Existing authority to exchange: a session mandate this STS issued.                                    |\n| `resource`                                                   | One or more target resource identifiers.                                                              |\n| `scope`                                                      | Requested scopes.                                                                                     |\n| `zone_id`                                                    | Zone boundary.                                                                                        |\n| `application_id`                                             | Calling application.                                                                                  |\n| `client_secret`                                             | Application authentication.                                                                           |\n| `session_id`, `agent_session_id`, `delegation_edge_id`       | Protocol names for Authority record ID, Session ID, and Delegation ID. They are distinct identifiers. |\n| `ttl_seconds`                                                | Requested TTL.                                                                                        |\n| `challenge_id`                                               | Consumes an approved Approval hold during retry.                                                      |\n\n`grant_type` must be `urn:ietf:params:oauth:grant-type:token-exchange`. `subject_token_type` must match the presented token class. `scope` is a space-delimited string. `ttl_seconds` must be a positive integer within server limits. Resource identifiers, zone, application, Session, Authority-record, and Delegation bindings are validated server-side; caller-supplied identifiers never establish authority by themselves.\n\nRFC 8693 `actor_token` is not supported: a request that carries one is rejected with `invalid_token`. Public `client_assertion` and `client_assertion_type` fields are also rejected; application authentication uses `client_secret`. The acting application's identity reaches policy input as `input.context.actor_claims.caracal_client_id`.\n\n## Subject Identity\n\nSTS never generates a subject identity. Every exchange records its subject verbatim: the `sub` claim of the presented `subject_token`, or the authenticated application's id when no subject token is presented. Two subject token classes are accepted:\n\n- `urn:ietf:params:oauth:token-type:access_token` - a session mandate this STS issued for the same zone; resource mandates are rejected as subject tokens (RFC 8693 subject-confusion mitigation).\n- `urn:ietf:params:oauth:token-type:id_token` - **subject federation**: an end user's identity token from a registered Subject issuer. The application authenticates with its client credentials and relays the token; STS verifies it against the issuer's JWKS and creates an Authority record whose `sub` is copied verbatim. A federation exchange names no resources and mints no scopes: the record is an identity and revocation anchor, not resource authority.\n\nThe Subject identifier is opaque to Caracal. It may be a user ID, UUID, employee ID, or customer ID. STS requires only that the same identity always presents the same value. The Authority record stores that `sub`; provider connections, approval bindings, revocation, and audit"},{"id":"/v0.2/api/gateway","title":"Proxy Through Gateway","url":"https://docs.caracal.run/v0.2/api/gateway/","concepts":"","body":"Gateway is served on port `8081`. It is not a CRUD API; it is a protected reverse proxy for configured resources.\n\nUse an SDK transport instead of constructing requests manually. Gateway does not offer pagination, management CRUD, or an application-visible idempotency API.\n\n## Operator Endpoints\n\n| Method | Path                           | Purpose                     |\n| ------ | ------------------------------ | --------------------------- |\n| `GET`  | `/health`                      | Liveness check.             |\n| `GET`  | `/ready`                       | Readiness check.            |\n| `GET`  | `/metrics`                     | Prometheus metrics.         |\n| `GET`  | `/metrics.json`                | JSON metrics.               |\n| `POST` | `/internal/revocations/reload` | Reload revocation snapshot. |\n\n## Request Requirements\n\n| Input                       | Required | Purpose                                                      |\n| --------------------------- | -------- | ------------------------------------------------------------ |\n| `Authorization: Bearer ...` | yes      | One-shot Caracal Gateway-ingress mandate (`use=gateway`).    |\n| `X-Caracal-Resource`        | yes      | Resource identifier STS resolves to the routed upstream.     |\n| Request path                | yes      | Forwarded to the configured upstream after traversal checks. |\n\nThe original HTTP method, query, headers, and body are proxied after safety filtering. Gateway strips hop-by-hop headers, caller `Authorization`, internal Caracal routing headers, `Forwarded`, and `X-Real-IP`, then applies the resource's provider credential. `X-Caracal-Resource` is routing metadata, not authorization; the signed mandate and STS decision are authoritative.\n\nGateway rejects any request that sets `X-Caracal-Client-ID`; the caller's application identity derives from the verified mandate's `client_id` claim.\n\n## Denial Checks\n\nGateway rejects before upstream dispatch. Treat these checks as the safety gate in front of every protected upstream.\n\n| Stage               | Checks                                                                                                                           |\n| ------------------- | -------------------------------------------------------------------------------------------------------------------------------- |\n| Request preflight   | Bearer token exists, token size is bounded, `X-Caracal-Resource` exists, and request path is not traversal.                      |\n| Token validation    | Token is well formed, not expiring inside the preflight window, signature-valid, not replayed, not revoked, and includes a zone. |\n| Resource resolution | STS resolves `(zone_id, resource)` to a gateway-routed resource, and the returned upstream passes host safety checks.            |\n| STS exchange        | Gateway’s STS circuit is closed and the signed exchange succeeds; STS enforces the resource’s declared scopes on the mandate.    |\n\n## Forwarding Behavior\n\nAfter checks pass, Gateway performs a signed STS exchange of the inbound `use=gateway` mandate, receives a `use=resource` mandate plus a private upstream directive, forwards the request to the resource's upstream, and emits audit evidence. Request and upstream timeouts are controlled by Gateway service config.\n\nGateway proxies HTTP request/response traffic, including streamed responses such as SSE. Streamed bodies are flushed chunk by chunk, and revocation anchors are re-checked on every chunk boundary; a mid-stream revocation truncates the stream and sets the `X-Caracal-Revoked` trailer. Gateway does not proxy WebSocket connections: hop-by-hop headers, including `Upgrade`, are stripped before forwarding, so upgrade handshakes never reach the upstream. Protect WebSocket workloads with in-process verification at the service edge instead, using [Protect an MCP Server](/guides/protect-mcp/) or a framework adapter.\n\n## Replay Protection\n\nEvery Gateway-ingress mandate is single-use. On first presentation Gateway records the token's `jti` in Redis with a lifetime equal to the token's remaining validity; presenting the same mandate again is rejected as a confirmed replay and emits a `replay_detected` audit event carrying the request id, resource, and client identity. Session mandates are not valid Gateway inputs. There is no client-side nonce to manage: the mandate's `jti` is the replay key. Scoped SDK transports therefore bypass mandate caching and single-flight for the final exchange, and application transports cache only their source/target Session and Delegation state before minting a fresh Gateway-ingress mandate per request. They never retry the data-plane request or replay its body. When Redis is unreachable, the production default rejects the request rather than silently widening the replay window; a deployment can opt into fail-open, which forwards the request and logs the tracker error.\n\n## Responses and Failures\n\nAfter dispatch, Gateway streams the upstream status, headers, and body. Before dispatch, Caracal failures use JSON `error`, `error_description`, and request correlation where available. Typical classes are `400` malformed routing input, `401` missing/invalid/expired/replayed/revoked mandate, `403` operation or authority denial, `404` unresolved resource, `502` upstream or STS failure, and `503` open dependency circuit. Do not assume a non-2xx response came from Gateway after dispatch; it may be the upstream's response.\n\nRetries are a business-operation decision. Mint a fresh mandate for each new attempt and retry only when the HTTP method/body and upstream contract are idempotent.\n\n## Next Step\n\nContinue to [Use Event Topics](/api/event-topics/) to understand the Redis Stream topics that carry audit, invalidation, revocation, and Session lifecycle events.\n\n## Related Pages\n\n- [Protect Upstreams](/services/gateway/)\n- [Enforce Boundaries](/architecture/trust-boundaries/)\n- [Protect an MCP Server](/guides/protect-mcp/)"},{"id":"/v0.2/api/event-topics","title":"Use Event Topics","url":"https://docs.caracal.run/v0.2/api/event-topics/","concepts":"","body":"Caracal uses Redis Streams for propagation. Published modes sign stream messages with `STREAMS_HMAC_KEY`.\n\n**Internal wire API:** applications normally should not consume these topics. They are deployment contracts for Caracal services, audit exporters, and revocation backends. Topic names and payload fields are protocol-facing and may retain `agents` terminology.\n\n## Topics\n\n| Topic                            | Producers                               | Consumers                                          |\n| -------------------------------- | --------------------------------------- | -------------------------------------------------- |\n| `caracal.audit.events`           | API, STS, Gateway, Coordinator, Control | Audit `audit-ingestor`, SIEM exporters             |\n| `caracal.audit.events.dlq`       | Audit                                   | DLQ observers                                      |\n| `caracal.policy.invalidate`      | API                                     | STS policy loader                                  |\n| `caracal.sessions.revoke`        | API, Coordinator                        | STS, Gateway, resource-server revocation consumers |\n| `caracal.keys.invalidate`        | API, STS                                | STS key caches                                     |\n| `caracal.agents.lifecycle`       | Coordinator                             | Coordinator lifecycle relay job                    |\n| `caracal.invocations.lifecycle`  | Coordinator                             | Invocation observers                               |\n| `caracal.delegations.invalidate` | Coordinator                             | Delegation observers                               |\n| `caracal.providers.ratelimit`    | Provisioner/provider coordination       | Provider rate-limit coordination                   |\n\n## Consumer Groups\n\n| Topic                            | Groups                          |\n| -------------------------------- | ------------------------------- |\n| `caracal.audit.events`           | `audit-ingestor`, `siem-export` |\n| `caracal.audit.events.dlq`       | `audit-dlq-observer`            |\n| `caracal.policy.invalidate`      | `opa-engine`                    |\n| `caracal.sessions.revoke`        | `sts-revocation`, stable per-host `gateway-revocation:<host>`, deployment-specific resource groups |\n| `caracal.keys.invalidate`        | `sts-keys`                      |\n| `caracal.agents.lifecycle`       | `coordinator-relay`             |\n| `caracal.invocations.lifecycle`  | `invocations-observer`          |\n| `caracal.delegations.invalidate` | `delegations-observer`          |\n\n## Message Integrity\n\nSigned stream messages include the `_sig` field. Consumers in published modes must reject unsigned or mismatched messages for streams that require origin verification.\n\nMessages use Redis Stream IDs and field/value maps. Consumers must validate required fields and bounds before applying state, deduplicate by the event identity carried by the producer, and acknowledge only after durable application. Poison messages go to the stream's bounded dead-letter path where implemented. Pending messages must be claimed and retried with consumer-group semantics rather than copied as a new logical event.\n\nRevocation and Delegation epoch updates are monotonic security state: delayed delivery must never reduce the recorded epoch. Audit events are append-only evidence and must preserve `request_id`, occurrence time, and signature fields unchanged.\n\n## Access and Stability\n\nUse a dedicated least-privilege Redis identity and private TLS connectivity. Do not expose Redis publicly or distribute Admin tokens to consumers. Consumer group names shown here are repository defaults, not a multi-tenant coordination API; independently deployed resource servers should use deployment-specific groups and unique consumer names.\n\n## Related Pages\n\n- [Propagate Events](/architecture/event-streams/)\n- [Operate Redis Streams](/operations/redis/)\n- [Wire Contracts](/reference/interoperability-contracts/)"},{"id":"/v0.2/services","title":"Understand Services","url":"https://docs.caracal.run/v0.2/services/","concepts":"","body":"Use this section for integration boundaries and operations. It is not a requirement for ordinary console setup.\n\n## Service Selection\n\n| User-visible operation               | Service                               | Supported caller                                             |\n| ------------------------------------ | ------------------------------------- | ------------------------------------------------------------ |\n| Manage product and policy state      | [API](/services/api/)                 | Console BFF, Admin SDK, documented Admin API                 |\n| Start Sessions or create Delegations | [Coordinator](/services/coordinator/) | SDK, documented Coordinator API, console operator views      |\n| Exchange authority for a mandate     | [STS](/services/sts/)                 | SDK, `caracal run`, Gateway, documented token client         |\n| Protect an HTTP upstream             | [Gateway](/services/gateway/)         | Client presenting a Caracal mandate                          |\n| Ingest and retain decision evidence  | [Audit](/services/audit/)             | Caracal stream producers; operators read through console/API |\n| Automate zone management remotely    | [Control](/services/control/)         | Trusted automation with a scoped Control credential          |\n\n## Dependency Shape\n\nAll five runtime services expose health and readiness. Health means the process responds. Readiness includes dependencies and service-specific thresholds. Control has no separate process: it is an optional plugin on the API port.\n\n## Direct-Call Rule\n\nDo not call `/internal/*` routes, write service tables, publish Redis topics, or manipulate replay directories from application code. Internal routes are authenticated service-to-service contracts. Use the console, SDKs, Admin API, Coordinator API, STS token endpoint, Gateway proxy, or Control API as documented.\n\n## Next Step\n\n[Manage Product State](/services/api/) for the management path, or [Issue Mandates](/services/sts/) for the authority path."},{"id":"/v0.2/services/api","title":"Manage Product State","url":"https://docs.caracal.run/v0.2/services/api/","concepts":"","body":"The API owns Zones, Applications, workloads, Providers, Resources, Policies, Policy sets, Grants, Subject issuers, Subjects, Authority records, Approval holds, admin audit, and related management state.\n\n## Who Calls It\n\nHuman operators use the web console; its auth backend calls the API with derived operator credentials. Trusted automation uses the Admin SDK or documented `/v1` Admin API. The optional Control plugin dispatches into the same management implementation.\n\nWorkloads must not receive the API admin token or use management routes as a data-plane credential source.\n\n## Runtime Contract\n\n| Item                      | Local value          |\n| ------------------------- | -------------------- |\n| Port                      | `3000`               |\n| Liveness                  | `GET /health`        |\n| Readiness                 | `GET /ready`         |\n| Metrics                   | `GET /metrics`       |\n| Management prefix         | `/v1`                |\n| Optional interactive docs | `/docs` when enabled |\n\nUse [Use the Admin API](/api/control-plane/) for route and auth reference rather than treating this service page as an endpoint catalog.\n\n## Write and Propagation Flow\n\nThe state change and outbox row commit together. Redis publication follows asynchronously. A successful API response can therefore precede policy reload, revocation, or audit consumption by a short interval.\n\n## Failure Implications\n\n| Failure                     | User impact                                                                                      |\n| --------------------------- | ------------------------------------------------------------------------------------------------ |\n| Postgres unavailable        | Reads/writes and readiness fail.                                                                 |\n| Redis unavailable           | Readiness or outbox signals degrade; committed state remains in Postgres for retry.              |\n| Dead/old outbox rows        | Consumers may retain stale policy, revocation, or audit state; Diagnostics surfaces the backlog. |\n| Missing/invalid admin token | `401`; the console may report not connected or unauthorized.                                     |\n| Valid token without scope   | `403`; narrow the operation or grant the required operator scope.                                |\n| Secret backend unavailable  | Secret-bearing create, reveal, rotation, or dependent issuance fails closed.                     |\n\nDo not call the API's STS-coordination or internal service routes directly. The owning console or Admin SDK workflow supplies the required authentication and validation.\n\n## Next Step\n\n[Coordinate Session State](/services/coordinator/) or [Manage Product Objects](/runtime-console/admin/)."},{"id":"/v0.2/services/coordinator","title":"Coordinate Session State","url":"https://docs.caracal.run/v0.2/services/coordinator/","concepts":"","body":"Coordinator owns governed Sessions, service leases, invocations, Delegations, graph epochs, and their durable outbox.\n\n## Who Calls It\n\nApplications use Caracal SDK Session and Delegation APIs or the documented Coordinator API. The console BFF uses an operator credential to inspect and intervene. Top-level runtime commands do not manage Coordinator state.\n\nDo not place the operator Coordinator token in workload code. Do not write Coordinator tables or Redis lifecycle topics directly.\n\n## Runtime Contract\n\n| Item      | Local value    |\n| --------- | -------------- |\n| Port      | `4000`         |\n| Liveness  | `GET /health`  |\n| Readiness | `GET /ready`   |\n| Metrics   | `GET /metrics` |\n\nUse [Use Coordinator API](/api/coordinator/) for routes and SDK contracts.\n\n## Operational Flow\n\nSession and Delegation writes commit to Postgres with outbox rows. Publishers relay lifecycle, invocation, delegation-invalidation, and revocation events through Redis. STS consults authoritative state before issuing authority tied to that lineage.\n\nTask Sessions expire by TTL. Service Sessions require heartbeat renewal. Sweepers mark stale leases, enforce invocation deadlines, expire Delegations, and clean terminal records according to retention settings.\n\n## Failure Implications\n\n| Failure                     | User impact                                                                                 |\n| --------------------------- | ------------------------------------------------------------------------------------------- |\n| Postgres unavailable        | Session/Delegation operations and readiness fail.                                           |\n| Redis/outbox delayed        | Downstream lifecycle and invalidation views lag; durable state remains available for retry. |\n| Service heartbeat stops     | The Session becomes unhealthy or expires even if its process still exists.                  |\n| Sweeper unavailable         | Stale state remains longer; alert on job/readiness metrics.                                 |\n| Operator credential missing | Console Session and Delegation views fail; workload SDK credentials are a separate path.    |\n\n## Next Step\n\n[Coordinate Sessions](/architecture/delegation-flow/) for the cross-service flow or [Manage Runtime Authority](/runtime-console/agents/) for the operator workflow."},{"id":"/v0.2/services/sts","title":"Issue Mandates","url":"https://docs.caracal.run/v0.2/services/sts/","concepts":"","body":"STS authenticates application or workload proof, evaluates current authority, and issues short-lived mandates or configured provider credentials.\n\n## Supported Callers and Routes\n\n| Route                                                | Caller                                        |\n| ---------------------------------------------------- | --------------------------------------------- |\n| `POST /oauth/2/token`                                | SDK, documented token client, Gateway         |\n| `POST /v1/run/manifest`                              | `caracal run`                                 |\n| `POST /v1/run/credential`                            | `caracal run`                                 |\n| `GET /.well-known/jwks.json?zone_id=...`             | Verifiers                                     |\n| `GET /step-up/{id}`                                  | Waiting client with the required hold context |\n| `POST /step-up/{id}/decision`                        | Subject-plane end-user decision flow          |\n| `GET /health`, `/ready`, `/metrics`, `/metrics.json` | Probes and authorized monitoring              |\n\nSTS listens on local port `8080`.\n\nRoutes under `/internal/` for policy simulation/status and zone signing-key rotation are service-to-service APIs. Do not call or expose them as workload APIs; the API service owns those operations.\n\n## Identity Boundary\n\nSTS does not authenticate end users and does not invent a user `sub`. It accepts a token only from a configured subject issuer, then treats its stable `sub` as opaque. Without a subject token, application identity is the Subject.\n\n## Synchronous Dependencies\n\nIssuance needs Postgres product, policy, authority, Session, Delegation, approval, signing, and secret state. It also consumes Redis invalidation and revocation state, requires the Secret Store KEK, verifies Gateway HMAC proof on the Gateway path, and emits audit evidence to Redis or replay storage.\n\n## Failure Posture\n\nInvalid client proof, subject issuer, resource, policy, operation, scope, Authority record, Session, Delegation, approval, signing key, or Gateway signature denies issuance. There is no fallback token. Resource mandates are capped at 15 minutes and Session mandates at 60 minutes.\n\nAn `interaction_required` response means policy raised an Approval hold. Approval alone is not issuance; the client retries and consumes the hold once.\n\nUse [Use STS Endpoint](/api/sts/) for exact request and error contracts and [Exchange Tokens](/architecture/token-exchange-flow/) for the trust flow.\n\n## Next Step\n\n[Protect Upstreams](/services/gateway/)."},{"id":"/v0.2/services/gateway","title":"Protect Upstreams","url":"https://docs.caracal.run/v0.2/services/gateway/","concepts":"","body":"Gateway is the HTTP enforcement boundary for configured upstream resources. It listens on local port `8081`.\n\n## Supported Request\n\nA client sends an authorization bearer mandate, `X-Caracal-Resource`, and the intended HTTP request. Gateway resolves the configured zone/resource binding; callers do not select an arbitrary upstream.\n\n## Deny Before Upstream\n\nGateway does not contact the upstream when the bearer is missing, malformed, oversized, expiring, replayed, revoked, or signature-invalid; the resource header or binding is missing; an enforced operation/scope is absent; the path traverses; STS fails or its circuit is open; or host safety rejects the destination.\n\nIt rejects caller-supplied `X-Caracal-Client-ID`, strips hop-by-hop and caller authorization headers, and applies only the upstream credential returned through the trusted exchange.\n\n## Runtime and Protocol Limits\n\n| Item               | Behavior                                                            |\n| ------------------ | ------------------------------------------------------------------- |\n| Liveness/readiness | `/health`, `/ready`                                                 |\n| Monitoring         | `/metrics`, `/metrics.json`                                         |\n| Request size       | `MAX_REQUEST_BYTES`, 10 MiB by default                              |\n| HTTP streaming     | Supported, including SSE; revocation is rechecked between chunks    |\n| WebSocket upgrade  | Not supported; upgrade headers are stripped                         |\n| Revocation reload  | `POST /internal/revocations/reload`, service/operator-internal only |\n\nProtect WebSocket services in process with a verification package or framework adapter.\n\n## Dependency Implications\n\nGateway depends synchronously on STS and needs Postgres/Redis-backed binding, key, replay, and revocation state. It buffers audit evidence in replay storage if Redis/Audit delivery is unavailable. In published modes, replay/JTI and authority uncertainty fail closed; `JTI_FAIL_OPEN` is forbidden.\n\nUse [Proxy Through Gateway](/api/gateway/) for the client contract and [Harden Production](/operations/tls-hardening/) for network placement.\n\n## Next Step\n\n[Ingest Audit Evidence](/services/audit/)."},{"id":"/v0.2/services/audit","title":"Ingest Audit Evidence","url":"https://docs.caracal.run/v0.2/services/audit/","concepts":"","body":"Audit consumes signed events from Redis, verifies them, writes append-only evidence to Postgres, manages failed delivery, and exposes operator search and metrics on local port `9090`.\n\n## Supported Read Paths\n\nHuman operators use the console **Audit** workspace. Zone-scoped automation uses the API service's documented audit routes. The Audit service's direct search and DLQ routes are operator endpoints protected by `AUDIT_ADMIN_TOKEN`; when that token is not configured, they return `404`.\n\n| Route                                       | Purpose                               |\n| ------------------------------------------- | ------------------------------------- |\n| `GET /health`, `/ready`                     | Liveness and dependency/lag readiness |\n| `GET /metrics`, `/metrics.json`             | Authorized monitoring                 |\n| `GET /api/audit/search`                     | Direct operator search                |\n| `GET /api/audit/dlq`, `/api/audit/dlq/{id}` | Inspect failed events                 |\n| `POST /api/audit/dlq/replay`                | Retry selected failed evidence        |\n\nDo not send application audit records to these routes. Caracal services publish the signed event contract.\n\n## Delivery and Recovery\n\nAudit consumes `caracal.audit.events` in the `audit-ingestor` group. It drains its pending entries, claims orphaned work, retries failures, and moves events beyond the delivery limit to the DLQ. STS and Gateway replay volumes preserve events that could not reach Redis at emission time.\n\n## Integrity and Readiness\n\n`AUDIT_HMAC_KEY` verifies producer signatures in published modes. Content-hash mismatch, chain breaks, and HMAC failures are security signals, not retryable formatting issues. The database role cannot update or delete evidence rows.\n\nReadiness considers DLQ size, consumer lag, and oldest pending-entry age. A healthy HTTP process can therefore be not ready because evidence is no longer being retained within the configured operating bounds.\n\n## Operator Response\n\n1. Check Audit readiness and metrics.\n2. Check Redis connectivity, lag, pending entries, and replay volume growth.\n3. Inspect DLQ detail before replaying.\n4. Treat integrity failures as incidents; do not replay tampered payloads as trusted evidence.\n5. Confirm recovery with a fresh protected request and console decision trace.\n\nUse [Audit and Request Traces](/concepts/audit-ledger/) for evidence semantics and [Configure Alerts](/operations/alerts/) for thresholds.\n\n## Next Step\n\n[Automate Management](/services/control/) when a trusted remote client needs product-management automation."},{"id":"/v0.2/services/control","title":"Automate Management","url":"https://docs.caracal.run/v0.2/services/control/","concepts":"","body":"Control is the zone-bound remote automation counterpart to the web console. It is an optional plugin inside the API service, not a separate deployable or port.\n\n## Choose Control or the Admin SDK\n\nUse the Admin SDK when trusted automation can call the Admin API directly with an appropriately scoped operator credential. Use Control when automation needs a self-describing, zone-bound invoke surface with short-lived Control tokens, replay protection, per-command scopes, rate limits, and mandatory audit.\n\nNeither surface manages local stack lifecycle or launches workloads. Control must never appear as a top-level `caracal` runtime command.\n\n## Enablement and Route\n\n| Item                 | Contract                       |\n| -------------------- | ------------------------------ |\n| Host                 | API service, local port `3000` |\n| Invoke               | `POST /v1/control/invoke`      |\n| Build-time mount     | `CARACAL_CONTROL_ENABLED=true` |\n| Runtime gate         | `CONTROL_GATE_FILE` must exist |\n| Default Helm posture | Disabled                       |\n\nRemoving the gate file makes invoke return `503` without restarting API. API health and readiness remain the service probes.\n\n## Credential Flow\n\nThe token's zone comes from the Control key. A caller must not select another zone. Each token is replay-protected, so mint a fresh token for each invoke.\n\n## Supported Management Model\n\nControl exposes the shared management catalog, including imperative noun/verb operations and declarative `ensure` and `state plan|verify|apply` workflows. Prefer declarative reconciliation for repeatable CI: it is idempotent, reports per-object outcomes, authorizes each touched noun, and can dry-run before writes.\n\nUse `catalog describe` to discover commands, scopes, flags, and desired-state schema instead of hardcoding a copied catalog. Authority records and governed Sessions are separate nouns.\n\nFor exact payload examples, scopes, reconciliation documents, and error envelopes, use [Use the Admin API](/api/control-plane/) and [Bootstrap Control State](/examples/control-bootstrap/).\n\n## Failure Behavior\n\n| Condition                                 | Result                                                 |\n| ----------------------------------------- | ------------------------------------------------------ |\n| Gate absent                               | `control_disabled`, `503`                              |\n| Token missing or invalid                  | `unauthorized`, `401`                                  |\n| Token reused                              | `token_replay`, `401`                                  |\n| Scope or policy insufficient              | `denied`, `403`                                        |\n| Bound zone conflicts with requested state | `zone_mismatch`, `409`                                 |\n| Subject/source exceeds rate               | `rate_limited`, `429`                                  |\n| Required audit cannot be recorded         | `audit_unavailable`, `500`; operation does not execute |\n| Body exceeds 256 KiB                      | `413`                                                  |\n\nControl depends on STS JWKS/issuer validation, Redis replay/rate state, the API's downstream credential, and durable audit. Keep it behind TLS and store the long-lived Control key in the automation platform's secret store.\n\n## Manage Keys Only in Console\n\nControl keys are managed in **Services → Control**. They are zone-bound applications restricted to `control:<noun>:<verb>` scopes. Control operations cannot mutate, rotate, or delete Control-key applications, so automation cannot use one key to take over another. Secret reveal is audited.\n\n## Next Step\n\n[Choose the Right Surface](/runtime-console/cli-and-console/) for the complete boundary or [Enforce Boundaries](/architecture/trust-boundaries/) for trust placement."},{"id":"/v0.2/reference","title":"Use Reference","url":"https://docs.caracal.run/v0.2/reference/","concepts":"","body":"Use Reference when you need an exact value or contract. Use [Guides](/guides/) for procedures and [API Reference](/api/) for endpoint behavior.\n\n## Find the Canonical Answer\n\n| Question                                                 | Canonical page                                           |\n| -------------------------------------------------------- | -------------------------------------------------------- |\n| What does this public term mean?                         | [Glossary](/reference/glossary/)                         |\n| Why did this request fail?                               | [Error Codes](/reference/errors/)                        |\n| Which setting controls this behavior?                    | [Configuration Keys](/reference/configuration/)          |\n| Which configuration source wins?                         | [Configuration Order](/reference/config-precedence/)     |\n| What is the current default or limit?                    | [Defaults and Limits](/reference/defaults-and-limits/)   |\n| What exit status does automation receive?                | [CLI Exit Codes](/reference/runtime-exit-codes/)         |\n| Which runtimes and version combinations are supported?   | [Compatibility](/reference/compatibility/)               |\n| Which packages and images belong to one release?         | [Release Map](/reference/release-package-runtime-map/)   |\n| Which bytes and JSON fields are interoperable?           | [Wire Contracts](/reference/interoperability-contracts/) |\n| How do I answer a likely product or operations question? | [FAQ](/reference/faq/)                                   |\n\n## Read Values by Layer\n\n- **Product language** is the terminology shown in the web console and SDK facades.\n- **Runtime configuration** controls the CLI, SDK loaders, and deployed services.\n- **Protocol behavior** covers HTTP fields, raw JWT claims, status codes, and schemas.\n- **Release behavior** covers SemVer, package lockstep, images, and documentation versions.\n\nDo not substitute a protocol name for its public product term. Public surfaces use **Session ID** and **Delegation ID** while raw Coordinator and JWT contracts retain fields such as `agent_session_id` and `delegation_edge_id`.\n\n## Source of Truth\n\nCode, package manifests, deployment configuration, and checked-in schemas are authoritative. This section explains those sources; it does not create a second contract.\n\n## Next Action\n\nSearch [FAQ](/reference/faq/) for a question, or open [Glossary](/reference/glossary/) before naming a product concept in code or documentation."},{"id":"/v0.2/reference/faq","title":"FAQ","url":"https://docs.caracal.run/v0.2/reference/faq/","concepts":"","body":"import FaqRegistryScript from '../../../components/FaqRegistryScript.astro'\n\n<section className=\"faqSearchPanel\" aria-label=\"Search FAQs\">\n  <div className=\"faqSearchMeta\">\n    <span id=\"faq-search-status\" className=\"faqSearchStatus\" aria-live=\"polite\">\n      32 results\n    </span>\n  </div>\n  <div className=\"faqSearchRow\">\n    <label className=\"faqSearchLabel\" htmlFor=\"faq-search\">\n      Search FAQs\n    </label>\n    <input\n      id=\"faq-search\"\n      className=\"faqSearchInput\"\n      type=\"search\"\n      placeholder=\"Search by FAQ ID, title, keyword...\"\n      aria-controls=\"faq-registry\"\n      autocomplete=\"off\"\n    />\n    <button type=\"button\" className=\"faqClearFilters\" data-faq-clear hidden>\n      Clear\n    </button>\n    <button type=\"button\" className=\"faqFilterToggle\" data-faq-filter-toggle aria-expanded=\"false\" aria-controls=\"faq-filter-popover\">\n      <span data-faq-filter-label>Filter</span>\n      <svg className=\"faqFilterIcon\" viewBox=\"0 0 16 16\" aria-hidden=\"true\" focusable=\"false\">\n        <path d=\"M2.5 4.5h11M5.5 8h5M7 11.5h2\" fill=\"none\" stroke=\"currentColor\" strokeLinecap=\"round\" strokeWidth=\"1.5\" />\n      </svg>\n    </button>\n  </div>\n  <div id=\"faq-filter-popover\" className=\"faqFilterPopover\" data-faq-filter-popover aria-label=\"Filter FAQs by category\" hidden>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"platform\" data-faq-filter-name=\"Platform\" /> <span>Platform</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"model\" data-faq-filter-name=\"Model\" /> <span>Model</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"resources\" data-faq-filter-name=\"Resources\" /> <span>Resources</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"providers\" data-faq-filter-name=\"Providers\" /> <span>Providers</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"runtime\" data-faq-filter-name=\"Runtime\" /> <span>Runtime</span>\n    </label>\n    <label className=\"faqFilterOption\">\n      <input type=\"checkbox\" data-faq-filter-option=\"scope\" data-faq-filter-name=\"Scope\" /> <span>Scope</span>\n    </label>\n  </div>\n</section>\n\n<nav className=\"faqStrip\" data-faq-strip aria-label=\"Suggested FAQs\">\n  <a className=\"faqStripItem\" href=\"#faq-001\">\n    <span>FAQ-001</span>\n    <strong>What problem does Caracal solve?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-003\">\n    <span>FAQ-003</span>\n    <strong>What should a zone represent?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-006\">\n    <span>FAQ-006</span>\n    <strong>Should I create one application per agent?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-008\">\n    <span>FAQ-008</span>\n    <strong>Can policy and audit tell agents apart under one application?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-021\">\n    <span>FAQ-021</span>\n    <strong>Policy vs grant - which wins?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-016\">\n    <span>FAQ-016</span>\n    <strong>403 from STS vs Gateway?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-017\">\n    <span>FAQ-017</span>\n    <strong>Where is the diagnostic bundle or doctor command?</strong>\n  </a>\n  <a className=\"faqStripItem\" href=\"#faq-019\">\n    <span>FAQ-019</span>\n    <strong>What does this documentation verify?</strong>\n  </a>\n</nav>\n\n<section className=\"faqListPanel\" aria-label=\"FAQ results\">\n<p id=\"faq-empty\" className=\"faqEmpty\" hidden>No FAQs match that search.</p>\n\n<div id=\"faq-registry\" className=\"faqRegistry\">\n    <details id=\"faq-001\" className=\"faqEntry\" data-faq-entry data-faq-category=\"platform\" data-faq-id=\"FAQ-001\" data-faq-title=\"What problem does Caracal solve?\" data-keywords=\"authority broker short-lived credentials policy STS Gateway Audit automation workflows\">\n      <summary className=\"faqEntryHeader\">\n        <span className=\"faqNumber\">FAQ-001</span>\n        <span className=\"faqTag faqTagPlatform\">Platform</span>\n        <h3>What problem does Caracal solve?</h3>\n        <button type=\"button\" className=\"faqShareButton\" data-faq-share aria-label=\"Share FAQ-001\">Share</button>\n      </summary>\n      <span id=\"faq-what-problem-does-caracal-solve\" className=\"faqAnchor\"></span>\n      <p>Caracal gives agents and automated workflows short-lived, policy-approved authority instead of long-lived credentials. The agent asks for scoped authority at the moment it acts, STS evaluates policy, Gateway or an adapter enforces the mandate, and Audit records the decision and result.</p>\n      <p>See <a href=\"/concepts/authority-model/\">Authority and Enforcement</a> and the <a href=\"/concepts/model-overview/\">Caracal Mental Model</a>.</p>\n      <p className=\"faqRelated\">Related: <a href=\"#faq-002\">FAQ-002</a>, <a href=\"#faq-003\">FAQ-003</a>, <a href=\"#faq-016\">FAQ-016</a></p>\n    </details>\n\n    <details id=\"faq-002\" className=\"faqEntry\" data-faq-entry data-faq-category=\"platform\" data-faq-id=\"FAQ-002\" data-faq-title=\"Is Caracal an identity provider, secrets manager, or API gateway?\" data-keywords=\"IdP identity provider secrets manager API gateway authority broker provider credentials\">\n      <summary className=\"faqEntryHeader\">\n        <span className=\"faqNumber\">FAQ-002</span>\n        <span className=\"faqTag faqTagPlatform\">Platform</span>\n        <h3>Is Caracal an identity provider, secrets manager, or API gateway?</h3>\n        <button type=\"button\" className=\"faqShareButton\" data-faq-share aria-label=\"Share FAQ-002\">Share</button>\n      </summary>\n      <span id=\"faq-is-caracal-an-idp-secrets-manager-or-api-gateway\" className=\"faqAnchor\"></span>\n      <p>No. Caracal is an authority broker for agent and workload actions. It can sit in front of HTTP resources like a protected Gateway, and it can broker provider credentials, but it does not replace your IdP, y"},{"id":"/v0.2/reference/glossary","title":"Glossary","url":"https://docs.caracal.run/v0.2/reference/glossary/","concepts":"","body":"Use these terms consistently across docs, API names, web console labels, and examples.\n\n| Term                        | Meaning                                                                                                                                                                                                                                                                                                                                                                   |\n| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Application                 | Registered client identity in a zone that software runs as; confidential applications hold a secret and can exchange credentials.                                                                                                                                                                                                                                         |\n| Authority record            | One STS exchange record. Its Authority record ID anchors revocation, ancestry, and audit for authority minted by that exchange. Stored in the `authority_records` table.                                                                                                                                                                                                  |\n| Audit ledger                | Append-only evidence stream and database records for decisions and operations.                                                                                                                                                                                                                                                                                            |\n| Approval                    | A policy-raised hold that pauses a request until a human with authority approves or rejects it; surfaced on the Console Approvals page and returned on the wire as `interaction_required`.                                                                                                                                                                                |\n| Caracal Operator            | Governed natural-language console assistant that turns intent into reviewed, audited control-plane changes within your operator scope.                                                                                                                                                                                                                                    |\n| Console                     | Browser-based management UI served by the packaged web tier in Compose and Helm; `caracal web` opens it during local development.                                                                                                                                                                                                                                         |\n| Control API                 | Optional authenticated automation surface for remote management dispatch, authorized by scoped control keys.                                                                                                                                                                                                                                                              |\n| Delegation                  | Bounded authority one session grants to another: scopes, optional resource, constraints, and expiry, revocable independently. Stored internally as an edge in the delegation graph.                                                                                                                                                                                       |\n| Delegation ID               | Unique product identifier for a Delegation.                                                                                                                                                                                                                                                                                                                               |\n| Gateway                     | Reverse proxy that verifies inbound authority, exchanges with STS, and forwards to upstreams.                                                                                                                                                                                                                                                                             |\n| Grant                       | Access assignment for a resource. Policy `grants` data maps roles to allowed scopes per resource; managed delegated grants record subject-level assignments and cascade revocation to the subject's sessions.                                                                                                                                                             |\n| Mandate                     | Short-lived scoped access token (a JWT) carrying Caracal authority.                                                                                                                                                                                                                                                                                                       |\n| Policy                      | Rego content that participates in allow/deny decisions.                                                                                                                                                                                                                                                                                         "},{"id":"/v0.2/reference/errors","title":"Error Codes","url":"https://docs.caracal.run/v0.2/reference/errors/","concepts":"","body":"Caracal packages and services use a shared machine-readable error shape.\n\n```json\n{\n  \"error\": \"invalid_token\",\n  \"error_description\": \"bearer signature invalid\",\n  \"requestId\": \"018f...\",\n  \"details\": {}\n}\n```\n\n`details` appears only when a service or package provides structured context. The same code can be emitted by different surfaces - an `access_denied` from STS is a policy decision, while an `access_denied` surfaced by an in-process verifier is a missing scope on an otherwise valid mandate. Use the `requestId` with the web console **Audit** decision trace to confirm which surface produced the error before acting.\n\n## HTTP Classes\n\n| Status       | Protocol meaning                                                                                        |\n| ------------ | ------------------------------------------------------------------------------------------------------- |\n| `400`        | Invalid, denied, or held request; transport retry alone will not repair it.                             |\n| `401`        | Credential was absent or not accepted: malformed, expired, wrong issuer/audience, revoked, or replayed. |\n| `403`        | Identity was accepted but lacks authority for this operation or management scope.                       |\n| `404`        | Object is absent, hidden by authorization, or an optional operator endpoint is disabled.                |\n| `409`        | Lifecycle transition conflicts with current state or idempotency history.                               |\n| `425`, `429` | Retryable timing or rate condition; honor `Retry-After` where provided.                                 |\n| `5xx`        | Service or dependency failed; use readiness, logs, and `requestId`.                                     |\n\n## Well-Known Codes\n\nThe **Likely source** column names the surface that most often emits the code, and **First check** is the fastest thing to confirm. When a call fails, start from the surface, not the code.\n\n| Code                                | Meaning                                                                                                          | Likely source                                                     | First check                                                                                                                                                     |\n| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `access_denied`                     | Request is authenticated but not authorized.                                                                     | STS policy decision, or an in-process verifier scope check.       | Run `request trace`: confirm the active policy set allows the application, subject, resource, and scopes.                                                       |\n| `invalid_token`                     | Token is missing, malformed, expired, invalid, replayed, or fails verification.                                  | Gateway or a resource verifier.                                   | Confirm the mandate is unexpired, the verifier trusts the issuing zone JWKS, and the token was not already consumed.                                            |\n| `resource_not_found`                | Requested resource does not exist or is unavailable in the current scope.                                        | API, or STS resource resolution.                                  | Confirm the resource identifier and that the resource carries an upstream URL for `(zone, resource)`.                                                           |\n| `internal_error`                    | Service failed unexpectedly.                                                                                     | Any service.                                                      | Check service readiness and logs for the `requestId`.                                                                                                           |\n| `policy_eval_failed`                | Policy evaluation failed.                                                                                        | STS policy engine.                                                | Confirm the active policy set compiles and that required `input` fields are present.                                                                            |\n| `provider_rate_limited`             | Provider or provider coordination rate limit denied work.                                                        | Gateway upstream or provider coordination.                        | Inspect provider limits and retry/backoff; confirm the provider grant is active.                                                                                |\n| `interaction_required`              | Policy holds the mint for human approval.                                                                        | STS approval gate.                                                | Wait for the hold to be decided, then retry with the approval id (`challenge_id` on the wire); see [Human Approval](/guides/step-up/).                          |\n| `sts_unavailable`                   | STS could not satisfy an exchange.                                                                               | STS, or Gateway's STS circuit.                                    | Run `caracal status --ready`; confirm STS readiness, JWKS, and policy bundle freshness.                                                                         |\n| `credential_expired_not_renewable`  | Credential is too close to expiry or cannot be renewed.                                                          | STS provider-token refresh.   "},{"id":"/v0.2/reference/configuration","title":"Configuration Keys","url":"https://docs.caracal.run/v0.2/reference/configuration/","concepts":"","body":"Choose the consumer before choosing a key. Caracal has four configuration domains, and a key accepted by one layer is not automatically accepted by another.\n\n| Domain                     | Used by                                             | Choose it when                                                |\n| -------------------------- | --------------------------------------------------- | ------------------------------------------------------------- |\n| Workload identity          | `caracal run`.                                      | A Workload launches with server-side credential bindings.     |\n| SDK profile                | SDK clients.                                        | An Application needs Session, Delegation, or Resource access. |\n| Service environment config | API, STS, Gateway, Audit, Coordinator, and web BFF. | A service needs URLs, secrets, limits, or readiness settings. |\n| Deployment values          | Helm, Compose, Postgres, and Redis.                 | Operators size, schedule, expose, or secure infrastructure.   |\n\n## Workload Identity Keys\n\n| Key                            | Meaning                                                                  |\n| ------------------------------ | ------------------------------------------------------------------------ |\n| `CARACAL_WORKLOAD_ID`          | Workload ID from the console's Launcher page; required by `caracal run`. |\n| `CARACAL_WORKLOAD_SECRET`      | Inline local-development workload secret.                                |\n| `CARACAL_WORKLOAD_SECRET_FILE` | Cloud/custom mounted workload-secret file path.                          |\n| `CARACAL_STS_URL`              | Cloud/custom STS URL override.                                           |\n| `CARACAL_CONFIG_HOME`          | Optional OS config-root override for the default workload-secret path.   |\n\nCredential bindings, zone, scopes, and failure behavior come from the workload's launch bindings, authored in the web console. Local dev and stable launches can omit both secret variables and read the owner-only file at `<Caracal config dir>/runtime/<workload_id>/secret`.\n\n## SDK Profile Fields\n\n| Field                    | Meaning                                               |\n| ------------------------ | ----------------------------------------------------- |\n| `sts_url`                | Cloud/custom STS URL for token exchange.              |\n| `coordinator_url`        | Cloud/custom SDK/Console Coordinator URL override.    |\n| `gateway_url`            | Cloud/custom Gateway URL override for SDK transports. |\n| `zone_id`                | Zone identifier.                                      |\n| `application_id`         | Application identifier.                               |\n| `app_client_secret_file` | Cloud/custom secret-file path override.               |\n| `app_client_secret`      | Inline local-development secret.                      |\n| `default_ttl_seconds`    | Default TTL for block-style Session calls.            |\n| `credentials[]`          | Resource audiences with optional upstream prefixes.   |\n| `optional_credentials[]` | Additional resource audiences and upstream prefixes.  |\n\nSDK credential entries use `resource` and optional `upstream_prefix`. SDKs do not read launcher fields or search the OS config directory. Set `CARACAL_CONFIG` to an explicit profile path. Environment loaders also support `CARACAL_BOOTSTRAP_TOKEN`, `CARACAL_RESOURCES_FILE`, `CARACAL_RESOURCES`, and `CARACAL_DEFAULT_TTL_SECONDS`.\n\n## Core Service Environment Keys\n\n| Key                                                                    | Services                                                    |\n| ---------------------------------------------------------------------- | ----------------------------------------------------------- |\n| `CARACAL_MODE`                                                         | All services.                                               |\n| `DATABASE_URL` / `DATABASE_URL_FILE`                                   | API, STS, Gateway, Audit, Coordinator.                      |\n| `REDIS_URL` / `REDIS_URL_FILE`                                         | API, STS, Gateway, Audit, Coordinator.                      |\n| `STREAMS_HMAC_KEY` / `STREAMS_HMAC_KEY_FILE`                           | Stream producers and consumers.                             |\n| `AUDIT_HMAC_KEY` / `AUDIT_HMAC_KEY_FILE`                               | Audit producers and Audit service.                          |\n| `IDEMPOTENCY_HMAC_KEY` / `IDEMPOTENCY_HMAC_KEY_FILE`                   | Coordinator idempotency receipt key digest.                 |\n| `IDEMPOTENCY_HMAC_KEY_PREVIOUS` / `IDEMPOTENCY_HMAC_KEY_PREVIOUS_FILE` | Coordinator during one receipt-retention rotation window.   |\n| `GATEWAY_STS_HMAC_KEY` / `GATEWAY_STS_HMAC_KEY_FILE`                   | API, STS, Gateway.                                          |\n| `SECRET_STORE_KEK` / `SECRET_STORE_KEK_FILE`                           | API and STS.                                                |\n| `SECRET_STORE_KEK_PREVIOUS` / `SECRET_STORE_KEK_PREVIOUS_FILE`         | API and STS during a master-key rotation window.            |\n| `CARACAL_SECRET_BACKEND`                                               | API and STS; selects the secret backend, default `builtin`. |\n| `CARACAL_ADMIN_TOKEN` / `CARACAL_ADMIN_TOKEN_FILE`                     | API and management clients.                                 |\n| `CARACAL_COORDINATOR_TOKEN` / `CARACAL_COORDINATOR_TOKEN_FILE`         | Coordinator and Console Session/Delegation views.           |\n| `METRICS_BEARER` / `METRICS_BEARER_FILE`                               | Metrics authentication in published modes.                  |\n\n## Deployment Values\n\nHelm values live under `infra/helm/caracal/values.yaml`. Compose environment and secrets are defined by `infra/docker/docker-compose.yml` and `infra/docker/runtime-compose.yml`.\n\nThis page is the index. [Configure Service Environment](/operations/env-"},{"id":"/v0.2/reference/config-precedence","title":"Configuration Order","url":"https://docs.caracal.run/v0.2/reference/config-precedence/","concepts":"","body":"## caracal run Workload Identity\n\n`caracal run` reads no profile file. It resolves only the workload identity:\n\n1. `CARACAL_WORKLOAD_ID` names the workload created on the console's Launcher page.\n2. The workload secret comes from `CARACAL_WORKLOAD_SECRET`, or `CARACAL_WORKLOAD_SECRET_FILE`, or (eligible local modes only) the owner-only file at `<Caracal config dir>/runtime/<workload_id>/secret`. Setting both variables is an error.\n3. `CARACAL_STS_URL` overrides the STS endpoint; local dev and stable resolve it automatically.\n\nThe credential bindings, zone, scopes, and failure behavior come from the workload's launch bindings, authored in the web console and served by STS at launch.\n\n## SDK Profile Resolution\n\nSDK loaders use this order:\n\n1. `CARACAL_CONFIG`, when set. A missing file at that path is an error, not a fallthrough.\n2. Environment runtime config.\n\nSDKs do not read `./caracal.toml` from the current working directory.\n\nEnvironment config uses `CARACAL_ZONE_ID` and `CARACAL_APPLICATION_ID` with one\nexplicit credential source. SDKs read secret and manifest files only when named\nby `CARACAL_APP_CLIENT_SECRET_FILE` or an explicit profile field. Setting `CARACAL_ENV=production` requires explicit\nservice URLs. Cloud and custom deployments can provide\n`CARACAL_RESOURCES_FILE` values when mounted\nsecret or config paths differ from the local convention. A deployment without a\nclient secret can instead supply a pre-minted session mandate through\n`CARACAL_BOOTSTRAP_TOKEN`; the SDK rejects a bootstrap token that is already\nexpired at startup.\n\nResource bindings resolve from profile credentials, then\n`CARACAL_RESOURCES_FILE`, then `CARACAL_RESOURCES`. Later bindings with the\nsame resource ID override earlier bindings, so a short environment override can\nreplace one entry from a mounted JSON file without duplicating the file.\n\nConflicting credential modes fail closed. A client-secret configuration and `CARACAL_BOOTSTRAP_TOKEN` are alternatives, not fallback layers.\n\n## File-Secret Resolution\n\nServices support `*_FILE` variants for configured secret keys. File values are resolved before validation so deployment templates can mount secrets instead of placing sensitive material directly in environment variables. Previous-key variables used during KEK or idempotency-key rotation are separate overlap inputs, not precedence fallbacks.\n\n## Deployment Values\n\n| Deployment path | Precedence model                                                                                                |\n| --------------- | --------------------------------------------------------------------------------------------------------------- |\n| Docker Compose  | Shell environment overrides defaults in compose files; secrets are mounted from files.                          |\n| Helm            | CLI `--set` and later values files override earlier chart values; runtime Secret keys feed mounted service env. |\n\n## Troubleshooting\n\n| Symptom                                | Check                                                                                     |\n| -------------------------------------- | ----------------------------------------------------------------------------------------- |\n| Expected SDK profile is ignored        | Set `CARACAL_CONFIG` to the exact profile path; SDKs do not search default directories.   |\n| `caracal run` cannot find bindings     | Bindings live on the **Launcher** page in the web console, not in a local file.           |\n| Service fails with missing secret      | Confirm the `*_FILE` variable name is supported by that service and the file is readable. |\n| Helm values render unexpected defaults | Check values file order and CLI overrides.                                                |\n\n## Next Step\n\nUse [Defaults and Limits](/reference/defaults-and-limits/) to confirm ports, TTLs, request limits, and stream defaults."},{"id":"/v0.2/reference/defaults-and-limits","title":"Defaults and Limits","url":"https://docs.caracal.run/v0.2/reference/defaults-and-limits/","concepts":"","body":"## Ports\n\n| Component   | Port   |\n| ----------- | ------ |\n| API         | `3000` |\n| STS         | `8080` |\n| Gateway     | `8081` |\n| Audit       | `9090` |\n| Coordinator | `4000` |\n| Postgres    | `5432` |\n| Redis       | `6379` |\n\n## Token and Authority Lifetimes\n\n| Limit                                        | Default                                                              |\n| -------------------------------------------- | -------------------------------------------------------------------- |\n| STS resource mandate cap                     | 15 minutes                                                           |\n| STS session mandate cap                      | 60 minutes                                                           |\n| STS `MAX_GRANT_TTL_SECONDS`                  | `3600`                                                               |\n| DCR application lifetime default and maximum | 3600 seconds                                                         |\n| `caracal run` injected credential TTL        | 900 seconds                                                          |\n| Runtime approval wait                        | until the hold expires; 5-minute fallback when expiry is unavailable |\n| Approval TTL                                 | 1800 seconds default, clamped between 60 seconds and 7 days          |\n| Approval decision reason                     | 500 characters maximum                                               |\n| Gateway expiring-token preflight window      | 35 seconds                                                           |\n\n## Service Limits\n\n| Limit                             | Default              |\n| --------------------------------- | -------------------- |\n| API body limit                    | `1_048_576` bytes    |\n| API request timeout               | `30_000` ms          |\n| Coordinator body limit            | 256 KiB              |\n| Coordinator request timeout       | 30 seconds           |\n| STS request body limit            | 64 KiB               |\n| Gateway max request bytes         | 10 MiB               |\n| Gateway STS timeout               | 5 seconds            |\n| Gateway upstream timeout          | 30 seconds           |\n| Gateway non-stream write timeout  | 60 seconds           |\n| Gateway stream idle timeout       | 60 seconds per chunk |\n| Gateway server idle timeout       | 120 seconds          |\n| Gateway STS circuit failure limit | 3 failures           |\n| Gateway STS circuit open window   | 10 seconds           |\n| STS `OPA_POLL_SECONDS`            | 60 seconds, max 300  |\n| Control body limit                | 64 KiB               |\n| Control rate capacity             | 60 per window        |\n| Control rate window               | 60 seconds           |\n| Control replay TTL                | 3600 seconds         |\n\n## SDK Timeouts and Retries\n\nThe SDKs share one timeout philosophy: **control-plane calls are bounded, data-plane calls are bounded by you.** Coordinator and STS operations carry defaults because a hung control plane must not hang the worker. Provider traffic through `transport()` carries no TypeScript/Go default because only the caller knows whether a request is a short lookup or a long stream; pass the language-specific timeout or cancellation primitive.\n\n| Operation                 | Default                              | Notes                                                                                                                                                                                                                                           |\n| ------------------------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Coordinator call          | 10 seconds                           | Per request, all SDKs.                                                                                                                                                                                                                          |\n| Session start             | up to 2 retries                      | 5xx/network only. The SDK generates one operation id before the first attempt; Coordinator durably replays the same creation response and rejects changed inputs. This protects Session creation, not callback execution or downstream effects. |\n| Delegation create         | 1 retry                              | Same durable creation-replay contract.                                                                                                                                                                                                          |\n| Coordinator retry backoff | 250 ms escalating, jittered          | A server `Retry-After` wins, capped at 10 seconds.                                                                                                                                                                                              |\n| STS token exchange        | 30-second budget, one attempt        | Issuance is never retried automatically because a lost response can hide a successfully minted token. Callers reconcile or explicitly retry according to their operation contract.                                                              |\n| TypeScript Control invoke | 30-second total budget               | Covers one token mint and one invoke. Neither request is auto-retried; an invoke timeout is outcome-ambiguous.                                                                                                                                  |\n| Approval wait             | 300-second default                   | Long-polls in chunks; `pending` on timeout means waiting again is safe.                                                                               "},{"id":"/v0.2/reference/runtime-exit-codes","title":"CLI Exit Codes","url":"https://docs.caracal.run/v0.2/reference/runtime-exit-codes/","concepts":"","body":"Top-level `caracal` commands use `0` for success and non-zero for failure. Commands that delegate to Compose or a child process can propagate that program's non-zero status.\n\n## Exit Behavior\n\n| Command                                                       | Success                                                                                              | Failure                                                                                                               |\n| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |\n| `caracal up`                                                  | `0` after stack start succeeds.                                                                      | Non-zero when Compose/build/start fails.                                                                              |\n| `caracal down`                                                | `0` after stack stop succeeds.                                                                       | Non-zero when Compose stop fails.                                                                                     |\n| `caracal status`                                              | `0` when health probes pass.                                                                         | `1` when service health fails.                                                                                        |\n| `caracal status --ready`                                      | `0` when readiness probes pass.                                                                      | `1` when dependency readiness fails.                                                                                  |\n| `caracal upgrade`                                             | `0` after images stage, migrations apply, services roll, and readiness passes.                       | Non-zero when image staging, migration, the roll, or the readiness gate fails.                                        |\n| `caracal purge`                                               | `0` after selected state is removed.                                                                 | Non-zero when cleanup fails.                                                                                          |\n| `caracal allowlist <add\\|remove\\|lock\\|unlock\\|list> [email]` | `0` after the entry change or listing succeeds, including no-op repeats.                             | `1` on an unknown subcommand, malformed entry, missing entry, locked entry on `add`, or an unreadable allowlist file. |\n| `caracal run -- <command>`                                    | Child process exit code after credentials are acquired; `128 + N` when the child dies from signal N. | `1` before start when config, credential exchange, or Approval fails; `127` when the command cannot start.            |\n| `caracal web`                                                 | `0` after the web console exits cleanly.                                                             | `127` when a required executable is absent; otherwise a propagated build, preflight, auth BFF, or child failure.      |\n\nHelp and version output exit `0`. Unknown commands and invalid usage exit non-zero. A child can return the same numeric status as a pre-start launcher failure, so automation that must distinguish them should preserve launcher logs or wrapper metadata.\n\n## Structured Output\n\n`caracal status --json` emits machine-readable status. Web-console-owned diagnostics are available through the web console **Diagnostics** section and management command catalog, not as top-level runtime CLI workflows.\n\n## Next Step\n\nUse [Compatibility](/reference/compatibility/) before changing supported runtime, package manager, deployment, or docs build targets.\n\n## Related Pages\n\n- [Choose the Right Surface](/runtime-console/cli-and-console/)\n- [Start and Check the Stack](/runtime-console/stack/)\n- [Run Workloads](/runtime-console/runtime/)"},{"id":"/v0.2/reference/compatibility","title":"Compatibility","url":"https://docs.caracal.run/v0.2/reference/compatibility/","concepts":"","body":"## Language Runtimes\n\n| Area                     | Current target                                  |\n| ------------------------ | ----------------------------------------------- |\n| TypeScript/Node packages | Node `>=22` where package engines are declared. |\n| Python packages          | Python `>=3.12`.                                |\n| Go modules               | Go `1.26` where modules declare a version.      |\n| Root package manager     | `pnpm@11.1.1`.                                  |\n\n## Deployment Targets\n\n| Target              | Source                                                         |\n| ------------------- | -------------------------------------------------------------- |\n| Local development   | `caracal up` and `infra/docker/docker-compose.yml`.            |\n| Self-hosted Compose | `infra/docker/runtime-compose.yml` with versioned GHCR images. |\n| Kubernetes          | `infra/helm/caracal`.                                          |\n| Docs site           | Astro `^7.0.6` and Starlight `0.41.3`.                         |\n\n## Release and Platform Targets\n\n- Release binaries target Linux x64/arm64, macOS x64/arm64, and Windows x64.\n- Current Kubernetes operations guidance targets Kubernetes 1.30 or newer.\n- Published modes are `rc` and `stable`. They use the same fail-closed security posture; `rc` denotes pre-release maturity, not relaxed security.\n\n## Version Pinning\n\nCaracal packages release in lockstep: every `@caracalai/*` npm package, `caracalai*` Python distribution, and Go module tag carries the same version, and cross-package wire contracts are only validated within one release. Pin every Caracal package in a service to one exact version and upgrade them together - a mixed set (for example an older `@caracalai/oauth` under a newer `@caracalai/sdk`) is unsupported and can fail in ways that look like platform errors. Lockfiles make this the default; when bumping, change every Caracal entry in the same commit.\n\n## v0.2 Stability\n\nCaracal is on a pre-1.0 SemVer line. Patch releases preserve documented public behavior while repairing defects. A minor release may change public APIs or operational contracts, so read its release notes and upgrade all Caracal artifacts together.\n\nCompatibility promises cover documented package exports, public product APIs, and raw protocol fields identified as wire contracts. Internal storage names, internal endpoints, unpublished exports, and implementation details are not public compatibility guarantees. When a public surface is deprecated, its notice names the replacement and intended removal minor; pre-1.0 consumers must not assume a multi-major deprecation window.\n\nThis open-source compatibility policy does not imply a support SLA.\n\n## Next Step\n\nUse [Release Map](/reference/release-package-runtime-map/) to map product versions to packages, images, and release surfaces.\n\n## Related Pages\n\n- [Release Map](/reference/release-package-runtime-map/)\n- [Deploy with Helm](/operations/kubernetes-helm/)\n- [Set Up Locally](/contributing/setup/)"},{"id":"/v0.2/reference/release-package-runtime-map","title":"Release Map","url":"https://docs.caracal.run/v0.2/reference/release-package-runtime-map/","concepts":"","body":"## Repository Release Surfaces\n\n`release.config.json` `product.version` is authoritative. The current source state is `0.2.0`; release candidates append `-rc.N`.\n\n| Surface                | Current source                                                      |\n| ---------------------- | ------------------------------------------------------------------- |\n| Root package manager   | `package.json` declares `pnpm@11.1.1`.                              |\n| Release scripts        | `scripts/release.sh`, `scripts/release.mjs`, `release.config.json`. |\n| Docker runtime compose | `infra/docker/runtime-compose.yml`.                                 |\n| Helm chart             | `infra/helm/caracal/Chart.yaml` and values files.                   |\n| Docs build             | `docs/package.json`.                                                |\n\n## Service Images\n\nCaracal shares built images across roles, but each role remains a separate process or pod.\n\n| Image name         | Roles                      | Runtime  |\n| ------------------ | -------------------------- | -------- |\n| `caracal-node`     | API, Coordinator           | Node     |\n| `caracal-go`       | STS, Gateway, Audit        | Go       |\n| `caracal-web`      | Web console, auth BFF      | Node     |\n| `caracal-postgres` | Postgres, migrations       | -        |\n| `caracal-redis`    | Redis, stream provisioning | -        |\n| `caracal-runtime`  | Runtime distribution       | Node/Bun |\n\nRole selection:\n\n- `caracal-node` → `command: [\"/app/api/dist/main.js\"]` or `[\"/app/coordinator/dist/main.js\"]`.\n- `caracal-go` → `command: [\"/usr/local/bin/sts\"]`, `[\"/usr/local/bin/gateway\"]`, or `[\"/usr/local/bin/audit\"]`.\n\nRuntime Compose references `${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}<image>:v${CARACAL_VERSION}`.\n\n## Published Package Names\n\n| Area          | TypeScript              | Python                 | Go                                                       |\n| ------------- | ----------------------- | ---------------------- | -------------------------------------------------------- |\n| SDK           | `@caracalai/sdk`        | `caracalai-sdk`        | `github.com/garudex-labs/caracal/packages/sdk/go`        |\n| Admin         | `@caracalai/admin`      | `caracalai-admin`      | `github.com/garudex-labs/caracal/packages/admin/go`      |\n| Core          | `@caracalai/core`       | `caracalai-core`       | `github.com/garudex-labs/caracal/packages/core/go`       |\n| Identity      | `@caracalai/identity`   | `caracalai-identity`   | `github.com/garudex-labs/caracal/packages/identity/go`   |\n| OAuth         | `@caracalai/oauth`      | `caracalai-oauth`      | `github.com/garudex-labs/caracal/packages/oauth/go`      |\n| Revocation    | `@caracalai/revocation` | `caracalai-revocation` | `github.com/garudex-labs/caracal/packages/revocation/go` |\n| Verify engine | `@caracalai/verify`     | `caracalai-verify`     | `github.com/garudex-labs/caracal/packages/verify/go`     |\n\n## Versioning Notes\n\n- One product release stamps every package, image, binary, chart, and generated release record with the same SemVer.\n- Pin exact image tags in production values.\n- Run migration and readiness validation after image or chart changes.\n- Do not use product-management runtime CLI aliases as release surfaces; broad automation belongs to the Admin API and zone-scoped dispatch belongs to the Control API.\n\n## Documentation Mapping\n\n- Before stable `v0.2.0`, unversioned source is served at `/v0.2/`; no `v0.2` snapshot directory exists.\n- Stable `v0.2.0` creates the writable `v0.2` snapshot.\n- `v0.2.x` patches update that same current minor after release; they never create patch-specific documentation trees.\n- A later stable minor archives unversioned source as the new current snapshot, exposes ongoing work at `/next/`, and locks the superseded minor with its SHA-256 digest.\n\n## Next Step\n\nUse [Wire Contracts](/reference/interoperability-contracts/) when validating SDK, adapter, exporter, or provider-plugin compatibility."},{"id":"/v0.2/reference/interoperability-contracts","title":"Wire Contracts","url":"https://docs.caracal.run/v0.2/reference/interoperability-contracts/","concepts":"","body":"Wire contracts are raw protocol behavior. Product and SDK facades deliberately map some names to the public terms in [Glossary](/reference/glossary/). Authored schemas live under `docs/public/schemas/`; public copies are available at `/schemas/<filename>`. Cross-language fixtures live under `tests/shared/fixtures/interoperability/`.\n\n## Schema Files\n\n| Contract                            | Schema                                                                                                                                              |\n| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Agent connector manifest            | [`caracal-agent-connector-manifest-2026-05-21.schema.json`](/schemas/caracal-agent-connector-manifest-2026-05-21.schema.json)                       |\n| Audit event                         | [`caracal-audit-event-2026-05-21.schema.json`](/schemas/caracal-audit-event-2026-05-21.schema.json)                                                 |\n| Audit exporter manifest             | [`caracal-audit-exporter-manifest-2026-05-21.schema.json`](/schemas/caracal-audit-exporter-manifest-2026-05-21.schema.json)                         |\n| Gateway upstream manifest           | [`caracal-gateway-upstream-manifest-2026-05-21.schema.json`](/schemas/caracal-gateway-upstream-manifest-2026-05-21.schema.json)                     |\n| JWT claims                          | [`caracal-jwt-claims-2026-05-21.schema.json`](/schemas/caracal-jwt-claims-2026-05-21.schema.json)                                                   |\n| Policy input                        | [`caracal-policy-input-2026-05-20.schema.json`](/schemas/caracal-policy-input-2026-05-20.schema.json)                                               |\n| Policy pack manifest                | [`caracal-policy-pack-manifest-2026-05-21.schema.json`](/schemas/caracal-policy-pack-manifest-2026-05-21.schema.json)                               |\n| Policy result                       | [`caracal-policy-result-2026-05-20.schema.json`](/schemas/caracal-policy-result-2026-05-20.schema.json)                                             |\n| Provider credential plugin manifest | [`caracal-provider-credential-plugin-manifest-2026-05-21.schema.json`](/schemas/caracal-provider-credential-plugin-manifest-2026-05-21.schema.json) |\n| Resource verifier manifest          | [`caracal-resource-verifier-manifest-2026-05-21.schema.json`](/schemas/caracal-resource-verifier-manifest-2026-05-21.schema.json)                   |\n| Revocation event                    | [`caracal-revocation-event-2026-05-21.schema.json`](/schemas/caracal-revocation-event-2026-05-21.schema.json)                                       |\n| Token response                      | [`caracal-token-response-2026-05-21.schema.json`](/schemas/caracal-token-response-2026-05-21.schema.json)                                           |\n| W3C baggage                         | [`caracal-w3c-baggage-2026-05-21.schema.json`](/schemas/caracal-w3c-baggage-2026-05-21.schema.json)                                                 |\n\nDates in schema filenames identify that wire contract; they are not product release versions. Preserve the full filename when pinning validation.\n\n## Fixture Files\n\nFixtures include valid examples for audit events, audit exporter manifests, Gateway upstream manifests, JWT claims, policy input/results, policy pack manifests, provider credential plugins, resource verifier manifests, revocation events, token responses, W3C baggage, trace context headers, and stream signature canonicalization vectors.\n\n## Product-to-Wire Mapping\n\n| Product concept          | Common SDK field                             | Raw protocol field                                 |\n| ------------------------ | -------------------------------------------- | -------------------------------------------------- |\n| Authority record ID      | `authorityRecordId` and language equivalents | JWT `sid`; STS form `session_id`                   |\n| Root authority record ID | `rootAuthorityRecordId`                      | JWT `root_sid`                                     |\n| Session ID               | `sessionId`                                  | `agent_session_id` and Coordinator `/agents` paths |\n| Delegation ID            | `delegationId`                               | `delegation_edge_id` and internal edge records     |\n| Approval ID              | `approvalId`                                 | `challenge_id` and `/step-up/{id}`                 |\n\nDo not rename raw fields in an interoperable implementation. Map them at the SDK or product boundary.\n\n## Usage\n\n- Use schemas when building adapters, exporters, or provider plugins.\n- Use fixtures when adding SDK or interoperability tests.\n- Preserve schema filenames when documenting versioned contracts.\n- Add equivalent TypeScript, Python, and Go tests when a shared contract changes.\n- Treat a valid fixture as one accepted representation, not a substitute for endpoint validation and negative tests.\n\n## Next Step\n\nUse [API Reference](/api/) for transport semantics, then validate the implementation against the corresponding fixture.\n\n## Related Pages\n\n- [Use Event Topics](/api/event-topics/)\n- [Verify Package](/sdks/verify/)\n- [Validate Changes](/contributing/testing/)"},{"id":"/v0.2/contributing","title":"Contribute to Caracal","url":"https://docs.caracal.run/v0.2/contributing/","concepts":"","body":"Caracal uses a multi-language workspace: TypeScript apps/packages, Go services/packages, Python packages/examples, Docker/Helm infra, and Astro docs.\n\n## Contributor Path\n\n| Need                                            | Page                                             |\n| ----------------------------------------------- | ------------------------------------------------ |\n| Prepare your machine                            | [Set Up Locally](/contributing/setup/)           |\n| Learn project boundaries and naming conventions | [Follow Project Standards](/contributing/style/) |\n| Work on an issue or pull request                | [Make a Change](/contributing/workflow/)         |\n| Run the right checks                            | [Validate Changes](/contributing/testing/)       |\n\n## Maintainer Path\n\n| Need                                               | Page                                               |\n| -------------------------------------------------- | -------------------------------------------------- |\n| Understand review, ownership, and security process | [Understand Governance](/contributing/governance/) |\n| Prepare, publish, or recover a release             | [Release Caracal](/contributing/release/)          |\n\n## Current Toolchain\n\n| Tool                | Version                                                             |\n| ------------------- | ------------------------------------------------------------------- |\n| Node.js             | 24+ for repository development; published packages support Node 22+ |\n| pnpm                | 11.1.1                                                              |\n| Docker + Compose v2 | Docker 25+                                                          |\n| Go                  | 1.26+                                                               |\n| Python              | 3.14+                                                               |\n| Bun                 | 1.3.14                                                              |\n\n## Before You Start\n\nFollow the path in order: setup, standards, focused workflow, targeted validation, governance. Release ownership starts only after those contributor gates are understood.\n\n## Next Step\n\nStart with [Set Up Locally](/contributing/setup/) before making source changes."},{"id":"/v0.2/contributing/setup","title":"Set Up Locally","url":"https://docs.caracal.run/v0.2/contributing/setup/","concepts":"","body":"## Prerequisites\n\n| Tool                | Version    |\n| ------------------- | ---------- |\n| Node.js             | 24+        |\n| pnpm                | 11.1.1     |\n| Docker + Compose v2 | Docker 25+ |\n| Go                  | 1.26+      |\n| Python              | 3.14+      |\n| Bun                 | 1.3.14     |\n\n## Install Dependencies\n\n```bash\ngit clone https://github.com/Garudex-Labs/caracal.git\ncd caracal\npnpm run setup\n```\n\n`pnpm run setup` installs the Node workspace, downloads Go modules, creates `.venv`, installs pinned Python test/style tools, and installs local Python packages in editable mode. Use plain `pnpm install` only when the Node workspace is the entire scope.\n\nRead [CONTRIBUTING.md](https://github.com/Garudex-Labs/caracal/blob/main/CONTRIBUTING.md) in the repository root for the contributor policy, review expectations, and testing requirements.\n\n## Start the Local Stack\n\n```bash\npnpm caracal up\npnpm caracal status --ready\n```\n\nThe runtime initializes its managed development secrets during startup, builds local service images, and binds host ports to loopback. Run `pnpm secrets:init` only when a focused infrastructure task explicitly needs the standalone secret initializer.\n\n## Open the Web Console\n\n```bash\nopen http://localhost:3001\n```\n\nThe packaged web console starts with the stack. Use `pnpm caracal web` only when developing the web console or auth BFF locally.\n\n## Verify the Checkout\n\n```bash\npnpm run style\nscripts/testCi.sh --smoke\n```\n\nIf either command fails on an untouched checkout, resolve the toolchain problem before editing source.\n\n## Stop or Reset\n\n```bash\npnpm caracal down\npnpm caracal purge\n```\n\nUse `purge` only when you intentionally want to remove local stack/runtime state.\n\n## Next Step\n\nRead [Follow Project Standards](/contributing/style/) before editing source files.\n\n## Related Pages\n\n- [Runtime and Web Console](/runtime-console/)\n- [Deploy with Docker Compose](/operations/docker-compose/)\n- [Validate Changes](/contributing/testing/)"},{"id":"/v0.2/contributing/style","title":"Follow Project Standards","url":"https://docs.caracal.run/v0.2/contributing/style/","concepts":"","body":"Caracal style favors small, explicit boundaries and source-aligned documentation.\n\n## Language Style Guides\n\n| Language                  | Required style                                                              | Enforcement                                                                     |\n| ------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |\n| TypeScript and JavaScript | Existing repository patterns plus the pinned Prettier workspace dependency. | `pnpm run style` checks changed TS/JS source files with Prettier.               |\n| Go                        | Effective Go with canonical `gofmt` formatting.                             | `pnpm run style` checks changed Go source files with `gofmt -l`.                |\n| Python                    | PEP 8 layout as formatted by the pinned Ruff version.                       | `pnpm run style` checks changed Python source files with `ruff format --check`. |\n\nThe pre-commit hook activated by `pnpm install` formats staged files automatically on every commit, and `pnpm run style:fix` formats changed files on demand. The gate always runs the Ruff version pinned in `scripts/pythonStyleRequirements.in`, so local formatting matches CI exactly. Pull requests run the same changed-file style gate automatically for primary-language source files.\n\n## Code Conventions\n\n| Convention                  | Apply it                                                                                                                                                  |\n| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Keep changes focused        | Avoid unrelated refactors in feature or docs PRs.                                                                                                         |\n| Prefer explicit validation  | Fail closed on auth, policy, config, stream, and key errors.                                                                                              |\n| Preserve product boundaries | Do not couple open-source code to enterprise-only code.                                                                                                   |\n| Respect command ownership   | Runtime CLI is lifecycle and setup; the web console is the human surface, the Admin API is broad automation, and the Control API is Zone-scoped dispatch. |\n| Use existing shared layers  | Reuse core config, errors, crypto, logging, engine dispatch, and SDK helpers.                                                                             |\n| Preserve language parity    | SDK capability and wire changes in shared packages must land idiomatically in TypeScript, Python, and Go with equivalent tests.                           |\n| Preserve file headers       | Source files keep the repository copyright/product header in the language's comment syntax.                                                               |\n\n## Documentation Conventions\n\n| Page type    | Pattern                                                               |\n| ------------ | --------------------------------------------------------------------- |\n| Landing      | Purpose, audience, map, recommended reading path.                     |\n| Workflow     | Prerequisites, steps, validation, troubleshooting, related links.     |\n| Reference    | Exact names, defaults, tables, examples, source-of-truth links.       |\n| Architecture | Diagram, component responsibilities, flow, boundaries, related pages. |\n\nDo not use docs to preserve stale command names, screenshots, package names, or workflows. Update the whole affected page coherently.\n\n## Project Boundaries\n\n- Top-level `caracal` commands are limited to runtime lifecycle, upgrade, purge, console admission, `caracal run`, and the optional web development launcher.\n- Product-management workflows for Zones, Policy, Grants, audit, Sessions, Delegation, and Control belong in the web console, Admin SDK, or Control API docs.\n- Open-source code must not import, reference, or depend on enterprise-only code.\n- Security-sensitive findings must follow [Report a Vulnerability](/security/disclosure/), not public issues.\n\n## Naming\n\nUse canonical terms from [Glossary](/reference/glossary/). Keep raw names such as `agent_session_id`, `delegation_edge_id`, and `/agents` at explicit protocol boundaries only.\n\n## Next Step\n\nUse [Make a Change](/contributing/workflow/) to plan and submit a focused pull request."},{"id":"/v0.2/contributing/workflow","title":"Make a Change","url":"https://docs.caracal.run/v0.2/contributing/workflow/","concepts":"","body":"## Standard Flow\n\n1. Sync `main`, create a focused branch, and confirm the checkout passes its baseline targeted check.\n2. Keep the change focused on one component or user workflow.\n3. Read the `instructions.md` file in the directory you are editing. Every service, app, and package directory has one; it lists the required and forbidden patterns for that area.\n4. Update docs when behavior, APIs, commands, config, examples, or operations change. Before `v0.2.0`, edit the unversioned source. Afterward, target either the current stable minor or the next unreleased minor as described below.\n5. Add a regression test for every bug fix and tests for major new behavior.\n6. Run the narrowest relevant check first, then broaden only when a shared or security boundary changed.\n7. Review the diff for generated files, secrets, unrelated formatting, and documentation drift.\n8. Open a pull request with the problem, solution, exact validation commands/results, risks, and explicit follow-up items.\n\n## Choose the Area\n\n| Area              | Common sources                                                                      |\n| ----------------- | ----------------------------------------------------------------------------------- |\n| Runtime CLI       | `apps/runtime`, `packages/engine`, runtime tests.                                   |\n| Web console       | `apps/web`, Admin SDK, web console tests.                                           |\n| API               | `apps/api`, migrations, Admin package, API tests.                                   |\n| Coordinator       | `apps/coordinator`, Coordinator tests, SDK tests.                                   |\n| STS/Gateway/Audit | `services/*`, Go tests, operations docs.                                            |\n| SDKs/adapters     | `packages/*`, language-specific tests, interoperability fixtures.                   |\n| Infra             | `infra/docker`, `infra/helm`, Postgres/Redis scripts.                               |\n| Docs              | `docs/src/content/docs`, `docs/versions.json`, site config/plugins, and docs tests. |\n\n## Choose the Documentation Line\n\nDocumentation versions follow product minor releases, not individual patch releases.\n\n| Change target                                           | Source to edit                                                                                   |\n| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |\n| Before the first `v0.2.0` release                       | Unversioned pages under `docs/src/content/docs/`.                                                |\n| Current stable patch line, such as `v0.2.1` or `v0.2.2` | The unlocked current snapshot under `docs/src/content/docs/v0.2/`.                               |\n| Next unreleased minor, such as work for `v0.3.0`        | Unversioned pages under `docs/src/content/docs/`, published at `/next/` after versioning begins. |\n| Older minor                                             | Do not edit it. Once superseded, its snapshot is locked and CI verifies its digest.              |\n\nDo not create `v0.2.1`, `v0.2.2`, or other patch-specific documentation directories. If a documentation correction applies to both stable and unreleased behavior, make two deliberate edits: one in the unlocked stable snapshot and one in the unversioned next-minor source.\n\nCreate snapshots only through `scripts/docsVersion.mjs` as part of the stable release flow. Never edit a locked snapshot or replace its digest.\n\n## Pull Request Evidence\n\nInclude:\n\n- what user or operator behavior changed;\n- which trust, command, language-parity, or product-isolation boundaries were reviewed;\n- exact targeted and broad checks that ran, with results;\n- deployment, migration, compatibility, or rollback impact;\n- unresolved ambiguity that a reviewer must decide.\n\n## Command Boundary\n\nDo not add top-level runtime CLI commands for zones, policies, grants, audit, Sessions, Delegation, or Control. Human workflows belong to the web console; broad automation uses the Admin API, and zone-scoped dispatch uses the Control API.\n\n## Security Reports\n\nDo not discuss suspected vulnerabilities in public issues. Use GitHub private advisories or the email path in [Report a Vulnerability](/security/disclosure/).\n\n## Next Step\n\nUse [Validate Changes](/contributing/testing/) to choose the narrowest useful test command before opening a pull request."},{"id":"/v0.2/contributing/testing","title":"Validate Changes","url":"https://docs.caracal.run/v0.2/contributing/testing/","concepts":"","body":"Run the smallest relevant suite first, then broaden when a change touches shared code, security boundaries, generated artifacts, or deployment behavior.\n\n## Testing Policy\n\nThis policy is mandatory and is enforced during review:\n\n- Major new functionality MUST add automated tests covering that functionality, in the same change that introduces it.\n- Every bug fix MUST add a regression test that fails without the fix and passes with it.\n- Reviewers MUST confirm the required tests exist and run in CI before approving; pull requests that omit them are not merged.\n\n## Root Commands\n\n| Command                     | Purpose                                                     |\n| --------------------------- | ----------------------------------------------------------- |\n| `pnpm run build:typescript` | Build TypeScript apps and packages.                         |\n| `pnpm run lint`             | Run package linters where present.                          |\n| `pnpm run typecheck`        | Run TypeScript type checks.                                 |\n| `pnpm run test`             | Full TypeScript, Go, and Python test suite.                 |\n| `pnpm run test:typescript`  | TypeScript app/package tests.                               |\n| `pnpm run test:go`          | Go service/package and interoperability tests.              |\n| `pnpm run test:python`      | Python package tests.                                       |\n| `pnpm run ci`               | Build, lint, typecheck, and test sequence.                  |\n| `pnpm docs:version:verify`  | Validate documentation version metadata and snapshot locks. |\n\n## Targeted Examples\n\n| Area                             | Command                                                                     |\n| -------------------------------- | --------------------------------------------------------------------------- |\n| Runtime CLI                      | `pnpm --dir apps/runtime test`                                              |\n| Web console                      | `pnpm --dir apps/web test`                                                  |\n| Web backend-for-frontend         | `pnpm --dir apps/auth test`                                                 |\n| API                              | `pnpm --dir apps/api test`                                                  |\n| Coordinator                      | `pnpm --dir apps/coordinator test`                                          |\n| STS, Gateway, Audit, Go packages | `pnpm run test:go` (the repository harness owns its multi-module workspace) |\n| Docs                             | `pnpm --dir docs build`                                                     |\n| Docs versioning only             | `pnpm exec vitest run tests/typescript/unit/docs/versioning.test.ts`        |\n\nRunnable examples live in the [Caracal examples repository](https://github.com/Garudex-Labs/examples) and carry their own test suites and CI.\n\n## CI Mirror\n\n```bash\nscripts/testCi.sh\nscripts/testCi.sh --smoke\nscripts/testCi.sh --go\nscripts/testCi.sh --py\nscripts/testCi.sh --ts\nscripts/testCi.sh --docs\n```\n\nUse broader checks when a change affects auth, crypto, config, release, infra, shared packages, SDK contracts, or interoperability schemas.\n\nRecord the exact commands and results in the pull request. Do not claim the full suite when only targeted checks ran.\n\n## Next Step\n\nAfter validation, review [Understand Governance](/contributing/governance/) for contribution scale, review ownership, and private security process."},{"id":"/v0.2/contributing/governance","title":"Understand Governance","url":"https://docs.caracal.run/v0.2/contributing/governance/","concepts":"","body":"Caracal is maintained by Garudex Labs. `.github/MAINTAINERS` names maintainers; `.github/CODEOWNERS` routes path ownership and review requests.\n\n## Contribution Scale\n\n| Change size                   | Expected process                                                                                   |\n| ----------------------------- | -------------------------------------------------------------------------------------------------- |\n| Small focused fix             | Pull request with clear validation.                                                                |\n| Medium bug or feature         | GitHub issue with context and expected outcome before implementation.                              |\n| Large or cross-cutting change | Proposal with problem statement, alternatives, trade-offs, open questions, and smaller sub-issues. |\n| Security-sensitive change     | Private security process and maintainer coordination.                                              |\n\n## Maintainer Responsibilities\n\n- Review changes in owned areas.\n- Enforce repository standards and product boundaries.\n- Keep security reports private.\n- Approve releases and release workflow changes.\n- Preserve open-source and enterprise product isolation.\n\n## Code Review Requirements\n\nEvery change is proposed as a pull request and reviewed before merge or release.\n\n| Requirement          | Expectation                                                                                                                                                                                                         |\n| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| Independent review   | At least one maintainer other than the author approves each pull request; authors do not approve or merge their own changes.                                                                                        |\n| Area ownership       | `.github/CODEOWNERS` owners are requested automatically for their paths.                                                                                                                                            |\n| What reviewers check | Correctness and edge cases, focused scope, Testing Policy compliance with passing CI, the `pnpm run style` gate, input validation and trust boundaries, secret hygiene, OSS/enterprise isolation, and updated docs. |\n| Acceptance bar       | One approving non-author review, all required CI checks green, resolved comments, and a judgment that the change is worthwhile and free of known disqualifying defects.                                             |\n| Release approval     | Stable releases require `release-approval` from a maintainer other than the release preparer.                                                                                                                       |\n\nMaintainers own the decision to merge, not just the mechanics. They must reject changes whose security, compatibility, product-isolation, or operational risk is unresolved even when tests pass.\n\n## Release Ownership\n\n- The release preparer stamps and validates the candidate but cannot supply the independent stable approval.\n- The approving maintainer verifies version consistency, release manifest evidence, documentation version plan, registry preflight, and rollback posture.\n- Published tags are immutable. Recovery is a roll-forward release, not tag deletion or artifact replacement.\n\nThe full contributor-facing policy lives in [`./CONTRIBUTING.md`](https://github.com/Garudex-Labs/caracal/blob/main/CONTRIBUTING.md#code-review).\n\n## Community Standards\n\nThe project follows the repository [Code of Conduct](https://github.com/Garudex-Labs/caracal/blob/main/.github/CODE_OF_CONDUCT.md). Harassment, private-information disclosure, and disruptive behavior are not acceptable.\n\n## Security Governance\n\nSecurity concerns must be reported through [Report a Vulnerability](/security/disclosure/). Public issues are not appropriate for vulnerabilities, credential exposure, unsafe execution, or exploitable operational failures.\n\n## Next Step\n\nMaintainers preparing a cut should use [Release Caracal](/contributing/release/).\n\n## Related Pages\n\n- [Make a Change](/contributing/workflow/)\n- [Review the Threat Model](/security/threat-model/)\n- [Respond to Incidents](/operations/incident-response/)"},{"id":"/v0.2/contributing/release","title":"Release Caracal","url":"https://docs.caracal.run/v0.2/contributing/release/","concepts":"","body":"Every Caracal release artifact shares one Semantic Version from `release.config.json` `product.version`. The current source state is `0.2.0`; release candidates append `-rc.N`. `scripts/release.sh stamp` propagates the configured version to owned artifact metadata.\n\n## Release Surfaces\n\n| Surface         | Source                                                                                    |\n| --------------- | ----------------------------------------------------------------------------------------- |\n| Product version | `release.config.json` `product.version`, currently `0.2.0`.                               |\n| Runtime binary  | `apps/runtime/dist/caracal-*`.                                                            |\n| Containers      | Shared Go/Node role images, Web, Postgres, Redis, and Runtime from `release.config.json`. |\n| Helm            | `infra/helm/caracal`.                                                                     |\n| npm packages    | Public `@caracalai/*` packages from `release.config.json`.                                |\n| PyPI packages   | Public `caracalai-*` packages from `release.config.json`.                                 |\n\n## Build Targets\n\nRuntime release builds compile Linux x64/arm64, macOS x64/arm64, and Windows x64 binaries through Bun compile scripts.\n\nGo-based service container builds strip debug symbols by default (`GO_LDFLAGS` defaults to `-s -w`) and honor native build arguments passed to Docker: `CGO_ENABLED`, `CC`, `CFLAGS`, `CXX`, `CXXFLAGS`, `LDFLAGS`, `GOFLAGS`, `GO_BUILDFLAGS`, and `GO_LDFLAGS`. The Dockerfiles add `-mod=readonly` and `-trimpath`; override `GO_LDFLAGS` for diagnostic builds that need symbol tables.\n\n## Release Flow\n\n| Stage          | Command pattern                                                              |\n| -------------- | ---------------------------------------------------------------------------- |\n| RC prepare     | Set `product.version` to `X.Y.Z-rc.N`, then `scripts/release.sh rc prepare`  |\n| RC dry run     | `scripts/release.sh rc dry-run --local`                                      |\n| Stable stamp   | Set `product.version` to `X.Y.Z`, then `scripts/release.sh stamp` and commit |\n| Stable dry run | `scripts/release.sh stable --dry-run`                                        |\n| Stable publish | `scripts/release.sh stable`                                                  |\n| Promote an rc  | `scripts/release.sh promote --from vX.Y.Z-rc.N`                              |\n\nRun `pnpm release:plan` and `pnpm release:stamp:check` before publication. Release automation generates the manifest and the docs release record; do not author either as an independent source of truth.\n\nProtected GitHub workflows publish npm and PyPI packages. Every release publishes all publishable packages at the shared version; `package=<name>` exists only to resume a partially failed publish. Local stable publishing requires explicit approval and environment override.\n\n## Documentation Versions\n\nOpen Source documentation is versioned by product minor release:\n\n- `v0.2`, `v0.3`, `v0.4`, and `v1.0` are documentation versions.\n- Patch releases such as `v0.2.1` and `v0.2.2` update the existing `v0.2` documentation; they never create new documentation versions.\n- Release candidates never create documentation versions.\n- The latest stable minor is the default at `https://docs.caracal.run/`. The unversioned source is available at `/next/` after the first stable documentation release.\n- Superseded minors remain available at `/vX.Y/`, lose their edit link, and are protected by a committed SHA-256 digest.\n\n`v0.2.0` is the first versioned documentation release. `docs/versions.json` currently has `target: \"v0.2\"`, `current: null`, and no snapshots. Until stable `v0.2.0` is cut, edit `docs/src/content/docs/`; the site serves it from `/v0.2/`, but no `v0.2` source directory exists. Do not create one manually. The stable release snapshots that exact source and registers it as current.\n\nAfter `v0.2.0`, patch documentation changes for the active release line go directly into `docs/src/content/docs/v0.2/`. Work intended for the next minor continues in the unversioned source. When `v0.3.0` is published, release automation performs one transaction:\n\n1. Archive the unversioned source as `v0.3` and preserve its sidebar and referenced assets.\n2. Make `v0.3` the default stable documentation.\n3. Lock `v0.2` and record its content digest.\n4. Leave the unversioned source in place as the starting point for `/next/`.\n\nThe same process repeats for every later minor. `scripts/release.sh stable` and `scripts/release.sh promote` invoke it automatically. Maintainers can inspect the decision without changing files:\n\n```bash\nnode scripts/docsVersion.mjs plan 0.3.0\nnode scripts/docsVersion.mjs verify\n```\n\nDo not edit `docs/versions.json` to bypass the release command. CI compares every previously locked entry and snapshot with the base branch, so replacing both a historical page and its digest is rejected.\n\n## Rollback Rule\n\nDo not delete published tags. Roll forward with a new SemVer tag. Pinned `vX.Y.Z` tags are immutable; the floating `vX.Y` series tag moves with the new cut.\n\n## Registry Cleanup After the SemVer Cutover\n\nThe migration from CalVer to lockstep SemVer leaves registry state that only a maintainer with registry access can retire. Complete these steps manually after the first `v0.2.0` stable release is live:\n\n1. Deprecate dead npm names with a pointer to their successors — never unpublish:\n   ```bash\n   npm deprecate @caracalai/transport-mcp@'*' 'renamed: use the current @caracalai packages'\n   npm deprecate @caracalai/transport-a2a@'*' 'renamed: use the current @caracalai packages'\n   npm deprecate @caracalai/mcp-express@'*' 'renamed: use @caracalai/express'\n   npm deprecate @caracalai/mcp-fastmcp@'*' 'renamed: use @caracalai/fastmcp'\n   npm deprecate @caracalai/tokenstate-postgres@'*' 'renamed: use the current @caracalai packages'\n   ```\n2. Depre"},{"id":"/v0.2/blog/caracal-joins-lfx-mentorship","title":"Caracal Joins LFX Mentorship","url":"https://docs.caracal.run/v0.2/blog/caracal-joins-lfx-mentorship/","concepts":"","body":"import BlogPost from '../../../components/BlogPost.astro'\nimport LandingFooter from '../../../components/LandingFooter.astro'\n\n<BlogPost title={frontmatter.title} post={frontmatter.post}>\n\nOpen source does not grow by code alone. It grows when people contribute, learn, review, document, and help others build with confidence. That is the real value of mentorship in open source.\n\nCaracal is an open-source authority system for AI workloads. It issues policy-evaluated Mandates, coordinates Sessions and Delegation, protects configured Resources, and records audit evidence. The mentorship focuses on making those existing workflows easier to integrate, understand, test, and operate.\n\n## Work in the project\n\nLFX Mentorship places contributors in an active open-source workflow with guidance from maintainers and mentors. Work moves through issues, focused changes, tests, documentation, review, and the same acceptance gates as other contributions.\n\nFor mentees, the work provides practical experience with code review, documentation, validation, communication, and maintenance. Start with the [contributor path](/contributing/) for the current setup commands and review rules.\n\n## What the project gets\n\nFor Caracal, mentorship adds focused contributor capacity and outside feedback on developer experience, integration, and operations. Changes still follow the project's security boundaries, language-parity rules, tests, and maintainer review.\n\nThis year, Caracal's LFX Mentorship project includes Ashutosh Singh and Mohammad Haider Ali. Their work is part of a larger effort to strengthen the project, improve the contributor experience, and show how open source software is sustained over time through collaboration and shared ownership.\n\n## Investing in the ecosystem\n\nMentorship gives new contributors a defined route into project ownership while helping maintainers improve contributor workflows. To contribute, follow [Set Up Locally](/contributing/setup/), [Make a Change](/contributing/workflow/), and [Validate Changes](/contributing/testing/).\n\n</BlogPost>\n\n<LandingFooter />"},{"id":"/v0.2/blogs","title":"Blogs","url":"https://docs.caracal.run/v0.2/blogs/","concepts":"","body":"import BlogsDirectory from '../../components/BlogsDirectory.astro'\nimport LandingFooter from '../../components/LandingFooter.astro'\n\nBlog posts provide project context and community updates. They are not API, compatibility, security, or release contracts. For current behavior, use [Documentation](/); for artifact evidence, use [Releases](/releases/).\n\n<BlogsDirectory />\n\n<LandingFooter />"},{"id":"/v0.2/guides/frameworks","title":"Govern Agent Frameworks","url":"https://docs.caracal.run/v0.2/guides/frameworks/","concepts":"","body":"Caracal does not ship framework connectors for LangChain, LangGraph, or CrewAI, because it does not need them. Frameworks orchestrate model and tool calls through the provider clients you construct; Caracal governs those clients through the [transport wiring](/guides/provider-recipes/#wire-the-transport-into-provider-clients). Configure the client once, and every chain, graph node, agent, and tool call the framework issues is authenticated, policy-checked, and audited - with no framework-version coupling to maintain.\n\n## When to use this guide\n\nUse it after a direct SDK-to-Gateway call works and before handing the governed client to an agent framework. It is not an onboarding path and does not govern arbitrary framework-local tool calls.\n\n## Prerequisites\n\n- One working provider recipe and resource binding per network host the framework can call.\n- A scoped SDK transport tested without the framework.\n- A Session and Delegation design for parallel agents and subagents.\n\nThe pattern is always the same:\n\n1. Bind the provider upstream to a Caracal resource.\n2. Build the provider client with a scoped transport and `gateway-only` propagation.\n3. Hand that client to the framework.\n4. Run framework work inside `session()` so each agent execution gets its own Session ID.\n\n## LangChain\n\nLangChain model classes accept the same custom HTTP client hooks as the underlying provider SDKs.\n\n```python\nfrom caracalai import Caracal\nfrom langchain_openai import ChatOpenAI\n\ncaracal = Caracal()\n\nllm = ChatOpenAI(\n    model=\"gpt-4o\",\n    api_key=\"caracal-gateway\",\n    http_client=caracal.sync_transport(\n        scopes=[\"inference:invoke\"], propagation=\"gateway-only\"\n    ),\n    http_async_client=caracal.transport(\n        scopes=[\"inference:invoke\"], propagation=\"gateway-only\"\n    ),\n)\n```\n\n```ts\nimport { Caracal } from '@caracalai/sdk'\nimport { ChatOpenAI } from '@langchain/openai'\n\nconst caracal = new Caracal()\n\nconst llm = new ChatOpenAI({\n  model: 'gpt-4o',\n  apiKey: 'caracal-gateway',\n    configuration: {\n        fetch: caracal.transport({ scopes: ['inference:invoke'], propagation: 'gateway-only' }),\n    },\n})\n```\n\nThe `configuration` object forwards to the OpenAI SDK, so the governed `fetch` carries every LangChain invocation. Anthropic and Gemini model classes take the corresponding hooks from their provider recipes.\n\n## LangGraph\n\nLangGraph nodes call whatever model client the graph was built with, so a governed client governs the whole graph. What LangGraph adds is concurrency: parallel branches and subgraphs fan out, and each logical agent should be distinguishable in policy and audit. Start a labeled Session around each run, or around each branch that represents a distinct agent:\n\n```python\nfrom langgraph.prebuilt import create_react_agent\n\nagent = create_react_agent(llm, tools)\n\nasync with caracal.session(labels=[\"research-agent\"]):\n    result = await agent.ainvoke({\"messages\": [(\"user\", question)]})\n```\n\nEvery model call and governed tool call inside the run carries that Session's identity. To narrow a subagent below its parent, start its Session with reduced authority; see [Implement Multi-Agent Delegation](/guides/delegation/).\n\n## CrewAI\n\nCrewAI routes model traffic through LiteLLM, so the LiteLLM session hooks govern an entire crew:\n\n```python\nimport litellm\nfrom caracalai import Caracal\n\ncaracal = Caracal()\nlitellm.client_session = caracal.sync_transport(\n    scopes=[\"inference:invoke\"], propagation=\"gateway-only\"\n)\nlitellm.aclient_session = caracal.transport(\n    scopes=[\"inference:invoke\"], propagation=\"gateway-only\"\n)\n```\n\nSet the sessions before constructing agents. Each provider LiteLLM reaches needs its own resource binding, exactly as in the [LiteLLM recipe](/guides/provider-recipes/#litellm).\n\n## Framework tools\n\nTools are where agent traffic actually touches your systems, and they follow the same rule as models: build the tool's HTTP client from the transport.\n\n```python\npipernet = caracal.transport(scopes=[\"pipernet:read\"])\n\nasync def fetch_report(report_id: str) -> dict:\n    response = await pipernet.get(f\"https://api.pipernet.example/reports/{report_id}\")\n    return response.json()\n```\n\nBuild the transport once and share it: identity is resolved per request from the bound context, so concurrent agents can safely use the same client. Passing `scopes` mints the narrowed `use=gateway` mandate required for each call, so a tool holds only the authority it needs. For a complete multi-agent LangChain and LangGraph deployment governed this way, see the [Lynx Capital example](/examples/lynx-capital/).\n\n## Related\n\n- [Provider Recipes](/guides/provider-recipes/)\n- [Integrate the Python SDK](/guides/sdk-python/)\n- [Integrate the TypeScript SDK](/guides/sdk-typescript/)\n- [Implement Multi-Agent Delegation](/guides/delegation/)\n\n## Validate the framework integration\n\nCapture every outbound host in a test run. Each governed host must route through Gateway under the expected resource; an unknown host must not receive Caracal headers. Run parallel branches and confirm distinct labeled Sessions in Audit.\n\n:::caution[Failure point: framework tools]\nPassing a governed model client does not govern a tool that constructs its own HTTP client. Inject a Caracal transport into every network client or protect the destination as a resource server.\n:::\n\n## Next step\n\nAdd [multi-agent delegation](/guides/delegation/) only after single-Session transport and audit validation pass."},{"id":"/v0.2/guides/idempotency","title":"Safe Retries and Idempotency","url":"https://docs.caracal.run/v0.2/guides/idempotency/","concepts":"","body":"Most developers can ignore idempotency. Caracal automatically protects its own session and delegation creation calls against the network failures and transient server errors that ordinary applications encounter.\n\nThis guide explains the exceptional case: work delivered by a queue, webhook provider, workflow engine, or scheduler that can arrive again after the original process has stopped.\n\n## Prerequisites\n\n- The source system's immutable delivery, event, workflow, or schedule identifier.\n- The destination's idempotency or transactional deduplication mechanism.\n- A retry budget that distinguishes network/`5xx`/`408`/`425`/`429` from terminal authorization and validation failures.\n\n## What Idempotency Means\n\nAn operation is **idempotent** when repeating the same request has the same effect as performing it once. For example, asking Caracal twice to create the same governed Session should not create two Session records.\n\nIdempotency does not mean that arbitrary application code runs exactly once. A process can call an external API successfully and crash before recording success. After restart, no generic SDK can determine whether the external API committed the change.\n\n## The Default: Do Nothing\n\nFor a normal request handler, command, or in-process task, omit `idempotencyKey`:\n\n```ts\nawait caracal.session(\n  async () => {\n    await caracal.fetch('resource://pipernet', '/reports/market-risk')\n  },\n  {\n    labels: ['research-agent'],\n    task: 'Prepare the market risk report',\n  },\n)\n```\n\nThe SDK generates a cryptographically random operation identifier before contacting the Coordinator. If the connection drops, the Coordinator returns a transient error, or the SDK refreshes a rejected bearer, every retry reuses that identifier. The Coordinator stores a durable receipt in PostgreSQL, so a committed session or delegation creation is replayed instead of created again.\n\nThe receipt contains:\n\n- a keyed digest of the operation identifier, never the plaintext value;\n- the authenticated tenant and application scope;\n- a canonical fingerprint of every security-relevant request field;\n- the original response and created resource id;\n- an explicit expiry time.\n\nReceipts store only bounded operational response fields. They do not duplicate session metadata, invocation parameters, credentials, or bearer tokens.\n\nThe default receipt window is seven days for explicit stable identifiers (`IDEMPOTENCY_RETENTION_SECONDS`) and one day for automatically generated retry identifiers (`GENERATED_IDEMPOTENCY_RETENTION_SECONDS`). The shorter generated window contains storage growth without weakening process-local retry safety.\n\nEach application and operation can retain at most 10,000 live receipts by default (`IDEMPOTENCY_MAX_RECEIPTS_PER_SCOPE`), preventing a compromised workload from growing the receipt index without bound. The Coordinator also caps request bodies at 256 KiB (`COORDINATOR_BODY_LIMIT_BYTES`).\n\n## When an Explicit Key Helps\n\nSupply a key only when the system delivering work already has a stable identifier that survives process restart:\n\n| Source          | Use                                                       |\n| --------------- | --------------------------------------------------------- |\n| Queue           | Queue or subscription namespace plus immutable message id |\n| Webhook         | Verified provider namespace plus delivery or event id     |\n| CloudEvents     | `source` plus `id`                                        |\n| Workflow engine | Workflow namespace, run id, and step or activity id       |\n| Scheduler       | Schedule namespace plus intended fire time                |\n\n```ts\nasync function handleWorkItem(message: QueueMessage<Ticket>) {\n  const ticket = message.body\n  await caracal.session(\n    async () => {\n      await caracal.fetch('resource://pipernet', '/reports/market-risk')\n    },\n    {\n      labels: ['research-agent'],\n      task: `${ticket.title} (${ticket.key})`,\n      idempotencyKey: `ticket-queue:v1:${message.id}`,\n    },\n  )\n}\n```\n\nPython uses `idempotency_key=`. Go uses `SessionOptions{IdempotencyKey: ...}`.\n\nThe namespace and version prevent unrelated integrations from colliding. Change the version only when intentionally defining a different operation.\n\n## Exact Guarantee\n\nWith the same key and the same request fields, Caracal replays the original **Coordinator creation response** during the configured retention window. A replay carries the `Idempotency-Replayed: true` response header and appears as `replayed: true` on SDK coordinator events.\n\nWith the same key and different fields, Caracal returns `409 idempotency_key_conflict`. This includes changes to the Subject authority record ID, parent Session, application, lifecycle, labels, task or metadata, authority mode, TTL, Delegation endpoints, resource, scopes, constraints, or invocation parameters.\n\nIf the receipt's governed Session or Delegation has already terminated, expired, or been revoked, Caracal returns `409 idempotency_result_inactive` instead of replaying an unusable identity. This prevents a delayed sequential redelivery from entering the callback. Use a newly versioned operation id only for an intentional rerun.\n\nWhile the original session remains active, a valid replay still enters your callback. The key does not claim a queue message, serialize consumers, cache the callback result, or make downstream effects exactly once. Two concurrent consumers can both execute application code. Use the queue's own visibility/lease mechanism to prevent concurrent delivery.\n\n## Protect External Side Effects\n\nPass a stable operation id to every destination that supports idempotency:\n\n```ts\nconst operationId = `market-risk:v1:${message.id}`\n\nawait caracal.session(\n  async () => {\n    await caracal.fetch('resource://pipernet', '/reports/market-risk', {\n      method: 'POST',\n      headers: { 'Idempotency-Key': operationId },\n    })\n  },\n  {\n    task: `Prepare ${ticket.key}`,\n    labels: ['research-agent'],\n    idempoten"},{"id":"/v0.2/guides/testing","title":"Test Caracal Integrations","url":"https://docs.caracal.run/v0.2/guides/testing/","concepts":"","body":"Caracal integrations test cleanly because every control-plane dependency enters through an injectable HTTP client: `fetchImpl` in TypeScript, `http_client` and `httpx.MockTransport` in Python, `HTTPClient` and `httptest` in Go. Fake the wire, not the SDK - your test exercises the same request construction, retry, and caching code that runs in production, and nothing reaches a network.\n\n## When to use this guide\n\nUse it before production rollout and whenever Session, Delegation, transport, policy, approval, or resource-server requirements change.\n\n## Prerequisites\n\n- The exact application workflow and enforcement boundary to test.\n- One allow case and explicit deny, expiry, revoke, timeout, replay, and cleanup expectations.\n- Injectable clients or a local runtime dedicated to the test.\n\n## What to stub\n\nA governed Session touches a handful of endpoints. Stub the ones your code path uses and let anything unexpected fail loudly:\n\n| Call                             | Respond with                                                                                                            |\n| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |\n| `POST /zones/{zone}/agents`      | Protocol Session-start response `{\"agent_session_id\": \"agent-1\"}`; long-lived Sessions also require `heartbeat_deadline_at` and `lease_generation`. |\n| `POST /zones/{zone}/delegations` | Protocol Delegation response `{\"delegation_edge_id\": \"edge-1\"}`.                                                        |\n| `POST .../agents/{id}/heartbeat` | `{\"agent\": {\"status\": \"active\", \"heartbeat_deadline_at\": \"...\", \"lease_generation\": 1}}`                                |\n| `DELETE .../agents/{id}`         | `204`                                                                                                                   |\n| `POST /oauth/2/token`            | `{\"access_token\": \"tok\", \"token_type\": \"Bearer\", \"expires_in\": 900}`                                                    |\n\nReturn an error status to exercise failure paths: a `503` drives the idempotent Session-start retry, a `429` from STS proves issuance is surfaced after one attempt, and a `401` from an `interaction_required` body raises the approval hold.\n\nMake scoped-transport Gateway fakes replay-sensitive: remember each presented bearer and reject a duplicate with `token_replayed`. Repeated and concurrent requests must carry distinct mandates, while an application transport must still create only one source/target Session pair and one delegation per authority-cache key.\n\n## TypeScript\n\n```ts\nimport { describe, expect, it, vi } from 'vitest'\nimport { Caracal } from '@caracalai/sdk'\n\nit('runs work inside a governed Session', async () => {\n  const calls: { url: string; method: string }[] = []\n  const fetchImpl = (async (input: RequestInfo | URL, init: RequestInit = {}) => {\n    calls.push({ url: String(input), method: init.method ?? 'GET' })\n    if (init.method === 'POST' && String(input).endsWith('/agents')) {\n      return new Response(JSON.stringify({ agent_session_id: 'agent-1' }), { status: 200 })\n    }\n    return new Response(null, { status: 204 })\n  }) as typeof fetch\n\n  const caracal = new Caracal({\n    coordinator: { baseUrl: 'http://coord.test', fetchImpl },\n    zoneId: 'z',\n    applicationId: 'app',\n    subjectToken: 'test-token',\n  })\n\n  const result = await caracal.session(async (ctx) => ctx.sessionId)\n\n  expect(result).toBe('agent-1')\n  expect(calls.at(-1)?.method).toBe('DELETE') // the session was retired\n})\n```\n\n## Python\n\n```python\nimport httpx\nfrom caracalai import Caracal, CaracalConfig\nfrom caracalai.coordinator import CoordinatorClient\n\nasync def test_runs_work_inside_a_governed_session() -> None:\n    async def handler(request: httpx.Request) -> httpx.Response:\n        if request.method == \"POST\" and str(request.url).endswith(\"/agents\"):\n            return httpx.Response(200, json={\"agent_session_id\": \"agent-1\"})\n        return httpx.Response(204)\n\n    coordinator = CoordinatorClient(\n        base_url=\"http://coord.test\",\n        http_client=httpx.AsyncClient(transport=httpx.MockTransport(handler)),\n    )\n    caracal = Caracal(\n        CaracalConfig(\n            coordinator=coordinator,\n            zone_id=\"z\",\n            application_id=\"app\",\n            subject_token=\"test-token\",\n        )\n    )\n\n    async with caracal.session() as ctx:\n        assert ctx.session_id == \"agent-1\"\n```\n\n## Go\n\n```go\nfunc TestRunsWorkInsideAGovernedSession(t *testing.T) {\n\tsrv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n\t\tif r.Method == http.MethodPost && strings.HasSuffix(r.URL.Path, \"/agents\") {\n\t\t\tw.Header().Set(\"Content-Type\", \"application/json\")\n\t\t\tfmt.Fprint(w, `{\"agent_session_id\":\"agent-1\"}`)\n\t\t\treturn\n\t\t}\n\t\tw.WriteHeader(http.StatusNoContent)\n\t}))\n\tdefer srv.Close()\n\n\tclient := &caracal.Caracal{\n\t\tCoordinator:   &caracal.CoordinatorClient{BaseURL: srv.URL},\n\t\tZoneID:        \"z\",\n\t\tApplicationID: \"app\",\n\t\tSubjectToken:  \"test-token\",\n\t}\n\n\terr := client.Session(context.Background(), func(ctx context.Context) error {\n\t\tcur, _ := caracal.Current(ctx)\n\t\tif cur.SessionID != \"agent-1\" {\n\t\t\tt.Fatalf(\"unexpected session: %s\", cur.SessionID)\n\t\t}\n\t\treturn nil\n\t})\n\tif err != nil {\n\t\tt.Fatal(err)\n\t}\n}\n```\n\n## Assert on behavior, not internals\n\nTwo seams make assertions precise without reaching into SDK state. The fake transport records every request, so you can assert the wire contract: an `idempotency-key` header on Session starts, the Delegation body, and termination on Session exit. `onEvent` reports control-plane operations as data, so tests can assert the expected `coordinator.call`, `token.exchange`, or `delegation.accept` events.\n\nClient-secret flows need the STS stub too: answer `POST /oauth/2/token` with a bearer and `expires_in`, and mint distinct tokens per call when the test asserts caching (a second exchange "},{"id":"/v0.2/operations/secret-backends","title":"Configure Secret Backends","url":"https://docs.caracal.run/v0.2/operations/secret-backends/","concepts":"","body":"`CARACAL_SECRET_BACKEND` selects storage for user-entered credentials. It does not replace runtime secret delivery. Values are envelope-encrypted under `SECRET_STORE_KEK` before backend storage.\n\n## Choose a Backend\n\nImplemented identifiers are `builtin`, `vault`, `infisical`, `azurekeyvault`, `awssecretsmanager`, `gcpsecretmanager`, and `custom`. `builtin` stores envelopes in Postgres. External backends require their documented endpoint, identity, project/region, or token variables on API and STS.\n\n## Safe Procedure\n\n1. Back up Postgres, runtime secrets, and external backend data.\n2. Configure one backend on API and STS with the same KEK.\n3. Restart API and STS; wait for readiness.\n4. Create a disposable provider credential and perform a token exchange.\n5. Verify backend operation/error metrics and delete the disposable object.\n\nSTS caches external reads for 60 seconds and may serve stale cached data for up to 10 minutes during errors. It never falls back to another backend.\n\n## KEK Rotation\n\nDeploy the replacement as `SECRET_STORE_KEK` and retiring value as `SECRET_STORE_KEK_PREVIOUS`. Run `node scripts/rotate-secret-store-kek.mjs` from the API environment, verify zero failures, then remove the previous key and restart API and STS.\n\n:::danger[Permanent credential loss]\nRemoving the retiring KEK before all envelopes are re-sealed makes remaining envelopes unreadable.\n:::\n\n## Backend Migration and Recovery\n\nUse `node scripts/migrate-secret-backend.mjs <target-kind>` while the source remains configured. Switch services only after copying succeeds. On failure, keep the source selected and rerun; do not create a fallback chain.\n\n## Next Action\n\nPlan dependent rotations in [Rotate Keys and Secrets](/operations/key-management/)."},{"id":"/v0.2/releases","title":"Releases","url":"https://docs.caracal.run/v0.2/releases/","concepts":"","body":"import ReleasesDirectory from '../../components/ReleasesDirectory.astro'\nimport LandingFooter from '../../components/LandingFooter.astro'\n\nCaracal releases use lockstep SemVer. Stable releases use `vX.Y.Z`; release candidates use `vX.Y.Z-rc.N`. The current source state targets `v0.2.0` as the first stable documentation release.\n\nUse a release record to choose an exact artifact version and inspect its validation evidence. Pin stable versions in production. Use an RC only for pre-release validation; RC and stable modes share the same fail-closed security posture.\n\nDocumentation is versioned by stable minor: `v0.2.0` creates `/v0.2/`, patches update that minor, and RCs create no documentation snapshot. See [Release Map](/reference/release-package-runtime-map/) before combining packages, images, chart versions, or docs.\n\n<ReleasesDirectory />\n\n<LandingFooter />"},{"id":"/v0.2/vlogs","title":"Vlogs","url":"https://docs.caracal.run/v0.2/vlogs/","concepts":"","body":"import VlogsDirectory from '../../components/VlogsDirectory.astro'\nimport LandingFooter from '../../components/LandingFooter.astro'\n\nNo video is a compatibility or operations contract. Check the video's publication date and verify commands, package names, and behavior against the selected documentation version. If the directory has no entries, use [Tutorials](/tutorials/) for guided learning or [Guides](/guides/) for task procedures.\n\n<VlogsDirectory />\n\n<LandingFooter />"}]
