{
  "name": "caracal-example-control-bootstrap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent environment pipeline that applies, verifies, and tears down zone state through the Control API.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "apply": "node apply.mjs",
    "verify": "node verify.mjs",
    "teardown": "node teardown.mjs",
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
