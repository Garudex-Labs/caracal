{
  "name": "caracal-example-control-bootstrap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Non-interactive zone provisioning through the Control API using a scoped, short-lived control key.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "bootstrap": "node bootstrap.mjs",
    "teardown": "node teardown.mjs",
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
