{
  "name": "@caracal-examples/provider-preflight",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Automatable provider preflight that validates control-plane and Gateway readiness, dependency resolution, provider configuration, reachability, and policy authorization before the first Gateway request.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "preflight": "node run.mjs",
    "test": "node --test"
  }
}
