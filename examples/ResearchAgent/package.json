{
  "name": "caracal-example-google-oauth-openai-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Google OAuth and OpenAI agent launched with caracal run using runtime-only credential injection.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
