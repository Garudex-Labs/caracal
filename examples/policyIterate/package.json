{
  "name": "@caracal-examples/policy-iterate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Audit-driven policy iteration: diagnose a denied request, simulate a candidate policy-set version, regression-check expected decisions, and gate activation on the evidence.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "iterate": "node run.mjs",
    "test": "node --test"
  }
}
