{
  "_config_version": "4.0",
  "_comment": "Copy this file to demo_config.json and set provider credentials in your environment. Most settings are controlled via environment variables; see CONFIG_SCHEMA.md.",
  "workspace": {
    "name": "demo-workspace"
  },
  "provider": {
    "name": "ops-api",
    "base_url": "https://ops-api.example.com",
    "credential_env": "OPS_API_TOKEN"
  }
}

