# SPDX-FileCopyrightText: 2026 Ryan Madhuwala <rawx18.dev@gmail.com>
# SPDX-License-Identifier: Apache-2.0
{{- if .Values.clickhouse.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "caracal.fullname" . }}-clickhouse
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "caracal.labels" . | nindent 4 }}
    app.kubernetes.io/component: clickhouse
spec:
  serviceName: {{ include "caracal.fullname" . }}-clickhouse
  replicas: 1
  selector:
    matchLabels:
      {{- include "caracal.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: clickhouse
  template:
    metadata:
      labels:
        {{- include "caracal.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: clickhouse
    spec:
      containers:
        - name: clickhouse
          image: {{ include "caracal.image" (dict "global" .Values.global "image" .Values.clickhouse.image) }}
          imagePullPolicy: {{ .Values.clickhouse.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            seccompProfile:
              type: RuntimeDefault
          env:
            - name: CLICKHOUSE_USER
              value: "default"
            - name: CLICKHOUSE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "caracal.secretName" . }}
                  key: CLICKHOUSE_PASSWORD
          ports:
            - containerPort: 8123
              name: http
            - containerPort: 9000
              name: native
          readinessProbe:
            httpGet:
              path: /ping
              port: 8123
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.clickhouse.resources | nindent 12 }}
          volumeMounts:
            - mountPath: /var/lib/clickhouse
              name: chdata
            - mountPath: /etc/clickhouse-server/config.d/memory.xml
              subPath: memory.xml
              name: config
            - mountPath: /etc/clickhouse-server/users.d/memory.xml
              subPath: memory.xml
              name: users
            - mountPath: /tmp
              name: tmp
      volumes:
        - name: config
          configMap:
            name: {{ include "caracal.fullname" . }}-clickhouse-config
        - name: users
          configMap:
            name: {{ include "caracal.fullname" . }}-clickhouse-users
        - name: tmp
          emptyDir: {}
  volumeClaimTemplates:
    - metadata:
        name: chdata
      spec:
        accessModes: [ "ReadWriteOnce" ]
        {{- if .Values.clickhouse.storage.storageClassName }}
        storageClassName: {{ .Values.clickhouse.storage.storageClassName | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.clickhouse.storage.size | quote }}
{{- end }}
