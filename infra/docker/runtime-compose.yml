# Copyright (C) 2026 Garudex Labs.  All Rights Reserved.
# Caracal, a product of Garudex Labs
#
# Runtime orchestration for self-hosted installs using versioned GHCR images.

name: caracal

x-logging: &defaultLogging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"

x-security: &defaultSecurity
  security_opt:
    - no-new-privileges:true
  cap_drop:
    - ALL

x-appService: &appService
  restart: unless-stopped
  init: true
  stop_grace_period: 20s
  logging: *defaultLogging
  <<: *defaultSecurity
  read_only: true
  tmpfs:
    - /tmp:size=64m,mode=1777
  networks:
    - caracalData

x-appResources: &appResources
  deploy:
    resources:
      limits:
        cpus: "${CARACAL_APP_CPU_LIMIT:-1.0}"
        memory: "${CARACAL_APP_MEM_LIMIT:-512M}"
        pids: 256
      reservations:
        cpus: "${CARACAL_APP_CPU_RESERVE:-0.1}"
        memory: "${CARACAL_APP_MEM_RESERVE:-128M}"

# Secret backend selection for user-entered provider credentials. The builtin Secret
# Store needs nothing beyond SECRET_STORE_KEK; the remaining values configure an
# external backend and stay empty (treated as unset) under builtin.
x-secretBackendEnv: &secretBackendEnv
  CARACAL_SECRET_BACKEND: ${CARACAL_SECRET_BACKEND:-builtin}
  CARACAL_VAULT_ADDR: ${CARACAL_VAULT_ADDR:-}
  CARACAL_VAULT_TOKEN: ${CARACAL_VAULT_TOKEN:-}
  CARACAL_VAULT_MOUNT: ${CARACAL_VAULT_MOUNT:-}
  CARACAL_VAULT_NAMESPACE: ${CARACAL_VAULT_NAMESPACE:-}
  CARACAL_INFISICAL_URL: ${CARACAL_INFISICAL_URL:-}
  CARACAL_INFISICAL_TOKEN: ${CARACAL_INFISICAL_TOKEN:-}
  CARACAL_INFISICAL_PROJECT_ID: ${CARACAL_INFISICAL_PROJECT_ID:-}
  CARACAL_INFISICAL_ENV: ${CARACAL_INFISICAL_ENV:-}
  CARACAL_INFISICAL_PATH: ${CARACAL_INFISICAL_PATH:-}
  CARACAL_AZURE_VAULT_URL: ${CARACAL_AZURE_VAULT_URL:-}
  CARACAL_AZURE_TENANT_ID: ${CARACAL_AZURE_TENANT_ID:-}
  CARACAL_AZURE_CLIENT_ID: ${CARACAL_AZURE_CLIENT_ID:-}
  CARACAL_AZURE_CLIENT_SECRET: ${CARACAL_AZURE_CLIENT_SECRET:-}
  CARACAL_AWS_REGION: ${CARACAL_AWS_REGION:-}
  AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
  AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
  AWS_SESSION_TOKEN: ${AWS_SESSION_TOKEN:-}
  CARACAL_GCP_PROJECT: ${CARACAL_GCP_PROJECT:-}
  CARACAL_GCP_CREDENTIALS_FILE: ${CARACAL_GCP_CREDENTIALS_FILE:-}
  GOOGLE_APPLICATION_CREDENTIALS: ${GOOGLE_APPLICATION_CREDENTIALS:-}
  CARACAL_CUSTOM_SECRETS_URL: ${CARACAL_CUSTOM_SECRETS_URL:-}
  CARACAL_CUSTOM_SECRETS_TOKEN: ${CARACAL_CUSTOM_SECRETS_TOKEN:-}

networks:
  caracalData:
    driver: bridge
    name: caracalData

secrets:
  postgresPassword:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/postgresPassword
  redisPassword:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/redisPassword
  caracalAdminToken:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/caracalAdminToken
  caracalCoordinatorToken:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/caracalCoordinatorToken
  metricsBearer:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/metricsBearer
  secretStoreKek:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/secretStoreKek
  auditHmacKey:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/auditHmacKey
  streamsHmacKey:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/streamsHmacKey
  gatewayStsHmacKey:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/gatewayStsHmacKey
  databaseUrl:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/databaseUrl
  apiDatabaseUrl:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/apiDatabaseUrl
  stsDatabaseUrl:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/stsDatabaseUrl
  gatewayDatabaseUrl:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/gatewayDatabaseUrl
  auditDatabaseUrl:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/auditDatabaseUrl
  coordinatorDatabaseUrl:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/coordinatorDatabaseUrl
  apiDbPassword:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/apiDbPassword
  stsDbPassword:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/stsDbPassword
  gatewayDbPassword:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/gatewayDbPassword
  auditDbPassword:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/auditDbPassword
  coordinatorDbPassword:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/coordinatorDbPassword
  authDbPassword:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/authDbPassword
  authDatabaseUrl:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/authDatabaseUrl
  caracalAuthSecret:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/caracalAuthSecret
  redisUrl:
    file: ${CARACAL_SECRETS_DIR:-./secrets}/redisUrl

services:
  replayVolumeInit:
    image: busybox:1.37.0@sha256:9532d8c39891ca2ecde4d30d7710e01fb739c87a8b9299685c63704296b16028
    restart: "no"
    network_mode: none
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
    read_only: true
    command:
      - sh
      - -c
      - |
        set -eu
        for dir in /state/sts /state/gateway; do
          mkdir -p "$$dir/audit-replay"
          chmod 0750 "$$dir" "$$dir/audit-replay"
          chown -R 65532:65532 "$$dir"
        done
    volumes:
      - stsReplay:/state/sts
      - gatewayReplay:/state/gateway

  postgres:
    image: ${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}caracal-postgres:v${CARACAL_VERSION}
    restart: unless-stopped
    init: true
    stop_grace_period: 30s
    logging: *defaultLogging
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_READ_SEARCH
      - FOWNER
      - SETGID
      - SETUID
    networks:
      - caracalData
    secrets:
      - source: postgresPassword
        target: postgresPassword
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-caracal}
      POSTGRES_PASSWORD_FILE: /run/secrets/postgresPassword
      POSTGRES_DB: ${POSTGRES_DB:-caracal}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgresData:/var/lib/postgresql
    tmpfs:
      - /tmp:size=64m,mode=1777
      - /var/run/postgresql:size=16m,mode=0755
    shm_size: "256m"
    healthcheck:
      # TCP probe: initdb's temporary server only listens on the unix socket, so
      # health reflects the real postmaster that dependents reach over TCP.
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB -h 127.0.0.1"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 15s
      start_interval: 250ms

  redis:
    image: ${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}caracal-redis:v${CARACAL_VERSION}
    restart: unless-stopped
    init: true
    stop_grace_period: 20s
    logging: *defaultLogging
    <<: *defaultSecurity
    networks:
      - caracalData
    secrets:
      - source: redisPassword
        target: redisPassword
    environment:
      REDIS_PASSWORD_FILE: /run/secrets/redisPassword
      REDIS_MAXMEMORY: ${REDIS_MAXMEMORY:-512mb}
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redisData:/data
      - type: tmpfs
        target: /run/caracal
        tmpfs:
          mode: 1023
    healthcheck:
      test: ["CMD-SHELL", "REDISCLI_AUTH=$$(cat /run/secrets/redisPassword) redis-cli --no-auth-warning PING | grep -q PONG && test -f /run/caracal/provisioned"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
      start_interval: 250ms

  dbMigrate:
    image: ${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}caracal-postgres:v${CARACAL_VERSION}
    restart: "no"
    init: true
    logging: *defaultLogging
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    networks:
      - caracalData
    depends_on:
      postgres:
        condition: service_healthy
    secrets:
      - source: postgresPassword
        target: postgresPassword
      - source: apiDbPassword
        target: apiDbPassword
      - source: stsDbPassword
        target: stsDbPassword
      - source: gatewayDbPassword
        target: gatewayDbPassword
      - source: auditDbPassword
        target: auditDbPassword
      - source: coordinatorDbPassword
        target: coordinatorDbPassword
      - source: authDbPassword
        target: authDbPassword
    environment:
      PGHOST: postgres
      PGPORT: "5432"
      PGUSER: ${POSTGRES_USER:-caracal}
      PGPASSWORD_FILE: /run/secrets/postgresPassword
      PGDATABASE: ${POSTGRES_DB:-caracal}
      # Internal isolated network with the bundled Postgres (no server TLS).
      # External/managed databases must set CARACAL_DB_SSLMODE to require or verify-full.
      PGSSLMODE: ${CARACAL_DB_SSLMODE:-disable}
      CARACAL_API_DB_PASSWORD_FILE: /run/secrets/apiDbPassword
      CARACAL_STS_DB_PASSWORD_FILE: /run/secrets/stsDbPassword
      CARACAL_GATEWAY_DB_PASSWORD_FILE: /run/secrets/gatewayDbPassword
      CARACAL_AUDIT_DB_PASSWORD_FILE: /run/secrets/auditDbPassword
      CARACAL_COORDINATOR_DB_PASSWORD_FILE: /run/secrets/coordinatorDbPassword
      CARACAL_AUTH_DB_PASSWORD_FILE: /run/secrets/authDbPassword
    entrypoint: ["/bin/sh", "/usr/local/bin/caracal-migrate"]

  sts:
    <<: [*appService, *appResources]
    image: ${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}caracal-go:v${CARACAL_VERSION}
    command: ["/usr/local/bin/sts"]
    depends_on:
      replayVolumeInit:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      dbMigrate:
        condition: service_completed_successfully
    secrets:
      - source: stsDatabaseUrl
        target: databaseUrl
      - source: redisUrl
        target: redisUrl
      - source: secretStoreKek
        target: secretStoreKek
      - source: auditHmacKey
        target: auditHmacKey
      - source: streamsHmacKey
        target: streamsHmacKey
      - source: gatewayStsHmacKey
        target: gatewayStsHmacKey
      - source: metricsBearer
        target: metricsBearer
    environment:
      <<: *secretBackendEnv
      CARACAL_MODE: ${CARACAL_MODE:-stable}
      DATABASE_URL_FILE: /run/secrets/databaseUrl
      REDIS_URL_FILE: /run/secrets/redisUrl
      SECRET_STORE_KEK_FILE: /run/secrets/secretStoreKek
      AUDIT_HMAC_KEY_FILE: /run/secrets/auditHmacKey
      STREAMS_HMAC_KEY_FILE: /run/secrets/streamsHmacKey
      GATEWAY_STS_HMAC_KEY_FILE: /run/secrets/gatewayStsHmacKey
      ISSUER_URL: ${CARACAL_STS_ISSUER_URL:-http://localhost:8080}
      STS_ADMIN_TOKEN: ${STS_ADMIN_TOKEN:-}
      METRICS_BEARER_FILE: /run/secrets/metricsBearer
      PORT: 8080
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - stsReplay:/var/lib/caracal
    healthcheck:
      test: ["CMD", "/healthcheck"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 15s
      start_interval: 250ms

  api:
    <<: [*appService, *appResources]
    image: ${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}caracal-node:v${CARACAL_VERSION}
    command: ["/app/api/dist/main.js"]
    depends_on:
      sts:
        condition: service_healthy
      dbMigrate:
        condition: service_completed_successfully
    secrets:
      - source: apiDatabaseUrl
        target: databaseUrl
      - source: redisUrl
        target: redisUrl
      - source: secretStoreKek
        target: secretStoreKek
      - source: streamsHmacKey
        target: streamsHmacKey
      - source: gatewayStsHmacKey
        target: gatewayStsHmacKey
      - source: caracalAdminToken
        target: caracalAdminToken
      - source: metricsBearer
        target: metricsBearer
      - source: auditHmacKey
        target: auditHmacKey
    environment:
      <<: *secretBackendEnv
      CARACAL_MODE: ${CARACAL_MODE:-stable}
      DATABASE_URL_FILE: /run/secrets/databaseUrl
      REDIS_URL_FILE: /run/secrets/redisUrl
      STS_URL: http://sts:8080
      CARACAL_GATEWAY_URL: http://gateway:8081
      CARACAL_COORDINATOR_URL: http://coordinator:4000
      # Service-to-service traffic stays on the compose-internal bridge network and is
      # authenticated with HMAC keys and tokens, so plain http between containers is safe.
      CARACAL_ALLOW_INSECURE_CONFIG_URLS: "true"
      SECRET_STORE_KEK_FILE: /run/secrets/secretStoreKek
      STREAMS_HMAC_KEY_FILE: /run/secrets/streamsHmacKey
      GATEWAY_STS_HMAC_KEY_FILE: /run/secrets/gatewayStsHmacKey
      CARACAL_ADMIN_TOKEN_FILE: /run/secrets/caracalAdminToken
      METRICS_BEARER_FILE: /run/secrets/metricsBearer
      AUDIT_HMAC_KEY_FILE: /run/secrets/auditHmacKey
      CARACAL_CONTROL_ENABLED: "true"
      CONTROL_GATE_FILE: /run/caracal-control/enabled
      STS_JWKS_URL: http://sts:8080/.well-known/jwks.json
      STS_ISSUER_URL: ${CARACAL_STS_ISSUER_URL:-http://localhost:8080}
      CONTROL_AUDIENCE: ${CONTROL_AUDIENCE:-caracal-control}
      CONTROL_API_TOKEN_FILE: /run/secrets/caracalAdminToken
      # Caracal Operator. AI providers are added in the console's AI settings: the key is
      # sealed into the caracal.sys system zone and the governed resource points at the
      # OpenAI-compatible endpoint the operator enters, so no model traffic leaves ungoverned
      # and no key ever lives in an env file. The Operator always self-governs: the control
      # plane provisions that zone and its rotating least-privilege identities autonomously.
      API_OPERATOR_ENABLED: ${API_OPERATOR_ENABLED:-true}
      PORT: 3000
      HOST: 0.0.0.0
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - "${CARACAL_CONTROL_STATE_DIR:-./control}:/run/caracal-control:ro"
    healthcheck:
      test: ["CMD", "/healthcheck"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 20s
      start_interval: 250ms

  gateway:
    <<: *appService
    image: ${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}caracal-go:v${CARACAL_VERSION}
    command: ["/usr/local/bin/gateway"]
    deploy:
      resources:
        limits:
          cpus: "${CARACAL_GATEWAY_CPU_LIMIT:-2.0}"
          memory: "${CARACAL_GATEWAY_MEM_LIMIT:-1G}"
          pids: 256
        reservations:
          cpus: "${CARACAL_APP_CPU_RESERVE:-0.1}"
          memory: "${CARACAL_APP_MEM_RESERVE:-128M}"
    depends_on:
      replayVolumeInit:
        condition: service_completed_successfully
      sts:
        condition: service_healthy
      api:
        condition: service_healthy
      dbMigrate:
        condition: service_completed_successfully
    secrets:
      - source: gatewayDatabaseUrl
        target: databaseUrl
      - source: redisUrl
        target: redisUrl
      - source: streamsHmacKey
        target: streamsHmacKey
      - source: auditHmacKey
        target: auditHmacKey
      - source: gatewayStsHmacKey
        target: gatewayStsHmacKey
      - source: caracalAdminToken
        target: caracalAdminToken
      - source: metricsBearer
        target: metricsBearer
    environment:
      CARACAL_MODE: ${CARACAL_MODE:-stable}
      DATABASE_URL_FILE: /run/secrets/databaseUrl
      REDIS_URL_FILE: /run/secrets/redisUrl
      STS_URL: http://sts:8080
      STREAMS_HMAC_KEY_FILE: /run/secrets/streamsHmacKey
      AUDIT_HMAC_KEY_FILE: /run/secrets/auditHmacKey
      GATEWAY_STS_HMAC_KEY_FILE: /run/secrets/gatewayStsHmacKey
      CARACAL_ADMIN_TOKEN_FILE: /run/secrets/caracalAdminToken
      METRICS_BEARER_FILE: /run/secrets/metricsBearer
      UPSTREAM_HOST_ALLOWLIST: ${UPSTREAM_HOST_ALLOWLIST:-}
      PORT: 8081
    ports:
      - "127.0.0.1:8081:8081"
    volumes:
      - gatewayReplay:/var/lib/caracal
    healthcheck:
      test: ["CMD", "/healthcheck"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 15s
      start_interval: 250ms

  audit:
    <<: [*appService, *appResources]
    image: ${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}caracal-go:v${CARACAL_VERSION}
    command: ["/usr/local/bin/audit"]
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      dbMigrate:
        condition: service_completed_successfully
    secrets:
      - source: auditDatabaseUrl
        target: databaseUrl
      - source: redisUrl
        target: redisUrl
      - source: auditHmacKey
        target: auditHmacKey
      - source: metricsBearer
        target: metricsBearer
    environment:
      CARACAL_MODE: ${CARACAL_MODE:-stable}
      DATABASE_URL_FILE: /run/secrets/databaseUrl
      REDIS_URL_FILE: /run/secrets/redisUrl
      AUDIT_HMAC_KEY_FILE: /run/secrets/auditHmacKey
      AUDIT_ADMIN_TOKEN: ${AUDIT_ADMIN_TOKEN:-}
      METRICS_BEARER_FILE: /run/secrets/metricsBearer
      AUDIT_EXPORT_S3_ENDPOINT: ${AUDIT_EXPORT_S3_ENDPOINT:-}
      AUDIT_EXPORT_S3_BUCKET: ${AUDIT_EXPORT_S3_BUCKET:-}
      AUDIT_EXPORT_S3_REGION: ${AUDIT_EXPORT_S3_REGION:-us-east-1}
      PORT: "9090"
    ports:
      - "127.0.0.1:9090:9090"
    healthcheck:
      test: ["CMD", "/healthcheck"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 15s
      start_interval: 250ms

  coordinator:
    <<: [*appService, *appResources]
    image: ${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}caracal-node:v${CARACAL_VERSION}
    command: ["/app/coordinator/dist/main.js"]
    depends_on:
      sts:
        condition: service_healthy
      dbMigrate:
        condition: service_completed_successfully
    secrets:
      - source: coordinatorDatabaseUrl
        target: databaseUrl
      - source: redisUrl
        target: redisUrl
      - source: streamsHmacKey
        target: streamsHmacKey
      - source: caracalCoordinatorToken
        target: caracalCoordinatorToken
      - source: metricsBearer
        target: metricsBearer
    environment:
      CARACAL_MODE: ${CARACAL_MODE:-stable}
      DATABASE_URL_FILE: /run/secrets/databaseUrl
      REDIS_URL_FILE: /run/secrets/redisUrl
      STS_URL: http://sts:8080
      ISSUER_URL: ${CARACAL_STS_ISSUER_URL:-http://localhost:8080}
      # Service-to-service traffic stays on the compose-internal bridge network and is
      # authenticated with HMAC keys and tokens, so plain http between containers is safe.
      CARACAL_ALLOW_INSECURE_CONFIG_URLS: "true"
      STREAMS_HMAC_KEY_FILE: /run/secrets/streamsHmacKey
      CARACAL_COORDINATOR_TOKEN_FILE: /run/secrets/caracalCoordinatorToken
      AGENT_COORDINATOR_SCOPE: agent:lifecycle
      METRICS_BEARER_FILE: /run/secrets/metricsBearer
      PORT: 4000
      HOST: 0.0.0.0
    ports:
      - "127.0.0.1:4000:4000"
    healthcheck:
      test: ["CMD", "/healthcheck"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 20s
      start_interval: 250ms

  web:
    <<: [*appService, *appResources]
    image: ${CARACAL_REGISTRY:-ghcr.io/garudex-labs/}caracal-web:v${CARACAL_VERSION}
    command: ["/app/auth/dist/server.js"]
    depends_on:
      postgres:
        condition: service_healthy
      api:
        condition: service_healthy
      coordinator:
        condition: service_healthy
    secrets:
      - source: authDatabaseUrl
        target: authDatabaseUrl
      - source: caracalAuthSecret
        target: caracalAuthSecret
      - source: caracalAdminToken
        target: caracalAdminToken
      - source: caracalCoordinatorToken
        target: caracalCoordinatorToken
    environment:
      NODE_ENV: production
      CARACAL_AUTH_DATABASE_URL_FILE: /run/secrets/authDatabaseUrl
      CARACAL_AUTH_DATABASE_SSL: disable
      CARACAL_AUTH_SECRET_FILE: /run/secrets/caracalAuthSecret
      CARACAL_AUTH_AUTO_MIGRATE: "true"
      CARACAL_AUTH_URL: ${CARACAL_WEB_URL:-http://localhost:3001}
      CARACAL_VERSION: v${CARACAL_VERSION}
      CARACAL_WEB_ORIGIN: ${CARACAL_WEB_ORIGIN:-}
      CARACAL_AUTH_TRUST_PROXY: ${CARACAL_AUTH_TRUST_PROXY:-}
      CARACAL_OPERATOR_ALLOWLIST_FILE: /run/caracalHostSecrets/operatorAllowlist.json
      CARACAL_OPEN_REGISTRATION: ${CARACAL_OPEN_REGISTRATION:-}
      CARACAL_PASSWORD_SIGNUP: ${CARACAL_PASSWORD_SIGNUP:-}
      CARACAL_SMTP_URL: ${CARACAL_SMTP_URL:-}
      CARACAL_SMTP_FROM: ${CARACAL_SMTP_FROM:-}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID:-}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET:-}
      CARACAL_API_URL: http://api:3000
      CARACAL_COORDINATOR_URL: http://coordinator:4000
      CARACAL_ADMIN_TOKEN_FILE: /run/secrets/caracalAdminToken
      CARACAL_COORDINATOR_TOKEN_FILE: /run/secrets/caracalCoordinatorToken
      LOG_LEVEL: ${LOG_LEVEL:-info}
      PORT: 3002
      HOST: 0.0.0.0
    # The allowlist file is written by `caracal allowlist` on the host, often after the stack is
    # already up, so it arrives through a directory bind rather than a compose secret, whose
    # file source must exist at start. Only the allowlist subdirectory is mounted, read-only,
    # so the container never sees sibling host secrets and cannot change admission policy.
    volumes:
      - ${CARACAL_SECRETS_DIR:-./secrets}/allowlist:/run/caracalHostSecrets:ro
    ports:
      - "127.0.0.1:3001:3002"
    healthcheck:
      test: ["CMD", "/healthcheck"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 20s
      start_interval: 250ms

volumes:
  postgresData:
  redisData:
  stsReplay:
  gatewayReplay:
