# Copyright (C) 2026 Garudex Labs.  All Rights Reserved.
# Caracal, a product of Garudex Labs
#
# Weekly Trivy scan of published container images for Critical and High CVEs.

name: Container image scan

on:
  schedule:
    - cron: '51 6 * * 1'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Image tag to scan; empty resolves the latest release.'
        required: false
        default: ''
        type: string

permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: garudex-labs

jobs:
  resolveTag:
    name: Resolve image tag
    if: github.repository == 'Garudex-Labs/caracal'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
      images: ${{ steps.images.outputs.images }}
    steps:
      - id: tag
        env:
          GH_TOKEN: ${{ github.token }}
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          if [ -n "${INPUT_TAG}" ]; then
            tag="${INPUT_TAG}"
          else
            tag="$(gh release view --repo "${GITHUB_REPOSITORY}" --json tagName --jq '.tagName' 2>/dev/null || true)"
          fi
          if [ -n "${tag}" ] && ! printf '%s' "${tag}" | grep -Eq '^v[0-9]+\.[0-9]+\.[0-9]+(-rc\.[0-9]+)?$'; then
            echo "::error::Tag ${tag} is not a Caracal release tag."
            exit 1
          fi
          echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          if [ -z "${tag}" ]; then
            echo "::notice::No release tag resolved; image scan will be skipped."
          else
            echo "::notice::Scanning images at tag ${tag}."
          fi

      # The image list must come from the resolved release's own record on its tag, not
      # the default branch or the current release.config.json: main only keeps the
      # current train's records, the container topology changes between releases, and
      # images only exist in the registry under the tags their release actually published.
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        if: steps.tag.outputs.tag != ''
        with:
          ref: refs/tags/${{ steps.tag.outputs.tag }}
          persist-credentials: false

      - id: images
        env:
          RELEASE_TAG: ${{ steps.tag.outputs.tag }}
        run: |
          set -euo pipefail
          if [ -z "${RELEASE_TAG}" ]; then
            echo "images=[]" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          manifest="releases/${RELEASE_TAG}/manifest.json"
          if [ ! -f "${manifest}" ]; then
            echo "::error::No release record at ${manifest} on tag ${RELEASE_TAG}; cannot derive the image list."
            exit 1
          fi
          echo "images=$(jq -c '[(.containers | keys[]), (if .runtimeImage then "runtime" else empty end)]' "${manifest}")" >> "$GITHUB_OUTPUT"

  scan:
    name: Scan ${{ matrix.image }}
    needs: resolveTag
    if: github.repository == 'Garudex-Labs/caracal' && needs.resolveTag.outputs.tag != ''
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        image: ${{ fromJSON(needs.resolveTag.outputs.images) }}
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Log in to registry
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Scan image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/caracal-${{ matrix.image }}:${{ needs.resolveTag.outputs.tag }}
          format: sarif
          output: trivy-${{ matrix.image }}.sarif
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          trivyignores: .trivyignore.yaml
          exit-code: '0'

      - name: Upload to code scanning
        uses: github/codeql-action/upload-sarif@7188fc363630916deb702c7fdcf4e481b751f97a # v4.37.1
        with:
          sarif_file: trivy-${{ matrix.image }}.sarif
          category: trivy-${{ matrix.image }}
