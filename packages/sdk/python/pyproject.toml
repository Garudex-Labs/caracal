# Copyright (C) 2026 Garudex Labs.  All Rights Reserved.
# Caracal, a product of Garudex Labs
#
# Python package metadata for the Caracal SDK.

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "caracalai-sdk"
version = "0.2.0rc5"
description = "Caracal SDK for Python: short-lived, policy-approved authority for AI agents - Sessions, Delegation, and transport hooks"
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "caracalai-oauth==0.2.0rc5",
  "httpx>=0.28.1",
  "idna>=3.18",
]

[tool.hatch.build.targets.wheel]
packages = ["caracalai"]
